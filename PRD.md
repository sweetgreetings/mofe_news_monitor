# 재정경제부 언론 모니터링 자동화 서비스 기획서 (PRD)

- 작성자: [홍정희]
- 작성일: (2026.7.22.)

> **이 문서의 역할**: 앱에 어떤 기능이 있고 각 기능이 지켜야 할 규칙이 무엇인지 적은 기획서이며, 코드·문서가 인용하는 번호 「기능N 규칙M」의 출처다. 번호는 바꾸지 않는다 — 없앤 규칙도 번호 자리는 남긴다.
> - 지금 동작의 세부 규칙(파일·함수·화면 수치)은 [CLAUDE.md](CLAUDE.md)가 진실이다. 이 문서와 어긋나면 CLAUDE.md가 맞다.
> - 변경 경위·실측·기각안은 [HISTORY.md](HISTORY.md), 수시 모니터링 설계 근거는 [ADHOC_DESIGN.md](ADHOC_DESIGN.md).
> - 날짜 태그와 경위가 붙어 있던 정리 전 원문은 [archive/PRD_2026-09-22_정리전_전체본.md](archive/PRD_2026-09-22_정리전_전체본.md).

---

## 1. 추진 배경 (문제 정의)

언론 모니터링 담당자는 네이버 뉴스에 게재되는 재정경제부 관련 기사를 하루에도 여러 차례 직접 검색해 확인하고, 언론사·제목·URL을 메모장에 일일이 옮겨 붙여 정리해 왔다. 반복 수작업이 많고, 기사를 놓치거나 정리에 시간이 많이 든다.

---

## 2. 현행 방식과 한계

- 현행 방식: 필요한 시점마다 네이버 뉴스에서 직접 검색하고, 기사를 하나씩 열어 언론사·제목·URL을 메모장에 옮겨 적는다. 소제목별 분류·요약도 담당자가 직접 판단한다.
- 한계:
  - 검색·정리에 반복적인 시간이 든다.
  - 사람이 직접 확인하다 보니 기사를 놓치거나 중복 정리할 위험이 있다.
  - 언론사 우선순위·시간대별 확인을 매번 사람이 판단해 일관성이 떨어진다.
  - 정리 결과가 개인 메모장에만 남아 형식이 통일되지 않고 재사용·검색이 어렵다.

---

## 3. 목표와 기대효과 (성공 기준)

- 기사 스크랩·정리에 드는 수작업 시간을 대폭 줄인다(별도 수치 측정은 하지 않는다).
- 담당자가 설정한 회차마다 등록된 검색어가 들어간 기사를 자동으로 모은다. 단 네이버 API의 키워드당 최신 1000건 상한 때문에 전수는 보장되지 않는다(기능1 규칙2).
- 네이버 요약(API description)과 소제목별 AI 요약(3문장·250자 이내)으로 담당자의 내용 파악 시간을 줄인다.

---

## 4. 사용자와 이용 흐름

- 사용자: 언론 모니터링 담당자 한 명. 로그인 없이 로컬(`localhost`)에서 쓴다.
- 실행 방식: 앱(`python3 main.py`, macOS에선 LaunchAgent 자동 실행)을 켜 두면 동작한다. 컴퓨터가 잠들어 있던 동안은 수집하지 못하고, 깨어나거나 앱을 켜면 오늘 놓친 회차를 오래된 것부터 전부 보충한다(기능1 규칙9). 앱을 켜면 브라우저가 홈(기능3)을 연다.
- 이용 흐름: 홈에서 아래 갈래로 나뉜다. 회차 시각·개수는 설정 값이라 여기 적지 않는다.
  - **전체 기사** — 지금까지 들어온 기사를 검색어에 걸린 그대로 본다. 저장·분류 없음(기능4). 흐름의 단계가 아니다.
  - **정기 모니터링**: `검색어` → `초안` →(회차 마감, 자동) `확정본` → `정기 보관함`. 담당자가 누를 버튼은 발송뿐이다(기능1·5·8, 기능2 규칙6).
  - **수시 모니터링**: `새 수집` →(수집) `원본` →(담당자가 골라 보냄) `확정본` → `수시 보관함`(기능9).
  - **라벨 보관함** — 두 흐름을 가로질러 담당자가 라벨을 붙인 기사만 모아 본다(기능10).
  - 공통: 설정(검색어·언론사·수집 시간) → 수집 → 소제목 AI 분류 → 담당자가 화면에서 확인·정리 → 복사·다운로드·발송.

---

## 5. 주요 기능

**Must**

1) 정기 모니터링 (기사 자동 스크랩)
- 설명: 네이버 뉴스 검색 API로 설정된 검색어의 기사를 회차마다 자동 수집해 보고서 텍스트 형식으로 만든다. 수시 모니터링(기능9)과 짝을 이루는 이름이며, 분류 축은 **일자 › 시간대**, 지난 회차는 **정기 보관함**(기능2 규칙6)에 쌓인다. 화면 이름은 CLAUDE.md "화면 용어"(`초안`·`확정본`·`정기 보관함`·`전체 기사`)를 따른다.
- AI가 꼭 지켜야 할 규칙:
  1. 하루 수집 회차는 설정 화면에서 정하는 **회차 시간창**(`{시작, 끝}`, 1~10개)이다. 회차 시각·개수는 담당자가 바꾸는 값이다(규칙20).
  2. 네이버 뉴스 검색 API로 설정의 **검색어 그룹**(규칙15) 중 `초안·확정본`을 켠 그룹으로 검색한다. 그룹 사이는 OR, 그룹 안은 그룹별 OR/AND.
     - 각 회차는 그 시간창 `(시작, 끝]`에 게시된 **당일(KST)** 기사를 모은다. 네이버가 마감 순간 아직 색인하지 않은 기사를 다음 회차가 줍도록 실제로는 시작 60분 앞(당일 0시가 바닥)부터 훑고, 앞 회차에 이미 실린 기사는 URL로 뺀다(늦게 들어온 기사로 표시).
     - 앱이 거는 건수 상한은 없다. 단 네이버 API는 **키워드당 최신 1000건**까지만 주고 기간 파라미터가 없어, 상한에 닿으면 그보다 오래된 기사는 조용히 잘린다(경고 로그만 남긴다).
     - 게시일자는 화면·출력에 쓰지 않고 수집 판단 기준으로만 쓴다(화면의 `HH:MM 게시`는 화면 전용).
  3. 언론사 순서: 방송사(KBS, MBC) → 고정 주요 언론사 목록 → 나머지(버리지 않고 뒤로). 설정에서 언론사를 하나 이상 고르면(규칙16) 고른 곳만, 고른 순서대로. 언론사 판별은 네이버 링크의 oid로 확정하고 없을 때만 도메인으로 추정한다. 매일경제인데 oid로 확정되지 않은 기사(매경이코노미일 수 있음)는 화면에서만 언론사명을 빨간 글자로 표시한다(저장값·복사 텍스트는 그대로).
  4. 같은 기사가 여러 언론사에 실려도 제목이 다르면 모두 둔다. **제목이 완전히 같은 기사는 하나만** 남긴다(정렬 후 첫 기사).
  5. 출력 형식은 다음과 같고, 게시일자는 쓰지 않는다. 기사마다 URL 다음에 빈 줄 하나.
     ```
     ㅇ (언론사) 기사제목
     URL
     ```
     첫 줄은 설정 「기사 제목 형식」에서 `{outlet}`·`{title}` 자리표시자로 바꿀 수 있다(기본 `ㅇ ({outlet}) {title}`). URL 줄과 게시일자 미표시는 바꿀 수 없다.
  6. 기사 본문 전체를 크롤링하지 않고 네이버 요약(description)만 저장한다. 요약·제목 속 형광펜 단어를 칠한다 — 형광펜 단어는 검색어와 별개 설정(최대 20개), 단어마다 파스텔 5색이 순환하고 칩을 눌러 색을 바꾼다. 칠하는 화면은 확정본·초안·전체 기사 셋이고(편집도 그 세 화면 좌측 하단 🖍️에서만 한다), 수시 화면에는 칠하지 않는다.
  7. 해당 회차에 수집된 기사가 없으면 화면에 「💤」를 표시한다.
  8. 네트워크 오류 등으로 수집이 실패하면 5분 간격으로 최대 3회 자동 재시도한다. 키워드 하나라도 검색에 실패하면 빈 결과로 바꿔치지 않고 회차 전체를 재시도한다.
  9. 앱이 꺼져 있거나 컴퓨터가 잠들어 지나간 회차가 있으면 **오늘 놓친 회차를 전부, 오래된 것부터** 실행한다. 회차 하나가 실패해도 나머지는 계속하고, 같은 회차는 하루 3번까지만 시도한다. 시간창은 설정 값 그대로 쓴다. 여러 회차를 한꺼번에 채워도 자동발송(기능8)은 가장 최근 회차만 한다. 어제 이전 회차는 채우지 않는다.
  10. 화면의 소제목별 목록을 클립보드로 복사하거나 `.txt`로 내려받을 수 있다(엑셀은 기능11). 소제목 하나만 복사하는 📋도 있다.
  11. 화면·내보내기 맨 위 헤더는 `언론 모니터링 N시 기준`(예: `9시 기준`, `9시 30분 기준` — 시 앞자리 0 생략, 정각이면 분 생략)이다. 파일명은 `날짜_종류_구분` 형식(`2026-09-17_언론모니터링_14-00기준.txt`, 초안은 끝에 `_초안`, 기능11 규칙7).
  12. **정기는 사진·인사 기사를 수집에서 빼지 않는다**(제외 설정 없음). 사진 기사 판정(제목 말머리 `[포토]` 계열 표식, 네이버 원문 판정, 요약·제목 모양 추정)은 화면에서 행을 흐리게 하고 「📷 사진 추정」 칩과 「사진 추정 모아 보기」를 보여주는 데만 쓴다. `[속보]` 기사는 사진으로 판정하지 않는다. 새 추정 신호는 전수 실측으로 오탐 0을 확인한 뒤에만 더한다.
  13. 화면에서 기사 제목을 누르면 저장된 요약이 펼쳐진다.
  14. 기사 URL(원문보기)을 누르면 원문 기사를 새 탭으로 연다.
  15. 설정 「검색어」(`/keywords`)에서 **검색어 그룹**을 코드 수정 없이 편집한다 — 그룹 최대 8개, 그룹당 키워드 최대 10개, 그룹 안 OR/AND는 그룹별. 모든 그룹이 이름·키워드 수정·삭제 가능하다. 그룹마다 목적지 스위치 둘 `전체 기사`·`초안·확정본`이 서로 독립이고, 둘 다 끄면 그룹 꺼짐. 저장을 막는 규칙은 「`초안·확정본`을 켠 그룹 ≥1」 하나뿐이다. 키워드를 누르면 지우지 않고 잠시 빼고(개별 on/off), 두 번 누르면 글자를 고친다.
  16. 설정 「언론사 선택 및 순서 지정」(`/outlets`) 한 화면에서, 분류된 언론사 목록 중 원하는 곳을 체크박스로 고르고 버튼(`5↑ ↑ ↓ 5↓`)으로 순서를 바꾼다. 아무것도 안 고르면 규칙3의 기본 순서를 따른다.
  17. 설정은 메뉴 화면(`/`)에서 시작해 하위 화면으로 들어간다. 묶음은 정기(검색어·수집시간대 │ 텍스트 표출 형식: 기사 제목·소제목 │ 발송·알림: 자동발송·[단독]·[속보] 기사 알림) → 수시(텍스트 표출 형식: 기사 제목·소제목) → 정기, 수시 공통(언론사 선택 및 순서 지정) → 받는 사람 지정(텔레그램·이메일·발송 기록) → 홈(워드클라우드 제외어) → 연동(네이버 뉴스 API·LLM(AI)·텔레그램 발송 계정·이메일 발송 계정). 형광펜 설정 화면은 없앴다 — 편집은 확정본·초안·전체 기사의 🖍️에서 한다. 세부는 CLAUDE.md "설정 메뉴".
  18. 화면 폰트는 Pretendard를 우선 쓰고, 없으면 Noto Sans KR, 그래도 없으면 시스템 기본 글꼴.
  19. 담당자는 보고에서 뺄 기사를 🗑️로 숨긴다(큐레이션). 원본 수집 데이터는 지우지 않고 숨긴 기사 URL 기록만 남겨, 화면·복사·내보내기·워드클라우드에서 뺀다. 판정은 URL 하나로만 한다(제목·언론사 대조 없음). 숨김은 7일 유지되고 그 안에선 확정본·초안 왼쪽 아래 🗑 팝오버나 `휴지통`(`/hidden`) 화면에서 언제든 되살린다.
  20. 설정 「수집시간대」(`/schedule`)에서 회차마다 시작~끝 시간창을 `HH:MM`으로 추가·삭제한다. 분은 `00`/`30`만, 회차 1~10개. 끝 시각이 회차의 정체성(헤더·파일명), 시작 시각은 수집 하한이다. **회차 그룹**(최대 5, 예: 주중/주말)마다 `사용` 토글과 요일을 두며, 켜진 그룹이 1개면 요일과 무관하게 그것, 2개 이상이면 오늘 요일을 가진 그룹, 없으면 첫 켜진 그룹을 쓴다. 같은 요일을 두 그룹이 가질 수 없다. 하루 마지막 회차만 수집을 30분 늦춘다. 초안에서 진행 중인 회차를 설정을 건드리지 않고 오늘만 끊을 수 있다(✂, CLAUDE.md "오늘만 회차 끊기").
  21. 확정본(최신 회차)·초안에서 담당자가 기사를 정리한다:
      - 기사 ↑↓는 **같은 소제목 안에서만** 순서를 바꾼다(소제목 경계를 넘지 않는다). 담당자의 ↑↓가 언론사 순서를 항상 이긴다.
      - 다른 소제목으로 옮기는 길은 기사마다 「다른 소제목」 드롭다운과, 체크박스로 여러 기사를 골라 쓰는 하단 일괄이동 바(옮기기 · ↑↓ · 🗑 · AI 기사 나누기)다. 담당자의 배정이 자동 분류보다 우선한다.
      - 「+ 새 소제목」으로 담당자가 직접 소제목을 만든다 — 빈 칸으로 시작하고, 목록 맨 아래에 오며, 하늘색 점선으로 AI 소제목과 구분하고, 자정에 비운다. 같은 화면 안에서 표시 이름이 겹치면 저장을 막는다.
      - 이 도구들은 화면 전용이라 복사·내보내기 텍스트에 나타나지 않는다. 정기 보관함은 읽기 전용이라 없다.
  22. (없앰 — 경위는 archive/PRD_2026-09-22_정리전_전체본.md)
  23. 큐레이션 세부(소제목 ▲▼ 순서·「기타」 맨 뒤 고정, 미니 목차, 「보기 순서」 화면 전용 정렬, 원문 다시 불러오기·기사제목 직접 수정, 형광펜 팝오버, 키워드 작성 메모, 방금 손댄 행 세이지 표시, Shift+클릭 범위 선택 등)는 확정본·초안 공통이다. 세부는 CLAUDE.md "정기 모니터링 — 큐레이션".
  24. 화면 배치(툴바는 왼쪽에 내용을 바꾸는 버튼, 오른쪽에 copy·txt·xlsx 글자 버튼과 「보기 순서」, 상단바 `홈 │ 전체 기사 · 초안 · 확정본 · 보관함`)는 CLAUDE.md "출력·발송"·"디자인"을 따른다.

2) 소제목 자동 분류
- 설명: 기사 요약을 분석해 소제목으로 묶고, 소제목별 요약을 만든다.
- AI가 꼭 지켜야 할 규칙:
  1. 소제목은 고정 카테고리가 아니라 회차마다 기사의 **제목과 요약을 함께** 넣어 분석해 새로 만든다. 최대 15개. 담당자가 검토하므로 완벽하지 않아도 되며, 묶음당 평균 2건 이상이라는 밀도 규칙은 AI_RULES.md에 있다.
  2. 소제목 분류는 Claude API(`LLM_MODEL`, 기본 Haiku)가 맡고, 같은 호출에서 소제목 요약도 쓴다. API 키·패키지가 없거나, 기사 수가 2~150건 밖이거나, 호출이 실패하면 예외 없이 규칙 기반 분류로 폴백한다.
      - **호출 시점 제한(비용 관리)**: 확정본은 다시 분류하지 않고 회차 파일에 저장된 소제목을 읽는다. 마감 시 분류는 초안 분류를 재사용하고 새 기사만 기존 소제목에 넣는다. 초안은 회차당 한 번만(기사 5건 이상 도달 시) 자동 분류하고, 그 뒤 새로 들어온 기사는 「📂 소제목 미분류」 칸에 모은다(보고서 텍스트에선 「기타」로 합친다). 분류 결과는 기사 URL 묶음을 키로 캐시한다.
      - 그 뒤 AI 호출은 담당자가 누를 때만: **AI 기사 배정**(초안, 미분류 기사만 기존 소제목에 넣음, 확인창 없음), **AI 모든 기사 재분류**(초안·확정본, 처음부터 다시, 항상 확인창), **AI 기사 나누기**(고른 기사만 새 소제목으로).
      - AI 분류가 실패해 규칙 기반으로 떨어진 회차는 자동발송을 보류하고 확정본에 「⚠️ AI 분류 실패」를 띄운다. 초안은 폴백 경고와 함께 복사·다운로드 전에 한 번 더 묻는다.
  2-1. AI가 만든 소제목에만 화면에 「🤖 AI」 배지를 붙인다. 이름을 바꾸거나 담당자가 만든 소제목엔 안 붙는다. 배지는 **화면 전용**이라 복사·다운로드·발송 텍스트에 들어가지 않는다.
  2-2. 담당자의 큐레이션은 「↩ 되돌리기」로 한 단계씩 되돌린다(왼쪽 아래 떠 있는 버튼, 되돌릴 게 있을 때만). 숨김·소제목 이동·↑↓·이름 변경·AI 재분류·배정·나누기 등이 대상이다. 여러 기사를 한 번에 숨긴 것은 한 걸음으로 되돌아간다. 최대 20단계, 자정 초기화.
  3. 기사 1건은 소제목 1개에만 배정한다. 담당자가 「다른 소제목」·일괄이동으로 옮긴 배정이 자동 분류보다 우선하며, 옮긴 뒤에도 소제목 안 순서를 자유롭게 조정할 수 있다.
  4. (없앰 — 경위는 archive/PRD_2026-09-22_정리전_전체본.md)
  5. 목록 맨 아래에 「💬 AI가 읽은 소제목별 주요 요약」을 두고 소제목별 요약(3문장·250자 이내, 문장 중간에서 자르지 않음)을 나열한다. 화면 전용이다(텔레그램 「요약」 발송은 별도, 기능6).
  6. **정기 보관함**: 지난 회차를 날짜 → 회차로 모아 보는 화면. 보관 기간 **1년**(`RETENTION_DAYS = 365`). 저장 시점의 소제목 구성을 그대로 보여주고(재분류 없음), 소제목 순서는 확정본과 같은 순서를 쓴다. **읽기 전용**(숨김·이동·이름 바꾸기 없음) — 예외는 라벨(기능10 규칙8)과 회차 삭제(trash로 옮김, 되살리기 가능, 오늘 회차와 가장 최근 회차는 못 지움). 최근 2일만 내용까지 그리고 나머지 날짜는 펼칠 때 불러온다. 회차·날짜·전체 단위로 복사·txt·엑셀을 뽑는다.
  7. 소제목 안 기사는 언론사 순서(방송사 → 주요 언론사 → 나머지)로 출발한다. 이 순서는 출발 순서일 뿐이고 담당자의 ↑↓가 항상 이긴다.
  8. 모든 소제목 위에 이름을 표시한다(이름 없이 나열되는 묶음 없음). ✏️로 표시 이름(이름표)을 바꿀 수 있고, 이름 고르기 창이 최근 회차에서 쓴 이름을 보여준다. 이름은 설정 「소제목 형식」(기본 `<{section}>`)을 입혀 화면·요약·목차·복사 텍스트에 표시하며, 담당자가 이름표로 바꾼 이름에 앱이 괄호를 다시 붙이지 않는다. 「📂 소제목 미분류」에는 형식을 입히지 않는다.

3) 홈 (진입 화면)
- 설명: 앱을 켜면 먼저 뜨는 화면(`home.html`). API를 부르지 않고 저장된 파일만 읽는다. 부정 추정 기사·[단독]/[속보] 칩·오늘의 쟁점·정책 단어 추이·워드클라우드·흐름도로 이뤄지며, 세부는 CLAUDE.md "홈".
- AI가 꼭 지켜야 할 규칙:
  1. 머리 한 줄 왼쪽에 작은 로고(파일이 없으면 로고와 세로선을 비운다)를 둔다.
  2. 로고 옆 제목은 `AI 뉴스 모니터링`과 날짜 줄이다.
  3. 워드클라우드: 오늘 모든 회차(URL 중복 제거, 숨긴 기사 제외)의 요약에서 뽑은 단어를 크기로만 차등해 **한 줄**로 보여준다(의미 있는 통계가 아니라 맥락을 훑는 칸이라 언급 횟수는 띄우지 않는다). 요청마다 다시 집계한다. 검색어도 포함하되 회색으로 구분한다. 뜻이 옅은 일상어는 공용 불용어로 거르고, 설정 「워드클라우드 제외어」(최대 15, 칩 편집)에 등록한 단어는 집계에서 뺀다. 수시 기사는 넣지 않는다(기능9 규칙10).
  3-1. **흐름도**: 두 줄 `정기`(검색어 → 초안 → 확정본 → 보관함)와 `수시`(새 수집 → 원본 → 확정본 → 보관함), 맨 아래 `라벨 보관함` 한 줄. 전체 기사는 흐름 단계가 아니라 초안 위 낮은 점선 칸이고 `📌 담아두기` 점선으로 초안에 이어진다. 칸에는 아이콘과 이름만 두고 설명·지금 상태는 말풍선으로 보여준다. 칸마다 건수는 없다.
  4. 홈에서 각 화면으로 가는 길은 흐름도 칸이다(누르면 그 화면으로 간다). 화면끼리는 상단바로 오간다.
  5. 설정은 머리 오른쪽 끝 ⚙ 설정 한 곳에서 들어간다.
  6. 하단에 「문의 📧 sweetgreetings@naver.com」을 표시한다.
  7. 앱을 켜면 브라우저가 이 화면을 연다(4절).
  8. **부정 추정 기사**(머리 아래 남색 카드): 정기 회차 기사 중 제목·요약에 「재경부」·「재정경제부」가 나온 것만(사진 기사 제외) 회차마다 한 번 AI에 보내, **부정으로 보이는 기사의 번호와 근거 한 줄만** 받아 최신순으로 모은다. 네이버를 따로 검색하지 않고, 회차 파일도 건드리지 않는다(결과는 별도 파일). 판정은 숨기기 전 목록으로 하되 **숨긴 기사는 화면에서 뺀다**. 판정 전·0건·키 없음은 각각 다른 안내를 띄운다. **보고서 텍스트로는 나가지 않는다**(화면 전용).
  9. **오늘의 쟁점**: 오늘 회차의 소제목을 건수 순으로 5개까지 막대로 보여준다. 순위에서 「기타」와 일정 나열 기사는 뺀다. 회차마다 이름이 달라진 같은 쟁점은 이름이 충분히 겹치면 **화면에서만** 하나로 합친다(저장값은 안 바뀐다). 건수는 막대 길이로 보이고 %는 쓰지 않는다.
  10. **정책 단어 추이**: 담당자가 고른 단어(최대 8개)가 정기 회차 기사의 제목·요약에 나온 건수를 기간별로 그린다. **앱이 단어를 추천하지 않는다.** 숨긴 기사도 세고(단어가 실제로 얼마나 나왔는지가 이 칸의 질문이다), 홈 카드와 `/trend` 모두 **어제에서 끝난다**(오늘은 아직 회차가 덜 찼다). 홈 카드는 최근 7일 고정에 조작이 없고, 누르면 `/trend`에서 기간·단어를 바꾸고 표·복사·엑셀로 뽑는다.

4) 전체 기사
- 설명: 회차 수집과 별개로, 열 때마다 오늘 0시~지금의 네이버 검색 결과를 있는 그대로 보여주는 화면(`live.html`). 홈 흐름도에서 흐름 단계가 아닌 별도 칸이다(기능3 규칙3-1).
- AI가 꼭 지켜야 할 규칙:
  1. 회차 수집(기능1)과 별개 파이프라인이다 — 요청마다 `전체 기사` 스위치를 켠 검색어 그룹으로 오늘 0시~지금을 검색하며, 회차로 저장하지 않는다. 새로고침은 수동이다.
  2. 언론사 화이트리스트·소제목 분류를 적용하지 않는다. 최신순이고 발행 시각의 「시」로 묶어 접는다(최근 두 시간대만 펼침, 끌 수 있음). 선별은 전부 화면의 필터 바가 맡는다 — 특수조건(`선택 언론사만`·`[단독]`·`[속보]`·`[사설]`·`아직 처리 안 한 기사만`, 모두 「~만 보기」), 제목 검색, 그룹 칩.
  3. **필터 없음이 규칙이다** — 사진 기사 제외·제목 중복 제거를 하지 않고, 숨긴 기사(기능1 규칙19)도 빼지 않고 「🗑️ 숨김」 표시만 한다. 결과를 좁히는 건 켜진 키워드·그룹 OR/AND·당일·같은 URL 1건뿐이다.
  4. 키워드별 증분 캐시로 새로고침마다 오늘 전체를 다시 검색하지 않는다(마지막으로 본 시각과 「지금 − 60분」 중 이른 쪽부터 다시 검색). 검색은 동시 8개. 키워드가 검색에 실패하면 그 키워드 캐시를 건드리지 않고 경고 배너를 띄운다. 오늘 처음 검색하는 키워드가 있으면 뒤에서 검색하며 「기사를 불러오고 있습니다」 화면을 보여준다.
  5. 기사마다 「📌 담아두기」로 당일 한정 목록에 담으면 확정본·초안의 「📌 담아둔 기사」 칸에 나온다(복사·내보내기·워드클라우드에선 제외, 자정에 비움). 그 칸에서 지금 보는 화면의 소제목 드롭다운(`초안에 넣기…`/`확정본에 넣기…`)으로 그 화면에 편입한다. 담당자가 직접 만든 소제목은 목적지 후보가 아니다.
  6. 담아두지도, 확정본에 실리지도 않았지만 진행 중인 회차가 자동으로 고를 것으로 보이는 기사는 「🤖 자동 선별」로 표시한다(근사치).
  7. 🗑️ 숨기기는 기능1 규칙19와 같은 전역 숨김 기록을 쓴다. 숨긴 행은 다시 누르면 되살린다.

5) 초안
- 설명: 진행 중인 회차를 확정본과 같은 파이프라인으로 미리 보여주는 화면(`preview.html`). 정기 흐름의 단계이며, 회차가 마감되면 **자동으로** 확정본이 된다 — 저장·넘기기 버튼이 없고 담당자의 손질은 누르는 즉시 저장된다. 회차 종료 카운트다운·마감 배너, 「+ 수기로 기사 추가」(주소 하나로 📌 칸에 담기), 오늘만 회차 끊기 등 세부는 CLAUDE.md "정기 모니터링 — 초안".
- AI가 꼭 지켜야 할 규칙:
  1. 확정본과 같은 파이프라인(`초안·확정본`을 켠 그룹, 같은 수집 창과 60분 되돌아보기, 앞 회차 기사 제외, 숨김 제외, 언론사 화이트리스트, 제목 중복 제거, 소제목 분류)을 거치되, 저장하지 않고 요청마다 다시 계산한다. 한 번 초안에 보인 기사는 담당자가 숨기거나 조건을 바꾸지 않는 한 그 회차 초안과 확정본에 남는다.
  2. 여기서 한 숨기기·소제목 이동·이름 바꾸기는 확정본과 공유하는 전역 저장소에 기록되어, 회차가 마감돼 확정본이 될 때 그대로 이어진다.
  3. 같은 소제목 안의 순서 조정은 별도 저장소(`preview_order.json`)에 두어 새로고침 뒤에도 유지되고, 회차가 저장될 때 그 순서를 이어받는다.
  4. 네이버 검색은 전체 기사와 같은 키워드별 증분 검색으로 반복 비용을 줄인다(캐시 기준은 진행 중인 회차).
  5. 기능1 규칙21의 큐레이션(다른 소제목·일괄이동·새 소제목)을 확정본과 똑같이 지원한다.
  6. 「📌 담아둔 기사」 칸을 확정본과 똑같이 보여준다. 초안 소제목으로 편입한 기사는 예약 목록(`draft_pending_articles.json`)에 두었다가 회차가 저장될 때 합류시킨다(그 전에도 초안 분류엔 바로 반영).
  7. 같은 회차의 이전 보기에 없던 새로 들어온 기사는 옅은 노란 배경으로 표시한다(브라우저에 저장한 URL 목록과 비교).
  8. 오늘 회차가 모두 끝났으면 안내 화면, 네이버 검색이 실패하면 오류 안내 화면으로 대신한다(둘 다 저장하지 않음).
  9. (없앰 — 경위는 archive/PRD_2026-09-22_정리전_전체본.md)

6) 텔레그램 전송
- 설명: 정기 확정본·수시 확정본을 텔레그램으로 보낸다. 발송 시점은 기능8(정기)·기능9(수시)가 정한다.
- AI가 꼭 지켜야 할 규칙:
  1. 봇 토큰(`TELEGRAM_BOT_TOKEN`)으로 인증한다. 선택 사항이라 없어도 앱은 돈다. 봇 이름은 설정 「텔레그램 발송 계정」(`/telegram-sender`)에서 봇 토큰과 함께 바꾸며, 값은 앱이 아니라 텔레그램에 저장된다.
  2. 정기 확정본의 자동 전송은 기능8(자동발송)을 따른다.
  3. 수동 전송은 확정본의 **(발송)** 버튼 하나로 한다(기능8 규칙1). 초안에는 발송 버튼이 없다.
  4. 메시지 1건당 4096자를 넘으면 줄바꿈 경계에서 나눠 보낸다.
  5. 미설정이거나 전송이 실패해도 예외를 올리지 않고 로그를 남긴다. 재시도하지 않고, 텔레그램 장애가 수집·화면을 막지 않는다. 정기 회차의 채널별 실패는 받는 사람 이름과 함께 회차에 기록해 보관함에 표시한다.
  6. 받는 사람은 설정 「텔레그램 받는 사람」(`/telegram`)에서 이름·chat id로 여러 명(최대 15명) 등록한다. 사람마다 받는 것 여섯 칸 — `[단독]`·`[속보]` 알림(기능12), 정기 `기사`·`요약`, 수시 `기사`·`요약` — 과 알림 시간(하루 종일/업무 시간/직접 지정, 그 밖의 시간엔 소리 없이 보냄)을 정한다. 기사·요약을 둘 다 켜면 한 통에 기사 목록 + 요약을 보내고, 요약이 없거나 AI 분류가 실패한 보고서면 요약만 켠 사람에게도 기사 목록을 보낸다.

7) 이메일 전송
- 설명: 정기 확정본을 이메일로 보낸다. 여러 명이 받을 수 있다. 수시 확정본은 이메일로 보내지 않는다.
- AI가 꼭 지켜야 할 규칙:
  1. 보내는 계정은 설정 「이메일 발송 계정」(`/email-sender`) 또는 `.env`(`EMAIL_*`)의 값을 쓴다(설정 화면 값 우선). 선택 사항이라 없어도 앱은 돈다. 메일 서비스는 네이버·Gmail 둘이고(서버·포트는 서비스가 정함), 비밀번호는 각 서비스의 「앱 비밀번호」를 쓴다. 보내는 주소로 시험 메일을 보낼 수 있다.
  2. 받는 사람은 설정 「이메일 받는 사람」(`/email`)에서 이름·주소로 여러 명(최대 15명) 등록하고, 사람마다 켜고 끈다.
  3. 정기 확정본의 자동 전송은 기능8(자동발송)을 따른다. 받는 사람마다 한 통씩 따로 보낸다(서로 주소가 보이지 않게).
  4. 수동 전송은 확정본의 **(발송)** 버튼 하나로 텔레그램과 함께 한다(기능8 규칙1).
  5. 메일 제목은 본문 첫 줄(`언론 모니터링 N시 기준`)이고, 본문은 URL만 링크로 감싸고 줄바꿈·공백을 유지한 HTML이다.
  6. 미설정이거나 받는 사람이 없거나 전송이 실패해도 예외를 올리지 않고 로그를 남긴다. 일부 주소만 실패해도 나머지에겐 계속 보낸다.

8) 자동발송 (담당자 검토 후 발송)
- 설명: 회차가 수집되면 곧바로 확정본이 되고, 담당자가 검토한 뒤 보내거나, 정한 시간 안에 누르지 않으면 앱이 스스로 보낸다. 담당자에게 남는 수동 조작은 (발송) 하나다.
- AI가 꼭 지켜야 할 규칙:
  1. 회차가 수집되면 곧바로 확정본 화면에 반영된다(별도 확정 단계 없음). 확정본 오른쪽 아래 초록 원형 **(발송)** 버튼을 누르면 텔레그램·이메일로 나간다.
  2. 설정 「자동발송」(`/auto-send`)을 켜 두면, 회차 수집 시각부터 유예시간(1~30분, 기본 5분) 안에 담당자가 (발송)을 누르지 않을 때 앱이 스스로 보낸다. 탭이 닫혀 있어도 스케줄러가 보낸다. 첫 발송 전 확정본엔 빨간 `⏱️ MM:SS 후 자동발송`이 뜬다(표시용, 판단은 서버). 끄면 자동발송도 카운트다운도 없다. AI 분류가 실패한 회차는 자동발송하지 않는다(기능2 규칙2).
  3. 발송이 끝나면 화면을 다시 열 때 서버가 기록한 결과(규칙5)로 바뀐다.
  4. 이미 보낸 회차를 다시 보내면 제목에 「(수정)」을 붙인다.
  5. 누가 보냈는지 구분해 표시한다 — 담당자가 누른 발송은 회색 `✔️ N시 M분 발송 완료`, 앱이 대신 보낸 발송은 `🤖 N시 M분에 자동발송 완료`. 두 번째부터 ` (N회)`를 붙인다.
  6. **발송 기록**(`/send-log`): 앱이 보낸 **모든** 메시지 — 정기·수시 보고서, [단독]·[속보]·몰림 알림, 호출 한도 경고 — 를 **발송 한 번 = 한 줄**로 남기고 사람마다 결과를 적는다. 보내지 않은 것(AI 분류 실패로 건너뜀, 받는 사람 없음)도 한 줄로 남긴다. **읽었는지는 적지 않는다** — 텔레그램은 「도착」, 이메일은 「메일 서버가 받음」까지만 말한다. 실패한 줄에는 이유와 고치는 방법을 함께 적는다. 90일 보관. 새 발송 경로를 만들면 반드시 여기에 기록한다.

9) 수시 모니터링
- 설명: 정기(기능1~8)가 시간대 축이라면, 수시는 담당자가 **사안·검색어·시간창을 직접 지정**해 모으는 별도 흐름이다(상위자 지시·갑작스런 현안). 흐름은 `새 수집` → `원본` → `확정본` → `수시 보관함`, 분류 축은 **사안 › 일자**. 세부는 CLAUDE.md "수시 모니터링", 설계 근거는 [ADHOC_DESIGN.md](ADHOC_DESIGN.md).
- AI가 꼭 지켜야 할 규칙:
  1. 담당자가 **사안명·검색어(최대 5개, 검색 방식 `하나라도`/`모두` — `모두`는 3개까지)·수집 시간(분 단위 자유)**을 입력하고 [수집]을 눌러야 실행된다. 자동 스케줄 없음, 정기 검색어 그룹과 별개. 네이버 검색 계층은 정기와 공유한다. 언론사 화이트리스트는 기본 꺼짐(`등록 언론사만`으로 켬), 사진기사·인사 발령 제외는 새 수집의 선택 옵션이다. 언론사 정렬 순서는 정기 설정을 쓰고, 기사 제목 형식·소제목 형식은 수시 전용 설정이다. 형광펜은 칠하지 않는다. 제목 중복 제거는 항상 한다.
  2. 수집 끝 시각이 지금보다 미래이면 막는다(자동 보정 없음). 시작이 끝보다 늦어도 막는다.
  3. **원본**은 조건에 걸린 기사를 소제목 없이 한 목록·최신순으로 보여주는 로데이터다. 원본 안에서 고칠 수 있는 것은 **수집 시간뿐**이고(검색어를 바꾸려면 「검색어를 바꿔 새로 수집」으로 새 원본을 만든다, 규칙13), 다시 수집(「지금까지 다시 수집」은 끝 시각만 지금으로)은 **교체이지 삭제가 아니다** — 조건에서 벗어난 기사도 카드에 남고 화면에서만 걸러지므로, 조건을 되돌리면 배정·순서까지 돌아온다. 원본의 🗑는 빼지 않고 「숨김」 표시만 한다.
  4. 저장은 **카드 하나 = 파일 하나**(`data/adhoc/cards/{id}.json`). 정기의 큐레이션 파일(숨김·소제목 배정·순서·이름·LLM 메모리 캐시)은 하나도 건드리지 않는다. 사안 아래 여러 카드가 쌓인다. 파일명에 쓰는 사안명은 금지 문자를 치환한다.
  5. 소제목 분류는 기능2와 같은 LLM을 쓰되 **자동 호출 0회**, 확정본(과 옛 원본)에서 담당자가 버튼을 눌렀을 때만 돌고 최대 5개다. 애매한 기사는 「기타」. 요약은 같은 호출로 만들어 페이지 맨 아래에 모아 보여준다. 「AI가 추출한 주요 키워드」 블록은 없다.
  6. (없앰 — 경위는 archive/PRD_2026-09-22_정리전_전체본.md) 정기와 수시 사이에 기사가 오가는 통로는 없다.
  7. **확정본**은 원본에서 골라 보낸(복사) 기사로 처음 보낼 때 생긴다. 헤더는 `수시 모니터링 {HH시 MM분} 기준(사안명)`(반올림 없음, 시각은 보낸 기사들의 원본 기준 시각 중 가장 늦은 것). 복사·`.txt`·엑셀을 뽑고, 파일명은 `날짜_수시모니터링_사안명`(원본은 끝에 `_원본`). **발송은 텔레그램만, 확정본에서 담당자가 (발송)을 누를 때만** 한다 — 수시엔 회차 마감이 없어 자동발송이 없다. 확인창에서 이번 발송만 뺄 사람을 고를 수 있고, 두 번째부터 「(수정)」.
  8. 수집이 실패하면 **자동 재시도 없이** 곧바로 오류를 보여준다(담당자가 화면 앞에 있으므로). 이미 모아둔 기사는 그대로 둔다.
  9. 보관 기간 **1년**(`ADHOC_RETENTION_DAYS = 365`), 정기 보관함(기능2 규칙6)과 같다. 지난 카드는 지우지 않고 `data/adhoc/trash/`로 옮긴다. 라벨이 붙은 기사는 라벨 저장소에 따로 남는다(기능10 규칙6).
  10. 수시 기사는 홈 워드클라우드(기능3)에 넣지 않고, 정기 보관함에도 넣지 않는다. **수시 보관함**(날짜 → 회차, 「보기」 스위치로 사안 → 회차로 축을 바꾼다)을 따로 두며, 여기엔 확정본만 쌓인다.
  11. 지금 없는 것: 수시 → 정기로 기사 보내기, 사안 병합.
  12. 흐름과 이름: 화면 이름은 `새 수집` → `원본` → `확정본` → `수시 보관함`.
      - 원본·확정본 화면 위에 오늘의 같은 종류 카드들을 탭으로 얹는다. 상단바 `원본`·`확정본`은 그 종류의 가장 최근 카드로 간다.
      - 같은 사안을 하루에 여러 번 모을 때, 조건이 같으면 한 원본에서 다시 수집하고, 검색어를 바꾸면 새 원본이 선다(규칙13). 「지금까지 다시 수집」마다 새 확정본이 생긴다(같은 시각에서 나눠 보낸 건 한 확정본).
      - 확정본에서 숨기기 = 보내기 취소, 원본의 「✓ 보냄」을 다시 누르면 보냄 취소. 확정본끼리는 다른 사안 확정본으로 「옮기기」만 있다.
      - **자정 잠금**: 어제 카드는 다시 수집할 수 없고, 어제 원본에서 오늘 확정본으로 보낼 수 없다. 어제 사안을 이어 모으려면 같은 사안으로 새로 수집한다(여러 날에 걸친 시간창은 1000건 상한 때문에 허용하지 않는다, 기능1 규칙2).
      - 확정본은 보관함에 들어간 뒤에도 계속 고칠 수 있다.
  13. **원본 하나 = 검색 조건 하나.** 원본 안에서는 수집 시간만 고치고, 검색어를 바꾸려면 「검색어를 바꿔 새로 수집」으로 **새 원본**을 만든다 — 한 원본 안에서 검색어를 고치면 조건이 다른 기사가 한 확정본에 섞이고 그 원본이 무엇을 모았는지 기록이 사라지기 때문이다. 같은 날·같은 사안에 원본이 여럿이면 둘째부터 화면 이름 뒤에 `#2`·`#3`을 붙여 가른다(첫 원본엔 번호가 없다 — 둘째가 생겨도 이미 보던 이름이 안 바뀐다). 번호가 하는 일은 이름이 같은 판을 가르는 것 하나뿐이라, 그 판만 이름을 고치면 번호를 뗀다. **사안명·보고서 첫 줄·파일명에는 붙이지 않는다.**

10) 라벨
- 설명: 기사 한 건에 담당자가 붙이는 자유 태그. 소제목은 「이 회차에서 어느 묶음이었나」로 회차 안에서만 의미가 있고, 라벨은 「회차와 상관없이 나중에 꺼내 볼 것」이라 회차를 가로지른다.
- AI가 꼭 지켜야 할 규칙:
  1. 담당자가 **직접 입력**한다(AI가 붙이지 않는다). 기사 한 건에 **여러 개** 붙일 수 있다.
  2. **URL을 키로 하는 전역 저장소**(`data/labels.json`)에 둔다 — 정기에서 붙인 라벨이 수시에서도 보이고 그 반대도 같다. 소제목·숨김·순서는 회차에 매인 판단이라 흐름끼리 격리하지만, 라벨은 기사 자체에 붙는 것이라 격리하지 않는다(기사 제목 교정 `summary_overrides`와 같은 이유).
  3. **보고서 텍스트에는 나가지 않는다.** 화면과 엑셀(기능11)에만 표시하고, 복사·`.txt`·텔레그램·이메일 텍스트에는 넣지 않는다.
  4. 입력할 때 **이미 쓴 라벨 목록**을 함께 보여주고(새로 타이핑도 가능), 비슷한 이름이면 알린다.
  5. **라벨 보관함**(`/labels`): 정기·수시를 가로질러 라벨이 붙은 기사만 모아 보는 화면. 라벨 칩을 여러 개 고르면 AND로 거른다. 숨긴 기사도 빼지 않고 「🗑️ 숨김」 표시만 단다. 목록은 **발행 최신순**이고 복사·`.txt`·엑셀로 내보낸다 — 복사·`.txt`는 평평한 목록이고, 첫 줄은 고른 라벨 이름에 소제목 형식을 입힌 것(예: `<세제>`)이다. 라벨 이름 변경·합치기·삭제는 라벨 관리 화면(`/label-manage`)에서 하고, 라벨 동작은 별도의 되돌리기 스택을 쓴다.
  6. **라벨이 붙은 기사는 1년 자동 삭제(기능2 규칙6·기능9 규칙9)와 무관하게 남는다** — 라벨을 붙이는 순간 그 기사 정보를 라벨 저장소에 복사해 두므로 회차 파일이 지워져도 라벨 쪽은 남는다. 라벨을 전부 떼면 항목째 지우고, 나이로는 지우지 않는다.
  6-1. 복사해 두는 필드는 엑셀 8열(기능11 규칙3) 중 라벨명을 뺀 7개(스크랩일자·스크랩종료시간·발행일 발행시간·언론사명·기사제목·URL·소제목)이고, **처음 라벨을 붙이는 순간 한 번만** 고정한다. 기사 제목만은 원문 교정(`summary_overrides`)이 있으면 그 값으로 덧씌워 보여준다. 소제목은 스냅샷 시점 값을 그대로 둔다(소제목은 그때그때의 분류 도우미라서, 기능2 규칙8·기능11 규칙4).
  7. 라벨을 AI가 제안하거나 자동 분류하는 기능은 두지 않는다.
  8. **라벨을 붙이는 화면은 확정본·초안·수시 확정본·정기 보관함 넷이다.** 정기 보관함의 읽기 전용 원칙(기능2 규칙6)에 걸리지 않는다 — 라벨은 보고서를 고치는 일이 아니다. 전체 기사(필터 없음 원칙, 「📌 담아두기」와 뜻이 겹침)·수시 원본(로데이터)·휴지통에는 두지 않는다.

11) 데이터 내보내기 (엑셀)
- 설명: 기사를 표 형태 파일로 뽑아 담당자가 엑셀에서 가공하게 한다. 복사·`.txt`(보고서 문장)와 목적이 다르다 — 이쪽은 **데이터**다.
- AI가 꼭 지켜야 할 규칙:
  1. 내보내는 곳은 여섯 군데다: 확정본 · 초안 · 정기 보관함 · 수시 카드(원본·확정본) · 수시 보관함 · 라벨 보관함.
  2. 어디서 뽑든 **지금 화면에 보이는 것**을 그대로 내보낸다. 기간·범위 선택 UI는 만들지 않는다 — 정기 보관함은 펼친 날짜, 수시 보관함은 보이는 사안, 라벨 보관함은 고른 라벨.
  3. 열은 8개: `스크랩일자` | `스크랩종료시간` | `발행일 발행시간` | `언론사명` | `기사제목` | `URL` | `소제목` | `라벨명`
  4. **소제목은 분류 도우미이지 절대적인 값이 아니다** — 스냅샷 원본 이름 그대로(형식 미적용) 쓰고, 빈 값·「📂 소제목 미분류」도 정상으로 둔다. 채우려고 AI를 부르지 않는다. 수시 확정본처럼 시간창이 없는 카드의 스크랩종료시간은 빈 값이다(지어내지 않는다).
  5. `라벨명`이 여러 개면 한 칸에 `세제, 보고서`처럼 합친다(기능10 규칙1).
  6. 파일 형식은 `.xlsx`(`openpyxl`)다. 셀 타입이 명시돼 `=`·`+`·`-`로 시작하는 제목이 수식으로 오인되지 않고, 자동 필터·틀 고정을 쓸 수 있다.
  7. 파일명은 `날짜_종류_구분`으로 통일한다(`.txt`·`.xlsx` 공통, 기능1 규칙11) — 예: `2026-09-17_언론모니터링_14-00기준.xlsx`, 초안은 끝에 `_초안`(`…_14-00기준_초안.xlsx`), `2026-09-17_수시모니터링_재경위.xlsx`, `2026-09-17_라벨_보고서.txt`. 세부:
      - 날짜는 정기·수시 카드면 그 회차·카드의 날짜, 라벨은 소속 날짜가 없어 **뽑은 날**.
      - 여러 날짜를 뽑을 때는 `2026-08-11~08-18_언론모니터링.xlsx`처럼 범위로 쓴다.
      - 사안명·라벨명의 파일명 금지 문자(`/:*?"<>|`)는 치환한다.

12) [단독]·[속보] 기사 알림
- 설명: 회차를 기다리지 않고 `[단독]`·`[속보]` 말머리 기사만 **즉시** 텔레그램으로 보낸다. 회차 보고서(기능6)가 「회차 전체를 정해진 시각에」라면 이쪽은 「지금 이 한 건을 빨리」.
- AI가 꼭 지켜야 할 규칙:
  1. **말머리 판정은 제목 맨 앞의 `[단독]`/`[속보]`만 인정한다**(괄호 변형 허용, 부분일치 배제). 전체 기사 화면 필터와 같은 판정(`app.filters.headline_kind`)을 쓴다.
  2. **세 경로가 판정·발송·중복 방지 함수 하나를 공유한다**(`detect_and_alert`) — ① **바닥**: 정기 회차 저장 직전 그 회차 기사 전부를 검사(추가 호출 0회, 설정을 다 꺼도 항상 동작), ② **폴링**: 감시 시간대·주기가 됐을 때만 감시 그룹 키워드로 검색, ③ **몰아보내기**: 앱을 켤 때 1회와 그날 첫 폴링에서 자정부터 게시된 것을 모아 발송. 셋이 같은 기사를 찾아도, 한 묶음에서 여러 키워드에 걸려도 **한 번만 나간다**(`data/alerted_urls.json`, 자정 초기화).
  3. **감시 대상은 담당자가 고른 검색어 그룹만이다** — 전체 키워드로 열지 않는다.
  4. **설정**(`/breaking-alert`, `data/breaking_alert.json`): 감시 사용 여부, 감시 그룹, 감시 시간대, 확인 주기(3~30분), 몰아보내기 사용 여부. 예상 네이버 호출량을 즉시 계산해 보여준다. 실행 상태(마지막 폴링 등)는 `data/breaking_alert_state.json`에 따로 저장한다.
  5. **받는 사람은 기능6의 텔레그램 받는 사람 명단을 쓰되 칸이 독립이다** — 사람마다 `[단독]`·`[속보]` 칸을 정기·수시 칸과 따로 켠다. 말머리별로 따로 보내고, 사람마다 정한 알림 시간 밖이면 소리 없이 간다.
  6. **API 호출 한도를 넘기면 폴링만 멈춘다** — 하루 호출이 25,000의 80%를 넘으면 이 기능의 폴링을 멈추고 경고를 1회 보낸다. 정기·전체 기사·수시는 멈추지 않는다.
  7. AI 요약은 넣지 않는다. 기사 줄은 설정 「기사 제목 형식」을 따른다. 같은 주기의 여러 건은 한 메시지로 보낸다.
  8. 기사마다 URL 다음 줄에 게시 시각(`15:31 게시`, 오늘이 아니면 `M/D HH:MM 게시`)을 적는다. 시각을 못 읽은 기사는 그 줄을 뺀다(지어내지 않는다).
  9. **[속보] 몰림 알림**: 한 사건을 여러 언론사가 동시에 [속보]로 내면 알림이 그 수만큼 쏟아진다. 정한 시간(20·30·60분 중, 기본 30분) 안에 [속보]를 낸 **언론사 수**가 기준(3~10 중 여섯 선택지, 기본 5곳) 이상이면, 개별 알림은 그대로 두고 **뒤에 요약 한 통을 더** 보낸다(몇 개 언론사인지·첫 기사 제목·언론사 이름). 한 번 울리면 60분 조용하고, 네이버 추가 호출은 없다. **[단독]은 대상이 아니다**(몰리지 않는다).
  10. **[속보] 같은 사건 묶기**: 제목이 충분히 겹치면서 게시 시각이 60분 안이면 같은 사건으로 본다(게시 시각을 못 읽은 기사는 묶지 않는다 — 확인할 수 없으면 따로 보내는 쪽이 안전하다). 한 통 안에서는 대표 기사 아래 `↳ 같은 사건: 언론사 …`로 접어 **메시지 길이를 줄이고**, 오늘 이미 나간 사건의 후속 [속보]는 **새 통을 만들지 않는다**(울리는 횟수가 줄어드는 건 이쪽이다). 접은 기사도 기록에는 남아 홈 [속보] 목록에 그대로 보이고, 몰림 판정(규칙9)은 접은 기사까지 센다. **[단독]에는 적용하지 않는다.**

---

## 6. 범위와 비범위

- 범위 (만든 것): 위 5절 기능 1~12 전부. 로컬 PC에서 실행하며, 앱을 켜 두면 동작하고 놓친 회차는 켜거나 깨어난 직후 보충한다.
- 비범위 (만들지 않음)
  - 네이버 뉴스 외 다른 매체(다음, 구글뉴스 등) 연동
  - 사용자별 로그인·계정 관리
  - 확정본·초안 화면의 언론사별 필터·검색(전체 기사 화면엔 있다)
  - 수시 새 수집의 AI 검색어 추천
  - 같은 네트워크 밖(외부)·모바일 원격 접속

---

## 7. 보안·개인정보 검토

- 공개등급: 내부용(담당자 개인 업무 도구, 외부 공개 없음).
- 개인정보 여부: 수집 대상은 네이버 뉴스에 공개된 기사로 개인정보 수집 목적은 아니다. 다만 기사에 특정인의 실명 등이 들어갈 수 있으므로 결과물을 외부에 공유할 때 주의한다.
- API 키 관리: 네이버·텔레그램·Claude·이메일 계정 값은 설정 화면(`data/` 아래) 또는 `.env`에 로컬로만 두고 커밋하지 않는다(`.env`·`data/`는 `.gitignore`).
- 데이터 보관: 정기 회차·수시 카드 모두 **1년 보관 후 자동 정리**(수시 카드는 trash로 이동). 담당자가 라벨을 붙인 기사는 라벨 저장소에 따로 남아 기한이 없다(기능10 규칙6).
  - ⚠️ **개인정보 관점 재검토 필요(미완)**: 1년 보관과 라벨 예외(사실상 무기한)는 개인정보 보호 관점에서 검토가 필요하며, 담당자가 별도로 진행한다. 결과에 따라 라벨 기사 수동 삭제 수단·보관 기간 설정값화 등이 더해질 수 있다.
- 저작권: 스크랩 결과는 담당자 개인 업무용으로만 쓰고 외부에 배포하지 않는다.
- 실행 환경이 로컬 PC(`127.0.0.1`)로 한정되어 외부 노출 위험은 낮다.

---

## 8. 기술 스택

- Python 3, 의존성은 `requests`·`python-dotenv`·`anthropic`·`openpyxl`(`requirements.txt`).
- **웹 프레임워크 없음.** 파이썬이 정적 HTML(`index.html`·`preview.html`·`history.html`·`home.html`·`live.html`)을 다시 만든다. 설정 저장·큐레이션·화면 서빙은 표준 라이브러리 `http.server`로 만든 `127.0.0.1:8765` 설정 서버(`app/settings_server.py`)가 맡고, 이 포트가 중복 실행 자물쇠를 겸한다.
- **저장소는 `data/` 아래 JSON 파일**(SQLite 없음, 쓰기는 원자적 저장). 스케줄러는 10초 폴링 루프(APScheduler 없음).
- 외부 API: 네이버 뉴스 검색(필수), Claude API(소제목 분류·요약·부정 추정, 선택), 텔레그램 봇(선택), 이메일 SMTP(네이버·Gmail, 선택). 키가 없거나 호출이 실패해도 앱은 멈추지 않는다.
- 운영 환경: macOS(자동 실행은 LaunchAgent). 파일명엔 `:`를 쓰지 않는다.
- 초기 설계(FastAPI·SQLite·APScheduler 안과 그 폐기 이유)는 [archive/DESIGN.md](archive/DESIGN.md).

---

## 9. 개발 단위

초기 개발 단위(프로젝트 구조, 네이버 API 연동, 정렬, 하이라이트, 재시도, 스케줄 루프, 소제목 분류·요약, 보관·자동 삭제, 화면, 복사·내보내기, 설정 화면, 통합 테스트)는 모두 끝났다. 원래 목록은 [archive/PRD_2026-09-22_정리전_전체본.md](archive/PRD_2026-09-22_정리전_전체본.md) 9절. 테스트는 `python3 tests/test_integration.py`.
