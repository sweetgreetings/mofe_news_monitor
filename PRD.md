# 재정경제부 언론 모니터링 자동화 서비스 기획서 (PRD)

- 작성자: [홍정희]
- 작성일: (2026.7.22.)

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

언론 모니터링 담당자는 네이버 뉴스에 게재되는 재정경제부 관련 기사를 하루에도 여러 차례 직접 검색해 확인하고, 언론사·제목·URL을 메모장에 일일이 옮겨 붙이는 방식으로 정리해왔다. 이 과정은 반복적인 수작업이 많고, 기사를 놓치거나 정리에 많은 시간이 소요되는 불편이 있다.

---

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

- 현행 방식: 담당자가 하루 중 필요한 시점마다 네이버 뉴스에서 재정경제부 관련 기사를 직접 검색하고, 관련 기사를 하나씩 열어 언론사·제목·URL을 확인한 뒤 메모장에 수기로 옮겨 적는다. 기사 내용을 소제목별로 분류하거나 요약하는 것도 담당자가 직접 판단해서 정리한다.
- 한계:
  - 검색·정리에 반복적인 시간이 소요된다.
  - 사람이 직접 확인하다 보니 기사를 놓치거나 중복 정리할 위험이 있다.
  - 언론사 우선순위, 시간대별 확인 등을 매번 사람이 판단해야 해서 일관성이 떨어진다.
  - 정리 결과가 개인 메모장에만 남아 형식이 통일되지 않고 재사용·검색이 어렵다.

---

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- 기사 스크랩·정리에 드는 수작업 시간을 대폭 절감 (별도 수치 측정은 하지 않음)
- [수정: 2026-08-18] 담당자가 설정한 회차마다 자동 수집으로 등록된 검색어가 포함된 기사를 모두 수집 (회차 시각·개수와 검색어는 설정 화면에서 바꾸는 값이라 여기 적지 않는다. "전수"였던 표현도 뺀다 — 네이버 API의 키워드당 1000건 상한이 있어 전수가 보장되지 않는다. 기능1 규칙2 참고)
- 기사 원문 요약(API description)과 소제목별 요약(3문장·250자 이내)·키워드 자동 추출로 담당자의 내용 파악 시간 단축

---

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

- 사용자: 언론 모니터링 담당자 (단일 사용자, 로그인 없이 로컬에서 사용)
- 실행 방식: 사용자가 앱을 실행해두면 동작함. 컴퓨터가 잠자기 모드였던 동안에는 예약 시각에 실행되지 않으며, 컴퓨터가 깨어난 직후 놓친 회차를 즉시 보충 실행함. 앱 실행 시 브라우저가 자동으로 열려 [수정: 2026-07-23] 진입 화면(기능3)으로 연결되며, 거기서 "스크랩 보러 가기"를 눌러 실제 결과 화면으로 이동함
- 이용 흐름: [수정: 2026-08-18] 예전엔 "매일 09:00 / 10:30 / 13:30 / 16:30 자동 스크랩"처럼 시각을 문서에 박아뒀으나, 회차 시각·개수는 담당자가 설정 화면에서 자유롭게 바꾸는 값이라(기능1 규칙1·20) 문서에 적어두면 금방 사실과 달라진다 — 시각 대신 흐름만 적는다. 진입 화면(기능3)에서 아래 네 갈래로 갈라진다:
  - **실시간** — 지금 들어오는 기사를 검색어에 걸린 그대로. 저장·분류 없음 (기능4)
  - **정기 모니터링** (일자 › 시간대): `초안` →(회차 마감) `확정본` →(다음 회차) `정기 보관함`. 두 화살표 모두 자동이며 담당자가 누를 버튼은 없다 (기능1·5·2 규칙6)
  - **수시 모니터링** (사안 › 일자): `새 수집` →(수집) `수집 결과` →(자정) `수시 보관함`. 마지막 화살표는 자정에 자동으로 넘어간다 (기능9). [지금 넘기기]로 앞당기는 버튼은 **아직 미구현**
  - **라벨 보관함** — 위 두 흐름을 가로질러, 담당자가 라벨을 붙여둔 기사만 모아 보기 (기능10)
  - 공통: 사전 설정(언론사 우선순위·검색어·수집 시간대) → 수집 → 소제목 자동 분류 → 담당자가 화면에서 확인·정리 → 필요 시 원문(하이라이트 포함) 열람 후 업무에 활용

---

## 5. 주요 기능 (must / nice로 구분)

**Must**

1) 정기 모니터링 (기사 자동 스크랩) — [이름 변경: 2026-08-18, 구현 완료: 2026-08-18]
- 설명: 네이버 뉴스 검색 API를 통해 재정경제부 관련 기사를 자동으로 수집하여 메모장 형식으로 변환. **[설계 확정: 2026-08-18, 구현 완료: 2026-08-18]** 화면에 쓰는 이름을 "자동 스크랩"에서 **정기 모니터링**으로 바꾼다 — 기능9(수시 모니터링)가 생기면서 둘을 나란히 부를 이름이 필요해졌고, "자동/수시"는 대비가 어긋난다(수시도 수집 자체는 자동이다). 대비되는 축은 **정기 ↔ 수시**다. 이 흐름의 분류 기준은 **일자 › 시간대**이며, 지난 회차를 모아 두는 곳은 **정기 보관함**이라 부른다(기능2 규칙6)
- AI가 꼭 지켜야 할 규칙:
  1. 매일 오전 9시 / 오전 10시 30분 / 오후 1시 30분 / 오후 4시 30분, 하루 4회 자동 실행 — [수정: 2026-07-24] 이 시각과 횟수는 기본값이며, 설정 화면에서 자유롭게 추가·삭제·변경할 수 있다 (규칙20)
  2. 네이버 뉴스 검색 API를 통해, 설정 화면에 등록된 키워드 목록(기본값: "재정경제부", "재경부")으로 검색 결과를 수집 (앱이 거는 건수 제한은 없음 — 단, [수정: 2026-08-18] 네이버 API 자체의 **키워드당 최신 1000건** 상한이 있다. 예전엔 이 문서에 "건수 제한 없음"이라고만 적혀 있었는데 사실과 달랐다. 검색 API에 기간 지정 파라미터가 아예 없어 날짜 필터링은 앱이 최신순 페이지를 넘기며 직접 하는 구조라, 시간창의 하한에 닿기 전에 1000건에 먼저 걸리면 더 오래된 기사는 어떤 방법으로도 가져올 수 없고 결과가 조용히 잘린다(예외로도 잡히지 않음). 2026-08-18 실측에서 등록 키워드 9개 중 `정부` 하나만 상한에 닿았고(약 284건/시간·하루 6,800건 환산, 나머지 8개는 1000번째 기사가 수 주~3개월 전), 실시간(기능4)은 자정 이후 첫 로딩에서만 걸린다. 우선 발생 시 로그만 남기고 화면 경고는 실측 후 판단하기로 했다 — 더 근본적인 해결은 `정부`처럼 지나치게 넓은 검색어를 좁히는 것이다. 배경·수치는 HISTORY.md "네이버 조회 상한(1000건) 잘림" 참고). 키워드가 2개 이상이면 OR(하나라도 포함, 기본값) / AND(모두 포함) 중 검색 방식을 설정 화면에서 선택할 수 있으며, 키워드가 1개뿐이면 이 선택지는 의미가 없으므로 표시하지 않음. [수정: 2026-07-24] 게시일이 당일(한국 시간 기준)인 기사 중에서도, 그 회차에 설정된 시작~종료 시간대(규칙20)에 게시된 기사만 수집한다(회차별 시간창 수집 — 예: 07:30~09:30 회차는 그 구간에 게시된 기사만 모으고, 다음 회차가 그 뒤 시간대를 이어받는다). 지난 날짜 기사나 이 시간창 밖의 기사는 수집 대상에서 제외함 (화면에 게시일자를 표시하지는 않으며, 수집 시 내부 판단 기준으로만 사용)
  3. 언론사 우선순위: ① 방송사(KBS, MBC) → ② 주요 언론사(조선일보, 중앙일보, 동아일보, 한국경제, 매일경제, 서울경제, 한국일보, 머니투데이, 이데일리, 연합뉴스, 뉴시스 등) 순으로 정렬하며, 목록에 없는 언론사는 제외하지 않고 맨 뒤에 붙여서 포함 — 단, [수정: 2026-07-23] 설정 화면에서 "언론사 선택"을 하나 이상 지정하면, 그 순간부터는 이 기본 우선순위 대신 사용자가 고른 언론사만 수집 대상으로 남기고(선택 안 한 언론사는 완전히 제외), 사용자가 지정한 순서대로 정렬한다. 아무것도 선택하지 않으면 기존 기본 동작(전체 포함) 유지. [수정: 2026-07-30] 방송사 카테고리에 SBS·YTN·MBN·JTBC·TV조선을 추가하고, 조선비즈·시사저널·주간조선·땅집고 등 언론사 자매 매체(같은 회사 하위 도메인) 매핑을 다수 보강했다. 또한 mk.co.kr 도메인은 매일경제(일간지)와 매경이코노미(주간지)가 함께 써서 도메인만으로 구분이 안 되므로, 화면에 표시되는 "매일경제" 언론사명만 빨간 글자로 강조해 매경이코노미일 수 있음을 알린다(화이트리스트 비교·정렬·복사/내보내기에 쓰이는 원본 값은 그대로 유지 — 화면 전용 표식). [수정: 2026-08-06] IT조선(it.chosun.com)이 조선일보(chosun.com)의 하위 도메인이라 별도 매핑 없이는 조선일보로 잘못 잡히던 것을 사용자 제보로 확인해 바로잡았고, "전문일간" 카테고리 맨 끝에 추가했다
  4. 동일 기사가 여러 회차나 언론사에 중복 게재되어도 별도로 제거하지 않고 모두 수집하되, 기사 제목이 완전히 동일한 경우(주로 [포토] 등 사진 기사에서 발생)에는 1개만 수집
  5. 출력 형식은 다음과 같고, 게시일자는 표시하지 않음
     ```
     ㅇ (언론사) 기사제목
     URL
     ```
     [수정: 2026-07-23] 첫 줄의 형식("ㅇ (언론사) 기사제목" 부분)은 설정 화면에서 `{outlet}`·`{title}` 자리표시자가 들어간 문구로 바꿀 수 있다 (예: 기본값 `ㅇ ({outlet}) {title}` → `[{outlet}] {title}`로 변경). URL 줄과 게시일자 미표시는 이 커스터마이징 범위 밖으로, 그대로 유지된다
  6. 기사 본문 전체를 크롤링하지 않고, 네이버 검색 API가 제공하는 간단한 요약(description)만 저장. 저장된 요약 중 일치하는 키워드는 하이라이트 표시 — [수정: 2026-07-23] 하이라이트 대상 키워드는 검색 키워드(규칙2)와 완전히 별개이며, 설정 화면에서 직접 지정한다 (기본값: "재정경제부", "재경부"). [수정: 2026-08-18] 문서 앞부분에 "형광 연두색(#C6FF00)" 단색으로 남아 있던 것을 지운다 — 아래 [변경: 2026-08-07]대로 지금은 **단어마다 다른 파스텔 5색이 순환**하며, 칩을 눌러 단어별 색을 바꿀 수 있다. 최대 개수도 **20개**다(`MAX_HIGHLIGHT_KEYWORDS`). [수정: 2026-07-25] 최대 개수를 3 -> 5개로 확대, 기본 노출 3칸 + "+ 형광펜 단어 추가" 버튼으로 확장(스크랩 시간대와 같은 방식). [변경: 2026-08-07] 5색 팔레트가 전부 너무 비비드해서(특히 "재정경제부"처럼 거의 모든 기사에 등장하는 단어에 쓰면 화면 전체가 튀어 보임) 톤을 낮춘 더스티 파스텔 5색으로 교체했다(순서는 그대로라 기존 단어별 색 배정은 유지됨)
  7. 해당 회차에 수집된 기사가 없으면 화면에 "💤" 표시 — [수정: 2026-07-24] "하나도 없어요" 문구에서 변경
  8. 네트워크 오류 등으로 스크랩 실패 시 5분 간격으로 최대 3회 자동 재시도
  9. [대체: 2026-09-04] 앱이 꺼져 있어 지나가버린 스크랩 시간대가 있으면, 앱을 켜자마자 **오늘 놓친 회차를 전부** 오래된 것부터 차례로 실행한다(예: 09:00·10:30을 모두 미실행하고 11시에 앱 실행 → 09:00 먼저, 그다음 10:30). 예전 규칙은 "가장 최근에 놓친 1개만"이었으나, 실측(2026-08-19~09-02)에서 예정 78회차 중 14회(18%)가 통째로 비었고 그중 여러 회차를 한꺼번에 놓친 날(8/28 4회·8/31 2회)은 앱을 켜도 마지막 하나만 채워졌다. 놓친 회차는 나중에 되살릴 방법이 없다 — 네이버 검색 API에 기간 지정 파라미터가 없고 키워드당 최신 1000건까지만 주는데, 정기 스크랩 키워드 중 가장 좁은 것이 실측상 하루치밖에 안 거슬러 간다. 그래서 되살릴 수 있는 건 전부 되살린다. 세부 규칙 넷: (1) **오래된 것부터** 실행한다 — 수집이 앞 회차에 이미 실린 기사를 걸러내는 방어(기능1 규칙2의 회수 로직)가 최신부터 돌리면 거꾸로 작동해 앞 회차가 자기 기사를 뺏긴다. (2) 회차 하나가 실패해도 나머지는 계속 실행한다 — 실패한 회차는 저장되지 않아 다음 확인 때 자연히 다시 시도되되, **같은 회차는 하루에 3번까지만** 다시 시도하고 그 뒤로는 그날 포기한다(그 회차는 비게 된다). 이 상한은 예전 규칙엔 필요 없었다 — 실패한 회차가 다음 회차 도착과 함께 후보에서 저절로 빠졌기 때문이고, 보충을 넓히면서 그 제동이 사라져 계속 실패하는 회차 하나가 10초마다 재시도를 반복하게 됐다. (3) 시간창의 시작은 설정된 값 그대로 쓴다(규칙20, 실제 지연된 실행 시각과는 무관). (4) 여러 회차를 한꺼번에 채워도 **자동발송(기능8)은 마지막 회차에 대해서만** 일어난다 — 몇 시간 지난 회차를 지금 와서 받는 건 담당자가 원하는 일이 아니고, 그 회차들은 확정본·정기 보관함에 그대로 남아 언제든 직접 보낼 수 있다. **어제 이전 회차는 채우지 않는다** — 회차 시간창은 언제나 "오늘"로 해석되고(기능1 규칙2), 위 1000건 한계상 그 이상은 어차피 못 가져온다
  10. 화면에 표시된 소제목별 스크랩 목록(언론사·기사제목·URL)을 클립보드로 복사하거나, 메모장 형식의 .txt 파일로 내보낼 수 있는 기능 제공
  11. 화면·내보내기 결과 맨 위에 "언론 모니터링 [스크랩 실행 시각] 기준" 문구를 헤더로 표시 (예: 09:00 회차라면 "언론 모니터링 09:00 기준"). [수정: 2026-07-30] 시각 표기를 "HH:MM 기준" 대신 자연스러운 한국어("9시 기준", "9시 30분 기준" — 시 앞자리 0 생략, 정각이면 "0분" 생략)로 바꿨다. 화면·내보내기 텍스트 모두 적용. **[수정: 2026-08-18, 구현 완료: 2026-08-18 — 단 초안 `.txt`는 예외로 남음] 저장 파일명 형식을 통일한다** — 예전엔 종류마다 규칙이 달랐다(정기 `.txt`는 `언론모니터링_날짜_시각`, 수시 `.txt`는 `수시모니터링_사안명_날짜`로 날짜 위치조차 서로 반대였다). 이제 모두 **`날짜_종류_구분`** 형식으로 맞춘다(기능11 규칙6 참고) — 이름순 정렬이 곧 시간순이 되고, 종류가 항상 같은 자리에 있어 다운로드 폴더에서 구분된다. `.txt`·`.xlsx` 모두 이 규칙을 따른다
  12. [수정: 2026-08-11] 자동선별(정기 회차 수집) 시 사진(`포토`, `[포토]`, `사진`, `화보`, `포토뉴스`, `PHOTO`, 대소문자 무시)·인사 발령(`[인사]`) 기사를 제외할지는 설정 화면(`/scrap-page`)의 체크박스로 담당자가 직접 고른다 — 기본은 **체크 해제 = 둘 다 수집됨**이고, "자동선별 시 [인사] 기사 제외하기"/"자동선별 시 [포토] 기사 제외하기"를 각각 켜야 그 유형이 빠진다(예전엔 사진 기사가 항상 무조건 제외였으나, "커스터마이징 관점에서 기본은 다 뜨고 빼고 싶은 것만 고르는 쪽이 자연스럽다"는 판단으로 뒤집었다). 단, 제외 옵션을 켜도 제목에 `[속보]`가 붙어 있으면 예외로 항상 수집(단신이어도 속보는 놓치지 않음). 본문 길이·문장수 기반 2차 필터는 이 앱이 네이버 API의 짧은 요약(description)만 갖고 있어 진짜 뉴스도 함께 걸러질 위험이 커서 도입하지 않기로 함(오탐 방지 우선). [추가: 2026-08-11] 연합뉴스·뉴시스·뉴스1 등 통신사 사진 기사는 제목에 `[포토]` 표식을 아예 안 붙이는 경우가 있어 위 힌트 단어만으로는 못 걸러진다는 사용자 제보 — 요약문 안의 사진 캡션 특유 신호(예: "재판매 및 DB금지", 사진기자단 표기, 사진기자 이메일, 촬영 날짜 도장)와 제목이 서술어 없이 "~하는/~한 + 직책"으로만 끝나는 패턴(예: "쪽방촌 둘러보는 구윤철 부총리")을 함께 감지해 추정한다(`app/filters.py` `looks_like_photo_caption`, 실제 저장된 기사 1,096건으로 검증해 오탐 0건인 신호만 채택). 이 추정 규칙은 힌트 단어와 별개로 항상 적용되며(설정 옵션과 무관), 확신할 수 없는 추정이므로 실시간 현황·초안 화면에서는 삭제하지 않고 "📷 사진 추정" 배지로만 표시하고, 정기 회차 수집에서만(포토 제외 옵션이 켜져 있을 때) 실제로 걸러낸다
  13. 화면에서 기사 제목을 클릭하면 저장된 요약(API description)을 볼 수 있음
  14. 화면에서 기사 URL을 클릭하면 원본 네이버 뉴스 기사 페이지로 새 탭에서 이동
  15. [수정: 2026-07-23] 설정 화면에서 검색 키워드를 최대 7개까지 등록·수정·삭제할 수 있으며, 코드 수정 없이 화면에서 바로 변경 가능 (기본값: "재정경제부", "재경부"). 키워드가 2개 이상 등록되면 OR/AND 검색 방식 선택 항목도 함께 표시 (기본값 OR). [수정: 2026-07-25] 최대 개수를 7 -> 15개로 확대, 기본 노출 7칸 + "+ 키워드 추가" 버튼으로 확장. 각 칸 옆 del 버튼으로 그 칸을 바로 없앨 수 있음(형광펜 단어·스크랩 시간대도 동일). [수정: 2026-07-25, 대체] 평평한 키워드 목록 방식을 "키워드 그룹" 방식으로 대체했다 — 최대 6개 그룹, 그룹당 최대 7개 키워드, 그룹 간에는 항상 OR(어느 그룹이든 조건 만족 시 수집)이고 그룹 안에서는 OR/AND를 그룹별로 선택한다. 최초 심어지는 "기관 정보" 그룹(재정경제부/재경부/기획재정부/기재부/정부/구윤철/이형일)도 다른 그룹과 동등하게 이름·키워드 수정, 그룹째 삭제가 가능하다(최소 1개 그룹은 항상 남아야 함). 그룹마다 "정기 스크랩에도 포함" 체크박스가 있어, 켜진 그룹만 예정된 회차 수집에 쓰이고(기능4의 실시간 현황은 이 플래그와 무관하게 항상 전체 그룹을 검색), 최소 1개 그룹은 이 플래그가 켜져 있어야 한다. 키워드별로도 개별 on/off 토글이 가능하다. [추가: 2026-08-12] 그룹 전체를 껐다 켰다 하는 "사용" 토글도 있다 — 꺼지면 정기 스크랩·실시간 현황 양쪽에서 이 그룹 자체가 검색되지 않는다(하위 키워드의 개별 on/off와는 별개 축, 최소 1개 그룹은 켜져 있어야 함). 그룹을 끄면 하위 키워드 칸이 흐려지고 조작이 잠기지만 각 키워드의 on/off 상태 자체는 그대로 저장돼 있어, 다시 켜면 꺼두기 전 구성 그대로 돌아온다. [수정: 2026-08-13] 최대 개수를 6그룹×그룹당 7개 → 8그룹×그룹당 10개로 확대 — 실측(동시성 8, 100키워드, 2회 반복)으로 429 발생률 0%를 확인한 뒤 결정했다(자세한 배경은 HISTORY.md "그룹/키워드 상한 확대" 참고). 실시간 현황도 같은 시점에 키워드 단위 증분 캐시로 바뀌어, 상한을 올려도 평상시 체감 속도엔 영향이 없다(콜드 스타트만 대기 화면으로 가려짐 — 기능4 항목 참고)
  16. [추가: 2026-07-23, 순서 갱신: 2026-07-23] 설정 화면에서 "언론사 선택" 항목을 두어, 방송사·전국종합일간·경제일간·영자일간·통신사·전문일간·기타 언론사로 분류된 언론사 목록(지정된 순서, OBS·이투데이·내일신문은 제외) 중 원하는 곳을 체크박스로 고를 수 있다. 고른 언론사는 화면에 순서대로(선택된 순) 나열되며, 각 항목 옆 "↑"/"↓" 버튼으로 순서를 바꿀 수 있다 (드래그 앤 드롭 대신 가벼운 방식 채택). 아무것도 선택하지 않으면 규칙3의 기본 우선순위 동작을 그대로 따른다
  17. [추가: 2026-07-23, 수정: 2026-07-24] 설정 화면은 "⚙️ 설정"이라는 메뉴 화면에서 시작하며, "🔎 검색 키워드"·"📰 주요 언론사 선택 및 순서 지정"·"⏰ 스크랩 시간 및 횟수"·"💡 형광펜 단어"·"📃 본문 형식"·"🗑️ 숨긴 기사 관리" 하위 페이지로 각각 이동해 편집한다 (규칙 6·15·16·5·20·19가 각각 이 하위 페이지에 대응). 메뉴는 수집 관련(키워드·언론사·시간)/표시 관련(형광펜·본문 형식)/관리(숨긴 기사) 세 그룹으로 나눠 보여준다. [수정: 2026-07-25] 본문 형식 이모지를 🖊️ -> 📃로 변경. [추가: 2026-07-25] "☁️ 워드클라우드 제외어" 하위 페이지를 네 번째 그룹으로 추가(기능3 규칙 3) — 자주 쓰지 않을 설정이라 숨긴 기사 관리 아래 별도 그룹으로 배치
  18. [추가: 2026-07-23] 화면 폰트는 Pretendard를 우선 사용하고, 없으면 Noto Sans KR, 그래도 없으면 시스템 기본 글꼴 순으로 대체 표시한다
  19. [추가: 2026-07-24] 담당자가 보고에서 제외하고 싶은 기사는 각 기사 옆 🗑️ 버튼으로 화면에서 숨길 수 있다(큐레이션). 숨김은 원본 수집 데이터를 지우지 않고 "숨긴 기사 URL 목록"만 별도로 기록해 화면(메인·지난 기사)과 복사/내보내기 텍스트·워드클라우드에서만 제외하며, 설정 화면의 "🗑️ 숨긴 기사 관리"에서 언제든 "되돌리기"로 다시 표시할 수 있다. [변경: 2026-08-05] "숨긴 기사 관리" 화면 상단 좌측 링크를 "홈"에서 "📝 스크랩 초안"으로 바꿨다 — 숨김을 되돌리는 목적이 대부분 초안이나 확정본으로 다시 가려는 것이라, 로고 화면보다 그쪽이 실제로 더 자주 필요했다
  20. [추가: 2026-07-24, 수정: 2026-07-24] 설정 화면 "⏰ 스크랩 시간 및 횟수"에서 회차마다 시작~종료 시간대를 "HH:MM" 단위로 자유롭게 추가·삭제할 수 있다 [수정: 2026-08-12] 단, 분(MM)은 00/30 두 값만 가능하다(30분 단위) — 회차 사이 최소 간격을 자동발송 유예 시간(최대 30분)과 안전하게 맞추기 위한 제약(사용자 요청, `app/settings.py` `validate_schedule_windows`) (최소 1개, 최대 10개 — 너무 잦으면 거의 같은 내용이 새 회차로 계속 쌓여 지난 기사 목록만 어수선해지므로 상한을 둠). 종료 시각은 화면 헤더·저장 파일명의 기준(run_slot)이 되고, 시작 시각은 그 회차가 수집할 시간창의 하한이 된다(규칙2). 등록한 개수가 곧 하루 실행 횟수가 되며, 화면 상단 "언론 모니터링 [종료시각] 기준" 제목도 실제로 실행된 회차의 종료 시각을 그대로 따라간다. 앱을 켜자마자 놓친 회차를 보충 실행할 때도(규칙9) 시간창의 시작은 설정된 값 그대로 쓴다(실제 지연된 실행 시각과는 무관). [추가: 2026-07-29] "스크랩 시간대 그룹" — 시간대 세트 자체를 여러 개(예: 주중용/주말용, 최대 5개) 만들어두고 라디오 버튼으로 정확히 1개만 "지금 적용 중"(active)으로 켤 수 있다. 각 시간대는 개별 on/off 토글도 가능해(최소 1개는 켜져 있어야 함), 요일별로 잠깐 끄고 싶을 때 지우지 않고 꺼둘 수 있다. 스케줄러는 항상 활성 그룹의 시간대만 본다. [대체: 2026-08-10] "정확히 1개만 라디오로 켠다"는 단일 활성 방식을, 그룹마다 독립적인 "사용"(enabled) 토글 + 요일(월~일) 선택으로 바꿨다 — 그룹이 1개만 켜져 있으면(수동 오버라이드 — 예: 추석처럼 평일에 꼭 껴야 하는 임시 그룹) 요일과 무관하게 그 그룹을 쓰고, 2개 이상 켜져 있으면 오늘 요일이 포함된 그룹을 쓰며(둘 이상이 같은 요일을 겹치게 설정하면 저장을 막는다), 일치하는 게 없으면 켜진 것 중 첫 번째로 대체한다. 옛 데이터(라디오 방식)는 처음 읽을 때 1:1로 자동 변환되어 동작이 그대로 유지된다. 설정 화면엔 그룹마다 오늘 실제로 적용 중인지 보여주는 "오늘 적용 중" 배지가 있다
  21. [추가: 2026-07-24, 수정: 2026-07-24] 메인 화면(최신 회차)에서 각 기사 옆 "↑"/"↓" 버튼으로 순서를 수동으로 옮길 수 있다. 같은 소제목 안에서는 순서만 바뀌고, 소제목 맨 위/아래에서 한 번 더 누르면 바로 앞/뒤 소제목으로 기사가 통째로 옮겨간다(소제목 경계 넘나들기). 버튼은 맨 처음 소제목의 첫 기사(↑)·맨 마지막 소제목의 마지막 기사(↓)일 때만 비활성화된다. 이 수동 배정은 그 소제목이 다음 회차 자동 분류에도 존재하는 동안 유지되며, 관련 기사가 모두 사라져 그 소제목 자체가 없어지면 자동으로 원래 분류 방식으로 되돌아간다. 지난 기사 화면은 소제목 재분류를 하지 않아(기능2 규칙 6) 이 기능이 없다. ↑/↓ 버튼은 🗑️ 버튼과 마찬가지로 복사/내보내기 텍스트에는 나타나지 않는다(화면 전용 버튼일 뿐, 별도로 생성되는 순수 텍스트에는 애초에 포함되지 않음).
     [추가: 2026-07-30] ↑/↓만으로는 자동분류가 틀린 기사 여러 개(3~4개)를 다른 소제목으로 옮기려면 경계를 여러 번 넘겨야 해 번거로워서, 두 가지를 추가했다: (a) 기사마다 "다른 소제목으로" 드롭다운 — 목표 소제목을 바로 골라 한 번에 이동(경계를 몇 번이고 넘나들 필요 없음). (b) 체크박스로 여러 기사를 선택하면 화면 하단에 "N개 선택됨 + 이동할 소제목 선택 + 옮기기" 바가 나타나(평소엔 숨겨진 상태) 선택한 기사 전부를 한 번에 옮길 수 있다. 두 방식 다 순서 재계산 없이 소제목 배정만 바꾸는 것이라 ↑/↓의 "이웃과 스왑" 로직과는 별개다.
     [추가: 2026-07-30] "+ 새 소제목 만들기" — 자동 분류로는 절대 나오지 않는 이름의 소제목을 사용자가 미리 만들어둘 수 있다. 기사가 하나도 없어도 화면에 점선 테두리와 안내 문구로 표시되며, 위 이동 기능들의 목표로 바로 선택할 수 있다. 다른 소제목처럼 이름 바꾸기가 가능하고, 헤더의 🗑️는 (기사가 없는 동안은) "이 빈 소제목 삭제"로 동작한다. [변경: 2026-08-05] 자정에도 비워지지 않던 동작을 "📌 담아두기"와 동일하게 당일까지만 유지되도록 바꿨다 — 하루만 쓰고 마는 소제목이 계속 남아 다음 날에도 빈 소제목으로 쌓이는 게 불편하다는 피드백. 이미 만들어둔 소제목이 바뀐 규칙이 배포된 순간 바로 사라지지는 않고, 그날 자정까지는 그대로 유지된 뒤 다음 자정부터 정상적으로 초기화된다. 초안·확정본 화면 어디서 만들거나 지우든 양쪽에 동일하게 반영되는 것은 그대로다. 소제목 미니 목차(규칙23)에서도 사용자가 만든 소제목은 볼드로 표시해 자동 분류 소제목과 구분한다. [변경: 2026-08-06] 새로 만든 소제목이 화면에 나열되는 기본 위치를, 예전엔 맨 아래였던 것을 맨 위(다른 사용자 지정 소제목들 바로 다음, 만든 순서 유지)로 바꿨다 — 소제목이 몇 개 쌓이면 스크롤해서 찾아야 하는 게 불편하다는 피드백. 어디까지나 "새로 만들 때의 기본 위치"일 뿐이라, 이후 ↑/↓나 소제목 미니 목차로 자유롭게 다시 옮기는 건 그대로 가능하다.
     [추가: 2026-07-30] 위 세 가지 큐레이션(체크박스 일괄 이동·개별 드롭다운 이동·새 소제목 만들기)은 모두 스크랩 확정본과 스크랩 초안(기능5) 화면 양쪽에서 동일하게 지원된다.
  22. [추가: 2026-07-24, 수정: 2026-07-24] 메인 화면은 1분마다 자동으로 새로고침된다(`<meta http-equiv="refresh">`, 자바스크립트 불필요, 최초 3분에서 실무자 요청으로 단축). 실시간 스트리밍이 아니라 정해진 스크랩 시각에만 내용이 바뀌는 구조라 이 정도 간격으로 충분하며, 🗑️/↑/↓ 조작은 이미 클릭 즉시 서버에 저장한 뒤 화면을 바꾸는 구조라 새로고침 타이밍이 겹쳐도 잃어버릴 편집 상태가 없다. 숨긴 기사는 화면 생성 시점에 아예 제외되므로 새로고침을 거듭해도 다시 나타나지 않는다. [삭제: 2026-07-25] 화면 상단 "🔄 [시각] 업데이트" 표시기는 없앴다 — 헤더의 "[종료시각] 기준"이 이미 그 회차의 정체성을 보여주는데, 여기에 "실제로 화면이 다시 만들어진 시각"까지 같이 보이면 두 시각이 다른 이유(놓친 회차 보충 실행, 🗑️/↑/↓ 조작, 담아둔 기사 반영 등으로 원래 예정 시각보다 늦게 갱신됐을 뿐 오류가 아님)를 모르는 사용자에게는 오히려 헷갈린다는 피드백으로 제거
  23. [추가: 2026-08-04~2026-08-05] 규칙21의 큐레이션 도구를 실사용해보며 나온 세부 개선을 한데 모았다(확정본·스크랩 초안 화면 공통):
      - 체크박스 일괄이동 바에 ↑/↓를 추가해, 선택한 여러 기사를 같은 소제목 안에서 한 블록으로 위/아래 이동할 수 있다(소제목 경계는 넘지 않음). 같은 바에 🗑️도 추가해 선택한 기사를 한 번에 숨길 수 있다 — 동시에 여러 건을 숨기는 요청이 겹치면 일부가 누락되는 경합 문제가 있어 파일 잠금으로 고쳤다.
      - 소제목 헤더에 "전체 선택" 체크박스를 추가해, 그 소제목 안 기사를 한 번에 전부 선택할 수 있다.
      - 소제목 자체의 화면 표시 순서를 헤더의 ↑/↓로 바꿀 수 있고(`data/group_order.json`), "소제목별/소제목 내 시간순/시간순/언론사순" 중 골라 화면에 나열되는 순서만 바꿔 볼 수 있는 보기 전환도 추가했다(둘 다 표시 전용, 저장된 분류·순서 자체는 바뀌지 않는다).
      - 화면 오른쪽 아래 ☰ 버튼으로 여는 "소제목 미니 목차" — 지금 화면의 소제목 이름을 목록으로 보여주고, 이름을 누르면 그 위치로 스크롤 이동한다. 소제목이 많아 하나씩 스크롤로 찾기 번거롭다는 피드백에서 나왔다. 각 항목 옆에도 작은 ▲▼가 있어 목차를 연 채로 소제목 순서를 바로 조정할 수 있고(위 헤더 ↑/↓와 동일한 결과), 조정 후에도 목차가 자동으로 다시 열려 있어 여러 소제목을 연달아 정리하기 쉽다.
      - 사용자가 만든 소제목의 점선 테두리 표시를, 기사가 없을 때만이 아니라 기사가 채워진 뒤에도 계속 유지하도록 바꿨다(자동 분류 소제목과 항상 구분되도록).
      - 같은 화면 안에 표시 이름이 같은 소제목이 둘 생기면(자동 분류에 이름표를 붙인 것과 새로 만든 소제목 이름이 우연히 같아지는 경우 등) 저장을 막는다. 다른 회차·다른 화면끼리 이름이 같은 것은 허용한다.
      - 네이버 API가 제공하는 요약이 사진 설명이나 문장 중간 등 이상한 지점에서 잘려 있을 때, 기사별로 "🔄 원문에서 다시 가져오기"(원문 페이지의 제목/요약으로 교체) 또는 "✏️ 직접 수정"(직접 타이핑)으로 고칠 수 있다. 자동 수집 속도에 영향 없도록 기본은 그대로 두고, 사용자가 필요하다고 판단한 기사만 수동으로 고치는 방식이다. 한 번 고치면 확정본·초안·실시간 현황·지난 기사 화면 전부에 반영된다.
      - 형광펜 단어를 설정 화면까지 가지 않고 화면 하단 🖍️ 팝오버에서 바로 추가·삭제하고, 등록된 단어 칩을 클릭하면 5가지 색 중 다음 색으로 바로 순환시킬 수 있다.
      - "+ 직접 키워드 작성하기"로 AI 추출 키워드와 별개인 자유 서식 메모 한 줄을 남길 수 있고, 저장하면 복사/`.txt`/텔레그램 전송 텍스트 헤더 바로 아래에도 포함된다. 값 하나를 초안·확정본이 공유해, 초안에서 적어두면 그 회차가 확정본이 될 때 그대로 이어진다.
      - 기사·소제목을 옮기면(단일 ↑/↓, 드롭다운 이동, 일괄이동, 미니 목차 순서 변경 모두 포함) 방금 옮긴 항목을 연두색으로 잠깐 표시한다 — 처음엔 "담아둔 기사 승격" 표시와 같은 노란색을 썼으나, 그 노란색이 이미 "승격됨"과 "초안에 새로 도착한 기사" 두 가지 의미로 쓰이고 있어 세 번째 의미까지 겹치는 게 혼란스럽다는 피드백으로 색을 분리했다.
      - 기사 줄의 "다른 소제목"/🔄/✏️/🗑️/↑↓ 묶음이 제목 길이에 따라 매번 다른 위치에 떠 보이던 것을, 제목 길이와 무관하게 항상 줄 오른쪽 끝에 고정되도록 정렬했다(소제목 헤더의 ✏️/🗑️/↑↓도 동일하게 오른쪽 정렬). 빈 소제목 카드의 위아래 여백도 실제 내용에 비해 과했던 것을 좁혔다.
  24. [추가: 2026-08-10] 확정본·초안 공통 화면 정리(사용자 실사용 피드백 모음):
      - **"지난 기사" 링크 위치**: 액션 툴바 안에 있던 걸 빼서 제목 줄 오른쪽(회색, 작은 글자)으로 옮겼다 — 복사·다운로드 같은 "이 화면의 데이터를 다루는 액션"과 "다른 화면으로 가는 링크"는 성격이 다르다는 이유. 초안 화면엔 애초에 이 링크가 없다.
      - **정렬 보기 방식(소제목별/시간순 등) 드롭다운 위치**: 액션 툴바 맨 오른쪽 끝으로 옮겼다(폭은 내용만큼만 짧게 유지) — 복사·다운로드·소제목 만들기 등 "액션"과 "화면 표시 방식을 바꾸는 옵션"은 다른 종류라는 이유로 시각적으로 분리했다.
      - **상단바 "실시간 현황" 위치 고정**: 화면마다 가운데/오른쪽을 오가며 위치가 달라지던 것을, 모든 화면에서 항상 가운데에 오도록 통일했다(나머지 한 링크가 오른쪽).
      - **"+ 직접 키워드 작성하기" 버튼 동작 개선**: (1) 버튼 라벨이 "+ 키워드 직접 작성"(메모 없을 때)/"키워드 수정"(메모 있을 때)으로 상황에 따라 바뀐다. (2) 메모가 이미 있으면 패널이 처음부터 열려있고 그 안에 자체 (수정)/(삭제) 버튼이 있어, 툴바 버튼은 메모가 없을 때만(패널을 열 방법이 필요한 경우) 보여준다 — 있을 때 툴바에도 "키워드 수정"을 또 두면 같은 기능이 중복돼 보인다는 이유. (3) 저장된 메모는 입력칸이 잠긴 채로 보여지며 (수정)을 눌러야 다시 편집 가능해진다("이미 저장됐다"는 상태를 입력칸 자체로도 보여줌). (4) 버튼을 눌러 패널을 열면 그 즉시 버튼 자신도 사라진다(패널이 이미 뜬 상태에서 버튼이 같이 남아있으면 안 된다는 피드백) — 취소하고 싶으면 "삭제"로 빈 값을 저장하면 버튼이 다시 나타난다. 라벨 앞의 "키워드 작성 :" 안내 문구는 없애고 입력칸이 그 자리까지 넓게 차지하도록 했다(placeholder 문구로 충분히 안내됨).
      - **설정 메뉴 아이콘 정리**: 메뉴 항목마다 있던 이니셜 배지(키/형/시/언/범/형/워/텔/메)를 전부 없앴다 — "형"이 형광펜 긋기·본문 형식 두 곳에 겹치는 등 항목이 늘수록 이니셜이 헷갈린다는 지적으로, 라벨 텍스트 자체가 이미 충분히 설명적이라 배지 없이 텍스트+화살표만 남겼다.
      - **지난 기사 화면 정비**: 이 화면에만 빠져 있던 게시 시각 스타일(작고 옅은 회색, 다른 화면과 동일)과 🗑️ 등 액션 버튼 오른쪽 정렬을 마저 적용했고, 상단바에도 "실시간 현황"/"스크랩 초안" 링크를 추가했다(예전엔 "홈" 하나뿐이었음).

2) 소제목 자동 분류
- 설명: 기사 요약을 분석해 소제목으로 자동 분류하고, 키워드와 요약을 추출
- AI가 꼭 지켜야 할 규칙:
  1. 소제목은 고정 카테고리가 아니라 매 회차마다 기사 요약(description)을 분석해 자동 생성 (담당자가 추후 검토하므로 완벽한 분류가 아니어도 무방), 최대 15개까지만 생성 [수정: 2026-08-20, 8→15 — 재정경제위 있는 날처럼 기사 40건 넘는 회차에서 8개로는 기타에 30~75%가 몰림(예: 61건 중 46건). 76건 회차를 상한 8·15로 각각 재보니 상한 15는 실제로 11개만 써서 기타가 23→16건으로 줄고 1건짜리가 사라졌다(상한을 채우려 억지로 안 쪼갬). 반대로 14건 회차는 상한 15에서 1건짜리 5→9개로 과분할이 재현돼, AI_RULES.md에 "묶음당 평균 기사 2건 이상, 미달이면 가까운 1건짜리끼리 합쳐라"는 밀도 규칙을 추가해 상한과 별개로 억제했다. HISTORY.md 참고] [이전: 2026-08-11, 5→8 — 사안이 많은 회차에서 한 소제목에 기사가 몰림. 실측: 61건 회차가 상한 5에서 [44,12,4,1]로 44건 덩어리가 생겼고, 8에서는 [25,21,8,7]로 갈렸다. 기사가 적은 회차의 과분할은 소제목 프롬프트의 "10건 미만이면 억지로 쪼개지 마라" 규칙이 막는다 — 7건 회차를 상한 5·8로 각각 돌려 결과가 동일함을 확인] — [수정: 2026-07-24] 분류 기준을 기사 제목에서 네이버 요약으로 변경. 제목은 언론사마다 표현이 제각각이라 같은 사건이어도 겹치는 단어가 잘 안 잡히는데, 요약문은 보도자료 내용을 비슷하게 옮기는 경우가 많아 공통 단어가 더 잘 드러남(요약이 비어 있으면 제목으로 대신함)
  2. [수정: 2026-08-11] 처음엔 별도 AI(LLM) API 없이 요약에 자주 나오는 단어를 묶는 규칙 기반 방식으로만 분류했으나("등을"·"방안"처럼 뜻이 옅은 단어가 그대로 소제목이 되는 문제가 실사용에서 계속 지적됨), 소제목 생성에 한해 Claude API(`app/llm_classifier.py`, 기본 모델 Haiku — `app/config.py` `LLM_MODEL` 한 줄로 교체 가능)를 도입했다. `.env`에 `ANTHROPIC_API_KEY`가 없거나, SDK 설치가 안 됐거나, 호출이 실패·타임아웃되면 예외 없이 조용히 기존 규칙 기반 분류로 되돌아간다(`app/classifier.py`) — 이 기능이 죽어도 앱은 예전과 동일하게 동작한다. 요약·키워드 추출(위 규칙 4·5)은 여전히 규칙 기반이다(LLM은 소제목 묶음만 담당).
      - **호출 시점 제한(비용 관리)**: 정기 회차 수집(collect_run)은 매번 자동으로 새로 분류해 그 결과를 회차 파일에 저장한다. 스크랩 초안(기능5) 화면은 그 회차에서 처음 열릴 때(기사 5건 이상일 때만) 딱 한 번 자동으로 분류하고, 그 이후로는 새로고침·기사 숨기기·소제목 이동/생성/이름변경/순서변경을 아무리 해도 다시 부르지 않는다 — 담당자가 수동으로 정리해둔 배치·이름을 자동 재분류가 소리 없이 날려버리는 걸 막기 위한 의도적 제약이다(재분류로 소제목 이름이 한 글자만 바뀌어도 그 이름을 키로 저장된 수동 이동·이름변경 기록이 전부 무효가 됨을 실측으로 확인). 그 사이 새로 들어온 기사는 기존 소제목을 건드리지 않고 화면 맨 아래 "📂 소제목 미분류" 임시 칸에 모아 두며, 복사/다운로드/전송 텍스트에는 이 임시 칸 이름이 나가지 않고 "기타"로 합쳐 내보낸다. 기사 URL 묶음을 키로 결과를 캐시해(디스크에도 저장되어 앱 재시작에도 유지) 같은 구성이면 재호출하지 않고, 기사가 "줄기만" 한 경우(예: 숨기기)는 기존 분류에서 빼는 것만으로 재사용하며, API 호출 자체가 실패해도 대신 쓸 수 있는 이전 분류가 있으면 그걸 우선 재사용한다(정말 대신할 게 없을 때만 규칙 기반으로 폴백).
      - **[변경: 2026-08-12] 확정본은 더 이상 화면을 열 때마다 재분류하지 않는다.** 회차 저장 시점의 소제목 구성을 그대로 읽어 보여주므로(화면과 복사/전송 텍스트가 항상 일치), 재분류는 담당자가 "🤖 전체 기사 재분류" 버튼을 직접 눌렀을 때만 일어난다 — 처음부터 다시 분류하는 되돌릴 수 없는 동작이라 항상 확인창을 거치고(수동으로 옮긴 기사 수를 알려줌), 결과는 회차 파일에 저장된다. 스크랩 초안은 재분류 버튼이 둘로 나뉜다: "🤖 미분류 배정"은 📂 소제목 미분류 칸의 기사만 골라 *기존* 소제목에 편입시키는 안전한 동작이라(기존 소제목 이름·구성은 그대로, 담당자가 만든 소제목은 목적지 후보에서 제외) 확인창 없이 바로 실행되고, "↻ 전체 기사 재분류"는 확정본과 같은 파괴적 동작이라 같은 확인창을 거친다. AI 분류가 시도됐다가 실패했고 대신 쓸 이전 분류도 없어 결국 규칙 기반으로 떨어진 회차는 확정본의 자동발송을 보류하고 "⚠️ AI 분류 실패" 경고를 화면에 표시해 담당자가 직접 확인 후 (발송)하도록 한다.
  2-1. [추가: 2026-08-11] 화면에는 AI가 만든 소제목에만 작은 "🤖" 배지를 붙여, 담당자가 자기가 손댄 소제목과 AI가 자동으로 묶은 소제목을 한눈에 구분할 수 있게 한다. ✏️로 이름을 바꾸면 배지가 사라지고(그 순간부터 담당자의 이름이므로), 규칙 기반으로 폴백한 회차에는 애초에 붙지 않는다. 이 배지는 **화면 전용**이라 복사/다운로드/텔레그램·이메일 발송 텍스트에는 절대 포함되지 않는다(소제목 이름 문자열이 아니라 별도 요소로 그린다).
  2-2. [추가: 2026-08-11] 담당자의 큐레이션은 "↩ 되돌리기"로 한 단계씩 되돌릴 수 있다(화면 왼쪽 아래 플로팅 버튼, 되돌릴 게 있을 때만 표시) — 기사 숨기기, 소제목 이동, 소제목 이름 변경, AI 재분류가 모두 대상이다. 여러 기사를 한꺼번에 숨긴 경우는 한 덩어리로 묶여 한 번에 되돌아간다. 최대 20단계, 자정에 초기화한다.
  3. 기사 1건은 소제목 1개에만 배정 (한 소제목에 여러 기사 포함 가능). [추가: 2026-07-24] 단, 담당자가 ↑/↓ 버튼으로 소제목 경계를 넘겨 수동으로 재배정할 수 있으며(규칙21), 이 수동 배정이 자동 분류보다 우선한다. [수정: 2026-07-30, 버그 수정] 경계를 넘겨 재배정된 기사는 항상 그 소제목 맨 끝에 고정되어 이후 ↑/↓로 순서를 조정해도 다시 화면을 그릴 때마다 맨 끝으로 되돌아가던 버그가 있었다 — 이제 다른 기사와 동일하게 "입력 순서 유지" 원칙을 그대로 따르도록 고쳐, 경계를 넘긴 뒤에도 자유롭게 순서를 조정할 수 있다
  4. **[삭제: 2026-09-10]** 전체 목록 맨 하단의 "🤖 AI가 추출한 주요 키워드" 블록(전체 스크랩 결과를 통틀어 뽑은 주요 키워드를 쉼표로 나열)을 없앴다 — 집계 대상이 이미 담당자의 검색어로 걸러진 기사라 빈도를 세도 검색어 주변 단어만 되돌려주고, 실제로 그렇게 나왔다(실측: 조사 잔재·숫자 토큰 11%, `부총리`·`부동산`이 회차의 12~13%에 상시 등장). 수시 모니터링이 2026-08-20에 같은 이유로 이 블록을 아예 넣지 않기로 한 판단(기능9 규칙5·ADHOC_DESIGN.md)을 정기에도 그대로 적용한 것이다. 칩을 눌러 "(AI 추출)" 그룹에 검색어로 등록하던 기능(2026-08-13)도 함께 없앴다 — 운영 중 그 그룹이 한 번도 만들어지지 않았다. 배경·실측은 HISTORY.md 같은 항목 참고
  5. 전체 목록 맨 하단에 "💬 AI가 읽은 소제목별 주요 요약" 항목을 두고, 소제목별로 요약을 각각 작성해 나열 (소제목 이름 + 요약문, 3문장 이내·**250자** 이내 — [수정: 2026-08-18] 문서에 150자로 남아 있었으나 실제 구현은 250자다. 문장 중간에서 자르지 않고 문장 단위로만 채운다)
  6. **[이름 변경: 2026-08-18, 구현 완료: 2026-08-18]** 이 화면의 이름을 "지난 기사 더보기"에서 **정기 보관함**으로 바꾼다(수시 보관함·라벨 보관함과 짝을 이루는 이름). 보관 기간도 7일 → **1년**으로 늘린다(기능11의 엑셀 내보내기가 이 보관함에서 이뤄지므로, 7일치만으로는 뽑을 게 없다) — **[구현 완료: 2026-08-18]** `RETENTION_DAYS = 365`. 화면은 이미 최근 2일치만 펼쳐 그리고 나머지 날짜는 접힌 채 두었다가 열 때 가져오는 구조라 기간을 늘려도 그대로 동작한다 — 1년치가 실제로 쌓였을 때 날짜 나열이 불편해지면 그때 표시 단위를 다시 설계한다(지금 미리 만들지 않는다). 이하 기존 규칙: [수정: 2026-07-23, 위치 수정: 2026-07-24] 화면에는 최신 회차 결과를 기본으로 표시하되, 화면 상단 버튼 영역(복사·내보내기·설정 옆)의 링크로 저장된 지난 회차를 날짜별 → 시간대별로 펼쳐볼 수 있음 (7일 보관 정책과 연동되어, 7일이 지나면 조회 목록에서도 사라짐). 최초 결정은 "지난 날짜 조회 기능 없음"이었으나 이후 요청으로 변경됨. 스크롤 없이 바로 접근할 수 있도록 화면 하단에서 상단으로 위치를 옮김(하단에는 대신 "🏠 메인으로 가기"를 배치). [수정: 2026-07-30] 처음엔 지난 회차를 재분류하지 않고 언론사 우선순위 순 평평한 목록으로만 보여줬으나(당시 화면 그대로 복원할 필요는 없다고 판단), 저장 시점의 소제목 구성을 각 기사에 함께 저장해두는 방식으로 바꿔 지난 회차를 열어도 그때 실제로 봤던 소제목별 구성이 그대로 복원되도록 개선했다(재분류하는 것이 아니라 저장해둔 결과를 그대로 보여주는 것). 이 기능 도입 이전에 저장된 지난 회차는 이 정보가 없어 계속 평평한 목록으로 대체 표시된다(하위 호환). 지난 회차는 여전히 읽기 전용이라 순서 조정·소제목 이동은 할 수 없다(🗑️ 숨기기만 가능)
  7. 소제목 안에서 기사는 언론사 우선순위 순(방송사 → 주요 언론사)으로 나열
  8. 첫 번째 소제목을 포함해 모든 소제목 그룹 위에 이름을 라벨로 표시 (라벨 없이 나열되는 그룹 없음). [추가: 2026-07-24] 각 소제목 옆 ✏️ 버튼으로 표시 이름을 직접 바꿀 수 있다. 분류 자체(어떤 기사가 어느 그룹인지)는 항상 자동 생성된 원래 단어 기준으로 이뤄지고, 이름표는 화면·복사/내보내기 텍스트에 보여줄 때만 마지막에 적용된다. 같은 단어가 나중에 다른 회차에서 소제목으로 다시 잡히면 이름표가 계속 적용되며, 원래 단어로 다시 바꾸면 이름표가 지워진다. [수정: 2026-08-12] 라벨을 자동으로 `<...>` 꺾쇠괄호로 감싸던 것을 없앴다 — 담당자가 직접 지은 이름을 앱이 임의로 장식하지 않고, 입력한 텍스트 그대로 보여준다(사용자 요청: "누군가는 다르게 지정할 수도 있어서")

3) 진입 화면 [추가: 2026-07-23]
- 설명: 앱을 실행하면 스크랩 화면 대신 먼저 뜨는 랜딩 화면
- AI가 꼭 지켜야 할 규칙:
  1. 화면 가운데에 로고 이미지를 넣는다 (로고 파일은 추후 제공, 파일이 없으면 자리만 비워두거나 대체 표시)
  2. 로고 아래 제목 표시 — [수정: 2026-07-25] "온라인 기사 모아보기"로 문구 변경
  3. 워드클라우드 형태로 표시 — 언급량이 많은 키워드는 글자를 크게, 적은 키워드는 작게 (의미 있는 통계가 아니라 직관적 눈요기용). [수정: 2026-07-24] "당일 주요 키워드"라는 설명 문구는 워드클라우드 자체로 직관적으로 알 수 있어 제거. 단어 추출 기준도 기사 제목에서 네이버 요약(description)으로 변경(기능2 규칙 1과 같은 이유). [수정: 2026-07-24] "당일 0시 이후 누적 기준"이라는 캡션과 함께, 실제로도 오늘 저장된 모든 회차를 url 기준 중복 제거해 합친 뒤 집계하도록 바꿈(기존엔 최신 회차 1개만 썼음). 접속할 때마다 새로 집계한다. [수정: 2026-07-25] 무궁화 연상 방사형 고정 배치는 폐기 — flex-wrap 기반의 평범한 태그 클라우드로 교체(검색 키워드가 아닌 단어 중 1위만 진한 헤더색·굵게, 나머지는 accent색, 빈도에 비례해 글자 크기만 차등). 캡션 문구도 "당일 0시 이후 현재까지의 누적 기준"으로, 스타일도 이태릭 대신 정자체·더 작은 글씨로 변경. 오늘 첫 스크랩 전(또는 키워드가 하나도 없을 때)에는 "💤"만 보임. [수정: 2026-07-25] 검색 키워드(등록해둔 인물명 등)도 더 이상 집계에서 제외하지 않고 포함하되, muted 회색으로 표시해 "언급량이 많아서 뜬 실제 화제어"와 구분한다 — 소제목 분류·기사 하단 "🤖 주요 키워드"는 여전히 검색 키워드를 제외(부서명이 소제목이 되면 기사 전체가 한 그룹으로 뭉치는 문제 방지). [추가: 2026-07-25] 설정 화면 "☁️ 워드클라우드 제외어"(검색 키워드 화면과 같은 방식, 최대 15개, AND/OR 없음)에 등록한 단어는 색 구분 없이 아예 집계에서 빠진다. [수정: 2026-09-11] 그 화면은 입력칸 목록 대신 칩(× 삭제, Enter·쉼표로 추가)이고, 아래에 오늘 워드클라우드에 뜬 단어를 순위대로 보여줘 누르면 바로 제외어로 올라간다(저장 뒤 홈 워드클라우드의 미리보기). [추가: 2026-08-10] 사용자가 워드클라우드에서 "방안·의견·발표·기준·확대·대책·대해"처럼 일상적인 단어가 정책 고유어(법률명·기관명 등)를 밀어낸다고 지적 — 처음엔 제외어 목록에 하나씩 등록했으나, "매번 손으로 관리하기 번거롭다"는 재지적으로 이 7개를 `app/tokenizer.py`의 공용 불용어 목록(STOPWORDS)으로 옮겼다(워드클라우드뿐 아니라 소제목 분류·기사 하단 "🤖 주요 키워드"에도 함께 적용). "실거주"는 정책 용어로 판단해 제외 대상에서 뺐다. [수정: 2026-08-10] "재정경제부가"처럼 이미 불용어/제외어로 등록된 단어에 조사 "가"만 붙은 잔재가 그대로 노출되는 문제 — "가"는 "경영평가" 같은 단어를 "경영평"으로 잘못 잘라내는 사고 때문에 일반 조사 제거 대상에서 빠져 있는데, "가"를 뗀 나머지가 이미 등록된 불용어와 정확히 일치할 때만 함께 걸러내는 규칙을 추가해 안전하게 해결했다(일반 단어는 전혀 건드리지 않음)
  3-1. **[설계 확정: 2026-08-18, 구현 완료: 2026-08-18] 진입 화면을 네 갈래 구조로 재구성한다.** 위에서부터 `실시간`(단독) / `정기 모니터링`(초안 → 확정본 → 정기 보관함) / `수시 모니터링`(새 수집 → 수집 결과 → 수시 보관함) / `라벨 보관함`(단독, 맨 아래) 순이며, 그 아래에 기존 워드클라우드가 온다. 세부:
      - 실시간을 정기 흐름에서 **떼어낸다** — 예전엔 `실시간 → 초안 → 확정본` 한 줄이라 실시간이 정기의 1단계처럼 읽혔는데, 실제로는 저장도 분류도 하지 않는 별개 화면이다
      - 흐름 사이 화살표 밑에 **"언제 넘어가는지"를 글자로 표기**한다(`회차 마감` / `다음 회차` / `수집` / `자정`). "자동"이라고만 쓰면 화살표가 이미 뜻하는 바라 정보가 없다. 담당자가 눌러야 넘어가는 곳은 한 군데도 없으므로 액션 이름(예: "지금 넘기기")은 여기 적지 않는다 — 진입 화면의 그 자리는 누르는 곳이 아니라 지도 표기이고, 실제 버튼은 [수집 결과] 화면 안에 있다(기능9)
      - **흐름마다 색을 하나씩 두고, 각 흐름의 마지막 칸(보관함)은 같은 색을 한 단계 흐리게** 해서 "지나간 것"이 공통으로 읽히게 한다. 라벨 보관함은 두 흐름을 가로지르는 별도 축이라 어느 쪽과도 겹치지 않는 색을 쓴다
      - `수집 결과` 칸에는 오늘 열려 있는 사안 수를 배지로 표시한다. 라벨 보관함 칸에는 상위 라벨 몇 개를 건수와 함께 미리 보여줘, 들어가지 않고도 무엇이 쌓였는지 알 수 있게 한다
  4. "👀" 링크로 실제 스크랩 결과 화면(index.html)으로 이동 — [수정: 2026-07-25] 텍스트를 이모지만 남기고 축약
  5. "설정" 링크로 설정 메뉴로 이동 — [수정: 2026-07-25] "⚙️ 설정"에서 이모지 없이 영문 "settings"로 변경(다른 설정 하위 페이지들과 통일)
  6. 하단에 "문의 📧 sweetgreetings@naver.com" 표시
  7. 앱 실행 시 브라우저는 이 진입 화면을 열며(PRD 4절), 여기서 다시 스크랩 화면으로 이동

4) 실시간 [추가: 2026-07-25, 이름 변경: 2026-08-18(구현 완료) — "실시간 기사 현황" → "실시간"]
- 설명: 예정된 회차 수집과 별개로, 열 때마다 지금까지의 네이버 검색 결과를 있는 그대로 보여주는 화면(`live.html`). 진입 화면에서는 정기 흐름에 속하지 않는 **단독 항목**으로 놓인다(기능3 규칙3-1)
- AI가 꼭 지켜야 할 규칙:
  1. 예정된 회차 수집(기능1)과 완전히 별개 파이프라인이다 — 요청이 올 때마다 등록된 키워드 그룹 **전체**(그룹의 "정기 스크랩 포함" 여부와 무관하게 항상 전체 그룹 OR)로 당일 0시~현재까지 재검색하며, 저장은 하지 않는다
  2. 언론사 화이트리스트를 적용하지 않고(전체 언론사), 소제목 분류도 하지 않는다 — 게시 순(최신순) 평평한 목록이며, 화면 전용 필터(체크박스)로 "스크랩 언론사만 보기"는 가능하다
  3. [수정: 2026-07-28] 사진/현장 기사 제외·완전 동일 제목 중복 제거는 기본적으로 적용하지 않는다(있는 그대로 보여주는 게 목적) — 설정 화면에서 두 개의 별도 체크박스로 각각 켤 수 있다(`live_exclude_photo`/`live_dedup_titles`, 둘 다 기본 꺼짐). 숨긴 기사(기능1 규칙19)도 걸러내지 않고 "🗑️ 숨김" 상태만 표시한다(원본을 그대로 비추는 화면이라는 성격)
  4. [추가: 2026-07-30] 새로고침할 때마다 당일 전체를 처음부터 다시 검색하지 않고, 마지막으로 확인한 시각 이후 것만 추가로 검색해 병합하는 증분 캐시를 쓴다(체감 51초→0.9초 개선). 검색이 실패해도 캐시가 있으면 마지막 성공 결과를 그대로 보여준다. 키워드 그룹 구성이 바뀌면 캐시가 자동으로 무효화된다. [수정: 2026-08-06] 위 캐시는 "이미 본 것 이후만" 다시 검색하는 것일 뿐, 캐시가 없는 최초 진입(하루 중 처음 열 때, 또는 키워드 구성을 바꾼 직후)엔 여전히 등록된 키워드를 전부 새로 검색해야 한다 — 이 전체 검색이 등록된 키워드 수만큼 하나씩 순서대로 이뤄지고 있어(실측 약 19초, "재정경제부"·"구윤철"처럼 그날 기사가 많은 키워드일수록 오래 걸림), 여러 사람이 "실시간 현황이 느리다"고 제보한 원인이었다. `app/naver_api.py` `search_articles_by_groups`를 동시에 최대 5개까지 병렬로 검색하도록 고쳐 같은 검색이 약 5초로 줄었다(전체 시간은 가장 느린 키워드 하나가 정한다 — 5보다 더 늘려도 이 이상은 안 줄어듦). 네이버 API의 초당 호출 제한에 더 쉽게 걸릴 수 있어(실측으로 429 오류 확인), 키워드 하나가 실패해도 나머지는 그대로 살리고 그 키워드만 빈 결과로 대체하도록 함께 고쳤다
  5. 기사마다 "📌 담아두기" 버튼으로 당일 한정 임시 목록(`manual_articles.json`)에 보관할 수 있고, 여기 담긴 기사는 스크랩 확정본·초안 화면 하단에 "📌 담아둔 기사" 구획으로 노출된다(화면엔 보이되 복사/내보내기·워드클라우드에서는 제외) — [변경: 2026-08-11] 처음엔 "📝 초안에 포함"/"📗 확정본에 포함" 이모지 버튼 두 개였으나, 어느 소제목으로 들어가는지 알 수 없어 승격한 기사를 되찾기 어렵다는 문제로 지금 보고 있는 화면의 소제목 드롭다운 하나로 바뀌었다 — 초안에서 고르면 초안, 확정본에서 고르면 확정본의 그 소제목으로 즉시 편입된다(화면을 넘나드는 승격은 없앰; 담당자가 직접 만든 소제목은 목적지 후보에서 제외)
  6. [추가: 2026-07-29] 담아둔 적도, 정식 스크랩에 반영된 적도 없지만 지금 진행 중인 회차가 자동으로 골랐을 것으로 보이는 기사는 "🤖 자동 선별"로 표시해(추정치이며 그룹 AND 모드·제목 중복 제거까지는 재현하지 않는 근사값) 이미 자동으로 잡힐 기사를 일부러 손대지 않아도 된다는 걸 알려준다
  7. 🗑️(숨기기)는 기능1 규칙19와 동일한 전역 숨김 기록을 공유한다

5) 초안 [추가: 2026-07-27, 이름 변경: 2026-08-18(구현 완료) — "스크랩 초안" → "초안", 확정본도 "스크랩 확정본" → "확정본"]
- 설명: 다음 예정 회차가 실행되기 전, 지금까지 수집된 내용을 정식 회차와 동일한 파이프라인으로 미리 보여주는 화면(`preview.html`). 정기 모니터링 흐름의 1단계이며, 회차가 마감되면 **자동으로** 확정본이 된다 — 별도의 저장 버튼도, 넘기기 버튼도 없다(담당자가 고치는 즉시 저장된다)
- AI가 꼭 지켜야 할 규칙:
  1. 정식 회차(기능1)와 동일한 파이프라인(등록된 키워드 그룹 중 "정기 스크랩 포함"만, 언론사 화이트리스트, 사진/인사 기사 제외 설정, 완전 동일 제목 중복 제거, 소제목 자동 분류)을 그대로 거치되, 저장은 하지 않고 요청마다 다시 계산한다 — "지금 정식 회차가 끝난다면 어떤 모습일지"의 미리보기이기 때문
  2. 여기서 🗑️ 숨기기·↑/↓ 순서 조정·소제목 이름 바꾸기를 하면 정식 회차와 공유하는 전역 저장소(`hidden_articles.json`/`group_overrides.json`/`group_labels.json`)에 그대로 기록되어, 별도의 "저장" 버튼 없이도 다음 정식 회차 자동 실행 시 그대로 반영된다
  3. [수정: 2026-07-30, 버그 수정] 같은 소제목 "안"에서의 순서 조정은 처음엔 저장할 곳이 없어(정식 회차처럼 "저장된 회차"가 없으므로) 새로고침하면 사라졌다 — 별도 저장소(`preview_order.json`, 당일 자정 또는 정식 회차 저장 시 소멸)를 만들어 저장하도록 고쳤다. 이제 같은 소제목 내 재정렬도 새로고침 후 유지되고, 정식 회차가 저장되는 순간 그 순서가 그대로 이어받아진다
  4. [추가: 2026-07-30] 기능4의 증분 캐시와 동일한 방식(캐시 기준은 "지금 진행 중인 회차")으로 반복 재계산 비용을 줄인다. ↑/↓ 이동은 페이지 전체를 다시 불러오지 않고, 서버가 이동 계산에 쓴 결과를 화면 조각(JSON)으로 그대로 돌려줘 그 부분만 갈아끼운다(예전엔 이동 1번에 검색이 2번 돌았던 것을 1번으로 줄임)
  5. 기능1 규칙21의 체크박스 일괄 이동·"다른 소제목으로" 드롭다운·"+ 새 소제목 만들기"를 확정본과 동일하게 지원한다
  6. "📌 담아둔 기사"(기능4 규칙5) 구획을 확정본과 동일하게 보여주며, 소제목 드롭다운으로 편입시키면(규칙5 참고) 아직 저장된 회차가 없어 예약 목록(`draft_pending_articles.json`)에 담아두고, 정식 회차가 실제로 저장될 때 합류시킨다(그 전까지도 초안 화면의 소제목 분류에는 바로 반영됨)
  7. 지난번 이 화면을 봤을 때는 없다가 새로고침 사이 새로 들어온 기사는 옅은 노란 배경으로 표시한다(브라우저에 저장해둔 이전 방문 시점의 URL 목록과 비교, 브라우저별 로컬 기준)
  8. 오늘 예정된 회차가 모두 끝났으면 안내 화면으로, 네이버 검색 실패 시 오류 안내 화면으로 대체한다(둘 다 저장하지 않음)
  9. [삭제: 2026-08-05] 화면 자체의 "🔄 새로고침" 버튼은 없앴다 — 이 화면은 자동 새로고침이 없어 넣어뒀던 것인데, 브라우저 자체 새로고침으로 충분하고 헤더만 불필요하게 복잡해진다는 피드백에 따라 제거했다.

6) 텔레그램 전송 [추가: 2026-08-03]
- 설명: 정기 스크랩 확정본을 텔레그램으로 자동 전송하거나, 초안 화면에서 수동으로 즉시 전송할 수 있는 기능
- AI가 꼭 지켜야 할 규칙:
  1. `.env`의 `TELEGRAM_BOT_TOKEN`으로 인증한다 — 네이버 키와 달리 선택 사항이라 없어도 앱 실행에는 지장 없으며, 설정 화면 "📤 텔레그램 전송"에서 설정돼 있는지를 상태 문구로 보여준다
  2. 설정 화면 체크박스("정기 스크랩 완료 시 텔레그램으로 자동 전송", 기본 꺼짐)를 켜면, 정기 회차(기능1)가 화면에 실제로 반영될 때마다(0건이라 화면 갱신을 건너뛴 경우는 제외) 확정본과 동일한 형식의 텍스트를 텔레그램으로도 보낸다 — [대체: 2026-08-10] "수집되면 곧바로 전송"하던 이 규칙은 전송 유예(기능8)로 완전히 대체됐다. 이 체크박스 자체는 코드에 남아있지 않다
  3. 스크랩 초안·확정본 화면에는 이 설정과 무관하게 항상 켜져 있는 "Telegram" 버튼이 있어, 지금 화면에 보이는 내용을 그대로 텔레그램으로 즉시 전송할 수 있다 — [대체: 2026-08-10] 이 개별 "Telegram" 버튼은 기능8의 통합 (발송) 버튼으로 대체됐다(이메일과 한 번에 같이 보냄)
  4. 텔레그램 메시지 1건당 글자 수 제한(4096자)을 넘으면 줄바꿈 경계에서 여러 메시지로 나눠 보낸다
  5. 미설정이거나 전송에 실패해도 예외를 던지지 않고 조용히 건너뛴다(로그만 남김) — 네이버 API와 달리 재시도하지 않으며, 텔레그램 장애가 정기 스크랩·화면 갱신 자체를 막지 않는다
  6. [변경: 2026-08-07] 챗 아이디 1개(.env) 고정 방식을 이메일(기능7)과 같은 "여러 받는 사람 + 켜고 끄기 토글" 방식으로 바꿨다 — 설정 화면에서 이름·chat id를 등록하고 각자 켜고 끌 수 있다(최대 15명). 기존에 `.env`에 있던 `TELEGRAM_CHAT_ID`는 최초 1회 "나"라는 이름으로 자동 등록되어 데이터 손실이 없다. chat id를 처음 확인하는 법(봇과 대화 시작 → `getUpdates` 호출)도 설정 화면에 안내 문구로 추가했다

7) 이메일 전송 [추가: 2026-08-06]
- 설명: 정기 스크랩 확정본을 이메일로 자동 전송하거나, 초안·확정본 화면에서 수동으로 즉시 전송할 수 있는 기능. 텔레그램과 달리 여러 명에게 동시에 보낼 수 있다(휴가 등으로 다른 사람이 대신 받아야 하는 경우를 위해 요청됨)
- AI가 꼭 지켜야 할 규칙:
  1. `.env`의 `EMAIL_SMTP_HOST`/`EMAIL_SMTP_PORT`(기본 587)/`EMAIL_SENDER_ADDRESS`/`EMAIL_SENDER_PASSWORD`로 인증한다 — 텔레그램과 같은 이유로 선택 사항이라 없어도 앱 실행에는 지장 없으며, 설정 화면 "📧 이메일 전송"에서 네 값이 모두 설정돼 있는지를 상태 문구로 보여준다. SMTP 서버를 특정 업체로 고정하지 않아 구글·네이버 등 어떤 메일이든 쓸 수 있다. `EMAIL_SENDER_PASSWORD`는 일반 로그인 비밀번호가 아니라 각 메일 서비스가 발급하는 "앱 비밀번호"를 쓴다(2단계 인증 필요)
  2. 받는 사람은 텔레그램(챗 아이디 1개 고정)과 달리 여러 명을 등록할 수 있다(최대 `MAX_EMAIL_RECIPIENTS`=15명) — 설정 화면에서 이름·이메일 주소를 입력해 추가하고, 각 사람마다 켜고 끄기(ON/OFF) 토글이 있어 지우지 않고도 잠깐 전송 대상에서 뺄 수 있다(예: 휴가 기간에만 다른 사람 켜두고 본인은 꺼두기)
  3. 설정 화면 체크박스("정기 스크랩 완료 시 이메일로 자동 전송", 기본 꺼짐)를 켜면, 정기 회차(기능1)가 화면에 실제로 반영될 때마다(텔레그램과 동일 조건) 확정본과 동일한 내용을 켜져 있는 받는 사람 전원에게 각각 개별 발송한다(받는 사람끼리 서로의 주소가 노출되지 않도록 한 통씩 보냄) — [대체: 2026-08-10] 텔레그램과 동일하게 전송 유예(기능8)로 대체됐다
  4. 스크랩 초안·확정본 화면 모두에 이 설정과 무관하게 항상 켜져 있는 "Email" 버튼이 있어, 지금 화면에 보이는 내용을 그대로 즉시 전송할 수 있다(텔레그램의 "Telegram" 버튼과 나란히 배치) — [대체: 2026-08-10] 이 개별 "Email" 버튼도 기능8의 통합 (발송) 버튼으로 대체됐다
  5. 메일 제목은 본문 첫 줄("언론 모니터링 [시각] 기준")을 그대로 쓰고, 본문은 기사 URL이 실제 클릭 가능한 링크가 되도록 HTML 형식으로 보낸다 — [수정: 2026-08-06] 처음엔 일반 텍스트로 보냈는데 일부 메일 앱(네이버 메일)에서 URL이 클릭 안 되는 글자로만 표시된다는 제보로, URL만 `<a>` 링크로 감싸고 나머지는 줄바꿈·공백을 그대로 유지하는 HTML로 바꿨다
  6. 미설정이거나 받는 사람이 하나도 없거나 전송에 실패해도 예외를 던지지 않고 조용히 건너뛴다(로그만 남김, 텔레그램과 동일한 이유) — 받는 사람이 여럿일 때 일부 주소로만 실패해도 나머지에게는 계속 보낸다
- [변경: 2026-08-06] 위 기능 구현에 맞춰 액션 툴바 버튼 이름을 정리했다 — "txt로 저장" → "다운로드", "📤 Telegram 전송" → "Telegram"(이모지 제거, "전송" 생략)로 바꾸고 그 옆에 같은 톤의 "Email" 버튼을 추가했다. [대체: 2026-08-10] "Telegram"/"Email" 액션 툴바 버튼은 기능8의 전송 유예 도입으로 완전히 없어졌다 — 확정본 화면의 플로팅 (발송) 버튼 하나가 두 채널을 모두 대신한다

8) 전송 유예 (담당자 검토 후 발송) [추가: 2026-08-10, 구조 단순화: 2026-08-11]
- 설명: 담당자가 회차 완료 시각까지 검토를 못 마쳐도 텔레그램·이메일이 검토 없이 그대로 나가는 걸 막기 위한 안전장치. "수집되면 곧바로 화면 반영 + 자동 전송"이었던 기존 방식(기능6 규칙2, 기능7 규칙3)을 "수집 → (사람 확인 후) 전송" 흐름으로 바꿨다. 담당자가 손 놓고 있으면 일정 시간 뒤 시스템이 스스로 전송해, 결과적으로 기존과 같은 완전 자동 동작도 그대로 가능하다.
  - [수정: 2026-08-11] 최초 설계는 "수집 → 확정 대기 → (확정) → (전송)" 2단계였다(수집된 회차가 곧바로 확정본이 되지 않고, 초안 화면이 별도의 "확정 대기" 상태를 보여주며, 파란 (확정) 버튼을 눌러야 확정본이 됐다). 실사용 중 문제가 드러나 이 (확정) 단계를 통째로 없앴다: (1) 확정해도 확정본에서 계속 숨기기/이동/수정이 가능해, 애초에 되돌릴 수 없는 동작이 아닌데 그걸 지키는 척하는 단계였다. (2) 확정 대기 중엔 초안 화면이 "다음 회차 미리보기"가 아니라 "확정 대기 화면"을 보여줘서, 담당자가 "회차 시각이 지났는데 왜 다음 회차로 안 넘어가지?"라고 실제로 혼란스러워했다(사용자 직접 제보). 이제 회차는 수집되는 즉시 확정본이 되고, 담당자에게 남는 유일한 수동 조작은 아래 (발송)뿐이다. 같은 이유로 "회차 종료 30분 전 지금 마감하기(=확정) 버튼"도 함께 없앴다 — 온라인 기사 특성상 회차 시간창이 닫히기 전에 미리 닫아버리면 남은 구간 기사가 유실되거나 초안·확정본에 중복으로 뜨는 문제가 근본적으로 있었다.
- AI가 꼭 지켜야 할 규칙:
  1. 회차가 수집되면 곧바로 확정본 화면에 반영된다(별도의 확정 단계 없음). 확정본 화면 오른쪽 아래 진한 그린(`#15803D`) 원형 **(발송)** 버튼을 눌러야 실제로 텔레그램·이메일(설정에서 켜둔 채널만)로 나간다 — 예전의 개별 "Telegram"/"Email" 버튼은 이 하나의 (발송) 버튼으로 통합됐다
  2. 회차가 수집된 시각(run_at)으로부터 `CONFIRM_SEND_GRACE_SEC`(기본 300초=5분) 안에 담당자가 (발송)을 누르지 않으면, 시스템이 그 시점에 스스로 발송한다(확정본 화면에는 아직 한 번도 안 보냈으면 그 마감까지 "⏱️ MM:SS 후 자동 발송"이 빨간 글자로 뜬다 — 순수 표시용, 실제 판단은 서버가 함)
  3. 카운트다운이 0에 도달하면 화면(탭이 열려있는 경우)엔 우선 파란 글자로 "자동발송 완료되었습니다."가 낙관적으로 뜨고, 그 뒤 화면을 다시 열면 서버가 실제로 기록한 결과(회색 배지, 규칙 5)로 정확히 바뀐다 — 실제 자동 발송은 탭이 닫혀 있어도 서버(스케줄러)가 그대로 수행한다
  4. 이미 한 번 이상 발송한 회차를 다시 발송하면(확정본에서 손본 뒤 재발송) 제목 앞에 "(수정)"을 자동으로 붙인다
  5. [추가: 2026-08-10] 이 회차의 발송이 담당자가 직접 누른 것인지, 담당자가 반응하지 않아 시스템이 대신 처리한 것인지 구분해야 한다는 요구 — 실제로 시스템이 자동으로 발송했으면(담당자가 직접 누르지 않았으면) (발송) 버튼 바로 위, 카운트다운이 있던 그 자리에 회색 "🤖 HH시 MM분에 자동 발송 완료" 문구를 대신 보여준다. 담당자가 직접 눌러 보냈을 때는 이 자리를 평소처럼 비워둔다(조용한 기본 상태 유지)

9) 수시 모니터링(비정기 스크랩) — [설계 확정: 2026-08-13, 구현: 2026-08-13]
- 설명: 위 기능1~8은 모두 "시간대"가 축이다(정해진 스케줄에 맞춰 등록된 키워드로 자동 수집). 그런데 담당자 업무에는 다른 축이 있다 — 상위자가 "이거이거 검색해와" 하고 지시하는 경우(예: "재경위가 3시부터 열려서 4시쯤 끝나니, 후속 기사가 나올 2시 30분~4시 구간만 따로 모아서 보고해야겠다")나, 갑자기 현안이 생겨 그 사안에만 집중해야 하는 경우다. 정기 스크랩과는 완전히 별개의 메뉴로 둔다. 이 항목은 논의를 통해 설계만 확정된 상태이며, 아직 코드가 없다. 세부 규칙(저장 구조·화면·동작 규칙·구현 전 준비물)은 [ADHOC_DESIGN.md](ADHOC_DESIGN.md)에, 결정 배경과 기각한 대안은 [HISTORY.md](HISTORY.md) "수시 모니터링(비정기 스크랩) — 설계 논의"에 있다. 여기서는 PRD 형식에 맞춰 확정된 규칙만 요약한다.
- AI가 꼭 지켜야 할 규칙:
  1. 담당자가 **사안명·검색어(최대 5개, `하나라도 있으면`/`모두 있는 기사만` 중 선택 — '모두'는 3개까지)·수집 시간대(임의 지정, 분 단위 자유)**를 직접 입력하고 [수집]을 눌러야 실행된다 — 자동 스케줄 없음, 정기 키워드 그룹과 완전히 별개(정기 검색어를 함께 검색하는 옵션도 두지 않는다). 네이버 검색 계층(`search_articles_by_groups`)은 정기와 공유하되, 언론사 화이트리스트는 기본 꺼짐(정기와 반대 — 상위자 지시는 보통 전체를 다 보고 싶어 하는 경우다), 언론사 정렬 순서·형광펜 단어·기사 제목 형식·소제목 형식은 정기 설정을 그대로 상속한다. 동일 제목 중복 제거는 정기와 동일하게 항상 적용
  2. 수집 종료 시각이 현재 시각보다 미래이면 입력을 차단한다(자동 보정 없음 — 받지 않은 시간대 데이터를 받았다고 착각하게 되는 걸 막기 위함). 시작이 종료보다 늦어도 차단한다
  3. **2단계**로 동작한다: [카드: 수집·정리] → [보관: 지난 사안으로]. 정기의 "초안" 단계가 없는 이유는, 초안은 "아직 수집이 안 끝나서" 존재하는데(회차 마감을 시계가 정함) 수시는 [수집]을 누르는 순간 이미 닫힌 과거 구간이라 "수집 중" 상태 자체가 없기 때문이다. 시간대를 바꿔 다시 [수집]을 누르면 병합만 한다 — **기존 기사(숨김·순서·소제목 배정 포함)는 절대 지우거나 되돌리지 않고**, 새 기사만 추가한다. 시간창이 좁아져 구간 밖이 된 기사도 자동 삭제하지 않고 "⚠️ 구간 밖" 표시만 한다
  4. 저장은 **카드 하나 = 파일 하나**로 자기완결한다(`data/adhoc/cards/{id}.json`) — 정기가 쓰는 전역 큐레이션 파일(숨긴 기사·소제목 배정·순서·이름·메모 목록)은 하나도 건드리지 않는다. 사안(issue) 아래 여러 회차(카드)가 쌓이는 구조로, 사안이 같으면 지난 사안 목록에서 한데 묶여 추이를 볼 수 있다
  5. 소제목 자동 분류는 기능2와 같은 LLM을 재사용하되 **자동 호출은 0회**, 담당자가 🤖 버튼을 눌렀을 때만 실행되고(비용 자기조절), 최대 5개까지만 생성한다(정기는 15개[2026-08-20, 8→15] — 사안이 한정적이라 수시는 더 늘리지 않는다). 애매한 기사는 기능2와 동일하게 "기타"로 보낸다. 요약도 같은 호출로 함께 생성되며(추가 비용 없음) **소제목 아래가 아니라 페이지 맨 아래에 한데 모아 보여준다** — 담당자가 제목만 보고 흐름을 훑어야 하는 화면이라 소제목 바로 아래 요약이 있으면 스크리닝을 방해한다는 판단. 검색어를 담당자가 직접 정했으므로 "AI가 추출한 주요 키워드" 블록은 넣지 않는다
  6. **[폐기: 2026-09-02]** ~~실시간 기사 현황(기능4)의 "📌 담아둔 기사"를 초안·확정본과 동일하게 수시 카드에도 띄운다 — 단, 정기 승격이 목록에서 빼는(pop) 것과 달리 수시로 넣을 때는 **복사(copy)**한다(넣어도 담아둠에 그대로 남아, 같은 기사를 정기 확정본에도 넣을 수 있다). 카드로 넣을 때 정기의 전역 소제목 저장소는 절대 건드리지 않는다~~ — 끝내 구현하지 않았고, 반대 방향(수시 → 정기)으로 잠깐 만들었던 `📌` 버튼도 2026-09-02에 기능째 없앴다. **지금 정기-수시 사이에 기사가 오가는 통로는 하나도 없다.** 근거는 ADHOC_DESIGN.md §6.4 — 정기의 `📌`는 실시간현황에서 확정본으로 건너가는 **회차 경계의 다리**인데, 회차도 마감도 없는 수시엔 건널 강 자체가 없다. 배경은 HISTORY.md "수시 → 정기 담아두기 › 기능째 제거" 참고
  7. **발송 기능 없음** — 복사·다운로드(.txt)만 제공한다. 헤더는 `수시 모니터링 [수집 종료 시각] 기준([사안명])` 형식으로, 시각은 반올림 없이 실제 종료 시각을 그대로 쓴다(담당자가 필요하면 텍스트 상태에서 직접 고친다). 메모(자유 서식 한 줄)가 있으면 헤더 아래 한 줄 추가된다. 저장 파일명은 기능1 규칙11·기능11 규칙6과 같은 형식(`날짜_수시_사안명`)을 따른다
  8. 수집 실패 시 **자동 재시도 없이 즉시 에러를 보여주고** [다시 시도] 버튼을 제공한다(정기는 5분 간격 최대 3회 자동 재시도 — 수시는 담당자가 화면 앞에 대기 중이므로 다르게 처리). 이미 모아둔 기사는 실패해도 그대로 유지된다
  9. **[수정: 2026-08-18, 구현 완료: 2026-08-18]** 보관 기간은 **1년**으로 정기 보관함(기능2 규칙6)과 맞춘다 — 예전엔 "제한 없음(무기한)"이었으나, 두 보관함의 정책이 서로 다르면 담당자가 "어느 쪽이 언제까지 남는지"를 따로 기억해야 한다. 아카이브 성격은 그대로이며, 1년 안에도 삭제는 사람이 직접 확인 후에만 한다. 라벨이 붙은 기사는 이 삭제에서 예외다(기능10 규칙6) — **[구현 완료: 2026-08-18]** `ADHOC_RETENTION_DAYS = 365`, `app/adhoc/card.py` `delete_expired_cards()`. 실삭제 대신 `data/adhoc/trash/`로 이동(작업 규칙)
  12. **[설계 확정: 2026-08-18, 구현 완료: 2026-08-18] 3단계 흐름과 이름 정리.** 화면 이름을 `새 수집` → `수집 결과` → `수시 보관함`으로 통일한다("카드 정리"→"수집 결과", "지난 사안"→"수시 보관함"). 분류 기준은 **사안 › 일자**다
      - **[수집 결과]에 들어가면 오늘 카드 중 가장 최근에 손댄 것을 바로 열고, 위에 사안명 탭을 얹는다.** 하루에 여러 사안을 볼 수 있으므로(예: 오늘 "재경위"도 보고 "민생물가"도 봄) 오늘 카드가 여러 개일 수 있는데, 여기에 목록 화면을 한 겹 더 두면 수시 보관함과 겹치고 카드가 하나뿐인 날엔 헛클릭이 된다. 탭이면 하나일 때는 탭도 하나라 거슬리지 않는다. 오늘 카드가 하나도 없으면 [새 수집]으로 보낸다
      - **같은 사안을 하루에 여러 번 = 카드 1개 + 재수집**, **다른 사안 = 카드 별개**. [수정: 2026-08-20] 재수집은 **병합이 아니라 교체**다(ADHOC_DESIGN.md §6.4에서 규칙이 뒤집혔다) — 목록은 언제나 "지금 이 카드의 조건으로 불러올 수 있는 기사 전부"여야 하기 때문. 다만 **교체이지 삭제가 아니다** — 조건에서 벗어난 기사도 카드에서 지우지 않고 화면에서만 걸러내므로, 조건을 되돌리면 소제목 배정·순서까지 그대로 돌아온다. 재수집할 때는 **수집 종료 시각을 현재까지 자동으로 연장한다** — 아침에 잡아둔 시간창 그대로 다시 긁으면 그 사이 나온 기사가 하나도 안 들어와 "재수집했는데 아무것도 안 늘어나는" 일이 반복된다. 화면에는 창이 어떻게 넓어지는지 함께 보여준다
      - **자정에 카드가 수시 보관함으로 자동으로 넘어간다.** 담당자가 아무것도 누르지 않아도 된다. **[미구현: 2026-08-20 확인]** 먼저 정리를 끝냈을 때 [수집 결과] 화면 오른쪽 아래 플로팅 [지금 넘기기] 버튼으로 앞당기는 부분은 아직 만들지 않았다 — 자정 자동 이월만 동작한다. 이 버튼에는 확인창을 두지 않는다 — 보관함에 들어간 뒤에도 계속 고칠 수 있어(아래) 잘못 눌러도 잃는 게 없다. 왼쪽 아래에 이미 있는 ↩ 되돌리기·🗑 숨긴 기사와 같은 스택에 붙이지 않는다(그 둘은 정리하며 계속 누르는 것이고 넘기기는 한 번 누르고 떠나는 것이라, 붙여두면 정리 중에 오발한다)
      - **보관함에 들어간 뒤에도 카드는 계속 고칠 수 있다**(숨기기·순서·소제목 이동 모두). 이게 성립해야 자정 자동 이관이 담당자의 진행 중인 작업을 잠가버리지 않는다
      - 기존 **자정 잠금(어제 카드는 재수집 불가)은 그대로 유지한다.** "어제 사안을 오늘 이어서 더 긁기"는 같은 사안 아래 **새 카드**를 만들어서 한다(사안에 지난 검색어가 저장돼 있어 자동으로 채워진다). 여러 날에 걸친 시간창을 허용하지 않는 이유는 기능1 규칙2의 네이버 1000건 상한 때문이다 — 창이 넓어질수록 조용히 잘릴 위험이 커지는데, 그 잘림은 실패로 잡히지도 않는다. 분류 기준이 '사안 › 일자'라 카드가 이틀을 걸치면 일자 자체가 모호해지는 문제도 함께 작용했다
  10. 워드클라우드(기능3)에는 반영하지 않는다(임시 검색어가 당일 집계를 오염시킴). 지난 기사 더보기(기능2 규칙6)에도 편입하지 않고 별도의 "지난 사안 더보기" 화면(사안 → 회차 2단 구조)을 둔다
  11. 1차 범위에서 제외: ⋯더보기의 원문 재수집/제목 수정, 카드→정기로 기사 보내기, 사안 병합, 별도 발송 수신자 ([수정: 2026-08-20] ↩ 되돌리기는 이 목록에서 빠졌다 — `app/adhoc/undo.py`로 구현 완료, 카드별 최대 10단계)(자세한 사유는 ADHOC_DESIGN.md 7절)

10) 라벨 — [설계 확정: 2026-08-18, 구현 완료: 2026-08-18]
- 설명: 기사 한 건에 담당자가 직접 붙이는 자유 입력 태그. 소제목(기능2)과는 목적이 다르다 — **소제목은 "이 회차에서 어느 묶음이었나"로 회차 안에서만 의미가 있고, 라벨은 "회차와 상관없이 내가 나중에 꺼내볼 것"이라 회차를 가로지른다.** 블로그 태그에 가깝다. 담당자가 정하기 나름이라 '세제'처럼 정책 용어일 수도, '보고서'처럼 "나중에 보고서 쓸 때 쓸 기사"라는 용도 표시일 수도 있다
- AI가 꼭 지켜야 할 규칙:
  1. 담당자가 **직접 입력**한다(AI가 자동으로 붙이지 않는다). 기사 한 건에 **여러 개** 붙일 수 있다
  2. **URL을 키로 하는 전역 저장소**에 둔다 — 정기에서 붙인 라벨이 수시에서도 보이고, 그 반대도 같다. 이는 "수시는 정기의 전역 큐레이션 파일을 하나도 건드리지 않는다"(기능9 규칙4)의 예외가 아니라 **경계를 다시 그은 것**이다: 소제목·숨김·순서는 "이 회차 보고서에서 이 기사를 어떻게 배치할까"라는 **회차에 매인 판단**이라 흐름끼리 격리해야 하지만, 라벨은 **기사 자체에 붙는 것**이라 격리할 이유가 없다. 기사 제목 교정(`summary_overrides`)이 이미 같은 이유로 전역인 것과 같다
  3. **보고서 텍스트에는 절대 나가지 않는다.** 화면과 내보내기 파일(기능11)에만 표시하고, 복사·`.txt` 다운로드·텔레그램·이메일 발송 텍스트에는 포함하지 않는다 — 라벨은 담당자 개인 메모지 보고 내용이 아니다(이걸 막지 않으면 '보고서' 라벨을 붙였다는 이유로 보고서에 "보고서"라는 글자가 박힌다). 소제목 🤖 배지(기능2 규칙2-1)와 같은 "화면 전용 표시" 원칙
  4. 입력할 때 **이미 쓴 라벨 목록을 함께 보여준다**(새로 타이핑하는 것도 가능). 자유 입력만 두면 '세제'/'세제개편'/'세재'로 갈라져 라벨별 보기가 무너진다
  5. **라벨 보관함**(기능3 규칙3-1): 정기·수시를 가로질러 라벨이 붙은 기사만 모아 보는 별도 화면. 라벨 칩 목록(건수 포함)을 보여주고, 고르면 그 라벨이 붙은 기사 목록이 뜬다. 숨긴 기사(기능1 규칙19)도 여기서는 빼지 않고 "🗑️ 숨김" 표시만 단다 — 숨김은 "보고서에서 뺀다", 라벨은 "나중에 본다"로 목적이 다르기 때문(실시간 화면이 숨긴 기사를 다루는 방식과 동일). **[추가: 2026-09-15, 구현 완료: 2026-09-15]** 목록은 **발행 최신순**이고, 다른 보관함처럼 **복사·`.txt`·엑셀** 세 가지로 내보낼 수 있다. 복사·`.txt`는 소제목으로 나누지 않은 평평한 목록이고, 첫 줄은 고른 라벨 이름에 "소제목 형식"을 입힌 것(예: `<세제>`)이며, 기사 줄은 "기사 제목 형식"을 그대로 따른다
  6. **라벨이 붙은 기사는 1년 자동 삭제(기능2 규칙6·기능9 규칙9)에서 예외다.** 지금 삭제는 회차 파일을 통째로 지우는 방식이라 "기사 한 건만 남기기"가 안 되므로, **라벨을 붙이는 순간 그 기사 정보를 라벨 저장소에 복사해 둔다** — 회차 파일이 예정대로 지워져도 라벨 쪽은 남는다. "라벨 달린 기사가 하나라도 있으면 그 회차 파일을 통째로 안 지운다"는 대안은 1년 넘은 회차가 계속 쌓이고 그 회차의 나머지 기사 수백 건까지 함께 남아 채택하지 않았다. 이 복사 구조 덕에 라벨 보관함이 1년치 회차 파일을 뒤지지 않아도 되는 이점이 따라온다
      - **[수정: 2026-08-18, 구현 완료: 2026-08-18] 복사해 두는 필드는 4개가 아니라 7개다** — 기능11 엑셀 8열 중 라벨명을 뺀 나머지 전부(스크랩일자·스크랩종료시간·발행일 발행시간·언론사명·기사제목·URL·소제목). 처음엔 제목·언론사·발행일·URL 4개만 생각했는데, 그러면 회차 파일이 지워진 뒤 라벨 보관함에서 뽑는 엑셀의 스크랩일자·스크랩종료시간·소제목 세 칸이 빈 채로 나가 라벨 기능의 핵심 목적("1년 뒤에도 온전히 꺼내보기")이 반쪽이 된다. **단, 기사 제목만은 스냅샷 값을 그대로 쓰지 않고 원문 교정(`summary_overrides`, URL 키 전역이라 회차 삭제와 무관하게 남는다)이 있으면 그 값으로 덧씌워 표시한다** — 제목 교정은 "원래 값이 틀렸다"를 고치는 것이라 항상 최신이 맞다. 반대로 **소제목은 스냅샷 시점 값을 그대로 둔다** — 소제목은 절대적 분류가 아니라 그때그때의 도우미이므로(기능2 규칙8·기능11 규칙3), 라벨 붙인 시점에 어느 소제목이었는지가 오히려 정확한 값이다
  7. 라벨 자체를 AI가 제안하거나 자동 분류하는 기능은 두지 않는다(1차 범위 제외)
  8. **[추가: 2026-08-18, 구현 완료: 2026-08-18] 라벨을 붙이는 화면은 확정본·초안·수집 결과 세 곳이다.** [수정: 2026-09-16] **정기 보관함(기능2)이 네 번째로 더해졌다** — `부정기사`처럼 나중에 알게 되는 라벨은 회차가 지난 뒤에 붙는데(“어? 어제 부정기사였는데 이것도 붙여야 되는 거 아니야?”), 그때 붙일 자리가 없었다. 정기 보관함이 읽기 전용인 근거(숨김·이동·이름바꾸기는 이미 나간 보고서를 고치는 일)는 라벨에 안 걸린다 — 라벨은 보고서 텍스트에 안 나가고(화면·엑셀 전용) 회차가 아니라 URL을 키로 하는 전역 저장소다. 담당자가 기사를 읽고 정리하며 "이건 나중에 쓰겠다"고 판단하는 자리가 이 셋이기 때문이다. **실시간(기능4)에는 두지 않는다** — 그 화면은 "필터 없이 날 것 그대로" 훑는 것이 규칙이라 기사가 수백~수천 건씩 쏟아지고, 이미 있는 "📌 담아두기"(기능4 규칙5)와 뜻이 겹쳐 보여 헷갈린다(담아두기="오늘 스크랩에 넣을 것" vs 라벨="나중에 꺼내볼 것"). 라벨 보관함 자체는 정기·수시를 가로질러 보는 화면이라 규칙5 그대로 유지된다

11) 데이터 내보내기 — [설계 확정: 2026-08-18, 구현 완료: 2026-08-18]
- 설명: 보관된 기사를 표 형태 파일로 뽑아 담당자가 엑셀에서 직접 가공할 수 있게 하는 기능. 기존 복사/`.txt` 내보내기(보고서 문장 형식)와 목적이 다르다 — 이쪽은 **데이터**다
- AI가 꼭 지켜야 할 규칙:
  1. **[수정: 2026-08-18, 구현 완료: 2026-08-18] 내보내는 곳은 다섯 군데다: 확정본 · 정기 보관함 · 수집 결과 · 수시 보관함 · 라벨 보관함.** 처음엔 세 보관함에서만 뽑고 진행 중인 화면은 뺄 생각이었으나("확정 안 된 데이터를 파일로 떨어뜨리면 어느 버전인지 모른다"는 이유), 다시 보니 **확정본과 수집 결과는 이미 확정된 값이다** — 확정본은 회차가 끝난 상태고, 수집 결과도 그 시점 실제 수집 데이터라 "미확정"이 아니었다. 그래서 둘을 추가했다. **초안만 계속 뺀다** — 초안은 회차 마감과 함께 자동으로 확정본이 되므로, 뽑고 싶으면 잠시 뒤 확정본에서 뽑으면 된다
  2. **[추가: 2026-08-18, 구현 완료: 2026-08-18] 어디서 뽑든 "지금 화면에 보이는 것"을 그대로 내보낸다.** 별도의 기간·범위 선택 UI를 만들지 않는다 — 정기 보관함에서 여러 날짜를 펼쳐 보고 있으면 그 날짜들, 수시 보관함에서 특정 사안만 펼쳤으면 그 사안, 라벨 보관함에서 특정 라벨을 골랐으면 그 라벨만 나간다. 화면과 내보내기 내용이 항상 일치하게 해, 복사/`.txt`가 이미 따르는 원칙(기능2 규칙6·기능9 규칙7)을 엑셀에도 그대로 적용한다
  3. 열 구성은 다음 8개다: `스크랩일자` | `스크랩종료시간` | `발행일 발행시간` | `언론사명` | `기사제목` | `URL` | `소제목` | `라벨명`
  4. **소제목은 분류 도우미이지 절대적인 값이 아니다**(담당자 명시) — 빈 값이거나 "📂 소제목 미분류"인 행이 섞여 있어도 정상으로 취급하며, 내보내기가 이를 채우려고 AI를 다시 부르지 않는다
  5. `라벨명`은 여러 개일 때 한 칸에 `세제, 보고서`처럼 합쳐 넣는다(기능10 규칙1)
  6. **[수정: 2026-08-18, 구현 완료: 2026-08-18] 파일 형식은 `.xlsx`로 정했다**(`openpyxl` 새 패키지 추가). CSV도 검토했으나(표준 라이브러리만으로 되고 UTF-8 BOM만 붙이면 한글도 안 깨짐) 세 가지 이유로 `.xlsx`를 택했다 — ① CSV는 엑셀이 셀 앞의 `=`/`+`/`-`를 수식으로 오인식하는 문제가 있는데(예: 기사 제목이 "-3.2% 하락…"으로 시작하면 셀이 깨짐) `.xlsx`는 타입을 명시해 저장하므로 이 문제가 없다. ② 내보내는 곳이 5곳으로 늘어 상시 기능이 됐다. ③ 1년치를 뽑으면 행이 수만 개라 자동 필터·틀고정이 실제로 필요하다
  7. **[추가: 2026-08-18, 구현 완료: 2026-08-18] 저장 파일명은 `날짜_종류_구분`으로 통일한다**(기능1 규칙11·기능9 규칙7과 동일 규칙, `.txt`·`.xlsx` 공통) — 예: `2026-08-18_정기_11시.xlsx`, `2026-08-18_수시_재경위.xlsx`, `2026-08-18_라벨_보고서.xlsx`. 날짜를 맨 앞에 둬 이름순 정렬이 시간순이 되게 하고, 종류를 그다음에 둬 다운로드 폴더에서 구분되게 한다. 세부:
      - **날짜의 의미가 화면마다 다르다** — 정기·수집 결과는 그 회차/카드의 날짜, 수시 보관함은 사안의 대표 날짜, **라벨은 소속 날짜가 없으므로 "뽑은 날"을 쓴다**(안 그러면 같은 라벨을 두 번 뽑을 때 파일명이 같아져 `(1)`이 붙는다)
      - 정기 보관함처럼 화면에서 여러 날짜를 펼쳐 뽑을 때는 `2026-08-11~08-18_정기.xlsx`처럼 범위로 표기한다
      - 사안명·라벨명은 자유 입력이라 파일명 금지 문자(`/:*?"<>|`)가 섞일 수 있다 — 기능9 규칙4가 이미 쓰는 치환 방식을 라벨에도 동일하게 적용한다
      - 기존 `.txt` 파일명(`언론모니터링_날짜_시각.txt`, `수시모니터링_사안명_날짜.txt`)도 이번에 함께 이 규칙으로 바꾼다 — 같은 회차/카드에서 뽑은 두 파일의 이름이 확장자만 다르고 나머지는 같아야 헷갈리지 않는다

12) [단독]·[속보] 기사 알림 — [설계 확정: 2026-08-20, 구현 완료: 2026-08-20]
- 설명: 회차를 기다리지 않고 `[단독]`·`[속보]` 말머리 기사만 **즉시** 텔레그램으로 보내는 기능. 기능6(텔레그램 전송)이 "회차 전체를 정해진 시각에" 보내는 것과 목적이 다르다 — 이쪽은 "지금 이 한 건을 빨리". 실측 근거: 저장된 회차 20건 기준 기사 발행부터 회차 수집까지 **중앙값 90분, 최대 168분** 걸려, 정기 회차만으로는 단독을 최대 3시간 가까이 늦게 알게 된다
- AI가 꼭 지켜야 할 규칙:
  1. **말머리 판정은 제목 맨 앞의 `[단독]`/`[속보]`만 인정한다**(괄호 변형 `[]()<>《》【】〔〕` 허용, 부분일치 배제) — 기능4의 실시간 화면 필터와 **같은 정규식**(`app.filters.headline_kind`)을 쓴다. 저장된 고유 제목 1,339건 전수조사에서 괄호형 말머리는 전부 맨 앞에만 나왔다
  2. **세 경로가 판정·발송·중복방지 함수 하나를 공유한다**(`app/breaking_alert_sender.py` `detect_and_alert`) — ① **바닥**: 정기 회차 저장 직전에 그 회차가 이미 검색한 결과를 검사(추가 네이버 호출 0회, 설정을 다 꺼도 항상 동작), ② **폴링**: 감시 시간대·주기가 됐을 때만 감시 대상 그룹 키워드로 별도 검색, ③ **몰아보내기**: 앱 시작 시 1회, 꺼져 있던 사이 놓친 것을 모아 발송. 셋이 같은 기사를 각자 찾아도 **한 번만 나간다**(`data/alerted_urls.json`, 자정 리셋)
  3. **감시 대상은 담당자가 고른 검색어 그룹만이다** — 전체 키워드로 열면 안 된다. 실측(2026-08-20): 그날 `[속보]` 79건 중 **76건이 '코스피' 하나**(증시 시황)에서 나왔고, 정기 스크랩 대상 그룹으로 좁히면 `[속보]` 0건·`[단독]` 2건으로 떨어졌다
  4. **설정**(`/breaking-alert`, `data/breaking_alert.json`): 감시 사용 여부, 감시 대상 그룹, 감시 시간대(기본 09:00~20:00), 확인 주기(3~30분, 기본 5분), 몰아보내기 사용 여부. 설정 화면은 **예상 네이버 호출량**(키워드 수 × 하루 폴링 횟수)을 즉시 계산해 보여준다 — "저장" 전에 비용 감을 잡게 하기 위함. 실행 상태(마지막 폴링 시각)는 `data/breaking_alert_state.json`에 **분리** 저장한다(같은 파일이면 저장할 때마다 폴링 간격이 리셋된다)
  5. **받는 사람은 기능6의 텔레그램 수신자 표를 그대로 쓰되, 축이 독립이다** — 각 수신자에 `alert_scoop`/`alert_flash` 두 필드를 더해, 정기 발송(`enabled`)은 끄고 알림만 받는 사람을 표현할 수 있다. 별도 주소록을 만들지 않은 이유: `chat_id`는 눈으로 검증이 안 되는 숫자열이라 두 군데 입력하게 두면 한쪽만 고치는 사고가 난다
  6. **API 호출 한도를 넘기면 폴링만 멈춘다**(`app/api_usage.py`) — 하루 호출이 `NAVER_DAILY_CALL_LIMIT`(25,000)의 80%를 넘으면 이 기능의 폴링을 중단하고 경고를 1회 보낸다. **정기 스크랩·실시간·수시는 절대 멈추지 않는다** — 본업이 우선이고 폴링이 부가 기능이므로 먼저 양보한다
  7. AI 요약은 넣지 않는다(토큰 비용 0 유지). 기사 줄 형식은 설정의 "📃 기사 제목 형식"을 그대로 따른다
  8. [추가: 2026-09-16] **기사마다 게시 시각을 URL 다음 줄에 한 줄로 적는다**(`15:31 게시`) — 세 경로 모두 발견까지 시차가 있어(폴링 주기, 네이버 색인 지연 3~18분, 바닥 경로는 회차 간격) 받는 사람이 방금 나온 기사인지 두 시간 전 기사인지 알 수 없었다. 시각을 못 읽은 기사는 그 줄 없이 예전 그대로 2줄이다(없는 값을 지어내지 않는다)

---

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

- 범위 (만들 것)
  - 정기 모니터링 — 기사 자동 스크랩 (위 5번 기능 1)
  - 소제목 자동 분류 (위 5번 기능 2)
  - 로컬 PC에서 실행 (사용자가 앱을 켜두면 동작하며, 컴퓨터가 잠자기 모드였던 동안 놓친 회차는 깨어난 직후 즉시 보충 실행됨)
  - [추가: 2026-08-18] 실시간(기능4) · 초안(기능5) · 텔레그램/이메일 전송(기능6·7) · 전송 유예(기능8) · 수시 모니터링(기능9) — 모두 구현 완료
  - [추가: 2026-08-18] 라벨(기능10) · 데이터 내보내기(기능11) · 진입 화면 네 갈래 재구성(기능3 규칙3-1) — 모두 구현 완료
  - [추가: 2026-08-20] [단독]·[속보] 기사 알림(기능12) — 구현 완료
- 비범위 (이번엔 만들지 않음)
  - 네이버 뉴스 외 다른 매체(다음, 구글뉴스 등) 연동
  - 사용자별 로그인/계정 관리 기능
  - ~~이메일 알림 기능 (이번 버전엔 제외, 다음 버전에서 추가 예정)~~ → **[수정: 2026-08-18] 이미 구현됨**(기능7, 2026-08-06). 문서가 오랫동안 자기모순 상태였던 것을 바로잡는다
  - ~~언론사별 필터/검색 기능 (소제목별 목록만 제공)~~ → **[수정: 2026-08-18] 이미 구현됨.** 실시간 화면(기능4)에 제목 검색 상자와 언론사·키워드 그룹 필터가 있다. 다만 확정본·초안 화면은 여전히 소제목별 목록만 제공하므로, "**정기 모니터링 결과 화면**에는 언론사별 필터/검색을 두지 않는다"로 범위를 좁혀 유지한다
  - [추가: 2026-08-18] 수시 [새 수집] 화면의 LLM 검색어 추천 — 검토했으나 담당자 판단으로 **기능상 제외**
  - 같은 네트워크 밖(외부)에서의 모바일 접속 — 우선 윈도우 PC 브라우저에서만 확인, 필요 시 추후 별도 논의

---

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 공개등급: 내부용 (담당자 개인 업무 도구, 외부 공개 없음)
- 개인정보 여부: 수집 대상은 네이버 뉴스에 공개 게재된 기사로 개인정보 수집 목적은 아님. 다만 기사 본문에 특정인의 실명 등이 포함될 수 있으므로, 결과물을 외부에 공유할 경우 주의가 필요
- API 키 관리: 네이버 검색 API(Client ID / Client Secret)를 사용하며, 발급받은 키는 코드나 저장소에 직접 노출하지 않고 `.env` 파일로 로컬에만 보관
- 데이터 보관: **[수정: 2026-08-18, 구현 완료: 2026-08-18]** 수집된 기사(추출된 문단 포함)는 정기 보관함·수시 보관함 모두 **1년간 보관 후 자동 삭제**한다(기존 7일에서 연장 — 기능11의 데이터 내보내기가 보관함에서 이뤄지는데 7일치로는 뽑을 게 없다). 단 **담당자가 라벨을 붙인 기사는 이 자동 삭제에서 제외되어 사실상 무기한 남는다**(기능10 규칙6)
  - ⚠️ **개인정보 관점 재검토 필요 (미완):** 바로 아래 "개인정보 여부" 항목에 적어둔 대로 기사 본문에 특정인의 실명 등이 포함될 수 있다. 7일 보관과 1년 보관은 개인정보 보호 관점에서 성격이 다른 문서가 되며, 라벨 예외는 보관 기간이 아예 없는 것과 같다. **보관 기간 연장 자체는 위 방향대로 진행하되, 이 관점의 검토는 담당자가 별도로 진행하기로 했다(2026-08-18 보류).** 검토 결과에 따라 "라벨 기사 수동 삭제 수단 제공", "보관 기간 설정값화" 등이 추가될 수 있다
- 저작권: 스크랩 결과는 담당자 개인 업무용으로만 사용하며, 외부 배포·재배포하지 않음
- 실행 환경이 로컬 PC로 한정되어 외부 서버 노출로 인한 보안 위험은 낮음

---

## 8. 기술 스택
*개발에 사용할 도구 모음 (난이도별 옵션 중 내가 고른 것과 그 이유)*

- 선택: 옵션 2 (보통) — 파이썬 웹서버 + 데이터베이스
  - 구성: Python(FastAPI) 웹 서버 + SQLite 데이터베이스(기사 저장) + APScheduler(하루 3회 자동 실행) + 네이버 뉴스 검색 API(기사 검색·요약) + 브라우저에서 `localhost` 주소로 접속해 화면 확인
  - 실행 OS: Windows
- 선택 이유: 코딩을 배운 지 얼마 안 되어 너무 복잡한 구조는 부담스럽고, 옵션 1(파일 저장 방식)만으로는 원하는 파란색 톤·하이라이트 같은 화면 디자인을 구현하기 어려움. "내 컴퓨터에서만 실행" 방침과도 맞고, 색상·하이라이트·소제목별 화면 구성을 자유롭게 만들 수 있어 옵션 2를 선택함

> ※ [수정: 2026-07-23] Design 단계(DESIGN.md)에서 실제로는 "쉬움: 파일 기반" 방식(파이썬 스크립트 + JSON 파일, FastAPI·SQLite·APScheduler 미사용)으로 최종 변경됨. 이유는 구현 난이도를 더 낮추기 위함이며, 화면 디자인 자유도는 정적 HTML로도 유지됨. 자세한 내용은 DESIGN.md 3절 참고.

---

## 9. 개발 단위
*핵심 기능을 만들기 좋은 작은 작업 단위로 나눈 목록 (만들 순서대로 정렬)*

1. 프로젝트 기본 구조 설정 (Python 가상환경, 스크립트 폴더 구성, `.env`로 네이버 API 키 관리) — ※ FastAPI 대신 파일 기반 방식으로 변경 (DESIGN.md 참고)
2. 기사 저장용 로컬 JSON 파일 구조 설계 — ※ SQLite 대신 파일 기반 방식으로 변경 (DESIGN.md 참고)
3. 네이버 뉴스 검색 API 연동, 설정된 키워드 목록(기본값: "재정경제부", "재경부") 중 하나라도 걸리면 기사를 검색·수집하는 기능 구현
4. 수집한 기사를 언론사 우선순위(방송사 → 주요 언론사) 순으로 정렬하는 기능 구현
5. 언론사·기사제목·URL 형식으로 변환하고, API가 제공하는 요약(description)을 DB에 저장하는 기능 구현
6. 저장된 요약에서 일치하는 키워드를 하이라이트 처리하는 기능 구현 — [수정: 2026-08-18] 문서에 "형광 연두색(#C6FF00)" 단색으로 남아 있었으나, 실제로는 **단어마다 다른 색이 순환하는 파스텔 5색**(`app/config.py` `HIGHLIGHT_COLORS`)이다. 하이라이트 단어는 검색 키워드와 별개 설정값이며(최대 20개), 칩을 눌러 단어별 색을 바꿀 수 있다
7. 스크랩 실패 시 5분 간격으로 최대 3회 자동 재시도하는 기능 구현
8. 파이썬 스케줄 루프로 담당자가 설정한 회차 시각에 자동 실행을 예약하는 기능 구현 — [수정: 2026-08-18] 고정 시각(09:00/10:30/13:30/16:30)은 초기 기본값이었고, 지금은 설정 화면에서 자유롭게 바꾼다 (잠자기 모드 중에는 실행되지 않으며, 컴퓨터가 깨어난 직후 놓친 회차를 즉시 보충 실행) — ※ APScheduler 대신 경량 스케줄 루프로 변경 (DESIGN.md 참고)
9. 기사 제목을 분석해 소제목(최대 15개 — [수정: 2026-08-20] 8→15, 4절 규칙1 참고)을 자동 생성하고 기사를 배정하는 기능 구현
10. 소제목별 요약(3문장 이내·**250자** 이내 — [수정: 2026-08-18] 문서에 150자로 남아 있었으나 실제 구현은 250자)과 전체 키워드(쉼표 나열 — [수정: 2026-08-18] `#키워드` 형식은 2026-07-24에 폐기됨) 자동 추출 기능 구현 (규칙 기반 — [수정: 2026-08-11] 소제목 묶음 자체만 별도로 Claude API 도입, 요약·키워드 추출은 여전히 규칙 기반. 기능2 규칙 2 참고)
11. 수집된 기사를 일정 기간 보관 후 자동 삭제하는 기능 구현 — [수정: 2026-08-18, 구현 완료: 2026-08-18] 7일 → **1년**, 라벨이 붙은 기사는 예외(7절·기능10 규칙6)
12. 소제목별로 기사를 보여주는 웹 화면(파란색 톤, PC 위주 반응형) 구현
13. 수집된 기사가 없을 때 "하나도 없어요" 빈 화면을 표시하는 기능 구현
14. 소제목별 스크랩 목록을 클립보드로 복사하거나 .txt 파일로 내보내는 기능 구현
15. 검색 키워드를 등록·수정·삭제할 수 있는 설정 화면 구현
16. 전체 흐름(스크랩 → 분류 → 화면 표시 → 복사/내보내기) 통합 테스트
