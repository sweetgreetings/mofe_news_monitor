# 전문가에게 자문받을 것

혼자 판단하기 어렵거나, 잘못 고르면 되돌리기 비싼 결정을 모아 둔다. 개발 경험이 있는 사람에게 그대로 보여 주면 되도록, 항목마다 **상황·실측치·후보안·묻고 싶은 것**을 한 자리에 적는다.

- 결정이 나면 그 항목을 지우고, 결과 규칙은 [CLAUDE.md](CLAUDE.md)에, 기각한 대안과 이유는 [HISTORY.md](HISTORY.md)에 옮긴다.
- 「지금 판단」은 자문 전까지의 잠정 결론이다.

---

## 1. 큰 파일을 기능별로 가를 것인가 (2026-09-23 올림)

### 상황

파이썬이 정적 HTML을 문자열로 만들어 내는 구조다(웹 프레임워크 없음). 총 45,423줄이고, 파일 크기가 한쪽으로 쏠려 있다.

| 파일 | 줄 | 안에 뭐가 있나 |
|---|---|---|
| `app/settings_server.py` | 7,823 | 설정 화면 약 35개(템플릿 상수 + 렌더 함수) + 라우트 104개·핸들러 77개 + `http.server` 핸들러 클래스 |
| `app/renderer.py` | 5,570 | 앞 2,282줄이 확정본 페이지 CSS·JS 한 덩어리(`_PAGE_TEMPLATE`), 나머지가 함수 60개 |
| `app/preview_renderer.py` | 3,775 | `app.renderer`에서 이름 42개를 가져다 쓴다(이미 공유됨) |
| `app/adhoc/renderer.py` | 3,592 | 수시는 `card`/`collector`/`routes`/`undo`/`archive_renderer`로 이미 갈려 있다 |
| `app/history_renderer.py` | 1,836 | CSS 400여 줄에 「app.renderer와 동일」 주석 — **손으로 복사한 중복** |

두 가지가 실제로 비용을 만든다.

1. **크기** — 설정 화면 한 줄을 고치려고 7,823줄짜리 파일을 열어야 한다. AI 협업에서는 이게 곧 토큰·시간이고, 사람이 볼 때도 어디를 고치는지 찾는 시간이다.
2. **중복** — 같은 CSS·JS가 렌더러 네 곳에 복사돼 있어, CLAUDE.md에 「한쪽을 바꾸면 다른 쪽도 바꾼다」는 규칙이 열 군데 넘게 적혀 있다. 규칙으로 막고 있을 뿐이라 화면 간 어긋남이 실제로 생긴다.

### 이 저장소의 특수 조건 (판단에 영향을 주는 것)

- **사용자 1명, 로컬 실행, 로그인 없음.** 배포·다운타임·동시 사용자 문제가 없다.
- **린터·타입체커 없음.** 모듈을 옮겨 생긴 이름 오류는 앱을 켜 봐야 드러난다.
- **테스트는 `tests/test_integration.py` 한 파일(49개)뿐.** 통과는 하지만, 렌더러가 뱉는 건 HTML 문자열이라 **눈에 보이는 깨짐(레이아웃·색·빠진 CSS)을 테스트가 잡아 주지 않는다.** 실제 비용은 가르는 작업이 아니라 화면을 하나씩 열어 확인하는 쪽이다.
- 앱은 macOS LaunchAgent로 상시 떠 있어, 렌더러를 바꾸면 재시작해야 화면에 반영된다.

### 후보안

- **A. `settings_server.py`를 화면별 모듈로** — 화면 하나 = 템플릿 상수 + 렌더 함수 하나라 기계적으로 갈린다. `app/settings_pages/{keywords,schedule,telegram,email,outlets,…}.py`로 옮기고 `settings_server.py`엔 서버·라우터만 남긴다. 깨지면 앱이 안 켜져서 바로 드러난다.
- **B. 중복 CSS·JS를 공용 모듈로** — `history_renderer`가 손으로 복사해 둔 400여 줄부터. CLAUDE.md의 「둘 다 고쳐라」 규칙들이 없어진다.
- **C. 안 한다** — 기능 작업하면서 손대는 파일만 그때그때 조금씩 정리.

### 묻고 싶은 것

1. 이 규모(사용자 1명·로컬·테스트 1파일)에서 **7,800줄 단일 파일은 실제로 문제인가**, 아니면 잘 정리돼 있으면 괜찮은가? 가르는 이유가 「AI가 읽기 쉽다」뿐이라면 그건 충분한 이유인가?
2. 가른다면 **A와 B 중 무엇이 먼저**인가? (A는 작업 편의, B는 화면 간 어긋남 = 버그 예방)
3. 렌더러가 HTML 문자열을 만드는 구조에서, **시각 회귀를 값싸게 잡는 방법**이 있나? (스냅샷 테스트로 HTML을 비교하는 게 이 규모에 값하는가)
4. 지금은 기능 하나가 파일 여럿에 걸쳐 있어 **기능별 커밋이 사실상 불가능**하다(2026-09-23 커밋 `ff628ad`: 156개 파일, 기능 열 개 넘게 한 덩어리). 커밋 단위를 되찾는 게 파일을 가르는 이유가 될 만한가?
5. 파이썬 문자열 템플릿으로 HTML을 만드는 이 구조 자체를 **지금이라도 템플릿 엔진(Jinja 등)으로 바꿀 값이 있나**, 아니면 돌아가는 걸 건드리지 않는 게 맞나?

### 지금 판단

**보류.** 트리가 깨끗하고 되돌릴 지점(`ff628ad`)이 있어 시작하기엔 좋은 때지만, 가르는 값이 「작업이 편해진다」에 그치면 확인 비용(화면 전체를 눈으로 훑기)이 더 클 수 있다. 자문 뒤에 정한다. 과거 커밋을 쪼개 역사를 다시 쓰는 것은 하지 않기로 했다 — 실제로 없던 중간 상태를 인공적으로 만들어야 하고 되돌릴 지점이 흐려진다.
