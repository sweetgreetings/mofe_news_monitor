# HISTORY.md

CLAUDE.md의 다이어트(2026-08-12)로 옮겨온 변경 이력·설계 배경 아카이브. CLAUDE.md는 "지금 유효한 규칙/구조"만 현재형으로 담고, "왜 이렇게 됐는지"(버그 스토리, 사용자 피드백, 시행착오, 리버트 과정, 실측 수치)는 전부 여기 있다.

**언제 열어보나**: 특정 기능이 왜 지금 이렇게 동작하는지 궁금할 때, 비슷한 버그를 다시 만들지 않으려 과거 시행착오를 참고할 때, 회귀(regression)를 디버깅할 때. 매 턴 자동으로 로드되지 않으므로 필요할 때 Read로 열어서 참고한다.

**목업 파일 위치** [2026-09-16 갱신]: 문서가 링크로 가리키는 시안은 `mockups/`에 있다. 어느 문서도 가리키지 않던 옛 시안은 `trash-can/2026-09-16_옛시안정리/`로, 그보다 먼저(2026-08-20) 정리한 것은 `trash-can/2026-08-20_목업정리/`로 옮겼다 (루트 정리). 실제 구현이 시안을 앞질러 가 있어 시안은 더 이상 참조 기준이 아니지만, 당시 판단 근거로서 파일은 남겨뒀다. 일부는 이전 정리 때 `trash-can/2026-08-20_동향제거/mockups/` 로 먼저 옮겨졌다.

**구조**: 원래 CLAUDE.md의 섹션·순서를 그대로 유지했다 (Architecture → Core business rules, 각 항목이 CLAUDE.md의 같은 제목 규칙에 대응). `[Added:]`/`[Changed:]`/`[Fixed:]`/`[Removed:]` 날짜 태그가 붙은 문장이 히스토리 본문이다.

## Architecture — 변경 이력

### 수시(카드) 보관도 무기한 → 1년 — 2026-08-18

정기 보관 기간을 1년으로 올린 직후, "수시도 무기한 대신 1년으로 맞추자"는 결정이 나왔다
(PRD 기능9 규칙9가 이미 요구하던 것 — 두 보관함 정책이 다르면 담당자가 "어느 쪽이 언제까지
남는지"를 따로 기억해야 한다는 이유).

**정기와 다른 점 — 삭제 동작 자체가 없었다.** 정기는 `RETENTION_DAYS`를 참조하는
`delete_expired_runs`가 이미 있어 상수만 올리면 됐지만, 수시는 "카드를 지운다"는 함수는
있어도(`delete_card`, 담당자가 직접 지울 때 씀) **나이로 자동 정리하는 함수가 없었다**.
`app/adhoc/card.py`에 `delete_expired_cards(retention_days=ADHOC_RETENTION_DAYS, now=None)`를
새로 만들었다 — `created_at` 기준으로 판단하고, 실삭제 대신 기존 `delete_card()`(trash 이동)를
그대로 재사용한다.

**라벨과의 상호작용을 걱정했으나 이미 해결돼 있었다.** PRD 기능10 규칙6("라벨 붙은 기사는
삭제 예외")을 카드 삭제 함수 안에서 따로 처리해야 하나 고민했는데, `app/labels.py`를 보니
**라벨을 붙이는 순간 기사 정보를 이미 별도 저장소(`data/labels.json`)로 복사**해 카드
생애주기와 완전히 독립시켜 두었다(그 모듈 자체 설명: "회차 파일이 있으면 안 지운다는 대안은
그 회차의 나머지 수백 건까지 함께 남아 채택하지 않았고, 대신 라벨을 붙이는 순간 기사 정보를
복사했다"). 그래서 `delete_expired_cards`는 라벨을 전혀 신경 쓸 필요가 없다 — 카드가
trash로 가도 라벨 목록엔 원본과 무관하게 그대로 남는다.

**아키텍처 규칙과 부딪힌 지점 — 그리고 어떻게 지켰나.** CLAUDE.md는 "정기 코드는
`app/adhoc/`를 import하지 않는다"고 못 박아 뒀다(`app/adhoc/` + `data/adhoc/` 두 폴더만
지워도 정기 쪽에 흔적이 안 남게 하려는 것). 그런데 주기적 정리는 자연스럽게
`app/scheduler.py`의 tick 루프에 들어가야 한다 — `cleanup_group_overrides`·
`delete_expired_exports`·`delete_expired_runs`가 전부 그 자리에 있다.

`scheduler.py`가 `app.adhoc.card`를 직접 import하면 규칙을 깨므로, **의존성 주입으로
풀었다**: `run_scheduler(..., cleanup_adhoc_cards: Callable = lambda: [])` — 기본값은
아무 일도 안 하는 람다이고, 합성 지점인 `main.py`가 스레드를 띄울 때
`cleanup_adhoc_cards=delete_expired_cards`를 실제로 주입한다. `scheduler.py` 소스 어디에도
`import app.adhoc`가 없다(정적 분석으로 확인) — 이미 `run_scheduler`가 `scrape`/`cleanup`
등을 전부 이 패턴(테스트가 콜러블을 주입하는 것과 같은 모양)으로 받고 있어서 위화감 없이
끼워 넣을 수 있었다. `main.py`는 이미 `app.settings_server`를 통해 간접적으로 adhoc과
엮여 있는 합성 지점이라, 여기서 한 번 더 import하는 건 새로운 종류의 결합이 아니라
기존 결합을 명시적으로 드러낸 것에 가깝다.

**검증**: 임시 폴더로 격리해 400일/366일(삭제)·364일/8일(유지)·깨진 파일(무시) 경계를 확인했다.
8일짜리를 유지 대상에 넣은 이유는, 예전 "정기가 7일이던 시절" 정책이 실수로 남아있지 않은지
확인하려는 것이었다(수시는 애초에 그런 정책이 없었으니 해당 없지만, 습관적으로 넣어 재확인).

**결과**: 정기 1년 / 수시 1년 / export 7일로 통일. `data/adhoc/trash/`도 삭제 없이 계속
쌓이는 폴더라는 점은 정기의 `trash-can/`과 같은 성격 — 사람이 주기적으로 확인 후 직접 비운다.

### 보관 기간 7일 → 1년 — 2026-08-18

위 인덱스 정리가 끝난 뒤 `RETENTION_DAYS`를 7에서 **365**로 올렸다(PRD 기능2 규칙6의 결정 반영).
계기는 "작년 이맘때 이 사안이 어떻게 다뤄졌나"를 되짚을 일이 실제로 있다는 것, 그리고 엑셀
내보내기(기능11)가 정기 보관함에서 이뤄지는데 7일치만으로는 뽑을 게 없다는 것이다.

**함께 정리한 것**

- `EXPORT_RETENTION_DAYS = 7` 신설. 전에는 `data/export/`(media_report 인수인계 JSON)가
  회차 기사와 **같은 상수**를 썼다. 그대로 뒀으면 보관 기간을 올리는 순간 인수인계 파일까지
  1년을 들고 있게 된다 — 그쪽은 "넘겨주면 끝"인 파일이라 늘릴 이유가 없어 상수를 갈랐다.
- 화면 제목 `정기 보관함 (최근 7일)`이 HTML에 그대로 박혀 있어, 상수를 바꿔도 화면이
  거짓말을 하는 상태였다. `_retention_label()`이 `RETENTION_DAYS`에서 문구를 만들어
  붙이도록 바꿨다(365 → "최근 1년", 30의 배수 → "최근 N개월", 나머지 → "최근 N일").
- `load_today_runs`(진입 화면 워드클라우드, **요청마다** 호출)가 전 회차를 파싱한 뒤 오늘 것만
  골라내고 있었다 — 인덱스 정리 때 놓쳤던 자리다. `list_runs_for_date`를 쓰도록 고쳤다.
  안 고쳤으면 워드클라우드 한 번에 22.8MB를 읽는 상태로 1년을 가는 셈이었다.

**남겨둔 것 / 앞으로 볼 것**

- **수시(`data/adhoc/`)는 여전히 무기한 보관이다.** PRD 기능9 규칙9는 "정기와 1년으로 맞추자"고
  하지만, 그건 지금 없는 **삭제 동작을 새로 만드는** 일이라 따로 결정할 문제로 남겼다.
  지금 상태: 정기 1년 / 수시 무기한 / export 7일.
- `group_overrides.json`은 만료 시각 없이 URL로만 쌓인다. 기사가 사라져야 정리되는데 이제
  기사가 1년을 남으니 사실상 정리되지 않는다(현재 55건, 1년이면 3천 건 300KB 추정).
  렌더마다 파싱하는 값이라 지금 규모에선 무해하지만, 커지면 항목에 시각을 넣어 나이로
  만료시키는 쪽을 검토한다(형식 변경 — CODING_CONVENTIONS.md §7-2 마이그레이션 필요).
- 정기 보관함 화면은 날짜가 하루에 하나씩 늘어 결국 365줄이 된다. 껍데기만 그리는 구조라
  무겁진 않지만(줄당 ~150바이트) 훑기는 불편해진다 — 기간 조회 UI(일별/1주일/1개월)가
  그래서 다음 작업이다. 시안: ARCHIVE_PERIOD_MOCKUP.html.

### 회차 메타 인덱스 도입 (보관 기간 확대 사전 정리) — 2026-08-18

**계기**: 보관 기간을 7일에서 1년으로 늘릴 수 있는지 물어봐서 실측했다. 답은 "용량은 문제가 아니다".
현재 파일을 1년치로 복제해 재보니 정기 1,460개 22.8MB, 수시 365카드 13.7MB — 합쳐 40MB 미만이다.
`data/undo_stack.json` 하나가 이미 2.3MB인 걸 생각하면 저장 용량은 논점이 아니었다.

**진짜 문제는 "매번 전부 열어보는 코드"였다.** 실측(맥 SSD, 1,460개 22.8MB 기준):

| | 측정값 |
|---|---|
| 전체 파싱(`list_all_runs`) | 80ms |
| URL 수집(`cleanup_group_overrides`) | 75ms |
| glob만 | 1ms |
| 수시 보관함 렌더(사안 40 × 카드 365) | 1,415ms |

그리고 그 자리들이 하필 **담당자의 클릭 경로 한복판**이었다. 🗑️ 한 번 누르면
`_regenerate_screens()` → `generate_history_page()` → `list_all_runs()`로 한 번,
`_find_run_file()`로 또 한 번 — 보관 7일(36개 파일)에선 안 보이지만 1년이면 클릭마다 45MB를 읽는다.
`cleanup_group_overrides`는 더 나빠서, 스케줄러 tick(10초)마다 75ms × 하루 8,640번 = **하루 197GB 읽기**였다.
대상 OS가 Windows(백신 실시간 검사)라는 걸 감안하면 실제로는 이보다 더 나쁘다.

**한 일**

1. `app/run_index.py` 신설 — 회차 파일의 `run_at`/`run_slot`/`confirmed`/기사 수만 담은 캐시.
   `(mtime_ns, size)` 서명을 대조해 **바뀐 파일만** 다시 읽는다. 평상시 회차 파일 열람 0회.
   `load_latest_run`·`load_latest_confirmed_run`·`_find_run_file`·`delete_expired_runs`·
   `confirm_send._other_confirmed_run_today`가 전부 이걸 통하도록 바꿨다.
2. `generate_history_page` — 날짜 목록은 인덱스에서(읽기 0회), 기사는 즉시 렌더링할
   `EAGER_HISTORY_DAYS`(2)일치만 읽는다. `render_history_page`에 `all_dates` 인자를 추가해
   "줄 세울 날짜"와 "내용을 가진 회차"를 분리했다. `/history/day` 지연 로딩도 그 날짜 파일만 연다.
3. `cleanup_group_overrides` — 회차 파일 서명이 그대로면 즉시 반환(glob+stat만).
4. `render_archive_page`(수시) — 사안마다 `list_cards(issue_id=...)`를 다시 부르던 O(사안×카드)를
   한 번 읽고 메모리에서 나누도록 고쳤다.
5. `_known_articles_by_url` — 필요한 URL(=옛 형식이라 outlet이 없는 기록, 대개 0건)만 받아
   최신 회차부터 읽다가 다 찾으면 멈춘다.

**결과** (1,460회차 21.5MB 기준)

| | 전 | 후 |
|---|---|---|
| `generate_history_page` (🗑️ 클릭마다) | 84ms+ | **20ms** |
| `cleanup_group_overrides` (10초마다) | 75ms | **6ms**(변화 없으면 즉시 반환) |
| 수시 보관함 렌더 (사안 40·카드 365) | 1,415ms | **38ms** |

**중간에 낸 사고 — 테스트 격리를 깼다.** `run_index`가 `app.config.ARTICLES_DIR`를 직접 import했는데,
통합 테스트는 실제 `data/`를 안 건드리려고 `patch.object(app.storage, "ARTICLES_DIR", 임시폴더)`로
바꿔치기한다. 그래서 인덱스만 실제 폴더를 보게 됐고 테스트 5개가 깨졌다 —
**`delete_expired_runs`가 테스트 도중 실제 회차 파일을 지울 수도 있는 상태**였다(그때 `now`가
과거 날짜라 우연히 아무것도 안 지워졌을 뿐이다). 폴더 경로를 `app.storage`에서 **호출 시점에**
읽도록 고쳤고, 인덱스 파일도 회차 폴더 옆에 만들어 함께 격리되게 했다.
CODING_CONVENTIONS.md §1-7과 같은 종류의 사고 — 검증 코드가 실제 데이터에 닿는 경로를 만들면 안 된다.

**또 하나 되돌린 것**: `delete_expired_runs`를 인덱스 기반으로 바꾸면서, `run_at`은 있는데
`articles`가 없는 반쯤 깨진 파일이 삭제 대상에서 빠져 영영 남게 됐다(예전 구현은 `run_at`만 읽히면
나이로 판단해 지웠다). 인덱스가 `bad`로 표시한 파일만 따로 한 번 더 확인하도록 되돌렸다.

**아직 안 한 것**: 보관 기간(`RETENTION_DAYS = 7`) 자체는 그대로 두었다. 위 정리가 선행 조건이었고,
실제로 며칠/몇 달로 늘릴지는 별도 결정이다. 늘릴 경우 `group_overrides.json`이 만료 없이
계속 쌓이는 점(1년이면 400KB 수준, 렌더마다 파싱)은 그때 다시 본다.


The original PRD plan (FastAPI + SQLite + APScheduler) was replaced during design with a simpler file-based approach to lower implementation difficulty. This is what's actually built:

- **No web framework**: plain Python scripts regenerate static HTML files (`index.html`, `history.html`, `home.html`) that are opened in the browser. No FastAPI, no Django.
- **Storage**: local JSON files under `data/` (`articles/`, `settings.json`, `hidden_articles.json`, `group_overrides.json`, `group_labels.json`) — no SQLite.
- **Scheduling**: a lightweight polling loop (`app/scheduler.py`, no APScheduler), checking every 10s [Changed: 2026-08-07, was 60s — 60초 간격이면 정시 슬롯을 막 지나친 tick 바로 다음 tick(최대 60초 뒤)까지 감지가 늦어져 텔레그램/이메일 자동 전송이 "정각+1분"처럼 체감됐다. 이 확인 자체는 설정 파일 읽기+시각 비교뿐이라 10초로 줄여도 비용 증가는 무시할 만하다]. Each slot is a `{start, end}` time window (default: 00:00–09:00 / 09:00–10:30 / 10:30–13:30 / 13:30–16:30 KST), user-editable from the settings screen (1–10 slots, no code change needed). `end` is the slot's identity (used for the storage filename and header, same as before); `start` is the lower bound for that slot's collection window (see Collection below). Only the single most-recently-missed slot is caught up on next tick if the app was closed during a scheduled time — using its *configured* start, not the actual catch-up time; the loop pauses during OS sleep like any other process and resumes catch-up on wake. **Schedule groups + day-of-week auto-selection** [Changed: 2026-08-10]: time slots are organized into named groups (up to `MAX_SCHEDULE_GROUPS` = 5, e.g. "주중"/"주말") — originally exactly one group was "active" via a radio button, manually toggled. Replaced with two independent per-group fields: `enabled` (bool, "사용" checkbox) and `days` (list of `WEEKDAY_KEYS` — `mon`/`tue`/.../`sun`, `app/config.py`). `app/settings.py` `pick_active_group_index(groups, now=None)` decides which group actually applies: if exactly one group is `enabled`, that one is used regardless of `days` (this is the deliberate manual-override path — for a holiday like 추석 that falls on a weekday, the user makes a new group, turns it on, and turns the other groups off; no need to touch their `days`); if 2+ groups are `enabled`, the one whose `days` includes today (`now.weekday()`) is used, falling back to the first enabled group if none match (a configuration gap). `validate_schedule_groups` requires at least one `enabled` group and rejects saves where two `enabled` groups both claim the same day ("요일이 겹칩니다" `SettingsError`, naming the groups and the conflicting day). `app/settings_server.py` renders a "오늘 적용 중" badge (computed via the same `pick_active_group_index`, applied to the possibly-unsaved in-progress `display_groups` so it stays correct while editing) and a row of 7 day checkboxes per group (`name="group{n}_days"`, repeated-name pattern like the outlet whitelist checkboxes, parsed as a list). **Migration**: old saved groups had `{"active": bool}` instead of `{"enabled": bool, "days": [...]}`; `app/settings.py` `load_settings()` converts `active` → `enabled` 1:1 and sets `days: []` for every group on first read — since `days: []` on the sole surviving `enabled` group means "applies regardless of day" (the single-enabled-group rule above), this exactly reproduces each user's pre-upgrade schedule with zero behavior change until they actually start checking days. [Changed: 2026-08-10] The "사용" control was first a plain checkbox, same look as the day checkboxes right below it — confusing at a glance. Switched to the same `.toggle`/`.track` ON/OFF switch markup already used for keyword on/off and email/telegram recipient toggles, so "turn this whole group on/off" reads visually distinct from "which days." [Fixed: 2026-08-10] This screen's `<form>` has multiple submit buttons (시간대 추가/그룹 추가/저장) — pressing Enter while focused in any text/number input triggers the browser's implicit-submission default, which targets the *first* submit button in DOM order (그룹1의 "+ 시간대 추가", not 저장), silently adding a time slot instead of doing nothing. Fixed with an `onkeydown` guard on the `<form>` that calls `preventDefault()` for Enter when `event.target` is an `<input>` — actual button clicks (mouse or keyboard-focused) are unaffected since their `event.target` is a `<button>`.
- **One exception to "no server"**: `app/settings_server.py` is a minimal `http.server`-only local HTTP server on `127.0.0.1:8765` that handles settings saves and article curation (hide/reorder/rename), and also serves the three static HTML screens from the same origin (needed because browsers block navigation from `http://` pages to `file://` paths).
- **External API**: Naver News Search API — credentials read from `.env` (`NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`), never hardcoded or committed. [Added: 2026-08-03] Telegram Bot API (`app/telegram_bot.py`) is a second, optional external integration — credentials (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`) also come from `.env`, but unlike Naver's, missing/invalid Telegram credentials never stop the app from starting or running; sends are just skipped with a log warning (`is_configured()`).
- **Target OS**: Windows, single local user, no auth/login layer.
- Summaries and keyword extraction are rule-based (word-frequency grouping over article **descriptions**, not titles — title wording varies too much by outlet to group well), explicitly to avoid API costs. [Changed: 2026-08-11] Sub-heading *grouping* is the one exception — it now calls the Claude API (see "LLM sub-heading classification" below), with an automatic, exception-free fallback to the original rule-based grouping whenever the API key is missing or the call fails, so the app never depends on it being available.

## 로그 파일 도입 — 2026-08-26

그 전까지 이 앱은 `logging.basicConfig`을 **아예 설정하지 않았다**. 기본 lastResort 핸들러가 WARNING 이상만 터미널에 찍고 그대로 사라졌다 — 남는 곳이 없었다. 평소엔 문제가 안 됐는데, 하루에 두 번 연달아 벽에 부딪히면서 넣기로 했다.

**막힌 사례 1 — "기타"가 왜 이렇게 많은가.** 2026-08-25 회차들의 "기타" 비율이 47~94%로 튀어 원인을 팠다. 원인 셋 중 둘(`assign_to_existing`의 `max_tokens=2000` 잘림, 150건 상한 초과)은 저장된 회차 파일만으로 역산해 확정했지만, 셋째(09:30·11:00 회차에서 `_refine_etc_bucket`이 안 먹은 것)는 끝내 확정하지 못했다. 그 함수에는 이미 `logger.warning("'기타' 재정리 호출 실패", exc_info=True)`가 있었다 — **코드가 이미 말하고 있었는데 들을 곳이 없었다.**

**막힌 사례 2 — 회차 파일 날짜 불일치.** 홈 시안을 오늘 데이터로 뽑았더니 142건 중 70건이 어제 저녁 기사였다. `data/articles/2026-08-26_19-30.json`의 `run_at`이 `2026-08-25T19:33:47`이고 기사 70건이 전부 08-25 17:02~19:28 발행이었다(그리고 `2026-08-25_19-30.json`은 아예 없었다). `app/storage.py` `save_run`이 파일명을 `datetime.now()`로 짓고 `run_at`을 안 보는 게 원인 후보인데, **어떻게 자정을 넘겼는지**(스케줄러 따라잡기인지, 앱 재시작인지)는 로그가 없어 확정하지 못했다. `save_run`의 호출부가 `collect_run` 하나뿐이라 큐레이션 재저장은 아니라는 것까지가 한계였다.

**설계 판단**

- **화면 WARNING / 파일 INFO로 가른 이유**: 지금까지의 터미널 동작(경고만 보임)을 그대로 지키고 싶었다. INFO를 화면에 쏟으면 담당자가 실제로 봐야 할 경고가 스크롤에 묻힌다.
- **보관 14일**: 회차 파일은 365일이지만 로그는 성격이 다르다. 위 두 진단 모두 **당일~이틀 안**의 기록이 필요했을 뿐이고, "작년 이맘때 로그"를 볼 일은 없다. 무한정 쌓으면 `data/`만 조용히 커진다.
- **`delay=True`**: import나 통합 테스트만으로 빈 로그 파일이 생기지 않게. 실제로 통합 테스트는 `setup_logging()`을 부르지 않아 `data/logs/`를 만들지 않는 것도 확인했다.
- **예외를 밖으로 안 낸다**: 로그 폴더를 못 만드는 상황(권한 등)에서 앱이 못 뜨면 안 된다. 파일 로깅만 포기하고 화면 로깅은 살린다 — 텔레그램·LLM이 실패해도 앱이 안 멈추는 것과 같은 원칙.
- **새 로그를 잔뜩 심지 않았다**: 두 사례 모두 **코드에 이미 로그가 있었는데 남을 곳이 없었던** 경우다. 그래서 이번 작업의 본체는 "남는 곳 만들기"이고, 새로 추가한 건 `save_run`의 저장 기록(INFO) 하나와 날짜 불일치 경고(WARNING) 하나뿐이다.

**이어진 작업**: 날짜 불일치는 같은 날 바로 고쳤다 — 아래 "회차 파일 날짜 불일치" 참고.


## 회차 파일 날짜 불일치 — 2026-08-26

**증상**. 홈 시안을 만들다 `data/articles/2026-08-26_19-30.json`의 `run_at`이 `2026-08-25T19:33:47`이고 기사 70건이 전부 08-25 17:02~19:28 발행인 걸 발견했다. `2026-08-25_19-30.json`은 아예 없었다 — 어제 저녁 회차가 오늘 이름표를 달고 있었다. 저장된 76개 회차 파일을 전수조사하니 어긋난 건 이 한 건뿐이었다.

**처음엔 피해를 과대평가했다.** "홈 건수·정기 보관함·워드클라우드가 다 오염된다"고 봤는데, 코드를 확인하니 **앱은 원래부터 날짜 판단을 전부 `run_at`으로 하고 있었다** — `list_run_dates`, `list_runs_for_date`, `load_today_runs`, `delete_expired_runs`, `app.run_index.find_path` 전부. `run_index.py`에는 "파일명으로는 못 짚는다(파일명 날짜는 저장 시각, run_at은 실행 시각이라 자정을 넘겨…)"는 주석까지 이미 있었다. 오염된 건 앱이 아니라 파일명으로 glob하던 **시안 생성 스크립트**였다.

**진짜 피해는 다른 데 있었다.** 파일명 자체가 키라서 두 가지가 깨진다.

1. **회차 통째 누락**: `run_exists(run_slot, date_str)`만은 파일명으로 판단한다(스케줄러가 "이 회차 이미 돌았나"를 보는 자리). 어제 회차가 오늘 이름으로 있으면 오늘 그 회차를 "이미 실행됨"으로 오판해 **건너뛴다**. 실제로 그날 저녁 19:30 회차가 그렇게 사라질 상황이었다.
2. **앞 회차 덮어쓰기**: 같은 이름이 다시 필요해지면 `write_text`가 그대로 덮는다. 임시 폴더에서 재현해보니 어제 70건이 오늘 5건에 덮여 사라졌다.

**고침**. `save_run`이 파일명 날짜를 `datetime.now()`가 아니라 **`run_at`에서** 뽑게 했다(`_date_from_run_at`). 파일명이 `{run_at의 날짜}_{run_slot}.json`으로 항상 성립하므로 두 결함이 구조적으로 사라진다. `run_at` 형식이 깨졌을 때만 현재 시각으로 물러서고 경고를 남긴다 — 회차를 저장하지 못하는 것보다 날짜가 하루 어긋난 채로라도 기사를 지키는 게 낫다.

**파일명을 만드는 곳이 `save_run` 하나뿐인지 확인하고 고쳤다.** `update_run_articles`/`confirm_run`/`record_send`/`app.undo`는 전부 `_find_run_file(run_at, run_slot)`으로 **이미 있는 경로**를 찾아 쓰므로 새 이름을 짓지 않는다. 그래서 이 한 곳을 고치면 전부 덮인다.

**원인 경로는 확정하지 못했다.** `run_at`이 어제 19:33이면 그때 실행된 것인데, 파일이 오늘 09:10에 쓰였다. `save_run`의 호출부가 `collect_run` 하나뿐이라 큐레이션 재저장은 아니다. 여기서 더 좁히려면 실행 기록이 필요했는데 — 바로 그날 오전에 로그 파일을 넣은 이유가 이것이었다(위 "로그 파일 도입"). 다만 원인이 무엇이든 **어긋남 자체가 구조적으로 불가능해졌으므로** 고침은 원인 규명과 독립적으로 유효하다.

**데이터 복구**: 잘못된 이름의 파일을 올바른 이름(`2026-08-25_19-30.json`)으로 복사하고, 원본은 `trash-can/`으로 옮겼다(작업 규칙 — 바로 삭제하지 않음). 복구 후 `run_exists('19:30', '2026-08-26')`가 False로 돌아와 그날 저녁 회차가 정상 실행될 수 있게 됐고, 08-25는 6회차 전부가 제자리를 찾았다.

**회귀 테스트**: `tests/test_integration.py`의 `test_run_filename_follows_run_at_not_save_time` — 자정을 넘긴 회차의 파일명, 같은 슬롯 다른 날짜의 비덮어쓰기, `run_exists`의 날짜별 구분, 깨진 `run_at`에서도 저장이 실패하지 않는 것 네 가지를 함께 본다.


## 홈 상단 통계줄 — 건수 강조 제거 · 칩 마우스오버 — 2026-08-27

위 "홈 화면 재구성"에서 만든 상단 카드를 실제로 며칠 써본 뒤 나온 세 가지 지적(사용자,
2026-08-27)을 반영했다.

1. **칩의 알약 테두리가 무겁다.** 목업으로 4안(글자만 / 왼쪽 색 막대 / 사각 태그 / 밑줄)을
   만들어 비교했고 **글자만**으로 정했다 — 테두리를 지우면 카드 오른쪽에서 두 개가 떠 있던
   무게가 사라지고, "눌러볼 수 있다"는 마우스를 올렸을 때 들어오는 회색 배경(`row_hover`)이
   대신 말해준다. 왼쪽 색 막대안(확정본 기사 행의 `COLOR_SCOOP_BAR`와 같은 문법)도
   후보였지만, 한 줄로 눕힌 스트립에서는 그것마저 과했다.
2. **둘을 다 누르면 둘 다 떠 있었다.** `<details>` 기본 동작이라 서로를 모른다 — 열려 있는
   다른 칩을 닫아주는 처리를 넣었다.
3. **큰 숫자가 과했다.** "건수가 있으면 좋은데 숫자가 강조될 건 아니다, 이건 정기
   모니터링하는 사람만 신경 쓰는 값이라 애매하다"는 지적. 목업에 "작게 남김 / 완전히 뺌"
   두 안을 넣어 비교했고 **작게 남김**으로 정했다 — 완전히 빼면 상단 카드가 알림 칩 두 개만
   남아 카드로 둘 이유 자체가 없어지고, "오늘 얼마나 모였나"를 물을 자리가 홈에서 사라진다.
   추이 카드 헤더에 흡수하는 안(세로 90px 추가 절약)은 기각했다 — "정책 단어 추이"라는
   제목이 윗줄의 건수까지 설명하는 것처럼 읽힌다.

**마우스오버로 바꾼 것도 같은 자리에서 나왔다** — "건수 한 줄 확인하는 데 굳이 클릭까지
하는 게 불편하다". 다만 hover만으로 열면 (a) 스트립 위를 스쳐 지나갈 때 툭툭 열리고,
(b) 목록으로 마우스를 내리는 도중 틈에서 닫히고, (c) 터치 화면에서는 아예 못 연다.
그래서 여는 지연 120ms / 닫는 지연 220ms + 투명 다리(`.hllist::before`)를 두고, `<details>`
자체는 남겨 클릭 경로가 항상 살아 있게 했다(터치는 hover 이벤트가 없어 자연히 이쪽으로
떨어진다). 클릭은 여기에 "고정"이라는 뜻을 하나 더 얹었다 — 목록의 링크를 여러 개 새 탭으로
열 때 마우스가 벗어나도 안 닫혀야 해서다.

**목업**: 칩 4안 · 건수 2안을 스위처로 비교하는 `HOME_STAT_STRIP_MOCKUP.html`을 만들어
골랐고, 결정 후 `trash-can/`으로 옮겼다.

## 홈 화면 폭 640 → 960 + 쟁점 ∥ 추이 — 2026-09-11

**요청**: "메인화면도 지금 폭이 좁다, 넓게. 대신 정책 단어 추이 + 오늘의 쟁점을 병렬로"
(사용자). 이어서 "쟁점을 왼쪽으로".

**시안** (`HOME_WIDTH_MOCKUP.html`, 그날 실제 데이터로 그림): 폭 800 / 960 / 1080 ×
비율 1:1 / 추이 넓게 / 쟁점 넓게를 스위처로 비교했고 **960 · 1:1 · 쟁점 왼쪽**으로 정했다.

- **800은 기각** — 설정 화면(2026-09-11)에서 "확정본·초안과 같은 800"으로 앱 폭을
  통일한 직후라 가장 먼저 검토했지만, 반으로 나누면 칸 안쪽이 330px대다. 그날 데이터로
  쟁점 5줄 중 1줄이 칩이 밀려 두 줄이 됐고, 그래프는 오른쪽 끝 라벨 자리(118px)를 빼면
  선이 180px 안에 갇혔다. 홈은 목록이 아니라 대시보드라 예외로 뒀다.
- **비율** — 7일 그래프는 점이 7개뿐이라 좁아도 읽히고, 오히려 쟁점 이름(15자 안팎)이
  폭을 더 먹는다. "쟁점 넓게"는 800에서 그래프가 250px대까지 좁아졌다. 1:1이 무난했다.
- **그래프 글자 크기가 진짜 문제였다.** 홈 그래프는 viewBox 760으로 그려 칸에 맞춰
  줄이는 방식이라, 예전 640px 한 줄에서도 558px로 줄며 축 글자가 7px대였다. 반쪽 칸에
  그대로 넣으면 5px대가 된다. 그래서 칸 안쪽 폭(412)으로 **다시 그리게** 했다 — 글자가
  1:1로 나온다. 대신 창이 좁아 위아래로 쌓일 때(800px 이하)는 그래프가 칸 폭만큼 커진다
  (휴대폰 375px에서는 칸 293px에 맞춰 0.71배로 줄어, 예전 0.39배보다 글자가 두 배 가까이 크다).
- **쟁점 줄** — 이름·칩·막대가 형제로 한 줄이라, 좁은 칸에서 칩이 막대까지 밀어냈다.
  이름+칩을 한 상자로 묶어 칩만 아랫줄로 내려가게 했고 막대를 84 → 64px로 줄였다.
- **쌓이는 순서** — 좁은 화면에서 쟁점이 위로 간다. 예전(추이가 위)과 반대지만, 넓은
  화면에서 왼쪽 = 먼저 읽는 자리로 정했으니 좁은 화면도 같은 순서를 따른다.

## 정책 단어 추이 축 맞춤 · 홈 카드 범례·각주 제거 — 2026-09-11

**제보**: 쟁점 밑에 여백이 너무 많다. 추이 그래프는 늘 100 축이라 위가 비고, 이걸
정량으로 보여주는 게 맞는지 모르겠다(사용자, 스크린샷).

**원인 둘**
- **쟁점 밑 여백** — 쟁점 카드 탓이 아니었다. `.pair`가 두 카드를 같은 높이로 늘리는데
  추이 카드가 더 길었다(실측 43px 차이). 그 길이는 **같은 말을 두 번 하는 범례**(그래프
  오른쪽 끝 라벨 "이형일 14" + 아래 범례 "이형일 144건")와 각주("숨긴 기사 포함 · 옅은 띠는
  주말")에서 왔다. 각주는 "본업 화면은 자기를 해명하지 않는다"에도 걸린다.
- **100 축** — 고정이 아니라 사다리가 거칠었다. `app/trend_chart.py`가 최댓값 자체를
  `10, 20, 50, 100, …`에서 골라 51~100이 전부 100이 됐다. 그 주 최댓값이 54라 위 45%가
  비었다. 축 = 「눈금 한 칸(1·2·5 계열) × 3」으로 바꿔 54 → 60, 120 → 150처럼 붙인다.
  `/trend`도 같은 함수라 함께 좋아졌다(3개월 주별: 600 축).

**정량이냐 모양이냐** — 시안(`HOME_TREND_SHAPE_MOCKUP.html`, 그날 실제 데이터)으로 다섯을
비교했다: A 정량 유지 / A-1 A에서 숫자만 지움 / A-2 한 그래프에 단어마다 자기 축 / B 단어마다
한 줄(자기 축 스파크라인 + "7일 N건") / C 농도 칸. Claude는 B를 권했다 — 공유 축은 작은
단어(종부세 7일 9건)를 바닥에 눌러 모양을 지우고, A-1은 그 문제를 못 풀면서 크기 단서만
없애며, A-2는 선끼리 높이가 서로 다른 잣대라 거짓말을 한다(9/10에 비거주 9건이 이형일
14건보다 위에 찍힘). C는 방향이 선보다 늦게 읽힌다. **사용자는 A를 골랐다** — 정량 그대로
두고 군더더기만 걷는다. 알고 감수한 한계: 공유 축이라 작은 단어는 여전히 바닥에 붙는다.

## 정책 단어 추이 x축 — `13일(일)` + 「어제」 — 2026-09-14

**제안**(사용자, 홈 카드 스크린샷): 날짜 아래에 어제의 경우 '어제'를 넣으면 어떨까.
이어서 "표기를 13일(월)처럼 하는 건 어때 — 월수까지 안 해도 되지 않을까".

**왜 필요했나** — 9/11에 추이를 어제까지로 자르고 홈 카드의 범례·각주도 걷어내면서,
오른쪽 끝 라벨 `이형일 30`이 **오늘 값처럼 읽히는** 상태가 됐다. 그게 어제 값이라는 걸
말해주는 건 `9/13(일)` 글자뿐이라, 오늘 날짜를 떠올려 한 번 따져야 했다.

**9/11 결정("어제까지" 문구 안 붙임)과 부딪히지 않나** — 그때 뺀 건 화면이 자기를 해명하는
**문장**이었다. 「어제」는 축 위 값의 **이름표**라, 만드는 사람의 걱정이 아니라 읽는 사람의
질문("이거 오늘 거야?")에 답한다고 봤다. 사용자에게 이 경계가 가깝다는 걸 밝히고 판단받았다.

**시안**(`YESTERDAY_LABEL_MOCKUP.html`, 그날 실제 데이터 — 1차에 네 안, 2차에 규칙 확정본):
A 그대로 / B `9/13(일)` + 「어제」 / C `13일(일)` / D `13일(일)` + 「어제」. **D로 정했다.**
- **C만으로는 안 풀린다** — `13일`이 `9/13`보다 빨리 읽히긴 해도 "이 칸이 어제"는 여전히
  오늘 날짜를 떠올려야 안다.
- **월을 빼도 되나** — 달이 바뀌는 주(8/28~9/3)도 그려 봤다. `31일 → 1일`로 숫자가
  되돌아가 달이 바뀐 게 저절로 보인다. 7일 폭에선 따로 `9월`을 붙일 필요가 없었다.
- **날짜를 「어제」로 바꾸지 않고 밑에 덧붙인다** — 요일 글자가 주말 띠와 서로를 설명하는
  짝이라(2026-08-27) 없애면 안 된다.

**범위** — 그래프는 홈과 `/trend`가 같은 `render_chart`라, 규칙을 하나로 두려고 셋으로 정리했다:
(1) 요일까지 다 찍히는 10칸 이하는 `13일(일)` (2) 1개월 이상은 달이 여러 번 바뀌므로 `9/7`
그대로 (3) 「어제」는 일별이고 마지막 칸이 실제 어제일 때만 — 주별·월별 마지막 칸은 한 주·한
달이라 「어제」가 틀린 말이 된다.

**구현하다 찾은 것** — `wd_label`(`9/13(일)`)을 그래프만 쓰는 줄 알았는데, `/trend`의 표·📋
복사·⬇ 엑셀 헤더도 **일별 전체(최대 31일)에** 같은 값을 쓰고 있었다. 그걸 `13일(일)`로 바꾸면
1개월 표·엑셀이 `17일(월) … 13일(일)`로 달을 건너 뭉개진다. 그래서 그래프 전용 `short_label`을
따로 두고 표·내보내기는 `wd_label` 그대로 뒀다 — 7일 보기에서 그래프 `13일(일)` ↔ 표 `9/13(일)`로
갈리는 건 알고 감수한다(표는 화면 표시가 아니라 내보내는 데이터라).

**높이** — 처음엔 "그래프가 11px 길어진다"고 말했는데, 시안·구현 모두 **그래프 전체 높이는
그대로 두고 아래 여백만 11px 늘렸다**(본체가 그만큼 낮아짐). 홈 카드 높이가 안 변해 쟁점
카드와 밑선이 그대로 맞는다.

**감수한 것** — 탭을 자정 넘어서까지 열어두면 「어제」가 그저께를 가리킨다. 날짜가 같이
적혀 있어 틀린 정보가 되진 않는다.

## 홈 화면 ⚙ 설정 자리 — 2026-09-11

**요청**: 화면 하단에 있는 [설정]을 「실시간」 메뉴 위로 끌어올려 달라(사용자).

**실제로는 두 개였다** — 흐름도 카드 맨 아래 알약(`.gearrow`)과 페이지 맨 아래 문의 메일
바로 위(`.settings-link`). 둘 다 흐름도 카드를 다 지나야 나왔다. 하나만 올리면 나머지가
아래에 그대로 남아 "아직 아래 있다"가 되므로, 올린 하나만 남기고 둘 다 없앴다.

**자리**: 두 안을 비교했다(`HOME_SETTINGS_POS_MOCKUP.html`). A = 「실시간」 라벨 줄의 빈
오른쪽(적용), B = 카드 맨 위 별도 줄 + 구분선. B는 줄 하나(약 37px)가 통째로 늘고 설정
하나 때문에 카드에 구분선이 생겨 무거워져서 기각했다. A는 새 줄을 만들지 않아 카드 높이가
거의 안 늘고, 아래 두 개가 빠진 만큼 페이지는 오히려 짧아졌다.

## 홈 화면 재구성 — 2026-08-26

**문제의식**: 홈(`home.html`)은 4갈래 흐름도(실시간/정기/수시/라벨) + 당일 워드클라우드가
전부였다 — "현 기사 상황을 알 수 있는 게 워드클라우드뿐"이라는 지적(사용자, 2026-08-26)에서
시작했다. 애초엔 흐름을 더 매끄럽게 만들려 했는데, 대화가 진행되며 "미니 대시보드처럼
현황을 보여주는 게 낫지 않겠냐"는 방향으로 바뀌었다.

**여러 차례 실측하며 좁혀간 설계.** 결과만 남기면 왜 이 모양인지 이해가 안 되므로 순서대로:

1. **AI 소제목 요약 재활용 검토** — 확정본 하단에 이미 있는 `group_summary`(LLM이 분류와
   같은 호출에서 쓴 요약)를 재사용하면 API 호출 0원으로 "오늘 뭐가 있었나"를 보여줄 수
   있다는 데서 출발했다. 그런데 실측(2026-08-25)해보니 그날 소제목의 66%(192/292건)가
   "기타"였고, 최대 그룹(17:00 회차 119건 중 112건)조차 요약이 없었다 — **0단계(왜
   기타가 이렇게 많은가)가 먼저였다.**
2. **0단계: "기타" 과다 원인 3개 발견.**
   - `app.llm_classifier.assign_to_existing`의 `max_tokens=2000`이 너무 작아 회차 마감
     시 새 기사 100건 이상을 배정하는 응답이 잘렸다(실측 필요 토큰 2,900+) — `classify_
     with_llm`이 2026-08-20에 이미 겪은 것과 같은 종류의 잘림인데 이 함수만 안 고쳐져
     있었다. `max_tokens=8000`으로 수정(app/llm_classifier.py).
   - `MAX_ARTICLES_FOR_CLASSIFY`(150) 초과 회차(152건)가 분류 자체를 건너뛰고 규칙
     기반으로 떨어졌다 — 코드 주석에 이미 "150건 근접 회차가 생기면 다시 실측할 것"이라고
     적혀 있었는데 그 회차가 실제로 생겼다. (이 항목은 실측만 하고 아직 안 고쳤다.)
   - 1차 분류가 흔들리거나 `_refine_etc_bucket`(재정리)이 안 먹는 회차가 있었는데,
     **로그 파일이 없어 원인을 확정하지 못했다** — 그래서 로깅부터 넣었다(아래 "로그
     파일 도입", 같은 날 별도 섹션).
3. **회차 파일 날짜 불일치 발견·수정** — 홈 시안을 오늘(08-26) 데이터로 뽑다가 어제
   19:30 회차가 오늘 파일명으로 저장된 걸 발견했다. `app.storage.save_run`이 파일명
   날짜를 `datetime.now()`가 아니라 `run_at`에서 뽑도록 고쳤다 — 별도 섹션("회차 파일
   날짜 불일치") 참고. 이걸 안 고쳤으면 오늘 저녁 회차가 "이미 실행됨"으로 오판돼
   통째로 누락될 뻔했다.
4. **홈 대시보드 반복 시안 — 주요 반려/채택 지점만:**
   - **시간대별 유입(회차별 막대)** → 반려. 회차 창 길이가 1.5~6시간으로 들쭉날쭉해
     막대 높이가 "기사가 많았다"와 "시간이 길었다"를 구분 못 했다. 3시간 고정 버킷으로
     바꿨다가, 최종적으로는 **아예 뺐다** — 아래 쟁점별 정보(순위·그룹칩)가 이미 더
     정확한 정보를 주고, 총량 막대는 매일 비슷한 모양(오전·오후 두 봉우리)이라 며칠
     지나면 안 보게 된다는 판단(사용자).
   - **AI 요약문(오늘의 쟁점 각 줄)** → 처음엔 "안 보이던 걸 홈으로 끌어오자"고 접었다
     안 접었다 시도했지만, 최종적으로 **뺐다**(사용자 결정) — 확정본 하단에서 요약이
     안 읽히던 원래 이유를 홈에서도 반복하게 되는 트레이드오프를 감수하기로 함.
   - **[단독]/[속보]를 다른 화면으로 보낼까** → 반려. 실시간(캐시 1,017건 등)과
     확정본(회차 1개)은 홈이 세는 292건(정기 전체)과 모집단이 달라 건수가 어긋난다.
     홈 그 자리에서 펼치는 것으로 확정.
   - **정책 카테고리(세제/예산/공공…) 막대 그래프(빅카인즈 "오늘의 뉴스 현황" 참고)**
     → 사전 방식(0원)과 LLM 방식(소제목만 분류, 하루 5원) 둘 다 검토했으나 "오늘의
     쟁점" 순위표로 충분히 대체된다고 보고 보류.
   - **정책 단어 추이 축 자체** → "3시간마다 갱신되는 순위" 아이디어에서 "며칠~몇 주
     단위로 쟁점이 이동하는 걸 그림으로" 아이디어로 발전, 최종적으로 "특정 정책 단어의
     N일치 추이를 꺾은선으로"(사용자 제안)로 수렴. 회차 단위 집계 대신 **단어 문자열
     매칭**을 쓰기로 한 결정적 이유: 소제목 분류(기타 66%)에 안 걸리기 때문 — 실측
     TOP5 회차별 배지 방식은 분류가 깨지면 같이 깨지는데, 문자열 매칭은 기타 기사
     안의 단어도 센다.
   - **추이에 넣을 단어 — 자동(TOP3/급등) vs 수동(담당자 지정)** → 수동으로 확정
     (사용자 판단, 2026-08-26). 실측: 건수 TOP3는 8일 중 6일이 "재정경제부/구윤철/
     기획재정부"로 사실상 동일 대상의 다른 표기였고, 급등 기준은 5일 중 4일이 매번
     다른 단어였다 — 추이 그래프는 "같은 대상을 계속 지켜본다"는 전제가 있어야 뜻이
     있는데 둘 다 그 전제를 깬다. 대신 급등은 "그래프 밖에서 한 줄 제안"으로 살렸다
     (그래프 선 개수는 늘리지 않음).
   - **추이 단어를 등록 검색어로만 제한할까** → 반려. "재정경제부"(검색어)와 "종부세"
     (정책 단어)는 다른 축이라는 지적(사용자) + 세제개편 그룹이 `include_in_scrap=
     False`라 정기 수집에 안 들어와도 51건이나 언급되고 있었다는 실측 — 자유 입력 +
     등록 검색어 추천(datalist)으로 결정.
   - **일정 기사(뉴시스 "오늘의 주요일정" 류)가 오늘의 쟁점 2·3위를 차지하는 문제**
     → "정책어라 볼 수 없다"는 지적(사용자)으로 발견. 제목 맨 앞 말머리 안에 "일정"이
     있는지로 판정([단독]/[속보]와 같은 모양) — 저장된 고유 제목 2,278건 전수조사,
     53건 적중·오탐 0건. 순위에서만 빼고 확정본·소제목 분류는 그대로 둔다.
   - **[단독]/[속보] 알림과 화면 표시가 다른 모집단을 볼 수 있는 문제** → "알림/화면
     보여지는 것 그냥 다 통일해야 맞아"(사용자)로 확정. `app.alerted_urls`가 URL만
     저장하던 걸 발송 기사의 제목·언론사·종류·발행시각까지 저장하도록 넓히고
     (`items`), 홈은 회차 파일을 다시 세지 않고 이 발송 기록 하나만 본다. 사이렌도
     이모지(🚨) 대신 SVG 아이콘으로(`app.icons`) — "의미 전달 안 됨" 지적에 맞춰
     문구도 "말머리 알림"에서 "[단독]·[속보]"로 바꿨다.
   - **상단 고정 네비게이션 바(9개 항목 한 줄)** → 반려. "지저분해 보인다"(사용자) —
     원인은 성격이 다른 항목(매번 누르는 것 vs 가끔 보는 것)이 구분 없이 평평하게
     늘어서 있었던 것. 처음엔 "지금 할 일"(초안·새수집 큰 버튼) + "둘러보기"(나머지
     작은 링크) 2단으로 재구성했다가, **최종적으로는 예전 4갈래 흐름도(화살표 포함)를
     그대로 되살리고 상태값만 덧붙이는 것으로 롤백**했다(사용자: "예전 플로우를 우선
     살려줘") — 새 레이아웃보다 이미 익숙한 구조에 정보만 얹는 쪽을 선택.
   - **오늘의 쟁점 각 줄의 "시간대 집중" 표시** — 처음엔 "오전 9~12시 집중 (12건)"
     문장으로 넣었다가 줄 수를 2배로 먹어 미니 막대(0~24시 8칸 스파크라인)로 바꿨는데,
     "그래프가 더 헷갈린다"(사용자)는 이유로 **최종 제거**. 정책 단어 추이가 이미
     시간축 정보를 담당하고 있어 중복이기도 했다.

**결과로 남은 원칙 셋**은 CLAUDE.md "홈 화면(진입 화면)" 참고 — 정기 기준만 본다,
API 호출 0회, 담당자가 명시적으로 고른 것만 지켜본다.

**알려진 단순화(다음 과제)**: 정책 단어 입력창에 "타이핑하는 동안 최근 7일 건수를
바로 보여주는" 실시간 미리보기는 시안에서만 그렸고 실제로는 안 만들었다 — AJAX
엔드포인트가 새로 필요해 이번 통합 범위 밖으로 뒀다. 지금은 단어를 추가한 뒤 그래프에
반영된 값으로 확인한다.

### 정책 단어 추이 — 오늘 대신 어제까지 — 2026-09-11

**제보**: "정책단어추이를 오늘 것을 보여주니까 오늘 것은 바로바로 반영이 안 돼서
이상하다. 지금 시간에."

**원인**: 추이는 저장된 정기 회차 파일만 센다(API 호출 0회 원칙). 그래서 오늘 칸은
"그 시각까지 저장된 회차"만 담은 **하루치의 일부**다. 실측(2026-09-11 09:48):

| 날짜 | 저장된 회차 | 기사 수 | 이형일 |
|---|---|---|---|
| 9/9 | 5 | 279 | 2 |
| 9/10 | 6 | 234 | 14 |
| **9/11(오늘)** | **2** (06:00·09:30) | **23** | **5** |

마지막 점을 점선 + 빈 원으로 "집계 중"이라 표시하고 있었지만, 눈은 점선보다 급락을
먼저 읽는다 — 모든 단어가 매일 오전 오늘 무너지는 것처럼 보였다. 흐름을 보라고 만든
화면에서 마지막 점 하나가 전체 인상을 틀리게 만들고 있었다.

**결정**(사용자): 추이는 **어제까지만** 그린다. 근거 — 모든 점이 하루 전체를 센 값이
돼 점끼리 비교가 성립하고, "오늘 무엇이 떴나"는 홈의 오늘의 쟁점 카드가 이미 답한다
(두 카드가 "오늘 / 흐름"으로 역할이 갈린다). 잃는 건 "오늘 튄 단어가 추이에 바로 안
보인다"뿐인데 그건 이 그래프의 역할이 아니다.

**기각한 대안 둘**: (1) 초안 캐시(`preview_cache`)까지 합쳐 오늘을 실시간으로 — 확정
회차와 모집단·창이 달라 오늘 점만 다른 잣대로 센 값이 된다(2026-09-01 숨김 필터 사고와
같은 모양). (2) 오늘을 "어제 같은 시각까지"와만 비교 — 홈 건수 줄은 그렇게 하지만,
그래프에선 점마다 뜻이 달라져 읽을 수가 없다.

**바꾼 것**:
- `app/trend_data.py` `last_complete_day(today)`(= 어제)를 두 화면이 끝 날짜로 쓴다.
  홈 7일, `/trend` 빠른 설정 다섯 개, 직접 입력(종료일이 어제를 넘으면 어제로 당김 +
  날짜 칸 `max`) 전부. 같은 화면 안에서 빠른 설정만 어제에서 끝나고 직접 입력은 오늘까지
  되면 규칙이 둘이 되므로 함께 묶었다.
- **점선 판정을 옮겼다.** 예전 `b["partial"] = 오늘 in b["dates"]`는 끝이 어제가 되는
  순간 항상 False가 돼, 3개월(주별)·1년(월별)의 이번 주·이번 달 칸 — 어제에서 잘려도
  여전히 덜 찬 칸 — 의 점선이 조용히 사라질 뻔했다. `fill_bucket_status`가 "그 칸의 원래
  기간(일/주 일요일/월 말일)이 아직 안 끝났나"로 판정하고, 두 화면에 복제돼 있던
  record/partial 채우는 루프도 이 함수 하나로 합쳤다.
- 홈 캡션에서 `마지막 점선은 아직 집계 중`을 뺐다(일별이라 점선이 더는 안 생긴다).
  "어제까지"라는 문구는 안 붙였다 — x축 마지막 라벨 `9/10(목)`이 이미 말한다.

**감수한 것**: 마지막 회차(지연 수집 포함 20시 무렵)가 끝나면 오늘치도 사실 다 찼지만
자정까지는 안 보인다. 몇 시간 차이라 규칙을 복잡하게 만들 이유가 없다고 봤다.

### 정책 단어 추이가 숨김에 흔들리던 문제 — 2026-09-01

**제보**: "종부세 오늘 많은데 왜 반영이 안 되고 있어?" — 홈 카드 그래프의 9/1 값이
`종부세 1건`이었다. 그날 종부세 기사는 실제로 쏟아지고 있었다(비거주 1주택 종부세
기본공제 12억 유지 확정).

**원인**: `app/trend_data.py` `articles_by_date_range`가 `app.curation.is_hidden`으로
숨긴 기사를 빼고 세고 있었다. 그날 담당자가 큐레이션하면서 같은 속보의 중복 기사를
뭉텅이로 숨겼고(14:03~17:01, 5·15·8·8건 단위), 종부세 원본 43건 중 42건이 그렇게
빠져 1건만 남았다.

**진짜 문제는 그 다음이다 — 이 필터는 오늘에만 걸린다.** 숨김 기록
(`data/hidden_articles.json`)은 `_load_records`가 오늘 것만 읽는 **당일 전용**
저장소라(자정 초기화, 2026-07-26 결정) 어제·그저께는 숨긴 게 있었어도 필터가
아무것도 못 걸러낸다. 결과적으로 그래프가 **과거는 원본 건수, 오늘만 큐레이션 후
건수**로 그려지고 있었다 — 같은 축에 다른 잣대를 섞은 셈이라 비교 자체가 성립하지
않았고, 오늘 값은 담당자가 정리할수록 내려갔다. 실측(2026-09-01, 저장분 전체):

| 날짜 | 종부세 원본 | 그래프에 찍힌 값 |
|---|---|---|
| 8/26 | 11 | 11 |
| 8/27 | 8 | 8 |
| 8/31 | 1 | 1 |
| **9/1** | **43** | **1** |

**결정**(사용자): 숨김 필터를 뺀다 — "절대값을 보려는 게 아니라 추이를 보는 거라
숨김 등에 영향을 받으면 안 된다." 근거 셋이 같은 방향을 가리켰다. ① 이 카드는
**주제 축**이다 — 같은 파일 `all_registered_keywords`가 이미 같은 이유로
`include_in_scrap`(수집 축)을 안 본다. ② 숨김은 "보고서에서 뺀다"이지 "그런 기사가
없었다"가 아니다(라벨 보관함이 숨긴 기사를 "🗑️ 숨김" 달고 남기는 것과 같은 원칙).
③ 애초에 **일관되게 적용할 방법이 없다** — 당일 전용 저장소라 과거엔 걸 수가 없다.

**반대 방향(숨긴 건 진짜 안 센다)은 기각.** 그러려면 숨김 기록을 날짜별로 1년
보관해야 하는데, 자정 초기화 정책과 정면으로 부딪히고 공사도 훨씬 크다.

**바꾼 것**: `articles_by_date_range`에서 `hidden_urls` 인자와 `is_hidden` 호출을
빼고, 호출부 둘(`app/landing_renderer.py` `_trend_card_html`,
`app/trend_renderer.py` `render_trend_page`)에서 `load_hidden_urls()` 전달을
없앴다. 캡션도 사실에 맞췄다 — 홈 `기사 제목·요약 기준(숨긴 기사 포함)`,
`/trend` 부제 `숨긴 기사 제외` → `숨긴 기사 포함`(그 문구는 **틀린 설명이 아니라
정반대 설명**이 된 상태였다). 결과: 9/1 종부세 43·비거주 42, 7일 합계 63·64건.

**같은 모양의 문제가 홈 "오늘 건수" 카드에도 있었고, 같은 날 이어서 고쳤다.**
`오늘 N건 · 어제 같은 시각 M건 대비 X%`도 오늘만 `filter_hidden`으로 거르고 어제는
`is_hidden`이 사실상 아무것도 못 걸러(어제 URL은 당일 전용 숨김 집합에 애초에 없다)
같은 편향을 갖고 있었다. 처음엔 "오늘 얼마나 모였나라는 다른 질문에 답하는 자리"라
범위 밖으로 뒀는데, 사용자가 "그것도 빼줘"로 결정. 실측(2026-09-01): 실제
`335건 / 어제 같은 시각 170건 대비 +97%`인 날이 `117건 / -31%`로 표시되고 있었다 —
부호까지 뒤집혀 있었다.

**단, 같은 함수(`generate_landing_page`) 안의 `articles` 목록은 안 건드렸다.** 그건
워드클라우드와 "오늘의 쟁점"이 쓰는 목록이고, 그 둘은 "오늘 보고서에 뭐가 실렸나"라는
**보고서 축**이라 숨김이 반영되는 게 맞다(`filter_hidden` docstring의 "키워드 추출·
워드클라우드도 이 함수를 거친 목록만 쓴다"가 그대로 유효하다). 상단 건수 줄만 별도
집계 `all_seen`(숨김 무시, url 중복 제거)을 쓰게 나눴다. 이 변경으로 `hidden_urls`를
쓰는 곳이 `render_landing_page`에 하나도 안 남아 그 인자와 `is_hidden` import도 함께
지웠다(죽은 채로 두면 매 홈 렌더링마다 `load_hidden_urls()`만 헛돈다).

### 정책 단어 추이 — 홈 카드 분리 + 전용 화면(/trend) 신설 — 2026-08-27

**계기**: 위 8/26 설계에서 "정책 단어 추이"는 홈 카드 하나가 조회(7/14/30일 탭)·조작
(단어 추가/삭제)·급등 제안까지 전부 떠안고 있었다. 사용자가 "1년마다 보고서를 써야
하는데 그때 6개월~1년 단위로도 볼 일이 있을 것 같다"고 요청 — 회차 보관이 이미 1년
(`RETENTION_DAYS`)이니 그만큼 볼 수 있어야 데이터를 버리지 않는 셈이다. 처음엔 "홈
카드에 6개월/1년 탭만 추가"를 검토했지만, 실측(2026-08-27, 그 시점 저장분 17일치
기준 외삽)해보니 180일 그래프는 257ms, 365일은 522ms가 걸렸다 — **홈은 요청마다
다시 그리는 화면이라 매 열람 500ms를 얹을 수 없다.** 그래서 "홈은 가볍게 보기만,
조작은 별도 화면에서"로 방향을 잡았다(사용자 동의).

**목업 3라운드로 좁힌 결정들**:
1. **1라운드(전용 화면 첫 목업)** — 기간 세그먼트(1개월/3개월/6개월/1년) + 시작~종료일
   직접 입력, 집계 단위 자동 전환(일/주/월), 최대 8개 단어, 표+엑셀 내보내기까지
   한 화면에 넣어 그려봤다. 일별 뷰의 x축 라벨이 3일 간격으로 솎여 하필 같은 요일만
   반복되는 문제를 이때 발견 — "월요일마다만 찍기"로 해결(아래 4).
2. **2라운드(사용자 피드백 4건 반영)**: ① 최근 그래프는 **5일 아닌 7일**로(사용자
   정정) — 실제로는 "최근 그래프만 보이게" 요청이라 홈 카드를 7일 고정으로 확정.
   ② 전용 화면 진입 시 기본값도 7일로(6개월 등으로 열려 있으면 "왜 이렇게 크게
   열리지"가 됨). ③ **홈의 "추이에 추가" 급등 제안을 없애 달라** — "정량 데이터를
   얻으려는 게 아니라 추이(흐름)에 집중하고 싶다"는 이유(사용자) — 급등 제안 자체를
   지운 게 아니라 "단어를 실제로 고치는 자리"인 전용 화면으로만 옮겼다. ④ 진입
   링크 글자를 화살표 없는 "더보기"로 단순화.
3. **3라운드(홈 카드 최종 슬림화)** — 사용자가 "메인은 최대한 간결하게"라며 정책
   단어 칩(×로 빼기)·"+ 추가" 입력폼·7/14/30일 탭을 홈에서 전부 빼 달라고 요청.
   그 결과 "홈에 단어가 0개일 때 뭘 해야 하는지 알 길이 없어지는" 구멍이 생겨(전엔
   입력폼이 그 자리에서 저절로 해결했었다) — 빈 상태 안내("더보기에서 단어를
   골라주세요")를 새로 추가해 메꿨다.

**마우스 호버 툴팁**: 사용자가 "홈 그래프도 마우스 올리면 팝업 뜨게 할 수 있냐"고
물어 확인차 만든 것 — 서버가 렌더링 시점에 이미 계산한 날짜별·단어별 값을 SVG의
`data-buckets`/`data-series` 속성에 그대로 심어두고, 크로스헤어+툴팁은 순수 클라이언트
JS(`app.trend_chart.HOVER_JS`)로 그린다. 서버 왕복도 재계산도 없어 "새로 도는 API
호출이 없다"는 홈 화면 원칙을 그대로 지킨다. 홈·전용 화면 두 곳 다 같은 JS/CSS를
공유한다(`app.trend_chart.render_chart`/`chart_css`).

**[수정: 2026-08-27] 홈 카드에서는 그 툴팁을 뺐다** — 사용자 지적: "마우스오버 이건
필요 없을 듯, '더보기' 있어서. 눌렀을 때 링크만 되면 될 듯." 홈 카드가 3라운드
슬림화를 거치며 그래프 전체가 `/trend`로 가는 링크 하나가 됐는데, 그 위에 툴팁이
뜨면 정작 누르려는 링크를 덮고 "여기서 자세히 볼 수 있다"는 신호까지 줘 "더보기"와
역할이 겹쳤다. 툴팁과 함께 `title="눌러서 추이 화면으로"` 네이티브 툴팁도 뺐다(같은
말을 두 번 하는 자리였고, 화면 요소가 이미 링크 커서로 그걸 말한다). 전용 화면은
그대로 — 거기서는 그래프가 링크가 아니라 판독 대상이라 툴팁이 본업이다. 구현은
`render_chart(hover=False)` 한 플래그로 갈랐다: 안 쓰는 화면에 `hover-layer`/
`trend-tip`/`data-buckets` 빈 껍데기만 남겨두면 다음 사람이 "왜 안 뜨지"부터
의심하게 되므로 자리 자체를 안 그린다.

**주말 표시(4번 질문) — 세 가지 시안 비교 후 B안 채택**: A(음영만, 기존)는 흑백
인쇄나 색약에서 "이게 왜 낮은지"가 안 읽혔고, C(요일 라벨만)는 낮은 구간과 "(토)"가
눈에서 안 이어졌다. **B(음영 + 요일)** — 주말 띠를 날짜 라벨 줄까지 내려서 띠와
"(토)" 글자가 서로를 설명하게 만든 안을 사용자가 택했다. 일별 x축이 10개를 넘으면
"월요일마다만" 라벨을 찍기로 한 것도 같은 논의에서 나왔다 — 3일 간격 솎기가 항상
같은 요일만 남겨 라벨이 무의미해지는 문제를 없애면서, 요일을 주 단위로 정렬해 주말
띠와 리듬을 맞췄다.

**색상 재검증**: 예전 홈 카드의 3색(`COLOR_HEADER`/`COLOR_ACCENT`/회색)을 dataviz
스킬의 `validate_palette.js`로 검증하니 FAIL — 1번 색은 명도가 밴드 밖, 3번 색은
채도가 바닥이라 회색으로 읽혔다(3개일 땐 버텼지만 8개로 늘리며 못 쓰게 됨). 8개
categorical 슬롯 모두 검증 통과하는 램프(참조 팔레트, CVD ΔE 9.1/일반시야 19.6)로
전량 교체(`COLOR_TREND_1`~`8`) — 홈은 이 중 앞 3개만 써서 두 화면에서 같은 단어가
항상 같은 색을 유지한다.

**[수정: 2026-08-27] 급등 제안을 전용 화면에서도 없앴다 — 기능 자체를 삭제.** 위 2라운드
③에서 홈에서 빼 전용 화면으로 옮겼던 그 한 줄(`✨ 기재부 — 최근 3일 9건 (직전 3일 0건,
9.0배). 지금 지켜보는 단어 밖입니다. [추이에 추가]`)을 사용자가 그래프 화면에서도 없애
달라고 요청. 옮길 때의 근거였던 "단어를 실제로 고치는 자리니 거기서는 맞다"가 실제로
써보니 성립하지 않았다 — 이 화면이 답하는 질문은 "무엇이 뜨고 있나"(탐색)가 아니라
"내가 고른 대상이 어떤 흐름인가"(추적)이고, 8/26에 자동 TOP3·급등 자동채택을 반려한
바로 그 이유("같은 대상을 계속 지켜본다"는 전제)가 제안 한 줄에도 똑같이 걸린다.
살려둘 자리가 없어 `surge_candidate`와 `_SURGE_MIN_RECENT`/`_SURGE_MIN_RATIO` 상수,
`.surge` CSS, 6일치 별도 집계(`articles_by_date_range` 추가 호출 1회)까지 함께 지웠다 —
쓰지 않는 계산을 "언젠가 다시 쓸지도"로 남겨두면 다음 사람이 화면 어디에 나오는지부터
찾게 된다. `_word_card_html`이 이 계산 때문에만 받던 `hidden_urls` 인자도 같이 없앴다.

**설계상 결정**: 단어 저장소(`app.home_trend`)는 그대로 하나(홈·전용 화면 공유)로
두고, 저장 상한만 3→8(`MAX_TREND_WORDS`)로 늘리고 홈 표시 개수를
`HOME_TREND_DISPLAY_COUNT`(3)로 따로 뒀다 — 두 화면이 "무엇을 지켜보는가"까지 다르면
안 되고, "몇 개를 보여주는가"만 달라야 한다는 판단. 급등 제안·전체 데이터 계산
로직(`app.trend_data`)과 SVG 렌더링(`app.trend_chart`)을 홈 카드(`app.landing_
renderer`)와 전용 화면(`app.trend_renderer`)이 공유하도록 분리했다 — 같은 계산을
두 파일에 복붙하면 나중에 한쪽만 고쳐 어긋나는 사고가 난다(이 문서 여러 항목이
반복해 지적한 패턴).

### 오늘의 쟁점 — 건수 숫자를 막대로, 부제·"기타" 줄 제거 — 2026-09-02

**계기**: 사용자가 홈 화면을 보고 세 가지를 한꺼번에 지적했다 — ① 행별 건수(`11건`,
`7건`…)가 필요 없어 보인다, ② 부제 `분류된 47건 기준 · 회차마다 갱신 · 일정 기사
1건 제외`에서 `분류된 nn건 기준`만 남기면 되지 않나("정기 모니터링을 안 하는
사람이면 회차가 없잖아"), ③ `기타 7건` 줄은 없애는 게 좋겠다.

**②는 사용자 말이 맞았고, 오히려 남기려던 것까지 뺐다.** `회차마다 갱신`은
CLAUDE.md "본업 화면은 자기를 해명하지 않는다"에 정면으로 걸리는 문구였고(만드는
사람의 걱정이지 담당자의 질문이 아니다), `일정 기사 1건 제외`는 1건일 때 정보가
아니라 잡음이었다. 문제는 남기려던 `분류된 47건 기준`이었다 — 바로 위 카드가
`오늘 112건`이라고 말하는데 여기서 47이라 하면 "왜 다르지?"부터 묻게 되고, 그 차이를
해명해주던 게 정확히 지우기로 한 `분류된`·`일정 N건 제외` 두 마디였다. 해명만 지우고
숫자를 남기면 **정체불명의 47이 혼자 남아 더 헷갈린다**. 게다가 그 47을 담당자가 쓸
데가 없었다.

**①은 처음에 반대했다.** 이 카드에서 행마다 값이 달라지는 정보가 그 숫자 하나뿐이라,
지우면 1위 11건과 5위 4건이 같은 무게로 보인다 — 순위 번호는 순서만 말하지 격차를
못 말한다. 그래서 "값은 남기고 무게만 덜자"(회색·작게, 상단 건수 줄을 2rem에서 본문
한 줄로 눕혔던 2026-08-27과 같은 방식)를 먼저 제안했다.

**사용자가 백분위(%)를 물어보면서 답이 갈렸다.** %는 기각했는데 이유가 셋이다:
(1) 분모가 `filter_hidden`을 통과한 목록이라 담당자가 기사를 숨길수록 남은 쟁점의
%가 저절로 올라가 날짜 간 비교가 성립하지 않는다 — 바로 전날(2026-09-01) 정책 단어
추이에서 `종부세` 43건 중 42건을 숨겨 그래프에 1건으로 찍혔던 것과 같은 원인이다.
(2) %는 절대량을 죽인다(5건 중 2건도 40%, 100건 중 40건도 40%). (3) 결정적으로
**%는 모수가 화면에 남아야만 읽히는 값이라, 지우려던 부제를 도로 붙들게 된다** —
덜어내려던 게 오히려 필수가 되는 셈이었다.

**그래서 막대(`.ibar`)로 갔다.** 1위를 100%로 둔 상대 길이라 모수를 설명할 필요가
없고, 그 덕에 부제를 통째로 지워도 카드가 성립한다 — 사용자가 원한 "덜어내기"를
%보다 더 많이 해내면서 격차는 남긴다. 정확한 건수는 `title` 툴팁에만 남겼다(값을
완전히 버리지는 않는다). 시안 다섯 개(현재/A: 건수 없음/B: %/C: 막대만/D: 회색 숫자/
E: 막대+숫자)를 [HOME_ISSUE_CARD_MOCKUP.html](mockups/HOME_ISSUE_CARD_MOCKUP.html)로 만들어
비교한 뒤 C를 골랐다.

**③은 ①과 세트라 자연히 따라왔다.** "기타 N건" 줄의 존재 이유는 "TOP5 합계 + 기타 =
전체"라는 검산이었는데, 행별 숫자가 사라지면서 검산할 대상 자체가 없어졌다.

**덤으로 확인한 사실 하나** — 사용자가 "이 건수는 숨김 처리한 게 반영 안 되는
거지?"라고 물었는데 반대였다. 오늘의 쟁점이 쓰는 목록은 `filter_hidden`을 통과한
것이라 숨긴 기사는 47건에서도 행별 건수에서도 이미 빠져 있다(2026-09-01에 상단 건수
줄만 숨김을 안 보게 나눴고, 쟁점·워드클라우드는 "보고서 축"이라 그대로 뒀다 — 위
항목 참고). 그 사실이 %를 기각한 첫 번째 근거가 됐다.

### 흐름도 — 칸 이름만 크게, 설명은 마우스를 올리면 — 2026-09-15

**제보**: "이 화면을 좀 시원시원하게 바꾸고 싶어. 오늘 0건 이런 절대 정량 수치는 별로
의미가 없어 보여." 이어서 "글자를 차라리 크게 하면 좀 낫겠니?"

**글자만 키우는 건 답이 아니었다.** 세어 보니 카드 한 장에 글자 덩어리가 26개(줄 제목 4 ·
칸 이름 8 · 건수 줄 6 · 화살표 설명 4 · 라벨 칩 3 · 설정 1), 칸 바탕색이 여섯 가지였다. 빽빽함은
글자 크기가 아니라 덩어리 수에서 왔고, 그대로 키우면 26개가 커질 뿐이다 — 시안 A로 실제로
그려 보여줬다. **덜어내야 키울 자리가 생긴다.**

**시안 네 개**([HOME_FLOW_CLEAN_MOCKUP.html](mockups/HOME_FLOW_CLEAN_MOCKUP.html)):
- A 글자만 키움 — 위 이유로 비교용.
- B 건수 줄·줄 제목·화살표 설명을 다 빼고 이름만 크게, 3칸 격자, 보관함은 흰 바탕+테두리.
- C B + 칸 안에 "누르면 무엇이 나오나" 한 줄(`11:00 회차 진행 중`, `인사청문회` 등).
- D 평소엔 B, 마우스를 올리면 C의 한 줄 + 설명 + 다음 단계를 말풍선으로. **채택**(사용자:
  "호버했을 때 설명이 뜨게 하는 건 어때? 테두리만 짓지 말구?" → "d로 하는 게 좋다").
  B·C 시안은 hover 때 테두리 색만 바뀌었는데, 그게 "눌러도 뭐가 나올지 모르겠다"는 불만으로
  이어졌다.

**덤으로 드러난 사실 둘:**
- 수시 칸의 `오늘 0건`·`오늘 1건`은 **기사가 아니라 카드 수**였다(`_todays_adhoc_state`의
  `count`). 게다가 카드가 없으면 그 칸을 누르는 순간 **새로 만드는 화면**으로 가는데, 칸만 봐서는
  그걸 알 수 없었다. 이제 말풍선이 "링크가 여는 그 카드"(사안명·시간창·검색어)나 `누르면 새로
  수집해요`를 말한다.
- 두 흐름의 칸 폭이 화살표 설명 길이(`회차 마감` vs `골라 보내기`)에 따라 몇 px씩 어긋나
  있었다(flex). 3칸 격자로 바꾸며 없어졌다.

**확정본 말풍선의 발송 표시는 기록이 있을 때만 붙인다.** 시안 C를 처음 그릴 땐
`09:30 회차 · 발송 완료`를 예시로 넣었다가, 실제 값으로 채워 보니 그 회차엔 발송 기록이
없었다(자동 발송도 꺼져 있음). 그렇다고 `미발송`이라 적으면 정기 보관함이 이미 피해간 실수
(앱 밖에서 처리했을 수 있다)를 반복하게 돼, 기록이 없으면 회차 시각만 적는다.

**말풍선 문구는 사용자가 나중에 일괄로 고치기로 했다** — 그래서 고정 문구를
`_FLOW_TIPS` 표 한 곳에 모았다.

**같은 날 2차 — 가운데 정렬 + 실시간을 정기 줄로** ([HOME_FLOW_ALIGN_MOCKUP.html](mockups/HOME_FLOW_ALIGN_MOCKUP.html)).
두 가지 지적이 이어졌다:
- "너무 쏠려 있어서 가운데 정렬은?" — 칸 폭 241px에 이름이 80~120px라 오른쪽 절반이 비어
  있었다. 가운데 정렬(B)을 채택. 아이콘 위·이름 아래(C)는 칸이 60 → 84px로 커져 "시원하게"와
  반대라 기각.
- "실시간은 정기에만 영향을 미치는데 그걸 표현할 방법이 없을까? 다르게 범주를 주는 건?" —
  실시간이 흐름도 맨 위에 한 줄을 따로 쓰고 있어 정기·수시 둘 다에 걸친 것처럼 읽혔다. 실제로는
  📌 담아두기로 정기에만 이어지고 수시와는 통로가 0개다(2026-09-02 수시 📌 삭제). CLAUDE.md
  "화면 용어" 절에 이미 있던 정기↔수시 대응표(실시간현황 칸이 수시에선 「수집 원본이 겸함」)를
  그대로 배치로 옮긴 D안을 채택 — 정기 네 칸, 수집 원본 두 칸 폭. 덤으로 줄이 4 → 3으로 줄었다.
  열 머리(단계 이름)를 얹는 E안은 덜어낸 글자가 다시 생기고 배치가 이미 같은 말을 해서 기각.
- 실시간 줄이 사라지며 설정 버튼 자리도 없어져, 다른 카드의 「더보기」처럼 카드 제목 줄
  오른쪽으로 옮겼다(카드 제목 `화면 바로 가기`는 그 자리를 만들려고 새로 붙인 것).
- 가운데 말풍선은 창이 페이지 폭(960)보다 좁으면 맨 오른쪽 칸에서 창 밖으로 삐져나가 **숨어 있는
  동안에도 가로 스크롤을 만들었다**(시안에서 실제로 보였다). 좁은 창에서만 끝 칸 말풍선을 칸
  끝에 맞춘다.

**같은 날 4차 — 줄 이름표 「정기」「수시」** ([HOME_FLOW_ROWLABEL_MOCKUP.html](mockups/HOME_FLOW_ROWLABEL_MOCKUP.html)).
제보: "이 화면에서 사람들은 '정기'와 '수시'를 잘 구별 못하는 것 같아. 카테고리를 나누면 될지 메뉴
이름을 바꾸면 될지?"
- **원인**: 두 말이 화면에 나오는 곳이 맨 오른쪽 보관함 두 칸뿐이었다. 두 줄은 모양(조건 → 기사가
  들어오는 곳 → 확정본 → 보관함)도 칸 이름(`확정본`/`수집 확정본`)도 거의 같아, 보는 사람에겐 색만
  다른 똑같은 줄 두 개였다. 게다가 **정기 줄에 「정기」라는 이름이 붙은 적은 한 번도 없었다** —
  오늘 1차에서 뺀 줄 제목도 정기 쪽은 `모니터링 · 일자 › 시간대`였다("본업은 한정어를 안 붙인다").
- **칸 이름을 바꾸는 안은 기각**: 칸 이름은 단계를 말하는 자리라 여덟 칸에 정기/수시를 붙여도 그
  말이 무슨 뜻인지는 여전히 안 알려주고, 9/2에 짝으로 맞춘 `원본 ↔ 확정본`만 깨진다.
- **시안 넷**: A 지금 / B 왼쪽 이름표(이름 + 「정해진 시각마다 앱이 알아서」·「사안이 생기면 내가
  직접」) / C 이름표엔 이름만, 방식은 입구 칸 둘째 줄 / D 줄 위 머리글(정기 머리글과 정기 칸
  사이에 실시간이 끼어 머리글이 실시간 이름처럼 읽혀 기각). B·C는 카드 높이가 그대로(368px)였다.
- **한 번 보류를 권했다**: 헷갈림이 ① 어느 줄이 어느 쪽인지 ② 언제 수시를 쓰는지 ③ 단어 뜻 중
  무엇인지 확인이 안 됐고, 수시 원본 카드 중 검색어가 전부 정기 검색어인 게 7장이었지만 인사청문회처럼
  따로 정한 시간대로 보고서를 만드는 건 수시의 원래 쓰임이라 증거가 못 됐다.
- **결정**(사용자): "B로 하고, 그냥 단순하게 정기 | 수시 — 단어만 남겨도 다 알아듣지 않을까? 그럼
  굳이 보관함에 '정기보관함'을 안 만들어도 되잖아." 둘째 줄 설명은 빼고(`title` 툴팁에만 남김),
  이름표가 줄을 말하므로 보관함 칸은 둘 다 `보관함`이 됐다. 다른 화면의 `정기 보관함`·`수시 보관함`은
  그대로다 — 거기엔 이름표가 없다.
- 이름표가 한 단어라 이름표 열이 118 → 48px로 줄어 칸 폭은 174px(지금 창 기준)이다. 폰 폭에선
  실시간이 `정기` 머리글보다 위로 올라가는 문제를 `display: contents` + `order`로 풀었다.

## Core business rules — 변경 이력

- **Collection**: [Updated: 2026-07-25] Keyword *groups*, not a flat keyword list — up to `MAX_KEYWORD_GROUPS` (6) groups, each with up to `MAX_KEYWORDS_PER_GROUP` (7) keywords and its own OR/AND mode, at least 1 group required. Between groups the relationship is always OR (any group's condition being met is enough); within a group it's that group's own OR/AND. The "기관 정보 (기본)" group is no longer fixed/read-only — it's seeded once on first load from `app/config.py` `BASE_GROUP_KEYWORDS` (재정경제부/재경부/기획재정부/기재부/정부/구윤철/이형일) via a one-time migration flag (`app/settings.py` `load_settings`, `base_group_seeded`), then behaves exactly like any other group: renamable, its keywords editable, and the whole group deletable (an admin's institution can change). [Updated: 2026-07-25] Each group also has an `include_in_scrap` flag (checkbox in the settings screen, one-time migration defaults existing groups to `True` for backward compat): the scheduled/official scrape (`app/scraper.py` `collect_run`) only searches groups with this flag on, while the real-time view (`app/live_renderer.py`, see below) always searches *all* groups regardless of the flag. At least one group must have it on (`app/settings.py` `validate_keyword_groups`). This lets an admin keep broad topic groups (e.g. 부동산, 환율) live-view-only and hand-pick individual articles into the official scrape via the "→ 스크랩" button, without those topics silently inflating every scheduled run. Editable from the settings screen (no code change needed) — `app/settings.py` `save_keyword_groups`/`validate_keyword_groups`, `app/naver_api.py` `search_articles_by_groups` (dedups identical keywords across groups so the Naver API isn't called twice for the same word). No result-count cap, only same-day (KST) articles. [Fixed: 2026-08-06] `search_articles_by_groups` used to search each unique keyword sequentially — with a live keyword set this measured at ~19s wall-clock (dominated by whichever keyword happens to have the most same-day articles, since `_search_one_keyword` paginates up to `_MAX_START`/100 = 10 pages per keyword). Switched to a bounded `ThreadPoolExecutor` (`_MAX_CONCURRENT_KEYWORD_SEARCHES = 5`) — measured ~5.3s for the same keyword set, a floor set by the single slowest keyword rather than the sum of all of them (more concurrency wouldn't help further without also parallelizing pagination *within* one keyword, which isn't done). A per-keyword `_search_safely` wrapper catches `requests.exceptions.RequestException` so one keyword's failure doesn't take down the others — this was added specifically *because* concurrency makes Naver's per-second rate limit easier to hit than sequential calls did (confirmed live during testing: a 429 on two keywords, gracefully degraded to empty results for just those two rather than crashing the whole search). 5 was chosen as a bounded middle ground for this reason, not unlimited concurrency. [Changed: 2026-08-13, 그룹/키워드 상한 확대] 사용자가 상한을 대폭 늘리고 싶다고 요청 — 처음엔 "느려지는 만큼 늘려도 되냐"는 질문으로 시작했지만, 실측해보니 진짜 병목은 키워드 개수가 아니라 동시성이었다. `ThreadPoolExecutor(max_workers=N)`은 키워드가 몇 개든 순간 동시 요청을 N개로 고정하므로, 개수를 늘려도 429 위험은 안 늘어나고 총 소요시간만 늘어난다(그리고 그 시간은 아래 실시간현황 콜드스타트 항목에서 화면 뒤로 숨겼다). 그래서 두 축을 따로 실측했다: **동시성**은 100키워드·1회 시도 기준으로 5/10/15/20을 재봤더니 429 발생률이 0%/5%/20%/86%로 **선형이 아니라 어느 지점부터 급격히 나빠졌다** — 5→8로만 올렸고(재시도로 흡수 가능한 여유만 확보), 8은 100키워드에서 2회 반복 모두 429 0%로 재확인했다(15/20은 위험 구간이라 후보에서 제외). **개수 상한**은 이 재발견 이후 순수하게 "설정 화면에서 사람이 관리하기 편한 숫자" 문제로 좁혀졌고, 사용자 제안대로 6그룹×7키워드(=42) → 8그룹×10키워드(=80)로 확정했다 — 실측한 100보다 20% 낮아 여유가 있고, 딱 떨어지는 숫자라 기억하기도 쉽다. 같은 세션에서 429 처리 자체도 고쳤다: 예전엔 `_search_safely`가 재시도 없이 바로 포기하고 빈 목록으로 조용히 대체해서, 결과 0건("오늘 기사 없음")과 실패가 화면·저장 파일 어디서도 구분이 안 됐다(정기 스크랩·실시간현황 둘 다) — `_KEYWORD_RETRY_ATTEMPTS`(3, 429는 `Retry-After` 헤더 우선)를 추가하고, 그래도 실패하면 `failed_keywords`로 사실 그대로 위 계층에 보고하도록 바꿨다. `collect_run`은 이걸 받으면 예외를 올려 이미 있던 전체 회차 재시도(5분 간격 최대 3회)에 태우고, 실시간현황은 그 키워드의 캐시 last_seen만 안 건드려 다음 새로고침이 같은 구간을 재시도하게 한다(다른 키워드는 영향 없음). 이 개편 과정에서 `search_articles_by_groups`를 `search_keywords`(순수 검색, 키워드별 다른 `after` 지원) + `match_articles_to_groups`(그룹 OR/AND 매칭)로 쪼갰다 — 아래 실시간현황의 키워드 단위 캐시가 바로 이 분리 덕에 가능해졌다.
- **검색어 화면 칩 개편 (`/keywords`)**: [Added: 2026-08-19] 사용자가 스크롤을 내려 다음
  키워드를 추가하려 하면 화면이 깜빡이며 맨 위로 튀는 문제로 시작됐다. 원인은 "+ 키워드
  추가"가 폼 전체를 `POST /keywords/add-keyword-slot`으로 보내 페이지를 통째로 다시
  그리던 구조 — 그런데 이 화면은 저장 성공 시 아무 확인 메시지도 없었던 터라 "깜빡 + 맨
  위로"가 사용자가 배운 유일한 저장 신호였고, 저장하지 않는 버튼이 그 신호를 그대로
  흉내내고 있었다("저장 눌러야 적용됐다고 느낀다"는 사용자 지적으로 이 문제의 심각성이
  분명해짐 — 안 눌렀는데 반영된 것처럼 보이면 조용히 입력이 사라져도 알아채지 못한다).
  그룹 on/off 스위치가 알약 모양으로 안 그려지는 것도 같이 발견됐다 — `.group-enabled-
  toggle .track`(48×28)과 `.toggle .track`(36×24)의 CSS 명시도가 같아 뒤에 선언된
  사각형이 이겼다(손잡이는 알약 좌표라 9px 밖으로 삐져나옴).
  세 차례 논의 끝에 확정한 것: (1) 키워드 추가/삭제/ON-OFF/이름 편집을 전부 화면
  메모리(JS `state`)에서 처리하고, 실제 서버 왕복은 "저장" 버튼(진짜 `<form>` POST +
  303 리다이렉트, 이 앱의 다른 저장 화면과 같은 방식)뿐으로 좁혔다 — 화면이 새로
  그려지는 건 이제 저장뿐이라 "깜빡 + 맨 위로"가 다시 유일한 저장 신호가 됐다. (2) 저장
  안내는 화면 상단 띠가 아니라 **하단 저장 바 안**(저장 버튼 바로 위)에 둔다 — 상단 띠는
  처음 뜰 때 본문이 밀려 새로운 튐을 만들었다. 3단계(저장할 수 없음/빨강·저장 안
  함/노랑·저장됨/초록)로, 저장을 막는 진짜 이유(그룹명 없음/키워드 0개/사용 중인 그룹
  없음/정기 스크랩 그룹 없음)까지 `app.settings.validate_keyword_groups`와 같은 문구로
  미리 보여준다. (3) 스위치는 `.pill`이라는 새 클래스명으로 뺐다 — 순서만 바꾸면 나중에
  규칙이 하나만 더 붙어도 같은 사고가 재발하므로, 이름이 안 겹치게 만들어 원천 차단.
  (4) 키워드 입력칸(ON 토글 36px + 입력 220px + 🖍️ 30px + del 46px, 420px 컨테이너의
  대부분)을 칩으로 바꿔 밀도를 3.5배가량 올렸다(키워드 7개 기준 세로 350px → 100px).
  형광펜 토글(🖍️)은 이 화면에서 완전히 뺐다 — 검색어와 형광펜은 원래 별개 목록인데
  화면이 섞어 보여주고 있었고, 이미 있는 "🖍️ 형광펜 단어 편집" 팝오버와 기능이
  겹쳤다. 칩 색은 검색 ON/OFF 하나만 뜻한다: ON은 파란 실선, OFF는 회색 배경+점선
  테두리(글자를 흐리지 않음 — "지금 안 쓰는 키워드"도 목록에서 또렷이 읽혀야 하는
  값이라서). 처음엔 OFF 칩에 작은 "OFF" 글자도 붙였다가 뺐다 — 글자가 붙으면 칩 폭이
  ON/OFF 상태에 따라 달라져 토글할 때마다 뒤 칩들이 줄을 넘나들며 재배치됐다("눌렀는데
  화면이 움직인다"는 이번 작업 내내 없애려던 바로 그 문제라 되살릴 수 없었다). 대신
  점선 테두리(비-색 채널)와 그룹 아래 "N개 잠시 뺌" 집계로 상태를 알린다. 그룹 삭제는
  칩의 `×`와 구분되도록 쓰레기통 아이콘으로 바꿨다(크기·무게가 다르면 모양도 다르다 —
  소제목엔 ▲▼, 기사엔 ↑↓를 쓰는 이 앱의 기존 관례와 같은 논리) — 이름이 아이콘 안에
  안 들어가는 점은 감수했다.
  **그룹 내 중복 키워드**는 `validate_keyword_groups`가 에러 없이 조용히 하나로 합친다
  (처음 등장 자리 유지, [수정: 2026-08-19]) — 처음엔 저장을 에러로 막는 안을 검토했으나,
  `app.naver_api.search_articles_by_groups`가 이미 키워드 단위(`seen_keywords`)로,
  `match_articles_to_groups`가 URL 단위(`seen_urls`)로 중복을 걷어내 검색 비용·결과
  어디에도 영향이 없다는 게 확인되면서(API 호출도, 기사 건수도, 실시간 캐시도 안 늘어남)
  에러로 막을 이유가 없어졌다 — 오히려 위험했다: 한 번 중복이 저장된 채로 남으면 그걸
  지우려는 다음 저장 시도까지 이 화면에서 막혀버리는("잠긴 화면을 그 화면에서만 풀 수
  있는") 구조적 위험이 있었다. 화면(JS)에서도 같은 그룹 안 중복은 애초에 못 넣게
  막는다(기존 칩이 노랗게 반짝) — 순수한 실수라 저장까지 갈 이유가 없다는 판단.
  반대로 **다른 그룹에 걸친 중복**은 막지 않는다 — 그룹마다 OR/AND가 달라 같은 단어가
  한쪽엔 단독으로, 다른 쪽엔 다른 단어와 AND로 묶여 의도적으로 존재할 수 있기 때문(예:
  "정부"가 A그룹엔 OR 단독, B그룹엔 "예산"과 AND). 대신 화면이 노란 `outline`(배경색과
  다른 층이라 ON/OFF 색을 안 뺏음)으로 알려주고, 저장 시도 시 저장 바에 겹치는 단어
  이름까지 짚어준다("겹쳐도 저장은 됩니다"). AI 키워드 칩(`add_ai_extracted_keyword`)의
  기존 중복 거부와는 검사 범위가 다르다는 점은 남겨둔 과제 — 그쪽은 그룹을 가로지르는
  전체 중복을 애초에 거부하고, 여기는 같은 그룹 안 중복만 조용히 합친다.

- **Real-time view** (`app/live_renderer.py`, route `/live.html`): separate from scheduled collection — regenerates fresh on every request (no caching, no auto-refresh; manual "새로고침" link only) by live-searching *all* keyword groups (ignoring both the outlet whitelist and the `include_in_scrap` flag) for today (KST midnight–now), sorted newest-first by `pub_date`. Shows outlet, title, URL, and a JS-computed "N분 전" per article (recalculated client-side on every load so it never goes stale) — no sub-heading grouping, no AI summary. [Corrected: 2026-08-03] Highlight keywords ARE applied here too (`app/live_renderer.py` `_render_row` calls `app.highlight.highlight_keywords` on both title and summary, same as the completed/draft screens) — this doc previously said otherwise, which was stale. [Updated: 2026-07-26] Two independent checkboxes on `/live-settings` (`app/settings.py` `save_live_settings`, both default off — raw Naver results): `live_dedup_titles` collapses exact-duplicate titles *across all outlets* (not scoped to a single outlet — a same-title-different-link article from the same outlet is intentionally left as-is, since the point of this view is showing what's live at Naver right now), and `live_exclude_photo` applies the same `exclude_photo_articles` filter used by the scheduled scrape (`app/filters.py`). [정정: 2026-08-14] 이 두 체크박스는 **지금 코드에 없다** — `live_dedup_titles`/`live_exclude_photo`/`save_live_settings`/`/live-settings` 어느 이름도 `app/` 전체 grep에서 0건이고, `generate_live_page`는 검색 결과에 어떤 제외 필터도 걸지 않는다. 언제 빠졌는지 기록이 없는데도 CLAUDE.md에는 유효 규칙처럼 남아 있어서, 사용자 확인("실시간은 아무 필터 없이 날 것 그대로 맞지?")을 계기로 CLAUDE.md를 지금 동작에 맞춰 정정했다. 확정된 방향은 "실시간현황은 필터 없음이 규칙" — 제목 중복·사진·인사·화이트리스트 어느 것도 서버에서 걸러내지 않고, 선별은 클라이언트 필터 바가 맡는다. 옵션을 다시 넣자는 얘기가 나오면 이 결정을 먼저 뒤집어야 한다. A "→ 스크랩" button on each row (disabled until at least one scheduled run exists today) calls `app/manual_articles.py` `add_manual_article`, which appends the article to a same-day-only list rendered as a "📌 직접 추가한 기사" section at the bottom of `index.html`/`history.html` (own up/down ordering via `move_manual_article`, hide reuses the normal hidden-articles mechanism) — visible on screen but excluded from copy/`.txt` export, and cleared automatically at midnight. [Updated: 2026-07-29] `app/live_renderer.py` `_auto_drafted_urls()` flags, per row, articles the currently-in-progress draft round (`app.scheduler.next_pending_slot`) would likely pick up on its own — approximated locally from data live.html already has (no extra Naver call): `pub_date` inside the round's window, outlet-whitelist match, `include_in_scrap`-flagged groups' keywords found in title/summary, and photo/personnel exclusion respecting the scrap-page toggles. Matching rows show a disabled "🤖 자동 선별" button instead of "📌" (precedence: hidden > scrapped > pinned > auto-drafted > normal) — an approximation, since group AND-mode and title-dedup aren't replicated. [Added: 2026-08-05] Promoting a manual article ("📝 초안에 포함"/"📗 완성본에 포함") drops it into the target screen's normal auto-classified list — no dedicated "pick a sub-heading" control was added (feedback: keep the existing buttons as-is, just move the promoted article afterward via the sub-heading dropdown every article already has). To make that just-promoted article easy to find among the rest, `promoteManualArticle`/`promoteManualArticleToDraft` (`app/renderer.py`, `app/preview_renderer.py`) stash its URL in `sessionStorage` before `location.reload()`; a script at the bottom of the reloaded page reads it once, adds `.just-promoted` (`background: #FFF9C4` — the same pale yellow `preview.html` already uses for `.is-new-arrival`, so "something just showed up" reads consistently) to that one `.article`, and clears the sessionStorage key so it doesn't linger past this one view. [Replaced: 2026-08-11] 그 두 이모지 버튼(📝/📗)을 **화면별 소제목 드롭다운 하나**로 대체했다 (`app/renderer.py` `_manual_promote_control_html`, 초안·확정본이 공유하는 `_render_manual_section`이 이제 `target`/`group_names`/`labels`/`custom_names`를 받는다). 두 가지가 겹쳐 헷갈렸다는 피드백 — (1) 이모지가 무슨 뜻인지 안 와닿고, (2) 눌러도 **어느 소제목으로 들어가는지 알 수 없었다**(`_handle_promote_manual_article`이 재분류해서 "마지막 소제목"에 꽂았다 — 아무 근거 없는 자리라, 승격한 기사를 되찾으라고 위의 `.just-promoted` 노란 배경을 따로 만들어야 했을 정도다). 이제 **목적지는 지금 보고 있는 화면이 정하고**(초안에서 고르면 초안, 확정본에서 고르면 확정본) 담당자는 소제목만 고른다 — 고르는 순간 그 소제목으로 들어가므로 "어디 갔지?"가 아예 안 생긴다. 드롭다운 안내 문구는 `초안에 넣기…`/`확정본에 넣기…`이고, 옵션은 그 화면에 실제로 떠 있는 소제목들(사용자가 만든 소제목은 기존 "다른 소제목" 드롭다운과 같은 기준으로 볼드), 시스템 임시 칸(`UNCLASSIFIED_GROUP_NAME`)은 담당자가 고를 목적지가 아니라 `_promotable_group_names()`가 걸러낸다. **화면을 넘나드는 승격은 같이 없앴다** — 두 화면에 버튼 두 개가 다 떠 있던 게 애초에 혼란의 원인이었고, 확정본은 수집 5분 뒤 자동 발송이라 실제로 쓸 일도 거의 없다. 고를 소제목이 하나도 없는 화면(회차 종료 안내·검색 오류)에서는 📥 버튼 하나로 떨어지고 서버가 자동 분류에 맡긴다 — `<button>`도 `value`가 있고 기본값이 `""`라, JS `_promoteManual(el, ...)`이 `<select>`와 버튼을 같은 코드로 읽는다(빈 값 = "소제목 안 정함"). 서버는 두 핸들러 모두 `form["group"]`을 받아 `set_group_override(url, group)`만 하면 되므로, `_handle_promote_manual_article`이 마지막 소제목을 찾으려고 부르던 `classify_articles` 호출이 통째로 사라졌다(그 결과 `filter_hidden` import도 `app/settings_server.py`에서 미사용이 되어 제거). 초안 쪽(`_handle_promote_manual_article_to_draft`)도 같은 override를 남기는데, `group_overrides.json`이 URL 기준 전역이라 **초안에서 골라둔 소제목이 정식 회차 저장 시점(`collect_run`의 `snapshot_group_names`)까지 그대로 따라간다** — 초안에서 골라두면 확정본에서도 그 자리에 있다. 낡은 화면이 이미 사라진 소제목 이름을 보내도 안전하다(`_apply_forced_groups`가 조용히 무시하고 자연 분류/"기타"로 떨어뜨린다 — 실측 확인). 구획 헤더의 힌트도 없어진 버튼을 설명하던 것이라 `자정에 자동으로 비워져요`만 남겼다 [Renamed: 2026-08-11] 대기 성격의 칸이 둘이라 담당자가 헷갈린다는 피드백 — 담당자 본인의 표현이 곧 멘탈모델이므로 화면 라벨을 그쪽에 맞췄다: `📌 직접 추가한 기사` → **`📌 담아둔 기사`**(`app/renderer.py` `_render_manual_section`; "추가한"이 "이미 어딘가에 추가됐다"처럼 읽혀 오히려 오해를 키웠다), `📂 아직 소제목 안에 분류되지 않은 기사` → **`📂 소제목 미분류`** (`app/llm_classifier.py` `UNCLASSIFIED_GROUP_NAME`; 긴 설명형 이름이 오히려 두 칸을 헷갈리게 만들어 짧게 줄이되 "무엇을 해야 하는지"(=분류)는 이름에 남겼다). 바꾸기 전에 `group_labels`/`group_order`/`group_overrides`/`custom_groups`/저장된 회차 파일 전부를 옛 이름으로 검색해 키로 남아있는 게 하나도 없음을 확인했으므로 마이그레이션은 없다 (이름이 저장 키라서, 남아 있었다면 조용히 고아가 됐을 자리다). 실시간 현황의 📌 툴팁 2곳(`app/live_renderer.py`, JS·서버 렌더 각 1곳)도 같은 말로 맞췄고, `_merge_unclassified`의 내보내기 유출 방지가 새 이름에도 그대로 동작하는지 재확인했다 (임시 칸 이름은 안 나가고 기사만 "기타"로 합쳐짐).(바로 위 `이 아래는 복사·내보내기에 포함되지 않음` 구분선이 "보고서 밖"이라는 사실을 이미 구조로 말하고 있어, 글로 또 쓰지 않는다). [Changed: 2026-08-13] "regenerates fresh on every request (no caching)"는 이 시점에 사실이 아니게 됐다 — 사용자가 그룹/키워드 상한을 늘리고 싶다고 해서 병목을 짚어보니, 원래 있던 증분 캐시(2026-07-29 도입, 그룹 구성 전체를 지문으로 삼아 하나라도 바뀌면 통째로 버림)가 "키워드 하나만 추가해도 등록된 전체를 당일 0시부터 재검색"하는 구조였다. 바꿔야 할 이유가 명확해서(사용자 워크플로우 자체가 "바로바로 키워드 추가하고 바로 반영") `app/live_cache.py`를 키워드 단위로 재설계했다 — 캐시가 이제 그룹 지문이 아니라 `{키워드: last_seen}` + `{키워드: 원시 검색 결과}`를 들고 있고, 그룹 이름·모드·소속이 바뀌어도(`app/naver_api.py` `match_articles_to_groups`가 캐시된 원시 결과로 매번 새로 매칭하므로) 재검색이 전혀 필요 없다 — 오직 "이 키워드를 오늘 검색해본 적 있는가"만 캐시가 기억한다. 실측(등록 키워드 10개, 동시성 8): 최초 호출 23.5초 → 바로 다음 새로고침 0.25초. 이 재설계가 가능했던 건 위 Collection 항목에서 `search_articles_by_groups`를 `search_keywords`+`match_articles_to_groups`로 쪼갰기 때문 — 검색과 그룹 매칭이 분리돼 있어야 "키워드별로 다른 after 시각"이라는 개념 자체가 성립한다. **콜드스타트가 여전히 느린 문제**(캐시가 아예 없는 하루의 첫 요청, 또는 새 키워드 추가 직후 — 실측 최대 47~120초)는 사용자가 "비개발자는 키워드가 많아서 느린 줄 오해할 수 있다"고 지적하면서 시간 자체를 줄이는 방향을 요구했다. 두 가지 대안을 저울질했다 — (1) 스케줄러가 자정 리셋을 감지해 아무도 안 볼 때 미리 데워두는 방식은 스케줄러의 단일 스레드 루프를 블로킹해 최근에 개선한 "스케줄러 정시성"을 다시 해칠 위험이 있어 기각, (2) 사용자가 직접 제안한 "로딩 화면을 띄우자"가 채택됐다 — 스케줄러를 전혀 안 건드리고, 요청을 받은 스레드(이미 `ThreadingHTTPServer`라 요청마다 스레드가 따로 있음) 안에서만 백그라운드로 처리하기 때문이다. `app/live_renderer.py` `generate_live_page_or_wait()`가 새 진입점 — 콜드(등록된 키워드 중 캐시에 없는 게 하나라도 있음)면 `_ensure_live_generation_running()`으로 백그라운드 스레드를 시작시키고(모듈 전역 `threading.Lock`으로 중복 시작 방지 — 동시 요청이 검색을 두 번 돌리면 순간 동시성이 2배가 되어 위 429 위험 구간에 다시 들어갈 수 있다) `render_live_loading_page()`를 즉시 반환한다. 처음엔 "새로고침을 누르면 대기시간이 더 길어집니다" 같은 경고 문구로 중복 실행을 막자는 제안이 나왔는데, 기각했다 — 자동 새로고침(`<meta http-equiv="refresh">`)이 만드는 요청은 사람이 안내를 읽어도 막을 수 없는 경로이고, 잠금을 실제로 넣으면 "새로고침하면 길어진다"는 문장 자체가 거짓이 돼 §2-5(화면이 거짓말하면 안 된다)에 걸린다. 최종적으로 잠금(기능) + 안심 문구(UX) 조합으로 갔고, 새로고침 버튼은 `disabled` 속성을 단 `<a>`가 아니라 아예 `<span>`(href 없음)으로 바꿔 "눌러도 이동할 곳이 없는" 상태로 만들었다 — CSS로 죽인 버튼과 달리 실수로도 눌리는 경로 자체가 존재하지 않는다. [추가: 2026-08-14] 사용자가 "실시간은 정말 필터가 하나도 없냐"고 확인하는 과정에서, `pubDate` 파싱 실패 기사(`_parse_pub_date`가 `None`)는 이 화면에서도 조용히 버려지고 있다는 게 드러났다 — 네이버 뉴스 검색 API에는 애초에 날짜 범위 파라미터가 없어서 "당일 기사만"은 전부 우리 쪽이 `pubDate` 문자열을 파싱해 최신순 정렬 위에서 잘라내는 방식인데, 그 파싱 자체가 실패하면 판단 근거가 없어 그 기사만 건너뛰었다(발생 빈도 실측 불가 — 실패한 기사는 애초에 저장이 안 돼 사후 검증할 방법이 없었다). "실시간은 날것이어야 한다"는 이 화면의 원칙에 비춰보면 이것도 조용한 손탐이라, 버리지 않고 화면에 올리기로 했다. 후보로 나온 것들을 순서대로 기각했다 — (1) "N건 놓쳤다" 카운터만 띄우는 안은 기사를 계속 버리는 반쪽짜리라 기각, (2) 목록을 접는(무한스크롤) 방식은 데이터가 이미 서버에서 한 번에 다 와 있어서(네이버처럼 다음 페이지를 물어볼 대상이 없음) 얻는 게 없다고 판단해 기각(콜드스타트가 느린 진짜 원인은 렌더링이 아니라 네이버 왕복이라 무한스크롤을 붙여도 대기시간이 안 줄어든다), (3) 클릭 시 팝업으로 띄우는 안은 팝업에 넣을 새 정보가 없어(제목·언론사·요약·URL 넷뿐, 이미 행에 다 있음) 기각 — 원문 iframe은 대부분 언론사가 `X-Frame-Options`로 막고, 있어도 "본문 크롤링 안 함" 원칙에 걸린다. (4) "직전에 성공적으로 읽은 기사 시각 바로 뒤에서 발견" 같은 근사 힌트를 화면에 보여주는 안도 "네이버 자신의 최신순 정렬에서 오늘 기사들 사이에 끼어 있었다"는 나름 근거 있는 신호였지만, "이용자에게 혼돈을 준다 — 오늘 기사라고 시스템이 보증하는 것처럼 읽혀서 원문 확인을 건너뛰게 만들 수 있다"는 사용자 판단으로 기각했다(계산은 해두되 화면엔 안 쓰는 것도 아니라, 아예 코드에도 안 넣음 — 실제로 배너가 뜨는 날이 오면 그때 다시 논의). 최종적으로 `app/naver_api.py` `_search_one_keyword`/`search_keywords`에 `include_unparsed_dates` 플래그(기본 `False`)를 추가해 `app/live_renderer.py`만 `True`로 호출하고, 그 기사를 `pub_date: None`으로 담아온다 — 정기 스크랩(`collect_run`의 회차별 시간창 수집은 `pub_date` 없이는 어느 창에 속하는지 판단 자체가 불가능하다)과 수시 모니터링은 플래그를 안 써서 예전 동작(건너뜀) 그대로다. `render_live_page`가 `pub_date`가 없는 기사를 시간순 목록·상단 "당일 누적" 건수에서 빼(그 정의를 만족하는지 확인이 안 됐으므로) 화면 맨 아래 별도 구역에 모으고, 상단에 0건이면 아예 안 뜨는 배너(`⚠️ 발행시각을 못 읽은 기사 N건`)로 그 구역을 가리킨다 — "몇 건 놓쳤는지"와 "그 기사가 뭔지"를 둘 다 보여주면서도, 정기 수집의 시간창 로직이나 이 화면이 이미 가진 개수 집계(그룹 필터 칩 등)는 하나도 건드리지 않았다.
- **Scheduled scrape photo/personnel exclusion** [Added: 2026-07-26, inverted: 2026-08-11]: `/scrap-page` settings screen (`app/settings.py` `save_scrap_page_settings`, fields `exclude_photo_in_scrap`/`exclude_personnel_in_scrap`, both default off — checkbox unchecked) lets an admin opt the *scheduled* run (`app/scraper.py` `collect_run`) and `app/preview_renderer.py`'s draft filter, and `app/live_renderer.py`'s auto-drafted-preview flag, into *dropping* photo/on-site and 인사 발령 articles — the title-dedup rule (`deduplicate_by_title`) still always applies regardless of these flags, only the two exclusion filters are conditional. [Changed: 2026-08-11] The polarity was flipped: the fields used to be `include_photo_in_scrap`/`include_personnel_in_scrap` (default off = always excluded), and turning them *on* opted a run *into* keeping those articles. Per user request ("커스터마이징 측면에서 [제외]하는게 맞는 것 같아"), default behavior is now "everything is collected" and the checkboxes opt a run *out* of specific categories — matches how the equivalent live-view toggles (`live_exclude_photo`, below) were already named. `app/settings.py` `load_settings()` migrates old saved values on first read (`include_* → exclude_*`, value inverted so `not include_photo_in_scrap` becomes `exclude_photo_in_scrap` — an existing user's actual collection behavior is unchanged, only the setting's name and polarity are); the old keys are deleted after migration. Settings-screen hint text: "기사 스크랩시 수집할 기사의 범위를 직접 설정하실 수 있습니다. 체크하지 않으면 해당 기사도 모두 수집됩니다." Turning an exclusion on means the admin is expected to hide unwanted articles manually via 🗑️ only for whatever the checkbox left in (nothing changes about `[속보]` always being kept regardless of hint words). [Added: 2026-08-11] `app/filters.py` `looks_like_photo_caption(article)` catches wire-service (연합뉴스/뉴시스/뉴스1) photo articles that never carry a `[포토]`-style title hint — validated against 1,096 stored real articles (48 hits, 0 false positives) on summary-text signals (`재판매 및 DB`, 사진기자단/사진공동취재단 markers, a reporter email at the end, a trailing shoot-date stamp) and a title-shape pattern (a bare noun-phrase ending in "관형형 + 직책" with no quote/bracket, e.g. "쪽방촌 둘러보는 구윤철 부총리"). `[속보]` titles are exempt from this too. It runs unconditionally (independent of the `exclude_photo_in_scrap` setting) and only *labels* — a "📷 사진 추정" badge on 실시간 현황 (full text) and 초안 (icon only, `.photo-badge`) — actual exclusion from the scheduled scrape only happens when the setting is also on, since it's an estimate rather than a certainty.
- **Waiting-for-first-scrap placeholder** [Added: 2026-07-26]: `app/renderer.py` `generate_waiting_page()` writes a "🐰⏱️ 아직 스크랩 시간 전입니다" placeholder to `index.html` whenever `app/storage.py` `is_today()` says the latest saved run isn't from today — both at app startup (`main.py`, replacing the old `_WAITING_PAGE` constant that only handled the zero-runs-ever case) and every scheduler tick while `app/scheduler.py` `current_active_slot()` is `None` (i.e., before today's first slot has completed — this check is time-of-day-only, so it naturally resets to "waiting" every midnight without tracking dates explicitly). Prevents yesterday's last scrap screen from lingering past midnight until today's first scheduled run completes and overwrites it.
- **매일경제/매경이코노미 구분 불가 표식** [Added: 2026-07-29]: mk.co.kr 도메인은 매일경제(일간지)와 매경이코노미(주간지)가 함께 쓰지만 `app/naver_api.py`의 언론사 판별은 도메인 기준이라 둘을 구분할 수 없다. `app/naver_api.py` `outlet_display_label()`이 화면 표시 시 "매일경제"를 "매일경제**"로 표시해 실제로는 매경이코노미일 수 있다는 걸 알려준다 — `app/renderer.py` `render_article`(라인 텍스트), `app/live_renderer.py` `_render_row`(outlet-tag), `app/settings_server.py` `render_hidden_page`(숨긴 기사 라벨) 세 곳에 적용. 화이트리스트 비교·우선순위 정렬·복사/`.txt` 내보내기에 쓰이는 원본 `outlet` 값은 그대로 "매일경제"를 유지한다(화면 전용 표식). [Fixed: 2026-08-03] MBN도 같은 매경미디어그룹이라 mk.co.kr을 공유하는데, MBN 기사의 실제 원문 링크는 `mbn.mk.co.kr` 서브도메인이라 이 규칙에 잘못 걸려 "매일경제"로 오분류되고 있었다 — `app/naver_api.py` `OUTLET_DOMAINS`에 `mbn.mk.co.kr` → `MBN`을 별도 등록해(더 긴 도메인이라 `mk.co.kr` 규칙보다 먼저 매칭됨, `koreajoongangdaily.joins.com`과 동일 패턴) 바로잡았다. `mbn.co.kr`(도메인 자체)로 오는 기사도 여전히 `MBN`으로 잡힌다. [Fixed: 2026-08-06] 같은 패턴의 버그가 IT조선(`it.chosun.com`)에도 있었다 — 조선일보(`chosun.com`)의 하위 도메인이라 별도 항목 없이는 조선일보로 오분류됐다(사용자가 직접 스크린샷으로 확인해 제보). `OUTLET_DOMAINS`에 `it.chosun.com` → `IT조선`을 등록해 바로잡고, 설정 화면의 "전문일간" 언론사 목록 맨 끝에도 추가했다. [Fixed: 2026-08-14] **판별 방식 자체를 바꿔 이 계열 전체를 끝냈다.** 그동안 세 번(MBN·IT조선·매경이코노미) 같은 버그를 만난 이유는 개별 매체가 아니라 "언론사 = URL 도메인"이라는 초기 전제였다 — 첫 커밋(`d9ea07a`)의 주석에 "네이버가 기사를 자체 미러링하면 link가 n.news.naver.com이 되어 도메인만으로는 언론사를 알 수 없다. 언론사 판별은 원본 링크로 한다"고 적혀 있듯, 네이버 미러 링크를 *도메인 기준으로* 한 번 보고 쓸모없다고 판단해 기각했다. 정답은 도메인이 아니라 그 링크의 **경로**에 있었다(`/mnews/article/{oid}/` — oid는 네이버가 매체마다 부여한 고유 번호). 같은 응답 dict 안에 있던 단서를 잘못된 렌즈로 봐서 놓친 것. 매경이코노미가 특히 오래 남은 건 MBN(`mbn.mk.co.kr`)과 달리 서브도메인조차 안 갈려서 도메인 예외 추가로는 원리적으로 못 고쳤기 때문이고, 그래서 7/29에는 "매경이코노미는 아직 도메인을 확인하지 못했다"고 적어둔 채 빨간 표식으로 덮었다 — 실제로는 *존재하지 않는 도메인*을 찾고 있었다. 또 이 버그는 조용히 틀린다(오분류돼도 예외 없이 그럴듯한 이름이 나옴). 그래서 발견 경로가 매번 사람 눈이었고(IT조선은 사용자 스크린샷 제보), 잡힌 3건이 전부라는 보장도 없었다.
  - **실측(2026-08-14).** 실제 검색어 30개로 기사 9,174건을 모아 oid 88개를 추리고, oid마다 네이버 기사 페이지의 `og:article:author`로 88개 전부 언론사명을 확인했다. 네이버 미러 링크가 있는 3,947건(43%)을 기존 도메인 방식과 전수 대조한 결과 — **오분류 36건(0.9%)**: 매경이코노미→매일경제 18, 스포츠동아→동아일보 7, 신동아→동아일보 3, 스포츠경향→경향신문 2, 주간경향→경향신문 2, 헬스조선→조선일보 2, 주간동아→동아일보 1, 한겨레21→한겨레 1. **이름 대신 도메인 문자열이 그대로 화면에 찍히던 것 587건(14.9%, 35개 매체)**: `biz.sbs.co.kr`, `ohmynews.com`, `yonhapnewstv.co.kr` 등. 즉 예상대로 매경이코노미는 빙산의 일각이었고, 도메인 예외를 하나씩 추가하는 방식으로는 영영 못 끝낼 규모였다. 대조에서 코리아중앙데일리가 독립 도메인(`koreajoongangdaily.com`)으로 옮겨간 것도 함께 발견해 `OUTLET_DOMAINS`에 추가했다.
  - **바꾼 뒤.** `resolve_outlet`(oid 우선 → 도메인 폴백) 도입 후 같은 표본 3,947건 **불일치 0건**, oid가 없는 5,227건은 기존 결과와 **100% 동일**(폴백 회귀 없음). 도메인 폴백을 남긴 이유는 표본 기사의 57%가 네이버 뉴스 채널 미제휴 매체라 oid가 아예 없기 때문이고, 그쪽은 매체마다 도메인이 갈려서 기존 방식으로 이미 맞는다(뉴스핌·아시아투데이·땅집고·IT조선·코리아타임스로 확인).
  - **빨간 표식은 없애지 않고 조건을 좁혔다.** oid로 확정된 매일경제 기사는 표식 없이 표시하고, 네이버 미러 링크가 없어 도메인 폴백으로 "매일경제"가 된 mk.co.kr 기사에만 남긴다(실측 mk 기사 133건 중 1건). 전부 빨갛게 칠하면 경고가 배경 소음이 되어 정작 확인이 필요한 1건이 묻힌다.
  - **이미 저장된 데이터도 한 번 소급 적용했다.** 저장된 기사엔 `originallink`이 없고 `url`(네이버 미러 링크)만 있어 oid 경로만 적용 가능하다 — 회차 파일 13건, 숨긴 기사 11건, 실시간 캐시 61건을 갱신했다(원본은 `trash-can/outlet-migration-20260814-182415/`에 백업). 숨긴 기사 기록을 같은 패스에서 함께 고친 게 중요한데, `filter_hidden`이 URL과 (outlet, title) 쌍으로 매칭하므로 한쪽만 바꾸면 숨김이 풀린다. 실시간 캐시(`live_cache.json`)는 계산된 `outlet` 값을 저장하므로 코드만 고치면 자정 초기화 전까지 옛 값이 계속 보였다.
- **Publish time display**: `app/naver_api.py` `_search_one_keyword` attaches each article's parsed `pub_date` (ISO 8601, KST) to the result dict purely for sorting/display — PRD's "no publish date in output" rule is about the copy/export line format, not on-screen UI, so this doesn't violate it. `app/renderer.py` `render_article` shows both a fixed `"HH:MM 게시"` (safe to bake into the static HTML since it never changes) and a live "N분 전" computed by the same client-side JS pattern as the real-time view (`renderRelativeTimes`, re-run on every page load/auto-refresh) — both screen-only, never included in `_build_plain_text`. [Updated: 2026-07-24] Windowed per-slot collection: each run only gathers articles published within its configured `start`(exclusive)–`end`(inclusive) window, not the whole day (`_search_one_keyword` `after`/`before` params, computed in `app/scraper.py` `collect_run` via `kst_today_at`). The main article screen (`index.html`) still shows only the latest single run's window, unchanged — only the landing-page word cloud (see below) aggregates across the whole day. Anywhere downstream needs "all currently registered search keywords" as a flat set (sub-heading classification exclusion, the bottom "🤖" keyword block, word-cloud color-coding) uses `app/settings.py` `all_search_keywords` instead.
- **Dedup rule**: keep duplicate articles across outlets/editions, but collapse articles with an *identical* title down to one (keeps the first-seen, so apply after outlet-priority sort). Articles whose title contains a photo-article hint word (`PHOTO_HINT_WORDS` in `app/filters.py`: 포토/사진/화보/포토뉴스/PHOTO, case-insensitive) are excluded entirely, not just deduped — except titles containing `[속보]`, which are always kept regardless of other hint words.
- **Outlet ordering**: broadcasters (KBS, MBC) first, then a fixed list of major outlets (조선일보, 중앙일보, 동아일보, 한국경제, 매일경제, 서울경제, 한국일보, 머니투데이, 이데일리, 연합뉴스, 뉴시스, …), then everything else appended at the end (never dropped) — **unless** the user has picked an outlet whitelist in settings, in which case only the checked outlets are kept, in the user-chosen order (`app/settings.py` `save_outlet_selection`, `app/filters.py` `filter_by_outlet_whitelist`). [Added: 2026-08-07] 사용자가 화면에서 매핑 안 된 도메인(ichannela.com)을 직접 발견해 4개 언론사를 추가 요청 — `app/naver_api.py` `OUTLET_DOMAINS`에 채널A(ichannela.com, 방송사)/시사IN(sisain.co.kr, 주간지)/이코노미스트(economist.co.kr, 통신사 및 종합 일간지)/경기일보(kyeonggi.com, 기타 언론사) 매핑과 `OUTLET_CATEGORIES` 소속을 추가했다.
- **Output line format** (no publish date shown), customizable via a `{outlet}`/`{title}` template in settings (default shown below); the URL line and the absence of a publish date are not customizable:
  ```
  ㅇ (언론사) 기사제목
  URL
  ```
- **No full-text crawling** — only store the Naver API's `description` summary field. Highlight keyword matches within it in a per-word color cycled from `HIGHLIGHT_COLORS` (`app/config.py`) — highlight keywords are a separate settings field from search keywords, max `MAX_HIGHLIGHT_KEYWORDS` (20), not 3 [Corrected: 2026-08-04, was stale]. [Added: 2026-08-04] A "🖍️ 형광펜 단어 편집" button sits left of 🗑️ in the bottom bar on all three screens (완성본/초안/실시간현황) — opens a popover (chips showing each highlight word in its actual highlight color, × to remove, text box + "추가" to add) that calls the existing `/keywords/toggle-highlight` endpoint (`app/settings_server.py` `_handle_toggle_highlight`, fixed in the same change to actually return 400 on `SettingsError` — e.g. max-count exceeded — instead of always 204) and reloads the page on success. `_highlight_words_json()` helpers in `app/renderer.py`/`app/preview_renderer.py`/`app/live_renderer.py` serialize `{word, color_hex}` pairs (color resolved server-side from `HIGHLIGHT_COLORS`) for the popover's JS to render matching chip colors. `app/highlight.py` `highlight_keywords()`'s `<span class="hl-word" data-word="...">` is a plain span (no `onclick`/`cursor:pointer` — an earlier attempt at click-to-cycle directly on article text was reverted per feedback); `data-word` stays since the popover's color-cycling below still needs it. [Added: 2026-08-05] The popover also closes on an outside click now (`document` click listener checks `!e.target.closest(".highlight-wrap")`), on top of the existing re-click-🖍️-to-close toggle — standard popover behavior, added instead of a dedicated × button to avoid cluttering the small popover further. [Added: 2026-08-05] Color-cycling now lives *only* in the "🖍️ 형광펜 단어 편집" popover — clicking a chip (not the × remove button, which stops propagation) cycles that word to the next `HIGHLIGHT_COLORS` entry. `app/settings.py` `cycle_highlight_color(word)` does the actual advance-and-persist; the shared `cycleChipColor(chip)` JS (same in all three renderer files) posts to `/keywords/cycle-highlight-color` (`app/settings_server.py` `_handle_cycle_highlight_color`, 404 if the word was removed from `highlight_keywords` in the meantime) and, on success, updates both the chip's own background *and* every `.hl-word[data-word=...]` span in the article list — the color is still global per word, only the trigger moved from inline text to the popover. [Changed: 2026-08-07] All 5 `HIGHLIGHT_COLORS` were replaced with a toned-down dusty-pastel set (`#DCE4C3`/`#E8D2BA`/`#C7D9E4`/`#E5D0D8`/`#DAD4E8`, same order so existing per-word `"color"` index assignments are unaffected) — the old vivid set (neon lime/orange/sky-blue/pink/purple) was fine for occasional words but painfully vivid on a word like "재정경제부" that appears in nearly every article, per direct feedback. `HIGHLIGHT_TEXT_COLOR` (plain body text color) still contrasts fine against all 5 since they're all still light backgrounds, just desaturated.
- Empty result for a run → show "💤" (changed from "하나도 없어요" on 2026-07-24), not an empty list.
- Failed scrape → retry up to 3x, 5 minutes apart.
- Header on screen/export: "언론 모니터링 [HH:MM] 기준" using the run's scheduled slot time.
- **Sub-heading classification**: regenerated fresh each run from article descriptions (not fixed categories), max 8 sub-headings [Changed: 2026-08-11, was 5], each article assigned to exactly one — except a user's manual drag-across-boundary reassignment (see curation below), which overrides auto-classification as long as that sub-heading still exists in later runs. [Removed: 2026-08-12] Every group's display name used to be wrapped in auto-added `<...>` angle brackets (both on-screen `<h2>` and in copy/download/Telegram/email plain text) — removed per user request, since a renamed subheading is the admin's own wording and the app forcing brackets around it isn't the admin's choice ("누군가는 다르게 지정할 수도 있어서" — someone might want a different convention). Now shows exactly the text the admin entered, with no app-added decoration. Applies to the confirmed/draft `<h2>` title (`app/renderer.py`/`app/preview_renderer.py` — the `<span class="subheading-title">` wrapper stays, only the literal `&lt;`/`&gt;` characters around `display_name` were removed), the bottom AI-summary block (`<p><strong>...`), `history.html`'s `<h3>`, and both plain-text builders (`_build_plain_text`/`_build_preview_plain_text`, which used to emit `f"<{name}>"` as the section header line). The renamed label can itself be renamed by the user (renaming only changes the display label, not the underlying grouping key). [Added: 2026-08-12] The 2026-08-12 bracket removal above wasn't a reversal of the *feature*, just of it being forced — a new "메일머지" (mail-merge) settings category (independent of "뉴스 스크랩", placed right after it — `app/settings_server.py` menu) now holds two opt-in output templates: **📃 기사 제목 형식** (renamed from "본문 형식", same `{outlet}`/`{title}` template, unchanged behavior/URL `/format`) and **📃 소제목 형식** (new, `/subheading-format`, `{section}` placeholder, default `<{section}>` — i.e. the same bracket look as before, just user-owned now instead of hardcoded). `app/settings.py` `validate_subheading_format_template`/`save_subheading_format_template` mirror the article-line-template pair exactly (`{section}` required exactly once). `app/renderer.py` `apply_subheading_format(template, name)` does the substitution (plain `.replace`, same reasoning as `apply_line_template`); HTML call sites pass `html.escape(subheading_format)` first (same convention as `apply_line_template`) since the default template's literal `<`/`>` would otherwise be parsed as a stray tag by the browser — confirmed this matters with a real subheading name starting with an ASCII letter ("ISA 개편안 철회"), where an unescaped `<ISA...` is exactly the shape an HTML tokenizer treats as a tag open. Applied at every point a subheading name is shown or exported — screen `<h2>` (`_render_groups`/`_render_preview_groups`), the AI-summary block (`_render_bottom`/`_render_preview_bottom` via a shared `app.renderer._format_summary_title` helper), the TOC popover's `data-toc-name`, and both plain-text builders (`_build_plain_text`/`_build_preview_plain_text`) — but *not* the "다른 소제목" dropdown or the rename-prompt prefill (`data-current`), which intentionally keep showing the raw editable name (2026-08-05 precedent: dropdown text is for editing, not for display). The 📂 소제목 미분류 temporary bucket and `export.py`'s media_report JSON (`display_group_name` raw, no format — that's a machine contract, not a display surface) are both explicitly excluded, same reasoning as the original bracket-leak bug this whole area is scarred from. Threaded through as an optional `subheading_format` parameter (default `DEFAULT_SUBHEADING_FORMAT_TEMPLATE`, `app/config.py`) alongside the existing `line_template` parameter at every layer (`render_page`/`generate_screen`/`build_latest_plain_text` in `app/renderer.py`; `_compute_preview_content`/`render_preview_page`/`preview_move_article`/`generate_preview_page` in `app/preview_renderer.py`; `render_history_page`/`generate_history_page` in `app/history_renderer.py`) — every existing caller already invoked these with no args (settings-driven default), so none needed updating. Verified live: default `<{section}>` renders correctly on all three screens and in copied plain text, validation rejects a template missing `{section}`, and a custom template (`[[{section}]]`) round-tripped through save → regenerate → screen correctly before being restored to the default.
- **LLM sub-heading classification** [Added: 2026-08-11]: the grouping step in `classify_articles()` (`app/classifier.py`) now tries `app/llm_classifier.py` `classify_with_llm()` first — Claude API (`LLM_MODEL` in `app/config.py`, default `claude-haiku-4-5`, a one-line swap to a higher-tier model) grouping articles into named sub-headings via structured outputs (`output_config.format` json_schema), motivated by the rule-based grouper's habit of picking a low-content word ("등을", "방안") as the sub-heading name. Falls back to the original rule-based path (unchanged, still the summary/keyword-extraction engine — LLM only replaces the grouping) whenever `ANTHROPIC_API_KEY` is absent, the `anthropic` package isn't installed, the article count is outside `[2, _MAX_ARTICLES=60]`, or the API call raises for any reason — every failure mode is caught and swallowed, never surfaced to the user, so the app never depends on this being available.
  - **Cost control — when it's actually called.** A naive re-classify-on-every-render would call the API on every page load; instead `classify_with_llm` takes `allow_call`/`force` flags threaded through `classify_articles(..., allow_llm_call=, force_llm=)`. The scheduled/confirmed-run path (`app/renderer.py` `render_page`, `app/settings_server.py`'s curation handlers) always passes the default `allow_llm_call=True` — every regenerated screen re-classifies. The draft path (`app/preview_renderer.py` `_compute_preview_content`) is the one that's rate-limited: `_take_auto_classify_turn(slot, article_count)` (keyed by `(date, slot["end"])`, in-memory `_auto_classified_slots` set + `threading.Lock`) grants exactly one automatic classification per slot, and only once the article count reaches `MIN_ARTICLES_FOR_AUTO` (5) — classifying a 2-article draft is wasted spend since it'll obviously need reclassifying later anyway. After that one auto-classification, the draft never calls the API again on its own — reloading, hiding an article, moving an article between sub-headings, creating a sub-heading, or renaming one are all zero-cost. This constraint exists for a reason beyond cost: **re-classification silently discards manual curation.** Manual moves/renames/ordering are keyed by sub-heading *name*, and a re-run measurably renames sub-headings by a word or two even on nearly-identical input (confirmed empirically: "물가안정 대책" → "물가안정 정책" from one new article arriving) — every manual edit keyed to the old name is then orphaned. The only way to re-classify after the first auto-run is the toolbar **"🤖 소제목 분류 요청하기"** button (`regenerateSubheadings()` JS → `POST /regenerate-subheadings` → `app/settings_server.py` `_handle_regenerate_subheadings`, calls `classify_articles(..., force_llm=True)`), which — if any of the currently-shown articles have a recorded manual override (`load_group_overrides()`) — first shows a confirm() dialog naming the count of manual edits at risk before proceeding. [Added: 2026-08-11] The confirmed-run screen (`index.html`) gets the same-labeled button too, wired to a separate endpoint `POST /regenerate-subheadings-final` → `app/settings_server.py` `_handle_regenerate_subheadings_final` — unlike the draft, the confirmed screen already re-classifies automatically on every render (`render_page`'s default `allow_llm_call=True`, cache-first so a same-URL-set render costs nothing), so this button exists purely to force a fresh LLM call (bypass the cache) when the admin dislikes the current grouping, not to lift a rate limit. It reuses the same manual-edit-count confirm() gate, and persists the result back into the run's JSON file (`app/classifier.py` `snapshot_group_names(..., force_llm=True)` → `update_run_articles`) since — unlike `preview.html`, which recomputes on every request — `index.html` is a static file that needs `_regenerate_screens()` to reflect the change without a page reload race.
  - **Caching and reuse.** `app/llm_classifier.py` keeps an in-memory `_cache: {(frozenset(article_urls), max_subheadings): [(name, [urls]), ...]}` (capped at `_CACHE_MAX=40` entries, cleared wholesale when full) so an identical article set never re-calls the API. Two subset/superset reuse paths avoid calls that would otherwise re-derive an already-known answer: `_find_reusable` — if the *current* article set is a strict subset of a cached one (articles only got removed, e.g. a 🗑️ hide, or the scheduled-run vs. draft-render article sets differing only by hidden articles), reuse the smallest cached superset instead of re-classifying (this closed a real double-call — the scheduled collect and the immediately-following screen render were classifying the same round twice because one includes hidden articles and the other doesn't). `_find_partial` — the mirror case for the draft's `allow_call=False` path: if the current set is a strict *superset* of a cached one (new articles arrived, nothing removed), reuse the cached grouping as-is and bucket the genuinely-new articles into a synthetic `UNCLASSIFIED_GROUP_NAME` ("📂 아직 소제목 안에 분류되지 않은 기사") group instead of calling the API — the existing sub-headings are left completely untouched. `app/preview_renderer.py` renders that group with a distinct red-dashed border (`.subheading-unclassified`, separate from the sky-blue `.subheading-custom` for user-made sub-headings) and a red count badge on the regen button (`.regen-badge`); critically, `_merge_unclassified()` folds that group's articles into "기타" (creating one if absent) for copy/download/send text only — the placeholder name itself must never leak into an actual report (a real bug caught and fixed during this implementation: the first version let the literal "🆕 아직..." string flow straight into `plain_text`). `_merge_unclassified` builds new dicts rather than mutating the ones shared with the rendered `render_groups` list, since the two consumers (screen HTML vs. plain text) run against the same group objects in the same request.
  - **Sticky names across re-classification.** `_last_names` (module-level list, updated after every real API call) is passed into the next prompt as "직전에 쓰던 소제목: … — 내용이 그대로인 묶음은 위 이름을 글자 그대로 재사용하라" — verified empirically that hiding one article out of 13 and re-classifying kept all 4 sub-heading names identical. This is a best-effort mitigation, not a guarantee (it resets on app restart, and a genuinely different article mix will still rename), which is exactly why the `allow_call=False` rate-limiting above is the real protection, not this.
  - **[사고, 수정: 2026-08-13] 초안→확정본 전환 시점에 이름·순서·이름표가 전부 초기화됨, 숨긴 기사로만 된 유령 소제목이 뜸.** 담당자가 17시 초안에서 20분 넘게 소제목을 정리(순서·이름·기사 이동)해뒀는데, 회차가 확정본으로 넘어가자 그 작업이 통째로 사라지고 숨겨둔 기사·소제목까지 화면에 다시 나타났다. 원인은 두 가지가 겹친 결과였다:
    1. **`app/scraper.py` `collect_run`이 숨긴 기사를 뺴지 않고 그대로 분류에 넣었다.** 초안(`_compute_preview_articles`)은 `filter_hidden` 이후 남은 기사만 분류하는데, `collect_run`은 검색 결과 원본을 그대로 `snapshot_group_names`에 넘겼다 — 17시 회차 48건 중 25건이 담당자가 이미 숨긴 기사였고, 그 25건이 만든 소제목 3개는 화면(`filter_hidden` 이후)에선 숨긴 기사만으로 이뤄진 빈 유령 폴더로 보였다.
    2. **기사 구성이 달라지면(위 25건 포함 여부) LLM이 "묶음이 실제로 달라졌다"고 판단해 새 이름을 짓는다.** "직전 이름을 재사용하라"는 프롬프트 부탁(위 항목)은 강제가 아니라서, 확정 저장 시점에 초안과 다른 기사 집합으로 다시 물어보면 이름이 바뀔 수 있다는 게 실제로 확인됐다. `group_order.json`/`group_labels.json`/`group_overrides.json`은 전부 소제목 **이름 문자열**을 키로 저장하므로, 이름이 한 글자만 달라져도 그 기록이 통째로 무효가 된다 — 초안(`classify_with_llm(..., allow_call=False)`, 회차당 1회 자동분류 이후로는 재호출 없음)은 이미 이 위험을 막아뒀는데, 회차가 저장되는 바로 그 순간만 무방비였다.
    - **고침 1 (유령 폴더):** `app/curation.py` `filter_hidden`을 URL뿐 아니라 (언론사, 제목)도 함께 보게 넓혔다 — 네이버가 같은 기사를 새 기사 ID(새 URL)로 재게재하면서, 옛 URL로 숨겼는데 새 URL로는 그대로 다시 뜨는 사고도 같은 회차에서 확인됐다("모두의창업 2기" 매일경제 기사, 16:40 숨김 URL과 16:55 확정본 URL이 언론사·제목은 같고 기사 ID만 달랐다). 언론사+제목 완전 일치를 "같은 기사"로 보는 판단은 `deduplicate_by_title`의 기존 전제와 같아 새로운 위험이 아니다. `collect_run`은 이제 `filter_hidden`을 거친 기사만 분류에 넣는다.
    - **고침 2 (이름 안정성):** `app/classifier.py`에 `classify_for_finalize()`를 새로 만들어 `collect_run`이 쓰게 했다. 먼저 API를 새로 부르지 않고(`allow_call=False`) 초안이 이미 만들어둔 캐시된 분류를 재사용하고, 그 사이 새로 들어온 기사만 `assign_to_existing()`(`/assign-unclassified` 버튼과 같은 API — "묶어라"가 아니라 "이미 있는 소제목 중 하나를 골라라"만 시키므로 이름을 바꿀 방법 자체가 없다)로 기존 소제목에 끼워 넣는다. 초안이 아예 없었던 회차(예: 아무도 안 열어본 심야 06:00)만 지금까지처럼 처음부터 분류한다 — 지킬 담당자 작업이 없으니 새 이름이 나와도 안전하다. `app/llm_classifier.py`에 `seed_cache()`도 추가해, 이렇게 API 호출 없이 조립한 결과를 방금 API로 받은 것처럼 캐시에 심어둔다 — 안 그러면 확정본에서 처음 기사를 숨기거나 옮길 때(`app/settings_server.py` `_handle_move_article` 등이 부르는 `snapshot_group_names`는 `allow_llm_call` 기본값 `True`) 정확한 캐시 적중이 없어 또 API를 불러 이름이 바뀔 수 있었다(같은 사고의 재발 경로).
    - 재현 테스트는 `tests/test_integration.py`가 아니라 별도 스크립트로 작성해 검증했다(실제 API 호출 없이 `_get_client`/`assign_to_existing`를 목으로 바꿔 "캐시가 있으면 API가 아예 호출되지 않는다", "새 기사는 기존 소제목에만 배정된다", "숨긴 기사는 어떤 소제목에도 안 남는다", "초안이 없으면 처음부터 분류한다" 네 가지를 확인).
  - **Undo (되돌리기)** [Added: 2026-08-11]: `app/undo.py` — 숨기기·소제목 이동·이름 변경·AI 재분류를 한 단계씩 되돌린다(`POST /undo`, 화면 왼쪽 아래 `.undo-fab` ↩ 버튼, 되돌릴 게 있을 때만 렌더링하고 툴팁에 "○○ 되돌리기"로 무엇이 되돌아가는지 명시). 동작마다 역연산을 짜는 대신 **큐레이션 상태를 통째로 스냅샷**하는 방식을 골랐다 — 역연산은 동작이 늘 때마다 빠뜨릴 위험이 있고, 이 앱의 상태는 작은 JSON 몇 개(`hidden_articles`/`group_overrides`/`group_labels`/`group_order`/`custom_groups`/`preview_order`)뿐이라 통째로 뜨는 비용이 무의미하게 작다. **핵심 함정: LLM 캐시도 같이 떠야 한다** — 확정본은 매 렌더링마다 `classify_articles`를 다시 부르고 그 답을 `app/llm_classifier.py`의 메모리 캐시에서 가져오므로, 회차 파일의 `group` 필드만 되돌리면 화면은 여전히 캐시에 남은 새 분류를 보여준다("되돌렸는데 안 되돌아감"). 그래서 `_cache`와 `_last_names`를 JSON 직렬화(캐시 키가 `(frozenset(urls), max)` 튜플이라 리스트로 펴서)해 스냅샷에 포함하고, 복원 시 되돌려 넣는다 — 되돌리기가 API를 새로 부르지 않는 이유이기도 하다. `push(label, coalesce_sec=3.0)`은 같은 label이 3초 안에 이미 쌓였으면 건너뛴다: 일괄 숨기기·소제목 통째 숨기기가 `/hide-article`을 기사 수만큼 **동시에**(Promise.all) 부르는 구조라, 안 묶으면 되돌리기를 기사 수만큼 눌러야 한다(에디터 Ctrl+Z가 빠른 연속 입력을 한 덩어리로 묶는 것과 같은 이유). 스택은 하루 단위 lazy reset(`app/manual_articles.py`와 같은 패턴) + 최대 20단계. 되돌린 뒤 `_handle_undo`가 회차 파일의 `group` 스냅샷을 다시 맞춰준다 — 화면엔 안 쓰이지만 이 회차가 나중에 "지난 기사"로 넘어갔을 때 쓰이기 때문.
  - **🤖 AI 배지 / 📂 임시 보관함 표기** [Added: 2026-08-11]: AI가 만든 소제목에만 `<h2>` 안에 작은 `.ai-badge`(🤖)를 붙인다. **반드시 소제목 이름 문자열 바깥의 별도 `<span>`이어야 한다** — 이름에 섞으면 복사/다운로드/텔레그램·이메일 발송 텍스트까지 배지가 따라 나간다(사용자가 명시적으로 우려한 지점이고, 앞선 `UNCLASSIFIED_GROUP_NAME` 유출 버그와 같은 종류다. 화면 4개·`plain_text`·`.txt` href·`build_latest_plain_text()` 전부 검사해 유출 0 확인). 배지 조건은 `group["name"] in ai_names and display_name == original_name` — `ai_names`는 `app/llm_classifier.py` `cached_group_names()`가 캐시만 읽어 돌려주므로(API 호출 없음) **규칙 기반으로 폴백한 회차엔 배지가 안 붙고**, 담당자가 ✏️로 이름을 바꾼 소제목에서도 사라진다("이건 이제 내 이름"). 임시 보관함(`UNCLASSIFIED_GROUP_NAME`)은 꺾쇠(`<>`)를 빼고 `.unclassified-title`(회색 기울임꼴) + `.unclassified-count` 건수 배지로 그린다 — 꺾쇠가 붙으면 "확정본으로 그대로 넘어갈 소제목"처럼 보인다는 지적 때문이다(이모지도 🆕 → 📂).
  - **재분류 시 담당자가 바꾼 이름을 AI에게 넘긴다** [Changed: 2026-08-11]: `_build_prompt`의 "직전에 쓰던 소제목" 힌트가 원래(자동 생성) 이름 대신 `display_group_name()`을 거친 **표시 이름**을 넘긴다. 이름표는 `{원래 이름: 표시 이름}`으로 저장되므로, 예전엔 "세제개편"을 "부동산 세제개편"으로 바꿔둔 담당자가 재분류를 요청해도 AI는 그 의도를 전혀 모른 채 이름을 지었고, 바뀐 이름이 살아남는 건 순전히 "AI가 우연히 같은 원래 이름을 다시 냈는지"에 달려 있었다(실측으로 확인). 이제 AI가 표시 이름을 그대로 내주면 그 자체가 화면 이름이 되므로 어느 쪽이든 담당자 의도대로 보인다. `app.curation`은 `app.classifier` → 이 모듈로 순환 임포트가 되므로 함수 안에서 지연 임포트한다.
  - **[버그, 수정: 2026-08-11] 소제목 삭제 확인창이 "원래 이름"을 보여줌**: `removeCustomGroup()`이 확인창 문구에 `data-name`(자동 생성된 원래 단어)을 써서, ✏️로 이름을 바꿔둔 소제목을 지우려 하면 담당자가 화면에서 본 적 없는 이름이 떴다(사용자 제보). 버튼에 `data-current`(표시 이름)를 추가해 **확인창은 표시 이름, 서버 전송은 원래 이름**으로 분리했다 — 저장 키가 원래 이름이라 전송값은 바꿀 수 없다. `app/renderer.py`·`app/preview_renderer.py` 양쪽 동일.
  - **Cost, measured.** ~3,184 input / ~141 output tokens per call on real article batches ≈ $0.0039/call on Haiku pricing ($1/$5 per MTok) ≈ ~5.4원. With the auto-1×-per-slot + manual-button design, a slot that's never touched by the button costs exactly 2 calls (scheduled collect + draft's one auto-classification) ≈ ~11원; unbounded manual "다시 만들기" clicks are the only way to exceed that, by design (user-paced, not silently accumulating).
- Within a sub-heading, articles are still ordered by the outlet-priority rule above.
[Added: 2026-08-05] Mousing over any article row (all screens — `.article` on 완성본/초안/지난 기사, `.live-row` on 실시간현황) shades it a neutral light gray (`#F3F4F6`) so a long list doesn't lose track of which row the cursor is on. Chosen deliberately different from the blue "checked" background and yellow "just-promoted" background so the three states never look alike; the `:hover` rule is placed *before* those state-specific rules in each stylesheet so a row that's both checked/promoted/status-colored (live.html) and hovered keeps showing its more meaningful color (same specificity, so source order decides).

- **Curation** (main screen, latest run only): each article has 🗑️ hide/restore (hidden articles stay in storage, just excluded from screen/export/wordcloud; reversible from the settings screen's "숨긴 기사 관리" — [Changed: 2026-08-05] its top bar's left link is "📝 스크랩 초안" (preview.html), not "홈", since undoing a hide is almost always followed by going to 초안 or 완성본, not the logo landing page; `app/settings_server.py` `_preview_href()`/`_HIDDEN_TOP_BAR_HTML`) and ↑/↓ reorder (moves within a sub-heading; at a group boundary, moves the article into the adjacent sub-heading). [Added: 2026-08-04] The bulk-move checkbox (article → "다른 소제목" dropdown) now shades the checked article's row with a soft background (`.article:has(.article-select:checked) { background: {hover}; }`) so multi-select state is visible before a move is committed. [Added: 2026-08-04] Each sub-heading with articles also gets a `.group-select-all` checkbox in its `<h2>` (before the name) — checking it sets every `.article-select` checkbox inside that `<section class="subheading">` to match (one-way only: unchecking an individual article afterward does not un-check the header). Not shown for empty custom sub-headings, since there's nothing to select. Same `toggleGroupSelectAll()` JS in both `app/renderer.py` and `app/preview_renderer.py`. [Added: 2026-08-04] Sub-headings themselves have ↑/↓ order buttons next to the rename/hide icons — persisted per-name to `data/group_order.json` (`app/group_order.py` `save_group_order`/`apply_group_order`, endpoint `/save-group-order`) and applied consistently across `index.html`, `preview.html`, and copy/export text (`app/renderer.py` `render_page`/`build_latest_plain_text`, `app/preview_renderer.py` `_compute_preview_content`) — a name not in the saved order falls back to the end, so new/renamed sub-headings still appear. Moving an article to a different sub-heading via the dropdown already auto-places it by outlet-priority order within the target group (`app/classifier.py` `_apply_forced_groups` sorts by `order_index` from the master outlet-sorted article list) — no separate implementation needed for that part. [Added: 2026-08-04] A "소제목별/시간순/언론사순" view-mode `<select>` sits in the action toolbar (`index.html`/`preview.html`) — this is a display-only toggle, it never touches stored sub-heading/order data. "시간순"/"언론사순" flatten every `.article` card across all sub-headings into one list (client-side JS `applyViewMode`, sorted by `data-pub-date`/`data-outlet-rank`), hiding the `.subheading` sections; switching back to "소제목별" physically moves the same DOM nodes (not clones, so 🗑️/checkbox/dropdown state stays correct) back to their recorded original parent+sibling position. `data-outlet-rank` is each article's index in the run's already outlet-priority-sorted master `articles` list (`app/renderer.py` `render_page`, `app/preview_renderer.py` `_compute_preview_content` both build `rank_by_url` and thread it through `render_article`). Restore must process recorded homes in reverse order — restoring forward can try to `insertBefore` a sibling that hasn't been un-flattened yet and throw. [Added: 2026-08-04] A fourth option, "소제목 내 시간순", keeps sub-heading boundaries intact (unlike "시간순"/"언론사순", which flatten across all of them) and just re-sorts each `.subheading` section's own `.article` children by `data-pub-date` — reuses the same `_FLAT_VIEW_HOMES` capture/restore plumbing so switching back to "소제목별" still works. [Changed: 2026-08-04] The dashed border that used to mark only *empty* custom sub-headings (`.subheading-empty`) now marks *every* user-created sub-heading regardless of article count (`.subheading-custom`, checked via `group["name"] in custom_names` rather than `not group["articles"]`) — a thicker sky-blue (`#7DD3FC`) dashed border so it stays visible even once the section is full of article cards, replacing an earlier icon-based idea (a border wrapping the whole card survives long article lists better than a small icon next to the title). [Fixed: 2026-08-04] Two sub-headings could end up with the exact same *display* name — e.g. an auto-classified group manually labeled "우리부 관련 및 기타" while a user-created custom group was also literally named "우리부 관련 및 기타" — rendering as two visually-identical duplicate sections with no way to tell them apart. `app/curation.py` `display_name_in_use()` checks a candidate display name against a caller-provided `active_names` set; `/rename-group` and `/add-custom-group` (`app/settings_server.py`) reject a collision with 409 and the client shows "중복된 소제목이 있습니다" instead of saving. Existing conflicting data (a stray `"국토교통부": "우리부 관련 및 기타"` label) was cleaned up manually — the check only prevents *new* collisions, it doesn't retroactively resolve ones already in `data/`. `_group_option_label()` (`app/renderer.py`, shared by `app/preview_renderer.py`) returns `display_group_name()` as-is (no original-name parenthetical — a same-day experiment showing `"표시 이름 (원래 이름)"` was tried and reverted per feedback: once renamed, users only think of a sub-heading by its new name). The helper's plumbing was kept since both files call it everywhere the dropdown is built. [Added: 2026-08-07] The "다른 소제목" dropdown (both the per-article one and the bulk-move-bar's) now bolds (`style="font-weight:700"`) any option whose raw name is in `custom_names` — plain text alone made auto-classified and user-made sub-headings indistinguishable in the dropdown, unlike the main card list which already has the sky-blue dashed border. Native `<option>` elements can't carry backgrounds/icons reliably across browsers, but `font-weight` does render, so this is the same "text-only" constraint noted earlier for this dropdown, just applied differently. Server-side, `_group_select_html()` (`app/renderer.py`/`app/preview_renderer.py`) now takes an optional `custom_names` set; client-side, `getAllGroups()` derives the same flag per group from `.closest(".subheading").classList.contains("subheading-custom")` so the bulk-move-bar's JS-built dropdown matches without a second source of truth. [Changed: 2026-08-04] Duplicate scope was narrowed from "anywhere in `group_labels.json`'s history" to "whatever sub-headings are currently rendered together on the screen the rename/create happened from" — per feedback, the same display name recurring across *different* runs (완성본 vs a past 회차, or 완성본 vs 초안) is fine and expected; only a collision *within* one render is confusing. The client already has this list for free (`getAllGroups()`, same source the bulk-move dropdown uses) and sends it as a repeated `active_names` form field — the server no longer recomputes or scans `custom_groups.json`/history itself, it only checks the names it's handed. [Added: 2026-08-04] The bulk-move bar also has ↑/↓ buttons (`#bulk-move-up`/`#bulk-move-down`) to shift several checked articles (e.g. 3-4) as one block, one position up/down within their shared sub-heading — disabled whenever the checked articles span more than one sub-heading, or when the block is already at that sub-heading's top/bottom edge (batch move never crosses a sub-heading boundary, unlike the single-article ↑/↓). `app/curation.py` `bulk_move_articles()` does the actual reorder: sort the selected articles by their current in-group position, then swap each one with its immediate neighbor in top-to-bottom order for "up" (bottom-to-top for "down") — swapping in that order is what makes the whole block shift together instead of the items crossing each other. `index.html` persists the result via `update_run_articles`/`snapshot_group_names` (endpoint `/bulk-move-order`); `preview.html` has no saved run yet, so `app/preview_renderer.py` `preview_bulk_move_articles()` saves it to `preview_order.json` instead (endpoint `/preview-bulk-move-order`) — both call the same `bulk_move_articles()` for the actual computation. [Added: 2026-08-05] The bulk-move bar also has a 🗑️ to hide every checked article at once (`bulkHideSelected()`, confirms first) — reuses the existing single-article `/hide-article` endpoint in a `Promise.all` (same pattern `hideGroup()` already used for "hide this whole sub-heading"), no server changes needed since hiding doesn't care about sub-heading boundaries. [Fixed: 2026-08-05] Firing several `/hide-article` calls concurrently (bulk-hide, or `hideGroup()` on a large sub-heading) could silently drop some of them — `app/curation.py` `hide_article`/`unhide_article` each do a read-modify-write of the whole `hidden_articles.json`, and `ThreadingHTTPServer` runs requests on separate threads, so two concurrent calls could both read the pre-hide state and the second write would clobber the first's addition. Fixed with a module-level `threading.Lock()` serializing that read-modify-write. [Fixed: 2026-08-05] Getting several articles to the exact target position with the batch ↑/↓ often takes multiple presses, but each press reloads the page (`bulkMoveOrder()`) and full reloads used to wipe every `.article-select` checkbox — the user had to re-check the same articles before every single press, easy to lose track of. `bulkMoveOrder()` now stashes the checked URLs in `sessionStorage` (`bulkMoveCheckedUrls`) right before `location.reload()`; a same-pattern IIFE at the bottom of the reloaded page (next to the existing `justPromotedUrl` one) re-checks those URLs and calls `updateBulkMoveBar()` so the bar reappears already active — same fix duplicated in `app/renderer.py` and `app/preview_renderer.py`.
- **소제목 미니 목차 (펼침형)** [Added: 2026-08-05]: a sub-heading with many articles means a lot of scrolling just to find another sub-heading to jump to. A floating round button (`.toc-toggle-btn`, fixed bottom-right, both `index.html`/`preview.html`) opens a small popup (`.toc-popover`) listing every currently-rendered sub-heading's display name — clicking one smooth-scrolls to it and closes the popup (same outside-click-to-close pattern as the 🖍️ highlight popover). Deliberately built as pop-up-on-demand rather than an always-visible fixed sidebar (the other option presented) per the user's own choice, expecting to iterate on this once tried. `buildTocPopover()` reads `.subheading[data-toc-name]` sections straight from the live DOM at open-time (not cached at page-load) — each `_render_groups`/`_render_preview_groups` section now carries `id="subheading-{index}"` and `data-toc-name="{표시 이름}"` for this purpose. Reading at open-time rather than build-time matters for `preview.html`, where `#preview-body` can be swapped out wholesale by `previewMoveArticle()` — the popover always reflects whatever's currently on screen. Anchor links are built via `document.createElement` (not string-concatenated HTML with an inline `onclick` containing a nested quoted argument) after a first version broke: the JS source lives inside a Python triple-quoted string, and a `\'` meant as a JS escape got consumed by Python's own string parser before reaching the browser, silently producing invalid JS embedded in otherwise-valid-looking `.py` source — worth remembering for any future inline-`onclick`-with-quoted-argument code in this codebase. [Added: 2026-08-05] Each row also got its own small ▲▼ (`.toc-order-btn`) so sub-heading order can be adjusted from inside the popover instead of scrolling to that sub-heading's own header buttons — deliberately per-row rather than "click a row to select, then use one fixed arrow pair at the top": the row's name click already means "jump to it", so reusing that same click for "select for reordering" would overload one gesture with two conflicting meanings. `moveTocOrder(rawName, direction)` reuses the exact same swap-and-post-to-`/save-group-order` logic as the existing header `moveGroupOrder()`, just keyed by the raw name captured from each row's `.rename-btn[data-current]` instead of reading `btn.dataset.name` off the clicked element. Because a `location.reload()` follows every order change and adjusting several sub-headings in sequence is the common case, `moveTocOrder()` stashes `tocPopoverReopen` and `tocJustMovedName` in `sessionStorage` before reloading — a same-pattern IIFE at the bottom of the reloaded page reopens the popover automatically and `buildTocPopover()` shades the just-moved row (`.toc-row-moved`, see color note below) for that one render, so a user doing several moves in a row can always tell which one they just touched. [Fixed: 2026-08-05] An empty custom sub-heading's dashed-border card looked far taller than its actual content (a title line + one hint sentence) — the real cause wasn't `.subheading-custom`'s own padding but `.subheading h2`'s un-reset browser-default margin (~15px both top and bottom at that font-size), which `.subheading-custom`'s border then wrapped around. Fixed by giving `.subheading h2` an explicit `margin: 0 0 6px` and trimming `.subheading-custom` padding (`14px 18px` → `8px 16px`) and `.empty-group-hint`'s top margin (`8px` → `6px`). While touching that rule, also switched `.subheading h2` to `display: flex; justify-content: space-between` (wrapping its content in `<span class="subheading-title">`/`<span class="subheading-icons">`) so ✏️/🗑️/↑↓ sit flush against the row's right edge instead of clustering right after the title with a large empty gap — requested alongside the padding fix. [Changed: 2026-08-05] `app/custom_groups.py` — custom sub-heading names used to persist indefinitely (`data/custom_groups.json` as a plain name array) until manually deleted, unlike "📌 담아두기" (`app/manual_articles.py`), which resets at midnight. Per feedback ("이것도 자정에 사라지게 못해?") this was switched to the same `{"date": ..., "names": [...]}` + lazy-reset-on-load pattern as `manual_articles.py` — `load_custom_groups()` returns `[]` once the stored date isn't today, self-healing the same way `_apply_forced_groups` already handles a sub-heading that vanished for any other reason (articles that were force-moved into a now-gone custom group just fall back to their natural classification or "기타" — verified empirically, no data loss). The pre-existing plain-array format is migrated on first read rather than discarded outright — treating already-created sub-headings as "made today" and re-writing them in the new dated format, so they survive through the rest of the current day and only start clearing from the following midnight (silently wiping a user's already-created sub-headings the moment this shipped would have looked like a bug, not a feature). [Added: 2026-08-05] The 소제목 미니 목차 popover also bolds (`font-weight: 700`) any row whose section carries `.subheading-custom` — the same class that draws the sky-blue dashed border on the actual sub-heading — so user-created sub-headings are visually distinguishable from auto-classified ones in the popover too, not just on the main card list. [Changed: 2026-08-06] New custom sub-headings now default to the *top* of the sub-heading list instead of the bottom (a prior request had explicitly asked for bottom placement — reversed per new feedback) — `app/custom_groups.py` `add_custom_group()` calls a new `_place_at_top_of_order()` that inserts the new name into `data/group_order.json` right after the last already-known custom name (preserving creation order among customs, matching `custom_groups.json`'s own append order) — or at position 0 if no custom name is registered yet. This is only the *default* position at creation time, same as the old "falls to the end" default — `apply_group_order` doesn't treat it as a permanent lock, so ↑/↓ and the TOC mini-menu can still freely move any sub-heading (custom or not) afterward.
[Changed: 2026-08-05] Per-article action icons (`.article summary`) were initially left as `justify-content: flex-start` (clustered right after the title, a prior explicit fix so short titles don't strand buttons far to the right) — but a follow-up screenshot showed exactly that: "다른 소제목"/🔄/✏️/🗑️/↑↓ landing at a different horizontal position on every row depending on title length, which read as inconsistent rather than helpful. Reversed per explicit request: `render_article()` (`app/renderer.py`, shared by `preview_renderer.py`/`history_renderer.py`) now wraps that whole button/dropdown group in `<span class="article-actions">`, styled `margin-left: auto` inside the still-`flex-start` summary row — the checkbox/title/pub-time keep hugging the left edge (unaffected), while the action group always sits flush at the row's right edge (fixed "column" position, like a table's action column) regardless of title length. `history.html` doesn't apply this CSS (its `.article summary` was never flex to begin with — plain list-item layout), so the added wrapper span is inert there. [Added: 2026-08-05] Moving an article now shades it — covers all four ways an article's position can change: single ↑/↓ (`moveArticle`), the per-article "다른 소제목" dropdown (`moveArticleToGroup`), the bulk-move-bar's dropdown (`bulkMoveSelected`), and the bulk-move-bar's ↑/↓ (`bulkMoveOrder`, which already had its own `bulkMoveCheckedUrls` restore — just extended that same loop to also add the class). The first three reuse a shared `justMovedUrls` sessionStorage key + restore IIFE (same one-shot pattern as `justPromotedUrl`); `preview.html`'s single ↑/↓ (`previewMoveArticle`) is the one move path that doesn't reload the page at all (a deliberate 2026-07-30 optimization swapping `#preview-body`'s innerHTML in place to avoid a redundant Naver re-search), so it adds the class directly after the DOM swap instead of going through sessionStorage. [Changed: 2026-08-05] Originally shaded the same yellow as `.just-promoted`, but that collides with two other existing meanings already on that exact color (`#FFF9C4`) — a manually-promoted article (`.just-promoted`) and, on `preview.html`, a newly-arrived article the periodic scrape just found (`.is-new-arrival`) — three different "why is this row highlighted" reasons all looking identical was confusing. `.article.just-moved` and `.toc-row-moved` (the sub-heading-mini-menu equivalent) now use a soft green (`#DCFCE7`) instead, reserved solely for "this item's position just changed" — chosen over light purple (the user's other suggestion) because purple is already one of the five `HIGHLIGHT_COLORS` rotation, so a purple row wash could be misread as a highlight-keyword match; green isn't used anywhere else in the palette. `.just-promoted`/`.is-new-arrival` themselves were left on yellow — untouched, out of scope for this fix. [Changed: 2026-08-07] The green (`#DCFCE7`) itself was still too vivid on real use — toned down to a muted sage `#E7EFE8` (`.article.just-moved`, `.toc-row-moved`, both `app/renderer.py` and `app/preview_renderer.py`). [Fixed: 2026-08-07] Mobile Safari was showing article titles (`.title-line`, inside `.article summary`) in a jarring purple after tapping to expand them. First fix attempt guessed this was `-webkit-tap-highlight-color`'s default tap-flash artifact and set it to `transparent` plus a fixed `color: {text}` on `.title-line`/`.article summary` across `app/renderer.py`, `app/preview_renderer.py`, `app/history_renderer.py`, and `app/live_renderer.py`'s `.live-title`/`.live-title summary` — a real, harmless fix (an explicit author-stylesheet color always beats this UA-stylesheet default), but it wasn't the actual source of the persistent purple the user meant. [Corrected: 2026-08-07] The purple she was asking about is a *deliberate* "이미 확인한 기사" (already-opened) indicator added 2026-07-28: `.article:has(details[open]) .title-line, .article:has(details[open]) .url { color: {seen_color}; }` in `app/renderer.py`/`app/preview_renderer.py` — an ordinary, freely-editable app CSS constant (was `seen_purple = "#A855F7"`), not any Safari-exclusive or unchangeable color. Per feedback ("이 색이 밝은 남색 정도였음 어땠을까"), renamed to `seen_color` and retoned to a bright navy `#3B5FA0`, keeping the "already opened" indicator itself — only the hue changed.

- **소제목 이름 고르기 창 (자동완성)** [추가: 2026-09-03]: 담당자 질문에서 시작됐다 —
  "부총리 후보자 관련 기사는 계속 올라오잖아. 지난 회차 이름을 기억하면 버그 확률이 높니?"
  답은 **기억이 아니라 자동 적용이 위험하다**였다: 2026-08-14 사고(전역 이름표라 「기타」에 붙인
  이름이 그 뒤 36개 회차·259건에 씌워짐)는 앱이 알아서 갖다 붙였기 때문이고, 목록으로 보여주고
  담당자가 고르는 건 그 경계를 안 넘는다. 그래서 자동완성으로 갔다.
  **설계는 시안 세 번을 거쳤고 축이 두 번 바뀌었다.** ① 처음엔 「+ 새 소제목 만들기」에 붙이려
  했는데, 담당자가 매 회차 실제로 반복하는 동작은 그게 아니라 **✏️로 AI가 새로 지은 이름을 직전
  회차 이름으로 되돌리는 것**이었다(실측 기록: 「후보자 비거주 주택 관련」이 11:00·14:00에서 AI
  원래 단어가 다른데도 같은 이름표로 두 번 입력됨). 그래서 ✏️가 주가 되고 + 만들기가 부수가 됐다.
  ② 다음엔 목록을 「사람이 친 이름 / AI가 지었고 그대로 둔 이름」 2단으로 나눴는데(전자는 항상,
  후자는 칠 때만), 담당자가 "정보가 많다, **직전 회차로 나누는 게 낫지 않냐**"고 했고 그게 맞았다 —
  사람/AI는 *출처*지 *쓸모*가 아니고, 직전 회차라는 축은 그 자체가 "지금 이어지는 사안"을 뜻한다.
  실측이 그 축을 받쳐줬다: 회차당 소제목 평균 7.9개(4~15)라 나눔 없이 목록 하나에 들어가고, 직전
  회차 이름 중 이번 화면에 이미 있어 취소선이 되는 건 9번 재보니 **거의 항상 1개**(대개 「기타」)라
  목록이 취소선으로 뒤덮이지도 않는다. 같은 조사에서 나온 더 중요한 숫자 하나 — **회차가 넘어갈 때
  직전 회차 이름 중 살아남는 건 1개뿐**이다(AI가 매번 거의 다 새로 짓는다는 뜻이자, 담당자가 매
  회차 ✏️를 누르는 이유). 다만 직전 회차만 보면 재사용의 절반을 놓쳐서(되쓴 이름 4건 중 2건이 더
  앞 회차 것) **치면 오늘·어제 전부(67개)에서 찾게** 했다. 이틀로 자른 건 며칠씩 이어지는 사안을
  잡으면서 목록이 안 부푸는 선이라서다. **목록 재료에서 조심할 것 둘**: `forced_group_target_names()`는
  「🤖 AI 기사 배정」이 지어낸 이름(`load_assigned_groups`)을 포함하므로 쓰면 AI 이름이 새어 들고,
  담당자가 **갈아치운** AI 원래 단어는 표시 이름만 모으면 자연히 빠진다(그게 맞다 — 거부한 이름이다).
  볼드는 담당자가 고치거나 만든 이름 표시인데, 커스텀 소제목은 원래 이름 자리에 저장되고 그 목록이
  자정에 비워져 **어제 만든 커스텀 소제목은 볼드가 안 붙는다** — 담당자와 상의해 그대로 두기로 했다
  (이름 자체는 다 나오므로 고르는 데 지장이 없고, 고치려면 회차 파일에 새 필드를 남겨야 하는데 그건
  앞으로 저장되는 것부터만 유효해 소급이 안 된다). 부품은 `app/renderer.py`에 두고 초안이 그대로
  받아쓴다(`render_article`·쓰레기통과 같은 이유 — 복제하면 한쪽만 고쳐져 갈라진다).

- **소제목 이름 고르기 창 — 찾은 게 없을 때 창이 안 줄어들던 문제** [수정: 2026-09-04]:
  제보는 스크린샷 한 장과 "왜 대화창이 튀어나와"였다. 「시장상황점검회의」처럼 **처음 쓰는
  이름**을 치면 목록에 걸릴 게 없는데, 창은 줄어드는 대신 그 자리를 `그런 이름은 오늘·어제
  쓴 적이 없어요. 그대로 새 이름으로 쓰면 됩니다.`라는 두 줄 산문(`.np-empty`)으로 채웠다.
  버그는 아니고 **설계가 한 갈래를 놓친 것**이었다 — 이 창은 "옛 이름을 되쓴다"는 쓸모 하나로
  설계했는데, 실제로는 새 사안 이름을 처음 치는 경우도 그만큼 흔하고 그때 이 창은 도움이
  아니라 통과해야 할 관문이 된다. 문제가 셋이었다: ① 담당자가 안 물어본 답을 한다("본업
  화면은 자기를 해명하지 않는다" — CLAUDE.md 화면 용어 절), ② 하단 `.np-msg`의 `처음 쓰는
  이름이에요`가 같은 사실을 이미 말하고 있어 한 화면에 같은 말이 두 번 있었다, ③ 그 산문이
  목록만큼 자리를 먹어 **창 크기가 목록이 있을 때와 똑같았다** — "이름 한 줄 치는데 큰 창이
  계속 버틴다"는 인상의 실제 원인이 이것이다. 고친 방향은 문구를 다듬는 게 아니라 **안내줄
  (`.np-lead`)·목록(`.np-list`)·힌트(`.np-hint`) 셋을 통째로 `hidden`으로 숨기는 것**이다
  — 그러면 창이 입력칸+버튼만 남아 사실상 예전 `prompt()` 크기가 되고, 목록이 실제로 있을
  때만 커진다. 📂 미분류 배지가 0건이면 안 그리고 📷 사진 추정 버튼이 0건이면 안 그리는 그
  관례를 그대로 적용한 것이고, `.np-empty` 규칙은 쓰는 곳이 없어져 CSS에서 지웠다. 창을 열자마자
  (아무것도 안 친 상태에서) 직전 회차 소제목이 하나도 없는 경우도 같은 경로라 `최근에 쓴
  소제목이 아직 없어요.`가 함께 사라졌다 — 같은 이유로 그것도 안 물어본 답이었다. 브라우저에서
  네 갈래를 다 확인했다: 열자마자(목록 3줄) / 「시장」(찾은 이름 1줄) / 「시장상황점검회의」
  (입력칸만) / 「부동산 대책」(취소선 + 빨간 테두리 + 바꾸기 잠김).

- **소제목 이름 고르기 창 — 이름 바꾸기가 현재 이름을 안 채워주던 문제** [수정: 2026-09-09]:
  제보는 "「<소제목>」을 작성하려고 하면 현재 소제목이 안 남아있게 돼. 지금 현재 소제목에서
  조금 수정하고 싶은데"였다. 의도한 동작이 아니라 **2026-09-03에 이 창을 만들며 딸려 나간
  회귀**다 — 그 전 `prompt("소제목 이름을 입력하세요", current)`의 두 번째 인자가 프리필이었고,
  새 창은 만들기(create)와 바꾸기(rename)를 안 갈라 `input.value = ""` 한 줄로 둘 다 빈 칸으로
  열었다. 값을 새로 구할 필요도 없었다 — `openNamePicker`가 이미 `opts.self`(= `data-current`,
  「소제목 형식」 템플릿을 안 씌운 원본 이름)를 겹침 검사용으로 받고 있었고, 그 값이 정확히
  프리필에 넣어야 할 값이다. CLAUDE.md도 계속 "rename-prompt prefill은 늘 가공 전 이름을
  보여준다"고 적고 있어 **문서가 말하는 규칙과 코드가 엿새 동안 어긋나 있었다**.
  **한 줄만 고치면 새 문제가 둘 생긴다는 게 이 수정의 핵심이다.** `input.value = opts.self`만
  넣으면 곧바로 도는 `_npCheck()`가 그 값을 검색어로 취급해서 ① `_npRender(현재이름)`이 목록을
  그 이름으로 걸러 **직전 회차 목록이 창을 열자마자 사라지고**(대개 0건이라 2026-09-04 규칙에
  따라 목록 자리가 통째로 숨는다 — 그 목록이 이 창의 존재 이유인데 rename 경로에서만 없어진다),
  ② `self`가 `_npTaken`에서 빠져 있어 clash도 아니고 오늘·어제 pool에도 없으면 `.np-msg`가
  **화면에 버젓이 있는 이름을 두고 `처음 쓰는 이름이에요`**라고 말한다(안 물어본 답인 데다 거짓말).
  그래서 `_npPristine` 플래그를 하나 뒀다 — 창을 열 때 켜지고 첫 `input` 이벤트나 목록 행 클릭에서
  꺼지며, 켜져 있는 동안 `_npCheck`는 `q = ""`로 계산한다(목록은 직전 회차 그대로, 아랫줄은 빈
  상태). 프리필은 `input.select()`로 전체 선택해서 열어 **통째로 새로 치는 쪽과 뒤에 몇 글자
  붙이는 쪽이 둘 다 산다** — 전자는 예전 `prompt()`와 완전히 같은 손놀림이고, 후자는 → 한 번이다.
  안 고치고 그대로 [바꾸기]를 눌러도 되는데, 같은 이름으로의 rename은 서버에서도 무해한 no-op라
  예전 `prompt()`와 동작이 같다. 만들기는 채울 "현재 이름"이 없으므로 그대로 빈 칸이다.
  부품이 `app/renderer.py` 한 곳이라 확정본·초안이 함께 고쳐졌다. 브라우저에서 여섯 갈래를
  확인했다: 열자마자(프리필 + 전체 선택 + 목록 3줄 + 아랫줄 빈 상태) / 뒤에 「책」 붙이기(목록
  자리 사라지고 `처음 쓰는 이름이에요`) / 「세제」로 검색(1줄) / 「기타」로 겹침(빨간 테두리 +
  바꾸기 잠김) / 만들기(빈 칸 + 목록 3줄, 예전과 동일) / 목록 행 클릭·[바꾸기] 제출.

- **소제목 이름 고르기 창 — 목록이 「직전 회차 하나」에서 「오늘·어제 전부」로** [수정: 2026-09-10]:
  제보는 "이전에 이름 선택했던 기능은 왜 뺐어?"였다. 뺀 적이 없었다 — 창을 열면 목록은
  정상으로 떴고, **그날 그 담당자의 화면에서만** 0줄이었다. 파고 보니 원인이 셋으로 겹쳐 있었다.
  (1) **앱이 6일째 옛 코드로 돌고 있었다.** 9/9에 고친 프리필이 화면에 안 나온 게 이 제보의
  시작이었는데, 파이썬은 프로세스가 뜰 때 모듈을 읽으므로 launchd로 자동 실행을 붙인 뒤로는
  파일을 고쳐도 재시작 전까지 반영되지 않는다. 그 사이 `index.html`은 회차마다 멀쩡히 다시
  구워지고 있어 "반영이 안 된다"는 신호가 어디에도 안 났다(그래서 `autostart/README.md`에
  「코드를 고쳤으면 재시작해야 한다」 절을 새로 넣었다). (2) **직전 회차가 빈 회차였다.**
  9/9 20:20에 실패한 19:30 회차의 재시도가 자정을 넘겨 00:19에 통과했는데, 회차 시간창이
  언제나 "오늘"로 해석되므로(`kst_today_at`·`_is_today_kst`) 전날 기사가 전부 걸러져 **기사
  0건짜리 `2026-09-10_19-30.json`**이 생겼다. 초안이 그걸 직전 회차로 집었고, 소제목이 0개라
  2026-09-04의 「찾은 게 없으면 목록 자리를 안 그린다」 규칙에 걸려 창이 통째로 입력칸만
  남았다(같은 사고가 `trash-can/2026-08-26_19-30.json.잘못된날짜`로 전에도 한 번 있었다).
  (3) **애초에 기본 목록의 모수가 너무 좁았다.** 회차 하나에 전부를 걸어두면 그 회차가 비는
  날 목록이 통째로 죽는다 — 실측으로 그제 시점에도 0줄이었다. 게다가 2026-09-03에 이미
  "되쓴 이름 4건 중 2건이 더 앞 회차 것"이라고 적어두고도 기본 목록은 직전 회차만 보고 있어,
  **자기가 적어둔 실측과 어긋난 기본값**이었다.
  그래서 기본 목록을 **오늘·어제 전 회차**로 넓혔다(사용자 결정). 겁냈던 건 길이인데 실측하니
  24~58줄이었고, `.np-list`에 `max-height: 250px; overflow-y: auto`가 **이미 걸려 있어서**
  창 크기는 그대로였다(도입 때 걸어둔 값이라 새로 할 일이 없었다 — "높이 제한을 같이 걸어야
  한다"고 했던 최초 판단이 틀렸다). 최신 회차순이라 위 8줄이 곧 직전 회차다.
  **시안 셋을 실물로 비교했다**([NAME_PICKER_RANGE_MOCKUP.html](mockups/NAME_PICKER_RANGE_MOCKUP.html),
  실제 CSS·실제 저장된 소제목 이름). A(지금 방식) / B(평평한 한 줄 목록) / C(회차 구분선).
  담당자가 C를 골랐다. 다만 **구분선은 기본 목록에만 둔다** — 검색 결과는 대개 1~3건인데
  서로 다른 회차라, 치는 순간에도 구분선을 그리면 `개각` 2건 → 4줄, `TF` 3건 → 5줄로 **줄만
  두 배가 되고 구분선 하나에 이름 하나가 매달린다**(실측). 게다가 그때 담당자가 묻는 건 "몇
  회차 것인가"가 아니라 "그 글자가 든 이름이 있나"이고, 줄 오른쪽 `.np-when`이 이미 회차를
  말한다. 그래서 **치면 구분선 없이 평평하게, 안 치면 구분선 + 첫 묶음에 「· 직전 회차」**다.
  **검색 결과가 0건이면 기본 목록으로 되돌린다**(2026-09-04 규칙의 예외). 이름 바꾸기는 현재
  이름을 미리 채워 열기 때문에 가운데를 눌러 한 글자만 고치면 그 긴 이름이 그대로 검색어가
  돼 **반드시** 0건이 된다 — 그때 목록을 숨기면 "고치려고 열었더니 목록이 사라지는" 화면이
  된다. 이제 목록 자리를 통째로 안 그리는 건 **오늘·어제에 저장된 회차가 아예 없을 때뿐**이다.
  **`name_pool`의 `all`에도 cutoff을 걸었다** — 확정본에서 안 빼면 목록 맨 위 묶음이 그 화면에
  이미 떠 있는 이름들이라 통째로 취소선으로 뜬다(실측 5줄). 검색 때도 그 이름들은 어차피 못
  고르는 값이고, 「이 화면에 있음」 취소선은 pool이 아니라 살아 있는 DOM(`getAllGroups`)으로
  판정하므로 그 장치는 그대로 산다. 같이 `prev`(직전 회차 목록)를 payload에서 뺐다 — 첫 묶음이
  곧 직전 회차라 화면이 구분선에 적기만 하면 된다.
  브라우저에서 확정본·초안 양쪽을 확인했다: 확정본 20줄·구분선 6개(첫 묶음 `오늘 06:00 ·
  직전 회차`) / 초안 24줄·구분선 7개(첫 묶음 `오늘 09:30 · 직전 회차`) / `개각` 입력 시 1줄·
  구분선 없음 / 현재 이름 뒤에 한 글자 붙여 0건일 때 기본 목록 복귀(`찾은 이름이 없어요 ·
  최근 쓴 이름`).

- **소제목 이름 겹침 — 검사를 통째로 걷어냈다** [수정: 2026-09-16]: 담당자가 초안에서
  「후보자 청문회 후속」을 만들려다 바로 아래 2026-09-03 검사에 막혔고, 그 안내문을
  캡처해 왔다 — *"이게 뭔 뜻이야...? 하루 전체 회차에 다 떠도 어차피 시간대가 다르잖아"*.
  두 가지가 드러났다. **첫째, 문구가 틀렸다.** `그대로 만들면 그 회차 화면과 보고서에
  같은 이름이 두 줄로 나옵니다`라고 했는데, 갓 만든 커스텀 소제목은 비어 있고 **빈
  소제목은 복사·txt·발송 텍스트에 안 들어간다**(`app/renderer.py` `render_page`가
  `groups = [g for g in render_groups if g["articles"]]`로 걸러 `_build_plain_text`에
  넘긴다 — 2026-07-30부터 그랬다. 바로 아래 2026-09-03 항목의 "복사·txt·발송 텍스트에도
  같은 소제목이 두 번 찍힌다"는 두 칸이 **둘 다 채워졌을 때만** 참인데 문구는 그걸
  안 가렸다). 정기 보관함도 커스텀 소제목을 안 그리므로 거기서도 안 겹친다. 실제 피해는
  **직전 확정본 화면에 빈 소제목 한 줄이 더 뜨는 것뿐**이었다. **둘째, 막는 것 자체가
  업무와 어긋났다** — 담당자 설명은 *"그냥 같은 제목으로 계속 보도 지속 사항을 볼 때가
  있어서"*, *"보관함엔 어차피 소팅될 때 시간별로 나오잖아"*였다. 같은 사안을 회차마다
  이어 보려면 같은 소제목 이름을 계속 쓰는 게 정상 업무인데, 화면 한 줄 때문에 그 이름을
  **아예 못 쓰게 하드 차단(409)** 하고 있었다. 「확인창으로 낮추기」·「문구만 정직하게 +
  ✏️ 이름표 경로 안내」도 같이 올렸지만 담당자가 통째 삭제를 골랐다(사용자 결정). 그래서
  `_other_round_using_name`·`_round_still_shows`·`_live_round_ids` 셋과 호출부 분기를
  들어냈고(`list_runs_for_date` import도 같이 죽어 제거), **같은 화면 안 중복
  (`display_name_in_use`)은 그대로 뒀다** — 그건 한 화면·한 보고서에 같은 제목이 두 번
  찍히는 진짜 사고라서다. 격리 서버로 실측 확인: 14:00 회차에 「후보자 청문회 후속」
  이름표를 붙여둔 상태에서 ① 17:00 초안에서 같은 이름 만들기 → **204(통과)**, ② 14:00
  화면에서 같은 이름 만들기 → **409(여전히 막힘)**. **남는 한계 하나**: 직전 확정본
  화면에 뜬 빈 쪽을 🗑로 지우면 `remove_custom_group`이 이름을 하루 전체에서 지우므로
  초안 쪽 같은 이름 칸도 함께 풀린다(기사는 `group_overrides`가 붙들고 있어 그 이름
  아래 남지만, 커스텀 소제목의 파란 점선·볼드는 사라진다). 실제로 밟히면 그때 막는다 —
  지금 없는 불편을 미리 막느라 되돌아온 게 이번 건이라서.

- **소제목 이름 겹침 — 회차별 이름표 vs 하루짜리 커스텀 소제목** [수정: 2026-09-03]: 목차에
  `<세제개편안 후속>`이 두 줄로 떴다. 원래 단어가 다른 두 소제목의 **표시 이름**만 같아진
  것이라(「1주택 세제 개편안 논란」의 이름표 ↔ 직접 만든 「세제개편안 후속」) 2026-08-04에
  만든 `display_name_in_use`가 막았어야 할 바로 그 상황인데 통과했다. 원인은 **두 저장소의
  범위가 다르다는 것**: 이름표(`group_labels`)는 2026-08-14부터 회차별로 살고(그 회차 화면만
  본다), 커스텀 소제목(`custom_groups`)은 예나 지금이나 **하루 전체**가 범위다. 그래서
  09:30 회차에서 이름표를 붙여둔 뒤 14:00 화면에서 같은 이름을 새로 만들면, 검사는 14:00의
  이름표만 뒤져 "안 겹친다"고 답하고 — 만들어진 소제목은 하루 전체에 뜨므로 09:30 회차
  화면으로 돌아가면 같은 이름이 두 줄이 된다. 화면만의 문제가 아니라 복사·txt·발송 텍스트에도
  같은 소제목이 두 번 찍힌다. 고친 방향은 **범위를 저장소에 맞추는 것**이다 — 커스텀 소제목을
  만들 때만(`_other_round_using_name`) 오늘 모든 회차의 이름표를 훑는다. 이름표를 바꾸는
  쪽은 안 건드렸다: 커스텀 소제목은 어느 회차 화면에도 늘 그려지므로 그 화면의 `active_names`에
  이미 들어 있어 기존 검사로 잡힌다. **이름표만 보고 바로 막지는 않는다** — 그 소제목의 기사를
  전부 숨기면 화면에선 사라져도 이름표는 파일에 남으므로, 저장된 회차 파일에 그 소제목이
  아직 실제로 있는지(`filter_hidden` 통과분)까지 확인한 뒤에만 막는다. 실측으로 그 차이가
  실제로 났다: 같은 날 이름표 중 「가상자산 과세 토론회」는 그 소제목이 이미 없어져 막지 않고,
  「세제개편안 후속」만 막는다. 틀리는 방향을 이렇게 잡은 건 **화면 어디에도 없는 이름을 있다고
  우기는 안내가 제일 나쁘기 때문**이다 — 그래서 409 응답에 "오늘 09:30 회차의 「1주택 세제
  개편안 논란」에 붙인 이름표"라고 자리를 특정한 문장을 실어 보내고(`_respond_conflict`),
  화면 JS가 그 본문을 그대로 띄운다(본문이 없으면 옛 문구로 물러선다). 이미 만들어진 중복은
  자동으로 손대지 않는다 — 어느 쪽을 남길지는 담당자 판단이라, ✏️로 한쪽 이름을 바꾸거나
  빈 커스텀 소제목이면 🗑️로 지우면 된다.

- **소제목 미니 목차 — 초록(.toc-row-moved)은 언제나 한 칸** [수정: 2026-09-03]: 제보는
  스크린샷 한 장이었다 — 팝오버에 초록 블럭이 두 개, 하단은 "2개 위치 바뀜"("이거 고쳤잖아
  ㅜㅜ"). 2026-08-26에 고친 *밀려난 이웃까지 초록* 증상과 겉모습이 똑같아 되살아난 것처럼
  보였지만, 브라우저에서 재현해보니 그 버그는 멀쩡했다(한 칸 옮기면 초록 1개·"1개 위치
  바뀜"). 진짜 원인은 **설계 자체**였다 — 초록의 근거인 `_tocTouched`가 "마지막에 만진 것"이
  아니라 **팝오버를 연 뒤 만진 것 전부를 쌓아두는 딕셔너리**여서(`_tocSwap`이 `[moving]=true`만
  하고 이전 걸 안 지운다; 지우는 건 제자리로 돌아온 경우뿐), 두 개를 옮기면 초록도 둘이었다.
  담당자에게 그 두 개는 "방금 뭘 만졌는지"가 아니라 "그동안 만진 목록"이라, 정작 지금 커서가
  어디 있는지를 흐렸다. **초록은 항상 한 칸으로 못 박았다**(사용자 결정) — 새 변수
  `_tocMovedIdx` 하나가 마지막으로 옮긴 행을 들고 있고, `_tocRender`는 `idx === _tocMovedIdx`
  일 때만 칠한다. **이름이 아니라 위치(인덱스)로 기억하는 게 핵심이다** — 표시 이름이 같은
  소제목이 실제로 공존할 수 있어서(이름표로 바꾼 이름과 직접 만든 이름이 겹치는 경우를 이
  화면에서 실측했다) 이름을 키로 쓰면 엉뚱한 행이 같이 켜진다. 그 대가로 **하단 숫자와 초록
  개수가 더는 일치하지 않는다**(세 개를 옮기면 초록 1·숫자 3) — 2026-08-26엔 그 일치가 규칙
  이었지만, 숫자는 "적용하면 저장될 개수"고 초록은 "방금 옮긴 자리"라 뜻이 애초에 다르다.
  숫자 쪽은 `_tocTouched`를 그대로 쓰므로(이제 개수 세기 전용) "0개 위치 바뀜인데 적용할 게
  남아 있는" 상태는 여전히 만들어지지 않는다. (적용) 직후 새로고침에서 보여주는 1회성 초록
  (`tocJustMovedNames`)도 마지막 하나만 싣는다. 확정본·초안 두 렌더러에 같은 6줄을 동시에
  넣었다 — 이 JS는 두 파일에 복제돼 있어 한쪽만 고치면 화면이 갈린다.

- **"🔄 원문에서 다시 가져오기"** [Added: 2026-08-05]: Naver's `description` field is a raw snippet — it can start mid-sentence (photo captions, mid-article) or, rarely, arrive as an already-terse (not necessarily wrong) title. A button next to 🗑️ in every article row (all three screens + 지난 기사 — `render_article` in `app/renderer.py` shared by `preview_renderer.py`/`history_renderer.py`, plus its own copy in `app/live_renderer.py` `_render_row`) — hidden by default, appears on hovering that article card (`.refetch-btn { opacity: 0 }` / `.article:hover .refetch-btn { opacity: 1 }`). Manual and per-article by design (unlike the automatic `_fetch_full_title` "..." fallback) so it never slows down scheduled scraping — the cost is only paid for articles a user actually flags. `app/naver_api.py` `fetch_full_title_and_summary(url)` fetches the article page and prefers `og:title`/`og:description` (often a complete, outlet-written sentence) over Naver's snippet; whichever field it actually finds gets saved to `app/summary_overrides.py` (`data/summary_overrides.json`, `{url: {title, summary}}`, partial — a field not found leaves the other alone). `apply_summary_overrides()` is applied wherever an article list is finalized before rendering (`generate_screen`/`build_latest_plain_text` in `app/renderer.py`, `_compute_preview_articles` in `app/preview_renderer.py`, `generate_live_page` in `app/live_renderer.py`, `app/history_renderer.py`) so the fix is global and permanent, not tied to one screen. Endpoint `/refetch-summary` (`app/settings_server.py` `_handle_refetch_summary`) returns 404 if the page had neither tag (button re-enables so the user isn't stuck). [Added: 2026-08-05] A ✏️ sits right next to 🔄 as the last-resort manual path — when even og:title/og:description are unusable (or the outlet has neither), the user types a corrected title/summary directly rather than waiting on a code fix. Clicking it expands an inline form (title `<input>` + summary `<textarea>`, pre-filled from `data-title`/`data-summary` — the raw pre-highlight text, already available on the button since `hide-btn` carries the same) right inside the article's `<details>` (forced open). Saving posts to `/edit-summary` (`app/settings_server.py` `_handle_edit_summary`), which calls the *same* `set_summary_override()` as 🔄 — same storage, same partial-field semantics, same global reach across screens; only the source of the values differs (typed vs fetched). `app/live_renderer.py` has its own copy of the button/JS since its row markup (`.live-row`/`.live-summary`) differs from the other three screens' shared `.article`/`.article-summary`. [Fixed: 2026-08-05] "Prefer og:title, only fall back to `<title>` if og:title is entirely missing" wasn't enough — confirmed live on 한국일보 (`n.news.naver.com/mnews/article/469/0000946328`) that an outlet's own `og:title` tag can itself be truncated mid-sentence (a CMS bug on their end) while the plain `<title>` tag on the same page has the complete text, which is also why clicking 🔄 changed nothing the first time (it kept re-fetching the same broken og:title). `app/naver_api.py` `_best_title_from_html()` now fetches both and picks whichever is longer (normally `<title>` is longer anyway since it carries an outlet-name suffix, so this only kicks in when og:title is the truncated one) — shared by both `_fetch_full_title` (the automatic "..." fallback used at collection time) and `fetch_full_title_and_summary` (the manual 🔄 button), since both had the same bug.
- **History**: the last 7 days of runs are browsable (`history.html`), grouped by date → time slot; it does not re-run sub-heading classification, so no per-run curation there. [Added: 2026-08-12] The "📅 지난 기사" link (`href="history.html"`) was confirmed-screen-only until now — added to `preview.html` too (`app/preview_renderer.py` `_PAGE_TEMPLATE`, shared by all three draft states: in-progress/round-done/search-error), at the identical position and style as `index.html` (`.preview-head`/`.history-link`, title-adjacent, muted gray, not in the `.actions` toolbar — it's a navigation link, not an action). User request: draft-time curation benefits from checking "how was this grouped last time" just as much as the confirmed screen does, and wanted the two screens' entry point to match exactly rather than invent a new spot. [Changed: 2026-08-12] Per-article `.pub-time` here now shows plain `HH:MM` with no "· N분 전" — `app/renderer.py` `render_article`/`_format_pub_time` gained a `show_relative_time` flag (default `True`, unchanged for every other screen), and `history_renderer.py` is the one caller that passes `False`. This wasn't purely a preference call: the "· N분 전" half was already silently broken here — `_format_pub_time` always emitted the `.pub-time-relative` span, but the JS that fills it in (`renderRelativeTimes()`) was never included in this screen's `<script>` block, so users were seeing a dangling "· " with nothing after it. Removing it here also fits the screen's own nature better — relative "N분 전" communicates freshness, and history is inherently not fresh; the fixed `HH:MM` is what a fact-check needs. [Changed: 2026-08-12] Time-slot order within each date flipped from oldest-first to newest-first (`render_history_page`'s `day_runs = sorted(..., reverse=True)`) — the outer date grouping was already newest-first (`sorted(grouped, reverse=True)`), so the two levels had been pointing opposite directions; per user feedback ("사람들은 가장 맨 위 것을 궁금해한다") both now agree. [Added: 2026-08-12] Opening a time-slot toggle now reveals gray-toned "복사"/"다운로드" buttons next to the slot's own title (`details.slot > summary`, flex layout; `.slot-export-actions` is `display: none` by default and only shown via `details.slot[open] > summary .slot-export-actions` — pure CSS, no JS state) — gray rather than the toolbar's blue tone because several slots can be open at once here, unlike the single always-open screens, and a row of accent-blue button pairs per open slot would compete for attention. The copied/downloaded text uses its own header, deliberately different from every other screen's "OO시 기준": `YYYY-MM-DD HH:MM 기준` (`app/history_renderer.py` `_build_slot_plain_text`) — "OO시 기준" silently assumes "today," which is exactly wrong for a screen whose entire purpose is looking at a day that isn't today; user's framing was "이건 로데이터 개념이니까" (this is meant to be raw/reference data, not a same-day report). `_group_articles_by_saved_group()` was factored out of `_render_slot` so the HTML rendering and this plain-text builder read the exact same group split off the saved `"group"` field — the same "screen and export must never disagree" principle this codebase has been burned by before (`app.renderer`/`app.preview_renderer`'s screen-vs-send-text bugs). Download filename: `언론모니터링_{date}_{run_slot}.txt`. Both buttons carry `onclick="event.stopPropagation()"` since they live inside `<summary>` — without it, a click would also toggle the `<details>` open/closed (same reasoning as every other in-summary button in this codebase).

[개편: 2026-08-21] 정기 보관함을 수시 보관함(`app/adhoc/archive_renderer.py`)과 같은 UI로 맞췄다 — 사용자가 수시 보관함 캡처를 보여주며 "정기도 이렇게" 요청, 목업을 여러 차례 주고받으며 다음을 확정했다(순서대로): (1) 기간은 **날짜 기준**, 시간대는 **나중 시간이 위**(기존에도 그랬음, 유지). (2) 회차 수·기사 건수는 상시 표시 안 함 — "하루 4번 고정이라 4는 정보가 아니고, 건수 보고 찾아보진 않는다, 그냥 그날 궁금했던 것뿐"(사용자). (3) 발송 상태 칩을 만드는 과정에서 "미발송"이라는 이름을 거절당했다 — 담당자가 앱 밖에서 복사해 개인적으로 처리했을 수 있는데 "미발송"은 앱이 모르는 걸 단정하는 말이라서. 대신 이미 저장된 `sent_by`(`app.confirm_send.record_send`가 채움)를 그대로 읽어 **긍정형만** 쓴다: `발송 완료`/`자동 발송`, 둘 다 확정본과 같은 muted 회색(연보라 제안도 나왔었는데 — "자동 발송도 AI가 한 셈이니 연보라 아니냐" — 반려: 연보라는 이 앱에서 "누르면 API 비용이 드는 AI 판단"에 예약된 색이고, 자동 발송은 판단이 아니라 그냥 유예 시간이 지나 시스템이 보낸 것뿐이라 그 색의 뜻과 안 맞는다). 발송 실패를 알려주는 칩(`발송 실패`)도 논의했지만 **보류** — 지금 구조(`app/telegram_bot.py`/`app/email_sender.py`)는 성공/실패를 `logger`에만 남기고 호출부(`send_confirmed_run`)가 반환값을 버리므로, 저장된 데이터가 아예 없다. 채널별·수신자별 실패 사유를 저장하고 확정본에 먼저 노출한 뒤에야 보관함 칩으로 내려올 수 있는 별건이라 남겨뒀다. "빈 회차" 칩도 시안에는 있었으나 실측(저장된 회차 54개 전수)에서 기사 0건 회차가 하나도 없어 뺐다 — 뜰 일이 없는 칩은 안 만든다. "최신" 칩도 뺐다 — 맨 위에 있으면 이미 최신이라 중복 정보. 검색어(`matched_keywords`) 표시도 목업에 잘못 그려 넣었다가 바로잡았다 — 그 기능은 확정본·초안 전용(`show_matched_keywords`)이고 보관함에는 원래 없다. (4) **기간 조회 바**는 수시와 달리 서버 왕복이 없다 — 수시는 카드 파일을 실제로 걸러 읽어야 하지만, 정기는 날짜 목록이 이미 전부 DOM에 있어(지연 로딩은 "회차 안의 기사 내용"만 늦춘다) 클라이언트 JS가 `data-date`만 보고 숨기고/보여주면 된다(`applyHistoryPeriodFilter`, `historyPresetRange`) — `history.html`이 정적 생성 파일이라는 제약(`generate_history_page()`가 큐레이션 때마다 새로 씀)을 안 건드리는 쪽을 골랐다. (5) 복사/txt/엑셀을 결과 전체·날짜·회차 세 층에 — 엑셀은 서버 재계산이 필요해 기존 `/download-excel-history`를 그대로 재사용(`app/settings_server.py` 무변경). 복사/txt는 회차 층이 이미 만들어둔 `data-copy-text`를 날짜·전체 층이 클라이언트에서 이어붙이는 식으로 처리해 서버에 텍스트를 새로 계산시키지 않았다 — 날짜를 한 번도 안 펼쳐봤으면(지연 로딩 미완료) "날짜를 먼저 펼쳐주세요" 안내. **함정 하나**: `_PAGE_TEMPLATE`이 raw 문자열(`r"""`)이 아니라 일반 `"""`라서, 새로 추가한 JS의 `.join('\n\n')`에 쓴 `\n`을 Python이 (JS로 전달되기도 전에) 실제 개행 문자로 먼저 해석해버려 그 JS 문자열 리터럴이 줄바꿈으로 깨지고 `<script>` 블록 전체가 파싱 실패하는 사고가 있었다(브라우저에서 열었더니 스크립트가 통째로 안 먹어 페이지가 빈 화면처럼 보임 — 원인은 문법 오류 하나 때문에 이후 모든 함수 정의가 무효화된 것). 고친 값을 검증하려면 `node -c`로 추출한 `<script>` 내용만 따로 문법 검사하는 게 빠르다 — 브라우저 콘솔은 탭을 새로 열지 않으면 이전 에러가 캐시돼 고쳤는지 아닌지 헷갈린다(실제로 겪음). 교훈: 이 파일들처럼 non-raw 템플릿 문자열 안에 새 JS를 쓸 때 `\n`/`\t` 등 백슬래시 이스케이프가 필요하면 **반드시 두 번 이스케이프**(`\\n`)한다.
- **Telegram send** [Added: 2026-08-03]: two independent send paths, both via `app/telegram_bot.py` `send_text()` (splits on `\n` into ≤4096-char chunks for Telegram's per-message limit; never raises, returns `False` on missing config or request failure — a scheduled run or draft view must never break because Telegram is down). (1) Scheduled auto-send: `/telegram` settings screen (`app/settings.py` `save_telegram_settings`, field `telegram_auto_send`, default off) — when on, `main.py`'s `_scrape_and_render` calls `app.renderer.build_latest_plain_text()` (same text the completed screen's copy/export button would produce) and sends it, but only in the same branch where the screen itself is actually regenerated (skips the "0 articles and today already has a run" branch, since that branch intentionally keeps the previous run's screen unchanged — sending would just re-send stale content). (2) Manual send: the "📤 Telegram 전송" button (renamed from "📤 텔레로 보내기" [Renamed: 2026-08-03]) — present on both `preview.html` (`app/preview_renderer.py` `_actions_html`, posts to `/telegram-send-draft`) and `index.html` (`app/renderer.py`, posts to `/telegram-send-scrap`) — sends the page's already-rendered `PLAIN_TEXT` JS variable as-is rather than having the server recompute state, guaranteeing what gets sent always matches what's currently on screen, including any reordering/hiding done moments before.
- **Email send** [Added: 2026-08-06]: same two-path shape as Telegram send, but multi-recipient. `app/email_sender.py` `send_text(subject, body, recipients)` opens one SMTP connection (`EMAIL_SMTP_HOST`/`EMAIL_SMTP_PORT`/`EMAIL_SENDER_ADDRESS`/`EMAIL_SENDER_PASSWORD` from `.env`, all optional — `is_configured()` gates every send attempt) and sends one message per recipient in a loop (not one message with multiple `To:` addresses, so recipients never see each other's address) — a per-recipient `smtplib.SMTPException` is caught and logged so one bad address doesn't stop the rest. [Fixed: 2026-08-06] Originally sent `text/plain` — a live test showed Naver Mail's app doesn't auto-linkify bare URLs in plain-text bodies, so article links arrived as unclickable text. Switched to `text/html`: `_linkify()` HTML-escapes the body then wraps `https?://` runs in `<a href>`, and the whole thing is wrapped in `<pre style="white-space: pre-wrap">` so line breaks and spacing still look identical to the plain-text version everywhere else (copy/`.txt`/Telegram). Subject is just the body's first line (`text.split("\n", 1)[0]`, e.g. "언론 모니터링 17시 기준") — matches the on-screen header. `app/email_recipients.py` stores the recipient list (`data/email_recipients.json`, `[{"name", "email", "enabled"}, ...]`) — unlike Telegram's single fixed chat ID, this supports multiple people, each independently toggleable ON/OFF without deleting (added specifically for "누군가 대신 받아야 할 때" — e.g. covering for the primary user while they're on leave); `active_recipient_emails()` filters to only the enabled ones at send time. Settings screen (`/email`, `app/settings_server.py` `render_email_settings`) follows the same "numbered slots + del button + save-whole-list" convention as the keyword-group/highlight-word screens (not a fetch-per-action API) — `"+ 받는 사람 추가"` is a `formaction`-based submit button (`/email/add-recipient-slot`) that re-renders with one more empty slot while preserving already-typed values, exactly like `_handle_add_wordcloud_exclude_slot`. Manual send buttons on `index.html`/`preview.html` are simply labeled "Email" (see button-rename note below) and post to `/email-send-scrap`/`/email-send-draft`, mirroring `sendToTelegram`. Auto-send (`email_auto_send` setting) is checked in `main.py` `_scrape_and_render` right after the Telegram auto-send check, same trigger condition (only when the screen is actually regenerated).
- **Action button rename** [Changed: 2026-08-06]: alongside adding the Email button, `index.html`/`preview.html`'s action toolbar labels were shortened — "txt로 저장" → "다운로드", "📤 Telegram 전송" → "Telegram" (emoji and "전송" dropped) — so "Telegram"/"Email" now read as a matched pair.
- **전송 유예(현재 설계)** [Added: 2026-08-10, 확정 단계 제거: 2026-08-11]: 담당자가 회차 완료 시각까지 검토를 못 끝내도 텔레그램/이메일이 검토 없이 그대로 나가는 걸 막기 위한 안전장치. `main.py` `_scrape_and_render`가 `collect_run_with_retry` 직후 곧바로 `app/confirm_send.py` `confirm_and_promote(result)`를 호출해 회차를 완성본에 반영한다(별도의 "확정 대기" 화면·상태 없음 — `app/storage.py` `save_run`은 여전히 내부적으로 `confirmed=False`로 저장한 뒤 `confirm_run()`이 같은 호출 안에서 곧바로 `True`로 바꾸지만, 이건 구현 세부일 뿐 사용자에게 보이는 지연은 없다). 담당자에게 남는 유일한 수동 조작은 완성본의 진한 그린(`#15803D`) 정원형 **(발송)** 버튼뿐 — 누르면 텔레그램·이메일(설정된 채널만, 예전 개별 "Telegram"/"Email" 버튼을 통합)로 나간다. 회차 수집 시각(`run_at`)으로부터 `CONFIRM_SEND_GRACE_SEC`(`app/config.py`, 기본 300초=5분) 안에 담당자가 안 누르면 `app/scheduler.py`의 매 tick(`app/confirm_send.py` `check_pending_confirm_and_send`, `POLL_INTERVAL_SEC`=10초마다 확인)이 스스로 발송하고 `sent_by="auto"`로 기록한다(`send_confirmed_run(..., auto=True)`) — 탭이 닫혀 있어도 그대로 일어난다. 완성본 화면엔 아직 한 번도 안 보냈으면(`send_count==0`) `⏱️ MM:SS 후 자동 발송` 카운트다운이 빨간 글자로 뜨고(순수 표시용 JS, 실제 판단은 서버가 함), 0에 도달하면 `updateSendCountdown()`이 우선 파란 글자로 "자동발송 완료되었습니다."를 낙관적으로 보여준 뒤(스케줄러 tick 주기 때문에 실제 처리까지 최대 ~10초 오차 있음), 화면을 다시 열면 서버가 실제로 기록한 회색 "🤖 HH시 MM분에 자동 발송 완료" 배지(`sent_by`/`sent_at` 기준)로 바뀐다. 이미 한 번 이상 보낸 회차를 다시 보내면(완성본에서 손본 뒤 재발송) `send_confirmed_run`이 `send_count`(2회째부터)를 보고 제목 앞에 "(수정)"을 자동으로 붙인다. `app/storage.py` `load_latest_confirmed_run()`(confirmed가 True이거나 필드 자체가 없는 옛 데이터 중 가장 최근 것)은 `generate_screen()`/`build_latest_plain_text()`가 계속 쓴다 — 방금 수집된 회차가 아주 짧은 순간 `confirmed=False`로 저장돼 있는 경우에도 완성본이 안내 화면으로 잘못 덮어써지지 않게 하는 안전망이다.
  - [Removed: 2026-08-11] 최초 설계는 "수집 → 확정 대기 저장 → (사람이 파란 (확정) 버튼을 눌러야) 완성본 전환 → (사람이 초록 (전송) 버튼을 눌러야) 발송"인 2단계였고, 초안에는 회차 종료 30분 전부터 뜨는 "지금 마감하기"(=확정) 버튼도 있었다(`app/config.py` `EARLY_CLOSE_BUFFER_SEC`, `app/preview_renderer.py` `_early_close_available`/`earlyCloseDraft()`, `/early-close-draft`). 실사용 중 (1) 확정해도 완성본에서 계속 숨기기/이동/수정이 가능해 애초에 되돌릴 수 없는 동작이 아니었고(지킬 게 없는 걸 지키는 단계), (2) 확정 대기 중엔 초안 화면이 "다음 회차 미리보기"가 아니라 "확정 대기 화면"을 보여줘 "왜 다음 회차로 안 넘어가지?"라는 실제 혼란을 낳았으며, (3) 지금 마감하기는 온라인 기사 특성상 회차 시간창이 닫히기 전에 미리 닫으면 남은 구간 기사가 유실되거나 다음 회차와 중복 표시되는 문제가 구조적으로 있었다 — 이 세 가지 이유로 확정 단계·지금 마감하기 버튼을 통째로 없애고 위 단일 (발송) 설계로 정리했다. 그 과정에서 시도됐다가 함께 없어진 UI: 확정 클릭 시 뜨던 `.confirm-toast` 배너, (전송) 버튼의 `send_count` 원문자 배지(①②…, `.send-count-badge` — `send_count` 값 자체는 계속 기록되고 "(수정)" 판단에 쓰이지만 화면엔 안 보임), 확정본 전용 카운트다운 on/off 분기(`mode="pending"`/`"final"`). 상단 툴바 "📤 보고서로 내보내기"를 대신하던 주황 "내보내기" 플로팅 버튼도 없앴다("내보내기=동향과 의미가 겹친다"는 피드백) — `exportReport()` JS와 `/export-report` 엔드포인트 자체는 코드에 남아있지만(고아 코드, 무해) 어디서도 호출하지 않으며, `export_latest_run()`은 `confirm_and_promote()`가 여전히 자동으로 호출하므로 media_report 연동 자체는 버튼 유무와 무관하게 계속 동작한다. 완성본 플로팅 버튼 최종 순서(위→아래): (발송, 초록, `bottom:150px`) → (동향, 연보라 자리표시자 — 아직 백엔드 없음, `bottom:80px`) → ☰(소제목 목차, `bottom:20px`).
- **소제목 중복 경고문에 실제 이름을 담는다** [Changed: 2026-08-12]: `+ 새 소제목`/✏️ 이름변경이 이미 있는 이름과 겹치면 서버가 409를 돌려주고, 화면은 "중복된 소제목이 있습니다 — 다른 이름을 써주세요."라는 뭉뚱그린 문구만 보여줬다 — 어떤 이름이 겹치는지 화면에서 안 알려줘 담당자가 스스로 찾아야 했다. 서버 쪽 변경은 필요 없었다: 겹치는 이름은 곧 **담당자가 방금 입력창(`prompt()`)에 친 값 그 자체**라 JS가 이미 들고 있다(`createCustomGroup()`의 `name`, `renameGroup()`의 `newLabel`) — 409를 받으면 그 변수를 그대로 문구에 넣어 `이미 '{name}'라는 이름의 소제목이 있습니다.`로 바꿨다(`app/renderer.py`·`app/preview_renderer.py` 각 2곳, 총 4곳). 실제 fetch를 브라우저에서 실행해 정확한 문구가 뜨는 것과, 거부된 시도가 `group_labels.json`/`custom_groups.json`에 아무 흔적도 안 남기는 것을 확인했다.
- **"전체 기사 재분류" 버튼 화면 간 통일 + 확인창 항상 표시** [Fixed: 2026-08-12]: 확정본과 초안에 각각 만든 "전체 기사 재분류" 버튼이 스크린샷 비교로 서로 다르게 생긴 게 드러났다(확정본은 채운 연보라, 초안은 테두리만) — 같은 기능이 화면마다 달라 보이는 일관성 버그. 확정본 쪽을 초안 기준으로 맞췄다: **파괴적 동작은 어느 화면에서든 채우면 안 된다**는 원칙에 따라 `.actions .regen-btn`(확정본)도 배경을 투명으로 바꿔 고스트 스타일로 통일했다. 아이콘도 초안의 `↻`를 `🤖`로 바꿔 맞췄다 — `↻`가 "다시"를 뜻하는데 "재분류"에 이미 그 뜻이 있어 겹쳤고, 🤖 하나로 통일해야 `🤖 미분류 배정`(채움)과 짝이 맞는다.
  - **확인창이 `manual_edits==0`이면 아예 안 떴던 버그도 같이 고쳤다.** `manual_edits`는 "드롭다운으로 옮긴 기사 수"만 세는데, 이 버튼은 그게 0이어도 **소제목 이름·순서는 항상** 새로 짓는다 — 0건은 "잃을 게 없다"가 아니라 "손으로 옮긴 적이 없다"일 뿐이다. 실사용 중 담당자가 미분류 0건(=분류가 잘 정리된, 오히려 가장 잃을 게 많은 상태)일 때 확인창 없이 눌러 그대로 재분류돼버린 걸 제보 — 정확히 경고가 가장 필요한 순간에 안 뜬 것. 이제 **항상** `confirm()`을 띄우고, `직접 옮겨둔 기사 N건,` 문장만 `edits > 0`일 때 조건부로 붙인다(`app/renderer.py`·`app/preview_renderer.py` `regenerateSubheadings` JS 동일 적용). "(숨긴 기사와 담아둔 기사는 그대로 유지됩니다)"는 두 경우 다 사실이라 항상 유지.
- **LLM 캐시 디스크 영속화 + 실패 시 이전 분류 유지 + 초안 버튼 분리** [Changed: 2026-08-12]: 담당자가 새로고침했더니 멀쩡하던 초안 소제목이 다시 `<일자리>`/`<의원>`/`<납입>` 같은 한 단어로 뭉개진 걸 재제보 — 원인 셋을 찾아 전부 고쳤다.
  - **원인 1: 기사가 하나 늘고 하나 빠지면 캐시 재사용이 통째로 실패했다.** `_find_reusable`(줄기만 함)/`_find_partial`(늘기만 함)이 엄격한 부분집합·상위집합 관계만 인정해서, 초안이 새로고침마다 네이버를 다시 검색하며 흔히 벌어지는 "하나 늘고 하나 빠짐"엔 둘 다 실패해 규칙 기반(단어 빈도)으로 떨어졌다. `_find_partial`을 **겹치는 기사 수 기준**으로 바꿨다 — 캐시된 기사의 과반이 지금도 남아 있으면 재사용(오재사용 방지용 하한), 그중 겹침이 가장 큰 캐시를 고른다. 실측: 기사 5건 중 1건 교체돼도 기존 소제목 4/5 유지, 전혀 다른 기사 묶음엔 재사용 안 함(오재사용 없음 확인).
  - **원인 2: 앱 재시작 = 메모리 캐시(`app.llm_classifier._cache`) 증발.** `_cache`가 프로세스 메모리에만 있어 재시작할 때마다 좋은 이름이 전부 날아갔다. `data/llm_classification_cache.json`에 매 성공 시점(`_persist_cache_locked`, 캐시 락을 쥔 채로 파일까지 쓴다)마다 저장하고, 프로세스 시작 후 첫 `classify_with_llm`/`cached_group_names` 호출에서 한 번만 디스크를 읽어 메모리에 채운다(`_ensure_cache_hydrated`, 이중 체크 락). `app/undo.py`의 `_restore_llm_cache`/`_snapshot_llm_cache`도 각각 복원 후 `_persist_cache_locked()` 호출·스냅샷 전 `_ensure_cache_hydrated()` 호출을 추가했다 — 안 그러면 되돌리기가 메모리만 고치고 디스크는 그대로 둬서, 되돌린 직후 재시작하면 되돌리기 자체가 조용히 무효화됐다. 실측: `_cache`를 강제로 비워 "재시작"을 재현해도 좋은 이름 그대로 유지됨을 확인.
  - **원인 3: API 호출이 실패하면 곧장 규칙 기반으로 떨어졌다.** "이미 분류가 있으면 LLM이 실패해도 그걸 따른다"는 원칙(사용자 요청)을 실제로 구현했다 — `classify_with_llm`의 실패 경로(응답 파싱 실패·예외 둘 다)에서 `None`을 바로 돌려주지 않고, `_cache.get(key) or _find_reusable(key) or _find_partial(key)`로 대체 가능한 이전 분류를 먼저 찾는다. 있으면 조용히 그걸 재사용(캐시에 없는 새 기사는 임시 칸으로), **정말 없을 때만** `None`. `force=True`(담당자가 재분류를 직접 요청한 경우)에도 동일하게 적용 — 재분류 자체가 실패했다고 화면이 한 단어로 망가지는 것보다는 "달라진 게 없다"가 낫다. **추가로 즉시 1회 재시도**를 넣어 순단성 오류 자체를 줄였다(대기 없이 즉시 재시도 — 동기 HTTP 요청 처리 중이라 스크랩 재시도의 5분 간격은 못 쓴다).
  - **확정본 자동발송 보류.** `classify_with_llm`이 "API는 설정돼 있었는데 실패했고 대체할 이전 분류도 없어 결국 규칙 기반으로 떨어졌는지"를 `llm_attempt_failed_without_fallback()`으로 노출한다(API 미설정·기사 수 범위 밖처럼 "정상적으로 건너뛴" 경우는 여기 해당 안 함 — 그건 실패가 아니라 이 기능을 안 쓰기로 한 선택이다). `app/scraper.py` `collect_run`이 `snapshot_group_names` 직후 이 값을 읽어 `save_run(..., classification_degraded=...)`으로 회차 파일에 남긴다. `app/confirm_send.py` `check_pending_confirm_and_send`는 `classification_degraded`면 자동발송을 건너뛴다. `app/renderer.py` `render_page`는 카운트다운 자리에 `⚠️ AI 분류 실패 — 확인 후 직접 발송해주세요`를 대신 보여주고 `GRACE_DEADLINE_MS`를 0으로 둬 카운트다운 자체가 안 뜨게 한다("자동화가 스스로 실패를 감지했을 때만 사람을 부른다"는 원칙 — 정상일 땐 지금처럼 손 안 대고 나가고, 실패했을 때만 멈춰서 담당자의 (발송)을 기다린다). **화면 전용 경고다 — 이메일·텔레그램 등 별도 알림 채널·수신자 설정은 추가하지 않았다**: 이 앱은 로그인/세션이 없는 단일 사용자 로컬 도구라 "그 순간 화면을 보고 있는 사람 = 담당자"이고, 텔레그램 수신자 목록(`telegram_recipients.json`)은 보고서를 받는 상관·동료용이라 내부 운영 알림과 섞으면 안 된다(사용자 판단). 나중에 원격 알림이 실제로 필요해지면 그때 별도 설정을 만들기로 함.
  - **초안 버튼을 성격이 다른 둘로 분리했다** — 같은 자리가 상황에 따라 안전↔파괴적으로 바뀌는 걸 피하기 위해(담당자가 미분류를 방금 정리해 비운 직후 같은 자리를 또 누르면 전체 재분류가 실행돼 방금 정리한 걸 갈아엎는 시나리오를 피함):
    - **🤖 미분류 배정**(`app/settings_server.py` `_handle_assign_unclassified`, `/assign-unclassified`, JS `assignUnclassified`) — 기존 증분 배정 그대로, 이름만 "미분류 기사 분류"→"미분류 배정"으로 바꾸고 **툴바에서 📂 소제목 미분류 칸의 헤더로 옮겼다**. 미분류를 발견하는 그 자리에서 바로 누르도록(스크롤 불필요), 미분류 0건이면 그 칸 자체가 안 뜨니 버튼도 자동으로 사라진다(disabled 처리가 따로 필요 없어짐). 채운 연보라 배경(`.assign-unclassified-btn`) — 안전하고 자주 눌러도 되는 쪽이라 눈에 잘 띄어야 한다.
    - **↻ 전체 기사 재분류**(신규, `_handle_regenerate_subheadings_draft`, `/regenerate-subheadings-draft`, JS `regenerateSubheadings`) — 회차 초반 몇 건으로 잡은 분류가 기사가 쌓이며 안 맞게 될 때, 확정본까지 기다리지 않고 통째로 다시 묶는 수단(사용자 요청). 기존 소제목 이름·구성·순서를 전부 새로 짓는 파괴적 동작이라 **툴바에 남겨두되 테두리만(고스트, `.reclassify-btn`)** 둬 안전한 배정 버튼과 눈에 띄는 정도를 차등화했다. 확정본의 같은 이름 버튼과 **동일한 확인창 문구**를 쓴다(잃는 게 같으므로): `전체 기사를 다시 분류합니다. / 직접 옮겨둔 기사 N건, 소제목 이름과 순서가 초기화됩니다. / (숨긴 기사와 담아둔 기사는 그대로 유지됩니다)`. 확정본과 달리 저장된 회차가 없으므로 결과를 파일에 저장하지 않고 `app.llm_classifier._cache`(이제 디스크에도 남는다)에만 남겨, 다음 렌더링의 `classify_articles(allow_llm_call=False)`가 그대로 읽어가게 한다.
- **워드클라우드 전용 폰트(3.1MB) 제거** [Changed: 2026-08-12]: `app/config.py`가 4.2MB였던 원인은 `WORDCLOUD_FONT_BASE64`(Noto Sans KR 전체 글리프, base64 인코딩된 woff2) 하나였다 — Gaegu 서브셋(손글씨, 10KB)은 무해했다. 워드클라우드는 이미지가 아니라 그냥 `<span class="wc-word">` HTML 텍스트라는 걸 확인했고, 앱의 다른 모든 텍스트가 이미 `FONT_STACK`("Pretendard" → "Noto Sans KR" → 시스템 sans-serif)에 맡기고 폰트를 안 박아넣는데 워드클라우드만 예외로 3.1MB를 강제할 이유가 없었다(사용자 판단). `WORDCLOUD_FONT_NAME`/`WORDCLOUD_FONT_BASE64`와 `_WORDCLOUD_FONT_FACE_CSS`(`@font-face` 규칙)를 통째로 없애고, `.wc-word`의 `font-family`를 다른 화면과 동일하게 `FONT_STACK`으로 바꿨다(`app/landing_renderer.py`). **결과: `config.py` 4.2MB → 41KB, `home.html` 4.2MB → 24KB.** 실제 브라우저로 확인 — macOS 시스템 폰트로 자연스럽게 렌더링되고 시각적 차이 없음. Gaegu(빈 상태 손글씨체)는 그대로 유지 — 10KB라 무해하고 시스템에 없는 폰트라 서브셋 유지가 맞다.
- **키워드 그룹 전체 on/off 토글** [Added: 2026-08-12]: 기존엔 `include_in_scrap`(정기 스크랩 포함 여부, 꺼도 실시간 현황엔 계속 검색됨)과 `disabled_keywords`(키워드 단위 on/off)만 있어서, 분기(주중/주말 등)에 따라 통째로 안 쓰는 그룹을 매번 키워드 하나하나 꺼야 했다(사용자 요청으로 추가). `enabled` 필드를 그룹에 추가 — 꺼지면 **정기 스크랩·실시간 현황 양쪽에서 이 그룹 자체를 검색하지 않는다**. 단일 지점(`app/settings.py` `active_search_groups`)에서 걸러, 이 함수를 거치는 모든 호출부(`app.scraper.collect_run`의 사전 필터든 `app.live_renderer`의 전체 그룹 호출이든)에 자동 적용된다. `all_search_keywords`도 같은 이유로 `enabled` 체크를 추가했다(그렇지 않으면 실제로는 검색 안 되는 그룹의 키워드가 소제목 분류 제외 목록 등에 "쓰인 키워드"로 잘못 집계된다).
  - **"첫 그룹은 항상 on(기본 라벨)" 대신 "최소 1개는 켜져 있어야 함" 검증을 택했다** — 라벨을 붙여도 키워드를 전부 꺼버리면 우회되어 보장이 안 됐고, 이 그룹(`BASE_GROUP_NAME`)은 애초에 "담당 기관이 바뀔 수 있다"는 이유로 특수 취급을 없앤 적이 있어(2026-07-29) 그 결정을 되돌리는 셈이었다. `validate_keyword_groups`에 `include_in_scrap` 검증과 같은 자리·같은 패턴으로 추가했고, `include_in_scrap` 검증도 `enabled`를 같이 보도록 고쳤다(꺼진 그룹이 `include_in_scrap=True`인 채로 남아 있어도 실제로는 스크랩되지 않으므로, 그 경우를 "스크랩 대상 있음"으로 잘못 통과시키면 안 된다).
  - **"키워드를 켜면 그룹도 자동으로 켜진다"는 채택하지 않았다** — 실수로 그룹 전체가 되살아나는 걸 막기 위해서다. 대신 그룹이 꺼지면 하위 키워드 칸을 흐리고 잠근다(`app/settings_server.py` `.group-keywords-locked`, `opacity:0.45; pointer-events:none`) — 조작만 막을 뿐 `disabled_keywords` 데이터는 그대로 보존되므로, 다시 켜면 꺼두기 전 켜져 있던 키워드 구성 그대로 돌아온다(다시 세팅할 필요 없음). `"정기 스크랩에도 포함"` 체크는 이 잠금 밖에 둔다 — 다른 축이라 그룹이 꺼져 있어도 계속 설정할 수 있어야 한다.
  - **[버그, 수정: 2026-08-12] 토글을 눌러도 아무 반응이 없었다 + 계층이 안 보였다**(사용자 제보): 두 가지가 겹쳐 있었다. (1) 꺼진 그룹의 키워드 칸을 흐리고 잠그는 `.group-keywords-locked`를 **서버가 HTML을 그릴 때** 저장된 `enabled` 값으로 붙였다 — 그래서 화면에서 토글을 눌러도 저장하고 새로 그려지기 전까진 아무 변화가 없어, 담당자 입장에선 "규칙이 적용 안 된" 것처럼 보였다(사실상 없는 기능). 체크박스 상태에 즉시 반응하도록 `.group-block:has(.group-enabled-toggle input:not(:checked)) .group-keywords`로 옮겼다 — 스크랩 시간대의 `.time-row:has(...)`와 같은 방식이라 자바스크립트가 필요 없고, 서버 렌더 시점의 클래스 분기(`keywords_wrap_class`)는 통째로 없앴다(같은 걸 두 군데서 말하지 않는다). (2) 그룹 토글이 키워드 토글과 **똑같은 사각 ON/OFF 36×24**에 같은 왼쪽 정렬이라, 그룹 1개 + 키워드 5개가 형제처럼 한 줄로 늘어서 보였다("키워드가 그룹명과 병렬적 같아. 그룹명 on/off에 속해야 하는데"). **그룹 토글만** 알약형 슬라이딩 스위치로 바꿨다(`.group-enabled-toggle`, 48×28 + 흰 손잡이, `translateX`로 이동, ON/OFF 글자 없음 — 손잡이 위치와 색으로 읽힌다). **전부 알약으로 통일하지 않은 게 핵심**이다 — 모양을 통일하면 예쁘긴 해도 "누가 상위인지"는 여전히 안 보여서 원래 문제가 그대로 남는다(사용자도 같은 판단: "위 토글 이미지는 '그룹명'에 한해서야"). 모양만으론 절반이라 키워드 묶음에 들여쓰기 + 왼쪽 세로선(`.group-keywords`에 `margin-left:12px; padding-left:16px; border-left:2px`)도 같이 넣었다. 실제 브라우저에서 저장 없이 토글만 눌러 잠금이 즉시 걸리는 것(`opacity: 0.45`/`pointer-events: none` 실측)과 손잡이 이동을 확인했다.
  - UI는 스케줄 그룹의 "사용" 토글과 완전히 같은 마크업(`.toggle`/`.track`)을 재사용했다 — 새 패턴을 안 만들어도 "그룹째 켜고 끄기"라는 의미가 이미 그 모양으로 학습돼 있다. 마이그레이션: `enabled` 필드가 없는 기존 저장 그룹은 `load_settings()`가 `include_in_scrap` 때와 같은 방식으로 True(하위 호환)를 채워 넣는다 — 실측: 기존 3개 그룹 전부 `enabled: True`로 자동 채워지고 디스크에 저장됨, 검색 동작 변화 없음. 실 데이터로 그룹 하나를 껐다 켜며 저장·검증·`active_search_groups` 필터링·`all_search_keywords` 필터링·잠금 CSS까지 전부 실측 확인 후 원상복구했다.
- **확정본 재분류 차단 + 초안 증분 분류** [Changed: 2026-08-12]: 담당자가 ✏️로 AI 소제목 이름을 바꿨는데 화면이 다시 그려지면서 이름이 제멋대로 되돌아간 사고에서 출발. **원인**: `/rename-group` → `set_group_label()` → `_regenerate_screens()` → `render_page()` → `classify_articles(allow_llm_call=True)` → (앱 재시작 직후라 LLM 캐시가 비어 있으면) **진짜 API 호출** → 새 이름 생성 → 방금 저장한 이름표가 사라진 옛 이름에 묶여 고아. **이름을 저장하는 그 동작이 그 이름을 파괴**하고 있었다.
  - **확정본은 이제 재분류하지 않는다.** `app/classifier.py` `groups_from_snapshot()`/`groups_for_confirmed_run()` 추가 — 회차 파일에 이미 저장돼 있는 각 기사의 `group` 필드를 그대로 읽는다(`history.html`이 원래 쓰던 방식이라 그쪽은 이 버그가 없었다). 저장값은 항상 최신이다: `collect_run`이 저장할 때, 그리고 기사 이동·숨기기 등 모든 큐레이션 핸들러가 `snapshot_group_names`로 갱신한다(6곳). **화면(`render_page`)과 발송/복사 텍스트(`build_latest_plain_text`)가 반드시 같은 함수를 써야 한다** — 한쪽만 스냅샷을 읽으면 화면과 실제로 나가는 내용이 다른 소제목으로 갈린다. `group` 필드가 없는 옛 회차는 예전처럼 분류(하위 호환). 확정본에서 재분류가 일어나는 유일한 경로는 이제 **🤖 전체 기사 재분류** 버튼뿐이다.
  - **초안 🤖는 전체 재분류 → 증분 배정으로 바뀌었다.** `app/llm_classifier.py` `assign_to_existing(articles, existing_names)` 추가 — 기존 분류와 근본적으로 다르게 **"묶어라"가 아니라 "이 목록에서 골라라"**를 시킨다. 모델이 기존 소제목 이름을 바꿀 방법 자체가 없어서 담당자가 다듬어둔 분류가 구조적으로 안 흔들린다. 규칙(사용자와 합의): 📂 미분류 기사만 대상 / 기존 소제목에 넣되 정말 없으면 새 소제목 제안 / **담당자가 직접 만든 소제목은 후보에서 제외**(특정 기사만 넣으려고 만든 칸일 수 있는데 모델은 그 의도를 모른다 — 안 넣어서 생기는 손해는 드롭다운 한 번이지만 잘못 넣으면 신뢰가 깨지는 비대칭) / `MAX_SUBHEADINGS`가 이미 찼으면 새 이름 대신 `기타` / 프롬프트에서 `기타`를 후보로 주지 않는다(주면 애매한 걸 전부 거기로 흘려버린다). 배정 결과는 `group_overrides`(URL→소제목)에 저장 — 초안은 저장된 회차가 없어 여기 말고 담을 곳이 없고, URL 기준 전역이라 정식 회차 저장 시점까지 그대로 따라간다. 실측: 미분류 5건 → 3건은 기존 소제목, 2건은 새 소제목(진짜 다른 주제), 기타 0건, **기존 소제목 이름 변화 없음**.
  - **버튼이 화면별로 갈렸다**: 초안 `🤖 미분류 기사 분류`(+미분류 건수 배지, 0건이면 `disabled`) / 확정본 `🤖 전체 기사 재분류`. **초안 버튼은 상태에 따라 동작이 바뀌지 않는다** — "📂 있으면 증분, 없으면 전체 재분류"로 라벨이 변하는 안을 검토했다가 접었다: 담당자가 미분류를 막 정리해 비운 직후 같은 자리를 또 누르면 이번엔 전체 재분류가 실행돼 **방금 정리한 걸 갈아엎는다.** 같은 자리가 안전↔파괴적으로 바뀌는 건 나쁜 패턴이라, 초안에서는 전체 재분류를 아예 제공하지 않는다(없어서 불편한 걸 나중에 추가하는 게, 있어서 사고 나는 걸 나중에 빼는 것보다 안전하다).
  - **경고창은 확정본에만.** 초안 증분은 기존 소제목을 안 건드려서 잃을 게 없으므로 확인 팝업을 뺐다. 확정본 문구는 과장하지 않고 정확하게: `전체 기사를 다시 분류합니다. / 직접 옮겨둔 기사 N건, 소제목 이름과 순서가 초기화됩니다. / (숨긴 기사와 담아둔 기사는 그대로 유지됩니다)`. "이번 회차 작업이 모두 초기화" 같은 표현은 쓰지 않는다 — 실제로 숨긴 기사·담아둔 기사·키워드 메모는 그대로 남으므로, 과장하면 담당자가 필요할 때 못 누르거나 한 번 눌러보고 사실과 다른 걸 안 뒤로는 경고를 안 읽게 된다. 손댄 게 0건이면 경고 없이 바로 실행.
  - **연보라 `.just-classified`** — 🤖가 방금 배정한 기사를 한 번만 표시하고 다음 새로고침에 사라진다(`.just-moved`와 같은 sessionStorage 방식, 핸들러가 배정된 URL을 JSON으로 돌려준다). 색은 🤖 버튼과 **같은** `#F0EAFB` — "저 버튼을 눌렀더니 이것들이 생겼다"가 그대로 이어진다. CSS에서 `.is-new-arrival`(노랑)보다 **뒤에** 선언해 겹칠 때 연보라가 이긴다. 두 색의 구분 기준: **노랑 = 저절로 일어난 일(기사가 왔다) / 연보라 = 내가 시켜서 일어난 일(🤖가 배정했다)**. 실제로는 순차적이라 거의 안 겹친다 — `is-new-arrival`은 `localStorage.previewKnownUrls`와 비교해 그 기사가 화면에 **처음 뜬 로드에만** 붙고 즉시 갱신되므로, 담당자가 배지를 보고 🤖를 누를 때쯤이면 이미 노랑이 빠진 상태다.
- **🤖 소제목 분류 요청 버튼 위치·색 변경** [Changed: 2026-08-12]: 복사·다운로드·+새소제목·+키워드작성과 같은 파란 톤이라 툴바 가운데 껴서 잘 안 보인다는 피드백 — 왼쪽 버튼 그룹 맨 끝(구분선 `.actions-divider` 뒤)으로 옮기고, 배경을 연보라(`#F0EAFB`/글자 `#4A3F8F`/테두리 `#DCD0F2`)로 바꿨다(`app/renderer.py`·`app/preview_renderer.py` 동일 적용). 새 색을 만든 게 아니라 랜딩 화면 "동향 보고" 흐름(`app/landing_renderer.py` `.brief`)이 이미 쓰던 연보라를 가져온 것 — "파랑=담당자가 하는 일 / 연보라=AI가 하는 일"이라는 색 언어가 생긴다. 채운 진한 색 대신 옅은 배경 + 가는 테두리로 둔 이유: 너무 튀면 "눌러야 할 것 같은" 압박을 줘서 불필요하게 자주 눌리고, 그만큼 API 호출(=비용)이 늘어난다(사용자가 명시적으로 우려한 지점). 복사·다운로드 등 자주 쓰는 버튼 순서는 그대로 두고 위치만 옮겨, 다른 조작 흐름에 영향이 없다. [Fixed: 2026-08-12] 색을 바꾸자 버튼 하나만 다른 버튼보다 눈에 띄게 커 보인다는 제보 — 실측해보니 padding·font-size는 옆 버튼들과 완전히 같은데도 발생했다. 원인은 🤖 색이모지: 브라우저가 줄 높이(line-height)를 계산할 때 색이모지 글꼴의 내부 ascent/descent가 라틴·한글 텍스트보다 훨씬 커서, 같은 padding이라도 이모지가 든 줄만 박스 전체가 부풀 수 있다(다른 툴바 버튼엔 이모지가 없어 이 버튼만 겪는 문제). `.actions button, .actions a.btn { height: 32px; box-sizing: border-box; }`로 툴바 버튼 전체의 높이를 고정값으로 못박아, 내용이 이모지든 텍스트든 박스 크기가 항상 같게 했다(`app/renderer.py`·`app/preview_renderer.py` 동일 적용).
- **수집 시간 30분 단위 강제 + 자동발송 유예 상한 30분으로 축소** [Changed: 2026-08-12]: `/schedule` 화면의 분(MM) 입력을 자유 숫자 칸에서 `00`/`30` 두 값만 고를 수 있는 `<select>`로 바꿨다(`app/settings_server.py` `_minute_select_options`) — 09:00~09:10처럼 30분보다 짧은 회차를 만들 수 있었는데, 그러면 자동발송 유예 시간과 겹칠 수 있다는 사용자 지적. `app/settings.py` `validate_schedule_windows`도 서버 측에서 다시 검사한다(폼 데이터는 조작될 수 있으므로) — 시작·종료 시각의 분이 00/30이 아니면 `SettingsError`. 두 시각이 다 30분 단위면 시작~종료 차이는 항상 30분 이상이 보장된다. **함정**: `<select>`는 number input과 달리 아무 옵션도 `selected`가 아니면 브라우저가 첫 옵션을 선택된 것처럼 제출한다 — "+ 시간대 추가"로 만든 빈 칸의 분이 자동으로 "00"으로 제출돼, 시(hour)는 비어 있는데 분만 채워진 상태가 되면서 `_combine_hhmm`의 "둘 다 비어 있으면 그 칸은 삭제하려는 것"이라는 기존 규칙이 깨지고 형식 오류가 났다. 값이 빈 옵션(`""`, "--" 로 표시)을 맨 앞에 둬서 고쳤다 — 새 칸은 분도 진짜로 빈 문자열을 제출해 기존 동작이 유지된다. 같은 이유로 `MAX_AUTO_SEND_GRACE_MIN`도 60 → 30으로 낮췄다(`app/config.py`) — 회차 사이 최소 간격이 이제 30분까지 좁아질 수 있으므로, 유예 상한도 그와 같은 30분으로 맞춰 아무리 촘촘하게 회차를 잡아도 유예가 다음 회차를 잡아먹지 않게 한다. `/auto-send` 화면 문구도 정리했다: 힌트를 "정합니다" 서술형에서 "발송됩니다" 직접 서술로, 체크박스 라벨을 "자동 발송 사용" → "자동발송 ON/OFF"로, 괄호로 감싸던 "(발송)"을 전부 "발송"으로 뺐다(사용자 요청).
- **자동발송 설정 통합 + 고아 설정 복구** [Fixed: 2026-08-11]: `/telegram`·`/email` 화면에 "정기 스크랩 완료 시 자동 전송" 체크박스(`telegram_auto_send`/`email_auto_send`)가 각각 있었는데, 같은 날 확정 단계를 걷어내며 발송 경로가 `app/confirm_send.py` `check_pending_confirm_and_send`로 바뀌면서 **아무도 그 값을 읽지 않는 고아 설정**이 됐다 — 체크를 풀어놔도 유예가 지나면 그냥 나갔다. 2026-08-11 17시 회차가 소제목이 망가진 채(아래 `_MAX_ARTICLES` 항목) 자동 발송된 뒤 발견됐다. 확정본의 개별 Telegram/Email 버튼을 (발송) 하나로 합친 것과 같은 이유로 **채널별로 쪼개지 않고 하나로 통합**해 되살렸다: 새 설정 화면 `/auto-send`(`app/settings_server.py` `render_auto_send_settings`, `_AUTO_SEND_SETTINGS_TEMPLATE`)에 `auto_send_enabled`(사용 여부)와 `auto_send_grace_min`(유예 분) 두 값만 둔다. `app/settings.py` `save_auto_send_settings`가 검증(1~`MAX_AUTO_SEND_GRACE_MIN`=60분, 숫자 아니면 `SettingsError`)하고, `auto_send_grace_sec()`/`is_auto_send_enabled()` 헬퍼를 `app/confirm_send.py`(실제 자동 발송 판단)와 `app/renderer.py`(카운트다운 표시)가 공유한다. **끄면 카운트다운도 안 뜬다** — 아무 일도 안 일어나는데 "N분 후 자동 발송"이 흐르면 거짓말이라는 지적(사용자). **유예 상한이 60분인 이유**: `check_pending_confirm_and_send`가 `load_latest_run()` 하나만 검사하므로, 유예가 회차 간격보다 길면 다음 회차가 수집되는 순간 이전 회차가 발송되지 않은 채 조용히 묻힌다(가장 짧은 회차 간격이 90분: 09:30→11:00). **마이그레이션은 옛 값을 이어받지 않고 버린다**(`load_settings`가 `pop`) — 저장된 `False`는 "자동 발송을 끄고 싶다"는 의사가 아니라 아무 효력 없던 잔재라, 그대로 옮기면 지금까지 실제로 나가던 자동 발송이 갑자기 멈춘다. 설정 메뉴도 재편했다: **자동발송**(자동발송 설정 / 텔레그램 수신 대상자 지정 / 이메일 수신 대상자 지정) · **기타**(워드클라우드 제외어) — 전송 관련 3개가 "기타"에 섞여 있던 걸 자기 분류로 뺐다.
- **[버그, 수정: 2026-08-11] `_MAX_ARTICLES` 문턱 하나로 소제목이 통째로 망가짐**: 담당자가 "14시는 소제목이 멀쩡한데 17시는 `<대통령>`/`<부총리>`/`<세제>`/`<개편안>`이 떴다"고 제보. 원인은 `app/llm_classifier.py` `_MAX_ARTICLES = 60`인데 그 회차가 **61건**이었던 것 — 딱 1건 넘겨 `classify_with_llm`이 즉시 `None`을 돌려주고 규칙 기반(단어 빈도) 분류로 폴백했고, 그 단어 빈도 결과가 저 한 단어 소제목들이다. 저장된 42회차 중 3회차(61·61·62건)가 이미 이 문턱에 걸려 있었고 **하필 기사가 가장 많은 바쁜 회차에만** 터져서, 소제목이 가장 필요한 순간에 품질이 무너졌다. 60은 근거 없이 보수적으로 잡은 값이었다 — 실측하면 23건 = 입력 4,565토큰(기사당 약 200토큰)이라 61건도 약 12,000토큰이고, 모델 컨텍스트 200K에 비하면 무시할 수준이며 비용도 회차당 20원 안쪽이다. 진짜 제약은 컨텍스트가 아니라 출력(`max_tokens=2000` 안에 모든 기사 번호가 들어가야 함)인데 61건 응답이 약 250토큰이었으므로 150건도 충분하다. **`_MAX_ARTICLES` 60 → 150**(관측 최대 62건의 2배 이상 여유)으로 올리고, 이 분기가 **조용히** `None`을 돌려주던 것도 고쳤다 — `logger.warning`을 추가해 다음부터는 "기사 N건은 LLM 소제목 분류 범위 밖이라 규칙 기반으로 대체합니다"가 로그에 남는다(3회차를 겪고도 원인을 몰랐던 이유가 이 침묵이었다).
- **소제목 프롬프트를 "정책" 기준에서 "쟁점" 기준으로** [Changed: 2026-08-11]: `_SYSTEM_PROMPT`의 `같은 사건이나 같은 정책을 다룬 기사는 한 묶음이다`가 하나의 거대 덩어리를 만드는 원인이었다 — 세제개편 관련이면 쟁점이 뭐든 다 뭉쳐서, 23건 회차에서 17~19건짜리 묶음이 나왔다. 실제 제목을 보면 그 안에 '대통령 발언'(9건)·'부부공동명의 논란'(4건)·'시장 반응'(4건)이 명백히 섞여 있어 모델이 덜 나눈 것이었다. `쟁점 중심으로 묶는다 / 같은 정책이라도 쟁점이 다르면 나눈다('정부·여당 입장', '야당·시민 비판', '시장·업계 반응', '특정 조항 논란') / 한 묶음이 절반을 넘으면 덜 나눈 것이다 / 10건 미만이면 억지로 쪼개지 마라`로 교체했다. 측정: 23건 회차 최대 덩어리 17 → 13이고 2회 실행 결과가 동일해졌다(교체 전엔 같은 입력에 `19,1,1,1,1`과 `9,8,3,2,1`이 번갈아 나올 만큼 편차가 컸다). **`MAX_SUBHEADINGS` 5 → 8** [Changed: 2026-08-11]: 처음엔 옛 프롬프트로 재고 "상한이 병목이 아니다"라며 **기각했는데, 그 판단이 틀렸다.** 두 가지를 빠뜨렸다 — (1) 옛 프롬프트엔 적은 회차를 보호하는 규칙이 없어 7건 회차가 1건짜리 5개로 갈라졌고(그래서 올리면 나빠진다고 봤다), (2) **가장 큰 61건 회차를 측정 대상에서 빼먹었다** — 상한이 가장 절실한 케이스를 빼고 잰 것이다. 쟁점 기준 프롬프트(위)를 넣은 뒤 61건 회차를 포함해 다시 재니 결론이 뒤집혔다: 상한 5는 `[44, 12, 4, 1]`로 **44건이 한 덩어리**였는데 상한 8은 `[25, 21, 8, 7]`에 1건짜리 0개였고, 23건 회차도 최대 덩어리 13→10, 36건도 15→12로 줄었다. 과분할 우려는 새 프롬프트의 `기사가 10건 미만으로 적을 때는 억지로 쪼개지 마라`가 막는다 — 7건 회차를 상한 5·8로 각각 돌려 결과가 완전히 동일함을 확인했다(**프롬프트가 하한을 지키니 상한만 넉넉히 두면 된다**). PRD.md·prd_lite.md의 "최대 5개" 문구도 함께 고쳤다.
- **`MAX_SUBHEADINGS` 8 → 15, 밀도 규칙 추가, `max_tokens` 버그 발견** [Changed: 2026-08-20]: 재정경제위원회가 열린 날 기사량이 평소(5~20건)의 3~4배로 뛰면서 08-20 09:30 회차(61건)가 46건(75%)을 "기타"로 흘려보내는 걸 사용자가 직접 발견해 시작됐다. 지난 며칠(08-17~08-20) 저장된 회차를 전수조사하니 20건 이하 회차는 대체로 기타 0~30%대로 감당됐지만, 40건을 넘는 회차부터 급격히 무너졌다(08-20 14:00, 76건 중 22건). **저장된 실제 기사로 Claude API를 직접 재호출하는 A/B 실측**(캐시 파일을 스크래치패드로 격리 + 가짜 `round_id`로 실제 캐시/회차와 절대 안 섞이게 함)을 두 축으로 진행했다.
  - **상한 8 vs 15**: 76건 회차는 상한 15를 줘도 실제로는 11개만 써서 기타 23→16건으로 줄고 1건짜리가 사라졌다(AI가 상한을 채우려 억지로 안 쪼갬 — 필요한 만큼만 만듦). 반대로 "상한을 올리면 적은 회차가 잘게 쪼개진다"는 우려도 그대로 재현됐다 — 14건 회차가 상한 15에서 1건짜리 5→9개, 밀도 1.8→1.3으로 나빠졌다. 원인은 프롬프트의 "10건 미만이면 억지로 안 쪼갠다"가 절대 건수 기준이라 12~20건 구간엔 안 걸리기 때문. `AI_RULES.md`에 **"묶음당 평균 기사 수 2건 이상, 미달이면 가까운 1건짜리끼리 합쳐라"**는 밀도 규칙을 추가해 상한과 별개로 과다분할을 억제했다 — 이 규칙을 넣은 뒤 76건 회차는 여전히 11~15개·1건짜리 0~1개를 유지했다.
  - **비용**: 실측해보니 입력 토큰(기사 배치)은 상한과 무관하게 거의 그대로였고(76건 회차: 상한 8·15 둘 다 입력 13,050), 출력(소제목명+요약)만 상한에 비례해 늘어 회당 +2원 안팎에 그쳤다. `LLM_COST_USAGE.md`의 예전 "7~9원/회" 추정도 실측(12~33원, 회차 규모에 비례)으로 갱신했다 — 요약 필드 추가 이후 한 번도 실측을 안 갱신했던 게 드러났다.
  - **모델 교체(Haiku→Sonnet 5) 검토는 기각**: 사용자가 "더 큰 모델이 필요 없을 수도"라고 짚은 게 계기가 되어 실측했는데, 진짜 문제를 발견했다 — `app/llm_classifier.py`가 `thinking` 파라미터를 아예 안 넘기는데, Haiku 4.5는 그 기능이 없어 상관없지만 **Sonnet 5는 파라미터를 안 주면 기본으로 thinking이 켜진다.** 14건짜리(가장 쉬운) 회차로 진단 호출해보니 `output_tokens` 4,783개 중 4,233개(88%)가 안 보이는 추론이었고 실제 JSON은 12%뿐이었다 — `max_tokens=4096`으로도 부족해 8000을 줘야 겨우 끝났고, 회당 비용이 78원으로 같은 조건 Haiku(12.6원)의 6배가 넘었다(겉보기 단가차 2배보다 훨씬 크게 벌어짐 — thinking 토큰도 출력 단가로 청구되기 때문). 이 작업은 패턴 매칭에 가까워 깊은 추론이 필요 없다고 판단해 모델은 Haiku로 유지했다. 나중에 다시 검토한다면 `thinking: {"type": "disabled"}`를 명시하고 재실측할 것.
  - **부수적으로 발견한 별개 버그**: `max_tokens=2000` 하드코딩이 상한 15 도입 여부와 무관하게 이미 위험했다 — haiku·상한15·76건 케이스가 `stop_reason: max_tokens`로 **정확히 2000토큰에서 끊긴 채 우연히 파싱에 성공**한 게 실측으로 드러났다(다음엔 진짜 잘려 조용히 규칙 기반 폴백으로 떨어질 뻔했다). `max_tokens`를 4096으로 올려 재확인하니 2,003토큰에서 자연 종료(`end_turn`)했고 비용은 변화 없음(32.6원 동일 — `max_tokens`는 상한일 뿐 청구액이 아니다).
- **기사 한 건만 복사** [Added: 2026-08-10]: 보고할 기사가 1건뿐일 때를 위해 `render_article()`(완성본·초안·지난 기사 공유)에 📋 버튼을 추가했다 — 이미 있는 🔄/✏️와 같은 자리·같은 숨김 방식(`.refetch-btn` 클래스 재사용, 평소엔 안 보이다 카드에 마우스를 올리면 나타남)이라 아이콘이 더 늘어난 것처럼 안 보인다. 복사 본문은 전체 복사(PLAIN_TEXT)와 같은 줄 형식("ㅇ (언론사) 제목" + URL, `apply_line_template` 재사용)이라 붙여넣었을 때 서식이 어긋나지 않는다. `copyArticleText(btn)` JS가 버튼의 `data-copy-text`를 그대로 클립보드에 쓰고 성공하면 아이콘을 잠깐 ✅로 바꾼다(alert 없이 조용한 피드백) — JS는 공유가 안 되므로 `app/renderer.py`·`app/preview_renderer.py`·`app/history_renderer.py` 세 곳에 각각 추가했다. 실시간 현황(`app/live_renderer.py`)은 자체 행 마크업이 달라 이번엔 포함하지 않았다.
- **기사 행 "⋯ 더보기" 메뉴** [Added: 2026-08-11]: 제목 줄에 붙어 있던 📋(복사)·🔄(원문 다시 가져오기)·✏️(직접 수정) 세 아이콘을 `⋯` 하나로 접고, 그 안에서 **글자 라벨**로 보여준다 — `복사하기` / `원문 다시 불러오기` / `기사제목 직접 수정`. `app/renderer.py` `render_article`(확정본·초안·지난 기사 3화면 공용)의 `more_menu_html`, CSS·JS는 공유가 안 돼 `renderer.py`/`preview_renderer.py`/`history_renderer.py` 세 곳에 각각 넣었다(실시간 현황은 행 마크업이 달라 이번엔 제외 — 사용자 결정). **바꾼 이유 두 가지**: (1) 세 아이콘이 `.refetch-btn { opacity: 0 }` → `.article:hover` 로만 나타나서, 목록을 훑을 때 행마다 컨트롤이 4개↔8개로 요동쳐 산만했다. (2) 더 결정적으로 **모바일엔 호버가 없어 이 셋을 아예 누를 수 없었다**(이 앱은 폰에서도 본다 — 모바일 사파리 제목색 이슈를 따로 고친 적이 있을 정도). 그래서 `⋯`는 호버와 무관하게 **항상** 보이고, 메뉴 안 라벨이 `title` 툴팁을 대신한다(툴팁 역시 마우스를 올려야만 보이고 모바일에선 영영 안 보였다). **🗑️·↑·↓는 메뉴에 넣지 않고 밖에 그대로 뒀다** — 판단 기준은 "메뉴에는 한 번 누르면 끝나는 동작만"이다. ↑↓는 원하는 자리까지 여러 번 눌러야 해서 메뉴에 넣으면 열고-누르고-닫히고를 반복하게 되고, 🗑️는 한 번이면 끝나지만 큐레이션에서 가장 빈번한 동작이라 한 번에 닿아야 한다. 행 최종 순서: `[체크박스] 제목 … [다른 소제목 ▾] [⋯] [🗑️] [↑] [↓]`. 메뉴 동작은 🖍️ 형광펜 팝오버와 같은 패턴(재클릭 닫힘 + 바깥 클릭 닫힘)에 "한 번에 하나만 열림"을 더했다(`closeArticleMenus()`/`toggleArticleMenu()`). **`copyArticleText`도 같이 고쳤다** — 예전엔 버튼 글자를 잠깐 `✅`로 바꿔 복사 완료를 알렸는데, 메뉴 안으로 들어가면서 메뉴를 먼저 닫으면 그 표시를 볼 수 없게 됐다. 이제 라벨을 `복사했습니다`로 900ms 보여준 뒤 메뉴를 닫는다. 실측 확인: `⋯`를 눌러도 기사 `<details>`가 펼쳐지지 않고(`event.stopPropagation()`), 다른 기사의 `⋯`를 누르면 이전 메뉴가 자동으로 닫힌다.
- **상단/하단 내비게이션 이모지 제거 + "완성본"→"확정본" 명칭 통일** [Changed: 2026-08-10]: 각 화면 topbar의 "🔴 실시간 현황"/"📝 스크랩 초안"/"📗 스크랩 완성본" 링크에서 이모지를 뺐다(`app/renderer.py`, `app/preview_renderer.py`, `app/live_renderer.py`, `app/settings_server.py`의 `_HIDDEN_TOP_BAR_HTML`) — 페이지 본문 안 아이콘(🗑️·🔄·✏️ 등)이나 랜딩 화면의 흐름 다이어그램 아이콘(🔴⛏️💎 등)은 "상단/하단 바"가 아니므로 그대로 뒀다. 동시에 "완성본"이라는 명칭을 사용자 화면에 보이는 곳 전부 "확정본"으로 바꿨다(위 topbar 링크, `app/landing_renderer.py` 홈 화면 흐름 라벨, 텔레그램·이메일 설정 화면 안내문 — 이 안내문은 이미 없어진 초안의 개별 Telegram/Email 버튼을 언급하고 있던 것도 함께 바로잡았다). 코드 주석·docstring 속 "완성본"은 굳이 다 바꾸지 않았다(사용자에게 안 보이는 부분). [되돌림: 2026-08-10] "실시간 현황" 링크에는 🔴만 다시 붙였다(사용자 요청 — 나머지 "스크랩 초안"/"스크랩 확정본"은 계속 이모지 없이 유지) — `app/renderer.py`(확정본 topbar)와 `app/preview_renderer.py`(초안 topbar) 두 곳. `app/live_renderer.py`의 topbar는 애초에 "실시간 현황" 자기 자신으로의 링크가 없어서(이미 그 화면이므로) 해당 없음.
- **☰(소제목 목차) 버튼 색 톤다운** [Changed: 2026-08-10]: 원래 accent(파랑) 배경이라 (발송) 버튼과 색이 같아 헷갈린다는 피드백 — `app/renderer.py`/`app/preview_renderer.py`의 `.toc-toggle-btn`을 카드 배경 + 옅은 테두리의 중립색으로 바꿨다(호버는 accent 대신 hover 색).
- **[버그, 발견 즉시 수정: 2026-08-10] `load_latest_confirmed_run()` 도입 계기**: `generate_screen()`이 원래 `load_latest_run()`(가장 최근 "수집된" 회차, 확정 여부 무관)을 썼는데, 그 회차가 아직 `confirmed=False`인 순간에 앱이 재시작되면 "확정된 최신 회차가 없다"는 RuntimeError를 던져 `main.py`가 이를 안내 화면(🐰⏱️)으로 잘못 처리했다 — 실제로는 그 이전에 이미 확정된 회차(예: 11시)가 멀쩡히 있는데도 "아직 스크랩 시간 전입니다"로 덮어써지는 실사고가 발생했다(14시 회차 처리 중 앱 재시작). 위 전송 유예 절에서 언급한 `load_latest_confirmed_run()`이 이때 추가된 것이다.
- **초안 회차 종료 카운트다운** [Added: 2026-08-11]: 확정본의 "⏱️ MM:SS 후 자동 발송"처럼, 초안(`preview.html`)에도 "이번 회차가 언제 정식 수집으로 넘어가는지" 참고용 카운트다운을 추가했다 — `app/preview_renderer.py` `.round-countdown` span(☰ 소제목 목차 버튼 바로 위, `bottom:74px`), 회색(`{muted}`)으로 발송 카운트다운(빨강, 경고성)과 맥락을 구분했다. `_round_deadline_ms(slot_end)`가 `app.naver_api.kst_today_at(slot_end)`로 오늘 KST 기준 회차 종료 시각을 밀리초로 계산해 `ROUND_DEADLINE_MS`로 JS에 넘기고, `updateRoundCountdown()`이 매초 다시 계산해 표시한다. 확정본 카운트다운은 유예가 `CONFIRM_SEND_GRACE_SEC`(기본 5분)라 분:초로 충분하지만, 회차는 몇 시간짜리일 수 있어(예: 00:00~09:00) **시:분** 단위로 표시한다(사용자 요청) — "⏱️ 이번 회차 종료 시간까지 00시간 00분 남음". 순수 표시용이라 실제 정식 수집 전환은 여느 카운트다운과 마찬가지로 `app/scheduler.py` tick이 수행하며, 이 탭이 닫혀 있어도 그대로 일어난다(그래서 확정본과 달리 0에 도달했을 때 보여줄 별도 완료 문구도 필요 없다고 판단 — AI가 알아서 다음 화면으로 넘겨준다는 걸 사용자가 이미 알고 있음). `render_preview_page`/`render_preview_error_page`/`preview_move_article`(진행 중인 회차가 있는 경로) 세 곳은 `_round_deadline_ms(slot["end"])`를, 오늘 남은 회차가 없는 `render_preview_done_page`는 `0`을 넘겨 카운트다운을 아예 표시하지 않는다. [삭제: 2026-08-11] 위 카운트다운을 추가하자마자 기존 "⏰ 아직 정식 회차가 아니에요 — HH:MM에 자동으로 정식 수집됩니다..." 안내문(`.preview-hint`)이 같은 정보의 중복이 됐다는 사용자 판단으로 삭제 — `_compute_preview_content`가 이제 `hint = ""`을 돌려주고, `.preview-hint:empty { display: none; }` CSS가 빈 박스(배경·패딩·여백)까지 통째로 접어 시각적 공백이 남지 않게 했다. 오늘 회차가 모두 끝났을 때/검색 오류일 때의 안내문(`render_preview_done_page`/`render_preview_error_page`)은 이 카운트다운과 무관한 별개 정보라 그대로 남아 있다.
- **Telegram multi-recipient** [Changed: 2026-08-07]: Telegram send was originally a single fixed `TELEGRAM_CHAT_ID` from `.env`. Rebuilt to match Email's shape exactly — `app/telegram_recipients.py` (`load_telegram_recipients`/`save_telegram_recipients`/`active_recipient_chat_ids`) stores `data/telegram_recipients.json` as `[{"name", "chat_id", "enabled"}, ...]`, and `app/telegram_bot.py` `send_text(text, chat_ids)` now takes a list and sends one message per recipient in a loop (same per-recipient exception isolation as Email). Between the two toggle-vs-add/delete mockups shown earlier, the user picked the toggle design (same reasoning as Email: swap who's active for a vacation without deleting/re-adding). `is_configured()` now only checks `TELEGRAM_BOT_TOKEN` — the chat ID requirement moved to "at least one active recipient" (`active_recipient_chat_ids()`), checked at send time instead. **Migration**: `load_telegram_recipients()` seeds itself once from the old `.env` `TELEGRAM_CHAT_ID` (named `"나"`) the first time it's called and the JSON file doesn't exist yet — so upgrading doesn't silently drop a user's already-working single-recipient setup; after that first read the JSON file is the sole source of truth and `.env`'s `TELEGRAM_CHAT_ID` is never consulted again. The `/telegram` settings screen also gained a static how-to box explaining chat ID discovery (message the bot once, then check `https://api.telegram.org/bot<token>/getUpdates` for `chat.id`) — mirrors the same explanation given in chat, now visible in-app so it doesn't have to be re-explained.
- **Action toolbar redesign** [Updated: 2026-08-03]: the flat solid-blue `button, a.btn` look was too uniform across unrelated actions, so `index.html`/`preview.html`'s top action row (복사/txt로 저장/Telegram 전송/지난 기사) now uses a softer tonal fill (`.actions button, .actions a.btn` — bg `#EFF6FF`, text `{accent}`, hover `#DCEAFE`) that's more specific than the base rule and so overrides it only inside `.actions`, leaving 🗑️/↑/↓/dropdowns elsewhere untouched. "+ 새 소제목 만들기" is the one exception within that row — being a create action rather than an export/send action, it keeps a distinct ghost-outline style (`.actions .create-group-btn` — transparent bg, `#C7D9F7` border, hover `#EFF6FF`/`#A9C6F5`). Button labels were also clarified: "내보내기" → "txt로 저장", "📤 텔레로 보내기" → "📤 Telegram 전송". The `index.html` action row's "새로고침" link was removed as redundant (the page already auto-refreshes every 60s, see below). [Removed: 2026-08-05] `preview.html`'s own separate `.refresh-btn` "🔄 새로고침" (next to the `<h1>`, outside `.actions`) was removed too — despite that screen having no auto-refresh, per feedback the browser's own refresh works fine and a dedicated button was one more thing cluttering the header.
- Main screen (`index.html`) auto-refreshes every 60s via `<meta http-equiv="refresh">` (no JS needed for this).
- [Added: 2026-08-05] "+ 직접 키워드 작성하기" (완성본·초안 action toolbar) opens a free-text box ("키워드 작성 : ...") completely separate from the AI-extracted block below — a single global note (`data/manual_keyword_note.json`, `app/manual_keyword_note.py`) the user types by hand, e.g. "키워드1, 키워드2 .... 특이사항 없음". If non-empty it's shown open by default and, critically, appears as a `- {note}` line right under the header in copy/txt/Telegram text (`app/renderer.py` `_build_plain_text`, `app/preview_renderer.py` `_build_preview_plain_text`) — unlike the AI keyword block, which is reference-only and export-excluded. Being one global value (not scoped per-run), a note written in 초안 carries forward into 완성본 automatically once that round completes; saving/clearing (`/save-manual-keyword-note`) reloads the page since the plain-text export is baked in at render time. [Fixed: 2026-08-07] 긴 기사 목록을 스크롤하며 메모를 쓰려면 계속 위로 되돌아가야 했던 문제 — `.keyword-note-zone`에 `position: sticky; top: 60px`를 줘서 상단 바 바로 아래 계속 보이게 했다. `preview.html`은 이미 `.container` 바로 아래(기사 목록과 같은 부모) 구조라 CSS만 추가하면 됐지만, `index.html`은 이 칸이 `<header>`(제목+툴바만 감싸는 짧은 블록) 안에 갇혀 있어 헤더 높이를 넘겨 스크롤하면 sticky가 먹히지 않았다 — `</header>` 밖, 기사 목록과 같은 `.container` 자식으로 옮겨서 고쳤다(sticky는 자신을 감싼 부모 높이만큼만 붙어있을 수 있다). [Fixed: 2026-08-07] 회차마다 키워드가 다른데 메모가 전역 값 하나뿐이라, 예전 회차에 남긴 메모가 다음 회차 초안에도 그대로 남아있는 문제가 있었다. `app/manual_keyword_note.py`가 이제 메모를 `(날짜, run_slot)`과 함께 저장하고, 조회 시 그 키가 "지금 회차"와 다르면 빈 문자열로 취급한다(파일은 안 지우는 lazy reset). "지금 회차"는 초안처럼 회차를 명시하지 않고 부르면 `app.scheduler.next_pending_slot`(아직 안 끝난 회차) 우선, 없으면 가장 최근 저장된 회차로 자동 판단하고, 완성본·`app/export.py`(media_report 전달용)처럼 특정 회차를 보여줄 땐 그 회차의 키를 명시로 넘긴다 — 완성본은 이미 끝난 회차를 보여주므로 자동판단에 맡기면 그새 시작된 다음 회차로 잘못 붙기 때문이다. 완성본에서 저장할 때는 `index.html`의 `#keyword-note-zone`에 심어둔 `data-run-slot`을 저장 요청에 실어 보내 이 회차 구분을 명시한다. (`app.manual_keyword_note`가 `app.scheduler`를 모듈 최상단에서 import하면 scheduler→renderer→manual_keyword_note 순환 임포트가 생겨, 함수 안에서 지연 임포트했다.)

Bottom-of-page summary block (once per run, across all results, in this order): `🤖 AI가 추출한 주요 키워드` (comma-separated list, no `#` tags) then `💬 AI가 읽은 소제목별 주요 요약` (per sub-heading, ≤3 sentences / ≤150 chars).
- Export: copy-to-clipboard or `.txt` download of the sub-heading-grouped list (curation buttons and icon glyphs are excluded from the copied/exported text).
- **Tokenizer bound-noun filter** [Extended: 2026-08-11]: `app/tokenizer.py` `_BOUND_NOUN_PATTERN` used to only strip "것"(+optional josa) — extended to a tuple of meaning-light bound nouns (`_BOUND_NOUNS = ("것", "등", "때", "뿐", "만큼", "대로", "채", "탓")`, each optionally followed by a `_JOSA` suffix), fixing wordcloud entries like "등을"/"등에"/"등이" that survived because `_strip_suffix`'s safety guard (`len(word) > len(suffix) + 1`, protects against over-stripping short real words) blocks stripping the josa off a 2-char token like "등을" down to 1 char — so instead of relying on suffix-stripping, the whole bound-noun-plus-josa token is now matched and dropped as a unit. Validated against 1,096 stored real articles: 18 distinct newly-filtered tokens, 146 total occurrences, all confirmed to be josa residue (등을/등에/등이/등으로/만큼/탓에/…), zero legitimate words affected. Candidates like "수"/"중" were deliberately excluded from `_BOUND_NOUNS` since they collide with real 2-char nouns ("수도", "중도").
- **Landing page word cloud** (`home.html`, `app/landing_renderer.py`): [Updated: 2026-07-24] aggregates keywords across *all* of today's saved runs (url-deduped), not just the latest run — separate pipeline from article scraping, reads already-stored data only. Regenerated on every request (`app/settings_server.py` `/home.html` route calls `generate_landing_page()` before serving, not just at scrape time). Shows a "* 당일 0시 이후 현재까지의 누적 기준" caption (normal weight, small). [Updated: 2026-07-25] Flat flex-wrap tag cloud (no more fixed radial layout). Search keywords (e.g. a registered person's name) are included in the frequency count here — unlike subheading classification / the article-list "🤖" keyword block, which still exclude them — and rendered in a distinct muted-gray color so they read as "shows up because it's a search term" rather than genuine trending words. [Added: 2026-08-07] 랜딩 화면 제목을 "AI 기반 뉴스 모니터링 및 언론동향 보고"로 바꾸고, 기존 스크랩 흐름 아래 점선 구분선을 두고 신규 "동향 보고" 3단계 흐름(🗂️ AI 분석 → 📝 동향 초안 → 📑 동향 확정본)을 UI만 먼저 얹었다 — 백엔드(자료 취합·AI 분석·보고서 생성)는 별도 프로젝트("media_report", `app/export.py`가 완성본을 그 프로젝트로 넘기는 JSON 계약을 이미 갖고 있다)에서 나중에 구현 예정이라, 이 3칸은 `<a href>`가 아닌 `<div class="box">`(클릭 안 됨) + "준비 중" 배지로만 표시한다. 같은 김에 기존 스크랩 흐름의 아이콘도 초안 📝→⛏️, 완성본 📗→💎로 바꿨다(동향 보고 흐름의 📝를 스크랩 초안과 겹치지 않게 하기 위한 재배치, 사용자 선택). `.flow-a a, .flow-a .box`로 베이스 레이아웃(flex-column·패딩·둥근모서리)을 공유하되, 배경/글자색은 `.material`(회색, AI 분석)과 `.brief`(보라, 동향 초안·확정본)로 스크랩 흐름의 빨강/파랑/남색과 겹치지 않게 분리했다.
- Data retention: delete stored articles after 7 days.
- **`data/export/` 보관 기간 추가** [Added: 2026-08-12]: 앱 파일 다이어트 점검 중 발견 — `data/articles/`(수집 원본)는 `delete_expired_runs`로 7일 지나면 자동 정리되는데, `data/export/`(media_report로 넘기는 JSON)는 그런 정리 로직이 아예 없어서 매 회차·매 전송마다 무기한 쌓이고 있었다(발견 시점 43개, 8/7~8/12분). `app/export.py` `delete_expired_exports()`를 `delete_expired_runs`와 완전히 같은 방식(같은 `RETENTION_DAYS`=7, 같은 파일-훼손 방어: 파싱 실패한 파일은 판단 불가로 보고 안 건드림)으로 추가하고, 나이 판단 기준은 `run_at` 대신 각 export 파일이 이미 갖고 있는 `exported_at`을 썼다. 호출 위치도 `delete_expired_runs`와 동일하게 두 곳 — `main.py` 시작 시 1회, `app/scheduler.py` `run_scheduler`의 매 tick(`cleanup_exports` 파라미터, 테스트 주입 가능하도록 `cleanup`과 같은 패턴). 격리된 임시 폴더에 오래된/최근/깨진 파일을 섞어놓고 돌려 "오래된 것만 지워지고 최근·깨진 파일은 안전하게 남는다"를 직접 확인했다.
- **"동향" 플로팅 버튼 제거** [Removed: 2026-08-12]: 실사용자 관점 점검 중 발견 — 확정본의 (발송) 버튼 바로 아래 "동향" 버튼이 있었는데, 백엔드가 아직 없어(별도 프로젝트 예정) 눌러도 `showToast('동향 AI 분석 시작했습니다.')`만 뜨고 실제로는 아무 일도 안 했다. 홈 화면의 "동향 보고" 3단계 흐름은 같은 미구현 상태를 작은 "준비 중" 배지로 정직하게 표시하는데, 이 버튼은 (발송)과 똑같은 크기·색 대비의 원형 플로팅 버튼이라 실제 기능처럼 보였다(사용자 확인). `app/renderer.py`에서 버튼과 `.confirm-send-fab.trend` CSS를 통째로 제거하고, (발송)을 `bottom:150px`에서 기본값 `bottom:80px`로, 카운트다운을 `bottom:216px`에서 `bottom:146px`로 내려 빈 자리를 메웠다(초안 화면엔 애초에 이 버튼이 없었다 — 발송 자체가 확정본 전용이라서).
- **AI 소제목 분류 — 애매하면 "기타"로 주저 없이** [Changed: 2026-08-12]: `app/llm_classifier.py` `_SYSTEM_PROMPT`가 원래 "모든 기사는 정확히 한 묶음에만 들어가야 하며 빠뜨리는 기사가 없어야 한다"만 강조하고 "'기타'는 필요할 때만 만든다"고 덧붙여서, 모델이 애매한 기사를 억지로 가장 비슷해 보이는 소제목에 끼워 넣는 경향이 있었다 — 실사용 점검 중 실제 사례로 확인: MBC 정치 토크쇼 기사(이재명 대통령이 구윤철 부총리에게 농담한 것, 김민석·정청래 지지율 공방)가 "청년 고용 현황과 정책" 소제목에 들어가 있었고, 하단 AI 요약도 그 엉뚱한 내용을 그대로 요약했다. 규칙 문장을 "애매하면 '기타'가 맞다 — 억지로 가장 비슷해 보이는 묶음에 끼워 넣지 마라"로 명시적으로 뒤집었다(사용자 판단 — 애매한 기사는 담당자가 어차피 직접 숨기거나 "기타"로 옮기므로, 억지로 분류하려는 시도 자체가 낭비이자 오분류 위험이다). 근본 원인은 분류 프롬프트가 아니라 검색 키워드가 너무 넓어서(예: "정부"라는 한 단어 키워드가 무관한 정치 기사까지 끌고 옴)일 수도 있다는 점은 별도로 남겨둠 — 실시간 현황의 검색어 필터(계획 중, 아래 참고)로 어떤 키워드가 원인인지 진단할 수 있게 될 예정.
- **소제목 요약을 규칙 기반 발췌 대신 AI가 직접 쓰게 함 + 250자로 확대** [Changed: 2026-08-12]: 예전엔 `app/summarizer.py`가 그 소제목의 **첫 기사 하나**의 네이버 요약문만 가져와 150자로 잘랐다 — 화면 문구는 "🤖 AI가 읽은 소제목별 주요 요약"인데 실제로는 AI가 관여하지 않았고("AI가 읽은"이 거짓 라벨이었다), 첫 기사가 소제목 전체를 대표하지 못하는 경우도 흔했다(위 MBC 사례처럼 첫 기사가 엉뚱하면 요약도 엉뚱해짐). `app/llm_classifier.py`가 분류와 **같은 API 호출**에서 각 소제목의 요약도 함께 쓰도록 `_SCHEMA`에 `summary` 필드를 추가하고(3문장·250자 이내, 완결된 문장으로 끝맺을 것, "기타"는 빈 문자열), `app/summarizer.py` `summarize_group()`이 `group["summary"]`가 있으면 그걸 우선 쓰도록 바꿨다 — 그제야 화면 문구가 사실이 된다. 규칙 기반 폴백(API 실패·미설정 시)은 `_summarize_group_from_first_article()`로 이름을 명확히 하고 그대로 남겨 안전망 역할을 유지했다. 글자 수는 150 → 250(`app/config.py` `SUMMARY_MAX_CHARS`)로 늘렸다 — AI가 진짜로 소제목 전체를 요약하게 되면 150자로는 자주 부족해서.
  - **함정 1: `app/export.py`의 `rule_based_summaries` 계약이 깨질 뻔했다.** 이 필드는 원래 media_report(별도 프로젝트)에게 "이건 값싼 참고용 힌트일 뿐, 너희 쪽 LLM 요약을 대체하지 않는다"고 이름으로 약속한 자리인데, `summarize_group`이 LLM 요약을 기본으로 쓰게 바뀌면 이 필드도 몰래 AI 요약을 흘려보내 이름과 내용이 어긋나게 된다. `summarize_group`/`summarize_groups`에 `use_llm_summary` 파라미터를 추가해 `app/export.py`만 명시적으로 `False`를 넘기도록 분리했다 — 코드 리뷰 중 발견해 구현과 함께 고쳤다(실사용 사고로 이어지기 전에 잡음).
  - **함정 2: 확정본은 재분류를 안 하므로(위 "확정본 재분류 차단" 참고) 요약도 스냅샷돼야 한다.** LLM이 요약을 만들어도 확정본이 그걸 볼 방법이 없었다(화면을 열 때마다 재호출하지 않으므로). `app/classifier.py` `snapshot_group_names()`가 "group"과 같은 이유로 "group_summary"도 각 기사에 붙이고(그룹의 모든 기사에 동일한 문자열이 찍힘 — "group" 필드와 같은 중복 방식), `groups_from_snapshot()`이 그룹의 첫 기사에서 이 값을 읽어 복원한다.
  - **문장 중간에 안 끊기게(안전망).** `app/summarizer.py` `_trim_summary()`를 "문장을 이어붙인 뒤 글자 수로 자르기"에서 "문장 경계 안에서만 자르기"로 바꿨다 — 실사용 중 "…서울 중구." "…'생산적금융 ISA' 보유." "고 회장은." 같이 문장 중간에 뚝 끊긴 요약을 실제로 확인했다. 다음 문장을 더하면 250자를 넘는 시점에서 멈추고, 그때까지 담은 문장은 전부 온전하다. 첫 문장 하나가 이미 250자를 넘는 극단적인 경우만 마지막 수단으로 단어 경계에서 자른다. **구현 중 실제로 버그를 하나 잡았다**: 첫 루프 조건이 `if kept and length + extra > SUMMARY_MAX_CHARS`였는데, `kept`가 비어있는 첫 문장 차례에는 이 조건이 항상 거짓이 되어(길이 검사 자체가 스킵됨) 아무리 긴 첫 문장도 무조건 통과해버렸다 — 아래 단어-경계 폴백이 죽은 코드가 되는 버그. 300자짜리 단일 문장으로 직접 재현해 발견하고, `kept` 유무와 무관하게 항상 길이를 검사하도록 고친 뒤 재검증했다.
- **다운로드를 `data:` URI에서 서버 `Content-Disposition` 응답으로 전환** [Changed: 2026-08-12]: 실사용자 관점 점검에서 확정본·초안·지난 기사의 "다운로드" 버튼이 전부 `<a href="data:text/plain;charset=utf-8,{percent-encoded 전체 본문}">`이었던 게 드러났다 — 한글은 UTF-8 한 글자(3바이트)가 percent-encoding되면 `%XX%XX%XX`(9글자)로 3배 가까이 부풀어서, 회차마다 다운로드 링크가 하나씩 있는 `history.html`(43개)에서만 그 인코딩된 문자열이 389KB(전체 3.4MB의 13%)를 차지했다(실측). `app/settings_server.py`에 `POST /download-text`(`_handle_download_text`/`_respond_download`)를 새로 만들어 `Content-Type: text/plain`·`Content-Disposition: attachment`(한글 파일명은 `filename*=UTF-8''...`, RFC 5987)로 응답하는 표준 다운로드로 바꿨다. 확정본·초안은 실제 `<form>`을 두고 제출 직전(`onsubmit`)에 화면의 `PLAIN_TEXT`(복사·텔레그램 전송과 이미 공유하는 같은 값)를 히든 인풋에 채워 넣어, 다운로드 내용이 화면과 항상 일치하도록 유지했다 — 지난 기사는 읽기 전용이라 렌더링 시점 텍스트를 히든 인풋에 바로 심는다. 세 파일(`app/renderer.py`/`app/preview_renderer.py`/`app/history_renderer.py`) 모두 이제 `urllib.parse.quote` import가 필요 없어져 함께 제거했다.
- **`history.html` 용량 다이어트 — 오래된 날짜는 펼칠 때만 로드** [Changed: 2026-08-12]: 실측 3.4MB(회차 42~43개, 기사 카드 1,037개)로 가장 무거운 화면이었다 — 대부분의 조회가 최근 날짜에 몰리는데도 7일 전부를 매번 통째로 내려받고 있었다. `app/config.py` `EAGER_HISTORY_DAYS`(2) 도입 — 최근 2일치(오늘·어제)만 슬롯 내용을 즉시 렌더링하고, 그보다 오래된 날짜는 `<summary>`(날짜 라벨)만 있는 빈 `<details>` 껍데기로 렌더링한다(`data-date` 속성 + `ontoggle="loadHistoryDay(this)"`). 실제로 펼칠 때만 JS가 `GET /history/day?date=YYYY-MM-DD`를 fetch하고(`app/settings_server.py` `_handle_history_day` → `app/history_renderer.py` `render_history_day_fragment`, 즉시 렌더링 경로와 **완전히 같은 함수**(`_render_day_slots`)를 써서 나중에 펼쳐도 처음부터 펼쳐진 날짜와 100% 같은 모양이 나온다), 받아온 HTML로 빈 껍데기를 통째로 교체한다. `holder`가 없어지면(이미 로드됨) `ontoggle`이 다시 불려도 아무 일도 안 하므로 같은 날짜를 여러 번 여닫아도 한 번만 요청한다. 실측: 3,378,557 bytes → 700,774 bytes (79% 감소, 오늘 기준 8일치 데이터). 브라우저로 직접 열어 오래된 날짜를 펼쳤을 때 네트워크 요청이 정확히 한 번 나가고 내용이 채워지는 것, 이미 펼쳐진 날짜(오늘·어제)는 요청 없이 즉시 열리는 것을 확인했다.
- **실시간 현황 — 그룹 필터 칩 + 키워드별 건수 드릴다운** [Added: 2026-08-13]: 실사용자 관점 점검(5-1/5-2 논의)에서 나온 요청 — 검색 키워드가 20~30개까지 늘어날 수 있는데 필터가 "선택 언론사만 보기" 하나뿐이라, 어떤 키워드가 결과를 홍수내고 있는지 알 방법이 없었다(그 진단이 곧 AI 오분류 원인 추적의 전제이기도 했다 — 위 "AI 소제목 분류 — 애매하면 기타로" 항목 참고). 구현 배경:
  - **키워드 매칭 정보가 이미 계산되고 있었는데 버려지고 있었다.** `app/naver_api.py` `search_articles_by_groups`는 `results_by_keyword`(키워드별 검색 결과)를 그룹 배정 후 곧바로 버렸다. `track_keyword_matches=True`(기본 False, 정기 스크랩엔 영향 없음) 옵션을 추가해, URL별로 어떤 키워드의 검색 결과에 있었는지 `matched_keywords` 필드로 기사에 붙인다 — 새 검색을 만드는 게 아니라 이미 계산해둔 걸 버리지 않는 것뿐이라 API 호출 비용 증가는 0이다. `app/live_renderer.py` `_matched_groups_for()`가 이 키워드들을 그룹 이름으로 되돌려 매핑한다(그룹의 keywords 중 하나라도 matched_keywords에 있으면 그 그룹에 속함 — 한 기사가 여러 그룹에 동시에 속할 수 있다, 예: 부동산 세제 기사가 "재정경제부 관련"과 "현안" 양쪽 키워드에 다 걸림).
  - **그룹 칩은 OR, 기본은 CSS로만 자르고 저장값은 그대로.** 처음엔 "체크박스 여러 개 = AND"로 생각했으나(사용자 초안 의견), 관행적으로 체크박스 다중 선택은 OR로 읽힌다는 점을 짚어 OR로 확정 — AND면 칩을 하나만 더 켜도 거의 항상 0건이 되어 "고장난 것처럼" 보인다. 좁히고 싶을 땐 제목 검색을 같이 쓰면 된다. 그룹 이름 길이 제한도 논의했으나(가안: 7자) 강제 검증 대신 **칩에서만 CSS `text-overflow: ellipsis`로 말줄임**하기로 확정(사용자 선택) — 저장을 막으면 "왜 안 되지"가 하나 더 생기고, 화면에서만 자르면 그 문제가 아예 없다.
  - **키워드별 건수 드릴다운은 원래 설계(칩마다 펼치기)보다 단순화했다.** 그룹 칩 최대 6개 각각에 개별 펼침 팝오버를 다는 건 JS 복잡도가 크고, 진단이라는 목적("어떤 키워드가 홍수내고 있나")에는 그룹 구분 없이 전체 키워드를 건수 내림차순으로 한 번에 보여주는 공유 패널 하나("키워드별 건수 ▾")로도 충분하다고 판단해 단순화했다. 실사용 확인: 캐시를 비우고(요약 필드처럼 이 필드도 없던 예전 캐시가 섞여 있으면 집계가 축소돼 보임 — 자정 리셋으로 자연히 해소) 다시 검색하니 "재정경제부 관련 · 재정경제부 316" "재정경제부 관련 · 구윤철 225" 순으로 정확히 잡혔다.
  - **"아직 안 담은 것만"은 담당자가 실제로 하는 일에 맞춘 필터다.** 각 행은 이미 숨김 > 스크랩됨 > 담아둠 > 자동 선별(추정) > 평범(`data-status`) 우선순위로 상태가 갈리는데, 담당자가 이 화면에서 실제로 찾는 건 "🤖가 안 잡았고 아직 아무 상태도 아닌" 기사들이다 — 그 상태(`normal`)만 남기는 칩 하나로 나머지 5가지 필터를 다는 것보다 정확히 필요한 것 하나만 뒀다.
  - **더보기(⋯) 메뉴로 🔄/✏️ 통합, URL·제목 클릭은 그대로 유지.** 그룹 칩이 생기며 한 줄에 아이콘이 너무 많아진다는 지적으로 app.renderer/preview_renderer와 같은 "⋯ 더보기" 패턴(원문 다시 불러오기/제목·요약 직접 수정)으로 옮겼다. 처음엔 URL 줄까지 접어 "🔗 원문 보기"를 메뉴 안에 넣는 안을 제시했으나, "원문 보기는 접근이 빨라야 한다" · "제목 클릭은 요약 펼치기라 그 아래 URL이 필요하다"는 사용자 반박으로 URL 줄은 기존 그대로 뒀다 — 접근 속도가 중요한 동작(원문 이동)과 정리 목적으로 접어도 되는 동작(가끔 쓰는 수정 기능)을 구분한 것.
  - **캐시(`app/live_cache.py`)와의 상호작용.** `matched_keywords`는 일반 dict 필드라 캐시 저장/복원에 특별한 처리가 필요 없었다(캐시가 JSON을 그대로 왕복하므로) — 다만 이 필드가 생기기 전에 이미 캐시된 기사(오늘 앱을 껐다 켜기 전 검색분)는 이 필드가 없어 그룹 칩 집계에서 빠진다. 별도 마이그레이션 없이 자정 캐시 리셋으로 자연히 해소되도록 뒀다(하루 지나면 전부 새로 채워짐) — 실사용 확인 중에는 즉시 확인하려고 `data/live_cache.json`을 수동으로 한 번 비웠다.
- **LLM_COST_USAGE.md 신설** [Added: 2026-08-13]: "얼마나·언제 API 비용이 드는지 유지보수용으로 따로 보고 싶다"는 요청. AI_RULES.md(예정, 아직 미생성)와 성격이 달라 — 그건 "AI가 뭘 해야 하는가"의 규칙이고 이건 "그게 얼마나 드는가"의 모니터링 자료라 같은 파일에 안 넣고 분리했다. 실제 호출 지점 3곳(`classify_with_llm` 분류+요약, `assign_to_existing` 미분류 배정, `force=True` 강제 재분류)을 코드에서 직접 확인해 정리하고, 2026-08-11 실측치(분류만 하던 시점, ~3,184 입력/~141 출력 토큰)를 기준으로 요약 필드 추가 이후분은 "미실측"으로 명시했다 — 근거 없는 숫자를 실측인 것처럼 적지 않기 위해서다. `data/settings.json`의 실제 스케줄(주중 6슬롯/주말 3슬롯)을 대입해 "가만히 두면 하루 12회 안쪽"이라는 상한선을 계산해뒀다.
- **초안 회차 종료 카운트다운을 시:분 → hh:mm:ss로** [Changed: 2026-08-13]: 원래도 `setInterval(…, 1000)`으로 1초마다 다시 그리고 있었는데 표시는 분 단위뿐이라, 59초 동안 숫자가 멈춰 있어 화면이 죽은 것처럼 보였다(사용자 지적 — "이력서 제출할 때처럼 초 단위로 줄어드는 게 맞다"). 계산 비용은 그대로고 표시만 바뀌므로 초를 넣었고, 문구도 "이번 회차 종료 시간까지" → "수집 종료시간까지"로 줄여 폭을 확보했다. 다만 회차는 9시간짜리(00:00~09:00)일 수 있어 **초가 흐른다는 사실만으로는 임박 여부를 알 수 없다** — 담당자가 기사를 읽는 화면에서 계속 움직이는 숫자는 주변시야를 뺏기만 한다. 그래서 남은 시간 **10분 이하일 때만** `.urgent`(확정본 발송 카운트다운과 같은 빨강 `COLOR_ERROR`)로 톤을 올려, "초는 늘 보이되 급한지 여부는 색이 말하게" 했다. 숫자가 매초 바뀌므로 `font-variant-numeric: tabular-nums`로 자릿수 폭을 고정했다(안 하면 문구 전체가 초마다 미세하게 흔들린다). **0 도달 시 완료 문구는 일부러 두지 않았다** — 확정본 발송 카운트다운은 0에서 멈춰 있으면 "안 보내진 건가?" 혼란이 생겨 문구를 넣었지만(2026-08-11), 이쪽은 0이 되는 순간 회차가 끝나 화면 자체가 확정본으로 넘어가므로 그 문구를 볼 사람이 없다(사용자 확인). 확정본 발송 카운트다운(`MM:SS`)과 표기 체계가 `hh:mm:ss`로 통일되는 부수 효과도 있다.
- **확정본 — 담당자가 직접 (발송)한 흔적 남기기** [Added: 2026-08-13]: 자동 발송은 "🤖 N시 M분에 자동 발송 완료" 회색 배지가 플로팅 버튼 위에 남는데, 담당자가 직접 (발송)을 누른 경우엔 그 자리를 **일부러 비워두고 있었다**(2026-08-10 구현 당시의 판단 — "사람이 눌렀으면 본인이 아니까"). 실사용에서 뒤집혔다: 발송 직후 뜨는 토스트는 0.9초 뒤 `location.reload()`와 함께 사라져서, 화면을 다시 열면 보냈다는 증거가 **하나도 남지 않는다** — 기계가 한 일은 흔적이 남고 사람이 한 일은 안 남는 비대칭이고, "내가 보냈나?" 하고 다시 누르면 실제로 중복 발송된다. 같은 자리·같은 스타일로 `✔️ N시 M분 발송 완료`를 채웠다. 사람/기계 구분은 **색이 아니라 앞머리 이모지(✔️/🤖)**가 맡는다(사용자 판단) — 둘 다 "이미 끝난 일"이라 색까지 나눌 이유가 없어, CSS 클래스도 `.auto-sent` → `.sent-done` 하나로 합쳤다. 2회째부터는 `(N회)` 꼬리표가 붙어, 받는 사람에게 나가는 제목의 "(수정)" 접두어(`app/confirm_send.py`)와 화면이 짝을 이룬다. 시각 표기는 콜론 없는 한국어("13시 7분")를 유지했다 — 2026-08-10에 `HH:MM`이 카운트다운 `MM:SS`와 겹쳐 보인다는 피드백으로 바꾼 결정을 되돌리지 않기 위해서다(이제 카운트다운이 `hh:mm:ss`가 되어 더욱 그렇다). `.sent-done`의 `width`는 고정값(170px) 대신 `auto`로 뒀다 — `(2회)` 꼬리표가 붙으면 고정폭에선 잘리기 때문(fixed 요소 + `width:auto` = 내용 폭, `right:20px` 기준으로 왼쪽으로 늘어난다).
- **"새로 들어온 기사"(노랑) 표시를 확정본에도 + 기준선을 회차별로 분리** [Added/Changed: 2026-08-13]: 초안에만 있던 `.is-new-arrival`(옅은 노랑 `#FFF9C4`, "지난 로드 이후 새로 생긴 기사")을 확정본에도 들여왔다. 발단은 "확정본에 새 기사가 들어왔는데 아무 표시 없이 소제목 안에 다 섞여 있어 못 찾겠다"는 실사용 제보 — 확정본은 저장된 스냅샷이라 "새 기사가 생길 리 없다"고 착각하기 쉽지만, 실제로는 📌 담아둔 기사 승격(`_handle_promote_manual_article`)·초안에서 예약해둔 기사 합류(`app/draft_articles.py`)로 회차 도중에도 늘어난다. 기존 `.just-promoted`(노랑) 표시가 있긴 했지만 **승격을 누른 그 브라우저의 `sessionStorage` 기준으로 딱 한 번**만 떠서, 다른 탭에서 열거나 한 번만 새로고침해도 증발해 사실상 없는 것과 같았다.
  - **핵심은 기준선을 회차별로 나눈 것.** 초안의 원래 구현은 `localStorage["previewKnownUrls"]` 키 하나로 전역 비교였는데, 이걸 확정본에 그대로 옮기면 **회차가 바뀌는 순간 화면 전체가 노랗게 물든다**(회차가 바뀌면 기사가 통째로 갈리므로) — 전부 새 것이라는 표시는 아무 정보도 아니다. 확정본은 `confirmedKnownUrls:{날짜}:{회차}`를 키로 쓰고, 초안은 이미 format 인자로 있던 `ROUND_DEADLINE_MS`(회차 종료 시각 = (날짜, 회차)마다 유일)를 그대로 식별자로 재사용했다(`_PAGE_TEMPLATE.format` 호출부가 4곳이라 새 인자를 넣으면 전부 손봐야 하는데, 이미 모든 호출부에 있는 값이라 어긋날 수가 없다). 기준선이 없는 회차(처음 여는 회차)는 비교 대상이 없으므로 **아무것도 표시하지 않고** 이번 목록을 기준선으로만 저장한다 — 초안이 원래 쓰던 "첫 방문이면 표시 안 함" 규칙 그대로다. 회차마다 키가 하나씩 쌓이므로 로드할 때 자기 키를 뺀 나머지 같은 접두사 키를 정리한다(초안은 전역 방식 시절의 `previewKnownUrls`도 함께 지운다).
  - **노랑 vs 연보라의 축이 다르다.** 연보라(`.just-classified`)는 "🤖를 눌러서 방금 배정된 것" = 내가 시킨 일의 결과이고, 노랑은 "그냥 새로 왔다" = 배경 정보다(사용자 확인). 그래서 확정본의 🤖 전체 재분류에는 표시를 붙이지 않았다 — 전체가 움직이므로 전부 연보라가 되어 아무 의미가 없다. CSS 선언 순서는 `.is-new-arrival`을 `.just-promoted`/`.just-moved`보다 **먼저** 둬서 겹칠 때 그 둘이 이기게 했다(방금 내 조작의 결과가 더 급한 정보다). 체크 선택 배경은 `:has()` 특이도가 더 높아 순서와 무관하게 항상 이긴다.
- **소제목 순서 버튼을 ↑↓ → ▲▼로** [Changed: 2026-08-13]: 소제목 순서 버튼(`.order-btn`)과 기사 순서 버튼(`.move-btn`)이 글리프·색·크기까지 사실상 동일해서, **뭘 옮기는 버튼인지가 화면상 위치로만 구분**됐다. 반대로 같은 일(소제목 옮기기)을 하는 목차 팝오버 버튼(`.toc-order-btn`)은 이미 ▲▼여서, 정작 같은 대상을 다루는 두 버튼끼리 기호가 어긋나 있었다 — 이게 진짜 불일치였다(사용자 지적). ▲▼로 통일해 **삼각형 = 소제목 / 화살표 = 기사**로 대상이 형태만 보고 구분되게 하고, h2의 버튼과 목차 팝오버의 버튼이 같은 기호가 되게 했다. 초안·확정본 두 화면을 **동시에** 바꿨다(한쪽만 바꾸면 지금보다 나빠진다). ▲▼는 같은 크기에서 ↑↓보다 크고 무겁게 보여 `font-size`를 1rem → 0.78rem으로 낮춰 균형을 맞췄다. 후보로 ⌃⌄도 검토했으나 폰트별 렌더링 편차가 커서 배제했다.
- **/outlets 순서 버튼 — "맨 위"/"맨 아래" → "5개 ↑"/"5개 ↓"** [Changed: 2026-08-13]: 실사용 제보 — 언론사가 30~40개까지 선택되는데(실측 41개) 버튼 4개(맨 위/↑/↓/맨 아래)가 전역 버튼 스타일(파란 채움)을 그대로 물려받아, 목록 길이만큼 파란 버튼이 반복돼 "눈이 아프다"는 지적. 처음엔 화살표를 고스트로, 맨 위/맨 아래만 아웃라인으로 남기는 2단 위계안을 제시했으나, 사용자가 그 위계 자체가 필요 없어지는 대안을 제안했다 — "맨 위"/"맨 아래"(순간이동, 다른 종류의 동작이라 색으로 경고해야 했던 것) 대신 "N개 위로"/"N개 아래로"(↑/↓와 "같은 종류 동작, 배수만 다른 것")로 바꾸면 위계 자체가 사라진다는 아이디어. 목표 지점까지 남은 칸이 N보다 적으면 끝까지만 이동하도록 클램프하면, 이 버튼이 예전 "맨 위"/"맨 아래" 역할을 경계에서 자연히 겸해 별도 버튼이 필요 없다는 것도 사용자가 먼저 짚었다.
  - **N값 결정.** 실측 선택 언론사 41개 기준 "맨 아래→맨 위" 왕복 클릭 수를 표로 비교(1칸=40회, 3칸=14회, 5칸=8회, 기존 순간이동=1회) — 5칸이 8회로 3칸(14회)보다 충분히 적으면서도, 5칸(약 150px)이 여전히 한 화면 안에 들어와 "어디로 갔는지 눈으로 쫓을 수 있다"는 3칸의 장점을 그대로 가져간다고 판단해 5로 확정(`app/config.py` `OUTLET_ORDER_JUMP_STEP = 5`).
  - **라벨 표기.** 처음 "5칸 위로"로 시안을 냈으나, 사용자가 "'칸'은 안 어울린다, 화살표가 이미 방향을 나타내니 '5개 ↑'/'5개 ↓'가 낫다"고 반박 — 그대로 반영했다(버튼 텍스트 "5개 ↑"/"5개 ↓", `title` 속성은 "5개 위로"/"5개 아래로"로 자연스러운 문장형 유지).
  - **구현.** `app.settings.VALID_DIRECTIONS`를 `("up", "down", "top", "bottom")`에서 `("up", "down", "jumpup", "jumpdown")`으로 바꾸고 `move_outlet()`을 "up/down은 인접 스왑, top/bottom은 리스트 끝으로 이동"하던 세 갈래 분기에서 "목표 인덱스를 계산하고 `pop`+`insert`로 옮기되 범위를 클램프"하는 한 갈래로 통합했다(up/down도 결과적으로 인접 스왑과 동일). 이 재사용을 아무도 다른 곳에서 "top"/"bottom" 문자열로 호출하지 않는지 확인 후 이름을 바꿨다(테스트에 이 함수 호출이 없어 하위호환 이슈 없음). CSS는 `.order-row .order-btn`(색 하나, `{muted}`)와 `.order-row .order-btn.jump`(추가로 테두리 `{ghost_border}`, hover 시 `{ghost_border_hover}`)로 — 이 두 색은 새로 만들지 않고 `app.renderer`/`app.preview_renderer` 액션 툴바가 이미 쓰는 고스트 색을 그대로 재사용했다(팔레트를 안 늘려야 나중에 톤을 한 곳만 고치면 된다). "지금 옮기는 언론사"는 기사 순서 이동과 완전히 같은 색(`.order-row.just-moved`, `#E7EFE8`)으로 1.2초 표시 — "위아래 이동은 이 색 하나"로 화면 전체가 통일된다(사용자 요청). `scrollIntoView({block:"nearest"})`를 안전망으로 붙였다 — 5칸이 항상 화면 안에 들어오므로 평소엔 아무것도 안 하지만, 검색/필터로 목록이 좁혀진 예외적 상태를 대비했다(비용 0). `_OUTLET_ORDER_SCRIPT`는 `_OUTLETS_TEMPLATE`처럼 `.format()`을 거치지 않고 값으로만 삽입되는 문자열이라, 안의 `{jump_step}` 자리표시자는 `.format()`(JS 리터럴 중괄호를 전부 이중화해야 함) 대신 `.replace("{jump_step}", str(OUTLET_ORDER_JUMP_STEP))`으로 채웠다.
- **사진기사 추정 필터 — 촬영 날짜 도장 앵커를 "8자 꼬리 제한"에서 "한글 없으면 통과"로** [Fixed: 2026-08-13]: 실사용 제보 — "[초안] 사진기사 제외를 켰는데도 사진기사가 여러 개 들어온다"며 실제 사례(뉴시스 "구윤철 부총리 주재 비상경제본부회의" 계열)를 받고 재현. `data/articles/`·`data/live_cache.json`에 실제로 저장된 그 기사군을 찾아 `looks_like_photo_caption()`에 직접 태워보니, **사진 한 장이 뉴시스에서 제목만 5가지로 바뀌어 나가는데 그중 4가지가 전부 통과**하고 있었다 — 제목 패턴("~하는 + 직책"으로 끝남)은 1가지 제목형에만 걸리고, 나머지 4가지는 "주재"/"회의"로 끝나 안 걸림. 요약 쪽 신호 두 개도 같이 무력화된 게 근본 원인이었다: 실제 저장된 요약 꼬리가 `...회의를 주재하고 있다. 2026.08.13. chocrystal...`인데 (1) 사진기자 이메일 패턴(`\S+@\S+\.(com|co\.kr)`)은 네이버가 이메일을 `chocrystal...`처럼 `@` 앞에서 잘라버려서 매칭 자체가 불가능해졌고, (2) 촬영 날짜 도장 패턴은 도장 뒤 꼬리를 "공백/슬래시 + 최대 8자"로만 허용했는데 그 잘린 이메일 찌꺼기(`chocrystal... `, 14자)가 8자 제한을 넘겨 도장이 분명히 있는데도 통째로 놓쳤다. 제목 완전 중복 제거(`deduplicate_by_title`)도 5개가 전부 다른 문자열이라 못 막았다.
  - **고친 방법.** `_PHOTO_CAPTION_SUMMARY_PATTERNS`의 날짜 도장 정규식을 `20\d\d\.\s?\d{1,2}\.\s?\d{1,2}\.?(\s*/?\s*[\w가-힣]{0,8})?\s*$`(꼬리 길이 제한)에서 `20\d\d\.\s?\d{1,2}\.\s?\d{1,2}\.?[^가-힣]*$`(도장 뒤로 한글이 더 이상 없으면 매치, 길이 무관)로 앵커를 바꿨다 — 잘린 이메일 찌꺼기가 몇 자든 상관없어진다.
  - **검증은 실제 저장 데이터 전수로 했다.** `data/articles/*.json` + `data/live_cache.json`을 URL 기준으로 합쳐 1,429건(당시 기준) 확보 → 새 정규식으로 새로 걸리는 기사를 전수 나열해 오탐이 있는지 직접 읽었다(오탐 0건, 전부 그 뉴시스 사진). 회귀도 별도로 확인했다 — 옛 패턴으로만 잡히던 4건(연합뉴스/뉴스1 계열, 도장 뒤에 `/뉴스1` 같은 한글 매체 꼬리표가 붙어 새 패턴에서는 빠짐)이 있었지만, 그 4건 전부 다른 신호(재판매·DB 문구 패턴, 또는 제목 패턴 "~하는 구윤철·윤호중 장관")로 이미 잡히고 있어 `looks_like_photo_caption()` 함수 전체로는 회귀 0건. 적용 후 그 뉴시스 사진 14건 전부(발견 시점 기준) 잡히는 것을 재확인했다.
  - **알려진 한계(이번엔 손대지 않음).** 이 수정은 "요약에 날짜 도장이 남아있는" 사진기사만 고친다. 도장도 이메일도 없이 제목만 여러 형태로 바뀌는 사진기사는 여전히 전부 통과한다 — 그건 "제목이 다른 유사 기사를 묶어내는" 별개 문제라, 이번 실사용 사례가 요구한 범위(도장 패턴의 앵커 버그) 밖으로 남겨뒀다.
- **실시간 현황 — "아직 안 담은 것만" → "미선정 기사"** [Changed: 2026-08-13]: 사용자가 그룹 필터 칩 2개를 눌렀는데 화면상 1개만 활성화된 것처럼 보인다고 제보 — 실제로 앱을 띄워 클릭해보니 코드(`toggleGroupChip`은 클릭한 칩만 토글, OR 로직 그대로 동작)는 문제없었고, 재현되지 않아 클릭이 버튼 히트박스를 살짝 빗나간 것으로 결론. 이 과정에서 "아직 안 담은 것만"이 정확히 뭘 뜻하는지(자동 스크랩 완료 여부 아님 — 담아둠·자동 선별·숨김·스크랩됨 4가지 상태 중 아무것도 아직 안 된 `normal` 상태만 남기는 필터) 설명하다가, 라벨 자체가 애매하다는 논의로 이어졌다. 첫 대안 "미선별 기사만"은 화면에 이미 있는 "🤖 자동 선별" 배지와 단어("선별")가 겹쳐 "자동 선별 안 된 것만"으로 좁게 오독될 위험이 지적됐다(사용자 판단에 따라 기각). "선택받지 않은 기사"(사용자 제안, "AI도 사람도 안 가져간 기사"라는 뜻은 정확)는 다른 라벨들(담아둠/자동 선별/숨김)의 사무적인 톤과 어울리지 않는다는 우려로 보류. 최종적으로 "미선정 기사"로 확정 — "선별"과 단어가 겹치지 않고, 재정경제부 업무 맥락에서도 자연스러운 표현(사용자 최종 선택). 논의 중 "차라리 이 필터 자체를 빼는 게 낫냐"는 질문도 나왔으나, 하루 800건 이상 쌓이는 화면에서 "아직 손 안 댄 것"만 걸러보는 게 이 화면의 핵심 용도라 판단해 기능은 유지하고 라벨만 바꿨다.
- **담당자 > AI: 정렬·분류 우선순위 3종 확정 + 초안→확정본 소제목 붕괴 재발 수정** [Changed: 2026-08-14]: 2026-08-14 09:30 회차에서 초안이 확정본으로 넘어가며 소제목이 또 뭉개졌다는 제보. 2026-08-13에 한 번 고쳤던(`classify_for_finalize` 도입) 같은 증상이라 저장 데이터를 전수로 다시 뜯어 **원인이 두 개**임을 확인했다.
  - **원인 1 — 큐레이션 핸들러가 숨긴 기사까지 분류에 넣었다.** `app/scraper.py` `collect_run`은 `filter_hidden`을 거친 **보이는 기사 9건**으로 분류하고 그 구성으로 캐시를 심는데(`seed_cache`), `app/settings_server.py`의 큐레이션 핸들러들(`_handle_move_article`·`_handle_bulk_move_article`·`_handle_undo` 등)은 `run["articles"]` = **숨긴 기사 포함 14건**을 그대로 `snapshot_group_names`에 넘겼다. 기사 집합이 다르니 캐시 정확 적중이 실패하고, `_find_reusable`은 "기사가 줄어들기만 한" 경우만 재사용하는데 여기선 오히려 늘어난 쪽이라 그것도 실패 → **진짜 API 재호출**이 나가 전체가 새로 분류됐다. 실측 재현: 09:30 회차 캐시를 사고 직전 상태(보이는 9건짜리 항목만)로 되돌린 뒤 14건을 넘기면 API 호출 시도 2회(1회+재시도), 9건을 넘기면 0회. 결과가 회차 파일에 그대로 남아 있었다 — 담당자가 쓰던 `세제 개편 발표`가 `세제 개편 관련`으로 바뀌고, 손수 만든 `<우리부 관련>` 옆에 AI가 지은 `우리부 관련`이 따로 생겨 화면엔 같은 이름 소제목이 두 개, 없던 `비거주 1주택 세제`까지 새로 생겼다. 소제목 이름을 키로 저장되는 `group_order.json`/`group_labels.json`/`group_overrides.json`이 한꺼번에 고아가 된 것이 "작업한 게 AI로 복귀"의 실체다. → `snapshot_group_names`가 **자기 안에서** `filter_hidden`을 거쳐 분류하고(= `collect_run`과 같은 입력), 숨긴 기사는 원래 `group` 값을 그대로 유지한 채 반환 목록에 남기도록 고쳤다. 호출부를 하나씩 고치지 않고 함수 안에서 막은 이유: 같은 실수를 할 수 있는 호출부가 6곳이고 앞으로 더 늘어난다.
  - **원인 2 — 캐시가 담당자의 ↑/↓ 순서를 매번 되돌렸다.** `app/llm_classifier.py` `_rebuild`가 소제목 안 기사를 **캐시에 적힌 URL 순서**대로 늘어놨다. 그 순서는 곧 "캐시를 만든 시점의 언론사 우선순위 정렬"이라, 담당자가 ↑/↓로 올려둔 기사가 렌더링 한 번에 원래 자리로 되돌아갔다(확정본·초안 양쪽). 실측: 같은 소제목 안 두 기사를 맞바꾼 뒤 `classify_articles`를 다시 부르면 순서가 그대로 원상복구됨(수정 후 유지됨 확인). 다른 분류 경로(`_parse_groups`·`_apply_forced_groups`)는 이미 "입력 순서 유지" 원칙을 지키고 있었고 여기만 예외였다. → 캐시는 **소속만** 정하고 **순서는 지금 넘어온 리스트**를 따르도록 고쳤다. 같은 이유로 `snapshot_group_names`의 반환 순서도 분류 결과 순(소제목별로 뭉치는 순서)이 아니라 입력 순서 그대로로 바꿨다 — 큐레이션 조작 한 번마다 저장 목록이 재배열되면 잡아둔 순서가 계속 흔들린다.
  - **규칙 변경 — ↑/↓는 소제목 경계를 넘지 않는다.** 원래는 소제목 맨 위/아래에서 한 번 더 누르면 옆 소제목으로 기사가 통째로 넘어갔다(규칙21). 순서를 다듬다 한 번 더 눌렀을 뿐인데 분류가 바뀌어 기사를 놓치는 사고가 반복돼, ↑/↓를 순서 전용으로 좁히고 소제목 간 이동은 "다른 소제목" 드롭다운 전담으로 확정했다(사용자 결정). 버튼도 그 소제목의 첫/마지막 기사에서 바로 비활성화한다 — 예전엔 화면 전체의 첫/마지막에서만 꺼져서 "눌리는데 아무 일도 안 일어나는" 구간이 있었다. 수시 모니터링(`app/adhoc/card.py` `move_article_order`)이 처음부터 이 규칙이었으므로 이제 정기·수시가 같아졌다.
  - **안 고친 것.** `app/curation.py` `move_article`의 반환값 `(new_articles, new_override)`에서 `new_override`는 이제 항상 `None`이지만 튜플 형태는 남겼다 — 호출부 두 곳(`_handle_move_article`, `preview_move_article`)이 이 형태를 기대하고, 두 곳 다 `None`일 때의 경로가 이미 정상 동작이라 지금 바꿔봐야 얻는 게 없다.
- **사진기사 추정 필터 — 잘린 위치 캡션 신호 추가 및 초안·확정본 처리 방식을 "제외(D)"에서 "표시(B)"로 전환** [Added/Changed: 2026-08-21]: 담당자가 초안 캡처(같은 회의 사진 8건, `한자리 모인 경제수장들` 등)를 보내며 "이게 다 포토 기사더라, 그렇게 분류돼 있는 거냐"고 물으면서 시작. 원인은 기존 두 신호와 같은 계열이지만 새로운 변종이었다 — 캡션의 인물 위치 나열 문장("왼쪽부터 이찬진 금융감독원장…")이 네이버 요약 글자수 제한에 걸려 끝이 잘리면서, 기존 신호가 기대던 촬영 날짜 도장·기자 이메일까지 통째로 요약에서 사라졌다. 제목도 "한자리 모인 경제수장들"처럼 직책으로 안 끝나 기존 제목 패턴에도 안 걸렸다.
  - **신호 설계.** `_looks_like_truncated_position_caption()`(`app/filters.py`) — (1) 요약이 `...`/`…`로 잘린 채 끝나고, (2) 요약의 **마지막** 위치 지시어(왼쪽부터/오른쪽 두 번째 등) 뒤로 문장이 안 이어지고, (3) 제목이 서술형 종결어미로 안 끝나고 따옴표·…·[도 없을 때만 True — 세 조건 모두 필요. 위치 지시어가 있다는 것만 보면(2번 없이) 본문 앞에 캡션이 붙는 진짜 기사(이데일리·세계일보 등)가 11건 걸린다(오탐) — "캡션이 요약의 끝을 차지했는가"로 앵커를 옮겨야 그게 걸러진다. 저장된 기사 + 당일 초안 원본 1,310건 전수 검증: 신규 적중 16건, 오탐 0건, 캡처 8건 중 7건 포착. 남은 1건("경제수장들 '한자리에'")은 짧고(≤26자) 따옴표가 낀 캡션형 제목이라 조건 3(따옴표 가드)에 막혀 못 잡는다 — 따옴표 가드를 제목 26자 이하에서 면제하면 신규 17건·오탐 0으로 그 1건도 잡히지만, 그 판단을 뒷받침하는 진양성 표본이 이 사건 1건뿐이라 위 "촬영 날짜 도장" 항목과 같은 이유로 보류(독립 사례가 더 나오면 조건 3에 추가). 놓친 그 1건이 B안(아래) 전환 이후로는 부작용이 작다 — 흐려진 행 7개 사이에 안 흐려진 1줄이 끼어 있으면 오히려 눈에 띄어("이 아래 것도 사진이겠거니") 사람이 자연히 잡아낸다(담당자 의견).
  - **D→B 전환 — 왜 뒤집었나.** 처음엔 팝오버로 사진 추정 기사를 목록에서 빼고 "📷 사진으로 빠진 기사" 버튼으로 확인·"이번 회차에 넣기"(예약 저장소 `app/draft_articles.py` 재사용, 비용 거의 0)로 복구하는 D안을 설계했다(목업 4종 `PHOTO_BADGE_MOCKUP.html`: 현재/문구칩/행흐리기/사진접기/제외+팝오버 비교, D를 채택). 그런데 D는 판정이 놓친 사진(위음성)이 `hidden_articles.json`(담당자가 직접 숨김)으로 가고, 판정이 잡은 사진(참양성)은 예약 저장소 경로로 가 **같은 성격의 기사가 두 보관함으로 갈리는** 문제가 있었다(담당자 지적) — PRD 기능9 규칙9가 이미 "두 보관함 정책이 다르면 담당자가 어느 쪽이 언제까지 남는지 따로 기억해야 한다"고 못박은 것과 같은 종류. 게다가 판정이 완벽하지 않다는 전제에서는 D의 장점(목록이 깨끗함)이 그대로 단점(오탐이 안 보임)으로 뒤집힌다 — 담당자가 "판정을 코드로 고쳐야만" 대응할 수 있는 구조였다.
    실측으로 fail-open 우려(설정을 꺼두면 무인 회차에 사진이 새 나갈 것)를 확인했다 — 저장된 회차 52개·기사 1,235건에서 회차별 사진 잔존율은 06:00(무인)이 0%, 09:30이 6%였고, 당일 초안 원본을 발행 시각대로 쪼개면 8시(브리핑 시간대, 사람이 보는 시간)에 75%가 몰리고 6~7시(무인)엔 0%였다 — 사진이 몰리는 시간대와 담당자가 없는 시간대가 겹치지 않아 fail-open 위험이 실질적으로 낮다는 근거.
  - **B안 구현.** `exclude_photo_in_scrap` 설정(기본값 이미 `False`, 이 설치본만 `True`였던 걸 `save_scrap_page_settings`로 되돌림)을 꺼둔 채 쓰는 걸 기본 워크플로로 삼는다. `render_article`(`app/renderer.py`, `app/preview_renderer.py`는 이 함수를 그대로 import) — 사진 추정 기사를 지우지 않고 `.article.is-photo`(opacity 0.55, hover·체크 시 복원)로 흐리게 표시 + 실시간현황과 같은 "📷 사진 추정" 문구 칩(예전엔 초안·확정본만 아이콘 전용이었는데, 흔히 보이는 상태가 되며 문구가 필요해져 통일). 툴바에 "📷 사진 추정 전체 선택 (N)" 버튼(사진 추정 0건이면 안 그림, 확정본·초안 둘 다)을 추가해 기존 일괄 숨기기(`bulkHideSelected`)로 바로 넘긴다 — 자동으로 숨기지 않고 체크만 하므로, 담당자가 오탐 체크만 해제한 뒤 직접 🗑️를 눌러야 실제로 빠진다. `app/preview_renderer.py`는 CSS(`.photo-badge`, `.article.is-photo`)가 이전엔 아예 없었다(설정이 항상 켜져 있던 전제라 배지가 실질적으로 뜬 적이 없었음) — 처음으로 실제 스타일을 갖췄다.
- **사진기사 추정 필터 — 촬영 날짜 도장 뒤 "/매체명" 꼬리표를 다시 허용** [Fixed: 2026-09-01]: 담당자 제보 — 뉴스1 "가방 메고 출근하는 이형일 경제부총리 후보자"·"이형일 경제부총리 후보자, 출근길 인사" 두 건이 사진 추정 배지 없이 그대로 화면에 남았다. 원인은 위 2026-08-13 수정이 세워둔 전제가 뉴스1에는 안 맞았다는 것 — 그때 "도장 뒤 한글 꼬리(`/뉴스1`)가 있어도 재판매·DB 문구·제목 패턴 등 다른 신호가 잡아주니 회귀 0건"이라 판단하고 도장 패턴에서 한글 꼬리 자체를 막아뒀는데(`[^가-힣]*$`), 뉴스1은 재판매·DB 문구도 기자 이메일도 안 붙이고 제목도 "대화하는 한성숙·김호철"·"텅 빈 국민의힘 의원석"처럼 직책으로 안 끝나는 경우가 많아 그 "다른 신호"가 애초에 없는 매체였다. 도장 패턴에 `\s*/\s*[가-힣A-Za-z0-9]{1,8}` 꼬리를 추가로 허용(기존 "도장 뒤 한글 없으면 통과"와는 OR 관계 — 이메일 찌꺼기 케이스는 그대로 유지)해 도장 바로 뒤 매체명 한 덩어리만 열었다. 실측(저장된 회차·수시 카드·`live_cache`/`preview_cache` 전체 고유 기사 3,600건 전수): 신규 적중 57건 전부 뉴스1(예결위·재경위 사진 뭉치)이고 오탐 0건 — 서술형·따옴표 낀 제목("배준영 '여야 합의 없는 전체회의'" 등) 6건도 개별 확인해 전부 실제 사진 캡션이었다. 직책 목록에 "후보자"를 추가하는 안도 같이 재봤으나 신규 적중이 1건뿐이라 반영하지 않았다(제목 패턴은 이미 도장 패턴이 잡아내는 사진과 대부분 겹쳐, 별도 신호로 늘릴 실익이 낮음).
- **사진기사 필터 — `[포토]` 표식을 화면이 몰랐고, 부분일치가 진짜 기사를 지우고 있었다** [Fixed: 2026-09-03]: 담당자 제보 — 이데일리 `[포토] 공공기관 이전 발표` 같은 기사 뭉치가 "포토인데도 왜 사진 기사로 분류가 안 되냐"는 캡처와 함께 올라왔다. 원인은 **두 갈래가 갈려 있었다는 것**이다: `[포토]` 제목을 담당하는 건 `exclude_photo_articles`의 `PHOTO_HINT_WORDS` 부분일치뿐이고 그건 `exclude_photo_in_scrap`이 켜져야 도는데 기본이 꺼짐, 반대로 화면의 `📷 사진 추정` 배지는 `looks_like_photo_caption` 하나만 보는데 그 함수는 애초에 *"제목에 [포토] 표식이 **없는** 기사를 추정한다"*가 목적이라 제목 표식을 한 번도 안 봤다. 덤으로 `_TITLE_QUOTE_PATTERN` 가드(`"\'“”…[`)가 `[`를 막아, `[포토] 성평등가족부 세종 이전 발표하는 한성숙 총리`처럼 제목 패턴(`~하는 + 직책`)에 딱 맞는 것까지 구조적으로 탈락시켰다. 결과는 **표식이 명확한 기사가 표식 없는 기사보다 화면에서 덜 눈에 띄는** 상태 — 흐림도 배지도 없고 `📷 사진 추정 전체 선택 (N)` 카운트에도 안 잡혀 일괄 처리 대상에서도 빠졌다. 실측(저장 회차 + 수시 카드 + `live_cache`/`preview_cache` 고유 기사 4,903건 전수): 괄호 말머리 적중 49건 중 배지가 잡던 건 **0건**.

  담당자가 물은 건 사실 다른 것이었다 — *"이걸 이렇게 하면 다음엔 좀 나아질까? 여러 번 손을 봤다보니"*. 이 필터는 2026-08-11 이후 다섯 번 손봤고 그때마다 새 구멍이 났다. **그 다섯 번이 전부 "추정" 층이었다는 게 답이다** — 표식 없는 캡션을 요약 모양·제목 모양으로 알아맞히는 일은 언론사가 캡션 쓰는 법을 바꿀 때마다 새 구멍이 나는 게 정상이고, 앞으로도 난다. 반면 `[포토]`는 **언론사가 자기 손으로 붙인 표식**이라 알아맞힐 게 없다 — 신호를 하나 더 얹는 게 아니라, 코드가 다른 경로(`exclude_photo_articles`)에서 이미 100% 믿고 쓰던 사실을 화면 쪽에만 안 알려주던 **배선 누락**이었다. 그래서 이 구멍은 한 번 닫으면 다시 안 열린다.

  고치면서 **첫 제안(“힌트 단어가 있으면 True” 한 줄)은 실측하고 취소했다.** 그대로 하면 지금 있는 오탐까지 화면으로 끌어오는 것이었기 때문 — 부분일치 54건 − 괄호 말머리 49건 = 차이 5건이 **전부 오탐**이었다: `이언주 "…세제 강화보다 정책 무게 둬야"`(강**화보**다 — 딱 `[단독]` 필터가 2026-08-20에 피해간 "…단독명의" 유형), `"냉면이 징그러워"…AI로 만든 메뉴 사진에`, `"냉면·국수 사진, 보기만 해도 속이 울렁"…`(둘 다 진짜 기사), `…2기 출범… < 포토박스 < 생활물류 < 기사본문`(사이트 메뉴가 제목에 섞인 진짜 기사). **`exclude_photo_in_scrap`이 기본 꺼짐이라 아무도 못 봤을 뿐, 켜면 저 기사들이 화면 어디에도 흔적 없이 사라졌다** — CLAUDE.md Curation의 "과잉 숨김은 안 보이고 왜 없는지 화면 어디에도 안 남는다"가 그대로 잠복해 있던 자리다. 즉 `[단독]`·`[속보]`(`HEADLINE_TAG_RE`)와 일정 나열 기사(`_SCHEDULE_LISTING_RE`)가 이미 "괄호 말머리만 인정, 부분일치 배제"로 피해간 실수를 **사진 쪽만 아직 하고 있었다**.

  **배지 라벨은 안 갈랐다**(사용자 결정). 표식과 추정이 한 배지를 겸하게 되면서 `[포토]` 기사에 "추정"이 엄밀히 틀린 말이 됐고, `📷 사진기사` / `📷 사진 추정`으로 나누는 안을 올렸는데 담당자가 기각했다 — *"'추정'이란 말 애매한 거 알지만 이미 사람들이 흐린 기사는 '사진이구나' 인식하게 됐으니 [포토]에도 붙어야 될 듯"*. **화면 어휘는 이미 학습된 자산이라, 정확한 이름 하나를 더 만드는 값보다 그 어휘를 쪼개는 대가가 크다**는 판단이다(`📷 사진 추정 전체 선택 (N)` 버튼 이름이 같이 애매해지는 문제도 딸려 있었다). 정확성은 툴팁에서만 갈랐다 — 표식이면 `제목에 [포토] 표식이 붙은 사진기사입니다`, 추정이면 옛 문구 그대로. 옛 문구를 그대로 둔 채 표식 기사에 붙였다면 배지가 화면에서 거짓말을 하게 되므로(`제목에 [포토] 표식은 **없지만**`) 툴팁만은 반드시 갈라야 했다.

  적용: `PHOTO_TAG_RE`/`has_photo_tag()`를 `headline_kind` 옆에 세우고(같은 모양의 판정이라 같은 자리), `looks_like_photo_caption`이 `[속보]` 예외 **다음, 다른 모든 신호보다 앞에서** 그걸 본다. `exclude_photo_articles`는 substring 루프를 통째로 버리고 `looks_like_photo_caption` 하나만 거친다 — **판정을 두 벌로 두지 않는다**는 규칙은 숨김 판정(`is_hidden`, 2026-08-27)에서 이미 같은 이유로 세운 것이고, 그 두 벌이 갈려 있던 것이 바로 이번 제보의 원인이었다. 위치는 맨 앞으로 안 묶었다 — 49건 중 1건(이투데이 `한성숙 총리, 행정·공공기관 이전 기자회견 [포토]`)이 제목 **끝**에 붙어 있었다. 표기는 49건 전부 `[포토]` 하나였다(다른 괄호 변형·`[포토뉴스]`는 실제 표본에 없었지만 정규식은 같이 받는다). 결과: 사진 판정 554건(표식 49 + 추정 505), 잃은 진양성 1건 — 뉴시스 `사진으로 보는 지난 주말 [뉴시스Pic]`은 진짜 사진 모음이지만 `[포토]` 계열 표식이 없다. 매체 고유 표식(`[뉴시스Pic]` 등)까지 넣는 건 **진양성 표본이 이 1건뿐이라 보류**한다(촬영 날짜 도장·제목 26자 가드와 같은 판단 — 독립적인 사례가 더 나오면 그때 검증한다).

- **사진기사 추정 필터 — 요약 신호 넷 전부에 "요약의 끝" 앵커를 걸었다** [Fixed: 2026-09-03]: 담당자 제보 — 뉴스핌 `[종합] 외교부·통일부도 세종 이전…윤호중 "대통령실 이전시기 맞춰 검토"`가 사진 추정으로 흐려졌다. 저장된 요약이 `왼쪽부터 ,허장 재정경제부 2차관, 윤호중 행정안전부 장관, … 2026.09.03 gdlee@newspim.com 윤호중 행정안전부 장관은 3일 부처 및 공공기관 2차 이전 방향 발표 브리핑에서 "세종 중심의...`였다 — **캡션이 본문 앞에 붙어 나오는 진짜 기사**다(뉴스핌·아주경제·뷰어스는 기사 맨 위 사진의 캡션 문단이 본문 첫 문단보다 앞서고, 네이버 `description`이 기사 첫머리를 그대로 잘라 준다). 담당자가 "사진기사로 냈다가 내용을 덧붙인 거면 우리가 어떻게 못 하는 것 아니냐"고 물었는데 그게 아니었다 — 언론사가 도중에 기사를 바꾼 게 아니라 원래부터 그 레이아웃이고, **우리가 판정할 것도 언론사의 의도가 아니라 "요약에 본문이 이어지느냐" 하나뿐이라 이미 손에 든 문자열로 갈린다.** 같은 뉴스핌 안에서 이메일이 든 기사 58건이 "이메일에서 요약이 끝남(진짜 사진기사)" 48건 / "이메일 뒤로 본문이 이어짐(일반 기사)" 10건으로 깨끗이 갈렸고, 제목도 그 차이를 그대로 따라갔다(`발언하는 허장 재정경제부 2차관` ↔ `[종합] 외교부·통일부도 세종 이전…`).
  - **이미 알고 있던 함정을 신호 하나에만 막아둔 게 원인**이었다. 바로 위 2026-08-21 항목이 잘린 위치 캡션 신호를 만들 때 "위치 지시어가 문장 어디에 있든 보면 이데일리·세계일보 등 본문 앞에 캡션이 붙는 진짜 기사가 11건 걸린다(오탐) — 캡션이 요약의 *끝*을 차지했는지로 앵커를 옮겨야 한다"고 같은 교훈을 적어뒀는데, 그때 **위치 지시어 신호에만** 앵커를 걸었다. 촬영 날짜 도장은 정규식 자체에 `$`가 있어 우연히 안전했고, 재판매·DB 문구·사진기자단·기자 이메일 세 신호에는 앵커가 없었다. 제보 기사의 요약은 심지어 `왼쪽부터`로 시작해 위치 지시어 신호가 걸러냈을 바로 그 형태인데, 이메일 신호가 먼저 True를 내버려 거기까지 가지도 못했다.
  - **고친 방식**: `_summary_signal_at_tail(summary)` 하나를 만들어 네 신호 **전부**에 같은 앵커를 건다 — 각 신호의 **마지막** 등장 뒤로 한글이 `_CAPTION_TAIL_MAX_HANGUL`(10)자 이상 이어지면 그건 캡션이 아니라 본문 앞머리로 보고 무시한다(신호마다 따로 판정하므로, 재판매 문구가 중간에 있고 날짜 도장이 끝에 있는 요약은 여전히 잡힌다). 신호를 지우거나 좁히지 않고 "어디에 있느냐"만 물은 것이라, 잡던 것은 그대로 잡는다.
  - **임계값 10은 실측으로 잡았다**(저장 회차 + 수시 카드 + `live_cache`/`preview_cache` 고유 기사 6,030건 전수, 2026-09-03). 신호 적중 511건의 꼬리 한글자수가 **0자 483건 / 2자 9건 / 8자 3건**과 **10자 이상 16건**으로 갈리고 **그 사이 구간이 완전히 비어 있다**. 앞쪽 495건은 전부 진짜 캡션이고, 특히 8자짜리 셋은 연합뉴스 `[그래픽]` 기사의 `X(트위터) @yonhap_graphics 인스타그램` 꼬리표라 **단순 `$` 앵커였다면 같이 죽었을 것**이다(그래서 "한글이 안 이어지면"으로 앵커를 잡았지 "끝에 붙어 있으면"으로 잡지 않았다). 뒤쪽 16건은 전부 본문이 이어지는 일반 기사였다 — 뉴스핌 9·아주경제 2·뷰어스 2, 사진기자단 신호 쪽 2건(세계일보 `반도체 초격차 유지 2.6조 특별회계 신설…`, 한겨레 계열 `李대통령, 세제개편안 후퇴 지적에…`)까지 포함해 `[2027 예산안] 재생에너지 확대 2.8조 투입`, `공공기관 109개 줄인다…LH는 분리` 같은 핵심 정책 기사들이다. 적용 결과 사진 추정 563건 → 551건, **잃은 진양성 0건**(`발언하는 허장 재정경제부 2차관`·`[그래픽] 국가 예산 추이` 등 개별 확인).
  - **"사진 추정 기능을 아예 끄자"는 안은 기각했다**(담당자와 상의). 6,030건 중 사진 추정이 563건(9.3%)인데 그중 오탐은 13건뿐이라 나머지 550건을 버리는 거래가 되고, `exclude_photo_in_scrap`이 꺼진 지금은 판정이 틀려도 지워지지 않고 흐려질 뿐이라(2026-08-21 B안) 오탐의 대가가 이미 작다.
- **사진기사 추정 필터 — "~하는 + 이름-이름" 제목 신호 추가** [Added: 2026-09-11]: 담당자 제보 — 초안 캡처에서 뉴시스 `대화 나누는 구윤철-배경훈-정성호`·`대화 나누는 한성숙-구윤철-배경훈-정성호`·`…-이언주` 세 건이 바로 옆 같은 행사 사진들과 달리 배지 없이 또렷하게 떴다("포토기사인데 '추정' 안붙은 이유"). 원인은 2026-08-21 잘린 위치 캡션과 같은 계열의 변종이다 — 사진 속 인물이 셋·넷이라 캡션이 길어 네이버가 요약을 `…본회의에 참석해 대화하고... `에서 잘라 **날짜 도장·기자 이메일이 통째로 사라졌고**, 제목은 직책이 아니라 이름(`정성호`)으로 끝나 `~하는 + 직책` 제목 패턴에도 안 걸렸다. 같은 행사의 두 사람짜리 `대화 나누는 구윤철 장관-배경훈 장관`은 캡션이 짧아 이메일이 살아남아 걸렸고, `자료 살펴보는 한성숙 국무총리`는 요약이 잘렸어도 제목이 직책으로 끝나 걸렸다 — 셋이 한 화면에 섞여 있어 판정이 들쭉날쭉해 보였다.
  - **새 신호** (`app/filters.py` `_PHOTO_CAPTION_NAME_LIST_TITLE_PATTERN`): 관형형(`는/은/한/던`) 뒤에 **하이픈으로 이은 인물 이름**(이름마다 직책은 붙어도 되고 안 붙어도 된다)만으로 끝나는 제목. 기존 제목 패턴과 같은 따옴표·…·[ 가드를 건다. 실측(저장 회차 + 수시 카드 + `live_cache`/`preview_cache` 고유 기사 7,819건 전수): 적중 24건, 그중 기존에 놓치던 9건이 전부 진짜 캡션 — 뉴시스 3·노컷뉴스 2·뉴스핌 4, 행사도 대정부질문·예결위·비상경제본부·G20으로 **서로 독립적인 사례**라 "진양성 표본이 한 사건뿐이면 보류"(2026-08-13·08-21) 기준을 넘는다. 사진 추정 835건 → 844건, **잃은 진양성 0건**.
  - **가운뎃점(`·`)은 인정하지 않는다.** 넓히면 한국경제 `현직 장관 19명 중 70년대생은 배경훈·원민경뿐`(일반 기사)이 걸린다(오탐 1건). 뉴스1 `대화하는 한성숙·김호철` 같은 가운뎃점 캡션은 2026-09-01에 넓힌 날짜 도장 `/뉴스1` 꼬리로 이미 잡힌다.
  - **기각한 신호: 요약 끝의 `(사진=○○)` 크레딧.** 새로 걸리는 6건 중 SBS 일반 기사 2건·이데일리 `[기자수첩]`이 섞였다 — 일반 기사에도 사진 크레딧이 붙는다.
  - **넣을지 망설인 이유도 적어둔다.** 이득이 작고(전 기간 9건, 하루 0~3건), 판정이 놓치는 쪽은 `exclude_photo_in_scrap`이 꺼져 있는 한 "흐려지지 않을 뿐"이라 무해하다. 반대로 오탐은 진짜 기사를 흐리고 `📷 사진 추정 전체 선택`에 끼운다. 오탐 0건·독립 사례 여러 건이라 넣었다(사용자 결정).
- **사진기사 추정 필터 — "…장관 후보" 제목 · 연도에서 잘린 날짜 도장** [Added: 2026-09-15]: 담당자 제보 — 초안에서 뉴시스 인사청문회 사진(`답변하는 이형일 부총리 겸 재정경제부장관 후보`·`인사청문회 질의에 답하는 …`·`인사말하는 …`·`선서하는 …`)이 전부 배지 없이 또렷하게 떴다("이거 다 사진기사야, 왜 사진추정으로 못 걸러내니"). 바로 옆 같은 제목의 `인사청문회 출석한 …` 한 건만 흐려져 있어 판정이 들쭉날쭉해 보였다. 원인 둘이 겹쳤다 — (1) 뉴시스는 캡션에 행사 정식 명칭을 통째로 넣어(`제439회 국회(정기회) 재정경제기획위원회 국무위원후보자(부총리 겸 재정경제부장관 이형일) 인사청문회에서…`) 네이버가 요약을 도장 **앞**(`…답하고... `)이나 도장 **한가운데**(`…하고 있다. 2026.... `)에서 잘랐고, 기존 도장 패턴은 월·일까지 있어야 걸린다. 흐려진 한 건은 우연히 `2026.09.15....`까지 살아남은 것이었다. (2) 제목은 `~하는 + 직책` 꼴인데 끝이 `장관 후보`라 직책으로 안 끝났다.
  - **새 요약 신호**: `다. 2026.... `처럼 캡션 문장이 끝난 직후 연도(·월)에서 잘린 도장. **연도 바로 뒤 점을 필수로 둔다** — 없으면 연합뉴스 `…이같이 나타났다고 밝혔다. 2024... `(일반 기사의 다음 문장이 연도에서 잘린 것)가 걸린다(오탐 1건 실측).
  - **제목 패턴 확장**: 직책 뒤 `후보`/`후보자` 허용, 서술어 어간 1글자 허용(`눈 감은`·`찾은`·`갖는`). 2026-09-01에 직책 목록에 "후보자"를 따로 넣는 안은 신규 1건이라 기각했는데, 이번엔 **직책 + 후보** 꼴만 열어 `후보`로만 끝나는 제목은 여전히 안 걸린다.
  - **따옴표 가드에 곡선 작은따옴표 `‘’` 추가.** 넓힌 제목 패턴이 조선비즈 `李 엄포에 ‘물가’ 16번 언급한 경제부총리 후보자`(일반 기사)를 잡았는데, 원인은 가드가 `'`·`“”`만 알고 `‘’`는 몰랐던 것 — 가드를 쓰는 세 신호에 원래 있던 구멍이다.
  - **실측**(저장 회차·회차 trash·수시 카드·수시 trash·`live_cache`/`preview_cache` 고유 기사 9,555건 전수): 적중 917건 → 950건, 신규 33건을 요약까지 열어 전부 캡션 확인(뉴시스 청문회 16·뉴스핌 비상경제본부 회의 5·뉴스1 재경위 항의 3 등 서로 독립적인 사례), **잃은 적중 0건·오탐 0건**.
  - **[추가: 2026-09-15, 같은 날 두 번째] 받침 ㄴ 서술어 제목** — 담당자 제보: 수시 수집 원본에서 노컷뉴스 `생각에 잠긴 이형일 경제부총리 후보자`가 배지 없이 떴다("이것도 사진추정기사인데"). 요약이 `…인사청문회에서 생각에 잠겨있다.`로 끝나 날짜 도장·기자 이메일이 아예 없는 매체라 제목이 유일한 단서였고, 제목 패턴은 서술어 끝을 `는/은/한/던` 네 글자로만 받아 `잠긴`(잠기+ㄴ)을 몰랐다. 과거 관형형 ㄴ은 어간에 붙어 음절 하나로 합쳐지므로(`만난`·`든`·`잠긴`) 글자를 하나씩 더하는 대신 **받침이 ㄴ인 음절 전부**로 넓혀 쟀다.
    - **그대로 넓히면 오탐 3건** — 전부 인사 기사였다: `…에 금융위 출신 우상현 전 BC카드 부사장`(명사 `출신`), `…신임 전무에 우상현 전 BC카드 부사장`·`…위원장에 윤성관 의원`(이름 자체가 ㄴ으로 끝남). 뒤의 둘은 **서술어와 직책 사이에 이름 한 덩어리(한글 2~4자)를 필수로** 두자 사라졌고(캡션은 언제나 `서술어 + 이름 + 직책` 순서다), 남은 `출신`은 서술어 자리 제외 목록(`_CAPTION_NOUN_MODIFIERS`)으로 뺐다. 제외는 정규식 안 부정 전방탐색으로 둔다 — 밖에서 거르면 `출신`에서 실패한 뒤 그 뒤의 진짜 서술어를 다시 찾지 않는다. `초선`·`재선` 같은 같은 종류의 명사는 실제로 걸린 적이 없어 짐작으로 넣지 않았다.
    - **기존 `는/은/한/던` 패턴은 안 건드리고 세 번째 패턴으로 따로 뒀다** — 그 패턴엔 이름 필수 조건이 없어 합치면 기존 적중을 잃는다.
    - **실측**(저장 회차·trash·수시 카드·캐시 고유 기사 9,415건 전수): 873건 → 885건, 신규 12건 전부 캡션(뉴시스·뉴스핌 `… 만난 구윤철 부총리` 10건 — 해외 출장 면담 사진, 뉴스1 `스마트폰 손에 든 허장 재정경제부 2차관`, 이 제보 1건), **잃은 적중 0건·오탐 0건**.
  - **[추가: 2026-09-15, 같은 날 세 번째] 직책 없는 `이름 + 후보자` 제목** — 담당자 제보: 노컷뉴스 `관계자와 대화하는 이형일 후보자`가 배지 없이 떴다("이것도 사진기사인데"). 요약은 `…인사청문회에서 관계자와 대화를 하고 있다.`로 끝나 도장·이메일이 없고, 제목은 `~하는 + 직책(+후보자)` 꼴이어야 걸리는데 **직책이 통째로 빠져** 이름 바로 뒤에 `후보자`가 왔다. `후보자`로 끝나는 제목을 전부 뽑아보니 못 잡던 건 이 제보와 m-economynews `「M포토」 선서문 전달하는 이형일 후보자`·`「M포토」 인사청문회서 모두발언하는 이형일 후보자`(「」 괄호라 `[포토]` 표식 판정에도 안 걸림), 그리고 일반 기사 둘(`李 엄포에 ‘물가’ 16번 언급한 경제부총리 후보자` — 따옴표 가드로 이미 빠짐, 동아일보 `빚투 말라는 정부, 10억아파트 5.3억 영끌한 국토 후보자`)이었다.
    - **이름 칸을 2~4자로 열면 동아일보 기사가 걸린다** — `국토`가 이름 자리에 들어간다. 서술어 바로 뒤 **정확히 3자**로 좁히자 사라졌다. 서술어와 이름 사이에 다른 말이 끼는 꼴(첫 패턴처럼 `[^따옴표]*`)도 재봤는데, 신규 적중 없이 그 동아 기사만 걸려 뺐다.
    - **2026-09-01에 직책 목록에 "후보자"를 통째로 넣는 안은 신규 1건이라 기각했다** — 이번 것은 "서술어 + 3자 이름 + 후보자"로 제목 끝을 통째로 고정한 훨씬 좁은 꼴이라 그 결정과 부딪히지 않는다. 한국 인명이 2자·4자인 경우(`허장` 등)는 이 패턴이 못 잡지만, 짐작으로 넓히면 명사가 이름 자리에 들어온다 — 실제로 놓친 사례가 나오면 그때 잰다.
    - **실측**(저장 회차·trash·수시 카드·캐시 고유 기사 9,809건 전수): 1,076건 → 1,079건, 신규 3건 전부 캡션, **잃은 적중 0건·오탐 0건**. 진양성이 같은 행사(이형일 청문회) 하나라 표본이 얇다는 점은 알고 둔다.
  - **[추가: 2026-09-15, 같은 날 네 번째] 매체 고유 표식 `[헤럴드pic]`** — 담당자 제보: 수시 수집 원본에서 헤럴드경제 `[헤럴드pic] 당당·초조·여유…누가 통과할까`가 배지 없이 떴다("이것도 사진기사였어"). 이 기사는 **추정으로는 원리적으로 못 잡는다** — 제목이 질문형에 `…`까지 있어 제목 모양 신호가 전부 막히고, 요약은 `…참석하고 있다. 여야는 이들을 둘러싼... `처럼 도장 앞에서 잘렸다. 단서는 표식 하나뿐인데 표식 판정은 괄호 안 `포토`만 알았다.
    - **`pic`/`픽`이 든 괄호 말머리를 전부 뽑아 쟀다**(고유 기사 8,665건): `[헤럴드pic]` 14건은 **전부 캡션**(`…모두발언을 하고 있다.`·`윤창빈 기자`)이고 기존 판정 적중 0건 — 그중 6건은 제목이 `모두발언하는 구윤철 부총리`처럼 캡션 모양인데도 `[` 가드에 막혀 제목 신호가 안 돌았다. 반면 서울경제 `[Pick코노미]`(정책 기사 약 15건)·`[AI픽]`·`[논설실 Pick]`·`[아침에 PICK]`·`[이코노픽]`은 전부 일반 기사, `[뉴시스Pic]`·`[뉴스1PICK]`은 사진 모음과 발언 기사(`장동혁 … "재판 취소용 개각" [뉴시스Pic]`)가 섞였다. 그래서 **`헤럴드pic` 한 표식만** 표식 층(`_OUTLET_PHOTO_TAG_RE`)에 넣었다 — 8/13~9/15에 흩어진 독립 사례 14건이라 "진양성 표본이 한 사건뿐이면 보류" 기준을 넘는다. `[` 가드를 푸는 쪽은 기각(그 가드가 막는 오탐이 더 많다).
    - **툴팁을 한 함수로 모았다** (`app/filters.py` `photo_badge_tip`). 표식 기사 툴팁이 `제목에 [포토] 표식이 붙은…`으로 고정돼 있어 `[헤럴드pic]`에는 틀린 말이 된다 — 이제 실제 붙은 말머리를 그대로 적는다. 고치다 보니 같은 문구가 확정본(`app/renderer.py`)·실시간현황(`app/live_renderer.py`)·수시(`app/adhoc/renderer.py`)에 따로 적혀 있었고, **수시만 분기가 없어 `[포토]` 기사에도 늘 "표식은 없지만 추정"이라고 하고 있었다**(2026-09-03에 두 화면만 고친 흔적). 배지 글자 「📷 사진 추정」은 그대로다.
    - **실측**: 1,053건 → 1,067건, 신규 14건 전부 `[헤럴드pic]`, **잃은 적중 0건**.
  - **[추가: 2026-09-15, 같은 날 다섯 번째] 캡션 문장 직후 잘린 요약** — 담당자 제보: 뉴스핌 `김승원 - 이형일 - 이소영 장관 후보자, 인사청문회 선서`가 배지 없이 떴다("이것도 포토기사 같은거야"). 요약이 `장동규, 정일구, 이건주 기자 = 김승원 법무부·이형일 …·이소영 … 장관 후보자가 15일 오전 서울 여의도 국회에서 열린 인사청문회에서 선서를 하고 있다.... `로 끝났다 — 세 사람 캡션이라 길어서 네이버가 **도장이 시작되기 직전**에 잘랐다(같은 행사의 한 사람짜리 뉴스핌 `인사청문회 선서하는 이형일 부총리 후보자`는 `2026.09.15 mironj19@newspim.com`까지 살아남아 걸렸다). 오전에 넣은 "연도에서 잘린 도장"은 `2026.`까지는 남아야 걸리고, 제목은 서술어 없이 이름 나열로 시작해 네 제목 패턴 어디에도 안 맞는다.
    - **후보 셋을 재봤다**(고유 기사 8,704건): (A) 제목 맨 앞 `이름 - 이름` — 21건 신규 중 20건이 `국회-경제계`·`대구-경북 광역철도`·`오세훈-김윤덕, 26일 다시 만나…` 같은 일반 기사라 기각. (B) 요약 맨 앞 `이름, 이름, 이름 기자 =`(여러 사진기자) — 오탐은 없지만 적중이 이 제보 1건뿐이라 보류. (C) 요약이 `…고 있다.` 직후 `...`로 끝남 — 신규 32건 중 캡션은 10건 남짓이고 나머지가 칼럼·사설·해설(`…이미 3.5%를 예상하고 있다....`)이라 그대로는 못 쓴다.
    - **(C)에 "마지막 문장에 주어 + 날짜" 조건을 붙였다** — `[이/가] (지난달·지난·N월)? N일((현지시간))?` 뒤에 `…고 있다.`로 끝나는 문장. "누가 언제 어디서 무엇을 하고 있다"는 사진 캡션의 고정 틀이고, 일반 기사의 `…고 있다.`는 날짜 없이 현재 상황을 서술하는 문장이 대부분이다. 조건에서 빠지는 것까지 확인했다: 뉴스핌 인터뷰 `금융통화위원회는 지난 7월 16일과 8월 27일 두 차례…유지하고 있다`(주어 조사 `는`), 서울경제 `14~18일 중 하루를 정해…논의되고 있다`(날짜 앞에 주어 없음).
    - **실측**: 적중 13건, 1,067건 → 1,075건, 신규 8건 **전부 캡션**(뉴스핌 시장상황점검회의·G20·존 힐리 면담, 뉴시스 기자회견 둘, 연합뉴스 레스퀴르 면담, 뉴스1 베선트 면담, 이 제보 — 매체 넷·행사 여섯으로 독립 사례), **잃은 적중 0건·오탐 0건**. `_summary_signal_at_tail`을 그대로 타도록 `_PHOTO_CAPTION_SUMMARY_PATTERNS`에 넣었다(정규식이 `$`로 끝나 꼬리가 늘 0자다).
    - **아직 못 잡는 형태**: 캡션 문장 안에 점이 낀 경우(`…참석,AI.디지털을 통한…` — 뉴스핌 KOAFEC 사진 3건). 문장 경계를 점으로 가르기 때문에 그 문장이 둘로 쪼개져 주어·날짜가 앞쪽 조각에 남는다. 표본이 한 행사뿐이라 넓히지 않았다.
- **회차 마감 안내 배너 추가** [Added: 2026-08-18]: 위 사고를 조사하던 중 별개로 드러난 문제 — 초안(`preview.html`)이 회차 마감 카운트다운(00:00:00)에 닿아도 화면이 아무 말도 안 하고 그대로 멈춰 있었다. 원래 주석엔 "회차가 끝나는 순간 화면 자체가 확정본으로 넘어가므로 완료 문구가 필요 없다"고 적혀 있었는데, 실측해보니 그 전제가 틀렸다 — 확정본 전환은 `app.scheduler`의 다음 tick(최대 10초 뒤)이 회차를 저장·확정할 때 일어나는 서버 쪽 사건이고, 이미 열려 있는 초안 탭은 아무 신호 없이 계속 옛 초안을 보여준다. 담당자가 "왜 확정본으로 안 넘어가지"라고 헷갈려 한 게 바로 이 상태였다. 목업 3안(`ROUND_END_BANNER_MOCKUP.html`, 실제 초안 화면 색·톤 그대로 만들어 상단 버튼으로 전환 비교) — A. 목록 위 안내 박스(이미 있는 빈 `#preview-hint` 자리를 채우기만 하면 됨), B. 상단 고정 띠(스크롤과 무관하게 항상 보임), C. 카운트다운 자리만 텍스트 교체(코드 변경 최소) — 를 보여주고 사용자가 B(노란 상단 고정 띠)를 선택했다("이 노란색이 딱 좋다"). 구현: `updateRoundCountdown`이 `remain<=0`이면 `showRoundOverBanner()`를 불러 `#round-over-banner`(`position:fixed`, topbar보다 위 z-index)를 띄우고, `body.round-over`가 `.topbar`를 배너 높이만큼 아래로 밀어낸다(높이를 하드코딩하지 않고 JS가 `offsetHeight`로 실측해 `--round-banner-h` CSS 변수에 반영 — 문구 길이가 줄바꿈되는 화면 폭에서도 안 겹친다). `나중에`는 `sessionStorage`(`roundOverDismissed:{ROUND_DEADLINE_MS}`, 마감 시각별 키)에 남아 같은 회차 동안 재로딩해도 안 다시 뜬다. `_PAGE_TEMPLATE` 하나를 모든 초안 화면(`render_preview_page`/`render_preview_done_page`/`render_preview_error_page`)이 공유하므로 서버 쪽 함수 서명은 하나도 안 바뀌었다 — `ROUND_DEADLINE_MS`가 이미 모든 렌더 경로에 있었고(0이면 애초에 카운트다운 자체가 안 도는 기존 가드), 배너는 순수 CSS/JS로만 얹었다.
- **초안에서 옮긴 소제목이 새로고침하면 AI 분류로 되돌아간 문제** [Fixed: 2026-08-18]: 담당자 제보 — 초안(`preview.html`)에서 직접 만든 소제목("우리부 관련 및 기타")으로 기사를 옮겨도, 새로고침하면 LLM이 잡은 원래 소제목("전력망 확충과 전기료 인상")으로 도로 돌아간다. **분류 쪽은 멀쩡했다** — 실측으로 확인: 그 회차 초안 기사 36건에 `group_overrides`만 손으로 넣어주고 `classify_articles(allow_llm_call=False)`를 부르면 문제의 기사가 정확히 "우리부 관련 및 기타"에 들어간다. 진짜 원인은 **배정 기록이 저장된 지 10초 만에 지워지고 있었던 것**이다. 2026-08-14에 넣은 `cleanup_group_overrides`(스케줄러 매 tick + 앱 시작)가 "살아있는 기사"를 `data/articles/`(확정 저장된 회차)에서만 모았는데, 초안 기사는 아직 회차로 저장되기 전이라 그 목록에 없다 — 그래서 `prune_group_overrides`가 초안에서 만든 배정 기록을 전부 "이미 사라진 기사 것"으로 오판해 삭제했다. 실측: 그 시점 `preview_cache.json`의 초안 기사 36건 중 `data/articles/`에도 있는 건 **0건**, `group_overrides.json`에 남아있던 초안 기사 배정도 **0건**이었다(담당자가 방금 옮겼는데도). 원래 의도는 "가리키는 기사가 보관 기간이 지나 삭제됐으면 지운다"였지 "아직 저장 전인 기사를 지운다"가 아니었으므로, `cleanup_group_overrides`가 훑는 범위에 아직 저장되지 않은 오늘치 기사 3종 — 초안 화면에 지금 떠 있는 기사(`app/preview_cache.py`), 다음 회차에 합쳐질 예약 기사(`app/draft_articles.py`), 실시간에서 담아둔 기사(`app/manual_articles.py`) — 을 더했다. 셋 다 자정 지나면 스스로 비는 저장소라 "쌓이기만 한다"는 원래 문제도 다시 생기지 않는다. 이 경로로 사라지던 건 초안의 "다른 소제목" 드롭다운·일괄 이동뿐 아니라 `_handle_promote_manual_article_to_draft`(담아둔 기사를 초안 소제목에 넣기)와 `/assign-unclassified`(🤖 미분류 배정)의 결과도 전부 포함이었다 — 넷 다 `set_group_override` 하나로 저장되기 때문이다. CODING_CONVENTIONS §3(담당자 작업은 조용히 사라지지 않는다)에 정확히 해당하는 사고이고, "정리(cleanup) 로직은 자기가 모르는 저장소의 데이터를 남의 것으로 취급한다"는 점에서 재발 위험이 남는 자리다 — 앞으로 기사 목록을 담는 새 저장소가 생기면 이 함수에도 같이 등록해야 한다.
- **초안에서 옮긴 소제목이 확정본에서 되돌아간 문제 — 같은 사고의 재발** [Fixed: 2026-08-21]: 담당자 제보 — "14시 초안에서 폴더 정리를 했는데, 다시 들어가니 처음 LLM 분류한 그대로 돌아가 있다". **바로 위 2026-08-18 항목과 같은 사고이고, 그때의 수리가 부족했던 것이다.**
  - **실측으로 확인한 경위**(추측 아님 — `data/undo_stack.json`의 스냅샷이 시각별 `group_overrides.json` 원본을 그대로 담고 있어 되짚을 수 있었다): 13:32:11에 담당자가 기사 2건을 "기타"로 옮겨 배정 기록이 141개 → **143개**가 됐다. 13:53:28 스냅샷까지 143개 그대로. 그런데 디스크의 `group_overrides.json`은 13:54:53에 **141개**로 다시 쓰였고, 빠진 2개가 정확히 그 2건(`.../366/0001187531`, `.../366/0001187545`)이었다. 14:00:07에 회차가 확정될 때는 이미 기록이 없어서, 두 기사가 AI 첫 분류("한국은행 인사 및 금리정책", "재정 기후재난 대응 예산 갈등")로 되돌아간 채 저장됐다. 담당자가 그 시간대에 한 다른 작업(숨기기 30여 건, 소제목 이름 변경, 커스텀 소제목 추가, 순서 조정)은 전부 멀쩡히 남아 있었다 — 오직 "소제목 이동"만 사라졌다.
  - **`group_overrides.json`에서 기록을 지우는 코드는 `prune_group_overrides` 하나뿐이다**(`set_group_override`는 더하기만 하고, 되돌리기는 `undo_stack.json`을 함께 다시 쓰는데 그 파일 수정시각은 13:53:28에 멈춰 있다). 소거법으로 정리 로직이 범인임이 확정된다.
  - **왜 또 났나 — 2026-08-18의 수리가 증상만 막았다.** 그때는 "살아있는 기사 URL 집합"에 초안 캐시(`preview_cache.json`)·예약 기사·담아둔 기사를 **더하는** 것으로 막았다. 하지만 `preview_cache.json`은 **회차가 넘어가거나 검색어가 바뀌면 통째로 비워지는 임시 캐시**다 — 즉 "지금 살아있는가"의 답이 임시 파일의 수명에 좌우된다. 그 캐시가 비는 순간과 회차 파일이 저장되는 순간 사이에 정리가 한 번이라도 돌면 같은 사고가 그대로 재현된다. 저장소를 하나 더 등록하는 식으로는 이 창문을 닫을 수 없다(다음에 새 임시 저장소가 생기면 또 열린다).
  - **고친 방식 — 삭제 근거를 바꿨다.** "지금 어딘가에 보이는가"를 버리고 **"배정한 지 `RETENTION_DAYS`(365일)가 지났는가"** 하나만 본다. 2026-08-14에 이 정리를 넣을 때 코드 주석이 이미 대안으로 적어둔 방식이다("눈에 띄게 커지면 항목에 시각을 넣어 나이로 만료시키는 쪽을 검토한다"). 저장 형식은 `{url: 이름}` → `{url: {"group": 이름, "at": ISO시각}}`. 옛 형식도 그대로 읽고, `at`이 없는 옛 기록은 **지우지 않고** 정리 때 현재 시각을 찍어 새 형식으로 옮긴다 — 언제 만든 기록인지 모르는 것을 "오래됐다"고 단정해 지우면 그게 바로 같은 종류의 조용한 삭제다. 읽는 쪽 계약(`load_group_overrides()` → `{url: 이름}`)은 그대로라 호출부 15곳은 손대지 않았다.
  - **덤으로 단순해졌다.** 정리가 회차 파일을 한 개도 안 읽으므로, 2026-08-18에 성능 때문에 넣었던 "회차 파일 서명이 같으면 즉시 반환" 장치(`_last_pruned_signature`)가 통째로 필요 없어져 삭제했다. 141개짜리 dict 하나만 훑는다.
  - **교훈(CODING_CONVENTIONS.md §3 보강).** 정리(cleanup) 로직의 삭제 근거는 **그 데이터 자신이 들고 있는 사실**(만든 시각)이어야지, **다른 저장소의 현재 상태**(어딘가에 보이는지)여서는 안 된다. 후자는 그 저장소의 수명·타이밍이 바뀔 때마다 조용히 되살아나는 사고다 — 실제로 3일 만에 같은 증상으로 재발했다.
- **실시간 현황 "[단독]·[속보] 필터"** [Added: 2026-08-19]: 사용자가 실시간 현황 화면 캡처에 손으로 표시해가며 "위쪽엔 [단독]·[속보]·미선정 기사 체크박스, 아래는 지금 있는 그룹 칩"으로 나누자고 제안. 목업(`LIVE_TAG_FILTER_MOCKUP.html`) 두 라운드로 합의했다.
  - **1라운드**: A안(체크박스 4개 나란히)과 B안(말머리를 점선 상자로 묶고 건수 표시) 두 시안을 인터랙티브 목업으로 나란히 보여줬다. 반대 근거 셋을 실측·논리로 제시: (1) "단독" 부분일치 금지 — 저장된 고유 제목 1,339건(정기 40회차+수시 카드) 전수조사에서 괄호형 말머리(`[단독]` 15건, `[속보]` 2건)는 전부 제목 맨 앞이었고, 부분일치로 걸렀다면 "시가 26억 이하는…장기 거주땐 **단독명의**" 1건이 오탐으로 잡혔을 것 — 그래서 "제목 맨 앞" 위치 판정으로 확정. (2) [단독]·[속보]를 옆의 "선택 언론사만"과 같은 모양으로 나란히 두면 전부 AND로 읽혀 "둘 다 켜면 항상 0건"이라는 함정이 생긴다는 점을 지적 — 이 둘 사이만 OR이어야 함을 시각적으로/논리적으로 구분해야 한다고 제안. (3) 실측 비율(1.3%)상 [속보]는 0건인 날이 흔하므로 그룹 칩처럼 0건일 때 아예 안 그리면 "필터가 사라졌나" 혼란을 준다 — 항상 그리되 0건이면 흐리게/비활성(B안 한정 제안).
  - **2라운드(사용자 확정)**: A안으로 최종 결정하되 다음을 수정 — ① [단독] 말머리를 **배지/칩으로 감싸지 않는다**(삭제), 제목 글자색만 바꾸는 건 허용. ② 워딩 간결화: `☑️ 선택 언론사만 보기` → `선택 언론사만`, 체크박스 라벨의 ✅ 이모지 3개 전부 제거(플랫 체크박스로). ③ 목업의 기사 행에서 실제 담아두기 버튼 라벨을 `→ 스크랩`으로 잘못 적은 것을 사용자가 캡처로 지적 — CLAUDE.md 문장("A '→ 스크랩' button…")을 코드를 안 보고 그대로 베낀 게 원인이었고, 실제 `app/live_renderer.py` `_render_row`의 5states(`📌 담아두기`/`📌 담아둠`/`🤖 자동 선별`/`✔️ 스크랩됨`/`🗑️ 숨김`)로 CLAUDE.md도 같이 고쳤다(CODING_CONVENTIONS §1 — 추측하지 말고 코드를 본다).
  - **구현**: `_HEADLINE_TAG_RE`(제목 맨 앞 괄호형 말머리만, `[]()<>《》【】〔〕` 변형 허용)로 판정한 결과를 `data-headline` 속성에 담아 클라이언트 필터(`applyLiveFilters`)가 읽는다. 색은 배지가 아니라 `highlight_keywords`가 만드는 형광펜 스팬과 겹치지 않도록 말머리 구간·나머지 구간을 나눠 각각 하이라이트를 태운 뒤 말머리 쪽만 `.t-scoop`/`.t-flash`로 감쌌다(제목 문자열 자체는 안 바뀌므로 복사/내보내기엔 영향 없음, CODING_CONVENTIONS §4). "미선정 기사"는 기존에 그룹 칩 줄(OR)에 혼자 AND로 섞여 있던 흠이 있었는데, 특수조건 줄로 옮기며 같이 바로잡았다.
- **실시간 현황 "[사설]" 체크박스 추가** [Added: 2026-09-16]: 사용자가 실시간 필터 줄 캡처와 함께 "[사설] 체크 표시도 넣어줘, [속보] 바로 뒤에"라고 요청. 자리·모양은 그대로 따랐고, 판정 규칙만 실측으로 갈렸다.
  - **`HEADLINE_TAG_RE`에 「사설」을 더하지 않은 이유.** 그 정규식 하나를 고치면 끝날 것처럼 보였지만, 실제로 그 상수를 읽는 곳이 넷이었다 — `app/breaking_alert_sender.py`(텔레그램 `[단독]`·`[속보]` 알림), 제목 글자색(`.t-scoop`/`.t-flash`, 실시간·확정본·초안·수시 넷), `sort_scoop_first`(`[단독]` 소제목 내 최상단 정렬), `looks_like_photo_caption`의 `[속보]` 예외. 한 글자를 더했다면 **담당자가 요청한 적 없는 사설 알림이 텔레그램으로 나갔을 것**이고, 그건 되돌릴 수 없는 종류의 실패다. 그래서 `EDITORIAL_TAG_RE`/`is_editorial`을 따로 두고, 화면 쪽 속성도 `data-headline`(단독/속보)과 `data-editorial`로 나눴다 — 실측상 한 제목에 둘이 같이 붙은 경우는 0건이지만, 그 우연에 기대면 언젠가 한쪽이 다른 쪽을 덮어쓴다.
  - **위치를 안 가리기로 한 근거(실측).** 2026-08-19에 `[단독]`·`[속보]`를 "제목 맨 앞만"으로 못 박았기 때문에 사설도 같은 규칙을 따를 뻔했다. 저장된 고유 제목 6,874건(정기 회차 + 수시 카드) 전수조사: 「사설」이 들어간 제목 52건 중 괄호형 `[사설]`이 50건이었고, 그중 **7건(14%)이 제목 맨 앞이 아니라 끝**에 붙어 있었다 — `'부동산·세제 그대로' 새 경제팀, 민심과 市場 수긍할까[사설]`, `비거주 1주택을 투기로 보는 시각부터 바꿔야 [사설]`(한국경제·조선일보 등). 맨 앞만 인정했다면 사설 7건이 체크박스에서 조용히 빠졌을 것이다. 위치 무관으로 열어도 **오탐 0건**이었고, 이미 같은 이유로 위치를 안 가리는 선례가 있다(`has_photo_tag` — "제목 끝에 붙이는 매체가 있다"). 2026-08-19의 "맨 앞만"은 말머리 일반의 원칙이 아니라 **그때 그 두 말머리에 대한 실측 결과**였다는 것이 이번에 드러났다.
  - **괄호 안이 정확히 「사설」일 때만.** 못 잡은 2건은 `[전국 주요 신문 사설](14일 조간)` — 사설을 모아 소개하는 일반 기사지 사설 자체가 아니라, 안 걸리는 게 맞는 판정이다.
  - **OR 묶음에 넣었다.** 말머리 셋은 서로 AND일 수 없다(한 제목이 동시에 사설이면서 속보일 수 없어 둘을 켜는 순간 항상 0건 — 2026-08-19에 `[단독]`↔`[속보]`를 OR로 둔 그 근거 그대로). 브라우저에서 실제로 확인했다: 사설 6 + 속보 4 + 단독 4 + 일반 10건을 그려놓고 셋을 차례로 켜면 6 → 10 → 14건으로 합집합이 넓어지고, `선택 언론사만`과는 여전히 AND로 좁혀진다.
- Explicitly out of scope for this version: other news sources (Daum, Google News), user accounts/login, email notifications, per-outlet filter/search UI, remote/mobile access off the local network.

## 검색어 표시 (확정본·초안) — 2026-08-21

"이 기사가 어떤 검색어로 스크랩됐는지 단어가 보이면 좋겠다. 다만 지금 칩을 붙일 칸이 너무 적잖아"라는
요청에서 시작했다. 실시간현황에는 이미 그룹 태그·키워드별 건수가 있지만, 정작 보고서를 만드는
확정본·초안에는 없었다.

**먼저 쟀다(CODING_CONVENTIONS.md §1).** 저장된 최근 8회차 353건에서 등록 키워드 9개가 제목+요약에
몇 개나 걸리는지 세어봤다: 1개 120건 / 2개 126건 / 3개 78건 / 4개 6건, **최대 4개**이고 쉼표로 이어
붙였을 때 **가장 긴 줄이 26자**(`재정경제부, 구윤철, 재경위, 재정경제기획위원회`)였다. 즉 "칸이 부족한"
문제가 아니라 **짧은 정보였다** — 새 자리를 만들 필요가 없다는 게 결론이었고, 접기(`+N`)도 두지
않기로 했다.

**같은 조사에서 이 기능의 진짜 근거가 나왔다.** 353건 중 **23건(6.5%)은 제목·요약 어디에도 검색어가
없었다** — 네이버가 본문으로 잡은 기사다. 지금 화면에서는 이런 기사가 왜 목록에 있는지 알 방법이
0이었다.

**목업 4안을 실제 CSS·마크업으로 만들어 비교했다**(`figma/10_검색어표시_목업.html`, `render_article`을
그대로 호출해 뽑았다 — 손으로 흉내 낸 목업은 밀도를 속인다). 안1 액션 줄 + "검색어" 글자 라벨,
안2 액션 줄 + 돋보기 아이콘, 안3 제목 아래 별도 줄, 안4 액션 줄 + 옅은 파란 배경 덩어리.

- 데스크톱 800px: 안1·2·4는 매칭 4개여도 한 줄에 들어가 **카드 높이 변화 0**, 안3만 기사마다 한 줄 증가.
- 모바일 375px: **안1·2·4 모두 액션 아이콘 묶음이 다음 줄로 밀린다**(기준선에서는 같은 줄에 붙어 있었다).
  즉 모바일에서는 어느 안이든 한 줄이 늘어나고, 그 조건에서는 안3이 오히려 정렬이 깔끔하다.

**처음엔 안1 채택**(사용자 선택). 데스크톱에서 공짜인 데다 "검색어"라는 글자가 그대로 있어 뜻이 안 헷갈리고,
게시시각·원문보기와 같은 회색 톤이라 "참고용 메타 정보"라는 무게가 맞는다. **칩은 명시적으로 뺐다** —
사용자 지적("칩 식으로는 안 해도 될 것 같아"). 안4(파란 배경)는 이 앱의 색 언어(파랑 = 담당자가 하는
동작)와 부딪혀 눌러야 할 것처럼 보인다. 안2는 아이콘만으로 "검색어"라는 뜻을 알아야 통한다.

**그런데 실제 화면에 띄우고 나서 안2(돋보기 아이콘)로 뒤집었다** — 같은 날, 사용자 지적. 목업에서는
5행만 봐서 몰랐는데, 회차 전체(50~100행)를 띄우니 "검색어"라는 같은 세 글자가 매 행 반복되며 정작
값보다 먼저 눈에 걸렸다. 결정적으로, **내가 안2를 반대한 근거("아이콘만으로는 검색어라는 뜻을 알아야
통한다")가 틀렸다** — 이 앱엔 이미 같은 관례가 있었다: 수시 수집 결과 화면 상단 메타 칩이
`app/adhoc/renderer.py`에서 `icon("search") + 검색어들` 형태를 쓰고 있어, 담당자가 이미 그 뜻으로 본
아이콘이었다. 목업을 만들 때 기존 화면에서 같은 관례를 먼저 찾아봤어야 했다. 설명을 없앤 건 아니고
`title` 툴팁으로 옮겼다. 부수 효과로 폭이 세 글자만큼 줄어 모바일에서 액션 아이콘이 다음 줄로 밀리는
것도 완화됐다.

**저장이 필요했다.** 실시간현황은 키워드별 원시 결과를 캐시해 두고 매 렌더링마다 `matched_keywords`를
다시 계산하지만(그래서 아무 것도 저장하지 않는다), 확정본은 그 원시 결과가 없다 — 수집 시점에
저장하지 않으면 영영 못 보여준다. `collect_run`/초안 수집 호출에 `track_keyword_matches=True`만
붙였고 **네이버 추가 호출은 0회**다. 부작용으로 저장 값은 수집 당시 검색어 기준으로 굳는데, 이건
버그가 아니라 "그 회차가 왜 이렇게 모였나"의 기록이라 맞는 동작으로 봤다. 대신 **이 기능 이전에
저장된 회차는 영원히 빈칸**이다(보관 1년이라 한동안 섞여 보인다).

**엑셀(기능11)은 이번에 안 넣었다.** 9번째 열로 맨 뒤에 붙이는 것 자체는 가능하지만(중간에 끼우면
담당자가 이미 만든 수식·피벗이 깨진다), 5개 내보내기 지점의 데이터 사정이 갈린다 — 수시 수집결과·
수시 보관함은 `matched_keywords`를 이미 저장 중이라 과거 카드까지 채워지고, 확정본·정기 보관함은
오늘 이후 회차만, **라벨 보관함은 손 안 대면 영원히 빈칸**이다(`labels.json`이 원본 회차와 독립된
7필드 스냅샷이라 나중에 회차에서 다시 읽어오는 게 원리적으로 불가능하다 — 채우려면 `attach_label`
스냅샷에 필드를 추가해 앞으로 붙이는 라벨부터 채우는 수밖에 없다). "화면부터 써보고 엑셀은 나중에"가
사용자 결정. 넣게 되면 빈칸을 AI로 추정해 채우지 않는다는 기능11 원칙은 그대로다.

### 실시간현황으로 확장 — 2026-08-25

"실시간에서 그룹으로 소팅되는 건 좋은데, 초안처럼 어떤 검색어로 걸렸는지도 보이면 좋겠다"는 요청.
확정본·초안 때와 사정이 정반대였다 — **저장이 필요 없었다.** 이 화면은 그룹 칩을 그리려고
`matched_keywords`를 이미 매 렌더링마다 계산하고 있어(`_matched_groups_for`), 그 값을 한 번 더
읽어 쓰기만 하면 됐다. 네이버 호출도, 저장 형식 변경도, 과거 데이터 빈칸 문제도 없다.

**다시 쟀다(§1).** 그날 `data/live_cache.json`에 있던 키워드 21개·기사 576건 기준: 매칭 1개
446건(77%) / 2개 83 / 3개 26 / 4개 12 / 5개 4 / 6개 3 / **8개 2건**, 문자열 길이는 중앙값 5자·
95백분위 20자·**최대 53자**. 확정본 때(최대 4개 26자)보다 꼬리가 길다 — 이 화면은
`include_in_scrap`과 무관하게 활성 그룹 전부를 검색하기 때문. 그래도 0.3%짜리 꼬리라
확정본과 같은 판단(접기 없음)을 유지했고, 실제로 렌더해보니 최장 케이스만 메타 줄에서
두 줄로 접히고 나머지는 그대로 붙었다.

**자리는 확정본과 같은 순서(게시시각 → 🔍검색어)로 맞췄다.** 그룹 칩 바로 뒤도 후보였지만
(거친 것 → 세밀한 것 순서라 논리적), 두 화면을 오갈 때 같은 정보가 같은 위치에 있는 쪽이
낫다고 봤다. 구분점(`·`)만 이 화면 관례대로 `margin-left: 6px`로 바꿨다 — 확정본의 액션 줄은
`·`로 항목을 잇고, 실시간의 메타 줄은 여백으로 잇는다.

**중복이 아닌 이유.** 그룹 칩과 나란히 서서 언뜻 같은 말로 보이지만, 축이 다르다 — 칩은
필터 축("이 그룹 켜고 끄기"), 검색어는 이유 축("왜 이 기사가 여기 있나")이다. 특히 상단
헤비 키워드 경고("코스피가 결과의 33%")를 보고 나서 *어느 행이 그 키워드인지* 찾는 길이
지금까지는 "키워드별 건수" disclosure의 숫자뿐이었다.

## 색 계열 정리 — 뜻 하나에 색 하나 (2026-08-21)

**계기.** 색 값을 config.py로 모은 직후, "컬러가 너무 많이 쓰여서 정보 단계가 안 보인다"는
피드백이 이어졌다. 실측(확정본 화면 하나 기준)으로 유채색이 27종이었다 — 값을 한 곳에
모았다고 화면에 보이는 색 수가 줄어드는 건 아니었다(그건 애초에 이번 정리의 목적이 아니었다).

**진단.** 원인은 "색을 많이 골라서"가 아니라 **상자(배너·배지)마다 전용 4색 세트를
새로 만든 게 쌓인 것**이었다. 팔레트 121키 중 54키(45%)가 회차 마감 배너·AI 분류 실패
배너·저장 안 함·조건 밖 기사·라벨 원본 사라짐 같은 개별 상자의 배경/테두리/글자/보조글자
세트였다. 뜻은 "주의"·"오류" 둘뿐인데 세트는 8~9개였다.

**결정 전 시안.** 구현 전에 실제 화면 조각(기사 목록·배너·배지+툴바)을 손으로 재현한
before/after 비교 시안을 먼저 만들어 검토받았다. 시안에서 정한 원칙 셋:
1. 상자는 뜻 3개(정보=파랑·주의=앰버·오류=빨강)로만 — 새 배너를 만들 때 새 색 세트를
   만들지 않고 기존 셋 중 하나를 재사용한다.
2. 한 색 계열은 한 뜻만 — 초록은 발송에만, 앰버는 주의·라벨 둘뿐(그 외 겸업은 다 양보).
3. 색을 안 써도 되는 건 뺀다 — 이미 `opacity`로 흐려지는 표시(📷 사진 추정)에는 색을
   더 얹지 않고, "이미 확인한 기사" 표시도 색(남색) 대신 opacity로 바꾼다.

**사용자 결정 3개.**
- `[속보]`를 초록에서 `[단독]`과 같은 빨강 계열로 — 승인. (같은 "말머리" 축인데 색이
  정반대였던 게 가장 크게 어긋난 지점이었다.)
- "저장됨" 배지의 초록 — 질문에 답한 뒤(아래) 파랑으로. "초록=발송" 하나로 좁히면서,
  다른 "성공/완료" 신호들과 같은 파랑(정보)으로 옮겼다. **예외**: 설정 화면의 "API 연결
  테스트 정상"(`ok_bg`/`ok_text`/`ok_border`, Naver·LLM·자동발송 연결 확인)은 손대지
  않았다 — "저장 성공"과는 다른, 훨씬 오래되고 널리 쓰이는 "테스트 통과=초록" 관례라
  사용자가 구체적으로 물은 범위 밖이었다.
- 정기 보관함의 회청색(`arch_reg_bg`/`arch_reg_text`) 제거 — **보류(현행 유지)**. 회수되는
  색이 2개뿐인데 홈 화면에서 정기/수시 구분이 이미 잘 되고 있어 이득보다 손실이 컸다.

**"저장됨"이 뭘 가리키는지** 사용자가 되물었다 — 설정 화면(키워드·언론사 등)에서 변경을
저장했을 때 뜨는 `.save-msg.saved` 배지("저장됨" 문구 + 초록 점)를 말한다. API 연결
테스트 결과와는 다른 컴포넌트다.

**구현.**
- `app/config.py`: `COLOR_SCRAP_GREEN`·`COLOR_AUTO_TEAL`·`COLOR_DONE`·`COLOR_SEEN`
  네 상수를 완전히 제거했다(값을 바꾸는 대신 없앴다 — 이름 자체가 "초록"·"청록"을
  주장하고 있어서, 값만 바꾸면 이름이 거짓말을 하게 된다). 대신 해당 템플릿 줄이
  `{accent}`/`{hover}`(스크랩됨)·`{ai_text}`/`{ai_bg}`(자동 선별)를 직접 참조하도록
  고쳤다 — 그 뜻을 가진 기존 토큰과 완전히 합류시켰다.
- `COLOR_FLASH_TEXT`(`[속보]`)는 이름을 유지하되 값을 `COLOR_SCOOP_TEXT`(`[단독]`과 같은
  빨강) 참조로 바꿨다.
- 뜻은 그대로인데 색만 겸업하던 자리(`round_over_*`, `degraded_*`, `out_*`, `must_*`,
  `snap_gone_*`, `query_mark`, `photo_badge_*`, `saved_*`)는 이름을 유지한 채 값만
  해당 세트(주의/오류/수시청록/중립/파랑)로 다시 가리키게 했다 — 이름이 "회차 마감
  배경"처럼 색이 아니라 쓰임을 말하고 있어 값만 바꿔도 거짓말이 안 된다.
- 파랑 연한 톤 8종 → 3종(soft 배경·line 테두리) + hover 시 한 단계 더 진한 테두리 1종.
  이름은 그대로 뒀다(화면마다 "톤온톤 hover"·"켜진 칩 배경" 등 쓰임은 계속 갈라 부름).
  ghost_border_hover만 남긴 이유: 실제로 hover 시 테두리 색이 바뀌는 상호작용 피드백이
  `settings_server.py`의 순서조정 버튼 등 여러 곳에 이미 있었다 — 완전히 하나로
  합치면 hover 피드백이 조용히 사라진다.
- 중립 회색 13종 → 8종(+토글 track 전용 2종은 남김 — 스위치 부품이라 합치면 꺼짐 상태가
  안 보인다).
- "이미 확인한 기사" 표시(`seen_color`, 남색)는 색 자체를 없애고 `opacity: 0.62`로
  바꿨다(3개 화면 각 1줄).

**검증.** 확정본 화면 실측 유채색 27종 → 23종(본문 글자색 2개 빼면 21종). 파랑 계열만
따로 세면 11종 → 6종으로 줄었다(값 기준). 24개 화면 전부 렌더링해 미치환 자리표시자
없음을 확인했고, 통합 테스트 10개 전부 통과했다. 실제 화면 스크린샷으로 `[단독]`·`[속보]`가
같은 빨강 계열로 보이는 것도 확인했다.

**작업 중 발견한 사고.** 검증 도중 `test_integration.py`가 `history_renderer.py`에서
`period_and_result_html` KeyError로 실패한 적이 있었다 — git diff로 확인해보니 이 색
정리와 무관한, 이미 작업 트리에 있던 별개의 미완성 변경(정기 보관함에 수시 보관함과 같은
기간·검색 UI를 추가하는 중이었던 것으로 보임)이었다. 잠깐의 과도기 상태를 우연히 붙잡은
것으로 보이고, 재확인 시 그 코드가 이미 완성돼 있어 테스트가 다시 통과했다 — 이 정리
작업과는 무관해서 손대지 않았다.

## 색 값을 config.py 한 곳으로 — 2026-08-21

**계기.** "컬러로 정보 단계를 좀 나눠야 할 듯하다"는 요청으로 먼저 전수조사를 했다.
`app/**.py` + `main.py`의 6자리 hex를 전부 세어보니 **서로 다른 색이 113개**였고, 그중
이름이 붙어 한 곳에서 고칠 수 있는 건 `app/config.py`의 `COLOR_*` 10개와 형광펜 5색뿐,
나머지 **98개는 렌더러 8개 파일의 CSS 문자열 안에 직접 박혀 있었다**(`app/settings_server.py`
56줄, `app/preview_renderer.py` 50줄, `app/renderer.py` 49줄, `app/adhoc/renderer.py` 49줄,
`app/live_renderer.py` 19줄, `app/landing_renderer.py` 9줄, `app/history_renderer.py` 6줄).

조사에서 나온 문제는 네 가지였다.

1. **같은 색이 여러 뜻을 겸했다.** `#FFF9C4`가 "새로 들어온 기사"·"방금 올린 기사"·"회차
   마감 배너" 셋에 동시에, `#15803D`가 발송 버튼·`[속보]` 제목색·"✔️ 스크랩됨" 배지 셋에
   동시에, `#FEF2F2`가 오류 배경이면서 실시간현황의 평범한 hover 배경이었다.
2. **같은 뜻이 여러 값으로 갈라져 있었다.** "담당자가 누르는 파랑"이 값 8개
   (`#EFF6FF`/`#DCEAFE`/`#E7F0FF`/`#D8E7FF`/`#DBEAFE`/`#BFDBFE`/`#C7D9F7`/`#A9C6F5`).
3. **같은 값이 파일 4곳에 복붙돼 있었다.** `[단독]` 제목색 `#B91C1C`가 네 파일에 네 번,
   라벨 팝오버 앰버 세트가 네 파일에 네 번. 한 화면에서 톤을 바꾸면 나머지는 옛 색으로 남는다.
4. **"이 화면 전용이라 공용 팔레트엔 안 넣는다"는 관행이 3번의 원인이었다.** `seen_color`
   (#3B5FA0), `ghost_border`(#C7D9F7), `scrap_green`(#15803D) 모두 그 주석과 함께 파일 안에
   상수로 있었는데, 결국 다른 화면에도 그대로 복사돼 "한 곳에서 못 고치는 값"이 됐다.

**그래서 색을 바꾸기 전에 값부터 모았다.** 3·4번을 그대로 둔 채 색만 바꾸면 화면마다 어긋난
채로 남기 때문 — 정보 단계를 나누는 작업은 그 다음이다.

**한 것.**

- `app/config.py`에 뜻 단위로 이름 붙인 상수 ~110개와, 그걸 `{자리표시자} → 값`으로 펼친
  `PALETTE` 표(121키)를 만들었다. 각 렌더러의 `_theme()`(또는 `.format()` 호출)이
  `**PALETTE`를 통째로 받으므로, 새 색을 추가할 때 파일별로 따로 등록할 필요가 없다.
- CSS 템플릿 안 색 리터럴 **306곳**을 자리표시자로 바꿨다. 치환은 줄 번호를 지정한 단발
  스크립트로 했고(같은 줄에 같은 값이 두 번 나오면 실패하도록 `count==1`을 강제),
  치환 후 **306건 전부에 대해 `PALETTE[키] == 원래 값`인지 기계적으로 재검사**해 0건 불일치를
  확인했다. `#fff`/`#ffffff` 47곳도 `{card}`(면) / `{on_fill}`(진한 바탕 위 글자)로 갈랐다.
- 뜻이 다르면 값이 같아도 이름을 나눴다: `COLOR_SEND`·`COLOR_FLASH_TEXT`·`COLOR_SCRAP_GREEN`·
  `COLOR_DONE`(전부 `#15803D`), `COLOR_ROW_NEW`·`COLOR_ROUND_OVER_BG`(전부 `#FFF9C4`),
  `COLOR_LIVE_BG`·`COLOR_SCOOP_BG`·`COLOR_ERROR_BG`(전부 `#FEF2F2`). 지금은 값이 같지만,
  나중에 한쪽만 조정할 때 나머지가 딸려가지 않게 하려는 것이다.
- 덤으로 실시간현황의 오래된 흠이 없어졌다. 그 화면만 `{hover}`를 붉은 톤(`COLOR_LIVE_BG`)으로
  덮어쓰고 있어서, 파란 hover가 필요한 자리(형광펜 팝오버·로딩 안내)마다 색 값을 직접 적고
  "왜 여기만 직접 적는가"를 주석으로 설명해야 했다. 이제 붉은 톤은 `{live_bg}`, 파란 hover는
  다른 화면과 같은 `{hover}`로 이름이 갈려 그 예외가 사라졌다.

**검증.** (1) 306건 값 일치 재검사 0건 불일치, (2) 모든 템플릿의 자리표시자가 실제로 채워지는지
`string.Formatter`로 전수 확인, (3) 화면 24개(확정본·초안·실시간·실시간 로딩·정기 보관함·홈·
수시 3종·설정 15종)를 실제로 렌더링해 미치환 자리표시자 0건 확인, (4) 통합 테스트 전부 통과.
치환 도중 `app/renderer.py`의 `.format()` 호출에 `bg=COLOR_BG`가 남아 `**PALETTE`와 충돌한
버그가 하나 나왔는데, 통합 테스트가 바로 잡았다.

**주의 — git HEAD는 이 작업의 기준선이 아니었다.** 처음엔 HEAD의 렌더러와 결과를 비교해
검증하려 했는데, 작업 트리에 커밋 안 된 변경이 이미 많아(화면 제목이 "스크랩 초안"→"초안"으로
바뀌어 있는 등) 내 변경과 남의 변경이 섞여 나왔다. 그래서 "치환 표 자체를 검사하는" 방식
(위 (1))으로 바꿨다 — 외부 기준선이 없어도 성립하는 검사다.

**안 한 것.** 위 1·2번(같은 색이 여러 뜻 / 같은 뜻이 여러 값)은 **일부러 그대로 뒀다.**
이번 작업은 값을 옮기기만 한 것이라 화면에 보이는 색은 한 픽셀도 안 바뀐다. 중복을 실제로
합치는 건 화면이 바뀌는 결정이라 별도 작업이다 — 이제는 config.py에서 상수 하나의 값만
고치면 되므로 그때 파일을 훑을 일은 없다.

## 수시 복사·txt에서 URL 뒤 빈 줄이 빠져 있던 문제 (2026-09-01)

정기 쪽은 2026-07-27부터 기사마다 URL 다음에 빈 줄을 넣어왔다("보고용으로 복사해
쓸 때 훑어보기 쉽게" — `app/renderer.py` `_build_plain_text`, 초안·정기 보관함도 같은
규칙). 수시(`app/adhoc/renderer.py` `build_adhoc_plain_text`)만 빈 줄을 **소제목 끝에
한 번**씩 넣고 있어서, 복사·txt를 받아보면 URL과 다음 기사 제목이 줄바꿈 하나로 붙어
한 덩어리로 보였다(사용자 제보, 스크린샷). 수시가 나중에 만들어지면서 그 규칙이
따라오지 않은 것으로, 의도적인 차이가 아니었다.

빈 줄을 기사 루프 안으로 옮겨 정기와 같은 규칙으로 맞췄다. 소제목 사이 간격도
정기와 동일해진다(마지막 기사의 빈 줄이 그대로 구분선 역할을 하므로 소제목 끝의
별도 빈 줄이 필요 없다). 수시 보관함의 사안·날짜·결과 전체 층
(`app/adhoc/archive_renderer.py` `build_plain_text_for_cards`)은 이 함수를 그대로
이어붙이는 구조라 같이 고쳐졌다. 엑셀 내보내기는 행 단위라 무관.

## 소제목별 복사 (2026-09-01)

복사 단위가 "회차/카드 전체" 아니면 "기사 한 건"뿐이라 중간이 비어 있었다. 쟁점 하나만
떼어 다른 부서에 넘기려면 전체를 복사한 뒤 손으로 지우거나 기사를 한 건씩 눌러야 했다.
정기 보관함은 이미 결과 전체 / 날짜 / 회차 3층으로 갖춰져 있는데, 정작 매일 만지는
확정본·초안·수시 수집 결과에는 그 중간 층이 없었다(사용자 제안).

소제목 헤더(`<h2>`)의 아이콘 묶음 **맨 앞**에 📋을 붙였다. 목업
([SUBHEADING_COPY_MOCKUP.html](mockups/SUBHEADING_COPY_MOCKUP.html))으로 두 자리를 비교해
고른 결과다 — 맨 앞이면 "보는 동작(📋) → 고치는 동작(✏️🗑️) → 옮기는 동작(▲▼)"
순서가 되고, 뒤에 두면 파괴적인 🗑️ 바로 옆에 무해한 📋이 붙어 손이 미끄러지는 자리가
된다. 소제목 헤더에 컨트롤이 하나 더 늘어나는 부담(체크박스·이름·배지·건수까지 7개)은
있지만, 컨트롤 하나 때문에 기사 행처럼 「⋯ 더보기」를 새로 만드는 건 2클릭을 만드는
과한 대응이라 그냥 붙였다.

**복사 텍스트에 회차 헤더를 포함한다**(사용자 결정). 소제목만 떼어 메신저에 붙일 때
"언제 기준인지"가 없으면 받는 쪽이 못 쓴다 — 정기 보관함의 회차 층 복사도 같은 이유로
헤더를 넣고 있다. 그래서 나가는 텍스트는 `언론 모니터링 14시 기준` + 키워드 메모 줄 +
빈 줄 + 소제목 이름 + 그 소제목 기사다.

**[수정: 2026-09-02] 그 회차 헤더를 뺐다 — 소제목 이름 + 기사만 나간다**(사용자 요청).
하루 써 보니 예측이 틀렸다: 이 버튼의 실제 쓰임은 "소제목 하나를 통째로 다른 부서에
넘기기"보다 "쓰고 있던 문서 중간에 이 쟁점만 끼워 넣기"였고, 그 자리에선 `언론 모니터링
14시 기준` 줄이 붙는 문서의 맥락과 겹쳐 매번 지워야 하는 군더더기였다. 회차 정보가 실제로
필요한 자리(회차 전체 복사·txt·발송·정기 보관함 회차 층)는 그대로 헤더를 붙이므로, "언제
기준인지"를 잃는 건 아니다.

빌더를 다시 가르지 않았다 — 세 함수에 `header: bool = True` 인자 하나만 더해 소제목별
복사만 `header=False`로 부른다(`build_group_copy_texts` / `_build_preview_group_copy_texts`
/ `build_adhoc_plain_text` 호출부). 아래 "전용 빌더를 만들지 않았다"와 같은 이유다.
덤으로 메모 줄이 헤더와 함께 빠져 소제목별 복사가 `manual_keyword_note.json`을 아예 안
읽게 됐다 — 도입 때 "소제목 수만큼 같은 JSON을 15번 연다"를 막으려고 `note` 인자를
넘겨두었던 최적화가 그대로 필요 없어졌다(그 인자 자체는 전체 복사 경로가 계속 쓴다).
버튼 툴팁의 `(회차 헤더 포함)`도 같이 뗐다.

**전용 텍스트 빌더를 만들지 않았다** — 세 화면 모두 기존 전체 복사 함수에 소제목
하나짜리 목록만 넘긴다(`app.renderer._build_plain_text` / `_build_preview_plain_text` /
`app.adhoc.renderer.build_adhoc_plain_text`에 `groups` 인자 추가). 헤더 문구·기사 줄
형식·기사 사이 빈 줄이 자동으로 전체 복사와 같아지기 때문이고, 빌더를 따로 두면 한쪽만
고쳤을 때 두 텍스트가 조용히 어긋난다(바로 위 "수시 복사·txt에서 URL 뒤 빈 줄이 빠져
있던 문제"가 정확히 그 사고였다). 정기 두 화면은 소제목 수만큼 텍스트를 만들면서 키워드
메모 파일을 매번 열게 되므로, 메모를 한 번만 읽어 `note` 인자로 넘긴다.

**AI 분류 폴백 상태의 확인창은 안 붙였다**(사용자 결정). 초안의 전체 복사는 "지금
소제목은 임시 이름입니다"를 한 번 더 묻는데, 소제목 하나를 뽑는 가벼운 동작에까지
매번 확인창이 뜨면 정작 읽어야 할 그 경고를 안 읽게 된다.

**📂 소제목 미분류에는 안 붙인다.** 그 칸은 소제목이 아니라 대기실이라 ✏️🗑️▲▼도 이미
다 빠져 있고, 갈 곳이 안 정해진 기사를 보고서 형식으로 뽑을 이유가 없다. 기사가 하나도
없는 빈 커스텀 소제목도 마찬가지(복사할 게 없다).

아이콘 전환(누르면 1초간 ✓)은 기사 복사 버튼이 이미 쓰던 `.is-copied` 규칙과 JS
(`copyArticleIcon`)를 그대로 재사용했다 — 같은 일에 같은 피드백이어야 해서 쌍둥이 함수를
만들지 않았다. 수시만 그 화면에 같은 함수가 없어 `copyGroupText`를 새로 뒀다(카드 전체
복사가 쓰는 `alert`은 안 쓴다 — 소제목마다 있는 버튼이라 확인창이 번거롭다).

## 실시간현황 — 복사를 「⋯ 더보기」 밖으로 (2026-09-01)

이 화면은 기사를 그때그때 집어 다른 데 붙여넣는 일이 잦은데, 복사만 ⋯ 메뉴 안 첫 줄에
있어 매번 한 번 더 눌러야 했다(사용자 요청). 2026-08-13에 이 화면에 복사를 처음 넣을 때
메뉴 안에 둔 건 그때 그룹 칩이 새로 생기면서 아이콘이 자리를 다퉜기 때문인데, 실제로
자주 쓰이는 건 복사 쪽이었다.

**자리는 「📌 담아두기」 바로 오른쪽**(사용자 지정). 목업(`LIVE_COPY_BTN_MOCKUP.html`,
반영 후 trash-can으로 옮김)으로 세 안을 비교했다 —
A안(복사를 🗑와 같은 무게의 아이콘만, 담아두기 안 줄임, 액션 묶음 158 → 190px),
B안(복사도 글자 버튼, 담아두기 폭만 축소, 158 → 212px), C안(담아두기도 아이콘만,
158 → 161px). 폭은 전부 브라우저에서 잰 값이다.

**B안으로 확정.** 이 화면 오른쪽 묶음에서 **글자 버튼은 상태를 말하는 자리**
(담아두기/담아둠/스크랩됨/자동 선별/숨김)이고 **아이콘은 ⋯·🗑처럼 "가끔 한 번"인
자리**라, 자주 쓰는 복사를 아이콘 쪽에 두면 무게가 실제 사용 빈도와 어긋난다. C안은
가장 좁지만 담아두기의 다섯 상태를 색으로만 구분하게 돼 이 화면의 핵심 동작이 눈에 덜
걸린다 — 비교용으로만 목업에 넣었다.

**담아두기 폭은 92 → 84px, 라벨 글자는 안 줄였다.** "담기"로 줄이면 다른 화면의
「📌 담아두기」와 말이 갈린다. 84px은 **가장 긴 라벨("자동 선별")의 실제 렌더 폭**이라,
줄이면서도 다섯 상태 버튼이 여전히 같은 폭으로 맞는다 — 2026-07-29에 세운 "상태마다
버튼 폭이 들쭉날쭉해 그 옆 아이콘 자리까지 흔들리던 걸 min-width로 통일한다"는 규칙이
그대로 유지되는 하한이다(74px로 재보면 74~84px로 갈라진다). 최종 액션 묶음 폭은 실측
158 → 221px(목업은 버튼 간격 2px, 실제 화면은 6px이라 목업의 212보다 조금 넓다).

**피드백은 확정본·초안과 같은 함수를 쓴다** — 누르면 아이콘만 1초간 ✓로 바뀌는
`copyArticleIcon`/`.is-copied`. 메뉴 안에서 쓰던 글자 치환(구 `copyArticleText`,
라벨을 "복사했습니다"로 바꿈)은 버튼이 상시 노출되면서 폭이 흔들리게 되므로 버렸고,
그 함수를 부르는 곳도 없어져 같이 지웠다. 이름을 굳이 맞춘 건, 하는 일이 같은데 이름만
다른 쌍둥이가 있으면 한쪽만 고쳤을 때 화면끼리 피드백이 어긋나기 때문이다.

메뉴에는 🔄 원문 다시 불러오기 · ✏️ 제목·요약 직접 수정 둘만 남는다(둘 다 "가끔 한 번"이라
메뉴 뒤가 맞는 무게). 복사 본문·URL 줄 형식은 그대로다.

## 「AI 모든 기사 재분류」 확인창과 🤖 배지 (2026-09-01)

담당자가 초안에서 소제목이 하나도 안 잡힌 상태를 만났다 — 미분류 21건, 그 와중에 직접
만든 `<기타>`에 기사 2건을 옮겨둔 상태. 「AI 모든 기사 재분류」를 누르면 그 2건도
흩어지느냐는 질문에서 시작했다.

### 답은 "안 흩어진다"였고, 그게 문제였다

`_apply_forced_groups`는 LLM이 전체를 새로 묶은 **뒤에** 한 번 더 돌아서, 배정 기록
(`group_overrides`)을 지정 소제목으로 다시 끌어다 놓는다(`force_llm=True`여도 이 단계를
안 건너뛴다). 게다가 `<기타>`는 담당자가 만든 커스텀 소제목이라
`forced_group_target_names()`를 통해 **빈 소제목으로 미리 끼워 넣어지므로** 재분류 결과에
그 이름이 없어도 돌아갈 자리가 항상 있다.

그런데 확인창은 `직접 옮겨둔 기사 2건, 소제목 이름과 순서가 초기화됩니다`라고 겁을 주고
있었다. 실제로 초기화될 건 0건이었다. 반대로 배정 목적지가 "그 회차 분류 때 AI가 지은
이름"이면 이름 자체가 사라지면서 배정이 조용히 무시돼 진짜로 흩어지는데, 예전 계산은 그
둘을 뭉뚱그려 셌다.

실측(그날 쌓인 배정 기록 62건)해보니 **보호 31건 / 흩어짐 31건으로 반반**이었다. 즉 숫자가
늘 부풀어 있었고, 그러면 정작 진짜 위험한 날에도 담당자가 그 숫자를 안 믿게 된다. 확정본
14:00 회차로 대조해보니 예전 계산 7건이 새 계산 4건으로 줄었다.

**N=0이면 그 문장 자체를 뺀다.** "0건이라고 표시하면 안 되냐"는 물음이 있었는데(구현
난이도 때문이 아니라 — 그 분기는 이미 코드에 있었다), 셋 다 안 된다고 봤다: `0건은 새
소제목을 따라갑니다`는 문장이 성립을 안 하고, 이 앱은 이미 같은 판단을 여러 번 내렸으며
(📂 미분류 배지·📷 사진 추정 버튼·발행시각 불명 배너 전부 0이면 안 그린다), 무엇보다
**줄의 있고 없음 자체가 신호**다 — 뜨면 "뭔가 움직인다", 안 뜨면 "아무것도 안 잃는다".
0을 박아두면 그 신호가 죽고 담당자가 매번 숫자를 읽어 판단해야 한다.

### 문구 — 네 번 고쳐 확정했다

문장 하나하나가 사용자와의 왕복으로 결정됐다.

1. **`해당 없음` → `영향을 받지 않습니다`.** 처음 제안은 `(직접 만드신 소제목 … 해당
   없음.)`이었다. "더 명시적"이라는 의도였지만 실제로는 덜 명시적이다 — `해당 없음`은
   "무엇에 해당이 없는가"를 문장 안에 안 담아서, 읽는 사람이 앞 문장으로 되돌아가 뜻을
   이어붙여야 한다. 확인창이 답해야 하는 질문은 "내가 손으로 해둔 게 날아가나?"이고 그
   답은 직접적인 안심이어야지 서식 답변이면 안 된다. 게다가 4줄 중 셋이 `~합니다`인데
   마지막 괄호만 명사형이면 양식지의 빈칸 표기처럼 읽힌다.
2. **`그대로 유지됩니다`도 아니다.** 원래 문구인데, 직접 만든 소제목은 내용은 그대로지만
   목록에서의 **위치는 밀릴 수 있다**(다른 이름이 전부 새로 생기면서 `group_order` 버킷이
   빈다). "그대로"는 위치까지 약속하는 것처럼 읽혀 그만큼이 과장이었다. `영향을 받지
   않습니다`가 완결된 문장이면서 그 과장을 안 싣는다.
3. **`드롭다운으로` → `「다른 소제목」으로`.** ↑/↓와 헷갈리지 않게 기구를 짚어주자는
   방향은 맞았는데, 이 화면에 드롭다운이 넷이다(기사별 `다른 소제목` · 일괄이동 바 ·
   툴바의 보기 순서 `소제목별/시간순` · 📌 담아둔 기사의 `초안에 넣기…`). 뒤의 둘은 배정
   기록을 아예 안 건드린다. 화면에 실제로 적힌 라벨을 쓰면 특정도 되고 담당자가 눈으로
   찾을 수도 있다 — `…「AI 모든 기사 재분류」를 눌러주세요`가 이미 쓰던 관례다.
4. **폴백 갈래도 같은 안심 문구를 쓰고, 골격을 맞췄다.** 폴백 갈래만 `[상황] → [질문] →
   [안심]` 순서라 질문을 던져놓고 그 뒤에 근거를 주고 있었다. 세 자리 모두 `[상황] →
   [안심] → 빈 줄 → [질문]`으로 통일했다. 안심 문구가 이 갈래에서도 **사실**이라는 건
   확인했다 — 규칙 기반 경로도 마지막에 같은 `_apply_forced_groups`를 탄다. 그리고 이건
   경고가 아니라 안심이라, "없어진 걸 잃는다고 겁주지 말자"는 이 갈래의 설계 의도와
   부딪히지 않는다(오히려 누르기를 쉽게 만든다).

문구가 세 자리에 따로 적혀 있어 한 곳만 고치면 갈라지던 문제는 `app/config.py`
`RECLASSIFY_SAFE_NOTE` 상수 하나로 묶어 없앴다. 초안은 `_theme()`에 실어 그 파일의 format
자리 네 곳에 따로 등록할 필요가 없게 했다(색이 아닌 값이지만 이유는 PALETTE와 같다).

### 🤖 배지 — 이름이 겹치면 안 단다

합쳐지는 쪽은 어떻게 할 거냐는 물음도 나왔다. AI는 어디에도 안 맞는 기사를 담는 이름으로
늘 `기타`를 쓰므로, 담당자가 만든 `<기타>`와 이름이 겹치는 건 사실상 확정이다.

**AI 쪽을 `기타2`로 갈라놓자는 안은 기각했다**(사용자 제안, 논의 끝에 철회). 그 이름은
화면 표시가 아니라 복사·txt·텔레그램·이메일로 **실제 나가는 보고서 텍스트**다. 받는 사람
화면에 `<기타>`, `<기타2>`가 나란히 뜨면 둘 차이를 아무도 설명 못 하고 담당자가 매번 손으로
합쳐야 한다 — 기계가 지어낸 구분자가 보고서에 새면 안 된다는 원칙(CODING_CONVENTIONS §4)의
연장이다. 그리고 합쳐져도 두 `기타`는 뜻("어디에도 안 맞는 기사")이 같아 보고서가 정확하고,
담당자가 옮겨둔 기사는 `_apply_forced_groups`가 제자리로 돌려놓는다.

**대신 표시의 자기모순만 고쳤다.** 합쳐진 뒤 `<기타>` 하나가 파란 점선 테두리(=내가 만든
소제목)와 🤖 배지(=AI가 묶은 소제목)를 동시에 달았다. 배지 조건에 `group["name"] not in
custom_names`를 더해, 이름이 겹치면 "담당자가 직접 만들었다"는 더 정확한 출처만 남긴다.
수시 화면에는 `.ai-badge`가 애초에 없어 같은 문제가 없다.

첫 조언이 "그 커스텀 소제목 이름을 `기타` 말고 다른 걸로 바꿔두고 누르라"였는데, 그건
담당자가 알 필요 없는 내부 사정을 떠넘기는 것이라 철회했다(사용자 지적).

## 수시 모니터링(비정기 스크랩) — 설계 논의

**[Designed: 2026-08-13, 구현 완료]** 확정된 설계 명세는 [ADHOC_DESIGN.md](ADHOC_DESIGN.md), 화면 시안은 [ADHOC_MOCKUP.html](trash-can/2026-08-20_목업정리/ADHOC_MOCKUP.html), 구현 중 바뀐 세부 결정·잡은 버그는 바로 다음 "— 구현" 항목에 있다. 여기는 **왜 그렇게 됐는지와 기각한 대안**만 남긴다. 하루치 논의로 설계가 여러 번 뒤집혔고, 뒤집힌 이유 자체가 다음에 비슷한 판단을 할 때 쓸모 있기 때문이다.

- **출발점과 세 번의 재정의.** 사용자의 최초 요청은 "정책 담당자는 시간대보다 '이 키워드의 이 시간 구간 기사'를 원한다 + 갑자기 현안이 터지면 그것만 집중한다"였다. 이 요구가 논의 과정에서 **세 번 다른 물건으로 재정의됐다**: (1) 처음엔 "기간을 지정해 과거를 긁는 수집"으로 읽었고 — 그래서 `_is_today_kst` 잠금 해제와 1000건 한도 경고가 핵심 과제로 잡혔다. (2) 사용자가 "오후 3시에 기사가 올라올 테니 그것만 찾아줘"라는 실제 업무 예시를 주면서 **감시(watch)**로 재정의됐다 — 자동 새로고침·미래 시각 예약·백그라운드 스레드가 설계에 들어왔다. (3) 마지막에 "재경위가 3~4시에 열리니 4시 30분에 2:30~4:00을 한꺼번에 모은다"는 구체적 시나리오가 나오면서 **사후 일괄 수집**으로 최종 확정됐다 — 감시 관련 설계가 통째로 폐기됐고, 동시에 `naver_api.py`를 한 줄도 안 고쳐도 된다는 게 드러났다(당일 구간 수집은 놓친 회차 보충 실행과 구조가 같아 `before` 초과 기사를 `continue`로 건너뛰는 기존 로직이 이미 지원한다). **교훈: 사용자가 든 구체적 업무 예시 하나가 추상적 요구 설명 열 줄보다 설계를 훨씬 크게 바꿨다.** 두 번의 재정의 모두 예시가 나온 직후 일어났다.
- **왜 정기 플로우를 복제하지 않았나.** 첫 검토에서 바로 막힌 지점 — 큐레이션 상태가 `group_overrides.json`(URL 기준 전역) · `hidden_articles.json` · `custom_groups.json` · `group_order.json` · `group_labels.json` · `preview_order.json` · `undo` 스택 · `llm_classifier._last_names`까지 **전부 전역**이고, 이게 전부 "동시에 살아있는 회차는 하나뿐"이라는 가정 위에 서 있다. 비정기를 정기 복사본으로 만들면 비정기에서 숨긴 기사가 정기 확정본에서도 사라지고, 비정기 소제목이 정기 초안 드롭다운에 뜨고, ↩ 되돌리기 한 번이 다른 화면 상태를 되돌린다. 제대로 하려면 저 8개 저장소 전부에 `scope_id`를 넣고 `settings_server.py`(3,358줄)의 큐레이션 핸들러를 전부 스코프 인지하게 고쳐야 했다. **대신 "카드 하나 = 파일 하나"의 자기완결 문서 모델로 갔다** — 정기는 "오늘 회차가 하나뿐 + 네 화면이 그 상태를 공유"라서 종류별 전역 파일이 맞고, 비정기는 "카드가 여러 개 + 각각 독립"이라 카드별 파일이 맞다. 구조가 다른 게 정상이고, 그 차이가 결정들을 코드가 아니라 **데이터 모양으로** 강제한다.
- **3단계 → 2단계.** 정기의 초안이 존재하는 이유는 "수집이 아직 안 끝났기 때문"이고 그 끝을 **시계가 정한다**. 비정기는 [수집]을 누르는 순간 시간창이 이미 닫힌 과거라 "수집 중" 상태가 물리적으로 존재하지 않는다 — 초안을 만들면 빈 껍데기가 된다. 대신 "조건을 바꿔 몇 번이든 재수집"은 별도 단계가 아니라 **같은 화면 안의 반복 동작**으로 정리했다.
- **실시간 화면을 복제하지 않기로 한 근거.** 사용자가 "실시간 메뉴를 2개 만들면 네이버 API도 2번 호출되는 것 아니냐"고 우려했는데, 호출 비용은 실제 문제가 아니었다(키워드 2~3개면 몇 초, 일일 한도 25,000 대비 무시). 진짜 이유는 두 가지다 — (1) 실시간 현황의 존재 이유는 "정기 회차 사이에 뭐가 올라오는지 **몰라서** 궁금하다"인데 비정기는 담당자가 시간창을 **이미 알고** 지정하므로 실시간이 풀어주는 문제가 애초에 없다, (2) 화면이 2개면 담당자가 "지금 어느 화면을 봐야 하지"를 매번 판단해야 하는 인지 비용이 API 비용보다 훨씬 크다. 게다가 카드의 시간창 끝을 비우면 `after=시작, before=None`이 되는데 이건 `live_renderer.py`가 하는 호출과 완전히 같은 모양이라, **카드 자체가 이미 실시간**이다.
- **기각한 안 — 실시간 상단의 "담을 곳" 셀렉트.** 실시간에서 발견한 기사를 비정기로 보내는 방법으로 처음 제안했던 안이다(상단에서 목적지를 한 번 고르면 모든 📌 버튼이 그리로 감). 행마다 드롭다운을 다는 대안보다는 나았지만 — 실시간 행은 이미 그룹 태그·📌·⋯더보기로 빽빽해서 드롭다운을 더 넣을 자리가 없다 — **사용자가 더 나은 안을 냈다**: 이미 초안·확정본 두 화면이 공유하는 "📌 담아둔 기사"를 비정기 카드에도 띄우는 것. 이게 나은 이유는 **담는 행위와 목적지 결정을 분리**하기 때문이다. 실시간을 훑다 기사를 발견하는 순간엔 그게 정기 건인지 비정기 건인지 아직 모를 수 있고, 심지어 둘 다일 수도 있다. 셀렉트 안은 "담을 때 목적지를 안다"를 전제했는데 그 전제가 틀렸다. 덤으로 모드 상태가 사라져 "목적지를 바꿔놓고 잊어버려 엉뚱한 데 담는" 사고 경로도 통째로 없어졌고, 실시간 화면은 한 줄도 안 바뀐다. **단 pop→copy 변경이 필요하다** — 지금 승격은 `pop_manual_article`로 목록에서 빼는데, 정기 초안/확정본은 "같은 회차의 두 얼굴"이라 pop이 맞지만 비정기는 정기와 독립된 아카이브라 같은 기사가 양쪽에 있는 게 정상이다.
- **소제목을 "없음"에서 "버튼으로만"으로.** 처음엔 "사안 자체가 하나의 주제라 소제목은 중복"이라고 판단해 빼려 했다. 사용자가 "회의에서 3가지를 논의하다 보니 AI 소제목이 필요하다"는 반례를 들었고, 그게 정확히 LLM 분류 프롬프트가 하도록 설계된 일(쟁점 기준 분리)이라 판단이 뒤집혔다. **기사가 12건이면 원래 판단이 맞고 40건이면 사용자 말이 맞다** — 그래서 정답은 "없음"도 "항상"도 아닌 **자동 0회 + 버튼 1회**다. 12건짜리 카드는 아무도 안 누르니 비용 0, 40건짜리는 누르니 1회 — 담당자가 필요를 판단하므로 과금이 자기조절된다. 이 결정으로 `_last_names` 전역 오염 문제가 다시 살아났고(앞서 "소제목을 빼면 통째로 사라진다"고 했던 것을 취소), 저장/복원이 필수 항목으로 남았다.
- **요약을 소제목 아래에서 페이지 맨 아래로.** 사용자 지적 — "담당자는 제목만 보고 흐름을 봐야 하는데 요약을 보면 정보 유입으로 스크리닝에 방해가 된다. 이건 요약 앱이 아니라 모니터링을 효율적으로 하기 위한 앱이다." 정기(초안·확정본)가 이미 하단 블록 방식이라 일관성도 맞았다. 같은 이유로 `AI가 추출한 주요 키워드` 블록은 아예 넣지 않기로 했다 — 검색어를 담당자가 직접 정했으므로 AI가 키워드를 다시 뽑아줄 이유가 없다.
- **발송 기능 철회.** 최초 요구는 "내가 수동 발송해서 정책 담당자에게 쏜다"였고, 그에 맞춰 우하단 플로팅 (발송) 버튼과 전용 수신자 설계까지 나왔다. 그런데 이게 확정한 2단계 플로우와 부딪쳤다 — 카드에서 바로 발송하면 [확정] 단계가 갈 곳이 없어진다. 이걸 짚자 사용자가 **발송 자체를 철회**하고 "복사/다운로드로 그친다"로 정리했다. 결과적으로 텔레그램·이메일 연동 코드가 통째로 불필요해졌고, "수신자를 정기와 공유할 것인가"라는 미결 질문도 함께 사라졌다. 다만 "이 카드는 끝났다"를 표시할 방법이 없어져 **[보관 (지난 사안으로)]** 버튼을 새로 뒀다.
- **API 동시 호출 — 과대평가했다가 사용자 지적으로 축소.** 정기와 비정기가 겹치면 `ThreadPoolExecutor(max_workers=8)`가 호출마다 새로 생성되므로 8+8=16 동시가 되고, 위 Collection 항목의 실측(동시성 15에서 429가 20%)상 위험 구간이라고 판단해 전역 세마포어를 제안했다. 사용자가 **"실시간도 초안도 비정기도 전부 사람이 액션할 때만 크롤링하는 것 아니냐, 그럼 걱정할 문제가 아니지 않냐"**고 반문했고, 확인 결과 맞았다 — 자동으로 도는 네이버 호출은 스케줄러의 회차 수집(`main.py` `_scrape_and_render`) 하나뿐이고 초안·실시간은 전부 화면 요청 시점이다(실시간 콜드스타트 백그라운드 스레드가 하루 첫 1회 예외). 하루 4회 × 7초 = 28초라 겹칠 확률이 0.1% 수준. 다만 두 가지가 남는다 — 정기 수집이 정시에 돌고 비정기를 쓰는 시점도 정시 근처라 **독립 사건이 아니고**(재경위 4시 종료 → 4시 30분 수집 → 16:30 슬롯과 정확히 겹침), 탭이 여러 개면 앞선 검색이 안 끝났을 때 다음 걸 눌러 겹칠 수 있다. **결론적으로 세마포어는 유지하되 위치가 "성능 최적화"에서 "만에 하나의 429 폭주 방지"로 바뀌었고, 이 지적 덕에 대기 안내 UI 하나를 설계에서 뺐다** — 거의 안 뜰 화면을 만들 이유가 없다. 표현도 정정했다: 세마포어가 대기를 *만드는* 게 아니라 원래 있던 혼잡을 429 없이 줄 세우는 것이다.
- **코드 배치 — 별도 프로젝트가 아니라 하위 패키지.** media_report(동향 보고)가 별도 프로젝트인 것과 대비된다. media_report는 데이터 흐름이 단방향(export JSON)이라 분리가 되지만, 비정기는 담아둠 공유·정기로 밀어넣기로 **양방향으로 얽혀** 있다. 별도 폴더로 빼면 `settings_server.py`가 서빙하는 오리진이 갈라져 담아둠 공유가 사실상 불가능해지고, 담당자가 `python3 main.py`를 두 번 켜야 한다. 대신 `app/adhoc/` 하위 패키지 + **단방향 의존**(`app/adhoc/* → app/*`만 허용)으로 격리했다 — 코드베이스가 뒤엉키는 문제는 폴더를 나눠서가 아니라 import 방향으로 푸는 문제다.
- **같이 드러난 정기 쪽 결함(별도 과제).** 논의 중 사용자가 "다음 날 아침에 어제 밤 11시 기사도 보고 싶다"고 했는데, 확인해보니 `app/scraper.py`의 `after_dt = kst_today_at(window_start)`가 **항상 오늘 날짜**라 00:00–09:00 회차의 하한이 오늘 00:00이고, 어제 23:50 기사는 어느 회차에도 안 잡힌다. 이건 비정기로 우회할 게 아니라 **정기 첫 회차의 `start`가 전일을 가리킬 수 있게 하는 별도 수정**이 맞다 — 매일 발생하는 정기 업무 결함을 비정기 수동 작업으로 메우는 건 자동 모니터링 앱으로서 지는 것이기 때문. 이번 범위 밖으로 남겨뒀다.

## 수시 모니터링(비정기 스크랩) — 구현

[Implemented: 2026-08-13] `app/adhoc/card.py`(카드·사안 CRUD) → `collector.py`(수집·병합) → `renderer.py`/`archive_renderer.py`(화면) → `routes.py`(settings_server.py 연결) → `classifier.py`(LLM 연동) → `undo.py` 순서로 만들었다. 매 파일마다 실제 Claude API·실제 서버로 검증했다(스크래치패드 렌더링이 아니라 `python3 main.py`를 띄우고 브라우저로 폼을 제출해 데이터 파일 변화까지 확인) — 그 과정에서 설계 문서만 봐서는 안 보였던 것들이 몇 개 나왔다.

- **`_build_prompt`가 `max_subheadings` 인자를 무시하던 기존 버그를 발견.** `app/llm_classifier.py`의 프롬프트 생성 함수가 파라미터를 받고도 실제로는 항상 전역 `MAX_SUBHEADINGS`(8)를 프롬프트 문장에 박아넣고 있었다 — 정기가 항상 기본값(8)으로만 호출해 우연히 안 드러났던 잠재 버그다. 수시가 5로 호출하는 게 계기가 되어 잡혔다. `_build_prompt(articles, max_subheadings, previous_names)`로 파라미터화(정기 동작은 완전히 그대로, 프롬프트 텍스트가 이미 "8"이었으므로).
- **`_last_names` 격리 — 메모리만 되돌리고 디스크는 안 맞췄던 것을 실측으로 잡음.** ADHOC_DESIGN.md §6.5가 요구한 "저장/복원"을 처음엔 메모리 변수만 저장해뒀다 복원하는 걸로 구현했는데, `classify_with_llm` 내부가 성공 시 `_persist_cache_locked()`로 캐시 파일까지 같이 쓴다는 걸 놓쳤다 — 카드 분류 직후 `data/llm_classification_cache.json`을 직접 열어보고서야 정기용 `last_names`가 이 카드의 소제목 이름으로 남아있는 걸 확인했다. 앱을 재시작하면 이 오염된 디스크 값이 다시 메모리로 로드돼 정기 쪽을 오염시켰을 것이다. `_cache_lock`을 잡고 복원 직후 재저장하도록 고치고, 서버를 재시작한 뒤 다시 재현해 고쳐졌음을 확인했다.
- **자정 잠금 경고 조건 버그.** `is_stale` 판단을 `card["collect_date"] != 오늘`이 아니라 `card["status"] != "archived"`로 잘못 짰다 — 오늘 만든 카드에도 매번 거짓 경고가 뜨는 상태였다. 실서버 스크린샷에서 바로 드러나 즉시 수정(오늘 카드는 경고 없음/재수집 정상, 어제 날짜로 조작한 카드는 경고+재수집 입력 잠금 — 둘 다 재확인).
- **아이콘이 화면을 뒤덮는 렌더링 버그.** `renderer.py`의 공용 스타일 블록에 다른 렌더러 파일마다 있는 `.ic { width:1em; ... }` 기본 규칙을 빠뜨려, SVG 아이콘이 원본 크기로 커져 화면을 가렸다. 스크린샷 찍자마자 바로 보여서 잡았다.
- **소제목 배정 버튼 — LLM 재분류는 "파괴적이라서" 카드에도 안전장치가 필요했다.** 처음 짤 때는 "소제목이 없으면 처음부터, 있으면 증분"까지만 생각했는데, "+ 소제목 추가"를 붙이면서 커스텀 소제목이 상한 계산에서 빠지면 AI가 그 존재를 모른 채 5개를 다 채워버리는 문제가 보였다 — `classify_card`가 `current_group_names`(add_custom_group과 동일 기준)로 남은 자리를 계산하도록 통일했다. 실제 LLM 호출로 "커스텀 소제목은 AI가 절대 안 건드리고, 총 개수는 상한을 넘지 않는다"를 확인했다.
- **체크박스 일괄이동 = 소제목 재배정, "묶음 위/아래로 밀기"는 아님.** 정기에는 이름이 비슷한 기능이 둘 있다(`bulk_reassign_group` — 선택한 걸 다른 소제목으로, `bulk_move_articles` — 선택한 묶음을 위/아래로 한 칸씩). 사용자가 "체크박스 일괄이동·↑/↓ 순서조정"을 요청했을 때 두 항목을 별개로 읽어 전자만 구현했다 — 후자(묶음 셔플)는 더 니치한 기능이라 요청받으면 추가하기로 하고 응답에 명시했다.
- **기사 순서는 별도 `order` 필드가 아니라 `card["articles"]` 리스트 안 위치로 표현.** ADHOC_DESIGN.md 스키마 예시엔 `"order": 0` 필드가 있었지만, 정기의 `app.curation.move_article`을 다시 읽어보니 정기도 순서 필드 없이 저장 리스트의 실제 위치 자체가 표시 순서였다 — `_article_groups`가 이미 리스트 순서를 그대로 훑어 소제목별로 묶고 있어서, 굳이 필드를 유지하지 않고 두 기사의 리스트 위치만 스왑하는 쪽이 더 단순하고 정기와 일관됐다. 스키마의 `order` 필드는 손대지 않고 그냥 안 쓴다(제거는 더 큰 변경이라 보류).
- **되돌리기 — 카드 모델 덕에 정기보다 단순해짐.** `app/undo.py`(정기)는 전역 파일 6개 + LLM 메모리 캐시를 따로 모아 스냅샷해야 한다(화면이 회차 파일이 아니라 캐시에서 매번 재분류를 읽으므로). 카드는 `render_card_page`가 `article["group"]`을 그대로 읽지 재분류하지 않으므로, **카드 JSON 전체를 통째로 스냅샷**하는 것만으로 충분했다 — `undo.py` 자체가 예상보다 짧게 끝났다. 대신 스냅샷 하나가 기사 본문까지 포함해 무거워서(726건 테스트 카드가 520KB) 상한을 정기(20)보다 낮은 10으로 잡았다.
- **`preview_start`가 `~/Desktop` 프로젝트 파일에 EPERM.** 실서버(`python3 main.py`) 검증을 표준 방식(`.claude/launch.json` 기반 `preview_start{name}`)으로 띄우려 하자 macOS 권한 문제로 실패했다(Bash 도구는 같은 경로에 문제없이 접근됨 — 도구 프로세스 자체의 폴더 접근 권한 차이로 추정). 서버는 Bash로 직접 백그라운드 실행하고, `preview_start{url: "http://127.0.0.1:8765/..."}`로 브라우저만 연결하는 방식으로 우회했다 — 이후 모든 실서버 검증(카드 생성·수집·소제목 배정·되돌리기 등)이 이 방식으로 이뤄졌다.
- **브라우저 자동화 클릭 좌표가 두 번 어긋남.** `computer{action:"left_click"}`이 `ref`를 다시 읽지 않고 이전 스크린샷 좌표를 재사용해 버튼을 빗맞힌 사례가 두 번 있었다(검색어 입력 폼의 "+ 추가", 소제목 배정 폼의 재제출) — 둘 다 `form.requestSubmit()`을 직접 JS로 호출하는 방식으로 우회해 재현·확인했다. 실제 코드 버그는 아니었지만, 검증 중 "안 눌린 건지 안 되는 건지" 구분하려고 `read_network_requests`로 실제 POST가 나갔는지 먼저 확인하는 습관이 여기서 자리 잡았다.
- **아직 안 만든 것(2026-08-13 시점).** ADHOC_DESIGN.md §6.10의 `app/naver_api.py` 전역 동시-호출 세마포어는 "만에 하나의 429 폭주 방지"로 우선순위가 낮다고 판단해 이번 구현 범위에서 뺐다 — 실제로 겹칠 확률이 낮다는 게 설계 논의에서 이미 확인됐기 때문(위 "API 동시 호출" 항목 참고). `app/icons.py`의 원문 열기 아이콘은 별도로 추가할 필요가 없었다 — 정기 쪽 8/13 작업에서 이미 `external_link`가 추가돼 있어 그대로 재사용했다.
- **[Added: 2026-08-14] 세마포어 추가 + 소제목 삭제 대상을 정확히 잡지 못했던 테스트 실수.** 사용자가 "CLAUDE.md 남은 작업 우선순위 판단해서 진행하되 기존 기능에 영향 있으면 확인해달라"고 한 요청에 따라, `app/naver_api.py`(정기 공유 파일)를 건드리는 세마포어와 설계가 없는 사안 병합 두 가지를 먼저 물었다 — 세마포어는 "지금 추가", 사안 병합은 "지금은 보류"로 답 받았다. 세마포어를 실제로 넣으면서 원래 계획("`app/icons.py`에 원문 열기 아이콘 추가·`app/naver_api.py`에 전역 세마포어" 두 곳 모두 손대야 한다던 초기 설계 메모)과 달리 **네이버 API 호출(`_search_one_keyword`의 `NAVER_NEWS_API_URL` 요청)에만** 걸고 `_fetch_full_title`/`fetch_full_title_and_summary`(개별 언론사 사이트를 호출하는, 네이버와 무관한 요청)는 뺐다 — 코드를 실제로 읽어보니 이 둘은 네이버 rate limit과 아무 관계가 없어서, 여기 세마포어를 걸면 오히려 무관한 병목(느린 언론사 사이트 하나가 네이버 검색 슬롯을 붙듦)이 생겼을 것이다. 20개 스레드를 동시에 돌려 실제 최대 동시 실행 수가 8을 넘지 않는지 직접 재서 확인했고, 세마포어 적용 후에도 실제 네이버 검색이 정상 동작하는지(68건 실수집) 재검증했다.
- 소제목 ▲▼·빈 소제목 삭제를 검증하던 중 삭제 함수의 "기사가 있으면 차단" 분기를 테스트하다 첫 시도에서 **잘못된 이유로 통과**한 걸 스스로 잡았다 — 애초에 `custom_groups`에 없는(AI가 만든) 소제목 이름을 지우려 했더니 "찾을 수 없음" 분기가 먼저 걸려 "기사 있음" 분기는 실제로 실행도 안 됐는데, 겉보기엔 "차단됐다"는 결과만 같아서 처음엔 통과로 착각했다. 실제로 커스텀 소제목을 만들고 기사를 그 안에 넣은 뒤 다시 재현해 올바른 분기를 확인했다 — "테스트가 통과했다"와 "의도한 코드 경로를 테스트했다"가 다를 수 있다는 사례로 남긴다.
- **[Added: 2026-08-13] 소제목 ▲▼ 순서조정 + 빈 커스텀 소제목 삭제.** 사용자가 "CLAUDE.md의 남은 작업 중 우선순위를 판단해서 진행하되, 기존 기능에 영향을 주거나 설계 변경이 필요하면 먼저 확인해달라"고 요청 — `app/naver_api.py` 세마포어(정기 공유 파일을 건드림)와 사안 병합(설계가 아예 없음)은 확인이 필요하다고 판단해 보류하고, 이 둘은 `app/adhoc/*`에서만 끝나고 스키마(`group_order`)·UI 패턴(정기의 "빈 소제목만 🗑️ 삭제")이 이미 있어 판단 없이 진행했다. 구현하면서 소제목 표시 순서 계산 로직이 `renderer.py`의 `_article_groups` 안에만 있다는 걸 발견했다 — ▲▼ 버튼이 "지금 화면에 보이는 순서"를 알아야 이웃을 스왑할 수 있는데, 그 계산을 두 곳(렌더링용 vs 버튼용)에 따로 구현하면 미묘하게 어긋날 위험이 있어 `card.ordered_group_names`로 뽑아 card.py 한 곳에만 두고 렌더러가 그 함수를 가져다 쓰도록 리팩터링했다(먼저 존재하던 함수를 옮긴 것일 뿐 동작은 그대로 — 실제로 옮긴 뒤 회귀 테스트와 실서버 화면이 이전과 동일하게 나오는 것을 확인). `remove_custom_group`은 기사가 있으면 지울 수 없다(먼저 다른 소제목으로 옮겨야 함) — 어디로 갈지 안 정하고 조용히 사라지게 둘 수 없다는 원칙(CODING_CONVENTIONS.md §3) 그대로.
- **[Added: 2026-08-19] 기사 행 — 요약문 펼쳐보기 추가 + URL 통짜 줄 제거.** 사용자가 캡처 두 장(수시 vs 정기 확정본)을 붙여 "수시에서도 요약을 보고 싶고, URL은 클릭으로 원문을 열게 하면 공간이 절약될 것"이라고 지적 — 정기는 이미 `<details>/<summary>` 제목 클릭 펼침 + "원문보기" 링크 방식인데 수시만 URL을 그대로 텍스트로 박아놓고 요약은 아예 안 보여주고 있었다. A안(정기와 완전히 동일한 구조 — 제목 클릭=펼침, 시각 옆 원문보기)·B안(원문보기는 같고 요약만 별도 아이콘 토글) 두 시안을 목업으로 만들어 실제 클릭 가능한 HTML로 보여주고 실측 높이(593px 폭 기준 한 건당 21.2px 절약)까지 붙였더니 A안 채택. 구현은 `app/adhoc/renderer.py`의 `_render_article_row`를 정기 `render_article`과 같은 `<div class="article"><details><summary>...</summary><p class="a-summary">...</p></details></div>` 구조로 바꾸고, 요약 색은 정기와 똑같이 `:has(details[open])`로 열렸을 때만 남색(`#3B5FA0`)을 준다. **요약 데이터는 이미 있었다** — `app/adhoc/collector.py`가 수집 시 `article["summary"]`를 카드 JSON에 저장하고 있었는데 화면에서만 안 쓰고 있었을 뿐이라 재수집·API 호출이 전혀 필요 없었다. 수시 행은 체크박스·다른 소제목 드롭다운·복사·라벨·숨기기·↑↓가 전부 `<summary>` 안에 같이 들어가 있어(정기와 달리 라벨 폼까지 포함), 이 중 어느 걸 눌러도 펼침이 같이 안 일어나게 각 인터랙티브 요소 하나하나에 `event.stopPropagation()`을 새로 붙였다 — 실서버가 아닌 격리된 `new_card()` 픽스처로 렌더링해 브라우저에서 실제 클릭 테스트(제목 클릭=펼침/닫힘, 복사·숨기기 버튼 클릭=펼침 안 됨, 요약 없는 기사="저장된 요약이 없습니다" 안내)까지 확인했다. 액션 줄의 ↗ 원문 열기 아이콘은 새 "원문보기" 링크와 같은 일을 하는 두 번째 입구가 되므로 제거했다(아이콘 6개→5개).
- **[Added: 2026-08-19] 상단바 — 수시에 들어오면 홈으로 돌아갈 길이 화면에 없었다.** 사용자가 캡처 한 장("← 수시 보관함" 링크 하나뿐인 상단바)을 붙여 "넘 불편해"라고 지적 — 정기 화면은 진작부터 상단바에 3개 링크(홈/실시간/다른 화면)가 있는데 수시(`app/adhoc/*`)만 보관함으로 되돌아가는 링크 하나여서, 수시로 들어온 담당자가 홈으로 나가려면 브라우저 뒤로가기나 주소창을 써야 했다. 화면마다 갈 곳을 사용자가 직접 지정했다 — 수집 결과(`/adhoc/card`)는 홈·새 수집·수시 보관함, 새 수집(`/adhoc/new`)은 홈·수집 결과·수시 보관함, 수시 보관함(`/adhoc`)은 **홈 하나만**(나머지로 가는 길은 그 화면 본문의 목록·버튼이 이미 제공하므로 중복을 뺐다). 지금 보고 있는 화면 자신은 링크에서 빼는데, 이건 정기 확정본이 상단바에 "확정본" 링크를 안 다는 것과 같은 규칙이다.
  - **"수집 결과" 링크의 목적지를 정하는 게 실제 판단 지점이었다.** 정기는 확정본이 회차마다 하나로 정해지지만 수시는 "지금 보고 있어야 할 카드"가 하나로 안 정해진다 — 가장 최근에 만든 카드로 보내기로 했고, 카드가 하나도 없으면 링크 자체를 안 그린다(갈 데 없는 링크를 회색으로 남기는 것보다 없는 편이 낫다). 이걸 기존 `list_cards()`로 구하면 **카드를 전부 파싱한다**(실측 365개 1,415ms, 위 archive_renderer 항목의 그 수치) — 화면마다 붙는 링크 하나 만들자고 치를 비용이 아니어서, 카드 id가 `YYYYMMDD-HHMMSS-hex`라 파일명 사전순이 곧 시간순이라는 점을 이용해 **파일을 한 개도 열지 않는** `card.latest_card_id()`(glob + max)를 새로 만들었다(CODING_CONVENTIONS.md §1).
  - 여백까지 정기와 같게 맞추라는 요청에 따라 `.topbar`를 정기(`app/renderer.py`)와 같은 `position:fixed` + `.topbar-inner`(max-width 800px, padding 12px 24px, space-between) 구조로 바꿨고, 고정 바에 본문이 가리지 않도록 `.container`의 padding-top을 24px→84px(정기의 60px + 원래 여백 24px)로 올렸다. 세 화면 모두 브라우저로 열어 링크 구성과 본문이 상단바에 안 가리는 것을 확인했다.
- **[Added: 2026-08-20] 수시 보관함 — 사안명 검색 + 기간 조회.** "사안별로 볼 수 있게" → "월 선택도" → "일별/7일/월별 단위로, 날짜도 직접" → "시작일~종료일 직접 입력(8.11~8.17)" → "1일/7일/1개월/전체 + 달력칸만 남기고 나머지는 빼자" → "사안명 검색도 추가, 내보내기 버튼은 조건 상자 밖으로" 순서로 목업(`ADHOC_ARCHIVE_MONTH_MOCKUP.html`)을 다섯 번 갈아엎은 뒤 "실제 코드로 반영해줘"로 마무리됐다 — 최종 형태가 처음 요청("월별로 보고 싶다")과 꽤 멀어져서, 실제 구현은 마지막 목업 스펙만 보고 만들었다.
  - **사안 → 회차 2단이 사안 → 날짜 → 회차 3단으로.** 같은 사안을 하루에 여러 번 재수집하는 경우(예: 오전/오후 따로)가 실제로 있어서, 날짜 층 없이 회차를 바로 늘어놓으면 기간을 좁혀도 목록이 뒤섞여 읽힌다. `card.list_cards()`가 이미 최신순으로 정렬해 돌려주므로, 같은 사안·같은 날짜의 카드는 정렬 안에서 항상 서로 인접하다는 성질을 이용해 재정렬 없이 한 번의 순회로 사안→날짜 dict를 만든다(`_group_by_issue_and_date`).
  - **"오늘"은 서버가 계산한다, JS가 아니라.** 목업 단계에서는 브라우저 JS가 `TODAY` 상수를 하드코딩해 1일/7일/1개월 버튼의 실제 날짜를 계산했는데, 실제 구현에서는 `1일`/`7일`/`1개월`/`전체` 버튼을 GET 폼의 `<button type="submit" name="preset" value="day">`로 만들고 서버(`_preset_range`)가 `datetime.now()` 기준으로 실제 start/end를 계산해 되돌려준다 — 기기 시계가 어긋나 있어도 항상 서버 기준 오늘과 일치하고, URL만 봐도(`?preset=day`) 무슨 조회인지 알 수 있다.
  - **복사/txt/엑셀을 결과 전체·사안·날짜 세 층에 다 붙이면서, 처음 세운 "전용 export 엔드포인트 3개(복사/txt/엑셀) 신설" 계획을 버렸다.** `app/history_renderer.py`의 회차별 복사·다운로드 버튼(`_slot_export_actions_html`)을 다시 읽어보니, 이 화면처럼 **지연 로딩이 없는(전부 한 번에 렌더링하는) 화면**은 렌더링 시점에 이미 계산한 텍스트/rows를 hidden input에 심어 기존 공용 `/download-text`·`/download-excel`(둘 다 settings_server.py에 이미 있는 엔드포인트)로 그대로 보내는 패턴을 쓰고 있었다 — 전용 재조회 엔드포인트(`/download-excel-history` 같은)는 **지연 로딩이라 렌더링 시점에 rows를 구워둘 수 없는 화면**에만 쓰는 것이었다. 수시 보관함은 이미 전부 렌더링하므로 후자 이유가 없다 — 계획했던 새 라우트 3개를 전부 걷어내고, 기존에 이미 있던 라우트 2개(`/download-text`, `/download-excel`)에 값만 채워 보내는 쪽으로 바꿨다(`build_plain_text_for_cards`, `_export_buttons_html`). 결과적으로 `routes.py`는 새 코드가 아니라 옛 `/adhoc/download-excel-archive`(issue_ids 기반) 삭제만 남았다 — 순수 삭제로 끝난 셈.
  - **`<summary>` 안 버튼 — `stopPropagation()`만 쓰고 `preventDefault()`는 안 쓴다.** `_slot_export_actions_html`의 기존 주석("클릭이 토글로 번지는 것만 막으면 된다")을 그대로 따랐는데, 처음엔 "toggle도 막고 제출도 막아야 하나" 헷갈려 `preventDefault()`까지 걸 뻔했다 — 그러면 `<button type="submit">`의 기본 동작(form 제출) 자체가 취소될 위험이 있다(둘 다 같은 클릭 이벤트의 default action이라, 버블 단계에서 먼저 `preventDefault()`가 불리면 늦게 실행되는 submit도 취소된다). 기존 코드가 이미 `stopPropagation()`만으로 안전하게 동작하고 있다는 걸 보고 그대로 재사용 — 임시 서버(`ThreadingHTTPServer`, 격리된 `DATA_DIR`)를 띄워 헤드리스 브라우저로 실제 클릭 검증까지 했다: 날짜 층 "복사" 클릭 → `<details>`는 계속 열린 채 유지(토글 안 됨) → "txt"/"엑셀" 클릭 → `read_network_requests`로 `POST /download-text`·`POST /download-excel` 둘 다 200 OK 확인.
  - **`issues.json`을 한 번만 읽도록 리팩터.** 초안엔 사안 이름을 찾을 때마다(`card.find_issue`) 매번 파일을 다시 읽었는데, 사안명 검색(`_filter_cards`)에서도 별도로 한 번 더 읽고 있었다 — `render_archive_page` 진입 시 `card.load_issues()`를 딱 한 번 불러 `issues_by_id` dict + `_filter_cards`/`_empty_suggestion` 인자로 넘기도록 정리했다(카드 쪽에 이미 있던 "한 번 읽고 메모리에서 나눈다" 원칙, CODING_CONVENTIONS.md §1을 사안 목록에도 그대로 적용).
  - 결과 0건일 때 "왜 없는지"를 보여주는 로직(사안명은 맞는데 기간 밖 vs 아예 없음)은 이미 있는 `all_cards`/`all_issues`를 메모리에서 다시 필터링하는 것뿐이라 추가 파일 읽기가 들지 않는다.

## 수시 모니터링 — 검색어 편집 · 재수집이 병합에서 교체로 (2026-08-20)

"수집 결과 화면에서 고칠 수 있는 게 시간밖에 없다 — 기사가 너무 많이 나와서 검색어를
고치고 싶다"는 요청에서 시작해, 결국 [ADHOC_DESIGN.md](ADHOC_DESIGN.md) §6.4의 대원칙
("재수집은 병합 — 교체 절대 금지")을 뒤집는 데까지 갔다. 확정 스펙은 그쪽 §6.4~§6.4c에
있고, 여기엔 어떻게 그 결론에 도달했는지만 남긴다.

**시작은 어긋난 전제였다.** 요청 문장이 "기사가 너무 많다"인데 예시는 "공공기관 단어를
추가"였다 — 수시는 OR 고정이라 추가하면 오히려 늘어난다. 이 모순을 짚고 나서야
"담당자가 원한 건 AND였다"가 드러났다. 검색엔진 경험상 **단어를 더하면 좁아지는 게
일반적인 직관**이고, 이 앱의 OR가 오히려 예외적인 쪽이었다(모니터링 도구라 놓치면 안 되므로).

**실측이 두 가지 설계 전제를 무너뜨렸다.** 저장된 카드 18개 전수 조사:

| 기사 | 검색어 | 숨김 | 사안 |
|---:|---:|---:|---|
| 1,100 | 2 | 0 | 비상대책 |
| 732 | 1 | 0 | 재정경제부 |
| 187 | 5 | 0 | 테스트 8/19 |

- **카드 18개 전체에서 숨긴 기사가 총 1건.** §6.4가 보호하려던 "담당자 노동 15분"은 실제로
  일어나지 않았다 — 1,100건을 하나씩 정리하는 일은 아무도 시작하지 않는다. 보호할 노동이
  없는 규칙이 정작 필요한 기능을 막고 있었다. 이 숫자를 보기 전에는 "빼기(opt-out)와
  담기(opt-in)는 판단 횟수가 같으니 빼기가 낫다"고 논리로만 답했었는데, 횟수가 같아도
  1,100번은 **아예 시작이 안 되는 숫자**라는 걸 실측이 알려줬다(CODING_CONVENTIONS §1).
- **§6.1의 "AND 없음 — 급하게 걸었다가 0건 나오는 게 최악"도 설계 시점의 추측이었다.**
  실제로 겪은 문제는 0건이 아니라 1,100건이었다. 다만 그 근거가 **카드를 만드는 시점**에는
  여전히 유효해서, 새 수집 화면은 OR 고정을 그대로 두고 카드 안에서만 AND를 허용하는
  것으로 갈랐다.

**AND의 성질을 코드에서 확인하고 설계가 한 번 바뀌었다.** 처음엔 "좁히기는 저장된
`matched_keywords`로 로컬 계산하면 API 호출 0회"라고 답했는데, `app/naver_api.py`
`_match_group`을 열어보니 AND는 **URL 교집합**이지 텍스트 매칭이 아니었다. 그래서 새 단어로
좁히려면 그 단어를 실제로 검색해야 한다(단, 그 단어 하나만 — 전체 재수집이 아니다).
동시에 더 중요한 사실이 따라 나왔다: **교집합에서 1,000건 상한에 걸리면 손해 방향이
OR와 정반대**다. OR는 "덜 가져옴"이지만 AND는 "남아야 할 기사가 잘못 빠짐"이다. 게다가
좁히는 데 쓸 만한 말일수록 넓어서 상한에 닿기 쉽다(`정부` 하루 약 6,800건, 실측).
그래서 상한에 닿으면 **적용 자체를 거부**하기로 했다 — 조용히 잘못 지우느니 아예 안 한다.

**"이 3건이 재수집에서 안 나오면 어떡하지"에 대한 답 — 핀도 초안/확정본도 아니다.**
지키고 싶은 기사에 미리 핀을 꽂는 안이 나왔는데, 핀은 **미리 예측해서 눌러야 하는
버튼**이라 기각했다 — 200건 중 어느 3건이 소중한지는 아직 모르는 시점이다. 정기처럼
초안/확정본 두 화면으로 나누는 안도 기각: 정기가 그 구조인 이유는 **회차 마감 시각이
있고 그 스냅샷을 발송**하기 때문인데 수시는 마감도 발송도 없다. 게다가 화면을 나눠도
"뭐가 새로 들어왔나"는 안 풀린다 — 정기는 화면이 나뉜 채로도 `.is-new-arrival` 노란 표시를
따로 만들어야 했다. 최종안은 **빠지는 기사를 숨김함으로 보내고 조건을 되돌리면 저절로
돌아오게** 하는 것이다. 사후에 "빠진 걸 보고 고르는" 쪽이 사전에 "지킬 걸 미리 찍는"
쪽보다 인지 부담이 훨씬 낮다.

**작업대(하단 보관 구역)도 검토했다 기각.** 조건이 바뀌어도 살아남는 별도 구역을 카드
하단에 두자는 안 — 정기의 `📌 담아둔 기사`와 같은 모양이고, 이러면 사안을 두 개 만들지
않아도 된다는 장점이 분명했다. 기각 이유 세 가지는 §6.4에 정리했는데, 결정적이었던 건
**사용자 본인이 내보내기 기준을 물어보다 스스로 발견한 모호함**이다 — 지금은 "카드 =
보고서"라 복사/txt/엑셀이 뭘 내보내는지 질문 자체가 없는데, 작업대가 생기면 매번
"둘 중 뭐가 보고서냐"가 생긴다. 여기에 위 실측(숨김 1건 = 일일이 고르는 행동을 안 한다)이
겹쳐 "작업대가 빈 채로 남아 보고서가 비는" 최악이 현실적이라고 판단했다.

**TMI 정리.** 처음 시안엔 조건을 바꿀 때마다 "N건이 숨김함으로 들어갔어요 (그 중 2건은
소제목을 배정해두신 기사예요)" 같은 결과 배너를 띄웠는데, 사용자가 TMI라고 지적했다.
맞는 지적이었다 — 몇 건이 빠졌는지는 **기사 목록의 건수와 좌하단 숨김함 배지가 이미
말하고 있어서**, "조용히 사라지면 안 된다"(CODING_CONVENTIONS §3)는 배너 없이도 충족된다.
같은 사실을 두 번 말하는 게 곧 TMI다. 1,000건 상한 경고만 남겼다 — 그건 "지금 결과가
틀렸을 수 있다"는 뜻이라 성격이 다르다.

**속도가 진짜 비용이었다.** 교체 모델은 조건을 바꿀 때마다 재검색을 부르므로 호출량보다
대기시간이 문제였다. 해결책은 이미 앱에 있었다 — `app/live_cache.py`의 키워드별 캐시를
카드에도 적용하면 새 키워드만 검색하면 된다. 호출량 자체는 사안 2개를 하루 종일 만져도
약 500요청으로 `NAVER_DAILY_CALL_LIMIT`(25,000)의 2% 수준이고, 이제 추정이 아니라
`app/api_usage.py`가 실측으로 센다.

**같이 없앤 것 — 키워드 작성 메모.** 수시 카드의 "키워드 작성 메모" 칸을 삭제했다. 카드
18개 중 메모를 쓴 카드가 1개였고 내용은 테스트값 `"A"`였다. 이름부터 틀린 칸이었다 —
수시엔 `AI가 추출한 주요 키워드` 블록이 애초에 없어서(§ADHOC_DESIGN 결정) "키워드 작성"이
가리킬 대상 자체가 없었다. 정기에서 이름만 딸려온 자리다.

시안은 [ADHOC_KEYWORD_EDIT_MOCKUP.html](trash-can/2026-08-20_목업정리/ADHOC_KEYWORD_EDIT_MOCKUP.html)에 있다 — 실제로
눌러볼 수 있는 목업으로, 조건을 바꿔가며 세 번(두 줄 분리 → 한 줄 통합 → 꼭 포함할 검색어
분리) 갈아엎은 결과다.

**[Fixed: 2026-08-24] 재수집·검색어 편집을 반복할수록 같은 기사가 계속 늘어나던 버그.** 위에서 "교체 모델"로 뒤집으면서, `run_collect`가 후보를 추릴 때 `app.adhoc.collector._apply_options`의 `deduplicate_by_title`이 **이번에 새로 찾은 후보끼리만** 서로 대조한다는 걸 놓쳤다 — 이미 `card["articles"]`에 들어있는 기사의 제목은 안 본다. 그래서 검색어를 고칠 때마다(추가·삭제·재수집 전부 `run_collect` 하나를 거친다) 네이버가 URL은 다르지만 제목이 같은 기사(사진 캡션류에 특히 흔함 — "발언하는 OOO 부총리" 같은 제목이 여러 기사 ID로 반복 색인된다)를 돌려주면 매번 새 URL로 다시 붙었다. 실측: 구윤철/09:00~16:00 카드를 세 번 편집하니 같은 제목이 5중복까지 쌓였다. 고친 방법은 새 후보를 카드에 붙이기 직전에 `card["articles"]`의 기존 제목 집합과 한 번 더 대조해 걸러내는 것 — §6.4의 "교체이지 삭제가 아니다" 원칙(조건을 되돌리면 옛 기사가 다시 보여야 한다)은 카드에서 아무것도 지우지 않으므로 그대로 지켜진다. "새로 추가하려는 후보"만 필터링 대상이라 원칙과 충돌하지 않는다.

### [새 수집]에 AND가 없다고 느낀 문제 — 안내 한 줄에서 라디오 2지선다로 (2026-09-01)

**결론부터: 같은 날 두 번 뒤집혔다.** 처음엔 "안내 한 줄만 붙이고 AND 추가는 기각"으로
끝냈는데, 그 기각의 핵심 근거 하나가 코드를 다시 읽어보니 **사실이 아니었다.** 아래
"기각했던 근거가 틀렸다" 항목이 그 대목이고, 최종 결과는 라디오 2지선다다.

### [새 수집]에 AND가 없다고 느낀 문제 — 1차 판단(기각) (2026-09-01)

"'재정경제부+국무회의' 결과만 보고 싶은데 [새 수집]에 그 기능이 없다"는 질문. 기능은
있다 — 위 §6.4a의 `꼭 포함할 검색어`(AND)다. 다만 그게 **수집 결과 화면의 접힌 편집
패널** 안에 있어서, 카드를 만드는 시점에는 존재 자체가 안 보였다.

**[새 수집]에 AND 입력을 추가하는 방향은 기각했다.** §6.1의 근거("급하게 걸었다가 0건이
최악")가 그대로 유효할 뿐 아니라, 결과 화면에서 좁히면 조건을 지우는 순간 되돌아와
실패 비용이 0이고 키워드별 캐시 덕에 네이버 호출도 0회다. 게다가 1,000건 상한 거부
판정(§6.4b)은 실제로 검색해 본 뒤에만 가능해서 새 수집 화면에서는 원리적으로 못 막는다.
즉 "넓게 모으고 결과에서 좁힌다"는 지금 경로가 안전성·속도·정확성 셋 다에서 낫다.

그래서 고친 건 코드가 아니라 **발견성 한 줄**이다 — `app/adhoc/renderer.py`
`render_new_card_page`의 검색어 칸 아래에 `하나라도 걸리면 가져와요 · 둘 다 포함된
기사만 보려면 수집 후 결과 화면에서 좁힐 수 있어요`. 앞 절반은 편집 패널의 기존
`.kw-hint` 문구를 그대로 써서 두 화면이 이어져 읽히게 했고, `.kw-hint.new`는 라벨 폭
차이(78px ↔ 104px)에 따른 들여쓰기만 맞춘 것이다. 로직·엔드포인트 변경 없음.

### 기각했던 근거가 틀렸다 — 라디오 2지선다로 확정 (2026-09-01)

"결과 화면에서 또 소팅하기가 어렵다"는 재지적을 받고 코드를 다시 읽었더니, 위 기각
근거 중 **두 개가 무너졌다.**

**① "1,000건 상한은 새 수집 화면에서 원리적으로 못 막는다" — 틀렸다.**
`app/adhoc/routes.py` `_handle_collect`은 **카드를 먼저 만들고 그 다음에 `run_collect`을
부른다.** `AdhocKeywordCapError`는 `AdhocCollectError`의 하위 클래스라 이미 잡혀서 카드
화면에 그대로 뜬다. 즉 새 수집에서 AND를 걸어도 상한 거부는 편집 패널과 똑같이 작동한다.

**② "0건이 최악"(§6.1의 원래 근거) — 같은 이유로 무너졌다.**
0건이 나와도 카드는 이미 만들어져 있어서 편집 패널에서 조건을 지우면 즉시 돌아온다.
"카드를 만들기 전에 거는 게 위험하다"는 전제 자체가 지금 코드에선 성립하지 않는다.

**남은 건 실제 비용 계산이었고, 담당자 쪽이 명백히 비쌌다.** 2단계 경로는 (1) 기본 접힘인
편집 패널을 열고 (2) **이미 한 번 친 단어를 또 치고** (3) `+ 추가`마다 `recompute_condition`
왕복 1회다. "둘 다 들어간 걸 보고 싶다"가 이미 확정된 의도인데 클릭 3~4번을 더 받는 건 과하다.

**저장 방식 — 같은 목록을 양쪽에 다 넣는다.** `keywords=[첫 단어], must=[나머지]`로 쪼개
담는 안을 먼저 검토했는데, 그러면 첫 단어가 1,000건에 잘릴 때 **교집합에서 기사가 조용히
빠진다**(§6.4b가 막으려던 바로 그 실패). AND 모드에선 모든 단어가 사실상 must이므로 상한
검사가 전부를 덮어야 한다. 대가는 결과 화면 편집 패널에 같은 단어가 두 줄에 다 보이는
것 — 카드에 `match_mode` 필드를 새로 두면 피할 수 있지만 `article_in_condition`·
`out_of_condition_reason`·메타 칩·편집 패널까지 다 손대야 해서, **보기 어색함을 감수하고
코드 위험을 줄이는 쪽을 골랐다.** 데이터 자체는 담당자가 손으로 만들 수 있는 상태와 동일해
거짓이 아니다. 실제로 써보고 거슬리면 그때 `match_mode`로 옮긴다.

**목업에서 잡은 것 둘.** 경고 문구에 `.warn`을 썼더니 페이지에 이미 있는 **빨간 오류 상자**와
클래스가 겹쳐 오류처럼 칠해졌다 — 주의는 앰버여야 하므로 `.is-over`로 분리했다(CLAUDE.md
색 규칙). 그리고 검색어가 1개 이하일 때 띄우던 `검색어를 2개 이상 넣으면 고를 수 있어요`는
사용자 요청으로 뺐다 — 흐려진 라디오가 이미 "지금은 못 고른다"를 말하고 있어서다. 문구가
비면 `.mode-note:empty`로 줄 자체가 안 그려져 빈 공간도 안 남는다.

**어제 붙인 안내 한 줄은 뺐다** — 라디오가 같은 말을 더 정확히 한다. 하루 만에 사라진 셈인데,
그 한 줄이 없었으면 "기능이 없다"가 아니라 "왜 여기서 못 하냐"로 질문이 좁혀지지 않았을 것이다.

## 소제목이 규칙 기반으로 조용히 무너진 사고 (2026-08-20 09:30 회차)

**[Fixed: 2026-08-20] 담당자 제보: "초안 정리하다 브라우저를 잘못 닫고 다시 열었더니 잘 붙어있던 LLM 소제목이 갑자기 `<부총리>` 이런 식으로 줄어들었다. 그리고 숨겼던 기사가 돌아와 있다."**

브라우저를 닫은 건 원인이 아니었다 — 다시 연 게 `/preview.html` 재계산을 유발한 트리거였을 뿐이고, 새로고침이나 ↑/↓ 한 번으로도 똑같이 터졌다. 저장된 데이터(회차 파일·LLM 캐시·되돌리기 스택·숨김 기록)로 사고 시각까지 역산해 원인을 각각 재현했다.

- **원인 1 — `_find_partial`의 과반 조건이 "숨긴 기사"를 분모에 넣고 있었다(`app/llm_classifier.py`).** 초안은 API를 새로 안 부르므로(`allow_call=False`) 캐시 재사용 3경로가 전부 막히면 곧장 규칙 기반(단어 빈도)으로 떨어진다. 09:30 회차 실측: 캐시에 담긴 기사 57건, 담당자가 그중 29건을 숨긴 시점(09:24:51 언저리 — 되돌리기 스택에 남은 숨김 건수 21→42 증가 기록으로 특정)에 그 사이 새 기사 2건이 들어왔다. → `_cache.get` 불일치, `_find_reusable`은 새 기사 2건 때문에 부분집합 조건 탈락, `_find_partial`은 **겹침 26 × 2 = 52 < 57**로 탈락. 세 경로 전멸. 그 기사 구성으로 규칙 기반 분류를 실제로 돌려보니 `['부총리', '비상경제본부', '민생물가', '기타']` — 제보된 화면 그대로였다. **분모가 캐시 크기 전체였던 게 핵심이다**: 숨길수록 분모는 그대로인데 분자만 줄어서, 회차 기사의 절반 넘게 숨기면 이 조건은 **반드시** 깨진다. 숨긴 기사는 어차피 `filter_hidden`으로 모든 화면에서 빠지므로 "이 분류를 아직 써도 되는가"의 분모에 낄 이유가 없다 — 분모를 `캐시 URL − 숨긴 URL`로 바꿨다. 사고 당시 입력으로 재현 테스트: 수정 전 재사용 실패 → 수정 후 원래 소제목 6개 그대로 복원.
  - 곁가지로 확인된 것: 소제목 이름이 바뀌면 `group_overrides.json`(담당자가 "다른 소제목"으로 옮겨둔 배치)은 **소제목 이름 문자열이 키**라 통째로 무효가 되고, `_apply_forced_groups`가 없는 소제목을 조용히 무시한다. 즉 이 폴백은 소제목만 망가뜨린 게 아니라 담당자의 이동 작업까지 에러 한 줄 없이 날렸다(CODING_CONVENTIONS.md §3 위반 지점).
- **원인 2 — 일괄 숨기기·소제목 통째 숨기기가 URL만 저장했다.** 2026-08-13에 "네이버가 같은 기사를 새 기사 ID로 재게재하면 옛 URL 숨김이 안 먹는다"는 사고 때문에 `filter_hidden`을 (언론사, 제목)까지 보게 넓혀뒀는데, `bulkHideSelected`/`hideGroup`(`app/renderer.py`·`app/preview_renderer.py`)은 `{url: url}`만 보내고 있어 그 보호를 통째로 못 받았다 — 오늘 숨긴 43건 중 **11건이 `outlet: null, title: null`**이었다. 그리고 그 재게재가 실제로 일어났다: 09:18:55에 일괄로 숨긴 뉴스1 `.../421/0009122524`가, 언론사·제목이 완전히 같고 기사 ID만 다른 `.../0009122522`·`.../0009122521`로 초안 캐시에 다시 들어와 있었다(이번엔 우연히 `deduplicate_by_title`이 삼켜 화면까지는 안 왔다).
  - **대조 기준을 (언론사, 제목) → 제목만으로 넓혔다.** 언론사까지 같이 보면 같은 기사를 **다른 언론사 이름으로** 전재한 경우가 계속 샌다. 실제로 새는 이유는 `filter_hidden` 자체가 아니라 순서다 — `deduplicate_by_title`이 먼저 돌기 때문에, 숨긴 기사가 그 제목의 대표였다가 나중에 우선순위가 더 높은 언론사가 같은 제목으로 기사를 내면 대표가 교체되면서 부활한다. 제목만으로 판단해도 화면에서 사라지는 기사는 늘지 않는다(제목이 같은 기사는 dedup이 어차피 1건만 남기므로, 추가로 걸리는 건 **원래도 화면에 없던 기사**다).
  - **"filter_hidden을 dedup 앞으로 옮긴다"는 처음 세운 해법은 정반대로 틀렸다** — 숨긴 기사가 대표 자리에서 빠지면 같은 제목의 다음 기사가 **항상** 대표로 올라와 오히려 매번 부활한다. 코드를 고치기 전에 두 함수의 순서를 실제 데이터(167건 → 화이트리스트·정렬 118건 → dedup 61건)로 따져보고서야 방향이 뒤집혔다.
  - 화면 JS 세 경로(단건·일괄·소제목 통째)가 같은 본문을 쓰도록 `hideArticleBody(btn)` 하나로 합쳤고, **서버에서도 한 번 더 받친다**(`_hide_meta_fallback`) — 제목이 안 왔으면 진행 중인 초안 캐시 → 저장된 회차 순으로 직접 찾아 채운다. 순서가 중요하다: 초안에서 숨긴 기사는 회차로 저장된 적이 없어 회차 역조회로는 못 찾는데, 못 찾을 때 `_known_articles_by_url`은 보관 기간 1년치 회차 파일을 끝까지 읽는다(CODING_CONVENTIONS.md §1). 이미 쌓인 11건은 초안 캐시에서 제목을 되찾아 일회성으로 채워 넣었다(원본은 `trash-can/`에 백업).
- **원인 3(진짜 문제) — 이 폴백은 화면에 아무 흔적도 안 남겼다.** `app/llm_classifier.py`의 모든 실패 경로가 예외 대신 `None`을 돌려주고 `app/classifier.py`가 단어 빈도 분류로 이어받는다("소제목 품질 하나 때문에 스크랩 전체가 멈추면 안 된다" — 이 의도 자체는 그대로 옳다). 문제는 그 결과가 화면에서 정상과 **구분되지 않는다**는 것이었다: 담당자가 뭉개진 소제목을 눈으로 발견할 때까지 아무도 몰랐고, 2026-08-11의 "61건이 상한을 1건 넘겨 조용히 폴백" 사고도 발견 경로가 똑같이 사람 눈이었다. `last_classification_was_rule_based()`를 새로 만들어(기존 `llm_attempt_failed_without_fallback()`은 "API를 실제로 불렀는데 실패한" 경우만 잡아서 이번 경우를 놓친다) 초안 기사 목록 맨 위에 붉은 경고 박스를 띄운다 — 무엇이 잘못됐는지(소제목을 믿지 말 것)와 무엇을 하면 되는지(재분류 버튼)를 둘 다 적었다. 플래그는 **기본값을 "폴백했다"로 두고 결과를 실제로 얻어낸 지점(`_accept`)에서만 내린다** — 반환 지점을 하나 빠뜨렸을 때 틀리는 방향이 "괜한 경고"여야지 "조용한 침묵"이면 안 되기 때문이다. 회차 초반(기사가 `MIN_ARTICLES_FOR_AUTO` 미만이라 아직 시도조차 안 한 상태)은 의도된 대기 상태라 경고하지 않는다 — 매 회차 첫 몇 분마다 무의미하게 뜨면 정작 진짜 폴백 때 무시하게 된다.
  - 경고는 **초안에만** 둔다. 확정본은 렌더링 때 재분류하지 않고 회차 파일의 스냅샷을 읽으므로(`groups_from_snapshot`) 화면을 여는 행위로 폴백이 발생할 수 없고, 저장 시점의 진짜 실패는 이미 `classification_degraded`로 기록돼 별도 경고와 자동발송 보류로 처리된다.

**[Added: 2026-08-20] 경고 배너 목업을 사용자와 세 번 주고받으며 다듬은 뒤 구현.** 첫 시안은 배너 안에 "🤖 전체 기사 재분류" 버튼을 따로 뒀는데, 사용자가 "툴바에 이미 있는데 왜 또 넣었냐"고 지적했다 — 실측해보니(`preview.html` 실제 DOM) 툴바 버튼이 배너에서 90~120px밖에 안 떨어져 있어 스크롤 없이 한눈에 보였다. 같은 라벨의 버튼이 화면에 둘이면 "다른 건가?"라는 새 의문만 는다는 게 근거였다. 배너 버튼을 빼고 문구로 툴바 버튼을 가리키는 `.btn-ref` 인라인 칩(실제 버튼과 같은 색·아이콘)으로 바꿨다. 아이콘도 처음엔 `↻` 유니코드로 대충 그렸다가, 사용자가 실제 화면 캡처를 보내 지적해서 `app/icons.py`의 로봇 SVG(`"bot"`)와 정확한 색(`#4A3F8F`/`#DCD0F2`, 연보라 = "AI가 하는 일" 색 언어)으로 맞췄다.

문구는 사용자가 직접 세 문장으로 다듬었고(배경 설명 없이 "무슨 상태" + "뭘 하면 되는지"만), 구현은 그 문구를 그대로 코드에 옮기는 방향으로 진행했다:
- 원인 3가지(기사 수 초과 / 재분류 연속 실패 / 일반 폴백)를 구분해야 한다는 게 논의 중에 드러났다 — 재분류 버튼이 "눌러도 되는 상황"과 "눌러봐야 소용없는 상황"이 섞여 있으면, 안 통하는 버튼을 반복해서 누르며 시간을 버리는 게 실제로 제일 비싼 실패 모드였다(담당자 피드백: "어차피 바쁜데 그 다음 대응을 어떻게 해야할까"). `app/llm_classifier.py`에 `last_classification_skipped_too_many_articles()`를 새로 만들어 "기사 수 초과"만 따로 구분했다 — 이 경우는 재분류를 몇 번 눌러도 기사 수를 줄이지 않는 한 매번 똑같이 실패하므로, 줄여야 할 건수(초과분)를 숫자로 알려준다.
- "재분류를 눌렀는데 또 실패"를 구분하려면 시도 이력이 있어야 해서 `app/reclassify_attempts.py`를 새로 만들었다 — 프로세스 메모리에만 두는 작은 모듈로, 초안의 **명시적** 재분류 클릭(`force_llm=True`)만 기록하고 회차 초반 자동 첫 분류는 대상에서 뺐다(담당자가 요청한 적 없는 시도까지 "재시도"로 세면 왜곡된다). 연속 2회 실패하면 세 번째 시도를 권하는 대신 "소제목 이름을 직접 수기로 고치길 추천합니다"로 바뀐다 — 마감이 코앞일 수 있는 상황에서 필요한 건 세 번째 시도가 아니라는 판단.
- 툴바의 "🤖 전체 기사 재분류" 버튼이 원래 갖고 있던 확인창("직접 옮겨둔 기사 N건, 소제목 이름과 순서가 초기화됩니다")이 폴백 상태에서는 **거짓에 가깝다**는 것도 논의 중 발견했다 — 이름은 이미 단어 빈도로 뭉개졌고, 옮겨둔 배치(`group_overrides`)도 그 이름을 못 찾아 이미 무효이기 때문이다. 없어진 걸 잃는다고 겁주면 정작 눌러야 할 순간에 담당자가 망설이게 된다. 버튼에 `data-degraded` 속성을 실어 JS `regenerateSubheadings(btn)`가 확인창 문구를 분기하게 했다.
- 복사·다운로드를 누를 때도 폴백 상태면 한 번 더 묻는다(지금 나갈 소제목 이름을 실물로 보여줌, `<부총리>, <비상경제본부>, …`) — 배너를 놓치고 스크롤을 내려 바로 내보내기만 누르는 경우를 잡는 마지막 방어선. `CLASSIFY_DEGRADED`/`CLASSIFY_DEGRADED_NAMES` 두 JS 전역을 `previewMoveArticle`(↑/↓ 이동, DOM만 갈아끼우고 새로고침 안 함)이 이동마다 같이 갱신해야 한다는 걸 놓칠 뻔했다 — 안 그러면 이동 직후엔 확인창이 이동 전 상태로 판단한다.

## "기타"가 회차의 절반 가까이 몰리던 문제 (2026-08-24)

**[Fixed: 2026-08-24] 담당자가 폴백(규칙 기반)도 아닌데 "기타"가 유독 크다고 느낀 여러 회차를 실측해보니 실제로 44~86%가 흔했다.** 위 사고와 달리 이건 폴백이 아니라 **LLM이 정상적으로 응답한 1차 분류 자체가** "기타"에 많이 몰아넣는 문제였다 — `last_classification_was_rule_based()`도 False, `classification_degraded`도 안 뜬 "정상" 상태에서 벌어졌다.

**원인을 코드가 아니라 모델 응답 자체에서 찾았다.** `_parse_groups`의 `[:max_subheadings]` 절단이 원인일 거라 처음엔 의심했는데(모델이 상한보다 많은 묶음을 제안했다가 뒤쪽이 잘려 "기타"로 흘러드는 것 아닐까), 절단 직전 원본 payload를 가로채 확인해보니 아니었다 — 모델이 애초에 정확히 15개(상한)만 제안하고, 그중 마지막 하나를 스스로 "기타"라 이름 붙여 89건 중 41건을 담고 있었다. 상한을 30으로 올려도(테스트) 모델은 여전히 스스로 15개만 만들고 "기타"는 그대로 45건이었다 — 상한이 원인이 아니었다.

**"기타" 안을 열어보니 답이 있었다.** 41건 중 30건 이상이 국회 예산결산특별위원회(예결위) 결산 심사 한 회의를 다루는 사진 캡션류 기사였다("[포토] 예결위 발언하는 구윤철 부총리", "국회 예결위 전체 회의 출석한 한동훈 의원" 등) — 요약문에도 "예산결산특별위원회 1차 전체회의"가 명시돼 있어 정보가 부족한 게 아니었다. 그런데도 모델은 그중 일부(많아야 7~8건)만 골라 별도 소제목을 만들고 나머지는 "기타"로 흘렸다.

**두 단계로 고쳤다.**
1. **AI_RULES.md에 캡션형 기사 처리 지침 추가.** "[포토]"·"발언하는 OOO" 같은 캡션형 제목도 같은 사건이면 한 묶음이라는 규칙, "기타"를 확정하기 전 그 안에 같은 회의를 다루는 기사가 몰려 있지 않은지 재확인하라는 규칙을 넣었다. 효과는 있었지만(재현 시 큰 클러스터가 만들어지는 빈도가 늘었다) **완전하지 않았다** — 같은 프롬프트·같은 89건으로 3회 재시도해 "기타"가 1건~39건까지 들쭉날쭉했다. `temperature=0`으로 고정해도 편차가 줄지 않았다(API 자체가 완전 결정적이지 않은 것으로 보인다 — 긴 응답 후반부일수록 판단이 흔들리는 정황).
2. **`_refine_etc_bucket` — "기타"만 따로 떼어 2차 호출.** 프롬프트만으로는 안정성을 못 잡아서, 코드로 한 번 더 검증하는 단계를 넣었다. "기타"가 크면(6건 이상 & 25% 이상) 그 기사들만 다시 분류를 시킨다 — 89건 전체를 한 번에 볼 때보다 41건만 놓고 다시 보게 하니 훨씬 안정적으로 정리됐다(재현: 40건 → 0~4건). **처음엔 "남은 슬롯이 2개 미만이면 건너뛴다"는 조건을 넣었는데, 실측해보니 1차 호출이 상한(15)을 거의 항상 다 채워 슬롯이 정확히 1개만 남는 경우가 대부분이었다 — 하필 재정리가 제일 필요한 상황에서 이 함수가 매번 조용히 아무 일도 안 하고 있었다.** 슬롯 1개도 허용하도록 고쳤다(여러 쟁점으로는 못 쪼개도, "기타"라는 이름을 실제 사건 이름으로 바꾸는 것만으로 의미가 있다는 판단). 재검증(89건 회차 3회 반복): "기타" 0%·13%·19%(평균 약 11%) — 원래 44~51%에서 크게 줄었고, 52건 회차도 0~4%로 확인했다. 문턱 밑인 회차(7건)는 추가 호출이 0회임도 확인했다(불필요한 비용 없음).

**비용**: 재정리가 실제로 걸린 89건 회차 기준 2차 호출은 입력 7,101 / 출력 347 토큰 — 1차 호출(입력 14,794 / 출력 2,233)의 절반 이하다(대상이 "기타" 기사만이라 입력이 훨씬 작다). 합쳐서 회당 약 40~55원 — LLM_COST_USAGE.md에 4번째 호출 지점으로 반영.

## 이슈 이벤트를 담당자가 LLM에 직접 지시하는 기능 — 기각 (2026-08-25)

**"오늘은 예결위가 있는 날이니 예결위 관련한 걸 다 묶어줘" 같은 지시를 재분류 요청 시 프롬프트에 실어 보내는 안을 검토했다가, 실측 후 만들지 않기로 했다.** 89건 회차(2026-08-24 14:00, 예결위 언급 69건)로 조건당 2회씩 6회 호출(약 222원): (1) **지시가 없어도 기준선이 이미 예결위 묶음을 33~36건 규모로 만들었다** — 바로 위 "기타" 항목의 캡션형 지침 + `_refine_etc_bucket`이 이 문제를 이미 해결해둔 상태였고, 지시를 우선 규칙으로 감싼 조건(36/38건)의 우위는 기준선 자체의 회차 간 편차(33~36) 안에 묻혀 판정되지 않았다. (2) 반대로 **지시를 규칙 위계 없이 그대로 던진 조건은 2회 중 1회가 "기타" 42%로 무너졌다** — 모델이 지시어를 분류 기준이 아니라 배경 정보로 읽은 것으로 보인다. 즉 감싸는 문구는 지시를 잘 먹게 하는 장치가 아니라 지시가 망치지 않게 막는 장치였다.

덧붙여, 예결위 언급 69건 중 한 묶음에 모이는 건 어떤 조건에서도 최대 38건이었다 — 나머지는 예결위장에서 나온 발언이지만 쟁점 자체는 대법관 제청·제주 실종사건인 기사들이라, 지시를 아무리 강하게 걸어도 따라오지 않는다. 담당자가 말한 "다"를 원리적으로 충족할 수 없는 지점이고, 그게 정말 필요하면 LLM이 아니라 제목·요약 검색 + 일괄이동(0원·100% 재현)이 맞는 도구다.

## 소제목 배정 기록 보관 기간을 1년에서 30일로 (2026-08-25)

**[Changed: 2026-08-25] "1년으로 바뀌면서 이제 기록이 안 지워지는 거냐, 부작용은?"이라는 담당자 질문에서 시작해 실측하고 값을 다시 잡았다.**

먼저 사실 확인: 안 지워지는 게 아니라 **배정한 지 365일 지나면 지워지는** 상태였다(2026-08-21에 "지금 화면에 안 보이면 즉시 삭제"에서 나이 기준으로 바꾸면서 생긴 것 — 그 변경 자체는 담당자 작업이 조용히 사라지던 사고를 막으려던 것이라 옳았다). 다만 그때 나이 기준으로 `RETENTION_DAYS`(365)를 **그대로 빌려 쓴 게 문제**였다 — 이건 회차 파일 보관 기간이고, 두 값은 성격이 다르다:

- **회차 파일 1년**: "작년 이맘때 이 사안이 어떻게 다뤄졌나"를 되짚기 위한 **열람용 자산**.
- **배정 기록**: "초안에서 옮긴 걸 확정될 때까지 지킨다"가 전부인 **작업 중 임시 기록**. 회차가 확정되는 순간 배정 결과는 회차 파일의 `group` 필드로 스냅샷되므로(`snapshot_group_names`), 그 뒤로 이 기록을 읽을 일이 사실상 없다. 지켜야 하는 구간이 몇 시간인데 1년을 들고 있을 이유가 없었다.

**부작용을 실측해보니 예상과 달랐다.** 처음엔 파일 크기·읽기 속도를 의심했는데 둘 다 문제가 아니었다:

| | 지금(2026-08-25) | 1년치 추정 |
|---|---|---|
| `group_overrides.json` | 32KB / 226건 | 3MB / 20,600건 |
| 파일 읽기+파싱 1회 | — | 7.4ms (무시할 수준) |
| **`undo_stack.json`** | **1.27MB** | **65MB** |
| **큐레이션 클릭 1회당 `undo_push`** | **5ms** | **250ms (50배)** |

**진짜 비용은 되돌리기 스택 증폭이었다.** `app/undo.py`는 큐레이션 동작마다(숨기기·이동·순서변경·이름변경) 스냅샷 파일 6개를 **통째로 문자열로 복사해** 최대 20단계까지 쌓는다 — `group_overrides.json`이 3MB가 되면 스냅샷 하나가 3MB, 20단계면 `undo_stack.json`이 65MB가 되고, 버튼을 누를 때마다 그 65MB를 다시 쓴다. 하루아침에 터지는 게 아니라 몇 달에 걸쳐 서서히 느려지는 종류라 더 알아채기 어렵다. 관측된 증가 속도는 평일 하루 약 57건이었다.

**고친 방법**: 전용 상수 `GROUP_OVERRIDE_RETENTION_DAYS = 30`(`app/config.py`)을 만들고 `cleanup_group_overrides`가 그걸 쓰게 했다. `RETENTION_DAYS`(회차 파일)·`ADHOC_RETENTION_DAYS`(수시 카드)는 그대로 1년 — 그 둘은 화면 문구("최근 1년 보관됩니다")까지 연결돼 있어 건드리면 안 된다. 30일은 "확정될 때까지 지킨다"는 목적에 차고 넘치면서 위 증폭을 통째로 없앤다.

**검증**: 30일 경계(29일 전 기록은 남고 31일 전은 지워짐), `at`이 없거나 깨진 옛 형식 기록은 **삭제 대신 도장만 찍는** 기존 안전장치, 읽는 쪽 계약(`load_group_overrides()`가 `{url: 소제목이름}`을 돌려주는 것)이 그대로인지 확인했다. 실제 데이터 복사본으로 미리 돌려보니 **지금 지워지는 건 0건**이다(현재 가장 오래된 기록이 8/21이라 전부 30일 안쪽 — 자연히 9/20에 만료된다).

**같이 확인한 것 — 다른 부작용 후보 둘은 실제로는 문제가 아니었다.** (1) 오래된 배정이 되살아날 위험: 키가 URL인데 이 앱은 당일 발행 기사만 수집하므로 같은 URL이 한참 뒤 다시 걸릴 일이 사실상 없다. (2) 죽은 항목이 남는 것: 코드 어디에도 없는 옛 버전 잔재(`<***new***>` 2건)가 실제로 남아 있었지만, 존재하지 않는 소제목 이름은 `_apply_forced_groups`가 조용히 무시하므로 무해하고 이제 30일 뒤 자연 소멸한다.

## "🤖 미분류 배정"이 한 번에 다 안 되던 문제 (2026-08-24)

**[Fixed: 2026-08-24] 미분류 5건에서 버튼을 눌렀더니 3건이 남고, 다시 눌러야 나머지가 처리되는 증상 제보.** 두 번째 클릭은 정상 동작했으니 치명적이진 않았지만, 왜 한 번에 안 됐는지 안내도 없어서 "버튼이 반만 먹었다"로 보였다.

**원인은 `assign_to_existing()`의 반환 계약 자체였다.** 이 함수는 넘긴 기사 전체가 아니라 **모델이 실제로 응답에 포함시킨 것만** `{url: 소제목}`으로 돌려준다 — 모델이 일부 기사를 응답에서 통째로 빠뜨리거나(article_id를 아예 안 씀), "기타"·빈 문자열로 답해 함수 내부의 `if not name or name == "기타": continue`에서 걸러지면, 그 기사의 URL은 반환값에 존재조차 하지 않는다. 호출부(`app/settings_server.py` `_handle_assign_unclassified`)는 `for url, name in assigned.items(): set_group_override(url, name)`처럼 반환된 것만 처리했으므로, 빠진 기사는 아무 에러도 없이 그냥 미분류 상태 그대로 남았다 — 다음에 다시 누르면(같은 확률로 이번엔 모델이 다르게 답해) 처리되는 식이었다.

이건 `classify_with_llm`(전체 재분류)이 이미 지키고 있는 "한 건도 빠지면 안 된다"는 규칙(`_parse_groups`의 "모델이 빠뜨린 기사는 기타로 모은다")이 `assign_to_existing`(증분 배정)에는 애초에 없었던 것이다 — 두 함수는 자매 관계지만 이 보장 하나가 한쪽에만 있었다.

**고친 방법**: `assigned`를 처리한 뒤, 원래 대상이었던 `pending`(정기)/`unclassified`(수시) 중 `assigned`에 없는 URL을 찾아 전부 "기타"로 보낸다 — `set_group_override`가 room 계산과 무관하게 항상 받아주는 칸이라 안전하다. **이 함수는 정기 초안(`app/settings_server.py`)과 수시 모니터링(`app/adhoc/classifier.py` `classify_card`)이 "정확히 같은 코드 경로"라고 CLAUDE.md에 이미 적혀 있던 대로 똑같이 부르고 있어서, 같은 버그가 두 군데에 동시에 있었다** — 정기만 고치고 끝냈으면 수시 쪽은 여전히 조용히 남아있을 뻔했다. 두 곳 다 같은 방식(응답에 없는 URL → "기타")으로 고쳤다.

**검증**: `assign_to_existing`을 "받은 기사의 절반만 응답하고 나머지는 통째로 빠뜨리는" 모의 함수로 바꿔치기해, 실제 관측된 사고를 그대로 재현했다 — 정기는 격리된 샌드박스에서 실제 HTTP 요청으로(6건 미분류 → 한 번의 클릭으로 0건, 응답에 없던 기사들은 "기타"로 정확히 배정됨을 `group_overrides.json`으로 직접 확인), 수시는 격리된 임시 카드로(6건 미분류 → 한 번에 0건, 마찬가지로 나머지는 "기타") 각각 확인했다.

## 제목이 잘린 기사 재요청이 순차로 돌아 검색이 오래 걸리던 문제 (2026-08-24)

**[Fixed: 2026-08-24] 수시 모니터링의 "꼭 포함할 검색어" 추가 하나에 66초가 걸린 사례를 조사하다 발견했다.** AND 교집합 계산 자체는 URL만 있으면 되는 가벼운 연산인데, 실제 지연은 다른 데 있었다 — `app/naver_api.py` `_search_one_keyword`가 네이버 API가 제목을 "..."로 잘라 보낸 기사마다, 그 기사 원문 페이지에서 온전한 제목을 다시 가져오는 `_fetch_full_title`을 **그 자리에서 하나씩 순서대로** 불렀다. 이 함수의 타임아웃은 3초라, 응답이 느리거나 안 뜨는 언론사 사이트가 하나만 섞여도 그때마다 최대 3초씩 그대로 블로킹됐다.

**얼마나 잘려 있는지부터 실측했다(CODING_CONVENTIONS §1).** 등록 키워드 중 하나('정부')로 실제 검색 1페이지(100건)를 받아보니 24건의 제목이 잘려 있었다 — 순차 처리면 이 페이지 하나만으로도 최악의 경우(전부 타임아웃) 72초가 걸릴 수 있는 구조였고, 실제로 관측된 66초와 규모가 맞아떨어졌다.

**고친 방법**: 페이지 하나를 처리할 때 (1) 먼저 순차로 시간창에 속하는 기사만 가려내고(`pending` — 이 단계는 조기 중단·시간창 필터 로직이 그대로라 순서를 지켜야 한다), (2) 그중 제목이 잘린 URL들만 모아 `ThreadPoolExecutor`로 한꺼번에 동시 재요청한다. 새 상한 `_TITLE_REFETCH_CONCURRENCY`(10)은 네이버 API 호출 상한(`_GLOBAL_REQUEST_SEM`, 8)과는 별개다 — 이쪽은 한 서버가 아니라 서로 다른 언론사 사이트로 요청이 흩어지므로 429 위험이 없고, 실측 24건을 한두 배치로 끝낼 수 있는 여유값으로 잡았다. 시간창 밖으로 걸러진 기사는 애초에 재요청 대상에 안 들어가는 것도 그대로 유지했다(불필요한 요청을 늘리지 않는다).

**검증**: 실제 코드 변경 전엔 손대지 못했던 부분이라(순차 로직 자체가 알고리즘적으로 옳았고, 병목은 순서가 아니라 동시성 부재였다), 모의 테스트 세 가지로 확인했다 — (1) 100건 중 24건이 잘린 페이지를 슬로우 사이트(0.2초 지연)로 흉내내 병렬화 전이면 4.8초 걸릴 것이 0.61초로 줄면서도 100건 전부·재요청 24건 전부 정확히 반영됨, (2) 시간창 경계·조기 중단 시나리오에서 창 밖 기사는 재요청 자체가 안 걸리고 창 안 기사만 정확히 걸리는 것, (3) 실시간현황 전용 `include_unparsed_dates=True` 경로도 그대로 정상 동작. §6.4b(1,000건 상한 오판 수정)·중복 기사 제거 수정과 같은 함수를 다시 건드린 거라 그 두 수정의 회귀도 함께 재확인했다.

## 실시간현황 — 검색어 하나가 결과를 대부분 차지할 때 안내 배지 (2026-08-24)

**[Added: 2026-08-24] 담당자 제보 성격이 아니라, 실시간현황이 왜 7.5MB·새로고침 10초씩 걸리는지 살펴보다 발견했다.** 프로파일링 결과 렌더링 자체는 0.12초, 나머지 10초는 전부 네이버 재검색(60분 되돌아보기 × 활성 키워드 21개)이었다 — 이 자체는 정상 동작(§ "Keyword-level incremental cache"). 다만 "키워드별 건수" disclosure를 펼쳐보니 등록 키워드 21개 중 `코스피` 하나가 결과의 33.3%(656/1,968건)를 차지하고 있었다 — 2위(`예산결산특별위원회`, 23.2%)와도 10%p 이상 벌어진 뚜렷한 이상치였다. 코드가 잘못 동작한 게 아니라 **검색어 선택 문제**라 앱 버그로 볼 순 없었지만, 화면 무게와 대기시간의 상당 부분을 검색어 하나가 만들고 있다는 사실 자체는 담당자가 알아야 조치(더 좁은 말로 바꾸거나 다른 검색어와 "모두 포함"으로 묶기)할 수 있다.

**"코드가 강제로 좁히지 않는다"는 원칙을 지켰다.** 이 앱의 다른 곳(§6.4b 1,000건 상한 거부 등)과 달리, 검색어가 넓은 건 담당자의 편집 자유 영역이라 자동으로 끄거나 줄이지 않고 **안내만** 하기로 했다(사용자 확인). `_heavy_keyword_warning`이 `_filter_bar_html`이 이미 계산하는 `matched_keywords` 건수와 같은 데이터를 재사용해, 배지에 적힌 숫자가 그 disclosure를 펼쳤을 때 보이는 숫자와 항상 일치하게 했다.

**문턱을 두 번 잡았다.** 처음엔 원래 문제 제보 문구("코스피가 든 그룹 전체가 61%")를 그대로 옮겨 그룹 단위 61%로 문턱을 잡으려 했는데, 실제로 배지가 짚어야 할 건 "그 그룹 안에서도 코스피 하나가 유독 넓다"는 사실이었다 — 그룹 합산(그 그룹의 다른 키워드 5개까지 포함)과 키워드 하나의 몫은 다른 숫자다. 키워드 단위로 다시 재보니 코스피는 33.3%였고, 이 값에 맞춰 `LIVE_HEAVY_KEYWORD_RATIO`를 25%로 잡았다(실측 분포에서 코스피만 걸러내고 2위는 안 걸리는 값). 최소 절대 건수(100건)도 같이 요구해, 이른 아침처럼 결과가 아직 적을 때 우연히 한 키워드가 비율만 높아 보이는 경우(예: 5건 중 3건=60%)까지 배지가 뜨는 걸 막았다.

**시각 규칙도 지켰다.** 이미 있던 두 배너(키워드 검색 실패, 발행시각 파싱 실패)는 진짜 오류라 빨강(`.live-warning`) 계열인데, 이건 오류가 아니라 참고 사항이라 CLAUDE.md의 색 규칙(빨강=오류, 앰버=주의)에 따라 새 클래스(`.live-heavy-kw-warning`)에 앰버 팔레트(`{warn_bg}`/`{warn_border}`/`{warn_text}`)를 썼다 — 이미 있는 다섯 색 계열 중 하나를 그대로 재사용했을 뿐, 새 색 세트를 만들지 않았다.

**검증**: 실제 저장된 캐시 데이터(`data/live_cache.json`, 21개 키워드·1,968건)로 실제 렌더링 함수를 직접 호출해 배지가 정확한 문구·숫자로 뜨는지 확인했고, 색상은 브라우저에서 계산된 스타일 값을 직접 읽어 앰버 팔레트와 정확히 일치함을 확인했다. 정상 분포(어느 키워드도 25%를 안 넘김)와 소량 우연한 쏠림(절대 건수가 100건 미만) 두 경우 모두 배지가 안 뜨는 것도 함께 확인했다.

## ↑/↓ 순서 이동이 되돌리기 스택에 안 쌓이던 문제 (2026-08-24)

**[Fixed: 2026-08-24] `app/undo.py` 모듈 맨 위 설계 의도 자체는 "숨기기·이동·이름변경·순서변경·재분류를 전부 한 번에 커버한다"고 적혀 있었는데, 실제로 "순서변경"(확정본·초안의 ↑/↓)만 빠져 있었다.** `_handle_move_article`(확정본 단건)·`_handle_bulk_move_order`(확정본 일괄)·`preview_move_article`/`preview_bulk_move_articles`(초안) 어디에도 `undo_push` 호출이 없었다 — 반면 "다른 소제목으로" 드롭다운(cross-group 이동, `bulk_reassign_group`)에는 있었다. 그래서 ↑/↓를 누른 직후 화면 좌하단 ↩ 버튼의 툴팁은 여전히 **그 이전에 쌓인 엉뚱한 동작**(예: 한참 전의 "AI 소제목 분류")을 가리키고 있었다 — 방금 한 일을 되돌리는 줄 알고 눌렀다가 다른 게 되돌아가는 함정이었다.

**단순히 `undo_push`만 추가하면 안 됐다.** 같은 소제목 안에서의 ↑/↓(`app.curation.move_article`/`bulk_move_articles`)는 소제목 경계를 넘지 않으므로 `group_overrides.json`을 전혀 안 건드린다(`new_override`는 항상 `None` — 예전엔 경계를 넘나들던 시절의 흔적으로 형태만 남아 있다, CLAUDE.md 참고). 결과가 남는 유일한 곳은 **회차 파일(`data/articles/*.json`) 안 `articles` 리스트의 순서 그 자체**인데, `app/undo.py`의 `_SNAPSHOT_FILES` 여섯 개(`hidden_articles`/`group_overrides`/`group_labels`/`group_order`/`custom_groups`/`preview_order`) 중 회차 파일은 처음부터 없었다. 그래서 `undo_push`만 얹었다면 되돌리기를 눌러도 여섯 개 파일이 (내용이 애초에 그대로라) 복원된 척만 하고 실제 순서는 안 바뀌는, **툴팁은 맞는데 눌러도 아무 일도 안 일어나는** 더 나쁜 상태가 될 뻔했다.

**고친 방법**: `_snapshot_latest_run_file`/`_restore_latest_run_file`을 추가해 `push()`/`undo()`가 최신 회차 파일의 원본 내용도 함께 뜨고 되돌리게 했다(`app.storage.list_run_meta()`로 경로만 짚어 원본 텍스트를 그대로 보관 — 다른 여섯 파일과 같은 "형식을 해석하지 않고 그대로 복사" 방식). 기존에 잘 동작하던 숨기기·이름변경·cross-group 이동·AI 재분류 되돌리기는 그대로 두고(회차 파일 스냅샷을 추가로 하나 더 뜨는 것뿐이라 부작용 없음, 실측으로 회귀 확인), `_handle_move_article`·`_handle_bulk_move_order`·`preview_move_article`·`preview_bulk_move_articles` 네 곳에만 `undo_push("기사 순서 변경")`을 추가했다 — 초안 쪽은 `preview_order.json`이 이미 스냅샷 대상이라 새 메커니즘 없이 호출만 추가하면 됐다. 넷 다 같은 라벨을 써서, 단건 ↑/↓와 일괄 ↑/↓를 연달아 눌러도(`coalesce_sec=3.0`) 되돌리기 한 번으로 합쳐진다.

**검증(격리된 샌드박스, 실제 HTTP 요청)**: 일부러 사고를 재현했다 — "AI 소제목 분류"를 먼저 스택에 쌓아두고 ↑ 이동 → ↩ 툴팁이 즉시 "기사 순서 변경"으로 바뀜(예전엔 그대로 "AI 소제목 분류") → ↩ 클릭 → 순서만 정확히 원상복구되고 그 아래 깔린 "AI 소제목 분류" 스냅샷은 그대로 스택에 남음(한 번 더 누르면 그것도 되돌릴 수 있음). 확정본 단건·일괄, 초안 단건 세 경로 모두 확인했고, 기존 숨기기+되돌리기도 회귀 없이 그대로 동작함을 재확인했다.

## 네이버 조회 상한(1000건) 잘림

**[Added: 2026-08-18] 로그만 먼저, 화면 경고는 실측 후 판단.**

수시 모니터링의 자정 잠금(어제 카드는 재수집 불가)을 유지할지 논의하다 "그럼 `1일~2일` 같은 다일 창을 만들면 되지 않나, 네이버 API로 끌어올 수는 있나"라는 질문에서 시작됐다. 확인 결과 **네이버 뉴스 검색 API에는 기간 지정 파라미터가 아예 없다** — 앱이 보내는 건 `query`/`display`/`start`/`sort`뿐이고, 지금도 "당일치"는 API가 걸러주는 게 아니라 `sort=date` 최신순으로 페이지를 넘기며 `_search_one_keyword`가 직접 발행일을 보고 자르는 방식이다. 그래서 다일 창 자체는 원리적으로 가능하지만, `_MAX_START = 1000`(네이버 쪽 제한, 우회 불가)에 걸리면 **더 오래된 기사에 어떤 방법으로도 못 닿는다.** 게다가 그 상황이 `while start <= _MAX_START`를 그냥 빠져나오면서 "이게 전부"인 것처럼 리턴돼 **실패로도 안 잡히고 경고도 없었다** — 담당자는 다 긁은 줄 알고 보고서를 쓰게 된다. 이 조용한 잘림이 다일 창을 접고 자정 잠금을 유지하기로 한 직접적인 이유다(분류 기준이 '사안 › 일자'라 카드가 이틀을 걸치면 일자가 모호해지는 문제도 함께 작용).

"그럼 경고를 띄우자"로 가기 전에, **하루 창에서 실제로 상한에 걸리기는 하는지부터 쟀다**(CODING_CONVENTIONS.md §1). 코드가 `start`를 1→101→…→901로 올려 1000건까지만 긁으므로, `start=901&display=100`을 한 번 호출해 **1000번째 기사의 발행일**을 보면 판정된다 — 아직 오늘이면 그 키워드는 하루 1000건을 넘는다. 등록된 키워드 9개 전부에 대해 키워드당 호출 1번으로 측정했다(2026-08-18 09:40 KST):

| 키워드 | 1000번째 기사 | 판정 |
|---|---|---|
| 재정경제부 | 08-13 11:46 | 여유 |
| 재경부 | 08-03 18:00 | 여유 |
| 기획재정부 | 07-26 17:00 | 여유 |
| 기재부 | **05-21 00:06** | 여유(3개월치) |
| **정부** | **08-18 06:02 (당일)** | **초과 → 잘림** |
| 구윤철 | 08-07 18:45 | 여유 |
| 이형일 | 07-14 11:41 | 여유 |
| 재경위 | 04-17 18:32 | 여유 |
| 재정경제기획위원회 | 08-04 21:38 | 여유 |

**9개 중 `정부` 하나뿐이었고, 나머지 8개는 근처에도 못 갔다.** `정부`를 더 재보니 100번째~1000번째 900건이 3.2시간에 걸쳐 있어 **약 284건/시간 · 하루 6,800건 환산**이다. 다만 이 속도는 06:30~09:40(기사가 제일 쏟아지는 출근 시간대) 구간에서 잰 값이라, 이를 그대로 적용한 회차별 추정(`00:00~06:00 = 1,705건` 등)은 **과대추정**이다 — 새벽 창은 실제로 훨씬 적을 것이라 정기 회차가 걸릴지는 아직 모른다. 반면 **실시간 화면은 확실히 잘린다**(0시~현재 9.7시간 → 2,747건 추정, 절반으로 깎아도 1000 초과). 단 실시간은 키워드별 증분 캐시라 **자정 이후 첫 로딩에서만** 발생하고, 캐시가 데워진 뒤의 조회는 창이 몇 분짜리라 안전하다.

완화 요인도 있다. `정부`가 잘려도 그 회차 기사가 사라지는 건 아니다 — 나머지 8개 키워드가 각자 긁어와 OR로 합쳐지므로, **`정부`에만 걸리고 다른 키워드엔 안 걸리는 기사**만 누락된다. 재정경제부 모니터링에 실제로 쓸모 있는 기사라면 대개 `기획재정부`/`기재부`에도 걸린다.

그래서 **화면 경고는 만들지 않고 `logger.warning` 한 줄만 먼저 깔았다.** 판정은 `start > _MAX_START`로 한다 — `start`는 루프 맨 아래에서만 증가하므로 `break`로 빠져나왔다면 여전히 상한 이하고, 넘겼다는 건 "창의 하한에 닿기 전에 조회 상한에 먼저 걸렸다"는 뜻이다(`while/else`는 오독하기 쉬워 쓰지 않았다). 메시지에 키워드·요청창·도달 지점(`deepest_pub_date`)·수집 건수를 모두 넣었다 — 이 프로젝트는 `logging.basicConfig`을 안 잡아 lastResort 핸들러가 타임스탬프 없이 메시지만 찍으므로, 전역 로깅 설정을 건드리는 대신 **메시지 자체에 날짜(`%m-%d %H:%M`)를 담아** 나중에 세어볼 수 있게 했다. 검증은 세 경우로 했다: `정부`+0시~현재(경고 뜸, 1000건, 07:02까지만 = 00~07시 통째 누락), `기재부`+0시~현재(조용, 4건), `정부`+최근 1시간(조용, 540건 — 창 하한에 먼저 닿음).

**남은 판단**: 며칠 로그를 모아 정기 회차에서도 실제로 찍히는지 본 뒤 화면 경고 여부를 정한다. 다만 더 근본적인 방향은 **`정부`라는 키워드 자체를 좁히는 것**이다 — 하루 6,800건이면 검색어라기보다 한국 뉴스 전체에 가깝고, 실시간 화면의 응답 속도에도 부담이다. `정부 정책`처럼 좁히거나 AND 그룹으로 묶으면 상한 문제가 통째로 사라진다.

**[Fixed: 2026-08-24] 수시 모니터링 §6.4b의 상한 거부(`AdhocKeywordCapError`)가 실제로는 거의 안 걸리고 있었다.** 이 절 맨 위 판정식(`start > _MAX_START`)은 `_search_one_keyword` 안에서 정확히 계산되는데, 그 값을 함수가 로그로만 남기고 반환하지 않아서 `app/adhoc/collector.py`의 `hit_search_cap()`이 `len(results) >= _MAX_START`로 **따로 재추정**하고 있었다. 이 재추정이 새는 지점: `_search_one_keyword`는 시간창 밖 기사(`continue`)나 pubDate 파싱 실패 기사(건너뜀)를 스킵하면서도 `start`(=API 요청 위치)는 그대로 올린다 — 그런 기사가 섞이면 `start`는 1,000을 넘겨(=진짜로 상한에 걸림) 페이지네이션을 멈췄는데도 `results`에 담긴 최종 건수는 1,000 밑으로 떨어진다. 재현: 카드 창을 과거(09:00~11:00)로 좁히고 `정부`를 "꼭 포함할 검색어"에 추가하니, 실제로는 상한에 걸려 08-24 14:55까지만 훑고 멈췄는데(로그에 찍힘) 반환 건수가 0건이라 `hit_search_cap`은 `False` — 거부가 발동하지 않고 조용히 통과됐다(§6.4b가 막으려던 바로 그 상황). 고친 방법은 재추정을 없애고 `_search_one_keyword`가 이미 알고 있는 값을 그대로 반환값에 실어 올리는 것 — `(results, capped)` 튜플로 바꾸고, `search_keywords`가 `capped_keywords` 목록으로, `app.adhoc.collector._search_with_cache`가 캐시 항목에도 함께 저장해(캐시 히트로 받은 결과도 같은 판정이 나오도록) 그대로 전파한다. 기존 2-튜플 언패킹을 쓰던 다른 호출부(`app.breaking_alert_sender`·`app.live_renderer`·`app.naver_api.search_articles_by_groups`)는 세 번째 값을 그냥 버리도록만 맞췄다 — 이 값을 실제로 쓰는 곳은 수시 모니터링뿐이다.

## 라벨

**[Implemented: 2026-08-18]** 목업(`LABEL_MOCKUP.html`) → 사용자 피드백 두 라운드 → 구현 순서로 진행했다. 시행착오와 왜 그렇게 정했는지는 아래, 지금 유효한 규칙은 CLAUDE.md "라벨" 참고.

- **저장 위치를 `app/adhoc/` 안이 아니라 `app/` 최상위로.** 처음엔 "수시는 정기의 전역 큐레이션 파일을 하나도 안 건드린다"는 기존 원칙과 부딪히는 것처럼 보였다. 그런데 다시 보니 그 원칙은 "회차에 매인 판단"(소제목 배정·숨김·순서 — *이 회차 보고서에서* 이 기사를 어떻게 배치할까)에 대한 것이고, 라벨은 "기사 자체에 붙는 것"이라 성격이 다르다 — **경계를 어긴 게 아니라 다시 그은 것**이다. 기사 제목 교정(`summary_overrides.json`)이 이미 URL 키 전역으로 같은 예외를 두고 있어, 그 옆에 나란히 두면 될 일이었다.
- **보관 정책 — "회차 파일이 있으면 안 지운다"를 기각.** 처음 생각한 대안은 "라벨 달린 기사가 있는 회차 파일은 통째로 보존"이었는데, 그러면 1년 넘은 회차가 라벨 하나 때문에 계속 쌓이고 그 회차의 나머지 수백 건까지 함께 남는다. 대신 **라벨을 붙이는 순간 그 기사 정보를 라벨 저장소로 복사**해 원본과 완전히 독립시켰다 — 회차 파일이 예정대로 지워져도 라벨 쪽은 그대로 남는다. 부수 효과로 라벨 보관함이 1년치 회차 파일을 뒤질 필요가 없어졌다(자기 저장소만 보면 됨).
- **스냅샷 필드가 4개에서 7개로 늘어난 이유.** 처음엔 제목·언론사·발행일·URL 넷이면 될 줄 알았다. 그런데 회차 파일이 지워진 뒤 라벨 보관함에서 엑셀을 뽑으면 스크랩일자·스크랩종료시간·소제목 세 칸이 빈 채로 나가 라벨의 핵심 목적("1년 뒤에도 온전히 꺼내보기")이 반쪽이 된다는 게 뒤늦게 보였다 — 엑셀 8열(기능11) 중 라벨명 뺀 나머지 전부를 스냅샷에 담기로 정정. 단 제목만은 스냅샷을 그대로 안 쓰고 `summary_overrides`가 있으면 표시할 때 덮어쓴다(제목 교정은 "원래 값이 틀렸다"를 고치는 것이라 항상 최신이 맞음), 소제목은 반대로 스냅샷 고정(그때그때의 도우미일 뿐이라 라벨 붙인 시점 값이 오히려 정확).
- **q1 — 라벨 보관함 행의 정기/수시 출처 태그, 수시는 사안명만.** 처음 목업엔 "수시 · 재경위"처럼 접두어를 붙였는데, 사용자가 "칩 색깔이 이미 정기와 다른데 '수시'라는 글자가 또 필요하냐"고 지적 — 맞는 말이라 접두어를 떼고 사안명만 남겼다(`snapshot_source`의 8번째 필드 `source`는 그대로 저장, 화면에서 렌더링만 바꿈).
- **q2 — "이름 변경 = 병합" 설계를 스스로 뒤집었다.** 최초 목업의 라벨 관리 화면은 소제목 이름 변경과 똑같이 "이미 있는 이름으로 바꾸면 자동으로 합쳐진다"는 방식이었다. 사용자가 "이 방법이 최선이냐, 더 단순한 방법이 있냐"고 물었을 때 스스로 재검토해 기각했다 — 이유 셋: (1) 이 앱에 이미 반대 규칙이 있다(소제목 이름 충돌은 409로 거절), 병합 여부가 화면마다 다르게 동작하면 일관성이 깨진다. (2) 병합인 걸 타이핑을 끝낸 뒤에야 안다 — 기능이 있는 줄 모르면 못 씀. (3) 철자 오타 하나로 병합이 아니라 세 번째 라벨이 생긴다(갈라짐을 고치려다 더 갈라짐). 대안으로 "이름 변경은 이름만(충돌 시 거절+합치기 유도 버튼) / 합치기는 행마다 드롭다운 하나(기사 행의 '다른 소제목'과 같은 모양·같은 뜻)"로 셋을 분리했다 — 오타로 새 라벨이 생길 수 없고, 목록에서 고르니 무엇과 합쳐지는지 항상 명시적이다.
- **q2-b — 병합보다 싼 예방책.** 병합 UI를 다듬다가 "애초에 안 갈라지게 막는 게 합치는 것보다 싸다"는 결론에 도달해, 새 라벨 입력창에 편집거리 1 이하/접두 일치 경고(`checkSimilarLabel`)를 추가했다 — 병합 대안 논의에서 자연스럽게 파생된 기능이라 사용자에게 먼저 제안하고 동의를 받았다. 서버 왕복 없이 이미 렌더링된 "이미 쓴 라벨" 칩 목록을 그대로 훑는 순수 클라이언트 JS라 비용이 0에 가깝다.
- **q2-a — 되돌리기, 별도 스택으로.** 라벨은 회차에도 카드에도 안 매이므로 `app/undo.py`(정기, 자정 초기화)나 `app/adhoc/undo.py`(카드별)에 끼워 넣을 자리가 없었다 — 새 파일(`app/label_undo.py`)로 분리하고 자정 초기화도 하지 않기로 했다(라벨 자체가 만료되지 않는 개념이라, 어제 붙인 라벨의 되돌리기를 오늘 못 하게 막을 이유가 없음). 특히 **합치기·이름 변경은 되돌리기가 없으면 복구가 물리적으로 불가능**하다는 점이 이 스택을 필수로 만들었다.
- **AND vs OR — 실시간 화면의 그룹 칩(OR)을 그대로 베끼지 않았다.** 처음엔 "이미 있는 패턴이니 그대로 쓰면 되지 않나" 싶었는데, 사용자가 "라벨 2개를 붙였을 때 어느 쪽으로 찾아도 나와야 하는 것 아니냐"는 질문을 던져 다시 짚어보니 **두 가지를 구분해야 했다** — (1) "어느 라벨로 찾아도 그 기사가 걸린다"는 이미 성립(단일 라벨 검색 시 각 라벨마다 매치), (2) "여러 라벨을 동시에 고르면 무슨 뜻이냐"는 별개 질문. (2)에서 실시간 그룹 칩은 OR(하나의 기사가 어느 그룹에서 왔는지 고르는 것)인데, 라벨은 한 기사에 여러 축(주제·용도)이 동시에 겹쳐 붙는 직교 태그라 OR로 넓히면 서로 무관한 결과가 섞여 실제로 못 쓴다 — "세제"+"보고서"를 같이 고르는 이유 자체가 그 교집합("보고서 쓸 때 쓰려던 세제 기사")을 보려는 것이므로 AND가 맞다는 결론에 사용자도 동의했다. 같은 모양의 칩이 화면마다 다른 논리를 가지는 게 정당한 이례적 사례로 CLAUDE.md에 남겨뒀다.
- **엑셀 내보내기(기능11)는 이때는 안 만들었다.** 라벨 보관함 목업엔 "⬇ 엑셀 내보내기" 버튼이 있었지만, 그건 확정본·정기 보관함·수집 결과·수시 보관함까지 다섯 곳에 걸친 별도의 큰 기능(다섯 화면 공통 파일명 규칙, `.xlsx` 8열, `openpyxl` 신규 의존성)이라 그때 구현 범위에서 뺐다. 눌러도 아무 일도 안 하는 버튼을 만들지 않는다는 원칙(CODING_CONVENTIONS.md)에 따라 실제 화면에도 그 버튼 자체를 넣지 않았다 — 다음 항목("데이터 내보내기(엑셀)")에서 별도로 설계·구현했다.
- **검증 — 실서버 대신 브라우저에서 실제 클릭으로 확인.** 이미 켜져 있던 사용자의 실제 `python3 main.py` 프로세스(포트 8765)를 재시작하면 스케줄러가 끊기고 운영 데이터에 영향을 줄 위험이 있어, 대신 각 렌더링 함수(`render_page`/`render_preview_page`/`render_card_page`/`render_labels_page`/`render_label_manage_page`)를 직접 호출해 실제 `app.labels` 모듈로 라벨을 붙인 뒤 정적 HTML을 프로젝트 폴더 안에 임시로 써서 브라우저로 열었다. 팝오버 열기/닫기, 붙은 라벨의 "이미 쓴 라벨" 자동 마킹, 유사 라벨 경고, 되돌리기 버튼 렌더링까지 실제 DOM 조작으로 확인했고, 그 과정에서 실제 버그 두 개를 잡았다 — (1) 라벨 이름을 onclick 인라인 JS 문자열에 직접 끼워 넣어 작은따옴표 하나로 깨질 수 있던 코드를 `data-label` 속성 방식으로 고침, (2) `.format()` 값 자리에 들어가는 문자열에 실수로 `{{`/`}}`를 이중 이스케이프해 되돌리기 버튼의 스크립트가 깨져 있던 것을 발견해 홑겹으로 수정. 검증이 끝난 뒤 임시 파일·테스트 라벨 데이터는 모두 정리했다. `python3 tests/test_integration.py`에서 5개 테스트가 실패했는데, 조사 결과 라벨 기능과 무관한 기존 버그(`app/run_index.py`가 `app.config.ARTICLES_DIR`를 직접 import해 테스트의 `patch.object(storage, "ARTICLES_DIR", ...)`를 우회함 — 테스트가 실제 운영 데이터를 읽어버림)로 확인해 별도 작업으로 분리했다(라벨 관련 4개 테스트는 그대로 통과).

## 데이터 내보내기(엑셀)

**[Implemented: 2026-08-18]** PRD 기능11. 5곳(확정본·정기 보관함·수집 결과·수시 보관함·라벨 보관함) 모두 `app/excel_export.py` 하나를 공유한다 — 화면마다 다른 건 "rows를 어떻게 모으는가"뿐이고 "rows를 엑셀로 만드는 방법"은 하나다. 지금 유효한 규칙은 CLAUDE.md "데이터 내보내기(엑셀)" 참고.

- **정적 3곳 vs 동적 2곳 — 같은 문제를 두 가지 방식으로 풀었다.** 확정본·수집 결과·라벨 보관함은 렌더링 시점에 화면에 뜨는 내용이 이미 다 정해져 있어서, `.txt` 다운로드가 쓰던 패턴(`PLAIN_TEXT`를 hidden input에 심어 보내는 것)을 rows(JSON)로 그대로 베꼈다 — `POST /download-excel` 하나로 셋 다 처리된다. 그런데 정기 보관함(history.html)·수시 보관함(/adhoc)은 **지연 로딩**이 걸려 있어 초기 렌더 이후에도 화면 내용이 계속 바뀐다(펼치는 순간에야 서버에서 그 날짜/사안을 가져옴) — 렌더링 시점에 rows를 구워둘 수가 없다. 이 둘은 서버에 아무것도 안 구워두고, 대신 모든 `<details>` 블록에 `data-date`/`data-issue-id`를 달아 JS가 **지금 펼쳐진(open) 것만** 모아 전용 엔드포인트(`/download-excel-history`, `/adhoc/download-excel-archive`)로 보내면 그 시점에 서버가 다시 읽어 만든다. 둘 다 "화면에 보이는 것을 그대로"(기능11 규칙2)라는 같은 요구인데, 화면 구조가 정적/동적으로 갈려 구현이 두 갈래로 나뉜 것뿐이다.
- **소제목 열은 "데이터"이지 "화면 표시"가 아니다.** `app/renderer.py`의 확정본은 이미 `subheading_format`(예: `<세제 개편>`)을 적용해 화면에 그리고, `UNCLASSIFIED_GROUP_NAME`("📂 소제목 미분류")을 `_build_plain_text`가 "기타"로 접어 보고서 텍스트로 내보낸다(CODING_CONVENTIONS.md §4, 화면 전용 표시가 보고서로 새면 안 됨). 엑셀은 정반대로 갔다 — PRD 기능11 규칙4가 "빈 값이거나 '📂 소제목 미분류'인 행도 정상"이라고 **명시적으로** 못박아 둬서, `groups`를 순회하며 raw 이름을 그대로 담았다(포맷·병합 둘 다 미적용). 같은 함수(`render_page`) 안에서 같은 `groups`를 두고 plain_text 경로와 엑셀 경로가 서로 다른 규칙을 따르는 게 처음엔 헷갈릴 뻔했는데, "이쪽은 보고서, 저쪽은 데이터"라는 PRD의 프레이밍이 명확해서 헷갈리지 않고 갈 수 있었다.
- **수집 결과(카드)의 엑셀 함수를 수시 보관함이 재사용한다.** `app/adhoc/renderer.py` `build_adhoc_excel_rows(card)`를 먼저 만들어 카드 화면의 "⬇ 엑셀"에 붙였는데, 수시 보관함(archive index)의 "지금 펼친 사안의 카드들"을 rows로 만들 때 이 함수를 그대로 재사용했다(사안 → 카드 목록 → 카드마다 `build_adhoc_excel_rows` 호출 → 이어붙이기). 소제목·숨긴 기사 제외 규칙이 카드 화면 자체와 항상 같아지므로, 두 화면에서 뽑은 엑셀이 어긋날 걱정이 없다.
- **`openpyxl`의 빈 문자열 round-trip 함정.** 유닛 테스트에서 `ws.append([..., "", ...])` 후 `load_workbook`으로 다시 읽으면 빈 문자열이 `None`으로 나와 처음엔 버그로 의심했다 — 확인해보니 openpyxl이 빈 문자열 셀은 XML에 값 자체를 안 쓰는 것뿐이고(저장 형식의 특성), Excel/LibreOffice에서 열어보면 빈 문자열이든 `None`이든 똑같이 빈 칸으로 보인다. 실제 결함이 아니라 검증 스크립트가 read-back에 의존해서 생긴 착시였다.
- **수식 오인식은 실제로 안 걸린다.** PRD가 CSV 대신 xlsx를 고른 이유 중 하나(`.xlsx`는 셀 타입을 명시해 저장하므로 "-3.2% 하락…"같은 제목이 수식으로 안 깨짐)를 유닛 테스트로 실제 검증했다 — `ws["E2"].data_type`이 `'s'`(문자열)로 나와, 별도의 이스케이프 코드 없이도 안전함을 확인했다.
- **검증 — 매 사이트마다 실제 서버로 왕복.** 5곳 전부 (1) 유닛 테스트(모듈 함수 직접 호출) (2) 실제 `main.py` 프로세스 재시작 (3) 브라우저로 화면 열어 버튼 클릭 (4) 받은 `.xlsx`를 `openpyxl.load_workbook`으로 다시 열어 행 수·값을 실제 화면과 대조, 네 단계를 다 거쳤다. 라벨 보관함은 실제 라벨 데이터가 없어서 `attach_label`로 실제 기사에 임시 라벨을 붙여 검증한 뒤 `detach_label`로 정리했다(`data/labels.json`이 검증 전후로 `{}`인 것까지 확인). 정기 보관함은 당일(즉시 렌더링) + 지연 로딩 날짜 두 개를 동시에 펼쳐 두 그룹핑 경로(즉시/지연)가 같은 형식의 rows를 내는지 확인했다.

## [단독]·[속보] 기사 알림

**[Implemented: 2026-08-20]** 사용자 요청 — "[단독]이나 [속보] 기사가 나올 경우 담당자(본인)가 텔레그램 알림을 받고 싶다. 수신대상자와는 다른 것"으로 시작해, 대화 중 세 갈래 질문(밤에는 어떻게 받나 / 토큰 드나 / 키워드 100개면 호출 한도 넘지 않나)을 거치며 지금 형태로 좁혀졌다. 지금 유효한 규칙은 CLAUDE.md "[단독]·[속보] 기사 알림" 참고.

- **"화면을 열 때만 감지된다"는 최초 설계의 구멍.** `[단독]`/`[속보]` 판정 자체는 이미 있던 `app.filters.headline_kind`(정기 확정본 정렬용)로 공짜였지만, 처음 떠올린 위치는 실시간현황(`app/live_renderer.py`)뿐이었다 — 그런데 그 화면은 **요청이 올 때만** 재생성되므로, 담당자가 자고 있거나 컴퓨터가 꺼져 있으면 아무 일도 안 일어난다. 사용자가 정확히 이 지점을 짚어 "밤에 자고 있을 때는 어떻게 받나"라고 되물어 설계를 다시 짰다.
- **새벽 알림은 만들 가치가 거의 없다는 걸 실측으로 확인.** 저장된 7일치 정기 회차에서 말머리 붙은 기사 20건의 발행 시각 분포를 뽑아보니 `00~09시 발행 = 2건`, 진짜 새벽(0~5시)은 **0건**이었다(언론사가 단독을 새벽에 잘 안 터뜨림 — 조간 맞춰 06시대이거나 업무시간). 그래서 "밤새 폴링"이 아니라 **"PC가 깨어날 때(또는 켜질 때) 자정~현재를 한 번 검색해 몰아서 보낸다"**로 정했다(사용자도 이 안을 채택) — 진짜 새벽에 즉시 알아야 할 실익이 낮고, PC가 꺼져 있으면 즉시 알림 자체가 물리적으로 불가능하다는 점도 공유했다.
- **토큰(Claude API) 비용은 0원 — 이 기능은 애초에 그 경로를 안 탄다.** 사용자가 "이거 실시간 알림 발송할 때도 토큰 사용하니?"라고 물어, 이 앱에서 Claude API를 부르는 지점이 정확히 셋([LLM_COST_USAGE.md](LLM_COST_USAGE.md), 전부 소제목 분류·요약)뿐이고 알림 경로(말머리 판정=정규식, 전송=텍스트 POST)는 그중 어디에도 안 걸린다는 걸 확인해 전달했다. 이후 설계에서도 알림 메시지에 AI 요약을 넣지 않기로 의도적으로 유지했다(CLAUDE.md "메시지 형식" — 비용 0원이 이 기능의 전제).
- **네이버 API 호출 한도(25,000/일)를 넘길 뻔한 지점 — "24시간 폴링 + 키워드 80~100개".** 초반엔 "실시간 폴링" 하나로 밀고 있었는데, 사용자가 "키워드가 50개가 되면?", 이어서 "키워드 100개 기준으로 다시 계산해달라"고 요청해 표로 재봤다:

  | 감시 키워드 | 5분 간격 · 낮 9시간 | 24시간 |
  |---|---|---|
  | 9개(현재 정기 그룹) | 972회(3.9%) | 2,592회(10.4%) |
  | 50개 | 5,400회(21.6%) | 14,400회(57.6%) |
  | 80개(그룹/키워드 상한 8×10) | 8,640회(34.6%) | **23,040회(92.2%)** |
  | 100개(상한을 올릴 경우) | 10,800회(43.2%) | **28,800회(115.2%, 초과)** |

  즉 **24시간 돌리면 상한(80개)에서 이미 92%, 100개까지 늘리면 한도를 넘는다.** 반면 낮에만(폴링 창) 돌리면 100개에서도 43%로 여유가 있다 — "새벽엔 몰아보내기"로 정한 결정이 우연히 이 문제도 같이 풀어준 셈이라, 낮 시간대 한정 폴링 + 감시 그룹을 사용자가 직접 고르는 체크박스(전체 키워드가 아니라)로 최종안을 좁혔다. 여기에 더해 "짐작 말고 재라"(CODING_CONVENTIONS §1) 원칙에 따라 실제 호출 지점(`app.naver_api._search_one_keyword`)에 카운터(`app.api_usage`)를 심어 **추정이 아니라 실측으로** 80% 도달 시 폴링만 자동으로 멈추게 했다 — 정기 스크랩·실시간현황·수시 모니터링까지 다 이 한도를 나눠 쓰므로, 알림 설정 화면의 예상치(그 폴링만의 추정)만으로는 실제 소진량을 알 수 없기 때문이다.
- **"정기 회차 안에서는 추가 호출 0회로 이미 감지된다"는 사실이 뒤늦게 드러나 설계를 한 겹 더 얹었다.** 폴링/몰아보내기를 설계하던 중, `collect_run`이 회차마다 이미 검색해 온 결과를 그 자리에서 검사만 하면 네이버 호출이 전혀 늘지 않는다는 게 분명해졌다 — 이걸 "바닥"(항상 켜져 있는 경로)으로 삼고, 폴링(수시 감시 설정)은 그 위에 얹는 "더 빠른 스위치"로 재정의했다. 그 결과 알림 설정 화면을 통째로 꺼도(수시 감시 OFF) 정기 회차 검사만은 항상 살아있다 — 이 비대칭을 설정 화면 맨 아래 "이 설정을 모두 꺼도…" 문구로 명시했다(안 그러면 "껐는데 왜 오지?"로 오해할 수 있다).
**[Fixed: 2026-09-16]** 알림이 몇 시간씩 늦게 오던 문제 — 두 가지가 겹쳐 있었다.

- **1) 진짜 원인은 감시가 한 번도 켜진 적이 없었다는 것.** 사용자 제보("속보/단독 발송이 느리게 오는 것 같다"). 파보니 `data/breaking_alert.json`이 아예 없어 기본값(`group_names: []`)으로 돌고 있었고, `poll_and_alert_tick`은 첫 줄에서 곧바로 `return`하고 있었다 — 즉 **알림이 나가는 경로가 정기 회차(하루 6회)뿐**이었다. 실측 증거: 그날 나간 알림 1건이 `발행 06:03 → 발송 09:30`으로 **3시간 27분** 늦었고, 그 09:30이 정확히 06:00~09:30 회차가 도는 시각이었다. 코드 결함이 아니라 설정 공백이라 화면(`/breaking-alert`)에서 감시 그룹 5개·06:00~23:00·3분 주기로 켰다(호출량 하루 약 6,100회 = 한도의 24%). 설정은 매 tick마다 다시 읽으므로 앱 재시작 없이 10초 만에 돌기 시작했다(`last_poll_at` 생성, api_usage 382 → 404로 확인).
  - **기본값이 "켜짐이지만 아무 일도 안 함"이었던 게 이 공백을 오래 숨겼다.** `enabled: True` + `group_names: []` 조합은 화면에서도 "감시할 그룹을 하나 이상 골라주세요"로 보이긴 하지만, 그 화면을 열지 않으면 알 길이 없다. 기본값을 "실시간 스위치가 켜진 그룹 전부"로 바꾸는 안도 검토했으나 기각했다 — 그러면 체크를 전부 푸는 행동이 아무 뜻도 못 갖게 되고(끄는 길은 `enabled`뿐인데 화면엔 두 축이 보인다), 담당자가 모르는 사이 호출량이 늘 수 있다.
  - **3분(`MIN_BREAKING_ALERT_INTERVAL_MIN`)보다 더 줄이지 않기로 했다.** 1분으로 내리면 호출이 3배가 되는데, **네이버 색인 지연 자체가 3~18분**이다(같은 문서 "회차 경계에서 기사가 영영 사라지던 문제"에서 잰 값). 기사가 나와도 검색에 안 떠 있으면 1분마다 물어봐도 못 찾는다 — 지연은 거의 안 줄고 비용만 는다.
- **2) 설정 화면 문구가 코드보다 앞서가 있던 것("다음 감시 시작 때 몰아서 받기").** 1)을 고치며 발견. 체크박스는 "감시 시간 밖에 나온 기사는 **다음 감시 시작 때** 몰아서 받기"라고 약속하는데, 실제 몰아보내기(`catch_up_on_wake`)는 **`main.py`가 앱 시작 시 1회** 부르는 게 전부였다. 그래서 컴퓨터를 밤새 켜두면(= 지금 LaunchAgent 운영 방식 그대로) 감시 시작 시각에 아무 일도 안 일어나, 감시 시간 전에 나온 [단독]은 다음 정기 회차까지 기다려야 했다. 사용자 결정은 **"코드를 문구에 맞게"** — 화면이 이미 한 약속을 지키는 쪽.
  - **별도 검색을 새로 돌리지 않는다.** 그날 첫 폴링의 하한(`after`)을 감시 시작 시각 대신 **자정**으로 내리고 문구만 바꿔 보낸다 — 어차피 돌 검색 한 번의 창을 넓히는 것이라 네이버 추가 호출이 사실상 없다. `catch_up_on_wake`를 스케줄러에서 한 번 더 부르는 안(검색이 두 번 난다)보다 이쪽이 싸고, 무엇보다 "감시 시작 = 그날 첫 폴링"이라 조건을 따로 만들 필요가 없었다.
  - **문구를 하나로 합치지 않았다.** 앱 시작 몰아보내기는 "🌙 밤사이·새벽에 놓친", 감시 시작 몰아보내기는 "🌙 감시 시간 밖에 나온"이다 — 메우는 구간이 실제로 다르기 때문이다(앞의 것은 앱이 꺼져 있던 동안이라 **감시 시간 한복판일 수도** 있고, 뒤의 것은 언제나 감시 시간 밖이다). 한 문구로 뭉개면 둘 중 하나는 반드시 거짓말이 된다.
  - **`polled_date`는 `catch_up_enabled`와 무관하게 늘 남긴다.** "오늘 폴링을 이미 시작했다"는 표시라, 이게 없으면 하루 중간에 몰아보내기 체크를 켰을 때 그날 아침 기사가 뒤늦게 쏟아진다 — 몰아보낼지 말지의 판단은 그날 첫 폴링에서 한 번만 한다. 반대로 검색이 **실패하면** 안 남긴다(다음 폴링이 몰아보내기를 다시 시도해야 하므로) — 틀리는 방향이 "한 번 더 확인"이어야지 "조용히 건너뜀"이면 안 된다.

**[Added: 2026-09-16]** 알림 메시지에 게시 시각 — 사용자 요청("혹시 알림 때 발행시각도 넣게 할 수 있어?"). 같은 날 위 두 가지를 고친 직후에 나온 요청이고, 사실 그 둘이 이 요청의 배경이기도 하다.

- **왜 필요했나 — 알림은 "지금 떴다"고 말하지만 실제로는 늘 시차가 있다.** 세 경로 모두 발견까지 지연이 있고(폴링 주기 3~30분 + **네이버 색인 지연 3~18분**, 바닥 경로는 회차 간격이라 실측 중앙값 90분·최대 168분), 그런데 메시지엔 `언론사 제목` + URL뿐이라 받는 사람 입장에서 **방금 나온 기사인지 두 시간 전 기사인지 구분할 방법이 아예 없었다**. 같은 날 고친 "감시가 한 번도 켜진 적 없어 3시간 27분 늦게 온 알림"이 정확히 그 구분이 필요한 사례다 — 시각이 찍혀 있었다면 지연이 그날 바로 눈에 띄었을 것이다.
- **자리 — 제목 줄 뒤가 아니라 자기 줄.** 처음 떠오른 건 `ㅇ (조선일보) 제목 · 15:31 게시`처럼 한 줄에 붙이는 것이었는데, 그러면 제목이 길 때 텔레그램이 알아서 줄을 접어 **시각이 건마다 다른 자리에 떨어진다**(한국어 기사 제목은 대체로 접힌다). 여러 건이 한 메시지로 묶여 오는 기능이라 훑는 눈이 같은 자리를 따라가야 해서 자기 줄로 정했다. 괄호로 감싸는 안(`(15:31 게시)`)도 기본 템플릿 `ㅇ (언론사) 제목`이 이미 괄호를 쓰고 있어 기각.
  - **[수정: 같은 날] 그 줄을 제목과 URL 사이가 아니라 URL 다음으로 옮겼다**(사용자 요청 — "혹시 발행일을 url 다음에 놓을 순 없니?"). 처음 자리는 "제목 바로 밑이 읽는 순서상 자연스럽다"는 생각이었는데, **제목 줄과 URL은 이 기능이 생기기 전부터 붙어 있던 한 쌍**이라 그 사이에 뭘 끼우면 원래 있던 두 줄이 갈라진다. 새로 더한 값은 덩어리 맨 아래에 얹는 게 맞았다 — 기존 두 줄은 예전과 완전히 같은 모양으로 남고, 시각은 다음 기사와의 빈 줄 바로 위에 붙어 덩어리의 꼬리표처럼 읽힌다.
- **"게시" 라벨을 붙였다 — 화면과 반대 결정이고, 그 이유가 정확히 반대다.** 화면(`app/renderer.py` `_format_pub_time`)은 2026-07-27에 "그 화면엔 다른 종류의 시각이 섞여 있지 않다"는 이유로 라벨을 뗐는데, 텔레그램은 **메시지마다 자기 전송 시각을 옆에 찍는다** — 거기서 시각만 덩그러니 있으면 그 둘이 헷갈린다. 같은 규칙("라벨은 헷갈릴 때만")을 적용했더니 결론이 반대로 나온 자리다.
- **못 읽으면 줄을 뺀다, 지어내지 않는다.** `pub_date`가 없거나(이 필드 이전 기사·직접 추가) 형식이 깨졌으면 그 기사만 예전 그대로 2줄로 나간다. 오늘이 아니면 `9/15 22:10 게시`로 날짜까지 적는다 — 세 경로 모두 당일 기사만 다루므로 실제로는 안 나올 분기지만, 그 전제가 언젠가 깨졌을 때 `22:10`만 찍혀 나가면 화면이 거짓말을 하게 된다.
- **보고서 텍스트는 한 글자도 안 바뀐다.** PRD의 "발행일 미표시"는 복사·txt·정기 발송 줄 형식의 규칙이고 그쪽은 `_build_plain_text`가 따로 조립하므로, 이 변경은 알림 메시지에만 닿는다(CODING_CONVENTIONS §4와 같은 경계). 통합 테스트 `test_breaking_alert_scoop_detection`에 `pub_date` 있는 기사와 없는 기사를 한 건씩 넣어 두 경로를 같이 검증한다.
- **곧바로 그 시각이 문구 하나를 거짓말로 드러냈다 — "🌙 밤사이·새벽에 놓친".** 시각을 붙이자마자 사용자 제보: **16시 24분 기사**에 그 머리말이 붙었다. 원인은 문구가 아니라 그 경로의 성격이다 — `catch_up_on_wake`(앱 시작 시 1회)는 **자정부터 지금까지**를 훑으므로, 낮에 앱을 다시 켜면(LaunchAgent 재시작·재부팅) 그 낮 기사까지 같이 딸려 온다. "밤사이·새벽"은 이 경로가 메우는 구간의 **한 경우일 뿐인데 늘 그렇게 말하고 있었다.** 실은 이 문서의 바로 위 항목이 2026-09-16 오전에 "(3a)는 앱이 꺼져 있던 동안이라 **감시 시간 한복판일 수도 있고**"라고 이미 적어두고도 문구는 안 고쳤던 자리다 — 게시 시각이 화면에 나오기 전까지는 아무도 그 어긋남을 볼 수 없었다(값 하나를 보여주기 시작하면 그것과 어긋난 문구가 같이 드러난다).
  - **사용자가 제안한 정의를 그대로 썼다** — "밤사이·새벽 = 23시~다음 날 6시"(`_NIGHT_START_HOUR`/`_NIGHT_END_HOUR`). 06:00 정각은 밤이 아니다: 지금 감시 시간대가 06:00에 시작하므로 그 시각부터는 "감시가 도는 시간"이다.
  - **판단은 발행시각으로, 묶음 전체가 밤일 때만.** `now`(앱을 켠 시각)나 마지막 폴링 시각으로 재는 안도 있었지만 둘 다 실제로 나가는 기사와 한 겹 떨어져 있다 — 밤에 켰어도 06:30 기사가 섞일 수 있고, 반대도 마찬가지다. 받는 사람이 보는 건 기사의 시각이므로 그것으로 판단한다. 한 건이라도 `pub_date`를 모르면 "밤"이라고 부르지 않는다(모르는 걸 단정하지 않는다 — 게시 시각 줄과 같은 규칙).
  - **밤이 아닐 때의 문구는 시간을 아예 말하지 않는다** — `📴 앱을 켜기 전에 게시된 [단독] N건`. 이 경로가 **언제나 참인 사실**만 남긴 것이라 다시 틀릴 여지가 없다("낮에 놓친" 같은 두 번째 시간 주장을 만들면 같은 종류의 사고가 반복된다). 이걸 기본으로 두고, 🌙 쪽을 "실제로 밤일 때만 붙는 덤"으로 뒤집은 셈이다.
  - **그래서 `header_fn`의 인자가 건수(int)에서 기사 목록(list)으로 바뀌었다** — 세 문구 함수와 호출부 하나뿐이라 값싼 변경이고, 앞으로도 문구가 기사 내용을 보고 갈릴 수 있게 열어둔 것이다.
  - **[수정: 같은 날, 2차] 세 문구에서 "놓친"을 뺐다**(사용자 지적 — "'놓친'이라는 말이 좀 그래. 차라리 발행된 또는 게시된이 맞지 않을까"). 두 가지가 걸렸다: (a) "놓친"은 **우리가 못 잡았다는 자책**이라 받는 사람에게는 쓸모없는 정보이고, (b) 사실인지도 확실치 않다 — 앱이 도는 동안 폴링이 호출 한도로 멈춰 있었을 수도 있어 "꺼져 있어서 놓쳤다"가 늘 맞지는 않는다. 「발행」과 「게시」 중에서는 **게시**를 골랐다: 바로 아래 기사 줄이 이미 `16:24 게시`라, 머리말과 줄이 한 단어로 이어진다(엑셀 열 이름만 예전부터 "발행일 발행시간"이라 그쪽은 그대로 둔다 — 화면 표시가 아니라 데이터 계약이라서). 밤이 아닐 때의 문구도 「앱이 꺼져 있던 사이」에서 **「앱을 켜기 전에」**로 바꿨다 — 앱이 언제 꺼졌는지는 아무 데도 안 남아 추측이지만, 이 경로가 자정~앱을 켠 순간을 훑는다는 건 코드가 보장하는 사실이다. 결국 세 문구가 전부 "언제 게시됐나"만 말하고, 앱 사정은 이모지(🌙/📴)로만 남았다.
  - **(3b) "🌙 감시 시간 밖에 게시된"은 그대로 뒀다.** 그쪽은 자정~감시 시작을 메우는 경로라 문구가 구조적으로 맞다. 다만 그날 첫 폴링이 감시 시작보다 최대 폴링 주기(3~30분)만큼 늦게 도는 탓에, 그 사이에 나온 기사는 엄밀히 "감시 시간 안"이다 — 3분 주기로 운영 중이라 지금은 최대 3분짜리 어긋남이고, 커지면 그때 같은 방식으로 가른다.

- **메뉴 위치 — "기타"가 아니라 "발송 · 알림"(옛 "자동발송") 카테고리 안, 점선 구분선으로.** 사용자가 처음 제안한 자리는 "기타" 메뉴였는데, 그 카테고리는 이미 "워드클라우드 제외어" 하나뿐인 잡동사니 서랍이라 거기 넣으면 나중에 못 찾을 것 같다는 우려를 전달했다 — "텔레그램 관련은 같은 카테고리에 있어야 찾기 쉽다"는 이유로 자동발송 카테고리 안에 두되, "정기 발송과 구별돼야 한다"는 사용자 요구는 카테고리 분리가 아니라 점선 구분선 + 이름으로 풀었다(자동발송 3항목 아래 `.menu-divider`, CLAUDE.md 참고). 카테고리 이름도 "자동발송"에서 "발송 · 알림"으로 바꿨다 — 이 안에 정기(자동발송)와 즉시(알림) 둘이 같이 있다는 걸 이름에서부터 알 수 있게.
- **메뉴 문구 — "[속보] 알림"이 아니라 "[단독]·[속보] 알림".** 첫 제안은 "[속보] 기사 알림 설정"이었지만, 실제로 보내기로 한 건 `[단독]`이 기본(하루 2~3통 실측)이고 `[속보]`는 부가(잘못 켜면 '코스피' 시황 속보 때문에 하루 60~80통까지 뛴다, 아래 참고)라 이름과 실제 기본 동작이 어긋났다. 사용자가 최종적으로 "[단독]·[속보] 기사 알림"으로 확정했다.
- **속보를 기본으로 다 받으면 안 되는 이유 — '코스피' 하나가 하루 76건.** 실시간 캐시(`data/live_cache.json`, 2026-08-20 기준)로 실측한 결과, 그날 하루 `[속보]` 79건 중 76건이 '코스피' 키워드 하나(증시 시황 속보)에서 나왔고, 정기 스크랩 대상 그룹(`include_in_scrap`)만으로 좁히면 `[속보]` 0건·`[단독]` 2건으로 떨어졌다. 이 실측이 "감시 대상은 전체 키워드가 아니라 담당자가 고른 그룹만"이라는 설계를 뒷받침했다 — 전체로 열면 알림이 무의미해질 정도로 쏟아진다.
- **정기 회차만으로 알았을 때의 지연도 실측했다.** 저장된 20건 기준 회차 발행~수집 지연이 **중앙값 90분, 평균 93분, 최대 168분**(1시간 초과 16건/2시간 초과 5건)이었다 — "바닥"만으로는 담당자가 단독을 최대 3시간 가까이 늦게 알 수 있다는 뜻이라, 낮 시간대 폴링(기본 5분)이 실질적 가치가 있다는 근거가 됐다.
- **수신자 — 별도 목록이 아니라 기존 텔레그램 수신 대상자 표에 체크박스 2열 추가.** 처음엔 "수신대상자와 다른 것"이라는 사용자 말을 "완전히 별도 화면·별도 저장소"로 해석해 두 축(정기 발송 여부/알림 여부)을 표현하는 안을 냈는데, 목업을 만들며 실수로 "정기 보고서" 열을 새로 그려 넣었다가 — **이미 그 자리(각 행 왼쪽의 ON/OFF 토글 스위치)에 정기 발송 여부가 있었다**는 걸 사용자가 캡처로 지적해 바로잡았다. `chat_id`는 눈으로 검증 안 되는 숫자열이라 두 군데 입력하게 두면 한쪽만 고치는 사고가 나기 쉽다는 점도 별도 목록을 기각한 이유였다. 최종적으로 `data/telegram_recipients.json`의 각 항목에 `alert_scoop`/`alert_flash` 두 필드만 추가해 기존 `enabled`(정기)와 독립적으로 두는 것으로 정리했다 — "나"는 정기(`enabled`)는 끄고 `alert_scoop`만 켜는 식으로 표현된다.
- **시안(Artifact) 제작 순서.** 사용자가 화면을 먼저 보고 싶다고 해, `BREAKING_ALERT_MOCKUP.html`을 만들어 실제 설정 화면과 같은 색·컴포넌트(`app/settings_server.py` `_BASE_STYLE`)로 두 화면(알림 설정 / 수신 대상자)과 실제 텔레그램 메시지 미리보기를 함께 그렸다. 호출량 계산기를 JS로 넣어 브라우저에서 실제로 그룹 체크박스·시간·주기를 바꿔보며 숫자가 맞는지(9개/5분/09-20시 → 1,188회, 20개/24시간 → 5,760회, 80개/24시간 → 23,040회·경고색 전환) 자바스크립트로 직접 검증한 뒤 SendUserFile로 전달했다 — 이 계산 로직을 실제 구현 단계에서 설정 화면의 JS로 그대로 재사용했다.
- **구현 — 세 경로가 함수 하나(`detect_and_alert`)를 공유하도록 설계.** 바닥(`app/scraper.py`)·폴링(`app/scheduler.py` tick)·몰아보내기(`main.py` 시작 시 1회) 세 진입점이 전부 같은 판정·발송·중복방지 로직을 타야 "같은 기사를 세 경로가 각자 찾아도 한 번만 나간다"가 보장된다 — 처음부터 공유 함수로 설계했고, 실제로 통합 테스트(`tests/test_integration.py` `test_breaking_alert_scoop_detection`)에서 이 함수 하나만 검증하면 셋 다 보장됨을 확인했다.
- **테스트 격리에서 실수로 실제 `data/`에 흔적을 남길 뻔했다.** `app.naver_api._search_one_keyword`의 실제 요청 루프에 `record_api_call()`을 심었는데, 기존 통합 테스트 중 `requests.get`을 직접 모킹해 그 루프를 그대로 타는 테스트(`test_search_window_filters_by_time`)가 실제 `data/api_usage.json`에 카운트를 남겼다(실측: 여러 번 테스트를 돌리며 `{"date": "2026-08-20", "count": 5}`까지 쌓임) — `collect_run`을 완전히 도는 다른 테스트도 새로 추가한 `detect_and_alert` 경로 때문에 `data/alerted_urls.json`을 건드릴 수 있었다. 둘 다 CLAUDE.md의 "실제 data/ 폴더는 건드리지 않는다" 테스트 원칙을 어길 뻔한 것이라, 발견 즉시 `patch.object(api_usage, "API_USAGE_FILE", ...)`/`patch.object(alerted_urls, "ALERTED_URLS_FILE", ...)`를 해당 테스트들에 추가하고, 테스트가 이미 만들어 둔 실제 파일(`data/api_usage.json`)은 삭제 대신 작업 규칙대로 `trash-can/`으로 옮겼다.
- **검증 — 실제로 돌고 있는 사용자 프로세스(포트 8765)를 건드리지 않고 확인.** 확인하러 보니 사용자의 실제 `python3 main.py`가 이미 포트 8765에서 돌고 있었다(실사용 중인 프로세스로 추정) — 재시작하면 스케줄러가 끊기고 마침 실제 텔레그램 수신자("홍정희")가 등록돼 있어 잘못하면 진짜 메시지가 나갈 위험도 있었다. 그래서 `run_settings_server(port=8766)`로 **별도 포트에 같은 코드**를 띄워 GET 요청(읽기 전용)으로만 메뉴·알림 설정·텔레그램 화면을 브라우저로 열어 확인했고, "저장" 버튼은 누르지 않아 실제 `data/settings.json`·`data/telegram_recipients.json`을 전혀 건드리지 않았다(확인 후 `git status --porcelain data/`로 무변경을 재확인). 실제 등록된 그룹(재정경제부 관련 등 4개)과 실제 수신자(홍정희)가 화면에 정확히 반영되는 것, JS 예상 호출량 계산기가 실시간으로 갱신되는 것(그룹 체크 시 "키워드 6개 × 하루 132회 확인 = 네이버 호출 792회/일(3.2%)")까지 확인했다.

## 동향 보고(media_report) 통째 제거 — 2026-08-20

담당자 요청: **"이 단계에서는 동향 리포트를 아예 생각하지 않으려 한다. 계획에서도 빼라."**
당장 구현할 게 아닌데 문서·코드 곳곳에 개념이 남아 있어 매번 판단 비용을 만들고 있었다.

### 무엇이었나

"언론동향 보고" — 확정된 스크랩 회차를 별도 프로젝트(`media_report`)로 넘겨 AI 분석·보고서를
만들려던 구상이다. `my_app` 쪽에는 **핸드오프 JSON 계약**만 있었고(`app/export.py`), 받는 쪽
프로젝트는 끝까지 존재하지 않았다. 화면은 이미 두 번 걷어낸 상태였다 — 확정본의 "동향" 플로팅
버튼(2026-08-12, 눌러도 토스트만 뜨는데 (발송)과 같은 무게로 보여서), 홈 화면의 "동향 보고"
준비중 3칸(2026-08-18, 안 눌리는 칸이 화면 절반을 차지해서).

### 왜 이번엔 코드까지 걷어냈나

**화면만 지운 상태가 오히려 나빴다.** 버튼과 칸은 사라졌는데 백엔드는 계속 돌고 있었다 —
회차를 확정할 때마다 `confirm_and_promote`가 `export_latest_run()`을 불러 `data/export/`에
JSON을 쌓았고(제거 시점 실측 **41개**, 8/13~8/20분), 아무도 그 파일을 읽지 않았다. 화면에
없는 기능이 조용히 파일을 쓰고 있는 상태다.

더 나쁜 건 **이 계약이 다른 코드의 모양을 붙들고 있었다**는 점이다. `app/summarizer.py`의
`use_llm_summary` 파라미터는 순전히 `export.py` 하나 때문에 존재했다 — "`rule_based_summaries`
필드는 값싼 참고용 힌트일 뿐 LLM 요약을 대체하지 않는다"는 media_report와의 이름 약속을
지키려고 만든 분기(2026-08-12, 코드 리뷰에서 계약 파손을 잡아낸 자리)인데, 받는 쪽이 없으면
지킬 약속도 없다. 쓰지 않는 기능이 쓰는 기능의 시그니처를 넓혀 두는 상태라 함께 정리했다.

### 지운 것

- `app/export.py` 통째 (→ `trash-can/`)
- `app/confirm_send.py` — `export_latest_run()` 호출과 그 `try/except`. 이 예외 처리가
  파일 안 유일한 `logger` 사용처였어서 `import logging`·`logger` 선언도 함께 죽었다
- `app/config.py` — `EXPORT_RETENTION_DAYS`(7). 회차 1년(`RETENTION_DAYS`)과 **일부러 분리해
  뒀던** 상수다("넘겨주면 끝인 인수인계 파일은 오래 들고 있을 이유가 없다")
- `app/scheduler.py` — `cleanup_exports` 파라미터·기본값·매 tick 호출, `main.py` — 시작 시 호출
- `app/summarizer.py` — `use_llm_summary` 파라미터(위 참고)
- 주석·문서의 개념 언급 전부: `renderer.py`(플로팅 버튼 제거 경위, 엑셀 데이터 계약 근거),
  `preview_renderer.py`(연보라 색 언어의 출처), `landing_renderer.py`, `classifier.py`,
  `curation.py`, PRD.md(기능3 규칙3-2 + 비범위 항목), prd_lite.md, CLAUDE.md 5곳,
  README.md, ADHOC_DESIGN.md
- `data/export/` JSON 41개, 동향 관련 목업 3개 (→ `trash-can/`)

### 남긴 것과 그 이유

- **홈 화면 h1 "AI 기반 뉴스 모니터링 및 언론동향 보고"** — 담당자 지시로 유지. 이건 기능
  이름이 아니라 앱 이름이다
- **연보라(`#F0EAFB`) = "AI가 하는 일" 색 언어** — 원래 홈 화면 동향 흐름에서 가져온
  색이지만(2026-08-12), 지금은 🤖 재분류 버튼들이 독립적으로 쓰는 규칙이라 그대로 둔다.
  `preview_renderer.py`의 주석에서 출처만 CLAUDE.md "Design direction"으로 바꿨다
- **이 기록** — PRD·CLAUDE·README에서는 규칙을 통째로 뺐지만 여기는 남긴다. 담당자와
  합의한 분업이다("HISTORY를 지우면 6개월 뒤에 누가 똑같은 걸 또 만들자고 한다")

### 되살릴 때

`app/export.py`를 `trash-can/2026-08-20_동향제거/code/`에서 꺼내면 계약 자체는 그대로 살아난다.
다만 그때 두 가지를 다시 정해야 한다 — (1) `summarize_group`에 규칙 기반 강제 분기를 다시
넣을지, (2) `data/export/` 보관 기간을 회차와 분리할지. 둘 다 이번에 지운 결정이다.

## 문서-코드 대조 정리 — 2026-08-20

동향 보고 제거 직후 "지금 문서-코드 불일치가 또 뭐가 있나" 점검에서 나온 것들을 한 번에 맞췄다.
**공통 원인은 하나다 — 2026-08-18에 설계를 확정하며 문서에 "미구현" 딱지를 붙였고, 그날
바로 구현했는데 딱지를 떼지 않았다.** 설계와 구현이 같은 날 이어질 때 생기는 전형적인 자국이다.

- **CLAUDE.md가 자기 자신과 모순됐다.** "Data retention" 항목은 "수시(`data/adhoc/`)에는 자동
  삭제가 아예 없다 … PRD 기능9 규칙9는 1년으로 맞추자고 하나 미구현"이라 했는데, 같은 파일의
  "수시 모니터링" 절은 "정기와 같은 1년"이라 했고 코드(`delete_expired_cards`)도 실제로 돌고
  있었다. 한 문서 안에서 정반대를 말하고 있었으므로 읽는 사람이 어느 쪽도 믿을 수 없는 상태였다.
- **PRD·prd_lite의 "미구현" 딱지 20곳이 전부 이미 구현돼 있었다** — 라벨(기능10), 데이터
  내보내기(기능11), 진입 화면 4갈래(기능3 규칙3-1), 정기 보관함 이름+1년, 수시 3단계 이름,
  수시 1년 보관, 실시간/초안/확정본 이름 변경. 화면 렌더 결과·라우트·상수값으로 하나씩 확인한 뒤 뗐다.
- **딱 하나 진짜 미구현이 섞여 있었다 — 수시 카드의 [지금 넘기기] 버튼.** 자정 자동 이월은
  동작하지만 앞당기는 버튼은 코드에 없다(`grep` 0건). 나머지를 "구현 완료"로 바꾸면서 이것만
  "미구현"으로 남겨 명시했다 — 전부 일괄로 뒤집었으면 없는 기능이 있는 것처럼 굳었을 자리다.
- **PRD에 [단독]·[속보] 알림(기능12)이 통째로 없었다.** CLAUDE.md와 HISTORY.md에는 상세히
  적혀 있고 코드도 모듈 5개 + 스케줄러 통합까지 끝났는데, 정작 요구사항 문서에만 없었다.
  같은 날 만들어 CLAUDE.md만 갱신하고 PRD로 돌아가지 않은 것이다. 기능12로 새로 썼다.
- **재수집 규칙이 뒤집힌 게 PRD에 반영 안 돼 있었다.** 2026-08-20에 "병합 → 교체"로 바꿨는데
  (위 "수시 모니터링 — 검색어 편집 · 재수집이 병합에서 교체로" 참고) PRD 기능9는 여전히
  "규칙3의 병합 그대로"라고 적혀 있었고, ADHOC_DESIGN.md §6.4는 스펙을 뒤집어 놓고도
  "(구현 전 — 코드는 아직 병합이고)"라는 괄호를 달고 있었다. 둘 다 정정.
  PRD 기능9 규칙11의 "1차 범위에서 제외: ↩ 되돌리기"도 마찬가지로 이미 구현된 것이었다.
- **고아 상수 `ROUND_GROUPING_FILE`을 지웠다** (`app/config.py`). 주석이 `app.round_grouping`
  모듈을 설명하는데 **그 모듈이 존재한 적이 없고**(git 이력에도 없음), 상수를 읽는 코드도 0곳,
  문서 언급도 0회였다. 증분 분류의 파일 기반 저장이라는 구상만 남고 실제로는
  `app/llm_classifier.py`의 메모리 캐시 + 디스크 캐시로 풀린 흔적으로 보인다.
- **`DESIGN.md` 첫 줄이 삭제된 `PLAN.md`를 가리키고 있었다**(2026-08-12 문서 다이어트 때 없어진
  파일). **`app/sorter.py`는 어느 문서에도 파일명이 없었다** — 언론사 우선순위 정렬이라는 핵심
  규칙을 정기·초안·수시 셋이 공유하는 함수인데도. CLAUDE.md "Outlet ordering" 항목에 명시했다.
  **`MULTIUSER_ROLLOUT_CHECKLIST.md`는 어느 문서도 가리키지 않는 고아 문서**였어서 CLAUDE.md에 연결했다.

**남겨둔 것**: README.md는 손대지 않았다 — 수시 모니터링·라벨·엑셀 내보내기가 통째로 빠져 있고
`app/` 파일 목록에 14개, `data/` 목록에 9개가 누락된 상태라 정정이 아니라 재작성에 가깝다.
CLAUDE.md의 값 세 개(`openpyxl` 누락, "정기는 8"→실제 15, `_MAX_ARTICLES`→실제
`MAX_ARTICLES_FOR_CLASSIFY`)도 다음 차례로 미뤘다.

## 발송 실패 저장 배선 (2026-08-26)

바로 위 항목("정기 보관함 — 발송 칩을 점으로")에서 빨간 실패 점의 모양만 그려두고 뒤로
미뤘던 것 — **"이미 아는 사유를 버리지 않고 저장하는" 배선**을 실제로 붙였다. 사용자
지시: "실패 저장 배선부터 시작해줘."

### 무엇이 바뀌었나

`app/telegram_bot.py`/`app/email_sender.py`의 `send_text()`가 `bool` 대신
`app.send_result.SendResult`를 돌려준다 — `ok`(성공 여부)와 `failures`(대상별 실패
사유 목록)를 함께 담는 작은 타입이다. `bool()`로 평가하면 `.ok`만 보므로 기존 호출부
(`app/breaking_alert_sender.py`의 반환값 무시, `app/confirm_send.py`·
`app/settings_server.py`의 `if send_text(...):` 형태 truthy 체크)는 **한 줄도 안
고쳤다** — 새 정보가 필요한 호출부만 `.failures`를 들여다본다.

두 함수 다 이전엔 실패 이유를 `logger.warning`/`logger.exception`에만 남기고 버렸다.
이제 그 이유를 사람이 읽을 한국어 한 줄로 바꿔(`_describe_error`, 텔레그램은 HTTP
상태 코드별로 401/403/400/429를 구분, 이메일은 SMTP 예외 타입별로 인증 실패/수신
거부/연결 실패를 구분) `SendResult.failures`에 담아 호출부까지 전달한다.

`app/confirm_send.py` `send_confirmed_run`이 텔레그램·이메일 두 채널의 실패를 받는
사람 **이름**으로 바꿔(`load_telegram_recipients`/`load_email_recipients`로 chat_id/
이메일 → name 매핑) 하나의 목록으로 합치고, `app.storage.record_send_failure`로 회차
파일에 남긴다:

```
run["send_failure"] = {
  "at": "2026-08-26T13:49:18",
  "auto": true,
  "channels": [{"channel": "텔레그램", "target": "홍길동", "error": "봇 토큰이 유효하지 않습니다(401) — ..."}]
}
```

### 설계 결정 세 가지

1. **`send_count`/`sent_by`/`sent_at`(성공 기록)과 완전히 별개 필드다.** "채널 하나라도
   전원 성공하면 발송 완료로 본다"는 기존 판단(2026-08-24)은 안 건드렸다 — 텔레그램이
   부분 실패해도 이메일이 전원 성공하면 여전히 `send_count`가 올라가 "발송 완료"다.
   대신 텔레그램 실패는 `send_failure`에 남아, "성공은 했는데 일부는 못 받았다"를
   놓치지 않는다. 필드를 합쳤다면 이 부분 성공/부분 실패 상태를 표현할 자리가 없었다.
2. **채널에 활성 받는 사람이 아예 없으면 실패로 안 친다.** `chat_ids`/`recipients`가
   비어 있으면(그 채널을 안 쓰기로 함) `send_text` 자체를 호출하지 않는다 — 안 쓰는
   게 정상인 채널까지 "실패"로 기록하면, 이메일을 애초에 안 쓰는 사용자의 회차마다
   매번 빨간 점이 뜨는 거짓 경보가 된다.
3. **원인이 지난번과 같으면 회차 파일을 다시 안 쓴다.** 자동 발송은 실패할 때마다
   10초 tick마다 재시도되는데(`check_pending_confirm_and_send`), 회차 파일 하나가
   수십~수백 KB라 원인이 안 바뀐 채 몇 시간씩 재시도되면 디스크 쓰기가 그만큼
   반복된다. `record_send_failure`가 `send_failure.channels`를 이전 값과 비교해
   똑같으면 건너뛴다 — 시각(`at`)만 갱신하자고 매번 다시 쓰지 않는다.

### 검증

실제 텔레그램/이메일 서버를 호출하지 않고 `requests.post`/`smtplib.SMTP`를
몽키패치해 두 단계로 확인했다(실제 `data/` 폴더는 건드리지 않도록
`tests/test_integration.py`와 같은 `patch.object(storage, "ARTICLES_DIR", ...)`
격리 패턴을 재사용):

- **전송 함수 단위**: 토큰 미설정·401·403(부분 실패)·SMTP 인증 실패·수신 거부(부분
  실패) 다섯 경우 모두 `SendResult.ok`와 `.failures`가 기대한 그대로 나오는지.
- **`send_confirmed_run` 통합**: (1) 전체 실패 → `send_failure`만 남고 `send_count`는
  그대로, (2) 같은 원인으로 재시도 → 회차 파일 재작성 없음(`at` 타임스탬프 불변),
  (3) 텔레그램은 그대로 실패·이메일만 성공 → `send_count` 증가하면서도 텔레그램
  실패는 남음, (4) 둘 다 성공 → `send_failure`가 파일에서까지 실제로 지워짐. 네
  시나리오 모두 통과.

### 이어서 — 정기 보관함 화면에 붙임 (같은 날)

사용자가 "응 이어서 붙여줘"로 화면 표시까지 이어서 진행. `app/history_renderer.py`
`_render_slot`에 실패 우선 분기를 추가했다:

- `run["send_failure"]`가 있으면 **성공 점보다 우선해서** 속 빈 빨간 링(`.sent-dot.fail`,
  9px·2px 테두리 — 채운 초록 점(7px)과 굵기를 감안해 살짝 키워 "같은 무게"로 보이게 함)을
  그린다. `send_count > 0`(부분 실패 — 다른 채널은 성공해 이미 "발송 완료"로 확정된 경우)
  이어도 실패 링이 이긴다 — "뭔가 안 갔다"는 담당자가 먼저 봐야 할 신호이기 때문이다.
- `_send_failure_tooltip(run)`이 `send_failure.channels`를 "채널 — 대상: 사유" 형태로
  줄마다 풀어 `title`에 심는다(`&#10;`로 줄바꿈, 기존 메모 툴팁과 같은 방식). 부분 실패면
  첫 줄에 "일부 채널만 실패했습니다(다른 채널은 정상 발송)"를 덧붙인다 — 안 그러면 빨간
  링만 보고 아무 것도 안 나간 걸로 오해할 수 있다.

**검증**: 실제 운영 데이터(`data/articles/2026-08-20_14-00.json`, 이미 발송 완료 기록이
있던 진짜 회차)를 백업한 뒤 `send_failure`를 임시로 주입해 실제 서버가 새 코드로 렌더링한
결과를 브라우저로 직접 확인했다(빨간 링이 그려짐, `title` 속성에 정확한 문구가 들어감 —
자동화 도구의 hover가 네이티브 OS 툴팁을 스크린샷에 못 담는 한계가 있어 마지막 확인은
소스 검사로 대신했다). 확인 직후 백업으로 원본 파일을 복원하고 `history.html`을
재생성해 실제 데이터에는 흔적을 남기지 않았다.

## 정기 보관함 — 발송 칩을 점으로, 키워드 메모를 회색 글자로 (2026-08-26)

키워드 작성 메모(바로 위 항목)를 만들고 나서 사용자가 낸 요청: **"보관함에는 키워드가 안
붙는데, 00:00 기준 옆에 키워드가 붙으면 어떨까?"**

### 왜 칩이 아니라 점인가

처음엔 메모도 발송 칩(`발송 완료`/`자동 발송`)과 같은 모양의 칩으로 붙이려고 했다. 그런데
사용자 지적: **"사람들이 생각보다 발송 완료 정보보다 키워드 정보를 더 보거든."** 하루 4~6회차가
거의 다 정상 발송되는 게 기본값이라 그 칩은 사실상 배경 소음에 가까웠고, 정작 자주 찾는
정보(그 회차에 뭘 적어놨는지)는 화면에 아예 없었다.

그래서 무게를 뒤집었다 — 발송 칩을 **점 하나**로 낮추고(`.sent-dot`, 성공만 있던 초록 꽉 찬
점), 그 자리를 키워드 메모가 가져간다. 시각·자동/수동 구분은 사라지지 않고 `title` 툴팁으로
옮겼을 뿐이다("11시 32분 발송 완료"/"17시 5분에 자동 발송 완료").

### 칩으로 만들 뻔한 걸 한 번 더 지적받았다

처음 목업엔 메모도 회색 **칩**(`.chip-note`, 테두리 있는 pill 모양)이었다. 사용자 지적:
**"굳이 칩식으로 할 필욘 없어. 이건 단순 텍스트 나열이기 때문이야. 데이터로 쓸 수 있는
정보가 사실 못 되고, 참고용이지."** 맞는 지적이었다 — 이 앱에서 테두리 두른 칩은 전부
구조화된 값(라벨, 발송 상태, 오늘 여부)이었는데, 메모는 담당자가 아무 형식 없이 적은 자유
텍스트라 칩으로 감싸면 실제보다 "정돈된 데이터"처럼 읽힌다. 확정본이 `🔍 검색어`를 칩이
아니라 회색 글자로 두는 것과 같은 이유였는데도 처음엔 안 맞췄었다. 최종적으로 `.slot-note`
— 테두리도 배경도 없는 회색 글자, 남는 폭까지만 쓰고 잘리면 `title` 툴팁에 전체.

칩을 글자로 바꾸면서 걸린 게 하나 있었다 — 칩엔 `flex: none`이 있어 안 눌렸는데, 일반
글자는 `flex: 1 1 auto`를 안 주면 긴 메모가 폭을 다 먹어 옆의 회차 라벨("17:00 기준")까지
두 줄로 꺾였다. 라벨에 `flex: none`을 못박아 라벨은 절대 안 줄고 메모만 줄어들게 했다.

### 발송 실패도 점으로 — 색이 아니라 형태로 가른다

이어서 발송 실패 표시도 의논했다. 처음엔 "빨간 칩"으로 그렸는데, 사용자가 물었다:
**"발송실패는 빨간 점으로 뜨는게 어때? 마우스 오버하면 그 실패 원인이 뜨도록."** 방향에
동의해 성공(꽉 찬 초록 점)과 실패(속 빈 빨간 링)를 **같은 자리, 같은 축**의 점으로
통일했다. 색만 바꾸지 않고 **형태**를 가른 이유 — 같은 크기·같은 모양에 색만 다르면
적록색약에서 구분이 안 되고, 7px짜리 작은 점이면 그 문제가 더 심해진다. 채움 vs 링이면
색을 못 봐도 갈린다.

이어서 사용자가 짚은 것: **"그럼 마우스에 커서 대면 발송 안되는 사유가 나온다는거야?"**
답은 "그렇다, 그리고 그 사유는 이미 앱이 알고 있다"였다 — `app.telegram_bot.send_text`/
`app.email_sender.send_text`가 실패할 때마다 `logger.warning`/`logger.exception`으로
구체적 원인(HTTP 상태 코드, SMTP 예외)을 남기고 있는데, 지금은 **로그로만 흘러가고
회차 파일엔 저장되지 않는다**. 두 함수 모두 `bool`만 돌려줘 호출부(`app.confirm_send`)는
"안 됐다"만 알고 왜인지는 모른다. 그래서 이 작업의 실제 본질은 "새 사유를 알아내는 것"이
아니라 "이미 아는 사유를 버리지 않고 저장하는 것"이다 — 반환값을 `bool`에서
`(성공여부, 사유)`로 바꾸는 배선이 먼저 필요하다.

추가로 짚어둔 두 가지 (아직 미해결, 발송 실패 저장을 실제로 붙일 때 같이 볼 것):

- **자동 재시도가 "실패 확정" 시점을 지운다.** `app.confirm_send.check_pending_confirm_and_send`는
  `send_count==0`인 한 매 tick(10초)마다 재시도하는데, `load_latest_run()`만 보므로 **다음
  회차가 수집되는 순간 이전 회차의 실패 시도 이력은 통째로 버려진다**. "실패로 확정"하는
  시점은 회차가 넘어가는 순간으로 잡아야 한다.
- **부분 실패가 성공으로 뭉개진다.** 지금은 텔레그램/이메일 중 하나만, 그리고 그 채널의
  받는 사람 중 한 명만 성공해도 "발송 완료"다 — 수신자 3명 중 1명이 봇을 차단해도 나머지
  2명이 받으면 성공 처리되고, 그 1명은 원인도 모른 채 계속 못 받는다.
- **보관함의 실패 표시는 사후 정보다.** 실패에 실제로 대응하는 자리는 그날의 확정본이지
  지난 기록을 훑는 이 화면이 아니다. 조용한 점으로 충분하지만, 확정본 쪽에서 실패를
  실시간으로 알리는 장치가 지금은 아예 없다 — 봇 토큰이 만료돼도 담당자가 화면에서 알
  방법이 없다. 이건 이 작업과 별개로 필요하다.

### 이번에 실제로 구현한 것

메모(회색 글자)와 발송 성공 점(초록 꽉 찬 원)만 붙였다 — 발송 실패는 저장되는 데이터가
없어 목업 단계로 남겨뒀다. `app/history_renderer.py` `_render_slot`이 그 회차의
`load_manual_keyword_note((run_at 날짜, run_slot))`을 그대로 읽어 붙인다(이어받은 값은
`load_manual_keyword_note_entry`가 초안·확정본 **화면에서만** 보여주는 값이라 보관함엔
안 새어든다 — 위 항목의 "화면 표시 vs 내보내기 텍스트" 구분과 같은 경계). `chip-sent` CSS
클래스는 이제 이 화면에서 안 쓰여 지웠다.

## 키워드 작성 메모 — 회차가 바뀌면 통째로 사라지던 문제 (2026-08-26)

사용자 제보: **"키워드 작성하다가 확정본 넘어가면 아예 키워드 자체가 사라진다. 이전
키워드라도 남아있어야지."**

### 무슨 일이 있었나 (실측)

제보 시점 저장 파일이 이랬다.

```
data/manual_keyword_note.json
{"text": "증시 변동성 레버리지, 비거주 1주택 등.", "date": "2026-08-26", "run_slot": "14:00"}
```

그때 상황을 그대로 재현하면:

```
now              2026-08-26 11:02
next_pending_slot   14:00   ← 초안이 보여주는 회차, 메모가 여기 붙어있다
가장 최근 회차       11:00   ← 확정본이 보여주는 회차
load_manual_keyword_note(('2026-08-26','11:00')) → ''
```

원인이 둘이었다.

**(1) 초안이 보는 회차는 늘 "아직 안 끝난" 회차다.** `_current_round_key()`가
`next_pending_slot()`을 쓴다 — 11:00 회차가 수집된 직후부터 초안은 이미 14:00 회차이고,
거기 적은 메모는 `(오늘, 14:00)`에 저장된다. 그런데 같은 순간 확정본은 **직전** 11:00
회차를 보여주므로 키가 안 맞아 무조건 빈 문자열. 2026-08-07에 "회차 전환 시 옛 메모가
새 회차로 새는" 문제를 고치려고 넣은 회차 키가, 반대 방향에서는 "지금 쓴 메모가 옆
화면에서 안 보이는" 문제가 됐다. 설계 의도("초안 메모가 **그 회차** 확정본까지 이어진다")
자체는 맞지만, 실제 작업 흐름은 "초안에 쓰다가 지금 확정본을 본다"였다.

**(2) 파일에 메모가 딱 1건만 들어갔다.** `save_manual_keyword_note`가
`{text, date, run_slot}` 한 덩어리를 통째로 덮어썼다 — 회차별 보관이 아니라 최신 1건만.
그래서 14:00 메모를 저장하는 순간 11:00 회차에 적어둔 메모는 **파일에서 없어졌다**.
"이전 키워드라도 남아있어야지"가 지금 구조에선 물리적으로 불가능했던 이유다.

덤으로, 확정본이 메모를 찾을 때 기준 날짜로 `datetime.now()`를 썼다 — 자정을 넘겨 어제
마지막 회차 확정본을 열면 날짜까지 어긋나 또 빈칸이 됐다.

### 고른 방향

세 안 중 사용자가 **"회차별 저장 + 이어받기"**를 골랐다(나머지: 회차별 저장만 / 회차 구분을
없애고 하루 단위로).

- 저장 형식을 `{"notes": {"YYYY-MM-DD|HH:MM": 문구}}`로 — 덮어쓰기가 원천적으로 없다.
  옛 형식도 그대로 읽고(`_read_notes`), 다음 저장 때 새 형식으로 옮겨 적힌다. 상한 60건은
  파일이 무한정 늘지 않게만 막는 값이고, 화면이 실제로 쓰는 건 두 회차뿐이다.
- 그 회차 메모가 없으면 **같은 날짜의 가장 가까운 회차** 메모를 끌어와 입력칸에 미리
  채운다. 처음엔 "직전 회차"로 짰다가 실측에서 바로 틀린 걸 확인했다 — 위 (1) 때문에
  이어받을 대상이 시간상 **뒤**(14:00)에 있는 게 정확히 제보된 상황이라, 앞뒤 양쪽을
  다 본다(동률이면 이전 회차 우선). 날짜는 안 넘는다(자정에 비우는 다른 저장소와 같은 관례).

### 이어받은 값은 보고서로 안 샌다

이어받기를 "그냥 보이게" 만들면, 담당자가 이번 회차용으로 **저장한 적 없는** 문구가
복사·txt·텔레그램·이메일 텍스트의 `- {메모}` 줄로 조용히 나간다(CODING_CONVENTIONS.md §4).
그래서 함수를 둘로 갈랐다 — 내보내기 텍스트가 쓰는 `load_manual_keyword_note()`는 예나
지금이나 **정확히 그 회차에 저장된** 메모만 돌려주고, 이어받기는 화면 전용
`load_manual_keyword_note_entry()`에만 있다. 이어받은 상태의 입력칸은 잠기지 않고 버튼도
"저장"이며, 앰버 안내 한 줄이 "N시 회차에 적어둔 메모예요 — [저장]을 눌러야 이번 회차
메모로 남습니다"라고 말한다. 앰버를 쓴 건 이게 설정 화면의 "저장 안 함"과 같은 뜻이라서다.

### 곁다리 — 같은 6줄이 다섯 자리에 흩어져 있었다

메모 칸의 템플릿 변수(`note_open_class`/`note_value_attr`/…)가 확정본 1자리, 초안 4자리에
똑같이 복붙돼 있었다. 이어받기 상태를 추가하려면 다섯 곳을 같이 고쳐야 했고, 그건 한쪽만
빠뜨리는 순간 두 화면이 어긋나는 구조다 — `app/renderer.py`
`keyword_note_template_vars()` 하나로 합쳤다(초안은 `**` 전개로 그대로 받는다).

## 키워드 작성 메모 — 회차 이어받기 되돌림, "취소"가 먹통이던 문제 (2026-08-27)

바로 위 2026-08-26 변경으로 들어온 "그 회차 메모가 없으면 가장 가까운 회차 메모를 이어받아
입력칸에 미리 채운다"를 다음날 되돌렸다.

### 증상 (사용자 제보)

이어받은 메모가 채워진 채로 열린 화면에서 "취소" 버튼을 눌러도 **아무 반응이 없다** —
칸이 안 닫히고 메모도 그대로 남는다.

### 원인

이어받은 상태는 `saved = False`라(`entry["inherited"]`가 True면 저장된 걸로 안 침,
`app/renderer.py` `keyword_note_template_vars`) 버튼이 "삭제"가 아니라 "취소"이고,
그 동작은 서버 호출 없는 `cancelKeywordNote() { location.reload(); }`
(`app/renderer.py`/`app/preview_renderer.py`) 하나뿐이다.

이 "새로고침만 하면 된다"는 전제는 2026-08-10에 이 버튼을 만들 때는 맞았다 — 그땐
저장 안 된 칸은 항상 빈 칸이라, 새로고침하면 서버가 다시 빈 칸을 그려주고 칸이 닫혔다.
그런데 2026-08-26에 "저장 안 됐는데 값이 있는" 이어받기 상태가 새로 생기면서 그 전제가
깨졌다 — `load_manual_keyword_note_entry()`가 **매 렌더링마다** 같은 이어받기 계산을
다시 하므로, 새로고침해도 서버가 아까와 똑같은 이어받은 메모를 다시 채워 돌려준다. 코드는
정상 동작 중이었고(취소 버튼도 새로고침도 실행됨), 다만 그 결과가 매번 이전과 동일해서
"먹통"으로 보인 것.

### 고친 방법 — 버그 수정이 아니라 기능 되돌림

땜질(이어받은 상태의 취소를 빈 값 저장으로 바꾸는 것 등)도 가능했지만, 사용자가 애초에
"이전 회차 메모를 다음 회차에 끌고 오지 않는 게 맞다 — 헷갈리기만 하고, 회차별 메모는
담당자 재량"이라고 판단해 이어받기 기능 자체를 되돌리기로 했다. 그래서:

- `app/manual_keyword_note.py` `load_manual_keyword_note_entry()`(가장 가까운 회차 검색,
  `_slot_minutes()`)를 통째로 없앴다. 화면 입력칸도 `load_manual_keyword_note()` 하나만
  쓴다 — 그 회차에 저장된 게 없으면 화면도 그냥 빈 칸으로 시작한다(이전처럼 2026-08-07
  당시 동작으로 회귀).
- `app/renderer.py` `keyword_note_template_vars()`에서 `saved = bool(text)`로 단순화
  (더 이상 `inherited` 분기가 없다). 앰버 안내 문구(`.note-inherited-hint`)와 그 CSS,
  8번째 템플릿 변수(`note_inherited_hint_html`)도 같이 뺐다 — 이제 7개.
- 부작용으로 취소 버그도 자연히 사라진다: 이어받기가 없으니 저장 안 된 칸은 다시 항상
  빈 칸이고, "새로고침하면 빈 칸으로 돌아온다"는 `cancelKeywordNote()`의 원래 전제가
  다시 맞다.

## 키워드 작성 메모 — 확정본에서만 상단 고정이 풀려 있던 문제 (2026-09-07)

"[확정본]에서는 키워드 메모가 상단 고정이 안 된다, [초안]과 다르게"라는 제보.

두 화면의 `.keyword-note-zone` CSS는 글자 하나까지 같았다(`position: sticky; top: 60px;
z-index: 15`) — sticky를 깨는 조상 요소(`overflow`/`transform`/`contain`)도 없었고, 실제로
브라우저에서 재보면 **평소에는 확정본도 60px에 정확히 붙었다**. 차이를 만든 건 메모 칸이
아니라 그 위에 뜨는 배너였다.

확정본의 「마감 후 자동 배정」 배너(`.finalize-banner`)가 뜨면 `body.has-finalize-banner
.topbar { top: var(--finalize-banner-h) }`가 상단바를 배너 높이만큼 밀어내는데, **메모 칸의
`top`은 60px 상수 그대로**라 그 자리에 그냥 머물렀다. 배너 56px + 상단바 54.5px = 110.5px가
위를 덮고 메모는 60px에 붙으니, z-index(15 < 20·21)까지 져서 **화면에서 통째로 사라진다**
— 담당자 눈에는 "고정이 안 되고 스크롤에 밀려 올라간 것"으로 보인다. 실측(1200px 폭,
배너 표시 상태): 메모 상단 60px, 상단바 하단 110.5px.

초안에도 같은 구조의 배너(`.round-over-banner` — 회차 마감 알림)가 있어 원리상 같은 버그를
갖고 있었지만, 그 배너는 **마감 순간 그 탭을 열어두고 있어야만** 뜨는 반면 확정본의 배너는
자동 배정된 기사가 있으면 화면을 열 때마다 떠 있어서, 확정본에서만 늘 깨져 보였다.

고친 건 두 줄이다 — 상단바가 따라가는 그 변수를 메모 칸도 같이 보게 했다.

- `app/renderer.py`: `body.has-finalize-banner .keyword-note-zone { top: calc(60px + var(--finalize-banner-h, 0px)); }`
- `app/preview_renderer.py`: `body.round-over .keyword-note-zone { top: calc(60px + var(--round-banner-h, 0px)); }`

배너 높이는 JS가 실측해 CSS 변수에 넣고 닫으면 0px로 되돌리므로(기존 배선 그대로),
배너가 없을 때의 동작은 예전과 완전히 같다. **앞으로 상단에 고정되는 것을 새로 만들 때는
`--finalize-banner-h`/`--round-banner-h`를 함께 더해야 한다** — 위에서 미는 요소가 생기면
`top` 상수를 쓰는 모든 sticky/fixed가 조용히 그 뒤로 숨는다.

## 키워드 작성 메모 — 엔터로 저장 (2026-08-21)

초안·확정본의 "+ 직접 키워드 작성하기" 메모 칸에서 **엔터만 쳐도 저장**되게 했다(사용자 요청).
"저장"/"수정" 버튼을 누른 것과 같은 경로(`saveKeywordNote`)라 동작·결과가 완전히 같고, 버튼은
그대로 둔다.

조합 중 엔터를 걸러내는 게 이 변경의 전부다 — 한글은 마지막 글자를 **엔터로 확정**하는 일이
잦은데, 그 엔터를 그대로 받으면 담당자가 글자를 확정하려던 순간에 저장+새로고침이 튀어
입력이 끊긴다. `keywordNoteKey`가 `ev.isComposing`(과 구형 폴백 `keyCode === 229`)이면
아무 일도 안 하고 돌아간다. 확정본의 잠긴 입력칸은 애초에 키 이벤트가 안 나므로 "수정"을
눌러 잠금을 푼 뒤부터 엔터가 먹는다.

## 확정본·초안 제목 옆 "정기 보관함" 링크 제거 (2026-08-21)

두 화면 제목 오른쪽에 있던 `🗓 정기 보관함` 링크를 없앴다(사용자 요청 — "[초안]이나
[확정본]에서 '정기보관함' 가는 버튼은 없어도 돼"). 2026-08-12에 "초안 담당자도 회차
진행 중 '이전엔 어떻게 묶었더라'를 참고할 일이 있다"는 이유로 확정본과 같은 자리에
넣었던 것인데, 실제로는 그 경로를 안 쓴다는 판단이다.

랜딩(`home.html` `.arch-reg`)과 안내 화면(`generate_waiting_page`)의 링크는 그대로 뒀다 —
그쪽은 "어디로 갈지 고르는" 화면이라 목적지 목록에 정기 보관함이 있는 게 맞다. 지운 건
링크 마크업과 그 링크만 쓰던 `.history-link` CSS 두 줄씩이고, 확정본의 `.header-top`
flex 래퍼는 남겼다(제목 한 줄만 남아도 레이아웃이 같다).

## 초안 제목 옆 "미분류 N건" 배지 (2026-08-21)

📂 소제목 미분류 칸은 언제나 화면 **맨 아래**에 붙는다. 기사가 쌓인 회차에서는 스크롤을
끝까지 내리기 전엔 그 칸이 있는 줄도 모른다는 지적이 나왔다("밑에 내려보기 힘드니까").
`초안 (…) [전체 N건]` 옆에 `[미분류 N건]`을 같이 띄우기로 했다.

**숫자만 띄우면 반쪽짜리라 눌러서 뛰는 버튼으로 만들었다.** "미분류가 3건 있다"는 걸
알려주고도 결국 스크롤을 내리게 하면, 원래 불편(내려가기 힘들다)이 그대로 남는다.
`scrollToUnclassified()`가 그 칸으로 부드럽게 스크롤한다.

**0건이면 아예 안 그린다.** 회차 중에는 기사가 계속 들어와 미분류에 쌓였다가 배정되는 게
정상 흐름이라, `미분류 0건`을 늘 띄워두면 며칠 만에 숫자를 안 읽게 된다. 사진 추정 전체
선택 버튼(0건이면 버튼 자체를 안 그림)과 같은 규칙이다.

**색은 새로 만들지 않고 그 칸 헤더의 `.unclassified-count`를 그대로 가져왔다** — 위의 이
숫자와 아래의 그 칸이 같은 것임이 색으로 이어져 읽히게. 대신 테두리 하나와 포인터
커서를 더해 "누를 수 있다"만 구분했다(CLAUDE.md 색 규칙 — 배지마다 자기만의 색 세트를
새로 만들지 않는다).

**뛰어갈 칸은 DOM에서 찾는다.** `id="subheading-{n}"`은 소제목 순서가 바뀌면 같이 바뀌고,
초안은 `#preview-body`를 통째로 갈아끼우는 화면이라 서버가 구운 번호를 배지에 박아두면
곧 어긋난다. 미니 목차가 열릴 때마다 DOM을 다시 읽는 것과 같은 이유로, 누르는 시점에
`.subheading-unclassified`를 찾는다.

**배지가 낡을 일은 없다.** 배지는 `<h1>` 안(=`#preview-body` 바깥)이라 `previewMoveArticle`의
DOM 교체 대상이 아니다. 하지만 그 경로는 소제목 **안에서의** ↑/↓ 이동뿐이라 미분류
건수를 바꿀 수 없고, 건수가 실제로 바뀌는 동작(미분류 배정·숨김·다른 소제목 이동·재분류)은
전부 `location.reload()`라 새로 렌더링된다.

**덤으로 `.subheading`에 `scroll-margin-top`을 넣었다.** `.topbar`가 `position:fixed`라
뛰어오면 소제목 제목줄이 그 띠에 깔려 안 보였다 — 새로 만든 이 배지뿐 아니라 원래 있던
미니 목차 점프도 같은 문제였다(실측: 점프 후 제목줄이 topbar 아래 55px 지점에 가려짐 →
지금은 64px 지점에 선다). 회차 마감 배너가 떠 있으면 그만큼 더 내려야 해서
`--round-banner-h`를 같이 더한다.

**확정본에는 안 넣었다** — `classify_for_finalize`가 미분류를 전부 기존 소제목이나 "기타"로
흡수하므로 그 화면엔 이 칸이 애초에 존재할 수 없다.

## 늦게 색인된 기사가 초안·실시간현황에서 통째로 빠지던 문제 (2026-08-21)

담당자 지적에서 시작했다 — "17시 회차 초안을 55분까지 봤는데 미분류가 없었다. 확정본에
`마감 후 자동 배정` 배지가 붙은 기사 2건은 어디서 나왔나?"

**원인은 증분 검색의 기준선이었다.** 초안(`app/preview_renderer.py`)과 실시간현황
(`app/live_renderer.py`)은 매번 처음부터 다시 검색하지 않고 "지금까지 본 가장 최신
`pub_date`"(last_seen) **초과**만 추가로 검색한다(`after < pub_date <= before`).
그런데 **네이버는 기사를 발행시각 순서대로 색인하지 않는다** — 16:54 기사를 이미 받아온
뒤에 16:35 기사가 색인되는 일이 흔하다. 그러면 그 기사는 이후 어떤 새로고침에서도
`after`(16:54)를 넘지 못해 그 화면에서 **영영 사라진다**. 초안에서는 "📂 소제목 미분류"
로도 안 보이고(애초에 목록에 없으므로), 회차 마감 수집(`collect_run`, 창 전체를 새로
검색)에서야 처음 나타나 미분류 → 자동 배정 → 배지가 붙는다.

**실측(배지 도입 2026-08-20 이후 전체 회차, 자동 배정 9건):**

| 회차 | 초안이 이미 본 최신 기사 | 자동 배정된 기사의 발행시각 | 판정 |
|---|---|---|---|
| 08-20 19:30 | 19:01 | 19:09, 19:11 | 정상(마지막 새로고침 이후 발행) |
| 08-21 11:00 | 10:57 | 10:40, 10:43, 10:45, 10:46, 10:55 | **전부 기준선보다 과거** |
| 08-21 17:00 | 16:54 | 16:35, 16:45 | **전부 기준선보다 과거** |

9건 중 7건이 이 구멍이었다. 색인 지연은 최소 17~19분(기준선이 그 시각을 지난 뒤에
색인됐다는 하한값이므로 실제로는 더 길 수 있다).

**고친 방식 — 기준선을 60분 물린다.** `app/naver_api.py` `incremental_search_after`
(두 화면 공용)가 `min(last_seen, 지금 − SEARCH_LOOKBACK_MIN)`을 돌려준다. 즉 최근 한
시간은 매 새로고침마다 다시 훑는다. 60분은 실측 지연(17~19분)의 세 배 남짓을 여유로
잡은 값이다(담당자 결정).

- **바닥이 화면마다 다르다** — 초안은 그 회차의 시작 시각, 실시간현황은 당일 0시.
  창 밖 기사를 캐시에 섞어 넣으면 초안이 확정본과 다른 목록을 보여주게 된다.
- **저장하는 last_seen은 예전 그대로 "가장 최신 `pub_date`"다.** 물러서는 건 읽는
  시점뿐이라, 되돌아보기 값을 바꿔도 캐시를 버릴 필요가 없다. 검색이 실패한 키워드도
  물러선 값이 아니라 **캐시에 있던 원래 값**(`stored_last_seen`)을 다시 저장한다 —
  안 그러면 실패할 때마다 기준선이 뒤로 밀려 남는다.
- **실시간현황의 병합에 URL 중복 제거를 넣었다.** 예전엔 `merged = known + new`로
  그냥 이어붙였는데, 되돌아보기 때문에 이제 같은 기사가 매번 다시 딸려온다. 그대로
  두면 캐시 파일이 계속 부풀고, `match_articles_to_groups(track_keyword_matches=True)`가
  같은 기사를 여러 번 세어 `matched_keywords`가 "정부, 정부"가 되고 "키워드별 건수"도
  부풀었다. 초안은 원래부터 `existing_cache_urls`로 걸러내고 있었다.

**비용.** 페이지네이션은 `after` 아래로 내려가면 멈추므로, 되돌아보는 60분에 기사가
100건 미만인 키워드는 요청 수가 그대로다(어차피 1페이지). 등록 키워드 중 `정부`만
많다(실측 하루 약 6,800건 ≈ 시간당 283건 → 3~4페이지). 새로고침 한 번당 +2~3 요청
수준이고, 이 날 실제 사용량이 25,000건 한도 중 1,117건(4.5%)이라 여유가 충분하다고 보고
넘어갔다.

**안 고른 대안: 초안이 주기적으로 창 전체를 다시 검색하기.** 확정본과 완전히 같은
결과를 보장하지만, 증분 캐시를 넣은 이유(↑/↓를 누를 때마다 전체 재검색이라 느렸다,
2026-07-30)가 그대로 되살아난다. 되돌아보기는 그 비용 구조를 건드리지 않고 실측된
지연폭만 덮는다.

## 수시 모니터링 — 조작할 때마다 화면이 맨 위로 튀던 문제 (2026-09-02)

담당자 지적: "[수시 확정]에서 기사 삭제하면 깜빡거리면서 화면이 위로 이동된다".
깜빡임은 괜찮고 **튀는 것만** 정기처럼 만들어 달라는 요구였다.

**원인은 fetch냐 form이냐가 아니었다.** 처음엔 "정기는 fetch라 부분 갱신, 수시는
form 제출이라 전체 재렌더"라고 봤는데, 코드를 세어보니 `app/renderer.py`의
`location.reload()`가 15번 나온다 — 정기도 소제목 이동·재분류·일괄이동·라벨은 전부
페이지를 통째로 다시 그린다. DOM만 건드리는 건 `hideArticle`의 `article.remove()`
하나뿐이다. 그런데도 정기는 안 튄다.

진짜 이유는 **`location.reload()`가 같은 히스토리 항목이라 브라우저가 스크롤을 알아서
복원해 주기 때문**이다. 수시의 `POST → 303 → GET`은 브라우저 입장에서 "새 페이지로의
이동"이라 복원 대상이 아니고, 스크롤이 언제나 0에서 시작한다. 즉 차이는 갱신 방식이
아니라 **끝나는 방식**이었다.

**그래서 fetch 전환은 기각했다.** 표면 작업량은 작다(인라인 `<form>` 9개는 전역 submit
인터셉터 하나로, `post()` 호출 13곳은 헬퍼 한 곳으로 커버된다 — `form.submit()`은
submit 이벤트를 안 띄우므로 헬퍼 안에서 직접 불러야 한다는 함정만 있다). 진짜 비용은
**303이 정보를 나르고 있다**는 것이다: `?error=`로 실려 `.warn` 배너가 되는 오류
메시지 10군데와, 카드 삭제(`/adhoc`)·새 수집(새 카드 id)처럼 목적지가 아예 다른 두
경로. fetch로 바꾸면 `res.url`을 읽어 분기해야 하고, 그 과정에서 카드 페이지 HTML을
받아 버리고 reload가 또 받아 **서버가 같은 페이지를 두 번 렌더링**한다. 게다가 그렇게
해도 깜빡임은 그대로다(reload도 전체 재렌더다). 담당자가 불편한 건 튐 하나뿐이라
비용 대비가 안 맞았다.

**택한 방법 — 제출 직전 위치를 직접 적어두고 돌아왔을 때 되돌린다.** 서버·라우팅·오류
배선을 한 줄도 안 건드린다. 조건 두 개를 달았다:

- **오류일 땐 복원하지 않는다.** 오류 배너는 화면 맨 위에 뜨는데 스크롤을 되돌려
  놓으면 방금 무엇이 실패했는지 못 보고 지나친다.
- **10초 지난 값은 버린다.** `submit` 리스너를 capture 단계에 달아서, 제출이
  `preventDefault`로 취소되는 경로(수집 시간이 잘못된 "다시 불러오기")에서도 값이
  저장된다. 이동이 안 일어났으니 그 값이 남아 있다가, 나중에 수시 보관함에서 이 카드를
  다시 열 때 엉뚱한 위치로 튀게 한다. 한 바퀴는 아무리 느려도 10초 안에 끝난다.

**검증 중에 두 번 헛돌았다** — 기록해 둘 만한 함정이다. (1) 검증용 서버를 8765로
띄웠는데 담당자의 앱이 이미 그 포트를 잡고 있어 바인딩에 실패했고, 브라우저는 계속
**옛 코드**를 보고 있었다. 포트가 안 뜬 걸 로그로 확인하지 않고 `curl`이 200을 준
것만 보고 넘어간 게 원인이다. (2) 테스트용 카드와 그 되돌리기 파일이 basename이 같아
`trash-can`으로 옮길 때 서로 덮어썼다. 작업 규칙의 "지우지 말고 trash-can으로"는
**이름이 겹칠 수 있다**는 전제까지는 안 담고 있다 — 옮길 때 접두어를 붙인다.

실측(517건짜리 카드, 8799 포트의 별도 서버): 스크롤 6000에서 🗑 → 재렌더 후 6000
유지. 소제목 추가(인라인 `<form>`) 5200 → 5200 유지. 같은 이름으로 다시 추가해 오류를
낸 경우 → 0(배너 노출). 11초 지난 값을 심고 새로 진입 → 0.

## 수시 모니터링 — 소제목 통째로 숨기기 (2026-09-02)

담당자 요청: "'수시' 확정본에서도 소제목별 삭제가 필요하다". 캡처로 보내온 화면은
「기타」 6건짜리 소제목이었고, 헤더에는 📋·▲▼만 있고 🗑이 없었다.

**정기엔 이미 있던 기능이라 새로 설계할 게 없었다.** 정기 확정본·초안은 소제목 헤더의
🗑이 두 갈래로 동작한다 — 기사가 있으면 "통째로 숨기기"(`hideGroup`), 0건이면 "빈
소제목 삭제". 수시는 **후자만** 있었다(`remove_custom_group`, 빈 커스텀 소제목 전용).
그래서 이건 기능 추가라기보다 두 흐름의 같은 자리를 맞추는 일이었다.

**이름은 "삭제"가 아니라 "숨기기"로 갔다.** 수시에서 기사는 지워지지 않고 `hidden`
플래그만 붙어 좌하단 「목록 밖 기사」에서 복구된다 — 진짜 삭제는 카드 삭제뿐이다.
담당자가 "삭제"라고 부른 걸 그대로 버튼 라벨로 옮겼으면, 되돌릴 수 있는 동작을
되돌릴 수 없는 것처럼 말하게 된다.

**중간에 나온 질문 — "수시에서 숨기기는 수시에서만 숨긴 걸 보여주는 거지?"** 맞고,
방향이 대칭이 아니라는 점을 같이 정리했다:

| | 수시에서 숨김 | 정기에서 숨김 |
|---|---|---|
| 저장 위치 | 카드 파일 안 `hidden` 플래그 | `data/hidden_articles.json`(전역) |
| 상대 화면에서 | 정기엔 아무 영향 없음 | 수시 목록에서 **안 빠짐** — `🗑 정기에서 숨김` 칩 + 📌 잠금만 |
| 어디서 복구 | 그 카드의 「목록 밖 기사」·↩ | 「숨긴 기사 관리」 |
| 수명 | 카드 수명(1년), 재수집해도 유지 | **자정에 통째 초기화** |

같은 🗑 아이콘인데 뜻이 다른 건 의도된 차이다 — 정기 숨김은 "오늘 보고서에서 뺀다",
수시 숨김은 "이 사안 목록에서 뺀다". 덤으로 확인한 것: 📌로 정기에 담아둔 뒤 수시에서
숨겨도 정기 「담아둔 기사」엔 그대로 남는다(담아두기는 복사라서).

**구현은 정기를 베끼지 않았다.** 정기는 화면 JS가 기사마다 `/hide-article`을 N번
fetch하고 되돌리기를 3초 coalesce로 억지로 한 스텝에 묶는데, 수시는 카드가 파일
하나라 라우트 하나(`/adhoc/card/hide-group`)에서 `card_lock` 한 번에 뒤집으면 된다 —
부분 실패가 없고 되돌리기도 자연히 1스텝이다.

**화면에 보이는 기사만 숨긴다.** 조건 밖(검색어를 좁혀 빠진) 기사까지 같이 숨기면,
나중에 조건을 되돌렸을 때 담당자가 본 적 없는 이유로 그 기사가 안 돌아온다 —
§6.4 "교체이지 삭제가 아니다"가 깨진다. 확인창 건수도 그래서 화면 건수와 같다
(정기는 몇 건이 사라지는지 누르기 전엔 알려주지 않는다).

### 같이 드러난 버그 — 화면과 서버의 판정 기준이 갈려 있었다

`card.remove_custom_group`은 `any(a.get("group") == name ...)`으로 **숨긴 기사까지**
세서 거절했는데, 화면은 숨김·조건 밖을 뺀 목록(`visible`)으로 🗑을 「빈 소제목 삭제」로
바꿔 그리고 있었다. 즉 커스텀 소제목의 기사를 다 숨기면 **버튼은 보이는데 누르면
에러**가 났다. 지금까지는 기사를 하나씩 다 숨겨야 밟히는 드문 경로라 안 드러났는데,
통째 숨기기가 생기면 클릭 두 번(숨김 → 삭제)에 바로 밟는다.

두 갈래 중 **(a) 숨긴 기사가 있어도 삭제 허용**을 골랐다(사용자 결정). 지울 때 남은
기사는 지우지 않고 `group=None`(미분류)으로 풀어둔다 — 없어진 소제목 이름을 계속
달고 있으면 복구하거나 조건을 되돌렸을 때 갈 곳이 사라진다. 기각한 (b)는 "화면 버튼도
hidden 포함으로 판정해 아예 안 그리기"인데, 안전하긴 하지만 "다 숨겼는데 왜 못 지우지"가
화면 어디에도 안 적혀 담당자가 막다른 길에 선다.

### 덤 — onclick 인자로 자유 텍스트를 넘기던 버튼 7개 (2026-09-02)

새 버튼을 만들다 발견한 것. 소제목 이름을 `onclick="hideGroup('...', '{이름}')"`처럼
인자로 넘기면, 이름에 작은따옴표가 하나만 섞여도 `html.escape`가 `&#x27;`로 바꾸고
브라우저가 그걸 다시 `'`로 되돌리면서 onclick 안의 JS 문자열이 그 자리에서 깨진다.
브라우저 콘솔에서 직접 재현한 결과(2026-09-02):

```
onclick 속성 원문:  window.__got = '이건'따옴표'
클릭 결과:          Uncaught SyntaxError: Unexpected identifier '따옴표'  → 아무 일도 안 일어남
```

**즉 실패 모드가 "에러 배너"가 아니라 "버튼이 조용히 먹통"이다.** 담당자는 눌렀는데
안 되는 이유를 화면 어디에서도 알 수 없다.

수시 카드 화면에서 자유 텍스트를 인자로 넘기던 자리가 7곳이었다 — 소제목 ▲▼ 2개,
빈 소제목 삭제, 기사 행의 「다른 소제목」·숨기기·↑·↓, 숨긴 기사 팝오버의 복구.
소제목 이름은 AI가 짓거나 담당자가 직접 입력하는 자유 텍스트라 따옴표가 실제로 들어올
수 있고, 기사 URL도 남의 사이트 주소라 장담할 수 없다(반면 카드 id는 앱이 만드는
`YYYYMMDD-HHMMSS-hex`라 안전해서 인자로 남겨도 되지만, 규칙을 한 줄로 유지하려고
같이 옮겼다).

전부 **누른 요소의 `data-*` 속성에서 읽는 방식**으로 바꿨다 — 정기 화면(`app.renderer`의
`hideGroup`/`renameGroup`)이 `data-name`을 쓰는 방식이고, 이 파일의 `togglePin`·
`bulkMove`가 이미 그렇게 하고 있었다(그 둘만 우연히 맞았던 셈). 검증은 소제목 이름과
기사 URL 양쪽에 작은따옴표를 심은 카드를 렌더링해 7개 버튼을 실제로 눌러보고, 폼에
실려 나가는 값이 원본 그대로인지 확인했다.

## 수시 → 정기 담아두기 (2026-08-25)

### 📌 버튼 상태가 안 읽히던 문제 (2026-08-25)

위 수정 직후 담당자 피드백 두 가지. **(1) "담아둔 기사라는 게 됐다는 걸 회색으로만
보여주는데 더 명확해야 할 듯."** 수시의 📌은 아이콘 하나뿐인데 담아둠 상태를
`COLOR_PIN_GRAY` 글자 + `COLOR_PIN_GRAY_BG`(거의 흰색) 배경으로만 표현하고 있었다.
실시간현황의 같은 버튼에서 이 톤이 멀쩡히 읽히는 건 "📌 담아둠"이라는 **글자 라벨**이
함께 있기 때문이고, 라벨이 없는 이 화면에서는 색이 상태를 혼자 짊어지는데 그 색이
너무 옅었다. 계열(중립 회색)은 CLAUDE.md 색 규칙대로 지키고 — 담아둠은 라벨(앰버)도
발송(초록)도 아니다 — 채도가 아니라 **채움**으로 대비를 줬다: 배경을 `pin_gray`로 꽉
채우고 아이콘을 흰색으로.

**(2) "아예 핀이 클릭조차 안 되는 경우는 무슨 경우? 마우스 오버로 보여줘야 할 듯."**
툴팁은 이미 달려 있었다. 진짜 문제는 `disabled` 버튼을 `color: {border}`(#E5E7EB)로
칠해둔 탓에 **핀 모양 자체가 거의 안 보였다**는 것 — 상태가 "잠김"이 아니라 그냥
"흐림"으로 읽혀서, 애초에 그게 버튼인지도 애매했다.

**의심한 원인을 먼저 재봤다.** "Chrome은 disabled 버튼에 마우스 이벤트를 안 보내니
네이티브 툴팁이 안 뜬다"는 통설이 있어 그것부터 확인했는데, 실제 hover를 넣어 재보니
`mouseover`/`mousemove`가 둘 다 도달했다(합성 이벤트는 항상 통과하므로 진짜 커서로
재야 한다). 툴팁은 뜨고 있었고, 원인은 이벤트가 아니라 **가시성**이었다 —
CODING_CONVENTIONS.md §1대로 추측한 원인을 먼저 재본 게 헛발질을 막았다.

그래서 빗금(`::after`, -45°)을 그어 "막힘"으로 읽히게 하고 아이콘은 알아볼 만큼 남겼다
(`opacity: 0.5` + `color: muted`). **처음엔 아이콘을 원래대로 아주 옅게 두고 빗금만
얹었는데, 빗금만 도드라져 연필(수정) 아이콘처럼 보였다** — 스크린샷으로 확인하고 다시
고쳤다. hover 시 배경이 들어오게 해 "여기 뭔가 있다"는 신호도 줬고, 같은 행의
`🗑 정기에서 숨김` 칩에도 결과를 말하는 툴팁("숨김을 풀기 전에는 담아둘 수 없어요")을
달아 이유를 두 군데서 찾을 수 있게 했다.

최종 네 상태(실측 확인): 보통(외곽선·muted) / 담아둠(회색 채움·흰 아이콘) /
담아둠+숨김(채움 + 앰버 링 — 담긴 건 사실이니 채움은 두고 "주의"만 덧댐) /
잠김(외곽선·opacity 0.5·빗금·`disabled`).

### 담아둔 기사 2건이 확정본에 안 나타난 문제 (2026-08-25)

담당자 제보 — 수시 수집 결과에서 📌으로 2건을 담았는데 확정본의 "📌 담아둔 기사"
구획에 없었다. 추적해보니 저장은 전부 정상이었다(`data/manual_articles.json`에 두 건
그대로). 문제는 렌더링 쪽이었다 — `app/renderer.py`가 그 구획을 그릴 때
`filter_hidden(load_manual_articles())`로 한 번 더 거르는데, 그 두 기사가 정기
`hidden_articles.json`에 이미 들어 있었다.

**시각을 맞춰보니 순서가 분명했다**: 10:51:15와 10:53:05에 정기 초안을 큐레이션하며
🗑️로 뺐고, 10:57에 만든 수시 카드에서 같은 기사를 📌로 담았다. 정기 숨김이 먼저였고,
`filter_hidden`은 URL뿐 아니라 **제목**으로도 대조하므로 확실하게 걸렸다.

**진짜 문제는 "안 담긴다"가 아니라 "담긴 것처럼 보인다"였다.** 수시 화면의 📌 버튼은
`pinned_urls`(= manual_articles의 URL 집합)만 보고 `on` 상태로 그려지므로, 담당자
눈에는 담긴 걸로 보이는데 확정본엔 없다. CODING_CONVENTIONS.md §3(담당자 작업은
조용히 사라지지 않는다)에 정면으로 걸리는 케이스다.

**실시간현황에는 같은 버그가 없다** — 그 화면은 `_render_row`의 우선순위가
`hidden > scrapped > pinned > ...`라, 숨긴 행은 📌 대신 "🗑️ 숨김"으로 바뀌어 애초에
담아두기를 못 누른다. 수시 화면만 정기 숨김 저장소를 안 보고 있었다.

**세 가지 안을 놓고 담당자가 골랐다**: (1) 담아두면 숨김을 자동 해제, (2) 실시간처럼
상태를 표시하고 막기, (3) 담을 수는 있게 두되 경고만. **(2)를 골랐다** — 수시 화면이
정기 상태를 몰래 바꾸면 다른 화면에서 그 변화를 설명할 자리가 없고, 실시간과 규칙이
갈라지지 않는 게 낫다는 판단.

**"숨김 + 이미 담아둔" 행만 예외를 뒀다.** 그냥 전부 잠그면 제보된 그 2건은 확정본에도
안 보이고 취소도 못 하는 막다른 길에 갇힌다(manual_articles가 자정에 비워질 때까지).
그 조합만 취소 방향으로 클릭을 열어두고, 툴팁이 왜 안 보이는지까지 말한다.

**판정 함수를 하나로 합쳤다.** `filter_hidden`이 함수 안에서만 계산하던 대조 집합을
`hidden_match_keys()`/`is_hidden()`으로 꺼냈다 — 수시 화면이 같은 규칙을 복사해서
구현했다면 "화면은 담을 수 있다는데 확정본은 걸러내는" 이번 어긋남이 규칙이 바뀔 때마다
그대로 재발한다. 실측: 제보된 카드 41건 중 12건이 정기 숨김 상태였고(생각보다 흔한
상황이다), 그중 2건이 담아둔 상태였다.

"예결위처럼 재경부 소관이 아닌 사안을 훑다가 정기에 넣고 싶은 기사가 나오면 어떻게
옮기나"는 질문에서 시작했다. 지금은 통로가 없다 — 수시 기사 행에는 다른 소제목·복사·
라벨·숨기기·↑↓ 여섯 컨트롤뿐이고, 정기로 넘기는 길이 아예 없었다.

**한국은행·재경위·예결위 세 예시로 "수시"의 정의를 다시 확인했다.** 셋 다 확정본에
못 넣는 이유가 다르다 — 한국은행은 소관 밖, 재경위는 비정기, 예결위는 남의 기관 기사가
섞여 들어옴. 공통점은 빈도가 아니라 **"확정본 밖에서 담당자가 지켜봐야 하는 것"**이라는
점이었다. 그래서 수시 → 정기 통로가 오히려 이 격리를 지키는 안전판이라는 결론이
나왔다 — 통로가 없으면 "그냥 정기 키워드 그룹에 예결위를 넣을까"로 흘러가고, 그 순간
기획처 기사가 확정본에 통째로 쏟아지고 소제목 분류까지 흔들린다.

**A/B/C 세 안을 목업으로 비교했다.**
- **A(지금)**: 통로 없음.
- **B(채택)**: 📌 아이콘 하나만 추가(6→7개). 상태는 실시간 화면(`app/live_renderer.py`
  `.add-btn.is-pinned-btn`)과 같은 `COLOR_PIN_GRAY`/`COLOR_PIN_GRAY_BG`로 — 라벨(앰버)·
  발송(초록) 어느 쪽도 아닌 중립색을 쓴다는 기존 색 규칙을 그대로 따른 것.
- **C(기각)**: "⋯더보기"를 같이 도입해 복사를 메뉴 안으로 옮기고 그 자리에 📌를 두는
  안. 정기 화면의 ⋯더보기가 이득이었던 건 **복사·원문 다시 불러오기·제목 직접 수정**
  셋을 하나로 묶어서였는데, 수시엔 뒤의 둘이 아직 없다(ADHOC_DESIGN.md §7, 1차 범위
  밖으로 이미 확정). 메뉴에 넣을 게 복사 하나뿐이라 총 컨트롤 수가 7개로 그대로다 —
  버튼 하나 줄고 ⋯ 하나 늘 뿐, 클릭만 한 단계 늘어난다. ⋯더보기는 원문 재수집·제목
  수정이 실제로 생기는 날 다시 꺼낼 안.

**저장소는 새로 안 만들고 실시간의 것을 그대로 재사용했다.** `app/manual_articles.py`
(`data/manual_articles.json`, 당일 자정까지만 유지)는 이미 실시간 화면의 "→ 스크랩"이
쓰고 있었다 — 확정본의 "📌 담아둔 기사" 구획도 이미 있고, 소제목 드롭다운으로 옮기는
승격 경로도 이미 있다. 수시가 같은 저장소에 같은 모양의 dict(outlet/title/url/summary/
pub_date)를 써 넣기만 하면, 나머지는 전부 기존 코드가 그대로 처리한다 — 실시간에서
담아둔 것과 수시에서 담아둔 것이 확정본 화면에서 구분 없이 섞이는데, 둘 다 "오늘 확정본
후보"라는 같은 뜻이라 의도된 동작이다.

**되돌리기는 새로 안 만들었다.** 재클릭으로 담아두기 자체를 취소하는 것(unpin)이 곧
되돌리기라 — 실시간 화면의 담아두기/취소 토글과 판단이 같다. `app.adhoc.undo`
(카드 스냅샷) 대상에도 안 넣었다 — `manual_articles.json`은 카드 상태가 아니라 정기
쪽 전역 저장소라, 카드 스냅샷에 담아도 애초에 복원되지 않는다.

**구현 중 확인한 것 — `app.manual_articles`가 요구하는 필드가 수시 카드의 기사 dict와
이미 100% 겹쳤다.** outlet/title/url/summary(선택)/pub_date(선택) 다섯뿐이라 변환 코드가
따로 필요 없었다. 라우트 핸들러는 URL로 카드 안 기사를 찾아 그 자리에서 필요한 필드만
추려 넘긴다(`_handle_pin_article`) — JS가 데이터를 들고 다니게 하지 않고 서버가 카드
원본에서 다시 읽는 쪽을 택했다(`_handle_set_hidden`과 같은 패턴). 담아두면 정기 확정본
화면도 즉시 다시 만든다(`handler._regenerate_screens()`) — 안 그러면 다음 정기 회차가
돌 때까지 확정본에 안 보여서, 방금 한 담아두기가 성공했는지 알 길이 없다.

### 기능째 제거 — 통로를 0개로 (2026-09-02)

**발단은 담당자(=만든 사람)가 자기 앱에서 길을 잃은 것이다.** 수집 확정본 행의 📌를
보고 "이게 정기 확정본으로 넘기기잖아. 그럼 정기-수시 왔다갔다 해? 헷갈리지 않아?"라고
물었다. **배선은 이미 안전했다** — 📌은 `manual_articles.json`에만 넣고, 거기 담긴
기사는 확정본 화면에 보이기만 할 뿐 복사·txt·엑셀·발송 어디에도 안 나가며, 실으려면
소제목 드롭다운으로 한 번 더 옮겨야 했다. 즉 "필터 없이 바로 넘어가는" 일은 원래
없었다. **문제는 화면이 그 안전을 한 군데도 말하지 않았다는 것이다.**

화면이 숨기고 있던 것 셋을 세어봤다:

1. 버튼 툴팁이 목적지를 `정기 확정본에 담아두기`라 불렀다 — 실제 목적지는 대기실인데.
2. 확정본의 「📌 담아둔 기사」 구획이 **출처를 안 보여줬다.** 실시간에서 온 것과
   수시에서 온 것이 같은 저장소에 섞이는데 저장 시점에 출처 필드 자체를 안 넣었다.
3. 반대로 수시 화면은 정기 상태(숨김)를 알고 표시했다 — 두 흐름이 얽혀 있다는 인상만
   키웠다.

**세 가지 안을 놓고 골랐다.** (A) 연결 유지 + 툴팁 고치기 + 출처 태그 붙이기,
(B) 저장소·구획 분리, (C) 기능 제거. 처음 추천은 A였다(배선을 뜯을 이유가 없으니
표시만 정직하게). B는 A가 주는 걸 다 주면서 저장소·자정 리셋·되돌리기 스냅샷이 두
벌이 되므로 일찍 뺐다.

**C로 간 결정타는 두 가지였다.** 하나는 실사용 — "📌 눌러본 적 있어?"에 "잘 없어".
다른 하나는 담당자가 스스로 짚은 개념 오류다: **"담아둔 기사는 실시간에서 볼 수 있는
건데, 수시엔 실시간이 필요 없잖아."** 정확한 지적이었고, 정리하면 이렇다 —
`📌 담아둔 기사`의 존재 이유는 **회차 경계를 넘는 것**이다. 실시간현황은 회차 밖이라
확정본에 넣을 길이 없어서 그 대기실이 필요했다. **수시엔 회차가 없다**(마감이 없어
초안/확정본을 가를 축조차 없다 — 「골격 3단 이름 재정리」 참고). 넘을 경계가 없으니
장치가 붙을 자리도 없다. 그런데 같은 버튼을 갖다 붙였더니 뜻이 바뀌어 있었다:

| | 넘는 경계 |
|---|---|
| 실시간 📌 | **시간** 경계 (회차 밖 → 회차 안) |
| 수시 📌 | **흐름** 경계 (수시 → 정기) |

뜻이 다른 두 동작이 한 저장소·한 구획에 섞여 있었으니 확정본의 그 구획이 두 가지 다른
이야기를 하고 있었던 셈이다. **만든 사람조차 헷갈린 이유가 이거였다.**

**이 논거는 사실 이미 적혀 있었다.** ADHOC_DESIGN.md §6.4가 "작업대(하단 보관 구역)"
대안을 기각하며 세 번째 이유로 쓴 문장이 그대로다 — *"정기에 담아두기가 있는 이유가
수시엔 없다. 정기의 📌는 실시간현황에서 확정본으로 건너가는 다리다. 수시는 한 화면
안이라 건널 강이 없다."* 그 설계 판단을 2026-08-25에 📌를 붙이면서 스스로 어겼고,
9일 뒤 같은 논거로 되돌린 것이다.

**의존 구조에서 잰 값이 결정을 뒷받침했다.** 수시가 정기 모듈을 import하는 지점을 전부
세어보니, 📌 하나 때문에 들어온 게 4줄이었다 — `routes.py`·`renderer.py` 각각에
`app.curation`(숨김 판정)과 `app.manual_articles`(쓰기). 나머지 import는 전부 순수
도구(filters/sorter/icons/highlight/excel_export/config/settings/llm_classifier)였다.
**`app/adhoc/* → app/*` 단방향 의존 규칙에서 정기의 런타임 상태를 읽고 쓰던 유일한
지점이 이 버튼이었다** — 그것도 거의 안 쓰는 기능 하나 때문에. 제거 후 남은 정기
런타임 의존은 라벨(`labels`/`label_undo`)뿐인데, 그건 애초에 정기·수시를 가로지르라고
만든 전역 저장소라 예외가 아니다.

**연쇄로 같이 사라진 것**(85줄 + 63줄): `🗑 확정본에서 숨김` 칩과 그 근거였던 정기
숨김 대조, 버튼 상태 3종(빗금 잠금 / 앰버 테두리 / 채움), `togglePin` JS, 두 라우트와
핸들러, `_regenerate_screens()` 호출. **수시 화면에서 "정기"라는 말이 통째로 없어졌다** —
「본업 화면은 자기를 해명하지 않는다」와도 맞아떨어진다. `COLOR_PIN_GRAY`/
`COLOR_PIN_GRAY_BG`는 실시간현황이 그대로 쓰므로 `app/config.py`에 남겼다.

**잃은 것은 정직하게 적어둔다.** 수시 검색어로**만** 걸리는 기사를 정기 보고서에 손으로
밀어 넣는 길이 없어졌다. 정기 검색어에도 걸리는 기사면 실시간현황에 그대로 뜨니 거기서
담으면 되고, 수시 검색어로만 걸린다면 그건 📌이 필요하다는 신호가 아니라 **그 단어를
정기 검색어에 넣어야 한다는 신호**다 — 매번 손으로 옮기는 건 증상 치료다. 사용자 판단도
같았다: *"만약 누군가 그걸 원한다면 검색어 설정 등을 다시 하겠지. 이게 연결되어 있으면
로직이 너무 많아."*

**📌 도입(2026-08-25)부터 제거(2026-09-02)까지 9일. 그 사이에 붙은 후속 수정이
버튼 상태 대비 개선·잠금 빗금·`locked-on` 앰버 테두리·서버측 이중 방어 넷이다** — 전부
"이 버튼이 지금 무슨 상태인지"를 설명하려던 것이었고, 하나도 기능 자체를 더 쓸모 있게
만들지 않았다. 어떤 기능이 자기를 해명하는 부속만 계속 늘린다면 그건 그 기능이 그 화면에
있을 이유가 없다는 신호일 수 있다.

## 담아둔 기사도 AI에 맡기기 · 「배정」과 「재분류」 이름 가르기 (2026-08-26)

### 출발점 — "미분류 배정에 담아둔 기사는 해당사항이 없잖아"

📂 소제목 미분류 칸의 배정 버튼은 그 칸에 있는 기사만 대상이라, 📌 담아둔 기사는
아무리 눌러도 안 건드린다. "담아둔 기사도 포함하고 싶으면 어떻게 하지?"라는 질문에서
시작했다. 그때까지 유일한 길은 기사마다 소제목 드롭다운을 열어 직접 고르는 것뿐이었다.

처음 제안한 건 **드롭다운 맨 위에 「🤖 AI가 알아서」 한 줄 추가**(서버는 이미 `group`이
비면 자동 분류에 맡기는 경로가 있어 공짜에 가까웠다)였는데, 사용자가 뒤집었다 —
**구획 헤더에 버튼 하나**가 맞다고. 근거가 더 좋았다:

- 한 건짜리엔 이미 답이 있다. 그 기사를 눈앞에 보고 있으니 어느 소제목인지 대개 안다.
  실제로 막히는 건 "여러 건 담아뒀는데 일일이 판단하기 귀찮다" 쪽이고, 그건 헤더 버튼의 몫.
- 📂 미분류 칸과 자리·모양이 같아서 새로 배울 게 없다("기능이 많으면 복잡하다"는 걱정).

### 두 칸은 "0건이 정상"이 서로 다르다 — 그래서 확인창을 한 겹 더

같은 모양으로 만들되 📌 쪽에만 `confirm()`을 뒀다. 📂 미분류는 **비워야 정상**이라
전체 처리 버튼이 곧 정답이지만, 📌 담아둔 기사는 **안 넣고 남겨두는 것도 정상**인
참고용 임시보드라 "전부"가 항상 맞는 답이 아니다. 일부만 넣는 길(기사별 드롭다운)도
그대로 남겼다.

### 되돌리기가 애초에 안 되고 있었다

`app/undo.py`의 `_SNAPSHOT_FILES`에 `manual_articles.json`·`draft_pending_articles.json`이
없었다 — "담아둔 기사 → 초안" 승격은 그 두 파일만 건드리므로, 되돌리기를 눌러도
**아무 일이 안 일어나는** 상태였다(2026-08-24의 회차 파일 누락과 같은 종류의 빈틈:
라벨은 맞는데 실제로는 no-op). 한 건씩 넘길 땐 티가 안 났지만 버튼 하나로 여러 건이
한꺼번에 넘어가게 되면서 실수의 대가가 커져 같이 메웠다.

### 이름이 곧 약속 — 세 버튼을 다시 갈랐다

사용자 지적: "담아둔 기사 / 소제목 미분류는 **기존 소제목을 남기는 방향**이라고
이용자가 생각하게 되지 않을까? '전체 기사 재분류'는 소제목까지 다 흐트러뜨리는 거고."

코드가 이미 정확히 그렇게 갈려 있었다 — 앞의 둘은 `assign_to_existing`(기존 소제목의
이름·구성을 건드릴 방법 자체가 없다), 뒤의 하나는 `classify_with_llm(force=True)`.
모양·확인창도 그 선을 따라 갈려 있었는데(배정=채움·확인창 없음, 재분류=고스트·항상 확인창)
**라벨만 어긋나 있었다**. 그래서:

- 「미분류 배정」 → **「AI 기사 배정」**, 📌 담아둔 기사 구획에도 같은 이름의 버튼.
- 「전체 기사 재분류」 → **「AI 모든 기사 재분류」**, 아이콘만 `bot` → `refresh`.
  글자 라벨은 그대로 뒀다(한 번은 아이콘 전용으로 만들었다가 되돌렸다 — 사용자 의도는
  "아이콘만 교체"였다). 새로고침은 "처음부터 다시"라는 뜻이 이미 붙어 있는 기호라,
  글자를 안 읽어도 성격이 먼저 읽힌다.
- 색은 **셋 다 채운 연보라**로 통일(사용자 결정). 예전 규칙은 "파괴적 동작은 어느
  화면에서든 채우면 안 된다"였는데, 그 결과 같은 AI 계열 안에서 색이 갈려 계열이
  하나로 안 읽혔다. 파괴적이라는 신호는 아이콘과 항상 뜨는 `confirm()`이 계속 맡는다.

한 가지 정확히 해둘 것: 「배정」이 지키는 약속은 **"기존 소제목이 안 흐트러진다"**이지
**"새 소제목이 절대 안 생긴다"**가 아니다 — 어디에도 안 맞는 기사가 있으면 새 소제목을
하나 만들 수 있다(`app/assigned_groups.py`가 그것 때문에 있는 파일이다). 문구도
거기까지만 약속한다.

### 곁다리 둘

- **📂 소제목 미분류 이탤릭 제거.** 옆의 건수 칩·버튼과 밑줄이 안 맞아 한 줄로 안
  읽혔다. "임시 칸"이라는 신호는 앰버 왼쪽 띠 + 회색 글자 + 폴더 아이콘이 이미 충분히 한다.
- **📂 이모지 → SVG(`icon("folder")`).** 이게 유일하게 남아 있던 진짜 이모지였다
  (🤖·📌은 이미 `icon()`이었다). 그런데 하필 `UNCLASSIFIED_GROUP_NAME = "📂 소제목 미분류"`
  처럼 **이름 문자열 안**에 있고, 그 값은 `group_order.json`·LLM 캐시·"기타" 병합이
  대조하는 **데이터 키**다. 상수를 고치면 저장된 순서·캐시가 이름을 못 찾으므로,
  저장값은 그대로 두고 **그릴 때만** 앞 이모지를 떼고 아이콘을 붙인다
  (`UNCLASSIFIED_DISPLAY_TEXT`/`UNCLASSIFIED_ICON`, 화면 전용 상수).

## 커스텀 소제목 기본 위치를 다시 맨 아래로 (2026-08-27)

「+ 새 소제목 만들기」로 만든 소제목이 목록 **맨 위**에 오던 것을 **맨 아래**로 되돌렸다
(사용자 요청). 2026-08-05에 정반대 방향으로 바꿨던 결정을 뒤집는 것이라 경위를 남긴다.

### 이번이 세 번째 뒤집기다

이 항목의 위 기록(2026-08-05·06 항목)이 이미 말하고 있다 — *"a prior request had
explicitly asked for bottom placement — reversed per new feedback"*. 즉 순서는
**아래(최초) → 위(2026-08-06) → 아래(오늘)**다. 같은 자리를 두 번 뒤집었으니, 다음에
또 "위가 낫지 않나"는 얘기가 나오면 그때는 바꾸기 전에 이 세 줄을 먼저 읽는다.

`app/custom_groups.py` `_place_at_top_of_order` [추가: 2026-08-05] 때의 근거는
"소제목이 몇 개 쌓이면 만든 소제목을 찾으려 스크롤해야 해서 불편하다"였다. 근거 자체는
지금도 틀리지 않지만, 실사용에서 담당자가 커스텀 소제목을 쓰는 방식이 "방금 만들어 바로
채우는 칸"보다 "보고서 끝에 붙이는 칸"에 가까웠다 — 만들자마자 찾아가는 비용보다,
보고서에서 늘 같은 자리(끝)에 있는 편의가 더 컸다.

### 실제로는 회차마다 위치가 달랐다 (이번에 같이 발견)

`_place_at_top_of_order`는 **만들 때 한 번만** `group_order.json`에 이름을 적었고, 그
파일은 2026-08-14부터 **회차별 버킷**이다. 그래서:

- 만든 그 회차 → 버킷에 이름이 있으니 맨 위.
- 다음 회차 → 그 회차 버킷은 비어 있으니 자연 순서를 타 **맨 아래**.

`custom_groups.json`은 자정까지 유지되므로 같은 소제목이 하루 안에서도 회차를 넘길 때마다
위→아래로 뛰었다. 어느 쪽으로 정하든 통일이 필요했고, 사용자가 아래를 골랐다.

### 어떻게 고쳤나

새 함수를 만들어 "맨 아래에 적는" 게 아니라, **`group_order.json`을 아예 안 건드리게**
했다(`_place_at_top_of_order` 삭제, `add_custom_group`의 `round_id` 인자도 함께 제거).
자연 순서가 이미 커스텀 소제목을 맨 뒤에 두기 때문이다 — 기사가 없으면 렌더러가
`empty_custom_groups`를 목록 끝에 붙이고(`app/preview_renderer.py`·`app/renderer.py`,
둘 다 `apply_group_order(groups + empty_custom_groups, round_id)`), 기사가 있으면
`app/classifier.py` `_apply_forced_groups`가 그 이름을 `groups` 끝에 끼워 넣는다.
📂 소제목 미분류는 `apply_group_order`가 언제나 그보다 더 뒤로 민다(2026-08-24 규칙).

**빈 버킷에 이름 하나만 적는 방식은 쓸 수 없다** — 그러면 `apply_group_order`의 순서
목록 첫 자리가 되어 오히려 맨 위로 간다(이번에 되돌린 그 동작 그대로). 진짜로 "맨 아래"를
명시적으로 적으려면 그 회차에 지금 떠 있는 소제목 전체 이름을 알아야 하는데,
`add_custom_group`은 그걸 모른다(알아야 할 이유도 없다 — 렌더러의 일이다).

두 경로(기사 있는 커스텀 / 빈 커스텀) 모두 맨 아래로 오는 것과 통합 테스트 11개 통과를
확인했다. 곁다리로 테스트 격리 구멍도 하나 막혔다 — `tests/test_integration.py`는
`custom_groups.CUSTOM_GROUPS_FILE`만 임시 폴더로 바꿔놨고 `group_order.json`은 안
바꿔놔서, `add_custom_group`이 실제 `data/group_order.json`에 쓸 수 있는 상태였다.

### 기본 위치일 뿐이다

▲▼·소제목 미니 목차로 옮기는 건 그대로다. 옮기면 그 회차의 `group_order` 버킷에 화면
전체 순서가 통째로 저장되므로(`save_group_order`), 담당자가 정한 자리가 이긴다.

## "마감 후 자동 배정" 확인 배너 — 「확인했어요」가 음영까지 걷어내게 (2026-08-27)

담당자 제보: "확정본에서 연보라색 블럭 「확인했어요」 눌러도 새로고침해도 안 사라져."

**재현해보니 배너 자체는 정상이었다.** 실제 앱(`http://127.0.0.1:8765/index.html`)을
브라우저로 열어 눌러보니 `localStorage`에 `finalizeBannerDismissed:2026-08-27:11:00`이
저장되고, 새로고침해도 배너는 다시 안 떴다. 안 사라지는 건 **기사 행 6건의 연보라 음영**
(`.article.finalize-added`)이었다 — 회차 파일의 `finalize_auto_assigned` 플래그로
영구 저장되는 값이라(`app/scraper.py` `collect_run`) 배너를 닫든 새로고침을 하든
그 회차 내내 다시 칠해진다. 즉 "배너 버그"가 아니라 **「확인했어요」를 눌러도 화면에서
달라지는 게 아무것도 없다**는 게 문제였다(배너 띠 하나가 사라질 뿐, 정작 눈에 걸리는
연보라 덩어리 6개는 그대로).

**고른 방식: 음영만 걷어내고 표식은 남긴다**(사용자 결정, 3안 중). 왼쪽 보라 세로선과
🤖 `마감 후 자동 배정` 배지는 그대로 둔다 — "봤다"는 표시는 하되 "어떤 기사였는지"는
그 회차 내내 계속 알아볼 수 있어야 한다. 안 고른 두 안: (1) 음영·선·배지를 통째로
지우기 — 그러면 "표시된 곳 보기 →"로 찾아간 기사를 나중에 다시 짚을 방법이 없다,
(2) 서버에 요청해 `finalize_auto_assigned` 플래그 자체를 끄기 — 되돌릴 수 없고 정기
보관함의 기록에서도 사라진다(회차 파일은 "그 회차가 어떻게 만들어졌나"의 기록이라
화면에서 확인했다는 이유로 지울 값이 아니다).

**구현.** `.article.finalize-added`가 배경·테두리를 한 규칙에 같이 들고 있어서
클래스를 지우면 선까지 딸려 사라졌다 — 배경만 `.article.finalize-added-bg`로 떼고
(`app/renderer.py` CSS, 선언 위치는 예전 그대로라 `is-new-arrival`(노랑)·`just-moved`와의
우선순위가 안 바뀐다), `render_article`이 두 클래스를 함께 붙이며, 배너 IIFE의
`clearFinalizeShading()`이 `-bg`만 걷어낸다. 부르는 자리는 둘 — 「확인했어요」를 누른
순간, 그리고 이미 닫아둔 회차를 다시 열었을 때(`dismissed === true`인 로드 시점).
서버 데이터는 안 건드리므로 `jumpToNextFinalizeAdded()`의 `.article.finalize-added`
선택자도 그대로 동작하고, 정기 보관함(`app/history_renderer.py`, 자기 CSS 사본을 쓰고
이 JS가 없는 화면)은 예전처럼 음영까지 그대로 보여준다.

**실측 확인**(브라우저로 직접): 닫기 전 배경 `rgb(240,234,251)` → 닫은 뒤
`rgba(0,0,0,0)`(일반 기사 행과 동일), 왼쪽 선 `rgb(167,139,250)` 3px·배지 유지,
새로고침 후에도 같은 상태 유지, `.article.finalize-added` 6건은 그대로 선택 가능.

### 정기 보관함에서는 연보라 자체를 뺐다 (2026-08-27)

이어서 사용자 제안 — "정기 보관함에서는 연보라색 안 나와도 될 듯". 동의했다.

- 연보라 음영이 하는 말은 "**초안에서 못 본 사이 AI가 붙였으니 확인해라**"는 할 일
  알림인데, 보관함엔 확인할 수단이 아예 없다 — 「확인했어요」 배너도 큐레이션 버튼도
  없는 읽기 전용 화면이라, **끌 수 없는 알림**이 그 회차 기록에 영원히 켜져 있었다.
- 여러 회차를 동시에 펼쳐 훑는 화면이라, 회차마다 흩뿌려진 연보라가 정작 찾는 걸 가린다.
  하루 전(2026-08-26) 이 화면의 발송 상태를 칩에서 점 하나로 줄인 것과 같은 이유다
  ("이 줄의 무게를 칩이 다 가져가면 안 된다").
- **🤖 `마감 후 자동 배정` 배지는 남겼다.** 여기서 그건 알림이 아니라 "이 배정은 담당자가
  검토한 게 아니라 마감 때 AI가 붙인 것"이라는 **출처 기록**이고 — 나중에 "이 소제목
  배정 왜 이래?"의 답이 된다 — 작고 조용해서 스캔을 방해하지 않는다.

결과적으로 보관함은 확정본에서 「확인했어요」를 누른 뒤 상태와 같아진다("보관함 =
이미 지나간 것"). 고친 곳은 `app/history_renderer.py`의 CSS 사본 한 규칙(`.article.
finalize-added`)을 통째로 지운 것뿐 — 마크업은 `render_article` 공용이라 안 건드렸고,
확정본의 동작에도 영향이 없다. 실측: 배경 `rgba(0,0,0,0)`·왼쪽 선 0px로 일반 기사 행과
완전히 동일(padding-left도 6px로 같음), 배지 6건 유지, `[단독]` 분홍 강조는 그대로.

## 숨긴 기사 관리 — 상단바에 홈, 묶음 이름에서 "에서 숨김" 제거 (2026-09-16)

### 상단바에 홈이 없어 막다른 화면이었다

`/hidden`의 상단바는 `초안 / 확정본` 둘뿐이었다. 2026-08-05에 "되돌리기 후 주로 가는 곳은
초안 아니면 확정본이지 홈으로 갈 일은 거의 없다"는 판단으로 홈을 뺀 것인데, 그 결과 이
화면만 앱에서 유일하게 홈으로 가는 길이 없는 화면이 됐다 — 확정본(`홈 / 실시간 / 초안`)·
초안·수시 어디에도 맨 왼쪽은 홈이다. 맨 왼쪽에 다시 넣었다(사용자 요청). `.topbar-inner`가
`space-between`이라 세 링크가 고르게 퍼지는데, 그건 확정본 상단바가 이미 그렇게 서 있다.

### 묶음 이름은 소제목 이름만

`<기타>에서 숨김`처럼 소제목 뒤에 "에서 숨김"을 붙여 왔는데, 그 뒷말은 **쓰레기통이라는 화면
자체가 이미 하는 말**이라 줄마다 반복될 이유가 없다(사용자 지적). 지금은 소제목 이름 그대로
`기타`이고, 소제목이 섞였거나 값이 없을 때만 예전처럼 `골라서 숨김`이 그 자리를 대신한다 —
그건 이름이 아니라 "이름을 못 붙인다"는 설명이라 문장 그대로 남는다. "통째로 숨김"이라고는
여전히 안 적는다(위 `group` 필드 항목 참고 — 저장된 값으로는 구분이 안 된다).

풀사이즈 `/hidden`(`app/settings_server.py`)과 확정본·초안 좌하단 팝오버
(`app/renderer.py` `_trash_batch_row_html`) 두 곳에 같은 문자열이 있어 함께 고쳤다.

## 좌하단 쓰레기통 — 숨긴 기사를 화면에서 바로 되살린다 (2026-09-02)

### 요청

수시 수집 결과 화면의 좌하단 「🗑 목록 밖 기사」 팝오버 캡처를 두고 "정기도 이렇게 구현할
수 있어? 보이는 거 똑같게." — 이어서 "이용자 입장에서는 통일성 있게 화면은 똑같아야 돼.
진입하는 것과. 하지만 안에 로직은 당연히 다를 수 있지."

### 그대로 못 베끼는 지점 셋

1. **모수가 다르다.** 수시의 "목록 밖 기사"는 카드 하나의 여집합이라 그 화면의 목록과 정확히
   짝을 이룬다. 정기의 `hidden_articles.json`은 **하루 전역**이라 초안·확정본·실시간 어디서
   숨겼든 한 목록에 들어온다 — 확정본과 초안의 배지가 같은 숫자일 수 있고 그게 정상이다.
   회차 교집합으로 걸러 "이 회차 것만" 보여주는 안은 기각했다: 초안에서 숨긴 기사는 아직
   회차 파일에 없어서, 숨겼는데 어느 쓰레기통에도 안 보이는 막다른 길이 생긴다.
2. **이름이 다르다.** "목록 밖 기사"는 조건 밖 + 직접 숨김 두 종류를 담는 이름이었고, 그래서
   수시 팝오버엔 "조건 때문에 빠진 기사는 검색어를 되돌리면 저절로 돌아와요"라는 안내가
   필요했다. 정기엔 조건 밖 개념이 없다 — 전부 담당자가 직접 누른 것이고 전부 복구된다.
   그래서 「숨긴 기사」이고, 안내 한 줄도 그 화면이 실제로 답해야 하는 사실
   ("자정이 지나면 목록이 비워져요")로 바꿨다.
3. **진입점이 둘이 됐다.** 정기 하단 고정 바에 이미 `/hidden`으로 가는 🗑️ 링크가 있었다.
   2026-08-20에 "배너 안 재분류 버튼"을 뺐던 것과 같은 문제라(같은 일을 하는 버튼이 한 화면에
   둘이면 "다른 건가?"라는 새 의문만 생긴다) 초안·확정본에서는 그 링크를 없앴다. 실시간현황의
   하단바 🗑️는 그대로 둔다 — 그 화면은 "필터 없이 날 것" 원칙이라 팝오버 대상이 아니다.

### 설계를 바꾼 한 마디 — "정기는 소제목째로 날려서 기사들이 더 많긴 해"

첫 시안은 수시와 똑같이 평평한 목록이었는데, 사용자가 이 사실을 짚었다. 소제목 통째
숨기기(`hideGroup`)·체크박스 일괄 숨기기(`bulkHideSelected`)는 한 번에 수십 건을 넣는다 —
평평한 목록이면 복구 버튼이 40개가 되고, 그중 하나를 찾을 수도 되살릴 수도 없다.

**그래서 화면을 둘로 갈랐다** (사용자 제안 — "우선 팝업으로 보이고 '더보기'를 해서 쓰레기통
풀사이즈로 보는 방안"):

- **팝오버(확정본·초안)** — 지금 몇 건이 빠져 있나 + 방금 날린 덩어리 되살리기. 묶음은 한 줄,
  낱개는 그대로 한 줄, 최대 5줄. 개별 펼치기가 **없다** — 그건 더보기 너머 일이다.
- **풀사이즈 `/hidden`** — 묶음을 펼쳐 한 건씩 고르기, 여러 묶음에 걸친 선택 복구.
  새 화면을 만든 게 아니라 이미 있던 화면을 승격시킨 것이라 비용이 오히려 줄었다
  (돌아가는 링크 `preview_href`/`scrap_href`도 그 템플릿에 이미 있었다).

**더보기는 같은 탭으로 간다.** 새 탭이면 거기서 복구했을 때 원래 탭이 옛 상태로 남아 화면이
거짓말을 한다(특히 초안은 작업 중). 대가로 초안에서 나갔다 오면 스크롤 위치를 잃지만, 더보기
너머까지 가는 일은 자주 있는 일이 아니라고 보고 감수했다.

### 묶는 기준 — `hidden_at` 3초, undo와 같은 값

`HIDDEN_BATCH_WINDOW_SEC = 3.0`은 `app/undo.py` `push(coalesce_sec=3.0)`과 **같은 값이다**.
↩ 되돌리기가 "한 걸음"이라 부르는 단위와 쓰레기통이 "한 묶음"이라 부르는 단위가 어긋나면,
팝오버에서 본 묶음 하나가 ↩ 두 번에 나뉘어 되돌아간다. 사용자 결정으로 3초를 그대로 갔다
(소제목 40건 숨기기가 3초를 넘으면 묶음이 둘로 갈리는 건 감수 — 실측 12건은 1.1초).

묶는 방식은 **기준점(가장 최신 기록)에서 3초** 안인 것만 같은 묶음에 넣는다. 앞 기록과의
간격을 이어 붙이는 연쇄 방식은 바쁜 1분 동안 숨긴 것이 전부 한 덩어리로 뭉칠 수 있어 안 쓴다.

### `group` 필드를 새로 저장한다 — 숨김 판정에는 관여하지 않는다

묶음에 "무엇을 날렸나"를 적으려면 소제목 이름이 필요해 `hide_article(..., group=)`을 늘렸다.
`outlet`/`title`/`pub_date`를 이미 그렇게 실어 보내고 있어 방식은 그대로고, 화면 세 경로
(단건 🗑️·체크박스 일괄·소제목 통째)가 `hideArticleBody(btn)` 하나를 쓰므로 JS도 한 줄이면
끝난다. 화면이 안 실어 보낸 경우는 `_hide_meta_fallback`이 초안 캐시·회차에서 찾아 채운다.

**이 값이 숨김 판정에 끼어들지 않는다는 게 규칙이다** — 2026-08-27에 "제목 대조 때문에 담당자가
누른 적 없는 기사가 조용히 사라진" 사고를 겪고 대조를 URL 하나로 되돌렸는데, 새 필드가 다시
판정에 섞이면 같은 종류의 사고가 재발한다. `group`은 오직 쓰레기통 화면의 묶음 이름에만 쓴다.

**"통째로 숨김"이라고는 안 적는다.** 저장된 값만으로는 그 소제목을 통째로 날린 것인지 그 안에서
몇 건만 골라 숨긴 것인지 구분할 수 없다 — 단정하면 화면이 거짓말을 한다. 묶음 안 기록의
소제목이 하나로 모이면 `<소제목>에서 숨김`, 섞였거나 값이 없으면 `골라서 숨김`이다.

### 그 밖에 정한 것

- **더보기 줄은 1건뿐이어도 항상 그린다.** 시안 단계에선 "갈 이유 없는 링크는 안 그린다"고
  했는데, 하단바 🗑️를 빼기로 하면서 이 링크가 `/hidden`으로 가는 **유일한 길**이 됐다.
  문구만 갈린다 — 묶음이 있으면 `쓰레기통 열어서 하나씩 고르기 →`, 없으면 `쓰레기통 열기 →`.
- **↩ 되돌리기가 위 칸(bottom: 88px)으로 올라갔다.** 쓰레기통이 0건이라 안 그려질 때도
  제자리를 지킨다 — 숨기기 한 번에 버튼이 아래에서 위로 뛰는 게 빈자리보다 불안하다
  (수시 화면의 `.fab.left.upper`도 같은 방식이다).
- **복구는 `location.reload()`다.** 숨김 해제는 기사 목록과 소제목 구성이 같이 바뀌어서,
  팝오버만 고쳐 그리고 배지만 줄이면 화면이 거짓말을 한다. 수시는 form POST + 303이라
  저절로 해결되던 부분.
- **복구는 건수와 무관하게 되돌리기 한 걸음이다.** 12건짜리 묶음을 되살린 뒤 ↩ 한 번이면
  통째로 다시 숨겨진다. `unhide_articles`가 한 번의 읽기-수정-쓰기로 처리해서, 단건 해제를
  12번 부를 때 생기던 파일 쓰기 12회·화면 재생성 12회도 함께 없앴다.
- 풀사이즈 화면에서 **가장 최근 묶음만 펼친 채**로 연다(정기 보관함이 최신 날짜를 미리
  펼쳐두는 것과 같은 규칙). 1건짜리 묶음은 접지 않는다 — 접을 게 없는데 접으면 클릭만 는다.

### 아직 안 한 것

- 풀사이즈에 "어느 화면에서 숨겼나"(초안/확정본/실시간) 표시 — 하루 전역 모수의 혼란을
  줄여주지만 저장 필드가 또 하나 늘어, 실제로 헷갈린다는 말이 나오면 그때 넣는다.
- 실시간현황에는 팝오버를 안 붙였다(위 진입점 항목 참고).

## 제목 대조로 인한 조용한 실종 — 자동 숨김을 없앴다 (2026-08-27)

### 제보

"이 기사는 '스크랩' 됐고 [초안]에서도 확인했는데 [확정본]에서는 안보여. 왜그래?" —
조선일보 `한은, 올해 성장률 전망 2.6%→3.3% 상향`(09:53). 실시간현황 화면에서는
`✔️ 스크랩됨` 배지가 붙어 있었다.

### 원인 — 두 겹이었다

**1. 제목 대조가 다른 기사를 잡아먹었다.** 10:32에 담당자가 숨긴 건 **세계일보**의
`한은, 올해 성장률 전망 2.6%→3.3% 상향`(10:12)이었는데, 조선일보 기사의 제목이
한 글자까지 같았다. `filter_hidden`이 URL **또는 제목**으로 대조했으므로 누른 적
없는 조선일보 기사가 함께 걸러졌다. 회차 파일(`2026-08-27_11-00.json`)에는
`group: 기타`로 멀쩡히 저장돼 있었고, 사라진 건 화면에 그릴 때뿐이었다.

**2. 실시간현황만 판정이 달랐다.** `app/live_renderer.py`가
`article["url"] in hidden_urls`로 URL만 봤다 — 확정본은 제목까지 보는데. 그래서
"확정본에서 걸러질 운명인 기사"에 `✔️ 스크랩됨`을 붙여 보여줬다. `curation.py`가
2026-08-25에 `hidden_match_keys()`/`is_hidden()`으로 판정을 한 곳에 모아두고
"두 벌로 갈라지면 어긋남이 재발한다"고 주석까지 달아뒀는데, 실시간현황만 그
규칙을 안 따르고 있었다(수시 모니터링은 따르고 있었다).

담당자가 원인을 추적할 방법이 **하나도 없었다** — 실시간현황은 거짓말했고,
확정본엔 그냥 없었고, "숨긴 기사 관리"에도 조선일보 기사는 안 뜬다(숨김 기록엔
세계일보만 있으니). 오늘만 이렇게 조용히 사라진 게 5건이었다.

### 같은 메커니즘이 정확히 일주일 전엔 반대 방향으로 터졌다

제목 대조를 넣은 게 바로 이 상황 때문이다(2026-08-20, 아래 항목):

> 숨긴 기사가 그 제목의 대표였다가 나중에 우선순위가 더 높은 언론사가 같은 제목으로
> 기사를 내면 대표가 교체되면서 부활한다

8/20의 담당자는 "숨겼는데 왜 **되살아나**?"였고, 8/27은 "숨기지도 않았는데 왜
**사라져**?"다. 코드가 보는 신호는 양쪽 다 "다른 URL, 같은 제목"으로 똑같아서
구별할 방법이 없다. 당시 근거였던 "제목만으로 판단해도 화면에서 사라지는 기사가
늘지 않는다(dedup이 어차피 1건만 남기므로)"는 전제가 깨지는 지점이 여기다 —
**dedup 대표는 회차마다 바뀔 수 있다**. 색인이 늦은 상위 언론사 기사가 나중에
들어오면 대표가 교체되고, 그때 옛 숨김 기록이 새 대표를 잡는다.

### 처음엔 배너로 보정하려 했다 (기각)

1차 대응으로 확정본·초안 상단에 "제목이 같은 다른 기사를 숨겨서 함께 안 보이는
기사 N건" 배너 + 되돌리기 버튼을 만들었다(`title_collision_dropped`,
`unhide_by_title`, `POST /unhide-by-title`). 실제로 동작했지만 사용자가 방향을
뒤집었다 — **"ai가 임의로 숨기는게 없어야 하네"**, **"기사 1개 1개가 소중해서
아예 '자동으로 숨기는' 것을 없애는게 낫지 않아? 제목이 똑같아도 이용자가 다 볼 수
있게 하는거지"**. 자동 숨김을 없애면 배너가 알릴 대상 자체가 사라지므로
("그렇게 되면 이런 불필요한 배너도 필요 없을거 아냐") 배너 코드는 통째로 걷어냈다.

### 세 방식을 실측으로 비교했다

오늘 회차 데이터 기준:

| 방식 | 되살아나는 기사 | 잘못 숨기는 기사 |
|---|---|---|
| 제목만 (사고 당시) | 0건 | **3건** |
| 언론사+제목 | 0건 | 0건 |
| URL만 (채택) | 2건 | **0건** |

`언론사+제목`이 오늘 데이터에선 만점이었는데도 `URL만`을 골랐다. 근거는 **두 실패의
무게가 대칭이 아니라는 것**: 부활은 중복이 화면에 *보이고* 🗑️ 한 번 더 누르면
끝나지만, 과잉 숨김은 *안 보이고* 회차 파일을 직접 파봐야 원인을 안다. 그리고
`언론사+제목`에도 "담당자가 클릭 안 한 기사가 자동으로 숨는" 성질은 그대로 남는데,
없애려던 게 정확히 그 성질이었다. 되살아난 2건도 실제로는 뉴시스 사진 캡션이었다.

저장된 회차 전체(고유 제목 2,515건)로도 재봤다 — 제목 중복은 38건뿐이고 그중
**20건이 제목만 우연히 겹친 별개 기사**였다. 제목 대조가 구해내는 재게재(18건)보다
잘못 치우는 쪽이 더 많았다. 부활 위험은 숨김 기록이 자정에 초기화되므로 하루 안으로
갇힌다는 점도 판단에 들어갔다.

### 최종

- `filter_hidden`/`is_hidden` — URL만 대조. `hidden_match_keys()`는 이미 있던
  `load_hidden_urls()`로 대체해 없앴고, `is_hidden(article, hidden_urls)` 2인자로
  줄였다(호출부 7개 파일: live/landing/trend_renderer/trend_data/adhoc 2곳/curation).
- 실시간현황이 `is_hidden()`을 거치도록 고쳤다 — **이 수정만은 남긴다**. 자동 숨김이
  없어져도 "URL로 직접 숨긴 기사"의 표시는 두 화면이 같은 기준을 봐야 한다.
- `title_collision_dropped`/`unhide_by_title`/배너 HTML·JS/`POST /unhide-by-title`/
  `raw_articles`·`raw_out` 배선 전부 제거.
- `hide_article`의 outlet/title은 이제 순수 표시용("숨긴 기사 관리" 화면이 URL만
  보여주지 않으려는 용도)이고 대조에 관여하지 않는다. 세 경로가 `hideArticleBody`를
  공유하고 서버가 `_hide_meta_fallback`으로 채우는 구조는 그대로 뒀다.

검증: 11:00 회차 45건 → 화면 12건, 빠진 33건이 **전부** URL로 직접 숨긴 것임을
확인(제목으로만 걸린 기사 0건). 통합 테스트 11개 통과.


## AI 분류 실패가 조용히 묻히던 3중 결함 (2026-08-27)

### 제보

홈 화면 "오늘의 쟁점"에 `국회`(2위) · `1주택자`(3위) · `이를`(5위)가 떴다. 사용자: "왜 또 이렇게 뜨는거?"

### 추적

그 이름들은 06:00 회차에 저장된 소제목이었고, `이를`·`한다`·`했다`·`강조`는 규칙 기반(단어 빈도) 폴백의 산물이다.

```
2026-08-27_06-00.json → 국회 4 · 1주택자 3 · 기타 3 · 이를 2 · 한다 1 · 했다 1 · 부총리 1 · 강조 1
```

로그에는 06:04:04에 Anthropic POST 200이 딱 한 번 찍혀 있고 경고가 한 줄도 없었다. LLM 캐시 파일에도 `06:00` round_id 항목이 아예 없어(09:00·09:30·11:00·14:00은 있음), **호출은 성공했는데 쓸 만한 분류를 못 받았다**는 게 확인됐다. 캐시 상한(40개) 때문에 밀려난 것도 아니다 — 그 사이 호출이 2번뿐이라 40에 닿을 수가 없다.

### 결함 셋이 겹쳐 있었다

**(1) 빈 응답이 재시도를 안 썼다.** `classify_with_llm`의 재시도 루프는 **예외일 때만** 재시도했다. HTTP 200인데 응답에 text 블록이 없거나 `_parse_groups`가 빈 결과를 주면 그냥 `break`로 빠져나가 남은 재시도 1회를 버렸다. 게다가 로그를 한 줄도 안 남겨, **결국 둘 중 어느 쪽이었는지 끝내 판별하지 못했다** — 코드가 증거를 버린 것이다.

**(2) `classification_degraded`가 저장된 적이 한 번도 없었다.**

```bash
python3 -c "import json,glob;print({k for f in glob.glob('data/articles/*.json') for k in json.load(open(f)) if k!='articles'})"
```
→ 81개 파일 **전부** 이 필드 없음. `finalize_new_subheadings`도 마찬가지.

원인은 `collect_run`이 `{"run_slot", "run_at", "articles"}` 세 개만 손으로 지어 돌려주고, 그 값을 받은 `confirm_and_promote`가 곧바로 `confirm_run`을 부르는데 그 함수가 `{**run, "confirmed": True, ...}`로 **파일을 통째로 덮어쓰는** 구조였다. `save_run`이 기껏 적어둔 필드가 저장 몇 밀리초 뒤에 사라진 것이다. 그 탓에 도입 이후 한 번도 동작한 적 없는 기능이 셋이었다 — 확정본 "⚠️ AI 분류 실패" 배너, 분류 실패 시 자동발송 차단, "마감 후 자동 배정" 배지.

**(3) 최신이 아닌 회차는 손댈 방법이 없다.** 재분류를 포함한 큐레이션 핸들러 8곳이 전부 `load_latest_run()` 하드코딩이고, 정기 보관함은 설계상 읽기 전용이다. 06:00 회차가 09:30 회차에 밀려난 뒤로는 담당자가 복구할 자리가 아예 없었다.

### 폴백 회차를 사후에 알아보는 법 — 실측으로 문턱을 찾았다

(2) 때문에 옛 회차에는 판정 근거가 저장돼 있지 않다. 저장된 데이터만으로 알아볼 방법을 찾으려고 80개 회차 전수를 재봤다 — "소제목 이름 중 한 어절(공백 없음)짜리 비율":

| 비율 | 회차 수 | 내용 |
|---|---|---|
| 0% | 68 | 정상 |
| 10~30% | 8 | `반부패정책협의회` `중앙지방협력회의` `금리정책` `예결위` — **정상 AI 이름** |
| **100%** | **4** | `2026-08-27_06-00` `2026-08-11_17-00` `2026-08-25_09-30` `2026-08-22_10-00` |

**0.3과 1.0 사이가 완전히 비어 있었다.** 100%인 4개는 전부 규칙 기반 폴백 회차였고 오탐이 0건이었다. 반면 처음 떠올린 "이름 하나하나를 보고 한 어절이면 버린다"로 갔다면 위 10~30% 구간의 멀쩡한 이름 10개를 같이 죽였을 것이다 — **이름 단위가 아니라 회차 단위로 봐야 갈린다**는 것이 실측의 결론이다(CODING_CONVENTIONS §1).

소제목이 3개 미만이면 판정하지 않는다. 정상 AI 이름이 우연히 전부 한 어절일 수 있어 표본이 너무 적고, 틀리는 방향이 "폴백을 못 알아본다"여야지 "멀쩡한 회차를 폴백으로 몰아붙인다"이면 안 되기 때문이다.

### 고친 것

1. `app/scraper.py` `collect_run`이 **방금 저장한 파일을 다시 읽어 돌려준다.** 필드를 손으로 나열해 채우는 대신 되읽는 이유는, 나열하면 `save_run`에 필드가 늘 때마다 여기도 같이 고쳐야 하고 안 고치면 같은 사고가 조용히 재발하기 때문이다. 되읽으면 "collect_run이 돌려주는 값 = 디스크에 저장된 값"이 구조적으로 보장된다. 회차당 파일 1개(하루 6번)라 비용은 무시할 수준.
2. `app/llm_classifier.py` — 빈 응답도 실패로 취급해 `continue`하고, 그 지점에 `stop_reason`·content 블록 타입·응답 앞부분을 `logger.warning`으로 남긴다. 다음에 같은 일이 나면 5초 만에 판별된다.
3. `app/classifier.py` `looks_rule_based`/`run_looks_rule_based` — 저장된 `classification_degraded`가 있으면 그 값을 우선하고(수집 시점에 실제로 관측한 사실이 사후 추정보다 정확하다) 없을 때만 이름 모양으로 추정한다. **소급 적용**되므로 오늘 06:00 회차도 즉시 걸린다. 확정본 배너·자동발송 차단(`app/confirm_send.py`)·정기 보관함 표식(`.degraded-mark`, 앰버) 셋이 이걸 쓴다.

### 남긴 것

(3)의 "최신이 아닌 회차 재분류"는 **일부러 안 만들었다.** "정기 보관함은 읽기 전용"이라는 기존 원칙을 깨는 일이라, 판정만 먼저 붙여 "이 회차는 AI가 못 묶은 회차"라는 사실을 정직하게 알리는 데서 멈췄다. 복구 통로를 열더라도 폴백으로 판정된 회차에만 열어 원칙 훼손을 최소화하는 쪽으로 간다 — [MORNING_ISSUES_DESIGN.md](archive/MORNING_ISSUES_DESIGN.md) §4.3.

### 곁가지로 드러난 더 큰 문제

이걸 파는 과정에서 홈 "오늘의 쟁점"이 정기 회차에 100% 종속이라는 점, 그래서 **담당자가 등록한 검색어 5개 그룹 중 2개(세제개편·추가 검색어, 오늘 972건)가 쟁점 카드에 원리적으로 못 올라온다**는 사실이 드러났다. 하필 담당자가 정책 단어 추이에서 지켜보겠다고 고른 단어(`종부세`·`비거주`)가 그 빠지는 그룹에 있었다. 그 대응은 별도 설계로 뺐다 — [MORNING_ISSUES_DESIGN.md](archive/MORNING_ISSUES_DESIGN.md).

## 검색어 계층을 화면이 거꾸로 말하던 문제 (2026-08-27)

### 어디서 나왔나

"AI 분류 실패가 조용히 묻히던 3중 결함"을 파는 중에 홈 "오늘의 쟁점"이 정기 회차에 100% 종속이라는 게 드러났고, 사용자가 이렇게 리프레임했다 — *"'실시간 현황' 검색어와 [초안] 검색어가 같지만 상위버전은 '실시간'이잖아. 이용자가 '실시간 현황'부터 검색어를 넣는다는 느낌을 줘야 하는데 지금 그게 안되는 것 같아."*

### 코드는 이미 맞았다

```
실시간현황   enabled=True 인 모든 그룹                      ← 상위집합
   ⊇
정기 스크랩  enabled=True AND include_in_scrap=True 인 그룹
```

`app/settings.py` `active_search_groups`가 정확히 그렇게 동작한다. 화면도 **틀린 말은 안 했다.** 문제는 읽는 순서와 단어가 인상을 뒤집는 것이었다.

| 위치 | 문구 | 문제 |
|---|---|---|
| 캡션 (제일 먼저 읽는 줄) | `저장 후 다음 스크랩 회차부터 적용됩니다. (실시간 현황은 즉시 반영)` | 정기가 주어, 실시간이 괄호 |
| 체크박스 라벨 | `…"→ 스크랩"으로 직접 옮겨야 **정식** 반영됩니다` | "정식"이 실시간을 비공식·미완으로 규정 |
| — | (없음) | **"실시간엔 기본으로 들어간다"가 어디에도 안 적혀 있었다** |

`정기 스크랩에도 포함`이라는 라벨 자체는 사실 정확하다 — "에도"가 상위집합 관계를 담고 있다. 그런데 그 앞의 "기본이 실시간"이 명시가 안 돼서, 담당자는 체크박스만 보고 "안 켜면 아무 데도 안 쓰이나?"로 읽게 된다.

덤으로, 그 라벨이 가리키는 `"→ 스크랩"` 버튼은 2026-08-11에 `📌 담아두기`로 이름이 바뀌었다 — **화면에 없는 컨트롤을 안내하는 죽은 문구**가 반년 가까이 남아 있었다.

### 실제 손실을 재봤다 (2026-08-27)

표현 문제로만 볼 수도 있었지만, 실측해보니 진짜 손실이 있었다.

```
오늘 정기 회차가 걸린 검색어 → 전부 scrap=True 그룹뿐
  재정경제부 115 · 구윤철 65 · 재경부 33 · 재정경제기획위원회 28 · 예결위 24 …

실시간에만 있는 그룹(scrap=False) → 오늘 972건
  인플레이션 376 · 물가안정 251 · 세제 개편 72 · ISA 58 · 보유세 55
  IMF 55 · 초과세수 40 · 종부세 38 · 양도세 26 · 장특공제 1
```

등록된 5개 그룹 중 **2개(세제개편·추가 검색어)가 통째로 홈 쟁점 카드에 못 올라온다** — 그 카드가 정기 회차 파일만 읽기 때문이다. 결정적으로 담당자가 정책 단어 추이에서 지켜보겠다고 고른 단어가 `종부세`·`비거주`인데, **그게 정확히 빠지는 그룹에 있었다.**

### 고친 것

**문구 3곳** — 캡션 어순을 뒤집고(`저장하면 실시간 기사 현황에 바로 반영됩니다. (정기 스크랩은 다음 회차부터)`), 안내문이 "등록한 그룹은 모두 실시간에서 검색되고 그중 일부만 정기"라고 먼저 말하게 하고, 체크박스 라벨에서 "정식"을 빼고 죽은 버튼 이름을 고쳤다.

**그룹 배지를 만들었다가 뺐다.** 처음엔 문구만으론 계층이 한눈에 안 읽힌다고 보고 그룹 이름 옆에 `🔴 실시간`(항상)·`💎 정기`(켰을 때만) 배지를 달았다. 브라우저로 검증까지 마쳤는데(scrap=True 3개는 배지 둘, false 2개는 하나, 토글하면 즉시 반영, 그룹을 끄면 opacity 0.4) 사용자가 이렇게 지적했다 — *"검색어에 일일히 '실시간, 정기' 뱃지를 붙이느니, 아예 안내문에 못을 박는게 낫지 않을까?"*

맞는 지적이었고 이유가 둘이었다:

1. **`🔴 실시간`은 모든 그룹에 똑같이 붙는다.** 행마다 값이 안 변하는 배지는 정보가 아니라 장식이다. 불변량을 행마다 반복하는 건 5번 가르치는 게 아니라 5번 성가시게 하는 것이다.
2. **`💎 정기`는 바로 세 줄 아래 체크박스와 같은 말이다.** 같은 사실을 두 번 말하는 것 — 수시 검색어 편집에서 "결과 배너를 띄우지 않는다(몇 건이 빠졌는지는 목록 건수와 배지가 이미 말한다)"고 정한 것과 정확히 같은 원칙이다.

**대신 계층을 화면 맨 위에서 한 번만, 문단이 아니라 구조로 못 박았다**(`.scope-note`):

```
🔴 실시간 현황    여기 등록한 그룹 전부를 검색합니다 (그룹 간 OR)
💎 정기 스크랩    그중 아래 "정기 스크랩에도 포함"을 켠 그룹만 (최소 1개 필요)
```

두 줄이 나란히 놓이면서 `전부` ⊃ `그중 …만`이 안 읽어도 눈에 걸린다. 배지가 잘하던 일(읽지 않아도 계층이 보임)은 남기고, 못하던 일(행마다 반복·중복)만 걷어낸 셈이다. 배지에 쓰려던 색 어휘는 이 상자가 그대로 물려받았다 — 홈 흐름도의 `a.live`=`live_bg`+`error`, `a.done`=`accent_tonal`+`header`(CLAUDE.md "Design direction" 규칙 1·2).

칩 문구도 한 번 고쳤다. 처음엔 `🔴 실시간` + `기사 현황 — …`으로 칩과 뒷문장이 이름 중간에서 갈라져 어색하게 읽혔다 — 칩을 목적지 이름 전체(`🔴 실시간 현황`)로 만들고 뒤에는 규칙만 남겼다.

### 검증

브라우저에서 확인 — 그룹 헤드에 배지가 남지 않았고(`switch`/`group-name-input`/`group-off-tag`/`group-trash` 넷뿐), 안내 상자 두 줄이 정상 렌더링되며, 통합 테스트 11개 통과. 체크박스 라벨은 2줄에 들어간다(처음 쓴 문안은 3줄이라 같은 내용을 줄였다).

### 남은 것

이 계층을 **데이터 쪽에서도** 맞추는 일(홈 쟁점 카드의 원천을 정기 회차에서 실시간으로 옮기기)은 [MORNING_ISSUES_DESIGN.md](archive/MORNING_ISSUES_DESIGN.md) Phase B로 뺐다. 화면이 먼저 계층을 바로 말해야 그 카드가 왜 그 원천을 쓰는지 설명이 되기 때문에 이 작업(Phase A)을 앞에 뒀다.

## 용어 정리 — 화면에서 「정기」를 걷어냈다 (2026-08-27)

**계기.** "수시는 번외 기능이고 모든 건 정기 위주인데, [정기]/[수시]로 두는 게 오히려
헷갈리는 것 아니냐"는 사용자 문제 제기.

**실측(홈 화면에 실제로 박힌 글자).** 「정기」7회 : 「수시」3회. 그런데 그 7회 중
5회는 수시와 구분하려는 게 아니라 **자기 숫자의 출처를 해명하는 한정어**였다 —
`정기 수집 기준 · 오늘 112건`, `정책 단어 추이 · 정기 수집 기준`,
`* 정기 수집 기사의 제목·요약 기준`, `오늘의 쟁점 · 정기 · 분류된 93건 기준`,
`정기 모니터링 · 일자 › 시간대`. 수시가 없었으면 전부 `오늘 112건`으로 끝났을 말들이다.
**번외 기능 하나 때문에 본업 화면이 카드마다 자기를 해명하고 있었다.**

**용어 자체의 결함 둘.**
1. **대립쌍이 깨져 있었다.** 정기/수시는 *주기*의 축인데, 실시간 기사 현황도 정해진
   시각에 안 돈다. 「정기의 반대편」 자리에 실시간과 수시가 둘 다 앉아 있었다. 실제로
   둘을 가르는 건 주기가 아니라 **검색어가 어디서 오느냐**(검색어 사전 ↔ 카드 자체)다.
2. **이름 체계가 섞여 있었다.** 확정본·초안·보관함은 *산출물* 이름인데 정기/수시는
   *방식* 이름이다. 그래서 "정기 보관함"은 방식+산출물 합성인데 "확정본"엔 아무것도
   안 붙었다.

**결정(사용자).** ① 번외 이름 = **사안 수집**(카드마다 사안 이름을 붙이는 실제 동작과
일치, "정기의 반대말" 자리를 비운다) ② 홈 카드 한정어 4곳 **전부 삭제** ③ 보관함 =
**보관함 / 사안 보관함** ④ 다운로드 파일명도 화면과 맞춤.

**④의 빈자리 처리.** 파일명 규칙 `날짜_종류_구분`의 종류 자리에서 「정기」가 빠지면
`2026-08-27.xlsx`처럼 맨숭맨숭해진다 — 파일은 화면과 달리 다운로드 폴더에서 다른
파일들과 섞이므로 종류 표기가 실제로 일한다. 그래서 그 자리에 **화면 헤더가 이미
쓰는 제품 이름(`언론모니터링`)**을 넣었다(회차별 txt가 이미 그 이름을 쓰고 있었다).

**안 바꾼 것.** 코드·데이터·URL의 내부 용어(`/adhoc`, `data/adhoc/`,
`include_in_scrap`)는 그대로다 — 화면 글자만 바꾸는 것과 달리 저장 데이터·경로·함수명이
전부 흔들리는데 담당자가 얻는 건 0이다. 화면 ↔ 내부 대응표는 CLAUDE.md "화면 용어" 참고.
「정기 스크랩」도 *나란히 대비해야 뜻이 서는* 세 자리(`/keywords` 계층 안내 상자,
`정기 스크랩에도 포함` 체크박스, `/breaking-alert` API 호출량 안내)에는 그대로 남겼다.

**곁가지로 같이 정리된 것.** `/breaking-alert`의 마스터 토글 `수시 감시 사용`은
「수시」가 기능 이름 자리에서 빠지면서 무슨 뜻인지 애매해져 `감시 사용`으로 줄였다.
라벨 보관함의 출처 태그 `정기`는 대비 상대가 사안명이라 **`회차`**로 바꿨다(본업에
이름을 다시 붙이는 대신, 이미 화면에서 쓰는 말을 골랐다).

## 용어 재정리 — 수시 쪽만 「사안」에서 「수시」로 (2026-09-02)

**계기 — 전제가 바뀌었다.** 2026-08-27의 결정("번외 이름 = 사안 수집, 정기는 한정어를
안 붙인다")은 **수시가 곁가지일 때** 성립하던 것이다. 그런데 이날 사용자가 수시에
`스크리닝 → 모아보기` 2단을 주기로 하면서(정기의 `실시간현황 → 확정본`과 같은 모양),
둘이 대등한 흐름이 됐다. 그러면 이름 있는 쪽/없는 쪽의 비대칭은 "본업 대 곁가지"가
아니라 **한쪽을 부를 말이 없는 상태**가 된다. 사용자 판단: "[정기]모니터링과
[수시]모니터링으로 이원화하는 게 맞다."

**근거 — 이미 이름이 네 벌로 갈라져 있었다.** 화면에 「정기」를 안 쓰기로 했는데도
정기를 가리키는 말이 셋이었다: 파일명 `2026-09-01_언론모니터링_14-00.txt`, 헤더
`언론 모니터링 09:00 기준`, `/keywords`의 `💎 정기 스크랩`. 수시도 마찬가지로
화면은 `사안 수집`인데 파일명은 `사안`이었다. 이름을 안 붙인 게 아니라 **자리마다
다른 이름이 붙어 있었다**는 쪽이 사실에 가깝다.

**결정(사용자) — 수시만 바꾼다.** `사안 수집` → **수시 모니터링**, `사안 보관함` →
**수시 보관함**, 파일명 종류 자리 `사안` → `수시모니터링`. **정기 쪽 화면 글자는 한
자도 안 건드렸다** — 헤더 `언론 모니터링`, `보관함`, 예외 세 자리의 「정기 스크랩」,
파일명 `언론모니터링` 전부 그대로. 2026-08-27의 "본업은 자기를 해명하지 않는다"가
정기에는 여전히 유효하다고 봤기 때문이다(즉 이번 변경은 그 원칙의 폐기가 아니라,
수시 쪽에만 이름을 세워준 것이다).

**파일명은 풀네임으로.** 종류 자리에 `수시`가 아니라 `수시모니터링`을 넣었다 —
2026-08-27에 정기 파일명을 `언론모니터링`으로 정한 것과 같은 이유다: 파일은 화면과
달리 다운로드 폴더에서 맥락 없이 혼자 놓이므로, `2026-09-01_수시_….xlsx`는 그
자리에서 아무 뜻도 못 낸다.

**경계 — 「사안」을 없앤 게 아니다.** 바꾼 건 **흐름 이름으로 쓰이던 자리**뿐이다.
카드 하나가 다루는 대상의 이름으로서의 「사안」은 그대로 남았다: `사안명` 입력칸,
수시 보관함의 `사안 ▸ 날짜 ▸ 회차` 3단과 `사안명` 검색, 라벨 출처 태그(수시는
사안명만), 홈 흐름도의 축 라벨 `수시 모니터링 · 사안 › 일자`. 일괄 치환으로 처리하면
이 자리들까지 같이 죽으므로, 실제 작업도 두 뜻을 갈라 놓고 시작했다.

**곁가지.** 수시 보관함의 빈 화면 문구는 `아직 보관된 사안 수집이 없습니다` →
`아직 보관된 수집이 없습니다`로 줄였다 — 그 문장이 놓인 화면 제목이 이미 「수시
보관함」이라 "수시"를 한 번 더 말할 이유가 없다.

**안 바꾼 것.** 내부 용어(`/adhoc`, `data/adhoc/`, `include_in_scrap`,
`active_search_groups`)는 2026-08-27과 같은 이유로 그대로다.

## 골격 3단 이름 재정리 — 정기/수시 두 흐름이 같은 모양으로 대비되도록 (2026-09-02)

**계기.** 같은 날 앞서 「수시 모니터링」 이름을 정한 직후, 사용자가 "이 골격을 뜻하는
이름이 바뀌어야 이해가 빠를 것 같다"며 화면 3단 구조 자체의 이름을 다시 짚었다 —
정기(초안→확정본→보관함)와 수시(새 수집→수집 결과→수시 보관함)가 같은 3단 흐름인데
이름 짓는 방식이 서로 달라, 두 흐름이 대응한다는 게 이름만 보고는 안 읽혔다.

**결정(사용자).**

| 정기 모니터링 | 수시 모니터링 |
|---|---|
| 초안 (그대로) | 새 수집 → **수집 시작** |
| 확정본 (그대로) | 수집 결과 → **수집 확정** |
| 보관함 → **정기 보관함** | 수시 보관함 (그대로) |

정기는 `보관함` 하나만 `정기 보관함`으로 바꿨고 `초안`·`확정본`은 그대로 뒀다. 수시는
`새 수집`·`수집 결과` 둘 다 바꿔 `초안 ↔ 수집 시작`, `확정본 ↔ 수집 확정`처럼 정기와
1:1로 짝지어 읽히게 했다 — "수집 확정"은 "이 카드의 결과가 (정기의 확정본처럼)
발행됐다"는 뜻이지 실제로 어떤 확정 절차가 새로 생긴 건 아니다. 이름만 정기와
대칭으로 맞춘 것이고, 화면의 동작(검색어 편집·소제목 배정 등)은 전혀 안 바뀌었다.

**정기 쪽 예외 — 2026-08-27 규칙과의 충돌.** 같은 날 오전에 "정기 쪽 화면 글자는
한 자도 안 건드렸다"고 적었는데, 이번에 `보관함` → `정기 보관함`으로 바뀌면서 그
문장이 더는 맞지 않게 됐다. 2026-08-27의 "본업은 자기를 해명하지 않는다"는 원래
"이 숫자에 수시는 안 섞였다"는 식으로 **자기 존재를 변명하는 한정어**를 겨눈
규칙이었다. `정기 보관함`은 그 뜻이 아니라 **수시 보관함과 짝을 이루는 고유
이름**이다 — 정기가 수시를 해명하는 게 아니라, 나란히 놓인 두 이름이 서로를 완성하는
관계라 성격이 다르다고 보고 예외로 다뤘다.

**"수집 결과"라는 말 자체를 없앤 게 아니다.** 화면의 정체성(nav 링크·h1·title·탭
제목)을 가리킬 때만 `수집 확정`으로 바꿨다. 카드 화면 상단의 "오늘의 수집 결과"
사안명 탭 헤더처럼 그날 만든 카드 여러 개를 가리키는 일반 서술은 그대로 뒀다 — 그
자리는 화면 하나의 이름이 아니라 "지금까지 모은 카드들"이라는 뜻이라 바꿀 이유가
없었다. `수집 결과가 0건인 것은 정상`(스케줄러 로그), `조건에 맞는 수집 결과가
없습니다`(수시 보관함 빈 상태) 같은 일반 명사 용법도 같은 이유로 남겼다.

**안 바꾼 것.** 내부 용어(`/adhoc`, `data/adhoc/`, `include_in_scrap`)는 물론,
2026-08-27에 정한 예외 세 자리(「정기 스크랩」)와 파일명 규칙(`언론모니터링`,
`수시모니터링`)도 그대로다. ADHOC_DESIGN.md의 화면 스펙 원문(§5.2 "새 수집" 등)은
이번 갱신 대상에서 뺐다 — 확정 스펙 원본이라는 위치상 코드·CLAUDE.md와 별도로
다룬다.

**[같은 날 3차 — 최종] 수시 1·2단계를 「~본」 체계로 다시 잡았다.** `수집 시작` →
**`수집 원본`**, `수집 확정` → **`수집 확정본`**. 위 2차 이름을 실제로 화면에 올려놓고
보니 두 가지가 걸렸다(사용자 지적: "플로우 용어가 잘 안 와닿음").

1. **층위가 안 맞았다.** 정기는 `초안`·`확정본`이라는 **산출물 이름**인데 수시는
   `수집 시작`·`수집 확정`이라는 **동작 이름**이었다. 나란히 놓으면 같은 층으로 안
   읽힌다.
2. **`수집 시작`은 그 화면이 하는 일의 5%만 가리켰다.** 스크리닝이 붙으면 그 화면은
   새 기사가 들어올 때마다 계속 돌아가는 곳이 되는데, "시작"은 카드를 만드는 첫 10초
   뿐이다. 3단 구조를 검토할 때 "세 번째 화면이 할 일이 없다"고 기각한 것과 정확히
   같은 결함이 이름에 남아 있었다.

`원본 ↔ 확정본`은 한국어 문서 어휘로 이미 짝이라 두 화면의 관계("원본에서 골라
확정본을 만든다")가 이름만으로 설명된다. 사용자가 중간에 제안한 `새 수집` /
`수집본` 안은 기각했다 — 구분자가 "새"(시간) vs "본"(형태)로 **축이 달라 대비가 안
서고**, `수집본`은 뜻이 오히려 1단계(모은 것 전부) 쪽에 붙어 실제 내용과 반대로
읽힌다. 다만 `~본`으로 가자는 방향 자체는 사용자 제안이 맞았고, 축만 바로잡았다.

**덤으로 얻은 것.** `확정본`을 맨몸으로 쓰는 자리가 사라졌다 — 정기는 `정기 확정본`,
수시는 `수집 확정본`. 같은 날 앞서 손으로 고친 한정어 규칙("자기 흐름은 한정어 없이,
남의 흐름을 가리킬 때만 붙인다")이 이름 층에서 자동으로 강제되는 셈이다.

**동작 CTA는 안 바꿨다.** 새 카드를 만드는 버튼 두 곳(수시 보관함 빈 화면의
`새 수집 시작`, 카드 탭 줄의 `+ 새 수집`)은 **화면 이름이 아니라 동작**이라
`수집 원본`으로 바꾸지 않았다. 이 둘이 가리키는 목적지가 `수집 원본` 화면인 건
맞지만, "만들기"를 "원본"이라 부르면 버튼이 무슨 일을 하는지 안 읽힌다.

**아직 이름과 화면이 어긋나 있는 구간.** 이 시점에 `/adhoc/new`는 여전히 **입력 폼
전용**이고 최신순 원본 목록은 아직 없다 — 스크리닝 구현 전이라 `수집 원본`이라는
이름이 앞서 나가 있는 상태다. 스크리닝이 붙으면 그 화면이 이름값을 하게 된다.

## "정기가 기사를 놓친다"는 의심 — 원인은 언론사 화이트리스트였다 (2026-08-27)

**의심의 출발.** 수시(사안 수집) 카드가 가져온 기사의 45~85%가 같은 날 정기 회차에는
없었다. 같은 검색어(`예결위`·`예산결산특별위원회`, 둘 다 `include_in_scrap` 켜진 등록
검색어)에 같은 시간대인데도 그랬다 — 정기 파이프라인이 자기 키워드로 잡았어야 할
기사를 흘리는 것 아닌가 하는 의심.

**첫 측정이 틀렸다.** 설정에서 화이트리스트를 `settings["selected_outlets"]`로 읽었는데
그런 키는 없다(빈 리스트 반환 → "화이트리스트 미사용"으로 오판). **실제 키는
`outlet_order`이고 43개 매체가 들어 있다.** 이 오판 때문에 "필터가 아니다 → 그럼 버그"로
한 칸 잘못 갔다. 설정 키를 확인 없이 짐작한 대가 — CODING_CONVENTIONS.md §1.

**전수 귀속 결과(미설명 0건).** 수시에만 있던 105건을 하나씩 원인에 배정했다:

| 카드 | 수시에만 | 화이트리스트 밖 | 미등록 검색어 | 제목중복 | 설명 안 됨 |
|---|---|---|---|---|---|
| 08-21 재경위 | 4 | 0 | 4 (`재정경제위원회`) | 0 | 0 |
| 08-24 재경위 | 54 | 43 | 10 (`재정경제`) | 1 | 0 |
| 08-25 예결위 | 25 | 25 | 0 | 0 | 0 |
| 08-25 재경위 | 22 | 22 | 0 | 0 | 0 |

**버그가 아니다.** 정기는 `collect_run`에서 `filter_by_outlet_whitelist(outlet_order)`를
거치고, 수시는 카드 옵션 `use_outlet_whitelist`가 **기본 꺼짐**이라 안 거친다. 같은
검색어·같은 창이라도 결과 규모가 다른 게 정상이다.

**규모 실측(2026-08-27 09:30~17:00, 등록 검색어 10개).** 원시 598건 → 화이트리스트 통과
211건(**64%인 387건이 걸러짐**), 제목중복까지 적용하면 192건. 2시간 창(15:00~17:00)만
따로 재면 180건 → 48건으로 **73%**가 걸러졌다.

**곁가지 관찰 셋.**
- 걸러진 387건 중 **`[단독]` 3건**이 있었다 — 알림 축(감시 그룹 키워드를 넓게 훑고
  화이트리스트를 안 본다)과 회차 축의 모집단 차이가 실제로 벌어진 사례다.
- 걸러진 것 중 **47건은 도메인이 아니라 매체 이름으로 인식된 곳**(앱의 oid/도메인 표에
  있는데 43개 목록엔 없는 매체): 아이뉴스24 8건, 더팩트 4건, SBS Biz 4건, MBN 4건.
  아주경제(`ajunews.com`)는 10건인데 이름 표에도 없어 도메인으로 떴다.
- **화면 어디에도 "이번 회차에서 언론사 목록 때문에 N건이 빠졌다"는 표시가 없다.**
  담당자가 그 규모(64~73%)를 알 방법이 지금은 없다.

**수시의 실제 쓸모가 재정의됐다.** 설계 의도는 "검색어 사전에 없는 사안을 즉석에서"였고
실제 그 용도로 쓴 건 4번 중 1번(`재정경제`)뿐이다. 나머지 3번이 한 일은 **같은 검색어를
언론사 필터 없이 넓게 다시 보기**였다 — 정기가 놓쳐서가 아니라, 정기가 의도대로 좁혀서.

**결정: 아무것도 만들지 않는다 (2026-08-27, 사용자).** 화면에 "언론사 목록으로 N건 제외"를
알리는 시안 셋(회차마다 칩 / 기사 제목까지+개별 되살리기 / 설정 화면에만 제안,
[OUTLET_EXCLUDED_MOCKUP.html](trash-can/OUTLET_EXCLUDED_MOCKUP.html) — 안 만들기로 해서 `trash-can/`으로 옮겨뒀다)을 만들어 검토했고 **전부 안
하기로 했다.** 근거는 둘 — ① 제외 387건 중 담당자가 실제로 손댈 수 있는 건 **15건(4%)**뿐이고
나머지 96%는 설정 목록에 아예 없어 체크할 방법조차 없다. 조치할 수 없는 값을 매 회차
띄우면 곧 안 보게 된다(홈 "기타 N건"을 중립 회색으로 둔 것과 같은 원칙). ② **정기는 지금
기능을 만족한다** — 43곳 목록이 의도대로 동작 중이고, 넓게 볼 창구는 사안 수집이 이미
맡고 있다. 고칠 것이 없는데 화면을 늘리지 않는다.

**다시 이 얘기가 나오면 먼저 볼 것.** 위 귀속표(미설명 0건)와 4% 숫자다. "정기가 기사를
놓친다"는 관찰이 또 올라오면 그건 대개 **화이트리스트 대 사안 수집의 옵션 차이**를 보고
있는 것이다. 별건으로 남은 진짜 한계는 하나 — 선택 목록이 58곳뿐이라 아이뉴스24(오늘 8건)·
아주경제(10건)는 고르고 싶어도 못 고른다(`app/naver_api.py` `OUTLET_CATEGORIES` 확장 +
oid 실측이 필요). 이건 화면 문제가 아니라 데이터 표 문제다.

## 라벨 — 방금 만든 라벨이 "이미 쓴 라벨"에 안 나오던 문제 (2026-08-28)

**제보**: 라벨 하나("비상경제본부회의")를 새로 만들어 붙인 직후, 다른 기사에서 같은 라벨을
다시 붙이려고 🏷 팝오버를 열었더니 "이미 쓴 라벨"에 그게 없고 예전 라벨("테스트")만 있었다.

**원인**: `.known`(이미 쓴 라벨 칩 목록)은 페이지를 구울 때 서버가 `known_label_chips_html()`로
**한 번** 만들어 그 페이지의 모든 팝오버에 같은 HTML로 박아둔 값이다(`app/renderer.py`
`render_page` → `render_article(known_labels_html=…)`, 초안도 동일). 붙이기/떼기 응답을 받는
`applyLabelState`는 그 기사 행의 `.attached`·`.lab-row`·🏷 배지·건수만 다시 그리고,
`.known-chip`에 대해서는 `used` 클래스만 토글할 뿐 **새 칩을 만드는 코드가 아예 없었다**.
그래서 새로고침 전까지 새 라벨은 자기 행의 목록에도, 다른 행의 목록에도 안 나왔고,
칩 옆 건수도 로드 시점 값에서 멈춰 있었다. 부작용으로 `checkSimilarLabel`("비슷한 라벨
있어요" 경고)도 같은 낡은 목록을 읽어, 이번 세션에 만든 라벨의 오타 변형을 못 잡았다.

**고른 방식과 기각한 대안**:
- (채택) 서버가 붙이기/떼기 응답에 최신 목록(`known`)을 함께 싣고, 화면은 그걸 전역 변수에
  담아뒀다가 **팝오버를 열 때** 칠한다. `applyLabelState`가 이미 "추측으로 DOM을 짜맞추지
  않고 서버가 돌려준 최종 목록을 그대로 믿는다"는 원칙을 `.attached`에만 적용하고 있었는데,
  `.known`까지 같은 원칙으로 넓힌 것이다.
- (기각) 클라이언트가 칩 하나를 append하고 건수를 ±1. 서버를 안 건드려 싸지만, 건수가 이
  페이지 기준 추측이라 전역 값과 어긋나고(라벨은 다른 회차·수시 기사에도 붙는다), 떼서 0이
  됐을 때 칩을 지워야 하는지를 화면이 알 수 없다. 같은 종류의 버그를 하나 더 심는 셈.
- (기각) `location.reload()`. 확실하지만 스크롤 위치·펼쳐둔 `<details>`·체크박스 선택이 다
  날아간다. 라벨은 여러 건을 연속으로 붙이는 동작이라 특히 나쁘다.

**열려 있는 팝오버는 일부러 갱신하지 않는다.** 목록이 건수 내림차순이라 라벨을 붙이는 순간
칩 순서가 바뀔 수 있고, 그러면 칩을 연속으로 누르던 담당자가 커서 밑에서 움직인 엉뚱한
라벨을 누르게 된다. 실제로 브라우저 확인에서 재현됐다 — 라벨 하나를 붙이자 전역 목록이
`테스트 2 · 비상경제본부회의 1`로 **순서가 뒤바뀌었고**, 열려 있던 팝오버는 원래 순서를
그대로 유지했다. 대신 팝오버를 "열 때" 칠하는데, `toggleLabelPopover`가 어차피 열 때마다
`used`를 다시 매기고 있어 그 자리에 그대로 얹힌다(추가 장치 없음). 감수한 불편: 라벨을
만든 그 팝오버를 **열어둔 채** 곧바로 비슷한 이름을 또 입력하면 유사 라벨 경고가 방금
만든 걸 못 잡는다(닫았다 열면 잡힌다). "위험을 피하는 쪽, 불편하더라도"가 사용자 결정.

**비용 실측(2026-08-28)**: `label_stats()`는 지금 데이터(항목 2건) **0.019ms**, 기사 5,000건 ×
라벨 200종을 가정해도 **1.67ms**. 응답에 붙는 바이트는 칩당 약 184B(라벨 200종이면 37KB).
같은 요청이 이미 `_regenerate_screens()`로 index.html(292K)·preview.html(312K)을 통째로 다시
굽고 있어서 반올림 오차다. DOM 쪽도 팝오버를 열 때 하나만 칠하므로 O(1).

**뒤로 안 깨진다**: 화면 JS는 `typeof data.known === "string"`으로 가드해, 응답에 `known`이
없으면(구버전 서버·수시 화면) 전역이 `null`인 채로 남아 페이지에 구워진 목록을 그대로 쓴다.

**수시 모니터링은 손대지 않았다** — 그 화면의 라벨 붙이기는 실제 `<form>` POST + 303
리다이렉트라 매번 전체가 다시 그려져서 애초에 이 증상이 없다.

**남은 한계**: 라벨 보관함/라벨 관리 화면에서 이름 변경·합치기·삭제를 하면, 열어둔 확정본
탭은 여전히 옛 목록을 들고 있다(다른 탭에서 벌어진 일이라 응답이 이 탭에 안 온다). 지금처럼
새로고침이 필요하다. 막으려면 폴링이나 SSE가 필요한데 이 앱 구조에 과하다.

## 실시간현황 「미선정 기사」 → 「아직 처리 안 한 기사만」 (2026-09-01)

**출발은 이름 문제가 아니라 기능 요청이었다** — "실시간에서 '숨긴 기사'나 '스크랩됨'
기사 안 보이게 체크하는 기능이 있으면 좋겠다"(사용자). 그런데 그 일을 하는 체크박스가
이미 있었다(`미선정 기사`). 다만 요청보다 넓게 걸러서, 숨김·스크랩됨뿐 아니라
담아둠·자동 선별까지 넷을 한 덩어리로 뺐다.

**실측(2026-09-01, 그 시점 실시간 캐시 3,152건 기준)**: 숨김 263 · 스크랩됨 333 ·
담아둠 12 · 자동 선별 약 204. 숨김+스크랩됨 둘만 빼면 380건(12%)이 걸러진다.

**기능을 쪼개는 시안 넷을 만들어 보고 전부 기각했다**(`LIVE_STATUS_FILTER_MOCKUP.html`,
네 라운드). A(통합 체크박스 1개: 숨김+스크랩됨만 제외) · B(분리 2개) · D(「빼고 보기」
칩 4개) · D′(같은 것을 체크박스로). 기각 사유는 두 가지였다:

- **A·B**: 같은 줄에서 `선택 언론사만`·`[단독]`·`[속보]`는 "~만 보기"인데 새 체크박스만
  "~빼고 보기"라 방향이 어긋난다. 개수가 늘어나는 것보다 이 섞임이 더 헷갈린다.
- **D**: 상태 칩이 **바로 아래 그룹 칩과 모양이 같은데 뜻이 정반대**다(그룹 칩=켜면
  그것만 보임, 상태 칩=켜면 그것만 사라짐). 같은 모양이 한 화면에서 반대로 동작하면
  담당자는 글자보다 모양을 먼저 믿는다. D′는 그 충돌은 없앴지만, 결국 **📌 담아둔
  기사를 남겨두고 훑을 일이 없다**는 사용자 판단이 나오면서 기능을 쪼갤 이유 자체가
  사라졌다 — 넷을 한 번에 빼는 지금 동작이면 충분했다.

**그래서 로직은 한 줄도 안 바꾸고 이름만 고쳤다.** 진짜 문제는 `미선정`이 *무엇에*
선정되지 않았다는 건지 화면 어디에도 없다는 것이었다.

**중간에 방향을 한 번 잘못 잡았다(기록).** 위 A·B를 기각한 근거가 "방향 섞임"이었는데,
정작 이름 후보로는 `이미 처리한 기사 제외`를 추천했다 — 그러면 넷 중 **하나만** 반대
방향이 되어 내가 반대 근거로 댄 그 문제를 스스로 만드는 셈이었다. 사용자가 바로
지적했다("이 UI는 긍정이잖아"). 그 줄은 원래부터 넷 다 "포함" 방향으로 통일돼 있었고,
`미선정 기사`도 그 점에서는 어긋나지 않았다 — 어려운 건 방향이 아니라 **단어**였다.

**확정 문구.** 라벨 `아직 처리 안 한 기사만`(`~만`으로 끝나 `선택 언론사만`과 어미까지
맞는다), 툴팁 `숨김·스크랩됨·담아둠·자동 선별 — 담당자나 앱이 이미 손댄 기사를 모두
보이지 않게 합니다`. 툴팁은 라벨과 방향이 반대인데 이건 의도다 — 툴팁을 여는 이유가
"그래서 뭐가 없어지는데?"라서고, 라벨끼리(줄에서 나란히 읽힘)와 달리 툴팁은 혼자
읽히므로 충돌하지 않는다.

**툴팁 문구에서 `AI` → `앱`으로 바꿨다.** 사용자 초안은 "담당자나 AI가 이미 손댄"이었는데,
넷 중 AI가 한 건 **하나도 없다**: 숨김·담아둠은 담당자, 스크랩됨은 스케줄러 수집,
`자동 선별`은 `_auto_drafted_urls`의 규칙 기반 근사치(게시시각·언론사 화이트리스트·
키워드 문자열 매칭, LLM 호출 0회)다. 🤖 아이콘과 연보라를 써서 AI처럼 보이지만
아니다. 마침표도 뺐다 — 이 화면 툴팁 관례(`스크랩에 추가`, `아직 오늘 첫 회차가
없어요`, `이 기사가 걸린 검색어`)에 마침표가 없다.

**같이 고친 버그 — 툴팁이 이미 절반만 맞고 있었다.** 옛 툴팁은
`담아둠·자동 선별·숨김이 아닌 기사만`이라 **스크랩됨(그날 333건, 넷 중 가장 큰 덩어리)이
빠져 있었다.** 판정은 `row.dataset.status !== "normal"`이라 스크랩됨도 실제로는
제외된다. 사용자가 중간에 제안한 라벨(`숨겨둔 기사, 자동선별 기사 제외하기`)도 같은
함정이었다 — **일부만 나열한 라벨은 모호한 라벨보다 나쁘다**: 모호하면 툴팁을 확인하게
만들지만, 절반만 맞는 목록은 확인할 생각 자체를 없애 없어진 333건을 찾지 않게 된다.
그래서 후보 셋(`이미 처리한 기사 제외`/`처리한 기사 제외`/`손댄 기사 제외`)을 전부
넷을 포괄하는 말로만 만들었다.

**남은 것**: 숨김·스크랩됨만 따로 빼는 기능은 만들지 않았다. 📌 담아둔 기사를 화면에
남겨두고 훑을 일이 생기면 그때 D′를 다시 꺼내면 된다.

## 수시 기사 행을 정기와 같은 규격으로 (2026-09-02)

담당자가 정기 확정본과 수시 수집 확정본 스크린샷 두 장을 나란히 놓고 지적했다 —
"수시도 정기처럼 아이콘들과 기능이 똑같이 있어야 하고, 텍스트나 여백 크기도 다르다."
실제로 재보니 제목 0.93 vs 1rem, 메타 줄 0.78 vs 0.8rem, 들여쓰기 25 vs 28px, 카드
여백 8/9px·위아래 2px + 아래 실선 vs 6/8px·위아래 10px였다. 기능은 넷이 통째로
빠져 있었다(N분 전 · 🔍 검색어 · 📷 사진 추정 · [단독]/[속보] 강조), 복사 피드백도
달랐고(버튼 안 HTML을 통째로 갈아끼워 그동안 옆 버튼들이 흔들렸다), ⋯ 더보기가 없었다.

**같은 스크린샷에 📌 담아두기와 「🗑 정기 확정본에서 숨김」 칩이 아직 보인다는 제보도
같이 왔는데, 그건 코드가 아니라 실행 중인 서버 문제였다** — 둘 다 같은 날 이미
제거돼 있었고(`grep -rn "pin-article\|hidden-tag\|확정본에서 숨김\|togglePin" app/`
→ 0건), 담당자가 보고 있던 건 그 변경 전에 띄워둔 서버였다.

**⋯ 더보기를 넣을지가 유일한 쟁점이었다.** 처음엔 "넣지 말자"고 답했다 — 그 메뉴의
두 항목이 `summary_overrides.json`(URL 키 전역)에 저장하므로, 수시에서 제목을 고치면
같은 URL의 정기 확정본·정기 보관함 제목까지 바뀌고, 그게 방금 📌를 없애며 정한
"정기·수시 연결을 끊는다"와 부딪힌다고 봤다. **담당자가 두 번에 걸쳐 되물으면서
그 판단이 뒤집혔다** — "수시에서도 원문 불러오기가 필요할 수도 있지 않아? 네이버 API
끌어오는 건 똑같잖아", 그리고 "기사제목 직접 수정도 역시 마찬가지로".

맞는 지적이었고, 근거 셋이 그쪽을 받쳤다:

1. **필요가 똑같다.** 같은 네이버 검색 결과를 쓰니 `"..."`로 잘린 제목이 수시에도
   그대로 나온다. 수집 시점의 자동 복구(`_fetch_full_title`)도 두 흐름이 같은 함수를
   쓰므로, ⋯는 그 자동 복구가 실패했을 때의 수동 보완이라는 성격까지 같다.
2. **📌와 성격이 다르다.** 📌가 옮기던 건 담당자의 **판단**("이 기사를 정기 보고서에
   넣겠다")이라 흐름마다 뜻이 달랐다. 여기 담기는 건 **"이 URL의 제목이 무엇인가"라는
   사실 하나**라, 같은 기사면 어디서 보든 같은 게 맞다 — 갈라두는 쪽이 오히려 이상하다.
3. **설계가 원래 그렇게 돼 있었다.** `summary_overrides.json`은 `labels.json`과 함께
   "두 흐름이 함께 쓰는 저장소는 `app/` 최상위에 둔다"는 규칙으로 이미 거기 있었다
   (CLAUDE.md 라벨 절에 그 이유가 적혀 있다). 단방향 의존 규칙 위반이 아니다.
   ✏️도 대개 🔄가 못 찾을 때의 대체 수단이지 제목 창작 도구가 아니고, 정기 안에서도
   이미 전역이다(초안에서 고치면 확정본·보관함·실시간이 다 바뀌는데 문제된 적 없다).

**진짜 위험은 다른 데 있었다.** `apply_summary_overrides()`가 정기 4곳(확정본·초안·
실시간현황·정기 보관함)에만 걸려 있고 **수시엔 아예 안 걸려 있었다.** 이 상태로 ⋯만
붙이면 *수시에서 고쳤는데 그 화면만 그대로이고 정기만 바뀌는* 정반대 어긋남이 됐다.
그래서 `_article_groups()`(화면·복사·txt·엑셀이 모두 지나는 길목)에 한 줄을 같이
걸었다 — 실측으로 세 출력 전부에 전파되는 걸 확인했다. 좌하단 「목록 밖 기사」도 같은
이유로 함께 입힌다.

**기각한 대안 둘**: ② 카드 안에만 저장하는 별도 필드 신설 — 같은 기사의 제목이 화면마다
달라지고, 저장소가 둘로 갈리면 어느 쪽이 맞는지 아무도 모른다. ③ 아예 안 넣기 — 위 1번
때문에 필요 자체를 부정할 수 없다.

**결과**: 목업(`ADHOC_ROW_MATCH_MOCKUP.html`, A 정기 기준 / B 수시 현재 / C 제안 3단
비교)으로 합의한 뒤 그대로 반영. 요약 상자(수시의 옅은 배경 + 왼쪽 테두리)는 수시 쪽이
더 예뻤지만 "같아 보이는 것"이 먼저라 정기의 "여백만"으로 내렸다. 행 안의 아이콘만
`.a-acts` 하위 선택자로 따로 잡아, 소제목 헤더의 📋🗑▲▼(같은 `.icon-btn`)는 안 건드렸다.

## Design direction

[Updated: 2026-07-24] Light card-based theme (switched from the original dark navy theme after readability feedback, esp. links). Palette (`app/config.py` `COLOR_*`): background `#F8FAFC`, card `#FFFFFF`, header/heading `#1E3A5F`, accent (links/buttons) `#2563EB`, body text `#1F2937`, muted text `#6B7280`, border/divider `#E5E7EB`, hover `#EFF6FF`, error `#DC2626`. Desktop-first layout, responsive enough to be viewable on mobile.

## 수시 모니터링 — 모음 카드 [2026-09-02]

**시작은 담당자의 질문 하나였다.** "종부세도 재정경제부도 건수가 너무 많은데, 양쪽에서
필요한 기사만 골라 확정본 하나로 넘기고 싶다. 지금 방법 자체가 잘못된 걸까?"

맞는 진단이었다. 카드 하나 = 검색 조건 하나 = 보고서 하나로 셋이 한 몸이라, "조건 2개 →
보고서 1개"가 원리적으로 불가능했다. **근본 원인은 수집 단위와 산출물 단위를 1:1로
묶어둔 것**이다 — 검색 조건은 넓을수록 좋고(놓치면 안 되니까) 보고서는 좁을수록
좋은데(읽는 사람이 있으니까), 한 몸이라 서로를 벌줬다. "종부세가 너무 많이 걸린다"가
문제로 **느껴지는 것 자체가 증상**이었다. 넓게 걸리는 건 원본의 미덕인데 원본이 곧
보고서라 벌을 받고 있었다.

### 세 번 갈아엎은 견적

처음엔 "제일 정확하지만 제일 비싸다 — 저장·되돌리기·보관함·엑셀을 전부 다시 짜야
한다"고 답했다. **그 견적이 틀렸다.** 코드를 열어보니 확정본을 카드 **밖에** 세울 때만
그 비용이 나고, **카드의 한 종류로 두면 대부분 사라졌다** — 카드 스키마에 `articles`/
`group_order`/`custom_groups`/`group_summaries`가 이미 다 있어서, "검색을 안 하는 카드"
하나만 만들면 소제목·순서·숨기기·복사·엑셀·되돌리기가 전부 공짜로 딸려왔다. 견적을
부풀린 채 담당자에게 답했다가 코드를 보고 정정한 사례다(CODING_CONVENTIONS.md §1
"추측하지 말고 잰다"가 설계 견적에도 적용된다).

**받는 쪽 배선은 절반이 이미 깔려 있었다.** 스키마의 `added_by: "manual"`과, 그 값을
시간창 검사에서 면제하던 `article_out_of_window`. 폐기된 §6.7(📌 담아두기)의 흔적이
남아 그 값을 **만드는 코드가 하나도 없는 채로** 놀고 있었다.

**대신 진짜 구멍을 하나 찾았다.** `article_in_condition`은 manual을 면제하지 **않았다** —
모음은 `keywords`가 비어 있어 이 한 줄이 없으면 보내는 즉시 전부 "검색어에 안 걸림"으로
조건 밖에 떨어진다. 기능이 통째로 안 도는 자리였는데, 짝인 `article_out_of_window`는
이미 면제하고 있어 **둘이 어긋나 있었다.**

### 기각한 대안 셋

| 대안 | 기각 이유 |
|---|---|
| **라벨 보관함을 키운다** | 담당자가 묘사한 것("사람이 이름 붙이고, 여러 출처에서 보내고, 자동 연동 아님")이 라벨과 90% 같아서 먼저 제안했다. 담당자 판단으로 기각 — "라벨은 완전한 별도의 제3의 기능이라 엮고 싶지 않다." 설계상으로도 라벨은 다축 태그(AND 검색)이고 모음은 순서가 본질인 문서라 층위가 다르다 |
| **확정본을 카드 밖 새 저장 단위로** | §3 자기완결 규칙이 깨지고 저장·되돌리기·보관함·엑셀·소제목이 두 벌이 된다. 그러고 얻는 게 없다 |
| **검색어를 한 카드에 합친다** | 지금도 되지만 두 조건을 항상 같이 봐야 하고 한쪽만 못 좁힌다. 건수 문제는 오히려 심해진다 |

### "보낸 기사는 어떻게 되나" — 필터가 딸려 나온 경위

담당자가 물었다. "소제목 중 일부만 보내면 그 기사는 어떻게 돼? 소제목 전체를 보내면
전체로 넘어가나?" 답은 **복사라서 아무것도 안 변한다**였는데(건수도 안 변하고 구멍도 안
난다), 그 질문이 진짜 문제를 짚고 있었다 — **원본이 안 줄어든다.** 43건에서 12건을
보내도 화면은 여전히 43건이다. 그래서 `☑ 아직 안 보낸 기사만` 필터가 **선택이 아니라
규칙 1의 필연**이 됐다. 정기 실시간현황의 「아직 처리 안 한 기사만」과 같은 문제였고,
그 화면의 해법을 그대로 가져왔다.

같은 대화에서 규칙 두 개가 더 떨어져 나왔다. **모음에서 🗑 숨기면 원본의 `보냄`이
풀린다** — 모음엔 완전 삭제가 없고 숨기기뿐이라, 이게 없으면 실수로 보낸 기사가 막다른
골목이 된다. `sent_index`가 숨긴 기사를 빼고 매번 다시 계산하므로 **별도의 「취소」
동작 없이 기존 숨기기가 그대로 취소가 된다**(새 UI 0개). 그리고 **소제목 📥를 다시
누르면 남은 것만 간다.**

### 표시 방식 — 세 안을 실물로 비교했다

담당자가 물었다. "이동한 기사는 아래로 내리는 게 좋겠니, 음영처리만 짙게 하는 게
좋겠니?" 실물 목업([ADHOC_SENT_MARK_MOCKUP.html](mockups/ADHOC_SENT_MARK_MOCKUP.html))으로
셋을 나란히 놓고 비교했다 — **보낸 기사를 2·4·7번째로 흩어놓은 게 핵심**이었다(몰려
있으면 세 안이 다 비슷해 보인다).

- **A. 음영만** 기각 — 43건짜리 실제 원본에서는 훑을 때 안 잡히고, 📷 사진 추정 기사가
  같은 `opacity: .55`를 써서 **왜 흐린지 구분되지 않는다.**
- **C. 아래로 내리기** 기각 — ① 담당자의 ↑↓ 순서를 앱이 자동으로 만지는 동작이라
  CLAUDE.md가 명시적으로 금지한 지점이고 실제로 사고가 났었다(HISTORY.md "담당자 > AI:
  정렬·분류 우선순위 3종"), ② 위 "숨기면 보냄이 풀린다"가 생기면 **원래 자리를 저장해야
  한다**, ③ **필터와 하는 일이 겹친다** — 목업에서 필터를 켠 채 A·B·C를 갈아 끼우면
  셋이 완전히 같아진다. 그게 C가 불필요하다는 증거였다.
- **B. 띠 + 흐리기 + 칩** 채택. 세 겹이 서로 다른 거리에서 일한다(훑을 때 / 눈이 닿을 때
  / 읽을 때).

**목업을 만들다 한 번 고쳤다.** 처음엔 `opacity`를 행 전체에 걸었는데, 그러면 **왼쪽 띠와
✓칩까지 같이 흐려진다** — 담당자가 요구한 "아주 가시적"을 담당해야 할 둘이 정작 약해지는
앞뒤 안 맞는 상태였다. 흐리기를 「내용」(`.a-title`/`.a-bot`)에만 걸어 **신호는 100%,
내용만 55%**로 바꿨다. 구현 때 하나 더 나왔다 — 띠를 `border-left`로 그리면
`.art-scoop`([단독] 기사)이 이미 그 속성을 쓰고 있어 **둘 중 하나가 상대를 지운다.**
`inset box-shadow`로 넣어 두 사실("단독이다" + "보냈다")이 같이 보이게 했다.

### 「모음 관리」 탭을 안 만든 이유

담당자가 물었다. "수시확정에서는 검색 조건 변경 탭 대신 '모음 관리' 탭이 있는 게
맞는 거지?" 답은 **아니오**였다. 검색어 편집 패널이 존재하는 이유는 "조건을 고치면
목록이 바뀐다" 하나뿐인데 모음엔 조건이 없다. 넣을 후보를 전부 세어보니 이름
바꾸기·출처별 건수·기사 빼기·삭제가 **이미 다른 자리에서 되고**, 남는 건 "원본으로
돌아가기" 하나뿐이었다 — 그건 패널이 아니라 **이미 있는 📥 출처 칩에 링크만 걸면**
끝나는 일이었다. 빈 칸을 못 견뎌 만든 패널은 담당자를 열게 만든 다음 실망시킨다.

**그 질문 덕에 설계 오류를 하나 잡았다.** 초안에 "「목록 밖 기사」 팝오버를 안 그린다"고
써뒀는데 틀렸다 — 모음에도 **숨긴 기사는 생긴다.** 안 그리면 🗑 누른 기사를 복구할 길이
사라진다. 다행히 목록을 만드는 코드(`숨김 or 조건 밖`)는 그대로 두면 조건 밖이 0이라
자연히 맞아떨어졌고, 고칠 건 이름(`숨긴 기사`)과 안내 문구뿐이었다 — 특히 "조건 때문에
빠진 기사는 검색어를 되돌리면 저절로 돌아와요"는 모음에서 **거짓말**이 된다.

### 담당자가 정한 것 넷

1. **복사** (이동 아님) — 원본은 검색 조건의 결과라 손대면 조건과 어긋나고, 재수집하면
   어차피 다시 딸려온다.
2. **자정 잠금 유지** — 검색을 안 하니 §6.3의 원래 근거는 안 걸리지만, 수시 전체를 하루
   단위로 일관되게 두는 쪽. 대가로 어제 원본 → 오늘 모음이 막힌다(그 사실을 명시적으로
   확인받았다).
3. **표시는 B안**(띠+흐리기+칩).
4. **소제목 통째로 보내면 이름도 들고 간다** — 낱개는 미분류. 담당자의 의도가 그 선택
   안에 이미 들어 있다는 근거(묶음을 통째로 집었다는 건 그 이름도 쓸 만하다는 판단).

### 구현 중 밟은 것

- **`window: None`이 11곳을 지나간다.** `card["window"]["end"]`를 그대로 두면 엑셀·라벨·
  보관함·행 렌더가 터진다. `window_of()` 하나로 모았고, **빈 문자열을 돌려준다** — 없는
  값을 `23:59` 같은 그럴듯한 값으로 채우면 엑셀 「스크랩종료시간」이 거짓말을 한다.
- 복사·txt 헤더도 갈렸다 — 모음은 `수시 모니터링 (모음명)`으로 **시각을 안 붙인다**
  (수집 시간창이 없어 "N시 기준"이 가리킬 대상이 없다).
- 안내 문구를 한 번 잘못 썼다 — 「빈 모음 만들기」 화면에서 "수집 원본 화면에서
  「모음으로 보내기」로"라고 적었는데, 실제로 보내는 화면은 **수집 확정본**이고 라벨도
  「모음으로」다. 화면에 없는 컨트롤을 가리키는 죽은 문구를 만들 뻔했다(2026-08-27
  `/keywords` 체크박스 라벨과 같은 종류의 실수).
- **입구가 없을 뻔했다.** `/adhoc/new?kind=bundle`을 만들어놓고 거기로 가는 링크를 어디에도
  안 달아서, 행 드롭다운의 `+ 새 모음 만들기` 말고는 그 화면에 닿을 길이 없었다. 수집
  원본 화면 부제에 링크를 달았다.

## 쓰레기통 화면 폭 800px · 좌하단 스택 규격 실제 통일 (2026-09-02)

### 어디서 나왔나

담당자가 `/hidden`(숨긴 기사 관리) 화면을 보고 "왜 쓰레기통은 목록 폭이 다르냐"고 물었다.
같은 기사를 확정본(800px)에서 숨겼는데 그걸 되살리는 화면만 480px이라, 제목이 두 줄로
접히고 묶음 헤더(체크박스·이름·시각·건수·모두 복구 다섯 요소)가 한 줄에 빠듯했다.

### 원인은 의도가 아니라 상속

`/hidden`은 `app/settings_server.py`의 `_BASE_STYLE`을 그대로 쓰는데 거기 `.container`가
`max-width: 480px`다. 그건 **설정 폼용 폭**이다 — 같은 파일에 `input[type=text]`가
`max-width: 360px`으로 잡혀 있는 게 그 증거다. 그런데 `/hidden`에 들어가는 내용물은
폼이 아니라 기사 목록이었다. 라벨 보관함·라벨 관리는 같은 문제를 먼저 겪고
`.container.wide { max-width: 760px }`로 탈출해 뒀는데, `/hidden`만 안 따라갔다.
결과적으로 **같은 성격의 기사 목록에 폭이 셋**이었다 — 800(확정본·초안·실시간·수시
수집 결과) / 760(라벨 두 화면) / 480(숨긴 기사).

### 고친 것 — 스위치를 body 하나로

`.container.wide`(760)를 없애고 `_BASE_STYLE`에 `body.wide .container`/
`body.wide .topbar-inner`를 800px로 뒀다. 세 화면(`/hidden`·`/labels`·`/label-manage`)이
`<body class="wide">`만 붙인다. **컨테이너 클래스가 아니라 body 클래스인 이유**는
상단바(`.topbar-inner`, 고정 위치)까지 같이 넓혀야 하기 때문이다 — CSS는 부모를 못
고르므로 컨테이너 쪽 클래스로는 상단바에 닿지 않는다. 라벨 두 화면은 컨테이너만
760으로 넓혀둔 상태여서 상단바 링크가 본문 안쪽에 떠 있었는데 이번에 같이 맞았다.
480px 자체는 그대로 뒀다 — 입력칸 하나 놓는 진짜 설정 화면들엔 그 값이 맞다.

### 곁가지 — 문서가 거짓말을 하고 있었다

CLAUDE.md는 정기 좌하단 스택을 "수시 수집 결과 화면(`.fab.left`/`.fab.left.upper`)과
**같은 자리·같은 규격**"이라고 적어뒀는데, 실제 값은 전부 어긋나 있었다:

| | 정기(`app/renderer.py`) | 수시(`app/adhoc/renderer.py`) |
|---|---|---|
| 쓰레기통 위치 | left 20 / bottom 20 | left 16 / bottom 16 |
| 쓰레기통 크기 | 56px | 52px(+테두리 2px로 실제 58px) |
| ↩ 되돌리기 | bottom 88 / 50px | bottom 78 / 46px |
| 팝오버 | left 20 / bottom 88 / 336px | left 16 / bottom 78 / 320px |
| 그림자 | 0 4px 12px | 0 2px 8px |

**정기 값으로 통일했다.** 정기 화면은 우측 발송 fab이 이미 56px이라 좌우가 짝을 이루고,
수시 화면엔 우측 fab이 아예 없어(`.fab`는 좌하단 스택 둘에만 쓰인다) 그 화면 안에서
부딪힐 대상이 없다. 수시 쓰레기통만 `<div>`(나머지는 `<button>`)라 UA 기본
`box-sizing`이 달라 56px로 적어도 실제로는 58px로 나왔다 — `box-sizing: border-box`를
명시해 막았다.

### 검증

브라우저에서 실측했다. `/hidden`·`/labels`·`/label-manage` 셋 다 컨테이너 850px(800 +
좌우 여백 24 + 테두리 1) · 상단바 848px로 확정본과 같은 값이고, 좌하단 스택은 두 흐름이
`fab [left 20, 56px, bottom 20]` / `↩ [left 20, 50px, bottom 88]` / `팝오버 [left 20,
366px, bottom 88]`로 완전히 같아졌다. 통합 테스트 11개 통과.

### 곁가지 2 — 라벨 화면의 ↩는 CSS가 아예 없었다

폭을 넓히고 화면을 다시 보다 발견했다. 라벨 보관함·라벨 관리의 ↩ 되돌리기
버튼(`_label_undo_fab_html`)은 `.undo-fab` 클래스를 달고 나가는데
`app/settings_server.py` 어디에도 그 CSS가 없었다 — CLAUDE.md는 "정기 화면과 같은
자리·같은 모양"이라고 적어뒀지만, 실제로는 좌하단 고정 원형 버튼이 아니라 `_BASE_STYLE`의
기본 `button` 스타일을 그대로 뒤집어쓴 **파란 네모 버튼**이 본문 흐름에 그려지고 있었다.
삽입 위치까지 두 화면이 달라서(라벨 보관함은 `body_html` 끝에 이어붙여 **카드 안쪽**,
라벨 관리는 `</script>` 뒤라 **화면 왼쪽 끝**) 같은 버튼이 두 화면에서 서로 다른
자리에 떠 있었다.

`app/renderer.py`의 `.undo-fab` 값을 그대로 `_LABEL_STYLE`에 옮기고, 라벨 보관함의
삽입 지점도 컨테이너 밖으로 빼 두 화면을 형제 자리로 맞췄다(`position: fixed`라 결과는
같지만, 삽입 위치가 다르면 다음에 또 갈린다).

**`bottom`을 20px이 아니라 88px(위 칸)로 뒀다.** 이 두 화면엔 아래 칸을 차지하는
쓰레기통이 없어 왼쪽 아래 모서리가 빈 채로 남는다 — 그래도 확정본·초안·수시에서 ↩가
늘 그 자리이므로, 같은 버튼이 화면마다 높이를 바꾸는 것보다 모서리가 비는 편이 낫다고
봤다(정기에서 "쓰레기통이 없어도 ↩는 위 칸을 지킨다"고 정한 것과 같은 판단).

실측: 두 화면 다 `[left 20, 50×50, bottom 88, position fixed]`로 정기 확정본과 동일.

## 앱 중복 실행 — 두 프로세스가 같은 회차를 각자 확정하던 사고 (2026-09-02)

담당자 제보: "또 초안에서 확정본 넘어가면서 소제목이 통째로 바뀌고, 초안에서 이미 본
기사들이 「마감 후 자동 배정」으로 떴다."

### 관측

- `ps`에 `main.py`가 **두 개** 떠 있었다(16:19 시작·16:47 시작).
- 로그에 같은 회차가 두 번 저장돼 있었다 — `17:00:11 (run_at=17:00:01)`,
  `17:00:16 (run_at=17:00:05)`. 저장된 파일의 `run_at`이 `17:00:05`이므로 담당자가 본
  확정본은 **나중에 켠 프로세스가 만든 것**이었다.
- 화면 배너: "기사 26건이 자동으로 배정됐어요 · 소제목 3개는 이때 새로 만들어졌어요."
- 확정본 소제목은 `국제경제 외교`·`이형일 부총리 후보 인사청문회`처럼 **멀쩡한 LLM
  이름**이었고 `classification_degraded`도 `False`였다 — **규칙 기반 폴백이 아니었다.**
  문제는 폴백이 아니라 초안에서 다듬어둔 이름(`G20 재무장관·중앙은행 총재 회의`,
  `부총리 후보자 언론간담회 등`, `중앙행정기관·공공기관 이전 계획`)이 통째로 다른
  이름으로 갈아엎힌 것이었다. 체감은 폴백과 같아 제보 문구도 그렇게 나왔다.

### 원인

LLM 분류 캐시는 **프로세스 시작 때 한 번만** 파일을 읽고(`_ensure_cache_hydrated`),
저장할 땐 **메모리 전체를 파일에 통째로 덮어썼다**(`_persist_cache_locked`). 프로세스
간 공유 장치가 하나도 없었다.

1. 16:47:20 — P2 시작, 그 시점의 캐시 파일을 읽음
2. 16:47:43 / 16:48:29 — **P1**(설정 서버를 쥔 쪽)이 초안 22건을 분류해 파일에 저장.
   P2는 이미 하이드레이트를 끝내 이 결과를 영영 모른다
3. 17:00 — P2가 확정 처리. 자기 메모리에서 찾을 수 있는 최선이 **14:39경에 만들어진
   11건짜리 옛 초안 스냅샷**뿐이라 `_find_partial`이 그걸 재사용 → 나머지 26건이
   미분류로 떨어지고 `assign_to_existing`이 소제목 3개를 새로 지어냄
4. P2가 캐시 파일을 자기 메모리로 덮어써, P1이 저장해둔 22건짜리 분류가 **디스크에서도
   사라졌다**(사후 확인: 캐시 파일에 그 항목이 아예 없음)

두 번째 앱이 살아남은 이유는 bind가 데몬 스레드 안에서 일어났기 때문이다 —
`Address already in use`로 그 스레드만 조용히 죽고 **스케줄러 스레드는 계속 돌았다.**

덤으로, 두 프로세스가 각각 `collect_run`을 도는 바람에 `clear_preview_order()`·
`clear_draft_pending_articles()`도 두 번 불려, 먼저 돈 쪽이 비운 뒤 나중 쪽이 계산해
**초안에서 ↑/↓로 정리한 순서가 사라졌다.** `group_overrides`·`hidden_articles`의
읽기-수정-쓰기 잠금도 프로세스 안에서만 걸려 있어 두 앱 사이엔 무방비였다.

### 고친 방법 — 포트를 단일 실행 자물쇠로

`main.py`가 설정 서버 포트를 **데이터를 건드리는 어떤 단계보다 먼저, main 스레드에서
동기로** bind한다(`app.settings_server.create_settings_server`). 실패하면 "이미 앱이
켜져 있다"는 뜻으로 읽고 브라우저만 그 앱으로 열어준 뒤 물러난다.

- **PID 파일을 안 쓴 이유**: 앱이 강제 종료되면 "죽은 자물쇠"가 남는다. 포트는 OS가
  프로세스 종료 시 알아서 풀어준다.
- **실측(2026-09-02)**: `HTTPServer`는 `allow_reuse_address = 1`(SO_REUSEADDR)이지만,
  다른 프로세스가 **LISTEN 중인** 같은 주소·포트에 거는 bind는 그래도 `EADDRINUSE`로
  실패한다(errno 48). 자물쇠로 쓰기에 안전하다는 걸 실제로 확인하고 넣었다.
- **부수효과가 오히려 이득이다**: 앱이 멈춘 것 같아 다시 실행했을 때(이 날도 10:49~10:57
  `settings.json` PermissionError 폭주 뒤에 두 번 다시 켰다) 조용히 두 개가 도는 대신
  **원래 창이 열린다** — 담당자가 실제로 하는 행동을 벌주지 않고 받아준다.

**2차 방어선**으로 `_persist_cache_locked`를 "덮어쓰기"에서 "파일을 읽어 병합한 뒤
쓰기"로 바꿨다. 같은 키는 메모리 쪽이 이기고, 파일에서 온 항목은 최근 것부터 상한의
2배까지만 남긴다. 중복 실행 자체는 포트 자물쇠가 막지만, 그 방어가 뚫려도 **남의 분류를
지우지는 않게** 하려는 것이다.

## 초안에서 숨긴 기사가 확정본에서 되살아나던 문제 — 원인은 정렬이었다 (2026-09-02)

같은 날 함께 제보된 두 번째 증상: "삭제한 게 다 튀어나왔어."

### 관측

`data/hidden_articles.json`의 16:52~16:56 기록 33건은 `group` 값이
`구윤철 부총리 G20 외빈 면담`(32건)·`G20 재무장관·중앙은행 총재 회의`(1건)이었다 —
**초안에서 소제목 통째로 숨긴 것**이지 실시간현황에서 낱개로 숨긴 게 아니었다.
그런데 확정본에는 제목·발행시각이 한 글자도 다르지 않은 같은 기사가 그대로 실려 있었다.

| 담당자가 숨긴 URL | 확정본에 뜬 URL | 제목 |
|---|---|---|
| 연합 001/00162859**32** | 001/00162859**31** | 구윤철 부총리, 스콧 베선트 미국 재무장관 면담 |
| 연합 001/00162859**37** | 001/00162859**39** | 구윤철 부총리, 니르말라 시타라만 인도 재무장관 면담 |
| 뉴시스 003/00141648**35** | 003/00141648**37** | 이탈리아 재무장관 만난 구윤철 부총리 |

…9쌍 전부 같은 패턴. **숨긴 ID는 확정본 회차 파일에 아예 없었고, 쌍둥이 ID만 있었다** —
즉 "숨김이 풀린" 게 아니라 **대표 기사가 뒤바뀐** 것이었다.

### 원인 — 부분 정렬이라 대표가 재현되지 않았다

통신사는 같은 기사를 네이버 기사 ID만 다르게 여러 건 올린다.
`app.filters.deduplicate_by_title`은 제목이 같으면 **"먼저 나온 1건"**만 남기는데,
그 "먼저"의 기준인 도착 순서가 두 화면에서 다르다:

- **초안** = 증분 캐시(`data/preview_cache.json`)에 여러 번에 걸쳐 쌓인 순서
- **확정본** = 마감 시점에 회차 시간창을 한 번에 재검색한 순서

`app.sorter.sort_by_outlet_priority`가 **언론사 순위 하나만 보는 안정 정렬**이라, 같은
언론사 안에서는 순서가 정의돼 있지 않고 입력 순서가 그대로 대표를 결정했다. 초안 캐시
위치를 실측하니 4쌍 모두 **담당자가 숨긴 쪽이 앞**(106↔110, 100↔107, 102↔108,
130↔145)이었다 — 초안에서는 그게 대표라 그걸 숨겼고, 확정본에서는 순서가 달라 쌍둥이가
대표가 됐다. 숨김 대조는 URL 하나뿐이라(2026-08-27 결정, "제목 대조로 인한 조용한 실종"
참고) 숨긴 적 없는 URL이 그대로 보고서에 실렸다.

### 측정 (오늘 원시 기사 1,702건)

| | 묶음 수 |
|---|---|
| 제목이 완전히 같은 묶음 | 34 |
| ↳ **같은 언론사끼리**(= 쌍둥이 케이스) | **11** |
| ↳ 다른 언론사 섞임 | 23 |

같은 언론사 11묶음은 전부 같은 기사 재게재이거나 서로 바꿔도 무방한 연속 사진이었고,
**우연히 제목만 겹친 별개 기사는 0건**이었다.

### 고친 방법 — 정렬을 완전 정렬로

`sort_by_outlet_priority`에 2차 키를 넣어 `(언론사 순위, 발행시각 최신순, URL)`로
정렬한다. 정렬 키가 결정론적이면 초안과 확정본이 **항상 같은 대표를 고르므로**, 담당자가
숨긴 URL이 곧 확정본에 나오는 URL이 된다. 검증: 오늘 원시 218건의 입력 순서를 30번
무작위로 섞어도 대표 기사 집합이 항상 동일했다(고치기 전에는 두 화면에서 갈렸다).

2차 키를 발행시각 최신순으로 잡은 건 사용자 결정이다 — URL 순도 재현되기는 하지만
순서에 뜻이 없고, 같은 언론사 안에서 최신 기사가 위로 오는 편이 읽기에도 낫다.
`pub_date`가 없거나 형식이 깨진 기사는 없는 시각을 지어내지 않고 맨 뒤로 보낸다.

### 기각한 대안

- **숨김 대조를 (언론사+제목)으로 되살리기** — 이걸로도 막히긴 한다. 하지만 2026-08-27에
  내린 "자동 판단이 담당자 대신 기사를 치우지 않는다"를 되돌리는 것이고, **정렬 수정이면
  그 원칙을 안 건드리고도 막힌다.** 원칙을 되돌릴 이유가 없었다.
- **확정본이 재검색 대신 초안 목록을 그대로 이어받기** — 문제 계열 전체(순서·분류·대표)를
  한 번에 없애는 가장 근본적인 길이지만, 마감 직전 기사를 놓칠 위험이 있고 수정 범위가
  너무 크다. 정렬 한 줄로 되는 일에 쓸 방법이 아니다.

### 남는 구멍

**실시간현황**은 "필터 없이 날 것" 원칙이라 중복 제거를 안 해 쌍둥이가 다 보인다. 거기서
대표가 아닌 쪽을 숨기면 확정본엔 여전히 대표 쪽이 뜰 수 있다. 이번 사고는 그 경로가
아니었고(초안 일괄 숨김), 그 화면에서 사진기사를 낱개로 숨기는 일이 실제로 잦아진 뒤에
다루기로 했다 — 지금 같이 고치면 그 화면의 원칙과 부딪힌다.

## 재시작이 초안 전체 재분류를 다시 열던 문제 (2026-09-03)

담당자 제보: **"초안에서 분명히 「AI 기사 배정」 미분류만 신청했는데 전체 다 진행이 됐어."**

### 그날의 클릭 자체는 정상이었다

되돌리기 스택(`data/undo_stack.json`)에 그 순간이 통째로 남아 있어 사후 재구성이 됐다 —
09:19:16 `AI 소제목 분류` 스냅샷과 그 다음 스냅샷을 비교하니 `group_overrides`가 731 → 732,
바뀐 건 **딱 1건**(`003/0014165848` → `확장 재정 정책 논의`)이었다. 그날 API 호출도
09:08:11(자동 분류)·09:19:16(배정) 둘뿐. `_handle_assign_unclassified`는 설계대로
증분으로만 돌았다.

### 진짜 원인 — 회차당 1회 기회가 메모리에만 있었다

`app/preview_renderer.py`의 `_auto_classified_slots`가 **모듈 전역 set**이었다. 도입 당시
주석에 이렇게 적혀 있었다: *"메모리에만 두므로 앱을 재시작하면 회차당 한 번 더 자동
분류될 수 있다(무해)."* 무해하지 않았다.

앱을 껐다 켜면 같은 회차인데 기회가 다시 열리고, 초안을 여는 순간 `allow_llm_call=True`로
들어간다. 캐시가 정확히 안 맞으면(그 사이 기사가 늘었으면 거의 항상) `classify_with_llm`은
`_find_partial` 경로가 아니라 **처음부터 새로 분류**하는 경로를 탄다 — `_find_partial`은
`allow_call=False`일 때만 보는 길이기 때문이다. 결과로 소제목 이름·구성·순서가 통째로
새로 지어지고, 이름을 키로 저장하는 이름표(`group_labels`)·순서(`group_order`)·배정
(`group_overrides`)이 전부 무효가 된다.

그리고 **「AI 기사 배정」은 끝나고 `location.reload()`를 한다.** 재시작 직후 초안을 아직
한 번도 안 그린 상태에서 그 버튼을 누르면, 그 reload가 곧 전체 재분류다 — 담당자 눈에는
"미분류만 눌렀는데 전체가 다시 분류됐다"로 보인다.

### 실측 — 하루에 몇 번이나 일어났나

- 2026-09-02 앱 시작 로그 **7회**. 10:57:31 재시작 → 10:57:44 API 호출, 16:47:20 재시작 →
  16:47:43 호출(둘 다 20초 안쪽 = 재시작 후 초안을 연 것).
- 그 흔적이 `data/llm_classification_cache.json`에 그대로 남아 있다 — 11:00 회차가
  **이름이 서로 다른 두 벌**(13건/24건)로, 14:00 회차는 **세 벌**(78/76/35건)로 저장돼
  있었다. 회차 하나에 분류가 여러 벌 있다는 건 그 회차에서 전체 재분류가 그만큼 돌았다는 뜻.

### 고친 방법

소진 기록을 파일로 옮겼다 — `app/auto_classify_turn.py`, `data/auto_classify_turns.json`,
`{date, slots}` 자정 초기화(`custom_groups.json`과 같은 패턴). 파일을 못 쓰는 상황에서는
**기회를 소진한 것으로 치고 False**를 돌려준다: 틀리는 방향이 "자동 분류를 한 번 걸렀다"
(담당자가 버튼으로 만회 가능)여야지 "매번 전체 재분류"(되돌릴 수 없는 손실)이면 안 된다.

고친 직후, 이미 자동 분류가 끝난 그날의 두 회차(09:30·11:00)를 파일에 미리 적어뒀다 —
안 그러면 이 수정을 적용하려고 앱을 재시작하는 그 순간 바로 그 버그가 마지막으로 한 번 더
터진다.

### 기각한 대안

- **배정 버튼이 reload 대신 부분 갱신** — 증상이 나타나는 창구 하나만 막을 뿐, 재시작 후
  그냥 초안을 여는 경로가 그대로 남는다.
- **초안에서 자동 분류를 아예 없애기** — 회차 초반에 담당자가 매번 버튼을 눌러야 한다.
  없애야 할 건 자동 분류가 아니라 "재시작하면 되살아나는" 부분이었다.

## 새로 만든 「기타」에 기사가 이미 들어와 있던 문제 (2026-09-03)

같은 날 이어진 제보: **"방금 또 <기타> 제목 새로 만들었더니 다시 들어와있어."**

### 그날 실제로 벌어진 순서

1. 09:08 AI 분류 결과에 `기타`(4건)가 이미 있었다(모델은 어디에도 안 맞는 기사를 늘 이
   이름에 담는다 — AI_RULES.md).
2. 09:16 담당자가 학자금·전기료 기사 2건을 그 칸으로 옮겼다 → `group_overrides`에
   `{url: "기타"}` 2건 기록.
3. 그 칸의 원래 기사 4건을 포함해 47건을 숨겼다 → **`기타` 칸이 화면에서 사라졌다**
   (배정 기록은 그대로 남아 있다).
4. 09:20쯤 `기타`를 새 소제목으로 만들었다 → 2번의 기록이 즉시 되살아나 그 2건이 복귀.

### 원인 — 배정 기록의 키가 "이름"이다

`group_overrides.json`은 `{url: 소제목 이름}`이고 30일 보관이다. `_apply_forced_groups`는
"그 이름의 소제목이 지금 존재하면" 무조건 다시 끌어온다(없으면 조용히 무시하는 자기 치유
동작 — 그래서 3번에서 아무 일도 안 일어난 것처럼 보였다). 즉 **같은 이름의 소제목을 다시
만드는 순간 과거의 모든 이동이 한꺼번에 복원된다.** `기타`는 AI가 catch-all로 늘 쓰는 데다
담당자도 가장 자주 만드는 이름이라 매번 여기 걸린다(저장된 배정 기록 732건 중 `기타`가
20건으로 최다).

### 결정 — "새로 만들면 항상 빈 칸" (담당자 선택)

`_handle_add_custom_group`이 `clear_group_overrides_for(name)`으로 그 이름을 가리키던
배정 기록을 먼저 끊는다. 세 안 중에서 고른 것:

- **(선택) 만들 때 끊기** — "새로 만들었다 = 백지에서 시작한다"는 기대와 일치하고, 확인창이
  늘지 않는다.
- 만들 때 물어보기(확인창) — 원칙적으로 가장 정확하지만 소제목 만들 때마다 확인창이 하나 더 는다.
- 지울 때 정리 — 이번 케이스는 담당자가 지운 게 아니라 **기사를 숨겨서 칸이 사라진** 경우라
  애초에 안 걸린다.

**조용한 삭제가 되지 않게** 실제로 지운 게 있을 때만 `undo_push("소제목 만들기")`를 먼저
쌓는다(순서가 중요하다 — `undo_push`는 "동작 전 상태"를 찍는다). `custom_groups.json`도
스냅샷 대상이라 ↩ 한 번이면 소제목 생성과 배정 기록이 함께 되돌아간다. 지울 게 없으면 안
쌓는다 — 원래 잃을 게 없는 동작이라 ↩ 스택만 의미 없이 밀린다.

지우는 범위는 **그 이름 전체**(날짜·회차를 안 가림)로 뒀다. 다른 날 URL은 오늘 목록에
없어 어차피 영향이 없고, 범위를 좁히면 "어떤 건 딸려오고 어떤 건 안 딸려오는" 설명
불가능한 상태가 된다.

2026-09-01에 정한 **"겹친 이름을 갈라놓지 않는다"(`기타2` 같은 접미사 금지)는 그대로다** —
그건 보고서 텍스트에 기계가 지어낸 구분자를 섞지 않겠다는 규칙이고, 이번에 바꾼 건
"과거 배정까지 물려받는가" 하나뿐이다.

## 자동 실행이 처음부터 한 번도 안 돌던 문제 (2026-09-04)

2026-09-03에 붙인 macOS 자동 실행(LaunchAgent)이 **도입 이후 단 한 번도 성공한 적이
없었다.** 위 초안 버그를 고치고 앱을 재시작하려다, 죽인 앱이 자동으로 안 살아나서 발견했다.

```
launchctl print … → last exit code = 2
launchd.err.log   → can't open file '/Users/marie/Desktop/my_app/main.py':
                    [Errno 1] Operation not permitted   (같은 줄 30회 이상)
```

**원인은 macOS TCC였다.** `~/Desktop`은 보호 폴더(`drwx------+`)라, launchd가 띄운
백그라운드 프로세스는 그 안을 아예 못 읽는다 — 그것도 프롬프트 없이 **조용히**. 파일
권한은 `-rw-r--r-- marie staff`로 멀쩡했으니 `chmod`로는 영영 못 푼다.

추측하지 않고 재봤다(테스트 LaunchAgent를 띄워 두 경로를 각각 읽게 함):

| 읽으려 한 경로 | 결과 |
|---|---|
| `~/tcctest/probe.txt` (홈 직속) | `HOME-OK` — 읽힘 |
| `~/Desktop/my_app/main.py` | `Operation not permitted` |

**진짜 문제는 그게 아니라, 실패가 어디에도 안 드러났다는 것이다.** `install.sh`는
`launchctl bootstrap`을 부른 직후 곧바로 `✅ 자동 실행 등록 완료`를 찍고 끝냈다 —
**"등록했다"를 "돈다"로 친 것**이고, 그 사이 에이전트는 30초마다 죽고 있었다. 초안
버그와 같은 종류다(방어를 넣고 실제로 도는지 확인하지 않았다).

**고친 것 둘.**

1. **`install.sh`가 이제 실제 실행까지 확인한다.** (a) 설치 전 — 프로젝트가
   데스크탑·서류·다운로드 안이면 미리 경고한다. (b) 등록 후 6초 기다렸다가, 그 사이
   새로 쌓인 `launchd.err.log`에 `Operation not permitted`가 있으면 원인(보호 폴더)과
   고치는 법 두 가지를 문장으로 알려주고 **종료 코드 1로 죽는다**. `launchctl list`의
   마지막 종료 코드가 0이 아니어도 실패로 본다. 종료 코드 0 + PID 없음은 정상으로
   친다 — 앱이 이미 손으로 켜져 있어 중복 실행 자물쇠에 걸려 얌전히 빠진 경우다.
   고친 직후 옛 위치에서 돌려 **실제로 실패를 잡는 것까지 확인했다.**
2. **앱을 `~/press-monitor`로 옮겼다**(담당자 결정 — 권한을 주는 안 대신). 홈 직속은
   보호 대상이 아니라 권한 설정 자체가 필요 없고, macOS 업데이트로 풀릴 일도 없다.
   데스크탑에는 같은 이름의 심볼릭 링크를 남겨 평소 쓰던 방식은 그대로다.
   `app/config.py`의 `BASE_DIR`가 `Path(__file__).resolve()`라 링크로 들어와도 실제
   경로 한 곳으로 모인다 — data 폴더가 둘로 갈릴 위험이 없다(확인함).

**검증.**

```
launchctl list           → 50626   0   local.press-monitor   (실행 중)
프로세스                  → /Users/marie/press-monitor/main.py --no-browser
포트 8765                 → 200
kill -9 후 자동 부활       → 1초
launchd.err.log          → 오류 0건
화면 4개(초안·확정본·실시간·홈) → 전부 200
통합 테스트               → 14개 전부 통과
```

**남는 한계는 그대로다.** 컴퓨터가 잠들어 있거나 꺼져 있는 동안은 여전히 못 모은다 —
`sudo pmset -c sleep 0`은 관리자 암호가 필요해 담당자 몫이다.

**옮길 때 주의**: plist에 절대경로가 박히므로, 폴더를 옮기면 새 위치에서
`bash autostart/install.sh`를 다시 돌려야 한다.

## 초안이 확정본보다 좁은 창을 보던 문제 (2026-09-04)

바로 아래 2026-09-03 항목의 **덜 끝난 절반**이다. 그때 `collect_run`의 하한만
`COLLECT_LOOKBACK_MIN`(60분)만큼 앞으로 물리고 초안(`_load_preview_search_base`)은
회차 시작을 바닥으로 그대로 뒀다 — 그래서 확정본은 앞 회차 기사를 주워 담는데
**초안은 그 기사를 끝까지 못 본다**.

담당자 제보(09:53): 실시간현황에는 09:27 뉴시스 「발전5사 합치고, LH 쪼개고…」가
버젓이 있는데 초안에도 확정본에도 없다. "이러면 앱의 존재 의미가 없다"까지 나왔고,
"차라리 설계를 바꿔 수동으로 넘기게 하자"는 말도 함께 나왔다.

**추적한 사실.** 그 기사는 09:27 발행이라 09:30 회차 창(06:00~09:30] 안인데,
09:30:07 수집 시점에 네이버가 아직 색인하지 않아 09:30 확정본에 안 들어갔다(같은 회차
로그에 `앞 회차 기사 회수 — 2건`이 남아 있다 — 이 방어 자체는 돌고 있었다). 그다음
11:00 회차는 하한이 08:30이라 **회수 대상이 맞다**. 즉 기사가 사라진 건 아니었다.
문제는 그 사이 담당자가 보는 화면인 초안이 09:30부터만 검색해서, 회수될 기사 7건이
통째로 안 보였다는 것.

측정(2026-09-04 09:54, 정기 스크랩 그룹 기준, 오늘 저장된 URL 제외 후 같은 필터를 태움):

| 창 | 건수 |
|---|---|
| 초안이 보던 창 (09:30~) | 11건 |
| 11:00 확정본이 볼 창 (08:30~) | 17건 |
| **초안엔 없고 확정본엔 들어올 기사** | **7건** (09:27 뉴시스 · 09:27/09:23 연합뉴스 · 09:11 뉴스1 · 09:09 뉴시스 2건 · 08:42 뉴시스) |

**고친 것.** 초안 바닥을 `slot_start − COLLECT_LOOKBACK_MIN`(당일 0시 하한)으로 맞춰
확정본과 같은 창을 보게 했다. 딸려 붙은 두 가지:

- **이미 나간 기사 제외를 초안에도 넣었다** — `collect_run`이 쓰던
  `_already_published_urls`를 **그대로 import**해서 쓴다(복제하지 않는다. 이 사고
  자체가 "같은 규칙이 두 곳에 갈려 있어서" 난 것이다). 자리도 collect_run과 같은
  자리 — 화이트리스트·정렬·중복제거보다 **먼저**. 나중에 빼면 이미 보고서에 나간
  기사가 `deduplicate_by_title`의 대표 자리를 차지해 새 기사를 밀어낸다.
- **`⏱ 앞 회차 기사` 칩이 초안에도 붙는다** — 안 붙이면 "왜 09시 기사가 11시 초안에
  있지?"라는 새 의문이 생긴다. `late_pickup`/`late_pickup_slot`은 확정본과 같은
  필드라 `render_article`이 알아서 그린다. 칩 CSS만 확정본 템플릿 안에 박혀 있어서
  `late_badge_style()`로 빼내 두 화면이 같은 값을 받게 했다(`.photo-badge`처럼 양쪽에
  복붙해 두면 한쪽만 고쳤을 때 조용히 어긋난다 — 이 사고의 축소판이다).

**캐시 서명에 `lookback_min`을 넣었다.** 서명이 안 바뀌면 더 좁은 창에서 모은 옛
캐시를 그대로 이어 써서, 고친 그 회차만 여전히 기사가 빈다. 값을 서명에 담아두면
규칙을 바꾸는 순간 캐시가 저절로 무효화돼 회차 시작−60분부터 한 번 다시 훑는다.

**남는 한계(감수).** 초안은 캐시 기반이라 두 번째 계산부터는 하한이 "지금−60분"으로
따라 올라간다(`incremental_search_after`). 그래서 발행 후 60분이 넘어서야 색인되는
기사는 초안이 못 보고 확정본만 회수한다 — 실측 색인 지연이 3~18분이라 실제로는 거의
안 걸리고, 회차 첫 계산은 항상 회차 시작−60분부터 전부 훑는다.

**설계는 안 바꿨다.** "수동으로 넘기는 식"은 필요 없었다 — 두 화면이 같은 창을 보게
하면 끝나는, 규칙이 한쪽에만 적용돼 있던 문제였다.

## 회차 경계에서 기사가 영영 사라지던 문제 (2026-09-03)

**발단은 오탐이었다.** 담당자가 네이버 뉴스에서 눈으로 찾은 기사(뉴스1
`재경부-WB, AI 주제로 KWPF 성과 공유 행사 개최`, 10:41 발행)가 앱 화면에 없다고 제보했다.
파보니 **그 기사는 11:00 회차에 정상 수록됐다** — 10:49:39의 실시간 캐시 갱신 시점에
네이버 *검색 결과*에 아직 없었고 10:52경 색인돼, 그 사이에 화면을 본 것뿐이었다.
네이버 뉴스 *화면*과 네이버가 앱에 주는 *검색 결과*가 서로 다른 통로라는 게 이날 내내
반복해서 확인된 사실이고, 나중에 배너 문구를 정할 때 결정적 근거가 된다.

**조사 중 화이트리스트를 또 놓쳤다.** 설정을 `settings["selected_outlets"]`로 읽어
"화이트리스트 미사용"으로 오판했다 — **실제 키는 `outlet_order`(43개 매체)다.**
그 상태로 잰 숫자가 "11:00 회차 38건인데 43건이 누락", "어제 회차는 96건 중 118건 누락"
같은 값이었고, 거기서 "네이버 검색 결과 집합이 시시각각 흔들린다 → 정기 수집도 실시간처럼
누적해야 한다"는 큰 처방까지 갔다. **전부 오판이었다.**
이 실수는 [2026-08-27 "정기가 기사를 놓친다"는 의심](#정기가-기사를-놓친다는-의심--원인은-언론사-화이트리스트였다-2026-08-27)에
**같은 키 이름으로 이미 기록돼 있었다** — 문서가 있었는데 안 읽었다. CODING_CONVENTIONS.md §1
("추측하지 말고 잰다")은 "설정 키를 짐작하지 않는다"까지 포함한다. 담당자가
*"어차피 선택한 언론사만 끌어오잖아. 대전일보 신아일보는 어차피 안 끌어올걸"* 이라고
짚어줘서 바로잡혔다.

**바로잡은 실측(2026-09-03, 화이트리스트 43개 적용).**

| 창 | 회차 저장 | 지금 재검색(선택 언론사·제목중복 적용) | 진짜 누락 |
|---|---|---|---|
| 06:00~09:30 | 62건 | 62건 | **0건** |
| 09:30~11:00 | 38건 | 44건 | **6건** |

그 6건은 **전부 마감 3분 이내 발행**이었다(10:57 파이낸셜뉴스 · 10:58 연합뉴스·뉴스1·데일리안 ·
11:00 아시아경제·매일신문). 즉 문제는 "대량 누수"가 아니라 **회차 경계 하나**였다.

**색인 지연 곡선(같은 창을 시간 두고 반복 검색).** 11:00 회차가 11:00:08에 수집된 직후를
자연실험으로 썼다.

| 시각 | 그 6건 |
|---|---|
| 11:00:08 (수집) | 없음 |
| 11:03 (3분 뒤) | **여전히 없음** |
| 11:18 (18분 뒤) | **6건 전부 등장** |

11:18에 새로 잡힌 8건의 oid를 풀어보니 정확히 그 6곳(014 파이낸셜뉴스 · 119 데일리안 ·
001 연합뉴스 · 421 뉴스1 · 277 아시아경제 · 088 매일신문)이었다. **색인 지연 3~18분.**

**구조적 원인(코드 확정).** 회차 창은 `(시작, 마감]`이고 슬롯이 서로 붙어 있어 다음 회차의
하한이 **정확히 이 회차의 마감**이다. `app/naver_api.py` `_search_one_keyword`가
`pub_date <= after`에서 페이지네이션을 끊으므로, 마감 순간 검색에 없던 기사는 다음 회차에서
**구조적으로 배제된다.** 그 뒤 어떤 회차도 그 구간을 다시 안 본다 → **정기 보고서에서 영구 누락.**
(실시간현황엔 뜬다 — 그 화면은 바닥이 당일 0시이고 `SEARCH_LOOKBACK_MIN`=60분 되돌아보기가
있다. 2026-08-21에 초안·실시간현황에는 이 방어를 넣었는데 **정기 수집에는 안 넣었다**는 게
이번 사고의 본질이다.)

**기각한 안 ① "실시간 캐시를 회차 수집에 합치기".** 화이트리스트를 놓쳤을 때 나온 처방이다.
바로잡고 재보니 10:49:39 시점 실시간 캐시의 그 창 기사는 원시 68건인데 **43개 매체로 거르면
38건 = 회차 저장 38건과 정확히 같다.** 끌어올 소스가 애초에 없었다(담당자 지적:
*"정기보고서에도 끌어올 소스가 없다는거지"*). 회차 전용 누적 폴링도 같은 이유로 폐기.

**기각한 안 ② "확정본을 띄운 뒤 20분간 그 회차를 계속 보충".** 뒤늦게 색인된 기사를
**원래 자기 회차**에 넣을 수 있어 발행시각과 회차가 안 어긋난다는 장점이 있었지만,
**"마감된 회차는 고정된 스냅샷"이라는 전제를 깬다** — 정기 보관함 읽기 전용·발송 기록·
소제목 스냅샷 분류·되돌리기 스택이 전부 그 위에 서 있다. 게다가 담당자가 정각에 바로
발송하는 방식이라 **"발송 후 N건 추가됨 → 재발송할까요?"가 거의 매 회차 뜨게 된다.**
얻는 건 "3시간 일찍"뿐이라 값이 안 맞았다.

**채택: 하한을 물리고 다음 회차가 줍는다 (담당자 제안).** 대가는 *"14시 보고서에 10:57
기사가 섞인다"*는 어색함 하나인데, **보고서 텍스트에는 발행시각이 원래 안 나간다**
(`ㅇ (언론사) 제목` + URL). 그래서 **어색함은 담당자 화면에만 있고 받는 사람에겐 0이다.**
이걸 알아채고 나서 이 안이 확정됐다.

- `COLLECT_LOOKBACK_MIN`=60 — 실측 지연 3~18분의 서너 배 여유이고, 초안·실시간현황이 이미
  쓰는 `SEARCH_LOOKBACK_MIN`과 **같은 값**이라 앱 안에 "되돌아보는 폭"이 하나로 유지된다.
- **넉넉히 잡는 게 항상 안전한 방향이다** — 오늘 앞 회차에 이미 실린 URL을 `collect_run`이
  제외하므로 중복 게재가 원리적으로 0이기 때문. 그래서 이 값에 튜닝 노브의 성격이 없다.
- **제외는 URL로만 한다.** 제목까지 대조하면 제목만 우연히 같은 별개 기사가 조용히 사라진다 —
  2026-08-27에 숨김 판정에서 제목 대조를 통째로 되돌린 것과 같은 이유(실측: 고유 제목 2,515건
  중 제목 중복 38건의 절반 이상이 별개 기사). 대가로 통신사 쌍둥이가 한 번 더 실릴 수 있는데
  그건 **보이는** 실패라 🗑 한 번이면 끝난다.
- `LAST_SLOT_COLLECT_DELAY_MIN`=30 — 하루 마지막 회차만 **수집 실행 시각**을 늦춘다(창·이름은
  그대로). 마지막 회차는 주워갈 다음 회차가 없고, 다음날 첫 회차는 전날 기사를 안 본다.
  **다음날로 넘기는 안은 기각했다** — 숨김 기록이 자정에 초기화되므로 어제 담당자가 뺀 기사가
  오늘 첫 보고서에 되살아난다(실효성도 낮다: 19:25 기사가 다음날 06:00 보고서면 10시간 뒤).
  실증: 2026-09-02 19:30 회차가 앱 재시작 때문에 20:01에 수집됐는데 아무 문제 없었다.

**화면 문구 — 네이버에 대한 주장을 한 줄도 안 쓴다 (담당자 지적).**
처음 시안은 `📥 네이버 검색에 늦게 올라온 앞 회차 기사 3건`이었는데, 담당자가
*"'네이버 검색에 늦게'라고 하면 '인터넷에서 직접 찾아봤는데 그건 있던데?'라고 대응할 수
있잖아"* 라고 짚었다. **바로 그날 아침에 실제로 일어난 일**이다. 그래서 배너 본문은
**우리가 한 일만 서술**하고(`11시 회차 기사가 뒤늦게 수집돼 함께 담겼어요`), 이유는 전부
ⓘ 툴팁으로 내렸다 — 툴팁 둘째 줄이 그 반박을 선제로 답한다(`네이버 뉴스 화면에는 이미
보이더라도 검색 결과에는 아직 없을 수 있습니다`).

**색·자리 — 목업으로 정했다** ([LATE_PICKUP_MOCKUP.html](mockups/LATE_PICKUP_MOCKUP.html)).
앰버/중립 회색/파랑 3안을 실제 목록에 섞어 비교했다. 회색은 `📷 사진 추정` 칩과 색이 같아
구분이 안 됐고, 파랑은 "누르는 것"·"확정본 상태"라는 다른 뜻을 이미 갖고 있었다. **앰버**는
회차 마감 배너가 쓰는 *"지금 보고 있는 게 네가 생각하는 그거랑 조금 다르다"* 와 뜻이 정확히
같아서 골랐다. 새 색은 하나도 안 만들었다(전부 `COLOR_WARN_*`).

**왼쪽 띠는 안 준다.** 만들다 보니 앱에 이미 같은 모양이 있었다 — 「마감 후 자동 배정」의
연보라 띠 + 🤖 배지. 그런데 **회수된 기사는 그 회차에서 처음 보는 기사라 거의 항상 그
자동 배정 대상이기도 하다.** `border-left`는 하나뿐이라 띠끼리는 반드시 하나가 진다.
그래서 회수 표시는 **메타 줄의 칩 하나**로만 두고 띠는 연보라에 양보했다 — 띠는 "훑을 때
덩어리로 보이는" 자리라 하나만 써야 하고, *"AI가 소제목을 대신 정했다"*가 *"언제
수집됐다"*보다 담당자가 손볼 일이 크다. 담당자 판단도 같았다(*"노랑은 그냥 들어간거고
연보라는 ai 니까 노랑칩처럼 보이는게 맞아"*). 자리가 갈려 있어 **둘이 겹쳐도 서로를 안 지운다.**

**⏱ 칩이 장식이 아닌 이유.** `🤖 마감 후 자동 배정` ⊃ `⏱ 앞 회차 기사`다 — 마감 직전에
정상 수집된 새 기사도 🤖가 붙으므로, 🤖 붙은 기사 중 **일부만** ⏱이다. 행마다 값이 변하니
정보가 맞다(`/keywords`에서 "실시간" 배지를 뺐던 그 기준을 통과).

**배너 문구에서 건수를 문장 밖으로 뺐다.** 처음엔 `[2건]이 뒤늦게…`로 문장 한가운데
버튼을 뒀는데, 렌더링해보니 조사가 버튼 경계로 끊겨 **"2건 이"** 로 벌어져 보였다.
초안의 `[미분류 N건]` 배지처럼 문장 끝 독립 버튼(`[2건 보기]`)으로 옮겼다.

**진짜 교훈은 "샜다"가 아니라 "1년 가까이 새는 줄 몰랐다"는 것이다.** 회차가 38건인지
44건인지 판단할 근거가 화면에도 로그에도 없었다. 그래서 `collect_run`이 회수 건수를
INFO 로그로 남긴다 — 다음에 같은 일이 생기면 하루 만에 안다.

## 통합 테스트가 실제 회차 파일을 덮어쓴 사고 (2026-09-04)

**AI(작업자)가 낸 사고다.** 깨진 테스트를 진단하려고 테스트 함수를 하네스 밖에서 직접
불렀다:

```
python3 -c "import tests.test_integration as t; t.test_late_pickup_recovers_previous_slot_articles()"
```

`tests/test_integration.py`의 `main()`은 테스트마다 임시 폴더를 `patch.object`로 갈아끼우지만,
**그 하네스를 건너뛰면 patch가 하나도 안 걸린다.** 그 테스트는 `storage.save_run("11:00", …,
run_at="2026-09-03T11:00:08")`과 `scraper.collect_run("14:00", …, run_at="2026-09-03T14:00:05")`을
호출하므로, 실제 `data/articles/`의 그 두 회차 파일을 그대로 덮어썼다. 당시 테스트가 날짜를
하드코딩하고 있어서(같은 날 고친 그 버그) 하필 **어제 회차 파일명과 정확히 겹쳤다.**

- `2026-09-03_14-00.json` (147건) → 0건으로 덮어써짐
- `2026-09-03_11-00.json` (38건) → 가짜 기사 1건으로 덮어써짐

**복구.** `data/undo_stack.json`이 살렸다 — `app/undo.py`가 되돌리기 단계마다 **최신 회차
파일 원본을 통째로** 스냅샷하는데(2026-08-24에 추가된 그 규칙), 20단계 중 18개가
`2026-09-03_14-00.json`의 147건짜리 원본이었다. 가장 최근 스냅샷(17:00:15, 마지막 숨기기
직전)을 그대로 되돌려 147건·소제목 구성까지 복원했다. **11:00은 복구하지 못했다** — 그때는
14:00이 이미 "최신 회차"라 11:00 스냅샷이 스택에서 밀려나 있었다.

**교훈 둘.**

1. **"실제 data/를 안 건드린다"는 약속이 하네스에만 있었고 어디에도 강제돼 있지 않았다.**
   이제 `_require_isolation()`을 테스트 13개 전부의 첫 줄에서 부른다 —
   `storage.ARTICLES_DIR`가 진짜 경로면 즉시 예외. 약속을 주석이 아니라 코드가 지킨다.
2. **날짜를 하드코딩한 테스트는 사고를 키운다.** 같은 날 고친 "하루짜리 테스트" 문제와
   같은 뿌리다 — 하드코딩된 날짜가 실제 운영 파일명과 겹치는 순간, 격리 실패가
   "임시 파일 몇 개 생김"이 아니라 "운영 데이터 파괴"가 된다.

되돌리기 스냅샷이 없었으면 147건은 그대로 사라졌다. `app/undo.py`가 회차 파일까지
스냅샷하기로 한 2026-08-24의 결정이 전혀 다른 종류의 사고에서 데이터를 구했다.

## 회차가 통째로 비는데 아무도 몰랐던 문제 — 자동 실행 + 「수집 기록 없음」 (2026-09-03)

담당자가 "결정해야 할 것들을 알려달라"고 해서 로그·저장 파일을 훑다가 나온 것.
**예정 78회차 중 14회(18%)가 비어 있었고, 화면 어디에도 그 사실이 없었다.**

```
8/22(토) 1회   8/23(일) 1회
8/28(금) 4회 (11:00·14:00·17:00·19:30)
8/29(토) 3회 전부   8/30(일) 3회 전부
8/31(월) 2회 (06:00·09:30)
```

원인은 단순했다 — **앱을 사람이 손으로 켜야 한다.** `python3 main.py`뿐이고 자동 실행
설정이 없었다(`~/Library/LaunchAgents`에 아무것도 없음). 금요일 오후에 컴퓨터를 끄면
월요일까지 10회가 그대로 빈다. 8/28~8/30이 정확히 그 케이스다.

### 왜 "나중에 채우기"가 답이 될 수 없었나 — 실측

먼저 소급 수집(backfill)을 검토했다. 네이버 뉴스 검색 API에는 **기간 지정 파라미터가
없고** 키워드당 최신 1000건까지만 준다(`_MAX_START`). 그래서 "며칠 전까지 거슬러 갈 수
있는가"는 키워드마다 다르고, 정기 스크랩 키워드 8개를 실제로 재봤다(키워드당 1호출,
`start=991`의 `pubDate`를 읽음, 2026-09-03):

```
재정경제부        1일 전  ← 병목       재경부              4일 전
재정경제기획위원회  4일 전              구윤철              7일 전
기획재정부         9일 전              기재부             86일 전
이형일            4일 전              재경위            135일 전
```

**가장 좁은 것이 전체를 결정하므로 실질 소급 한계는 하루다.** 8/28~8/30 10회차는 이 시점에
이미 영구 손실이 확정돼 있었다 — 고칠 수 있는 건 "앞으로"뿐이라는 게 이 실측의 결론이고,
그래서 backfill 기능은 설계조차 하지 않았다.

덤으로 나온 수치 하나: `재정경제부`가 하루 약 800건이다. 1000건 상한까지 여유가 20%밖에
없어, 기사가 조금만 늘면 **실시간현황 첫 로딩(당일 0시~현재)이 조용히 잘린다**. 회차
창(2~3시간)은 아직 안전하다 — CLAUDE.md "네이버 조회 상한(1000건) 잘림"이 남겨둔 "로그를
며칠 모아 판단한다"는 유예의 답이 정기는 안전 / 실시간은 곧 위험이라는 뜻이다.

### 결정 1 — 주말도 모은다, 그래서 자동 실행을 붙인다 (사용자 결정)

누락 14회 중 8회가 주말이라, **주말 스케줄 그룹을 끄면 코드 한 줄 없이 누락률이 18% →
8%로 떨어진다**는 선택지를 같이 올렸다. 담당자는 "주말도 필요하다"를 골랐고, 그러면
자동 실행이 선택이 아니라 필수가 된다.

`autostart/`(install.sh · uninstall.sh · README.md)를 만들어 macOS LaunchAgent로
등록한다. **설치 스크립트는 만들어만 두고 실행은 담당자가 직접 한다** — 로그인할 때의
동작을 바꾸는 지속 설정이라 앱이 알아서 걸 일이 아니다.

- `KeepAlive`를 `{SuccessfulExit: false}`로 둔다. 그냥 `true`로 두면 **중복 실행 자물쇠에
  걸려 정상 종료(0)한 경우까지 되살려 무한 재시작**이 된다(main.py의 EADDRINUSE 경로).
- `main.py`에 `--no-browser`를 새로 뒀다. launchd는 앱이 죽으면 다시 살리는데 그때마다
  브라우저 창이 새로 뜨면 담당자가 쓰던 화면을 밀어낸다. **자동 실행은 "회차를 안 놓치는
  것"만 하고, 화면을 여는 건 담당자가 앱을 직접 실행했을 때만 한다** — 그때는 포트
  자물쇠에 걸려 곧바로 `_open_home_in_browser()`로 빠지므로, 손으로 켜는 행동이 그대로
  "화면 열기"가 된다(우연히 얻은 게 아니라 2026-09-02 자물쇠 설계가 이미 만들어둔 길).
- **이걸로 안 풀리는 것도 README에 그대로 적었다** — 컴퓨터가 잠들어 있거나 꺼져 있는
  동안은 여전히 못 모은다. `sudo pmset -c sleep 0`은 관리자 암호가 필요해 담당자가 직접
  실행해야 하고, 노트북 뚜껑을 닫으면 그 설정과 무관하게 잠든다. 자동 실행이 푸는 건
  "앱을 켜는" 문제뿐이라는 걸 흐리지 않는다.

### 결정 2 — 정기 보관함에 「수집 기록 없음」 줄 (사용자 결정)

18%가 한 달 넘게 방치된 진짜 이유는 **빈 걸 알 방법이 없어서**였다. 회차가 없으면 줄이
안 그려지고, 하루가 통째로 비면 날짜 자체가 목록에서 사라진다(`list_run_dates()`가
"회차가 있는 날짜"만 돌려주므로) — 화면에서 8/28 다음이 곧바로 8/31이라 이틀이 사라진 걸
알 수가 없었다.

- `expected_slots_for_date(run_date)` — 그 날짜 요일의 예정 회차를 지금 설정에서 계산.
  `active_schedule_times(settings, now)`가 이미 `now`를 받으므로 과거 날짜의 정오를
  넘기면 그날 요일의 그룹이 그대로 나온다.
- `_render_day_slots`가 실제 회차와 빈 줄을 **시각 순으로 섞는다** — 빈 줄을 목록 끝에
  몰아두면 "몇 시가 비었나"가 안 읽히고 그냥 장식이 된다.
- 회차가 하나도 없는 날짜는 `_with_gap_dates`가 끼워 넣는다. 범위는 **저장된 회차의 가장
  오래된 날짜~가장 최신 날짜 사이로만** — 그 바깥은 앱을 쓰기 전이거나 보관 기간이 지나
  지워진 구간이라 빈 줄을 그리면 "안 돈 날"처럼 보여 거짓말이 된다. 이 날짜엔 지연
  로딩(`ontoggle`)을 안 붙인다(서버가 돌려줄 회차가 없어 영영 "펼치면 불러옵니다…"에
  머문다) 복사·txt·엑셀 버튼도 안 붙인다(눌러도 빈 파일).
- **오늘의 미래 회차는 제외한다.** 안 그러면 매일 오전 내내 "오늘은 다 비었다"로 읽힌다.
  실제로 첫 구현에서 17:47에 19:30이 「수집 기록 없음」으로 찍혀서 바로 잡았다.

**문구를 "누락"이 아니라 "수집 기록 없음"으로 한 이유**: 예정 회차는 *지금* 저장된 스케줄
기준으로 계산하는데, 그날 실제로 어떤 스케줄이 걸려 있었는지는 아무 데도 저장돼 있지
않다. 그래서 화면은 사실("이 시각에 저장된 회차가 없다")만 말하고, 그 한계는 툴팁이
그대로 밝힌다 — 발송 기록이 없는 회차를 "미발송"이라 단정하지 않는 것과 같은 규칙이다.

**구현 중 잡은 CSS 버그 하나**: `.chip-missing`을 `.chip`보다 **앞에** 뒀더니 `.chip`의
`border: 1px solid` 단축 속성이 뒤에서 `border-color`를 덮어써 테두리가 앰버가 아니라
글자색(진한 갈색)으로 나왔다. `.chip-today` 옆으로 옮겨 해결.

### 이어진 결정 — 보충을 "오늘 놓친 전부"로 (2026-09-04)

여기까지 하고 "놓친 회차를 1개만 채우는 규칙(PRD 기능1 규칙9)을 넓힐지"를 남겨뒀는데,
담당자가 **넓히기로 결정**했다("PRD 규칙도 바꾸고"). `find_slots_to_run`(복수)이 오늘
지나온 회차 중 아직 안 돈 것을 전부 돌려주고, `run_due_slot`이 **오래된 것부터** 차례로
수집한다. `find_slot_to_run`(단수)은 그 목록의 마지막 원소를 돌려주는 얇은 래퍼로 남겼다
— 판단 로직이 두 벌로 갈리지 않게.

**왜 오래된 것부터인가.** `collect_run`이 `_already_published_urls`(오늘 이미 저장된
회차들의 기사 URL)로 앞 회차에 실린 기사를 걸러낸다(2026-09-03 회수 기능). 최신부터
돌리면 그 방어가 **거꾸로 작동해 앞 회차가 자기 기사를 뺏긴다** — 순서가 곧 규칙이라
독스트링·테스트 양쪽에 못 박았다.

**자동발송은 건드리지 않았다.** `check_pending_confirm_and_send`가 `load_latest_run()`
하나만 보므로, 5회차를 한꺼번에 채워도 자동발송 후보는 마지막 회차 하나뿐이다 — 우연이
아니라 원하던 동작이라(몇 시간 지난 회차를 지금 와서 보내지 않는다) 그대로 두고 PRD에
명시만 했다. 다만 그 "마지막"이 흔들릴 여지가 있어 **`app/run_index.py`의 정렬 키에
`run_slot` 동점 처리를 넣었다** — `run_at`이 초 단위라 회차를 연달아 저장하면 두 회차가
같은 초에 걸릴 수 있고, 그러면 `sorted(reverse=True)`가 안정 정렬이라 "가장 최근 회차"가
glob 순서에 따라 갈린다(정렬이 결정론적이어야 한다는 2026-09-02 사고와 같은 자리).

**실패 격리**: 회차 하나가 실패해도 나머지는 계속 간다. 없으면 06:00 회차의 일시적
네트워크 오류 하나가 그날 되살릴 수 있는 나머지 4회차까지 통째로 막는다.

**자기 코드 리뷰에서 잡은 회귀 하나 — 재시도 상한.** 예전 규칙엔 없어도 됐던 제동이
이번 변경으로 사라진다는 걸 구현 뒤 리뷰에서 발견했다: 실패한 회차는 파일이 없어
`run_exists`가 False로 남는데, 예전엔 다음 회차가 도착하는 순간 그 회차가 "가장 최근"이
아니게 되어 **후보에서 저절로 빠졌다.** 보충을 "오늘 전부"로 넓히면 그 회차가 하루 종일
후보로 남아, 매 tick(10초)마다 `collect_run_with_retry`(3회 × 5분)를 다시 돈다 — tick
하나가 10분씩 잡아먹는다. 그래서 `_MAX_SLOT_ATTEMPTS`(=`MAX_RETRIES`, 3)를 두고
`_slot_failures`에 (날짜, 회차)별 실패 횟수를 센다. 안쪽 재시도와 곱해 회차당 최대
9번·30분이라 일시적 장애엔 충분하고, 영영 못 받는 회차에 하루를 쓰지도 않는다.
디스크에 안 남기는 이유: **담당자가 앱을 다시 켰다는 건 "이제 되겠지"라는 뜻**이고,
그 판단을 지난 실패 기록이 막으면 안 된다.

**어제 이전은 안 채운다** — 회차 시간창은 언제나 오늘로 해석되고(`kst_today_at`/
`_is_today_kst`), 위 1000건 실측대로 그 이상은 어차피 못 가져온다.

### 곁가지 — 하루짜리였던 테스트

이 작업 중 `test_late_pickup_recovers_previous_slot_articles`(2026-09-03 작성)가 깨졌는데,
원인은 코드가 아니라 **날짜가 넘어간 것**이었다. 테스트가 `2026-09-03`을 하드코딩했는데
`collect_run`의 회차 창은 `kst_today_at`이 만드는 **오늘** 시각이라, 자정을 넘기는 순간
어제 날짜 기사가 전부 "창보다 앞선 기사"가 되어 회수 표식이 잘못 붙었다. 작성 다음 날
바로 깨지는 테스트였던 셈 — `datetime.now().date()`로 바꿨다. **날짜를 하드코딩한
테스트는 통과가 아니라 유예다.**

### 남겨둔 것

자동 실행을 붙여도 **컴퓨터가 잠들어 있거나 꺼져 있는 동안은 여전히 못 모은다** —
`sudo pmset -c sleep 0`은 관리자 암호가 필요해 담당자 몫이고, 노트북 뚜껑을 닫으면 그
설정과 무관하게 잠든다. 보충이 "오늘 전부"로 넓어져 잠자기에서 깬 뒤의 회복력은 크게
올라갔지만(밤새 잠들었다 아침에 깨면 그 사이 회차가 전부 채워진다), 잠든 채로 하루가
통째로 지나가면 그날은 여전히 못 살린다.

## 수시 3단 이름이 한 칸씩 밀려 있던 문제 — 수집 원본/수집 확정본 재배치 (2026-09-03)

**담당자 지적으로 시작됐다** — "지금 내가 [원본]에서 검색했으면 결과가 [원본]에서
나와야 해. 그리고 내가 하나씩 체크했을 때 또는 소제목 자체를 [확정본]으로 보내야 해."
그리고 "아직 이 부분 우리가 하지 않은 것 같아서."

**절반만 맞았다.** 「하나씩 체크해서 보내기」·「소제목 통째로 보내기」는 2026-09-02
모음 카드(§6.13)로 이미 만들어져 있었다 — 입구 셋(기사 행 드롭다운 · 일괄이동 바 ·
소제목 헤더 📥)에 라우트 하나(`/adhoc/card/send-to-bundle`), 보낸 기사 표시, 「아직 안
보낸 기사만」 필터, 「모음에서 숨기기 = 보내기 취소」까지. **안 된 건 기능이 아니라
이름이었다.** 2026-09-02 3차 이름 재정리(`수집 시작`→`수집 원본`, `수집 확정`→`수집
확정본`)가 화면에 붙을 때 한 칸씩 밀려 붙었다:

| 화면 | 실제로 하는 일 | 붙어 있던 이름 |
|---|---|---|
| `/adhoc/new` | 검색 조건 **입력 폼** (결과 없음) | 수집 원본 |
| `/adhoc/card` (collect) | 검색 결과 날 것 목록 | 수집 확정본 |
| `/adhoc/card` (bundle) | 골라 담은 기사만 모인 보고서 | 모음 *(3단 이름 밖)* |

그래서 담당자 눈에는 "원본에서 검색했는데 결과가 확정본에서 나오는" 화면이 됐고,
정작 확정본의 자리에 있던 카드는 3단 체계에 이름이 없었다. 이름이 밀려 있으니 3단
대응표(CLAUDE.md)를 읽어도 화면과 안 맞았다.

**고친 방향: 이름을 제자리로 옮기고, 두 화면의 성격을 실제로 갈랐다.**

- collect 카드 = **수집 원본**, bundle 카드 = **수집 확정본**, `/adhoc/new` = **새 수집**
  (화면이 아니라 폼이라 3단에 자리가 없다). 내부 용어는 한 글자도 안 바꿨다 —
  `kind:"bundle"`·`send-to-bundle`·`sendToBundle`·`bundles_for_date` 전부 그대로다
  (CLAUDE.md "화면 용어 — 내부 용어와 분리한다").
- **원본은 시간순, 확정본은 언론사순**(담당자 결정). 그 전엔 둘 다 언론사순이라 이름만
  다르고 하는 일이 같았다. 원본은 "지금 뭐가 들어왔나"를 훑는 자리라 최신이 위여야 하고,
  확정본은 보고서라 언론사 우선순위가 맞다.
- **원본에서 기사 ↑↓를 뺐다.** 화면이 "시간순"이라고 말해놓고 손으로 뒤집을 수 있으면
  그 말이 거짓이 된다(정기 실시간현황에 ↑↓가 없는 것과 같은 이유). 순서를 다듬는 일은
  확정본이 맡는다. **소제목 자체의 ▲▼는 두 화면 모두 그대로다** — 그건 기사 순서가
  아니라 소제목 순서다.

**정렬을 거는 지점이 두 화면에서 다르다 — 이게 이 변경에서 가장 조심한 대목이다.**
원본은 **표시할 때**(`_article_groups`) 소제목 안을 시간순으로 다시 세운다. 확정본은
그렇게 하면 안 된다 — 매 렌더링 재정렬은 담당자가 ↑↓로 맞춰둔 순서를 조용히 지우고,
그건 CLAUDE.md "담당자 > AI: 정렬·분류 우선순위"가 금지한 바로 그 동작이다. 그래서
확정본은 **기사가 들어올 때**(`card.send_to_bundle`) 자기 자리를 잡는다: 목록 전체를
다시 세우지 않고, 같은 소제목 안에서 자기보다 순위가 낮은 첫 기사 **앞**에 끼워 넣는다.
기존 기사끼리의 상대 순서는 한 번도 안 건드린다. 정렬 키는 `sort_by_outlet_priority`가
쓰는 것과 **같은 함수**(`app.sorter.outlet_sort_key`)를 쓴다 — 정렬과 삽입이 각자 키를
만들면 한쪽만 고쳤을 때 조용히 어긋난다(2026-09-02 안정 정렬 사고와 같은 종류).

**상단바가 원본↔확정본을 오갈 수 있게 됐다.** 그 전엔 카드 화면 상단바가 `홈 · 새 수집
· 수시 보관함`뿐이라, 원본에서 확정본으로 가는 길이 수시 보관함을 거치는 것밖에
없었다(보내기만 되고 결과를 보러 갈 수가 없었다). 이제 지금 보고 있는 카드의 **반대
종류 최신 카드**로 가는 링크가 붙는다 — 그 종류의 카드가 하나도 없으면 링크 자체를
안 그린다(갈 데 없는 링크를 회색으로 남기지 않는다). `latest_card_id(kind=)`는 종류를
가려야 할 때만 파일을 여는데, 최신순으로 최대 30개까지만 본다(대개 첫 한두 개에서
걸린다) — kind를 안 주면 예전처럼 파일을 한 개도 안 연다.

**홈 흐름도의 수시 줄도 정기 줄과 같은 모양이 됐다** — `수집 원본 → 수집 확정본 → 수시
보관함`(정기의 `초안 → 확정본 → 정기 보관함`과 대칭). 그 전엔 첫 칸이 입력 폼이라
건수를 못 붙여 `&nbsp;`로 비어 있었다.

**「확정본」을 맨몸으로 쓴 자리가 셋 생겼다** — 카드 헤더 종류 칩(`확정본`), 수시
보관함 목록의 종류 칩(`확정본`), 소제목 헤더의 `+ 새 확정본으로 N건` 버튼. 셋 다
좁은 자리이고 수시 화면 안이라 정기 확정본과 헷갈릴 대상이 없어 예외로 뒀다 —
문장으로 읽히는 자리(드롭다운·버튼·안내·오류 문구)는 전부 `수집 확정본`이다.

## 쓰레기통을 7일로 — 단, 숨김은 하루 그대로 (2026-09-04)

### 요청

"쓰레기통 하루 지나면 삭제되잖아. 근데 생각해보니까 다음 날 확인하고 싶은 게 있는 거야.
혹시 쓰레기통도 한 일주일 정도 남길 수 있어?"

### 왜 "보관 기간만 7일로"가 답이 아니었나

`app/curation.py` `_load_records`가 **두 가지 일을 겸하고 있었다** — 하나는 기사를 화면·
보고서에서 실제로 빼는 필터(`load_hidden_urls` → `filter_hidden`/`is_hidden`), 다른 하나는
쓰레기통 화면 열람(`load_hidden_records`/`load_hidden_batches`). 그 함수의 날짜 조건 한 줄을
7일로 늘리면 **열람만 늘어나는 게 아니라 어제 숨긴 URL이 오늘 확정본에서도 계속 빠진다.**

그게 위험한 이유는 2026-08-27에 제목 대조를 걷어내며 이미 적어둔 그대로다 — 그 결정의
근거 문장이 "부활 위험은 숨김 기록이 자정에 초기화되므로 하루 안으로 갇힌다"였다. 그 바닥을
7일로 빼면 회수된 기사(`late_pickup`)나 재게재 URL이 걸릴 때 **담당자가 오늘 누른 적 없는
기사가 조용히 사라지고, 왜 없는지 화면 어디에도 안 남는** 실패가 열린다. 원인은 제목 대조
사고와 다르지만(그건 제목이 우연히 같은 별개 기사, 이건 어제 결정이 오늘로 새는 것) 결과
모양이 같다 — 안 보이고, 회차 파일을 직접 파봐야 안다.

### 결정 — 축을 둘로 가른다 (사용자 결정)

| | 기간 | 근거 |
|---|---|---|
| 숨김이 **작동**하는 기간 | 오늘 하루 (그대로) | 자동 판단이 담당자 대신 기사를 치우지 않는다 |
| 쓰레기통에 **보이는** 기간 | `HIDDEN_VIEW_DAYS` = 7일 | "어제·그저께 뭘 뺐더라"는 며칠 안에 끝나는 질문 |

7일인 이유: 그보다 오래 남겨야 하는 기사는 **라벨(1년 보관)**이라는 길이 이미 있다.

`_load_records(now, days=1)`의 **기본값이 1이라는 것이 안전장치다** — 숨김 판정이 인자를 안
넘기고 부르므로, 나중에 누가 인자를 빠뜨려도 틀리는 방향이 "덜 숨긴다"가 된다.

### 같이 걸린 함정 셋

1. **쓰기가 읽기만큼만 남긴다.** `hide_article`/`unhide_articles`는 `_load_records`로 읽은
   목록을 그대로 다시 쓴다 — 오늘치만 읽어 쓰면 **오늘 첫 숨기기 한 번이 어제 이전 기록을
   파일에서 통째로 지운다.** 읽는 범위를 7일로 올려 해결. 즉 파일에 남는 기간 = 여기서 읽는
   기간이고, 7일보다 오래된 기록은 다음 숨기기 때 자연히 정리된다.
2. **중복 검사가 🗑️를 먹통으로 만들 뻔했다.** `hide_article`은 "이미 있는 URL이면 안
   적는다"인데, 7일치를 읽게 되면 3일 전에 숨겼던 기사가 오늘 다시 올라왔을 때(어제 0시에
   숨김이 이미 풀렸다) 옛 기록에 걸려 오늘의 🗑️가 아무 일도 안 하게 된다. 검사를 **오늘
   기록끼리만** 하도록 좁혔다.
3. **복구가 옛 기록을 지울 뻔했다.** `unhide_articles`는 URL로 지우는데, 오늘 것 하나를
   되돌린 대가로 "그저께 뭘 뺐더라"의 기록이 말없이 사라지면 안 된다. **오늘 기록만** 지운다.

### 화면 — 오늘과 지난 날짜를 눈으로 가른다

- **좌하단 팝오버·배지는 오늘치만**(`load_hidden_batches(days=1)`). 그 숫자가 뜻하는 건
  "지금 이 화면에서 빠져 있는 기사가 몇 건"이라 지난 날짜가 섞이면 화면이 거짓말을 한다.
- **`/hidden`만 7일치**를 날짜별 `<details>`로 묶고 **오늘 구획만 펼친 채** 연다.
- **복구 버튼·체크박스는 오늘 구획에만.** 지난 날짜는 이미 숨김이 풀려 있어 되살릴 게 없고,
  `undo` 스택도 자정에 비워져 ↩로도 못 돌린다 — **버튼을 눌렀는데 아무 일도 안 일어나는 게
  제일 나쁜 상태**라(2026-08-24 회차 파일 누락과 같은 종류의 빈틈) 아예 안 그린다. 그 자리엔
  **원문 링크**를 뒀다: 되살릴 수 없는 기록에 남는 유일한 쓸모가 "그래서 그 기사가 뭐였더라"
  이고, 그걸 확인할 곳이 이 화면 말고 없다. 구획 머리에 `숨김이 풀린 기록 · 보기 전용`을 한
  번만 적어 그 이유를 말한다.
- 라벨 보관함의 "🗑️ 숨김" 표시도 `load_hidden_urls()`(오늘)로 바꿨다 — 그 표시의 뜻이
  "지금 숨김 상태다"라, 7일치가 섞이면 이미 풀린 기사에 숨김 딱지가 붙는다.

### 안 한 것

- **필터 기간을 같이 늘리지 않았다** (위 근거).
- **지난 날짜 복구를 만들지 않았다** — 어제 회차는 이미 확정·발송됐고 정기 보관함은 읽기
  전용이라 되돌아갈 화면이 없다.
- `undo_stack.json` 증폭은 계산상 몇 MB 수준이라(2026-08-25 `group_overrides` 65MB 사고와
  달리 기록 하나가 200바이트대) 별도 조치를 안 했다.


## 담아둔 기사 승격 색 — 노랑에서 초록으로 (2026-09-04)

**증상.** 📌 담아둔 기사를 「확정본에 넣기…」/「초안에 넣기…」로 소제목에 올리면 그 행이
새로고침 직후 한 번 **옅은 노랑**(`.just-promoted`)으로 떴다. 사용자 지적 —
"담아둔 기사를 소제목으로 이동한 건데 왜 노랑이냐, 이건 거의 이동 수준 아니냐.
노랑은 이 화면에서 아예 새로 보는 기사잖아."

**처음엔 노랑이 맞다고 답했다.** 근거는 "📌 구획은 복사·내보내기에서 빠지는 목록 밖
임시보드이고, 승격이 그 기사가 처음으로 보고서 본문에 들어오는 순간이니 새 도착이
맞다"였다. 축을 **목록 소속**(이 목록에 처음 왔냐)으로 잡은 것이다.

**틀렸다. 사용자가 잡은 축(담당자가 이미 봤냐)이 앱 자신이 쓰는 축이었다.**
`.is-new-arrival` 기준선(`confirmedKnownUrls:{date}:{slot}` / `previewKnownUrls:{deadline}`)은
`document.querySelectorAll(".article[data-url]")`, 즉 **그 페이지에 그려진 기사 행 전부**를
담는다. 그런데 📌 담아둔 기사 구획도 같은 `render_article`로 그려져
`<div class="article" data-url=…>`이다(`app/renderer.py` `_render_manual_section`). 그래서:

- 담아둔 기사의 URL은 **승격 전부터 이미 기준선에 들어 있다.**
- 승격 → `location.reload()` → 그 URL은 `known`에 있으므로 **`.is-new-arrival`이 안 붙는다.**

즉 노랑이 두 뜻(새 도착 / 승격)을 겸하고 있던 게 아니라, **새-도착 규칙이 명시적으로
"얘는 새 기사가 아니다"라고 판정한 행에 새-도착 색을 칠하고 있었다.** 겹침이 아니라
모순이다. (첫 답변에서 "승격 직후 `.is-new-arrival`도 같이 붙는데 `.just-promoted`가
나중 선언이라 이길 뿐"이라고 적었는데 그건 사실이 아니었다 — 코드를 다시 읽고 정정.)

**게다가 물리적으로 안 볼 수가 없다.** 승격 드롭다운(`_manual_promote_control_html`)이
📌 구획 **안에** 있다. 그 기사를 화면에 띄워놓고 그 행에서 직접 눌러야만 승격이 되므로
"처음 보는 기사"일 가능성이 0이다.

**표시의 목적도 원래 이동 쪽이었다.** `.just-promoted` 도입 주석(2026-08-05)이
*"승격시킨 기사가 자동분류 목록 **어디에 들어갔는지** 한눈에 안 보여서"*라고 적고 있다 —
그건 `.just-moved`가 하는 일 그대로다("방금 내가 만진 게 어디 갔지?"). 바로 아래
초록 도입 주석도 **같은 날짜**인데, 그때 "노랑이 세 뜻으로 겹쳐 헷갈린다"는 피드백을 받고
**이동만 초록으로 빼고 승격은 노랑에 남겨뒀다.** 반만 정리한 셈이었고 이번이 나머지 절반.

**고친 것.** `app/config.py` 한 줄 — `COLOR_ROW_PROMOTED = COLOR_ROW_NEW` →
`COLOR_ROW_MOVED`. 클래스(`.just-promoted`/`.just-moved`)도 JS도 선언 순서도 안 건드렸다
(둘 다 `.is-new-arrival`·`.finalize-added-bg`보다 뒤라 "내 조작이 이긴다"는 기존
우선순위가 그대로다). 남은 축:

| 색 | 뜻 |
|---|---|
| 옅은 노랑 `.is-new-arrival` | 앱이 알아서 들여온 기사 — 내가 안 만졌는데 화면에 생김 |
| 연보라 `.finalize-added-bg` | 마감 때 AI가 대신 배정한 기사 |
| 세이지 `.just-moved` / `.just-promoted` | **내가 방금 만진 결과** — ↑↓·소제목 이동·승격 |

**기각한 반론 하나.** 승격은 단순 자리 이동보다 무겁다 — 📌 구획은
`이 아래는 복사·내보내기에 포함되지 않음` divider 아래라 보고서 **밖**이고, 승격이 그
경계를 넘는다(`.just-moved`가 커버하는 건 전부 보고서 안에서의 이동). 하지만 색이 답하는
질문은 "방금 만진 게 어디 갔지" 하나뿐이고 둘이 같다. 경계를 넘었다는 사실은 divider와
구획 위치가 이미 말하고 있어서 색까지 그 짐을 질 필요가 없다.

**클래스를 하나로 합치지 않은 이유.** 지금은 값이 같아도 뜻은 여전히 둘이고(자리 이동 /
목록 밖에서 합류), 나중에 한쪽만 다시 갈라야 할 수 있다. 값을 별칭으로 묶어두면 그때
`config.py` 한 줄만 다시 떼면 된다 — CLAUDE.md "이름은 색이 아니라 뜻으로 붙인다"와 같은
규칙이다.

## 「🤖 AI가 추출한 주요 키워드」 블록을 없앴다 (2026-09-10)

담당자 지적 — "이거 기능이 별로 필요 없어보여". 실제로 저장된 회차 137개로 재보니
그 판단이 맞았고, 무엇보다 **우리가 이미 한 달 전에 같은 결론을 내려놓고 정기에만
적용을 안 하고 있었다.**

**근본 원인은 품질이 아니라 구조였다.** 이 블록은 회차 기사 전체에서 단어 빈도를 세
상위 5개를 보여준다(LLM 아님, 순수 규칙 기반). 그런데 **집계 대상이 이미 담당자의
검색어로 걸러진 기사**다 — 그 기사들은 정의상 등록된 검색어에 걸려서 들어온 것이라,
빈도를 세면 "내가 안 보고 있는 것"은 원리적으로 못 찾고 검색어 주변 단어만 되돌려준다.
2026-08-20에 수시 모니터링을 설계하며 이 블록을 아예 넣지 않기로 한 근거가 정확히
이것이었다("검색어를 담당자가 직접 정했으므로 AI가 키워드를 다시 뽑아줄 이유가 없다",
ADHOC_DESIGN.md §463). 정기도 같은 성질인데 그 판단이 정기까지 오지 않았다.

**실측(저장된 회차 137개, 뽑힌 키워드 683개):**

| 관측 | 수치 |
|---|---|
| 조사 잔재·숫자머리 토큰 | 72/683 = **11%** (`룸살롱서`, `尹정부땐`, `1인당`, `45개월째`) |
| 그 회차 소제목 이름에 이미 들어있던 단어 | 220/683 = 32% |
| 딱 한 회차에만 나온 단어 | 319/419 = 76% |
| `부총리` / `부동산` 등장 회차 | 18회(13.1%) / 17회(12.4%) |

실제 출력 세 줄이 그 표를 그대로 설명한다:

```
09-09 09:30  룸살롱서, 법카, 유흥업소서, 작년, 룸살롱
09-09 06:00  尹정부땐, 다주택, 주도, 중과세, 증세
09-10 09:30  부총리, 구조혁신, 경제관계장관회의, 경제, 관계장관회의
```

세 가지가 겹쳐 있다. (1) 토크나이저가 못 자른 조각(`룸살롱서`)이 다섯 자리 중 하나를
먹는다. (2) 한 리스트 안에서 같은 말이 두 자리를 먹는다(`경제관계장관회의` +
`관계장관회의`). (3) `부총리`처럼 늘 떠 있는 단어는 **행마다 값이 안 변하면 정보가
아니라 장식**이라는 기준에 그대로 걸린다 — `/keywords`에서 그룹 카드마다 붙이려던
"실시간" 배지를 뺄 때(2026-08-27) 쓴 그 기준이다.

**이름이 사실과 달랐던 것도 같이 정리됐다.** "AI가 추출한"이라고 적혀 있지만 LLM은 한
줄도 안 탄다. 바로 아래 `💬 AI가 읽은 소제목별 주요 요약`은 진짜 LLM이 쓴 것이라,
나란히 놓인 두 블록 중 하나가 거짓 라벨이면 진짜 쪽의 신뢰도까지 깎였다.

**칩 클릭 기능도 함께 없앴다.** 2026-08-13에 붙인 "칩을 누르면 `(AI 추출)` 그룹에 검색어로
바로 등록" 기능이 이 블록의 유일한 *동작*이었는데, 운영 중 `data/settings.json`에 그
그룹이 **한 번도 만들어진 적이 없다**(등록된 6개 그룹 전부 담당자가 직접 만든 것).
이 블록을 없애면서 갈 곳이 사라지는데, 다른 자리로 옮겨줄 근거가 실사용에 없었다.

**기각한 절충안: 이름만 정직하게 고치기(「자주 나온 단어」).** 라벨이 사실과 맞아져도
위 (1)(2)(3)이 그대로 남는다 — 쓸모없는 블록의 이름을 고치는 건 자리를 계속 내주는 것뿐이다.

**공정하게 적어둘 반대 논거 둘** (그래도 지우기로 한 이유와 함께):

- **화면 전용이라 해로울 게 없었다.** 이 블록은 복사·txt·텔레그램·이메일·엑셀 어디에도
  안 나간다(`_build_plain_text`가 의도적으로 제외). 즉 없애도 보고서 텍스트는 한 글자도
  안 바뀌고, 반대로 "가만 둬도 아무도 안 다친다"도 참이었다. 그래도 지운 건 **화면
  하단이라는 자리를 실제로 AI가 쓰고 복사 버튼까지 달린 요약 블록이 갖는 게 맞다**는
  판단 때문이다.
- **STOPWORDS 관리 부담은 안 줄어든다.** `app/tokenizer.py`의 불용어 목록은 홈
  워드클라우드(`extract_keyword_frequencies`)가 같은 집계 코어를 쓰므로 그대로 남는다 —
  "관리가 편해진다"는 삭제 논거로 쓸 수 없었다. 실제로 죽은 코드가 된 건
  `extract_keywords`(호출부 2곳)뿐이다.

**지운 범위** — 화면 두 곳(`app/renderer.py` `_render_bottom`,
`app/preview_renderer.py` `_render_preview_bottom`)의 `<h3>`+칩 마크업과 그 CSS
(`.bottom-keywords`/`.ai-keyword-chip*`)·JS(`addAiKeyword`), 그리고 딸린 배선 전부:
`app/summarizer.py` `extract_keywords`, `app/settings.py`의 `(AI 추출)` 그룹 기계
(`AI_KEYWORD_GROUP_NAME`/`add_ai_extracted_keyword`/`remove_ai_extracted_keyword`),
`app/settings_server.py`의 `/add-ai-keyword`·`/remove-ai-keyword` 라우트와 핸들러,
`app/config.py` `MAIN_KEYWORD_COUNT`. 키워드 블록이 사라지며 `_render_bottom`의
`articles`·`keywords` 인자가 둘 다 안 쓰이게 돼 함께 뺐고, `.bottom-summary`의
`border-top`도 없앴다 — 그 구분선은 위에 키워드 블록이 있을 때만 뜻이 있었고 이제는
상자의 첫 줄에 선이 하나 더 그어지는 꼴이 된다. `MAIN_KEYWORD_COUNT`를 기본값으로
쓰던 `extract_keyword_frequencies`는 `LANDING_KEYWORD_COUNT`로 바꿨다(워드클라우드
호출부가 예전부터 그 값을 명시적으로 넘기고 있었으므로 동작은 그대로다).

## 정기 보관함 소제목 순서 — 발송된 보고서와 달랐던 문제 + 「기타」 맨 뒤 고정 (2026-09-10)

담당자 질문 — "보관함에는 왜 소제목이 가나다순으로 저장이 돼?"

**가나다순이 아니었다.** `app/history_renderer.py` `_group_articles_by_saved_group`이
회차 파일의 기사 목록을 위에서부터 훑으며 소제목을 처음 만나는 순서대로 세웠는데, 회차
파일은 기사를 **언론사 우선순위순**으로 저장한다 — 그래서 가장 높은 순위 언론사(KBS·
MBC·조선일보…) 기사가 든 소제목이 맨 위에 왔다. 가나다처럼 보인 건 우연이었다(2026-09-10
09:30 회차가 마침 `경→기→무→지→미`였다).

**진짜 문제는 담당자가 ▲▼로 정한 순서가 보관함에서 사라진 것이었다.** 확정본은
`app.group_order.apply_group_order`로 회차별 순서(`data/group_order.json`)를 입히는데
보관함엔 그 단계가 없었다. 실측:

| 회차 | 확정본에서 정한 순서 | 보관함에 보이던 순서 |
|---|---|---|
| 9/10 09:30 | 경제관계장관회의 → 무역범죄 → 지식산업센터 → 미래대응기금 → 기타 | 경제관계장관회의 → **기타** → 무역범죄 → 지식산업센터 → 미래대응기금 |
| 9/9 09:30 | 종부세 → **ODA** → 일자리 → 대미투자 → 기타 | 종부세 → 기타 → 일자리 → 대미투자 → **ODA** |

발송 텍스트(`app.renderer.build_latest_plain_text`)에는 그 순서가 들어갔으므로, **보관함에서
꺼낸 복사·txt·엑셀이 실제로 나간 보고서와 달랐다.** ▲▼를 안 누른 회차는 두 화면이 같았고,
어긋나는 건 담당자가 손을 댄 회차뿐이라 더 늦게 드러났다. 고친 자리는
`_group_articles_by_saved_group` 한 곳 — 화면·복사·엑셀 세 경로가 모두 이 함수를 거친다.
순서는 이름표(`load_group_labels`)와 같은 방식으로 그 회차의 round_id로 읽는다.
`group_order.json`은 정리되지 않고 쌓여 있어(2026-08-14부터 42개 회차) 지난 회차 순서도
그대로 되살아났다.

**「기타」 맨 뒤 고정 (사용자 결정).** 조사하며 같이 드러난 것 — ▲▼를 안 누른 회차는
확정본에서도 기타가 맨 위·가운데에 왔다(9/10 11:00은 맨 위). 자연 순서가 위와 같은
"먼저 나온 순"이라서다. `apply_group_order`가 📂 미분류를 맨 뒤로 미는 것과 같은 자리에서
기타를 그 바로 앞에 고정했다.

- **판단은 화면 이름(이름표 적용 후)으로 한다.** 원래 이름만 보고 처음 구현했다가 실데이터로
  돌려보니 9/10 11:00이 여전히 기타가 맨 위였다 — 담당자가 `미래대응기금 관련`의 이름표를
  `기타`로 바꿔 둔 것이었다(그런 이름표가 3건, 그중 2건이 2026-09-10). 보고서를 받는 사람에겐
  그것도 `<기타>`다. 반대로 원래 `기타`에 진짜 이름표를 붙인 경우(1건, `공공기관 통폐합`)는 이제
  받이가 아니라 일반 소제목이라 옮길 수 있어야 한다.
- **고정 칸엔 ▲▼를 안 그린다.** 그려두면 눌러도 서버가 도로 뒤로 보내 아무 일이 안 일어나는
  버튼이 된다(📂 미분류에서 2026-08-21에 ▲▼를 뺀 것과 같은 이유). 미니 목차에서도 잠그고,
  헤더 ▲▼·목차 (적용)이 저장하는 순서 목록에서도 뺀다.
- **덤으로 고친 죽은 버튼:** ▼를 잠그는 기준이 "화면의 마지막 소제목"이라, 📂 미분류가 떠 있으면
  그 바로 위 소제목의 ▼가 켜져 있는데 눌러도 아무 일이 없었다(목록 계산에서 미분류가 빠져
  경계 밖으로 나가 조용히 return). 이제 "옮길 수 있는 마지막 소제목"에서 잠근다.
- **감수한 것:** 옛 저장 순서에 기타가 중간에 적힌 회차는 보관함에서도 이제 기타가 맨 뒤다 —
  그 회차만큼은 보관함이 발송된 보고서와 기타 위치 하나가 다르다. 실측 1개 회차(2026-09-07
  17:00, 10개 중 8번째)라 규칙을 예외로 흔드는 쪽보다 낫다고 봤다.
- **수시 모니터링은 안 바꿨다.** 카드의 소제목 순서(`app/adhoc/card.py` `ordered_group_names`)는
  그대로다 — 이번 결정은 정기 흐름에 대한 것이라, 수시에도 적용할지는 따로 정한다.

## 「앞 회차 기사」 칩 → 「늦게 들어온 기사」 (2026-09-11)

사용자 결정. 14시 초안에 뜬 10:54 발행 기사(11시 마감 순간 네이버 검색에 아직 없던 기사)에
`⏱ 앞 회차 기사` 칩이 붙어 있는 것을 보고 "이게 11시 것으로 넘어가는 거냐"는 질문이 나왔다.
실제로는 **14시 확정본에 담기고 보고서에도 「14시 기준」 아래 나간다** — 이미 저장된 회차
파일에 기사를 뒤늦게 더하는 경로는 코드 어디에도 없다(그날 11시 파일 39건에 그 기사가 없음을
확인). 오해의 원인은 칩 글자였다: 「앞 회차 기사」가 "이 기사는 앞 회차 소속"으로 읽혀 앞
회차로 옮겨질 것처럼 보였다.

- **바꾼 건 칩 글자 하나뿐이다.** 툴팁(`11시 회차 시간대 기사인데 그때 수집되지 않아 이번 회차에
  담겼습니다`)과 배너(`11시 회차 기사가 뒤늦게 수집돼 함께 담겼어요`)는 그대로 둔다 — 둘 다
  "이번 회차에 담겼다"를 이미 말하고 있어서다.
- **새 글자도 네이버에 대한 주장을 안 한다** — 「늦게 들어온」의 주어는 우리 앱의 수집이다
  (2026-09-03에 세운 "문구는 우리 동작만 서술한다" 규칙 그대로).

**같이 확인한 것 (실측 2026-09-11, 9월 회수분 71건):** 회차 마감보다 20분 넘게 일찍 발행된
회수분 26건 중 **21건이 이미 앞 회차에 같은 제목으로 실린 기사**였다(통신사 사진기사처럼 같은
기사가 네이버 기사 ID만 달리 여러 건 올라온 쌍둥이 — 앞 회차에선 `deduplicate_by_title`이
하나로 접었는데, 다음 회차에선 대표 URL만 `_already_published_urls`로 빠지고 쌍둥이가 살아남아
「늦게 들어온 기사」로 다시 실린다). 진짜로 늦게 색인된 기사는 5건, 가장 늦은 게 35분이었다 —
60분 여유(`COLLECT_LOOKBACK_MIN`)는 실측상 충분하다. 쌍둥이 재게재는 아직 손대지 않았다
(`_already_published_urls`가 URL로만 대조하는 건 제목만 우연히 같은 별개 기사를 지키려는 기존
결정이라, 바꾸려면 따로 정해야 한다).

## 보관함 삭제 — 줄 끝 🗑 + 선택 삭제 (2026-09-11)

### 요청
"수시 보관함 삭제는 어떻게 하면 좋을까? 필요 없는 것들이 있어. 정기는 굳이 싶긴 한데 정기도
필요할 듯." 실제로 수시엔 시험 카드가 쌓여 있었다(0건 카드 8장 중 7장이 8/21 시험·
「세재개편안 확정」). 그때까지 수시 카드를 지우는 길은 **카드 화면 안**(탭 ×·「사안 목록에서
삭제」)뿐이라 보관함에서 열고→지우고→돌아오기를 한 장씩 반복해야 했고, 정기는 지우는 길이
아예 없었다.

### 시안 두 번 (ARCHIVE_DELETE_MOCKUP.html)
- **1차 — 「정리」 모드 하나.** 평소엔 버튼 하나만 두고, 누르면 체크박스가 나오는 방식. 줄마다
  🗑를 상시로 두는 안은 "찾아서 꺼내는 화면을 시끄럽게 하고, 시험 카드 7장이면 확인창 7번"이라
  기각했다.
- **사용자 지적 두 개**: "개별로 삭제가 가능하게끔 해야 하지 않을까?" / "'정리'를 누르면 어떻게
  되는데?" — 하나만 지우는 데 3단계(정리→체크→삭제)였고, **물어봐야 알 수 있는 이름**이었다.
- **2차 — 길을 둘로.** 줄 끝 🗑(하나, 확인창 없음) + 「선택 삭제」(여러 개, 확인창 한 번). 확정본의
  기사 🗑(바로 숨김) ↔ 체크박스 일괄 숨기기(확인창)와 같은 짝이다. 1차에서 기각한 "줄마다 🗑"를
  되살린 근거는 **지운 자리에 곧바로 「삭제함 · 되살리기」가 남는다는 것** — 되돌릴 길이 눈앞에
  있으면 확인창은 한 번 더 누르게 할 뿐이라, 1차 기각 사유(확인창 7번)가 사라졌다. 사용자 채택.

### 정기에서만 걸린 함정 둘 — 오늘 회차와 가장 최근 회차
- **오늘 회차**: 파일이 없어지면 스케줄러가 `run_exists`로 "아직 안 돈 회차"라 보고 10초 안에
  다시 수집한다(2026-09-04부터 오늘 놓친 회차는 전부 보충). 지우기가 "다시 모으기"가 된다.
- **가장 최근 회차**: 코드를 따라가다 발견했다. 자동 발송(`check_pending_confirm_and_send`)은
  `load_latest_run()` 하나만 보고 `send_count == 0`이면 보낸다. 오늘 첫 회차가 오기 전 새벽에
  어제 19:30을 지우면 **어제 17:00이 "가장 최근"이 되고, 실측상 최근 회차는 전부
  `send_count 0`이라 그 회차가 텔레그램·이메일로 자동 발송될 수 있었다.** 되돌릴 수 없는 외부
  발송이라 🗑를 안 그리는 것에 더해 서버(`app/storage.py` `run_lock_reason`)가 막는다.

### 같이 드러난 것
- **보이지 않던 시험 카드 6장.** 사안 정보(`issues.json`)가 없는 카드를 보관함이 통째로 건너뛰고
  있었다 — 목록엔 17장인데 상단 합계는 「회차 23건」. 보이지 않으니 지울 방법도 없었다.
  「이름 없는 사안」 한 묶음으로 맨 아래에 보이게 했다.
- **카드 id 경로 조작.** 카드 삭제가 폼의 id를 그대로 `cards/{id}.json`에 붙여, `../issues` 같은
  값이면 사안 목록 파일을 trash로 옮길 수 있었다(카드 화면의 기존 삭제도 같았다). `delete_card`가
  id 형식을 먼저 본다.
- **9월 8일 19:30 · 0건.** 그날 오전 09:02에 0건으로 저장된 회차 — 이 파일 때문에 저녁의 진짜
  19:30 수집이 "이미 돌았다"로 건너뛰어졌을 가능성이 크다. 원인은 따로 조사한다(이 작업 범위 밖).

### 기각한 것
- **🗑마다 확인창** — 위 2차 근거. 여러 개일 때만 묻는다.
- **체크박스 상시 노출(모드 없이)** — 확정본은 작업대라 맞지만, 보관함은 매일 보는 목록이라 잡음.
- **정기는 "보관함에서만 숨기기"(파일은 두고 표시만 끔)** — 보관함에선 사라지는데 홈 정책 단어
  추이·엑셀엔 계속 잡혀 "지웠는데 어딘가 튀어나오는" 상태가 된다.
- **수시도 정기처럼 제자리 줄을 영구히** — 시험 사안을 치우려고 지웠는데 「삭제함」 줄 6개가 계속
  남으면 목적과 반대다. 수시는 빈 자리가 목록에 원래 안 남으므로 맨 아래 한 곳(「최근 삭제」)에
  모았다. 정기는 빈 자리가 어차피 「수집 기록 없음」 줄로 그려져 그 문구만 바꾸면 된다.

### 수시 사안 줄에도 🗑 (2026-09-15)
- **요청**: "수시 보관함에서 쓰레기통 버튼을 각 카드(사안명)에도 붙이는 게 나을 것 같은데."
  치울 대상이 대개 **사안 단위**(`햄버거`·`테스트3`·0건 사안)인데, 사안 하나를 지우려면
  [선택 삭제] → 사안 줄 체크 → 선택 바 삭제 → 확인 네 번이었다.
- **9/11 규칙과 부딪히지 않는 이유**: 그때 사안·날짜 줄에 🗑를 안 단 이유는 "확인창 **없는** 한 번에
  여러 회차가 간다"였다. 확인창을 붙이면 "하나 = 확인 없음 / 여러 개 = 확인 한 번"이 그대로
  지켜지고, 카드 화면에 이미 같은 짝(기사 🗑 바로 숨김 ↔ 소제목 헤더 🗑 이름·건수 확인창)이 있어
  담당자에게 새 규칙이 아니다. 사용자 채택.
- **지우는 범위 = 지금 보이는 회차만.** 기간 필터를 걸어두면 그 사안의 기간 밖 회차는 안 보이는데,
  🗑가 그것까지 지우면 담당자가 본 적 없는 카드가 사라진다. 남는 수는 확인창에 적는다.
- **문구는 "사안"이 아니라 "쌓인 회차".** 보관함에서 지워도 사안 이름은 새 수집의 사안 목록에
  남는다 — "사안을 지운다"로 읽히면 다음에 사안 목록에서 그 이름을 보고 헷갈린다.
- **통째로 지운 사안엔 「모두 되살리기」.** 코드상 되살리기는 이미 됐지만(지운 카드는 사안 블록 안
  제자리 「삭제함」 줄로 남는다) 회차 4개짜리 사안이면 되살리기를 네 번 눌러야 했다.
- **기각 — 수시 날짜 줄 🗑**: 실제 보관함 대부분이 `날짜 1 · 회차 1`이라 날짜 줄 🗑는 거의 늘 회차 줄
  🗑와 같은 일을 하고, 한 사안을 펼치면 🗑가 세 층에 겹친다.

### 정기 날짜 줄에도 🗑 (2026-09-15, 같은 날)
- **요청**: "정기 보관함 날짜 줄에도 붙여줘." 정기는 사안 층이 없어 **날짜 줄이 맨 위 층**이다 — 수시
  사안 줄과 같은 자리라 같은 규칙(확인창 한 번)을 준다. 수시 날짜 줄을 기각한 근거("거의 늘 회차
  1개")도 정기엔 안 맞는다 — 하루 4~8회차가 쌓인다(실측 9/14: 8회차 201건).
- **잠금을 그대로 따른다.** 오늘 날짜엔 🗑를 안 그린다(전부 잠김). 가장 최근 회차가 든 날은 그
  회차만 빼고 지우고 확인창에 남는다고 적는다 — 날짜 줄 🗑라고 "그 날 전부"를 약속하면, 새벽에
  어제를 통째로 지운 담당자는 가장 최근 회차가 남은 걸 버그로 읽는다.
- **지연 로딩이 걸린다.** 이틀보다 오래된 날짜는 펼치기 전까지 회차 줄이 화면에 없어, 화면에서
  회차를 모아 보낼 수 없다. 인덱스에서 서버가 키를 구워 넣는다([빈 회차 모두 고르기]가 이미 쓰는 길).
  지운 뒤엔 그 날을 펼쳐 불러와 「삭제함」 줄을 보여준다 — 접힌 채로 두면 지운 결과가 안 보인다.
- **통째로 지운 날짜 줄**: 정기는 제자리 「삭제함」 줄이 영구히 남으므로 8회차를 지우면 되살리기도
  8번이다. 수시 사안 줄처럼 날짜 줄에 「모두 되살리기」를 둔다. 되살리기 엔드포인트는 하나씩이라
  화면이 순서대로 부른다(한꺼번에 보내면 인덱스를 동시에 다시 쓴다).

## 초안에 한 번 보인 기사는 그 회차에서 안 빠진다 (2026-09-11)

**제보.** 11시 초안에 늦게 들어온 기사가 떴는데, ✏️로 소제목 이름을 바꾸고 나니 그 기사가
초안에서 사라졌다 — "앞 회차 시간대엔 내가 볼 수 없었으니 이번 회차에라도 넣어야겠다"고
생각했는데 그게 불가능해졌다.

**재현 시도 — 실패.** 프로젝트 전체를 임시 폴더에 복사하고, 되돌리기 스택에 남은 스냅샷으로
그날 두 번의 이름 바꾸기(10:42 「정부 경기 회복 평가」→「9월 경제동향」, 10:50 「경제정책
인사 및 부동산 의혹」→「여당 개각 비판」) **직전** 상태(숨김·배정·이름표·LLM 캐시)를
되살려 전/후 초안을 그려 비교했다. 둘 다 기사 28건 그대로, 늦게 들어온 기사(09:24 동아일보
정점식) 1건도 그대로였다. 코드로도 이름 바꾸기는 이름표만 저장하고, 새로고침 뒤 기사 목록
계산은 이름표를 안 쓴다. 11시 초안 후보 14건 중 12건은 선택 언론사 밖이라 애초에 안
떴다. 한계: 그때의 네이버 검색 결과는 저장돼 있지 않아 지금 다시 검색한 결과로 대신했다 —
**그 사이 검색 결과가 달랐다면 재현할 방법 자체가 없었다.** 원인이 "초안이 기억이 없다"는
구조에 있다는 것까지만 확인했다.

**결정(사용자, 추천안 그대로).** 원인을 못 찾아도 약속은 코드로 걸 수 있다 — 초안에 한 번
보인 기사를 회차 동안 붙잡아 두고, 확정본도 그 목록을 합쳐 만든다. 기존 규칙과 부딪히는
세 자리를 이렇게 정했다:
- **같은 제목 1건 규칙** — 먼저 보인 쪽이 대표를 지킨다. 안 그러면 나중에 들어온 순위
  높은 같은 제목 기사가 대표를 뺏어 담당자 눈앞에서 기사가 바뀐다.
- **설정 변경** — 다시 거른다. 담당자가 직접 조건을 바꾼 건 붙잡기보다 앞서야 한다.
  그래서 약속의 정확한 문구가 "담당자가 숨기거나 조건을 바꾸지 않는 한"이다. 검색어가
  바뀌면 목록을 통째로 새로 시작한다(붙잡은 기사가 새 검색어에 맞는지 가를 키워드별 원시
  결과를 안 들고 있다 — 넓게 남겨두면 새 검색어와 무관한 기사가 확정본에 섞인다).
- **네이버에서 사라진 기사** — 남긴다(기사 dict 전체를 저장). 링크가 죽어 있을 수 있고,
  번호만 바뀐 재게재면 같은 기사가 두 번 뜰 수 있다(제목이 같으면 1번 규칙이 접는다).

**구현 중 걸린 것.** 처음엔 "먼저 보인 시각"을 초 단위 ISO 문자열로 저장했더니 테스트에서
같은 초에 보인 두 기사가 동점이 돼 순위가 높은 쪽이 대표를 가져갔다 — 실사용에서도 일괄
동작 중 충분히 나올 수 있어서 1, 2, 3… 순번으로 바꿨다.

**다음을 위한 장치.** 이번처럼 기록이 없어 못 가리는 일이 다시 없도록, 직전 초안에 있던
기사가 빠지면 이유를 로그에 남긴다(`초안에서 빠진 기사 N건 (회차=HH:MM)` + 기사별 이유).
붙잡기 이후 정상적인 이유는 담당자 동작과 설정뿐이라 그 밖의 이유는 WARNING으로 올린다 —
그 줄이 찍히면 이 약속이 어딘가에서 새고 있다는 뜻이다.

## 설정 화면 폭 480px → 800px + 긴 화면 여러 칸 배치 (2026-09-11)

### 요청

담당자가 "설정 화면만 초안·확정본보다 폭이 좁고, 그래서 정보가 많아지면 스크롤을 더
내리게 된다"고 했다. 원인은 `_BASE_STYLE`의 `.container`·`.topbar-inner`·`.save-bar-inner`
세 자리가 480px이었던 것 — 2026-09-02에 기사 목록 화면 셋(`/hidden`·`/labels`·
`/label-manage`)만 `body.wide`로 800px을 쓰게 하면서 "입력칸 하나 놓는 진짜 설정 화면엔
480이 맞다"고 남겨둔 값이다. 그 판단이 검색어 그룹 카드·언론사 체크박스처럼 **목록이
길어지는 설정 화면**까지 좁게 묶고 있었다.

### 재보니 폭만으로는 스크롤이 안 줄었다 (SETTINGS_WIDTH_MOCKUP.html)

사용자는 처음에 "전부 800으로"를 골랐는데, 실제 설정 화면 15개를 지금 데이터 그대로
불러와 1280px 모니터 기준으로 페이지 길이를 재보니 **폭만 넓히면 합계 −3%**(17.6 →
17.1화면, 1화면=800px)였다. 검색어 2.5 → 2.4, 언론사 3.4 → 3.4. 화면 대부분이 한 줄에
하나씩(언론사는 두 칸씩) 쌓는 구조라, 넓어진 만큼 오른쪽만 비고 줄 수는 그대로였다 —
기사 한 건이 한 줄을 다 쓰는 확정본·초안과 달리 **폭이 곧 정보량이 아닌 화면**이었다.
그래서 "800 + 배치 변경" 시안을 하나 더 만들었고(합계 **−22%**, 17.6 → 13.7화면),
사용자가 그걸 전부 적용하기로 했다.

### 적용한 것

- **폭**: 세 자리 모두 800px. `body.wide` 스위치는 모든 화면이 800이 되면서 할 일이 없어져
  규칙째 없앴다(세 화면의 `<body class="wide">`도 `<body>`로).
- **배치** — 긴 화면 여섯만. 짧은 화면 아홉은 원래 한 화면 안이라 폭만 넓혔다.

| 화면 | 배치 | 지금 → 바꾼 뒤 |
|---|---|---|
| 설정 메뉴 | 메뉴 묶음 두 단(CSS 다단 — 위→아래 읽는 순서 유지) | 1.4 → 0.9화면 |
| 검색어 | 그룹 카드 한 줄에 두 장 | 2.5 → 1.7 |
| 형광펜 긋기 | 단어 한 줄에 세 개 | 1.1 → 0.6 |
| 수집 시간 | 「주중」「주말」 카드 나란히 두 장 | 1.5 → 1.1 |
| 언론사 선택 · 순서 지정 | 체크박스 두 칸 → 네 칸, 순서 목록 두 단 | 3.4 → 2.2 |
| [단독]·[속보] 알림 | 감시 그룹 체크 한 줄에 세 개 | 1.3 → 1.1 |

모두 640px 이하에선 예전 한 줄 배치로 돌아간다.

### 함정 — `1fr`은 내용물보다 작아지지 않는다

첫 적용에서 수집 시간 화면이 375px 폭에서 옆으로 스크롤됐다(문서 폭 437px). 그리드 칸을
`1fr`로 두면 그 최소값이 **내용물의 최소 폭**이라, 그룹 카드 머리줄이 칸을 밀어냈다.
예전 블록 배치에선 시간대 줄이 `flex-wrap`으로 접혀 들어갔던 것이다. 새로 만든 그리드
넷을 전부 `minmax(0, 1fr)`로 바꿔 해결했다. 같은 측정에서 기사 제목 형식·텔레그램·이메일·
라벨 관리 네 화면도 375px에서 넘쳤는데, 바꾸기 전 HTML로 재도 똑같이 넘쳐 이번 변경과
무관한 원래 문제라 손대지 않았다.

### 감수한 것

- **검색어 그룹 순서가 지그재그로 읽힌다**(왼쪽 → 오른쪽 → 다음 줄). 그룹 사이는 항상
  OR이라 순서가 뜻을 갖지 않아 받아들였다.
- **언론사 순서 목록은 1번부터 위→아래, 왼쪽 단이 끝나면 오른쪽 단 맨 위로 이어진다.**
  단 경계의 기사를 ↓로 내리면 오른쪽 단으로 옮겨 간다. 번호가 붙어 있어 순서는 안 헷갈린다.
- 카드 폭이 반이 되면서 검색어 칩·"정기 스크랩에도 포함" 안내 문구가 한두 줄씩 더 접힌다.

## 워드클라우드 제외어 화면 — 입력칸 목록 → 칩 + 오늘 뜬 단어에서 고르기 (2026-09-11)

### 요청

`/wordcloud-exclude`는 입력칸 한 줄에 `del` 버튼 하나씩을 세로로 쌓는 화면이었다(단어 4개면
4줄, 칸을 늘리려면 `+ 제외어 추가`가 폼 전체를 POST해 페이지를 다시 그렸다). 사용자가
"UI가 너무 구리다, 칩을 나란히 넣고 x로 지우자"고 해 시안 둘을 만들었다
([WORDCLOUD_EXCLUDE_MOCKUP.html](mockups/WORDCLOUD_EXCLUDE_MOCKUP.html)) — A: 칩 + ×만, B: A + 「오늘
워드클라우드에 뜬 단어」에서 눌러 빼기. **B로 결정**(사용자).

### B를 붙인 이유 — 실측

시안을 만들며 그날(9/11, 회차 4개) 홈 워드클라우드 상위 20개를 그대로 뽑아보니
`최근·이라고·모습·이어지·보이`처럼 담당자가 딱 빼고 싶을 단어가 이미 섞여 있었다.
제외어를 등록하는 계기는 늘 "홈에서 보고 거슬려서"라, 홈 → 설정 → 글자 치기를 한 번
누르기로 줄이는 게 이 화면의 실제 쓰임과 맞았다.

### 정한 것

- **칩 모양·×·점선 입력칩은 `/keywords`와 같고, 색만 중립 회색**(`COLOR_WC_EXCLUDE_CHIP_BORDER`).
  검색어 칩의 파랑은 "검색 ON"이라는 뜻인데 제외어엔 ON/OFF가 없어서, 빌려 쓰면 뜻이 섞인다.
- **Enter 또는 쉼표로 추가**(쉼표로 여러 개 한 번에). 한글 조합 중 Enter는 조합이 끝난 뒤
  읽는다 — 그냥 막으면(`isComposing`이면 무시) Enter를 두 번 눌러야 했다(시안에서 재현).
- **같은 단어를 또 넣으면 새로 안 만들고 있던 칩을 흔든다** — 검색어 화면의 "같은 그룹 안
  중복은 조용히 하나로"와 같은 규칙.
- **개수 상한 `(최대 15개)`는 제목에서 빼 상자 오른쪽 아래 `N / 15`로** 옮겼다. 다 차면
  빨간 숫자 + 입력칩이 사라지고 아래 목록 버튼이 잠긴다.
- **저장은 예전 그대로 `POST /save-wordcloud-exclude`**(진짜 `<form>` + 303, `exclude1..N`).
  칩 편집은 화면 메모리에서만 벌어지고 저장 전까지 저장 바가 "아직 저장하지 않았습니다"를
  띄운다(검색어 화면과 같은 규칙). **입력칸에 쳐두고 Enter를 안 누른 단어도 저장에 같이
  싣는다** — 저장을 눌렀는데 치다 만 글자가 조용히 사라지면 안 된다(CODING_CONVENTIONS §3).
  `+ 제외어 추가`(`/wordcloud-exclude/add-slot`)는 칩 입력으로 대체돼 없앴다.
- **「오늘 워드클라우드에 뜬 단어」는 저장 뒤 홈 워드클라우드의 실시간 미리보기다.**
  `app.landing_renderer.wordcloud_candidates`가 홈과 **같은 기사 목록(`_report_articles`,
  `generate_landing_page`도 이제 이 함수를 쓴다)·같은 집계 함수**로 제외어를 하나도 안 뺀
  순위를 넉넉히(+30) 뽑고, 화면 JS가 지금 칩에 있는 단어를 홈과 똑같은 규칙
  (`app.tokenizer.tokenize` — 그 단어 자체 + "단어+가" 잔재)으로 거른 뒤 앞 20개를 보여준다.
  단어별 횟수는 서로 독립이라 제외해도 남은 단어의 순위가 안 바뀌므로 이게 정확히 홈과
  같다. 그래서 ×로 뺀 단어는 원래 자리로 돌아오고, 하나를 빼면 21등이 올라온다. 저장된
  회차만 읽는다(API 호출 0회). 오늘 회차가 아직 없으면 그 구역을 통째로 안 그린다.
- 목록은 앱이 "빼라"고 **추천하는 게 아니다** — 오늘 뜬 단어를 순위 그대로 보여줄 뿐이다.
  검색어는 홈과 같이 회색 글자로 구분만 한다.

## 검색어 화면 촘촘하게 — 설명 네 덩어리 → 두 줄 (2026-09-11)

### 요청

폭을 800px로 넓히며 그룹 카드를 두 장씩 놓은 직후(위 "설정 화면 폭" 항목), 사용자가
`/keywords` 화면에서 세 가지를 짚었다: ① 칩·칸이 글자 크기에 비해 위아래 여백이 많다
② 개요·설명이 장황하고, 개요의 `🔴 실시간 현황` 칩 색이 와닿지 않는다 ③ 특히 카드마다의
정기 스크랩 체크 문구가 너무 길어 안 읽힌다(카드 폭이 반이 되며 3줄로 접혔다 — 위 항목의
"감수한 것" 마지막 줄이 바로 이것). 시안 둘([KEYWORDS_COMPACT_MOCKUP.html](mockups/KEYWORDS_COMPACT_MOCKUP.html))
— A: 정기 체크를 카드 아래 한 줄에 다섯 자로 / B: 머리줄의 `✓ 정기` 알약으로 — 중 **A로 결정**(사용자).

### 무엇을 바꿨나

- **여백**: 칩 32 → 25px(글자 .9 → .86rem, 위아래 7 → 4px), 그룹명 칸 34 → 27px, 카드 안쪽
  14·16 → 10·12px, 스위치 46×26 → 38×22. 카드 아래 세 줄(키워드 수 / 검색 방식 라디오 /
  정기 체크)을 한 줄로 — 키워드 수는 머리줄 `6/10`으로, 검색 방식은 두 칸 버튼으로.
  실측(등록 그룹 6개): 화면 높이 1,228 → 662px(−46%), 「재정경제부」 카드 321 → 157px.
- **개요**: 캡션 · 색 칩 두 줄 상자 · 한도 안내 문단을 문장 두 줄로. 빨간 실시간 칩은 뺐다 —
  실시간은 "켜 둔 그룹 전부"라 이름표를 붙일 대상이 아니다. 계층을 못 박는 일(2026-08-27
  상자의 존재 이유)은 문장이 이어받되, 문장 안에 카드의 실제 체크박스 모양을 그대로 넣어
  "그 체크 = 정기"가 이어 읽히게 했다. 한도는 카드의 `6/10`·잠기는 추가 버튼·저장 바
  차단 사유가 이미 말해서 뺐고, "(그룹 간 OR)"는 용어라 뺐고, "저장하면 실시간엔 바로…"
  캡션은 저장 바 안내로 옮겼다(고친 게 있을 때만 보인다).
- **정기 체크**: `정기 스크랩(예정된 회차)에도 포함 — 실시간 기사 현황에는 늘 나옵니다.
  꺼두면 …`(70자) → `정기 스크랩`(5자), 나머지는 툴팁. 켜면 글자가 진해져 카드를 훑기만
  해도 어느 그룹이 정기인지 보인다. 저장 차단 문구도 새 라벨에 맞췄다(화면 JS와
  `app.settings.validate_keyword_groups` 두 곳).

### 덤으로 찾은 것 — 그룹명 칸이 늘 회색 상자였다

코드의 `.group-name-input`은 "평소 투명, 올리면 테두리"로 적혀 있었는데, 화면에선 늘 회색
입력 상자였다. `_BASE_STYLE`의 공용 `input[type=text]`(명시도 0,1,1)가 이 규칙(0,1,0)을
이기고 있었기 때문이다(`:hover`·`:focus`·`.missing`은 0,2,0이라 제대로 먹었다 — 그래서 평소
모습만 틀렸다). `.group-head .group-name-input`으로 올려 원래 의도대로 돌렸다. 같은 클래스를
쓰는 수집 시간 화면(`_SCHEDULE_TEMPLATE`)은 거기 맞는 모양이라 건드리지 않았다.

### B를 안 고른 이유

머리줄만 훑어도 정기 여부가 보이는 건 B가 낫다. 다만 머리줄에 켜고 끄는 게 둘(그룹 스위치
· 정기 알약)이 돼 「그룹을 통째로 끄기」와 「정기에서만 빼기」가 한눈에 비슷해 보인다. A는
머리줄이 「켜기·이름·개수·지우기」만 남고, 옵션 둘(검색 방식·정기)이 아래 한 줄에 모인다 —
그리고 A도 켠 그룹의 글자가 진해져 훑기에는 충분했다.

## 기사 제목 줄 — `(언론사) 제목` → 회색 언론사 + 제목 (2026-09-11)

**계기**: 담당자 지적 — 보고서(복사·txt·메일)가 이미 `(언론사) 제목` 모양인데, 화면까지
그 모양을 따라 할 이유가 없다. 화면은 더 보기 좋게 만들 수 있지 않나.

**왜 괜찮은가**: 보고서 텍스트는 화면 글자를 긁지 않고 `apply_line_template`가 언론사·제목
값으로 따로 조립한다(CODING_CONVENTIONS.md §4) — 화면 모양을 바꿔도 받는 사람 쪽은 한 글자도
안 바뀐다. 게다가 화면은 2026-08-19에 `ㅇ `를 빼면서 이미 템플릿에서 떨어져 나와 있었고,
남아 있던 괄호의 근거는 "제목의 약 25%가 `[단독]`·따옴표로 시작해 경계가 안 보인다" 하나였다.
그 경계는 문장부호가 아니라 색·크기로도 나눌 수 있다.

**세 안을 실물로 비교했다** ([OUTLET_TITLE_MOCKUP.html](mockups/OUTLET_TITLE_MOCKUP.html), 9/11 11:00
회차 실제 기사 19건 + 실제 확정본 CSS):

| | 데스크탑 800 제목 줄 합계 | 폰 390 |
|---|---|---|
| 현재 `(언론사) 제목` | 19줄 | 38줄 |
| **A 회색 이름(채택)** | 19줄 | 37줄 |
| B 언론사 고정 칸(84px) | 19줄 | 48줄 (19건 전부 꺾임) |
| C 언론사를 메타 줄로 | 19줄 | 37줄 |

- **A 채택**(사용자 결정): 한 줄 구조 그대로라 줄 수가 안 늘고, 목록이 언론사 순이라 훑을 때
  언론사가 제목 앞에 먼저 보이는 것도 그대로다. 회색 언론사 뒤에 빨간 `[단독]`이 오면 경계가
  오히려 더 또렷하다. 매일경제 빨간 표식(`outlet_display_label`의 인라인 색)도 회색 속에서 잘
  보인다. 사진 추정 행(`opacity .55`)에선 회색 언론사가 꽤 옅어지지만, 원래 건너뛰라고 흐린
  행이라 감수했다.
- **B 기각**: 데스크탑에선 표처럼 가장 정돈되지만 제목 폭이 약 94px 줄어 폰에서 11줄이
  늘었다. 칸 폭은 실측으로 잡았다(저장된 기사 5,397건 중 99.8%가 언론사 이름 6자 이하).
- **C 기각**: 제목 줄은 가장 깨끗하지만 훑을 때 언론사를 보려면 시선이 매번 한 줄 내려간다.

**범위**: `render_article`을 쓰는 확정본·초안·정기 보관함(마크업 한 곳, CSS 세 곳), 수시 카드
행, `/hidden`. 이 바꿈으로 세 화면이 제각각이던 것(정기는 같은 색+괄호 / 수시는 회색+괄호 /
실시간은 메타 줄 칩)이 정기·수시 한 모양으로 모였다. **그대로 둔 곳**: 실시간현황(원래
메타 줄 칩 — "필터 없이 날 것" 화면이라 이번에 안 건드림)과 글자 전체가 이미 회색인 작은
팝오버(좌하단 쓰레기통·수시 「목록 밖 기사」) — 거기선 색으로 가를 수 없어 괄호가 유일한
경계다.

## 확정본·초안 🔍 검색어 말줄임 + 「늦게 들어온 기사」 칩 자리 (2026-09-14)

**증상.** 검색어가 여러 개 걸린 기사에서 오른쪽 버튼 묶음(다른 소제목·복사·🏷·⋯·🗑·↑↓)이
통째로 아랫줄로 떨어졌다(사용자 스크린샷 두 장 — 하나는 검색어만 긴 행, 하나는 거기에
`⏱ 늦게 들어온 기사` 칩까지 붙은 행). 사용자는 "끝에 `..` 처리하기로 하지 않았냐"고
물었는데, 말줄임은 **실시간현황에만**(2026-08-25, 210px) 들어가 있었고 확정본·초안은
2026-09-01에 시안 셋(`KW_ELLIPSIS`/`KW_DETAILS`/`KW_HYBRID`)을 만든 뒤 결정 없이 멈춰 있었다.

**전제가 깨진 거였다.** 2026-08-21에 "353건 중 가장 긴 줄이 26자라 한 줄도 안 밀린다"며
전부 보여주기로 했는데, 그 뒤 검색어가 늘어 최근 12회차 305건에서 다시 재니 중앙값 10자·
20자 초과 17건·최대 38자였다. 평소엔 멀쩡하다가 매칭이 많은 기사에서만 깨지는 모양.

**고정 폭으로 자르면 칩 붙은 행이 안 풀린다.** 실시간처럼 `max-width` 하나로 자르는 안(A)을
목업([KW_CHIP_MOCKUP.html](mockups/KW_CHIP_MOCKUP.html))에서 재보니, 칩 없는 행이 안 밀리는 최대값
(260px)으로도 칩 붙은 긴 행은 칩 폭(약 110px)만큼 모자라 여전히 밀렸다. 칩까지 감안해 폭을
줄이면 칩 없는 96%의 행에서 괜히 자른다. 그래서 **남는 자리만큼 줄어드는** 방식으로 갔다
(B·C). 800px 기준 밀린 행: 지금 3/7 → A 1/7 → B·C 0/7.

**칩은 검색어 앞(C).** 칩을 지금 자리(검색어 뒤)에 두면(B) 칩이 `…` 바로 뒤에 붙어 행마다
가로 위치가 108px까지 달랐다. 원문보기 뒤·검색어 앞으로 옮기니 1px — 목록을 훑을 때 칩이
늘 같은 자리에 서고, 잘리는 검색어는 줄 끝(버튼 바로 앞)에 온다. 게시시각 바로 뒤도
생각했지만(“늦게”는 시각에 대한 말) 그러면 칩 붙은 행에서만 원문보기 링크가 밀려, 누르는
자리가 흔들리는 게 더 나빠 원문보기 뒤로 했다.

**기각: 칩 있는 행만 칩을 세 번째 줄로.** 사용자가 물어본 안. 칩 붙은 행의 검색어가 100px
남짓 더 보이는 대신 — (1) 그 줄은 라벨 칩(`.lab-row`)이 "있을 때만 한 줄 더" 붙는 자리이고
두 칩이 모양·색까지 거의 같아(앰버 알약, 배경 `#FEF3C7`↔`#FBF0DA`) 라벨로 읽히며, (2)
"늦게"라는 뜻이 게시시각에서 떨어지고, (3) 그 행만 카드가 높아진다. 게다가 검색어만 긴
행(칩 없음)은 그걸로 안 풀려 말줄임은 어차피 필요했다. 겹치는 경우도 드물다(305건 중 칩
11건, 그중 검색어 긴 행 0건).

**구현에서 고른 것.** 목업은 `flex-wrap: nowrap`으로 줄였는데 실제로는 **줄바꿈을 살려둔 채**
검색어만 `flex: 1 1 0; min-width: 5em; max-width: max-content`로 뒀다 — nowrap은 창을
좁히면(700px대) 칩 붙은 행이 가로로 넘치고, 그걸 막으려면 새 `@media` 경계를 정해야 했다.
flex-basis 0이면 줄바꿈 판단에 검색어 길이가 안 들어가 버튼을 밀어내지 않고, 자리가 5em도
안 남는 좁은 창에서만 예전처럼 버튼이 아랫줄로 간다. 구분점(`·`)은 span 안으로 넣어 그때
점만 윗줄 끝에 남지 않게 했다. 잘린 행 툴팁은 실시간(로드 때 한 번 판정)과 달리 **마우스를
올리는 순간** 판정한다 — 잘림이 창 폭에 따라 달라서다. CSS·JS는 `app/renderer.py`
`kw_inline_style()`/`kw_inline_script()` 한 곳에서 만들어 초안도 받는다(예전엔 두 파일에
같은 CSS가 복붙돼 있었다).

**실측(실제 화면, 1100px 창).** 확정본 22행: 밀림 0, 잘림 4. 초안 13행(칩 5개): 밀림 0,
잘림 2, 칩 5개가 같은 가로 위치. 720px 창: 칩 붙은 1행만 버튼이 아랫줄로(가로 넘침 0).
400px 폭에서 행 3개가 넘치는 건 버튼 묶음 자체(295px)가 폰 폭보다 넓어서고, 이번 변경
전부터 그랬다(검색어는 행 안에 있다).

## 정책 단어 추이 — 홈에 단어 전부 + 날짜 점 빼기 (2026-09-15)

**계기.** 담당자가 /trend에서 단어(`세제개편`)를 추가하고 홈으로 나왔더니 반영이 안 됐다며
"저장 버튼이 필요할까"라고 물었다. 확인해 보니 저장은 `+ 추가`를 누르는 순간 이미 끝나
있었다(`data/home_trend_words.json`에 4개). 홈이 앞 `HOME_TREND_DISPLAY_COUNT`(3)개만
그리고, 새 단어는 목록 맨 뒤에 붙어서 4번째가 홈에서 말없이 잘린 것이다 — 화면이
"어느 3개가 홈에 나가는지"를 어디서도 말하지 않았다. 저장 버튼으로는 안 풀리는 문제였다.

**결정(사용자).** 홈도 저장된 단어를 전부 그린다 — "3개만 보이니 또 애매하다, 겹치더라도
`이형일`처럼 추이를 보는 거다". 대신 선이 늘어나는 만큼 어지러워지니, 날짜마다 찍던 점
(40칸 이하에서 r 2.8)을 뺐다 — "둥근 점은 도리어 지저분하다, 추이를 보는 거니까". 점은
`app.trend_chart` 한 곳이라 /trend 짧은 기간에서도 같이 빠진다. 그 화면은 호버하면 그 날짜에
점이 뜨므로 값을 읽는 데 지장이 없다. 「집계 중」 마지막 칸의 빈 원만 남겼다.

**시안.** [TREND_LINE_ONLY_MOCKUP.html](mockups/TREND_LINE_ONLY_MOCKUP.html) — 실제 데이터·실제
그래프 함수로 A 지금 / B 선만 / C 선만 4개를 비교. 선 밑에 흰 테두리를 까는 안도
그려봤는데 홈 크기에선 B와 거의 구분이 안 돼 뺐다.

**실측.** 단어 8개(최대)를 홈 폭(412px)에 그려도 오른쪽 끝 라벨 8개가 13px 간격으로
그래프 안에 다 들어간다(가장 위 y 34.5). 작은 단어들이 큰 단어 기준 축에서 바닥에
몰리는 건 점과 무관한, 공유 축을 고른 대가다(2026-09-11 결정 그대로).

**무지개색 검토(결정 전).** 같은 날 "이 그래프만 무지개색(7색)으로?"를 검증기로 재봤다 —
빨주노초파남보: 명도 밴드 FAIL(노랑), 색약 분리 FAIL(남↔보 ΔE 1.6), 노랑 대비 1.36(흰
바탕에서 거의 안 보임). 지금 팔레트 앞 7개는 전부 PASS(최악 ΔE 9.1). 지금 색도 이미
파랑·주황·청록·노랑·분홍·초록·보라·빨강 8가지라, 홈이 3개만 그려 파랑·주황·초록만
보였을 뿐 단어를 늘리면 여러 색이 자연히 나온다.

## 되돌리기 후 손댄 기사 세이지 표시 (2026-09-15)

### 요청
"[초안]에서 되돌리기 했을 때, 다시 되돌리는 기사가 음영처리 됐으면 좋겠다." 되돌리기는
새로고침만 해서, 숨겼던 기사가 어디로 돌아왔는지·옮긴 기사가 어느 소제목으로 돌아갔는지
화면을 훑어 찾아야 했다. 확정본도 같은 버튼·같은 코드라 함께 적용했다(사용자 결정).

### 처음 제안했다가 기각된 안 — 상태 비교(diff)
처음엔 `app/undo.py`의 "스냅샷 하나로 전부 커버" 설계에 맞춰, 되돌리기 직전 지금 상태와
돌아갈 스냅샷을 비교해 달라진 기사를 칠하자고 했다. 그런데 ↑↓는 이웃과 자리를 맞바꾸는
동작이라 결과만 보면 누가 옮긴 기사인지 가를 수 없어 "둘 다 칠한다"가 됐고, 사용자가
바로잡았다 — **세이지는 "담당자가 방금 손댄 것만"이라는 뜻이라 두 개가 칠해질 수 없다.**
구현 한계를 이유로 표시의 뜻을 흐리면 안 된다(미니 목차 초록이 "마지막으로 옮긴 한 칸뿐"인
2026-09-03 결정과 같은 원칙). 그래서 동작 시점에 서버가 이미 받고 있는 "누른 기사 URL"을
되돌리기 기록에 같이 적어두는 방식으로 바꿨다.

### 3초 합치기와 부딪히는 두 경우
- 서로 다른 기사를 3초 안에 따로 ↑(또는 🗑️) — 되돌리기는 한 걸음으로 합쳐지지만 칠하는 건
  마지막 기사뿐(사용자 원칙 그대로).
- 체크박스로 3건 골라 일괄 숨기기 — 담당자가 셋을 직접 골랐으니 셋 다 손댄 것(사용자 결정).
  문제는 이 동작이 `/hide-article`을 기사 수만큼 따로 보내 서버 입장에선 위 경우와 똑같이
  보인다는 것. 화면이 한 동작마다 `batch` 값을 실어 보내 구분한다.

### 덤으로 찾은 것
- **숨긴 기사를 되살리면 노랗게("새로 들어온 기사") 떴다.** 숨긴 뒤 새로고침하면 기준선이 그
  기사를 뺀 채 저장되기 때문. 게다가 초안 CSS에선 `.just-moved`가 `.is-new-arrival`보다
  앞에 선언돼 있어(CLAUDE.md "새로 들어온 기사" 항목은 반대로 적혀 있다) 겹치면 노랑이 이긴다 —
  칠한 행에서 노랑을 떼는 것으로 해결했다(실측: 숨기기 → 새로고침 → 되돌리기, 노랑 0·세이지 1).
- **스크롤 판정 타이밍.** load 50ms 뒤에 "보이나"를 봤더니 브라우저의 스크롤 복원이 아직 안
  끝나 맨 위 기준으로 판정했다(y=0에서 판정 → 이후 1184로 복원). 그 새로고침만 복원을 끄고
  위치를 직접 들고 가게 했는데, `auto`를 페이지 파싱 중에 곧바로 되돌리니 브라우저가 load
  뒤에 옛 위치를 다시 되살려 덮어썼다 — `pagehide`에서 되돌리게 해 해결. 또 가려진 탭에서는
  `behavior: "smooth"` 스크롤이 미뤄지는 게 아니라 통째로 버려져서, 그땐 즉시 옮긴다.
- **3초 합치기 때 기록이 바뀌면 되돌리기 파일을 한 번 더 쓴다**(예전엔 합쳐지면 안 썼다).
  실측 스택 7MB에 쓰기 17ms라 무시할 수준이고, 같은 기사 ↑ 연타처럼 기록이 안 바뀌면 안 쓴다.


## 수시 모니터링 — 지금까지 불러오기 (2026-09-15)

**제보(사용자)**: 수집 원본에서 "지금 시각 기준으로 다시 불러오려면" 편집을 열고 → 끝 시각
칸을 확인하고 → 「다시 불러오기」를 눌러야 해서 어렵다. 「다시 불러오기」를 누르면 시간이 지금으로
자동으로 바뀌고 불러와졌으면 좋겠다.

- **이미 거의 그렇게 돼 있었다** — 편집 창의 끝 칸은 열 때마다 지금 시각으로 미리 채워진다
  (2026-08-25). 모자랐던 건 그 버튼이 접힌 창 **안에** 있다는 것 하나라, 같은 동작을 창 밖으로
  꺼낸 버튼 하나로 풀었다(`to_now=1`, 시작 그대로 · 끝은 서버가 누른 순간 시각).
- **자리 셋을 목업으로 비교했다**([ADHOC_RELOAD_NOW_MOCKUP.html](mockups/ADHOC_RELOAD_NOW_MOCKUP.html)):
  A 편집 옆(칩 줄 끝) / B 시간 칩에 반쪽 버튼으로 붙이기 / C 헤더 줄 오른쪽. A가 뜻은 제일
  잘 읽혔지만 **1100px 창에서도 이 카드의 칩 줄이 두 줄로 넘어갔다**(24 → 55px, 칩 줄은 검색어가
  늘수록 길어진다). B는 한 줄에 들어가지만 버튼 글자가 1분마다 바뀌고 이 앱에 없던 모양이
  생긴다. C는 원래 비어 있는 자리라 아무것도 안 밀고, 누르면 바뀌는 「N시 N분 기준」이 같은 줄이라
  C로 정했다(사용자 결정).
- **이름을 「다시 불러오기」로 하지 않았다** — 편집 창 안의 버튼(입력한 시간으로)과 하는 일이 다른데
  이름만 같아진다. 같은 이유로 편집 창을 열면 헤더 버튼은 숨는다(2026-08-20 초안 AI 분류 실패
  배너에 툴바와 같은 버튼을 뒀다가 "다른 건가?"가 됐던 일).
- **덤 둘도 같이 켰다(사용자 결정)**: ① 📥 칩 줄이기 — 버튼이 쉬워지면 여러 번 누르게 되고 칩이
  한 줄씩 길어진다. `(~10:15)`는 「지금까지」로 불러오면 늘 수집 시각과 같아 뺐고, 4회부터는
  첫·마지막만. ② 새로 들어온 기사 노랑 — 누른 뒤 "뭐가 왔지?"를 `+N건` 숫자만으로는 못 찾는다.
  정기처럼 localStorage 기준선으로 하지 않은 이유: 수시는 모든 조작이 form 제출 + 전체 새로고침이라,
  기준선 방식이면 새 기사 하나를 확정본으로 보내는 순간 나머지 노랑까지 사라진다. 대신 저장된
  `added_at` = 마지막 수집 이력 `at`으로 판정해 **노랑 개수와 칩의 마지막 `+N건`이 늘 같다.**

## 수시 모니터링 — 일괄이동 바를 화면 아래로 (2026-09-15)

**요청(사용자)**: 수집 원본에서 기사를 체크하면 뜨는 일괄이동 바(`N개 선택됨 · 옮기기 · 수집 확정본으로
보내기 · 선택 해제`)는 하단 메뉴바에 고정돼야 할 것 같다.

- **문제**: 바가 툴바 바로 밑(목록 맨 위) 흐름 안에만 있어서, 스크롤을 내려 아래쪽 기사를 체크하면
  바가 이미 떠 있는데도 화면 밖이었다. 체크했다는 신호는 행 배경색뿐이고, 옮기거나 보내려면 맨 위까지
  다시 올라가야 했다.
- **두 안을 목업으로 비교했다**([ADHOC_BULK_BAR_MOCKUP.html](mockups/ADHOC_BULK_BAR_MOCKUP.html)): A 정기
  확정본·초안의 하단 흰 띠(`.bottombar`)와 같은 모양 / B 지금의 연파랑 상자를 그대로 아래에 띄우기.
  B는 체크한 행과 색이 이어지는 장점이 있지만 같은 버튼들이 정기와 수시에서 다른 옷을 입게 되고,
  좌하단 쓰레기통을 피하느라 폭이 줄어 좁은 창에서 먼저 두 줄로 접힌다. **A로 정했다(사용자 결정)** —
  같은 일을 하는 바가 두 흐름에서 같은 자리에 있어야 한다는, 좌하단 🗑·↩를 정기와 맞출 때와 같은 규칙.
- **정기와 다른 점 하나**: 정기는 🖍 형광펜 때문에 띠가 늘 떠 있지만 수시엔 띠에 올릴 게 이것뿐이라
  체크했을 때만 올라온다. 기능(버튼·드롭다운·동작)은 한 글자도 안 바꾸고 자리만 옮겼다 — 정기 바에
  있는 ↑↓·🗑 일괄 숨기기는 수집 원본엔 원래 없어(시간순이라 ↑↓가 없다) 이번에도 안 넣었다.
- **덤으로 챙긴 것 둘**: ① 좌하단 `.fab`에 `z-index`가 없어서, 위치 지정된 띠(z 20)가 나중에 그려지면
  🗑·↩를 덮을 수 있었다 — `z-index: 25`를 줬다(팝오버 30보다는 아래). ② 뒤로 가기·새로고침 때 브라우저가
  체크 상태를 되살려 두면 "체크는 돼 있는데 띠는 내려간" 화면이 되므로 `pageshow`에서 `updateBulkBar()`를
  한 번 부른다.
- **실측(1024px 창)**: 띠 안쪽이 상단바·본문 카드와 정확히 같은 x·폭(848px, 800 + 여백 24×2)에 맞고,
  끝까지 스크롤하면 마지막 기사 밑단(641px)이 띠 윗단(719px) 위에 온다(본문 아래 여백 80px 덕분).
  375px 폭에서는 띠가 두 줄(85px)로 접히고 가로 스크롤은 없다. 같은 코드를 쓰는 수집 확정본 화면은
  「보내기」 칸과 세로선 없이 그려진다.

## 라벨 — 정기 보관함(지난 회차)에서도 붙일 수 있게 (2026-09-16)

**질문(사용자)**: "근데 지나갔는데 '어? 어제 부정기사였는데 이것도 붙여야 되는 거 아니야?' 라고 생각할 수도
있잖아."

- **빈 구멍이었다.** 라벨을 붙일 수 있는 화면이 확정본·초안·수시 수집 확정본 셋뿐이라, **오늘 회차가 지나가면
  그 기사에 라벨을 붙일 길이 아예 없었다.** 그런데 `부정기사`처럼 나중에 알게 되는 라벨은 본질적으로 회차가
  지난 뒤에 붙는다 — 붙일 자리가 오늘 회차에만 있는 게 앞뒤가 안 맞았다.
- **정기 보관함이 읽기 전용인 근거가 라벨에는 안 걸린다**(이게 이번 결정의 핵심): ① 숨김·이동·이름바꾸기는
  *이미 발송된 보고서를 고치는* 일이라 지난 회차에 하면 뜻이 안 서지만, 라벨은 복사·txt·텔레그램·이메일
  어디에도 안 나가는 화면·엑셀 전용 표시다. ② 큐레이션 핸들러 8곳이 전부 `load_latest_run()` 고정인데,
  라벨은 회차가 아니라 URL을 키로 하는 전역 저장소(`data/labels.json`)라 그 함수를 아예 안 쓴다.
  `attach_label`은 article dict 하나만 받는다. ③ 애초에 그 화면 기사 행엔 🗑·복사·⋯(원문 다시 불러오기·
  제목 수정)가 예전부터 있었다 — 생각만큼 읽기 전용이 아니었다.
- **스냅샷이 저절로 맞는다**: `scrap_date`/`scrap_end`/`group`엔 **그 회차 값**이 그대로 들어가고
  `labeled_at`만 오늘이 된다(실측: 9/15 19:30 회차 기사에 9/16 10:50에 붙이니 정확히 그렇게 저장됐다).
  **붙인 시간과 회차 시간이 갈라지는 것 자체가 "뒤늦게 붙였다"는 기록**이라, 다음에 라벨 보관함 행에서
  「회차」 고정 글자를 「라벨 붙인 시간」으로 바꾸기로 한 논의와 서로를 받쳐준다.
- **어제 숨긴 기사도 붙는다** — 숨김은 자정에 풀리므로("어제 부정기사라 보고서에서 뺐던" 바로 그 기사가)
  오늘 정기 보관함엔 다시 보인다. 이 시나리오에서 제일 자주 걸릴 기사가 마침 맞아떨어진다.
- **남겨둔 구멍 하나**: 오늘 숨긴 기사에 오늘 라벨을 붙일 길은 여전히 없다(좌하단 쓰레기통 팝오버·`/hidden`에
  라벨을 안 단다). 사용자 판단으로 안 만들었다 — **"보통 라벨 붙이고 '숨김' 처리하니까 휴지통 자체에서는
  붙일 일 없어."** 순서가 반대라는 것.
- **기각한 대안**: ① 라벨 보관함에 "URL 붙여넣어 추가" — URL을 복사해 올 곳이 결국 정기 보관함이라 두 화면을
  오가게 된다(찾는 곳이 곧 붙이는 곳이어야 한다). ② 확정본을 지난 회차도 열 수 있게 — 핸들러 8곳을 건드려야
  하고 원치 않는 숨김·이동까지 딸려 들어온다. ③ 실시간현황에 라벨 — 당일 0시부터라 "어제"를 못 덮는다.
- **덤으로 고친 것 — 팝오버 CSS/JS가 이미 갈라져 있었다.** 확정본(`app/renderer.py`)과 초안
  (`app/preview_renderer.py`)에 같은 값이 복붙돼 있었는데 diff를 떠보니 **초안 쪽 `onclick` 세 자리에
  `event.preventDefault()`가 빠져** 있었다. 정기 보관함이 세 번째 사본이 되기 전에
  `label_popover_style()`/`label_popover_script(host, port)` 한 곳으로 모았다(`hidden_trash_style`·
  `photo_gather_style`과 같은 패턴). 세 화면이 자리표시자로 받는다.
- **성능 — 재던 게 실제로 문제였다.** `render_article`이 기사마다 `labels_for_url()`로 라벨 파일을 다시
  여는데, 확정본은 33건이라 안 보이던 비용이 정기 보관함은 **494건**이라 드러난다(실측 83ms, 라벨 수에
  비례해 커진다). `render_article(..., labels_lookup=)`을 더해 회차마다 한 번만 읽게 했다 — 그 용도로
  만들어둔 `known_url_labels_lookup`의 docstring이 이미 이 상황을 예고하고 있었다. 결과: 페이지 생성
  29~31ms(494건, 라벨 붙기 전과 같은 수준).
- **검증**: 통합 테스트 29개 통과. 브라우저에서 (1) 확정본·초안 팝오버가 그대로 열리는지(폭 260px = CSS가
  제대로 붙음), (2) 정기 보관함 494개 행 전부에 🏷가 붙고 지난 날짜(9/15) 기사에 실제로 붙여
  `data/labels.json`에 그 회차 스냅샷으로 저장되는지, (3) **지연 로딩 날짜**(9/14, 201행)에서도 같은지,
  (4) 라벨 보관함에 그 행이 `2026-09-15 19:30` 회차로 뜨는지 확인했다. 테스트로 붙인 라벨은 화면의 × 로
  떼어 `data/labels.json`을 원래 6건 그대로 되돌렸다.

## 라벨 보관함 — 복사·txt 추가 + 발행 최신순 (2026-09-15)

**질문(사용자)**: 라벨 보관함에 xlsx만 있는데 「복사」·「txt」도 필요하지 않을까?

- **넣은 이유**: ① 다른 목록 화면(확정본·수집 결과·정기 보관함·수시 보관함)은 전부 복사·txt·xlsx
  세 개가 짝인데 이 화면만 xlsx 하나라 빠뜨린 것처럼 보였다. ② 주제 라벨(`비상경제본부회의`)은
  여러 날 모아뒀다가 보고서에 붙이는 용도라, 이 앱의 붙여넣기 형식(`ㅇ (언론사) 제목 / URL`)이
  맞는 도구다. xlsx는 엑셀을 열어 다시 다듬어야 한다. 반대 근거도 적어 둔다 — 그날 실제 라벨은
  4건·3개뿐이었고 `사진 아님`은 보고서 재료가 아니라 사진 추정 오탐을 적어 두는 메모로 보였다.
  효용이 증명된 건 아니지만 새 엔드포인트 없이(공용 `/download-text`) 만들 수 있어 넣었다.
- **목업**([LABEL_EXPORT_MOCKUP.html](mockups/LABEL_EXPORT_MOCKUP.html))으로 두 가지를 정했다(사용자 결정, 둘 다 추천안):
  - **머리줄 = 라벨명에 소제목 형식을 입힌 것**(`<사진 아님>`). 붙여 넣으면 소제목 한 칸처럼 읽히고,
    소제목 형식을 바꾸면 따라간다. 기각한 안: 라벨명 그대로(태그라서 소제목 모양을 빌리는 게 뜻이
    어긋난다고 볼 수 있지만, 담당자가 붙여 넣은 뒤 괄호를 손으로 다는 일이 생긴다).
  - **순서 = 발행 최신순**(예전엔 라벨 붙인 최신순). 붙인 순서 그대로 보고서에 붙으면 읽는 사람에겐
    뒤섞여 보인다 — `사진 아님`에서도 11:21 기사가 12:11 기사 위에 있었다. 화면과 내보내기는 같아야
    하므로 **화면·xlsx 순서도 같이 바뀌었고**, "방금 붙인 게 맨 위"는 사라졌다(알고 감수). 처음 답에서는
    "화면 순서 그대로"를 권했다가 목업을 실물로 보고 뒤집었다. 언론사 우선순위순은 여러 날이 섞이는
    화면이라 날짜가 뒤죽박죽이 돼 후보에서 뺐다.
- **소제목으로 나누지 않는다** — 라벨 자체가 묶음이고, 회차마다 AI가 이름을 새로 지어 나누면 같은
  사안이 여러 이름으로 쪼개진다(홈 「오늘의 쟁점」이 화면에서 따로 합쳐야 했던 그 문제).
- **전용 빌더를 안 만들었다** — `build_group_copy_texts`에 "라벨 이름을 소제목 이름으로 둔 그룹 하나"를
  넘긴다. 수시 복사에서 URL 뒤 빈 줄이 빠졌던 사고(2026-09-01)가 빌더가 갈려서 났다.
- **덤으로 고친 것**: 예전 xlsx 버튼은 `class="btn ghost sm"`이었는데 이 파일 CSS엔 `a.btn.ghost`(링크용)만
  있고 `.sm`은 없어서, 공용 `button` 규칙대로 큰 파란 채움 버튼(16px)으로 그려지고 있었다. 두 보관함의
  `.exp` 값을 `_LABEL_STYLE`에 옮겨 세 버튼을 같은 모양(12px 회색 테두리)으로 맞췄다.
- **검증**: 통합 테스트(발행 순서·시각 없는 기사는 맨 뒤·복사=txt 텍스트·파일명·xlsx 순서·0건 조합엔
  버튼 없음) 통과, 실제 `data/labels.json`은 테스트 전후 해시가 같다. 앱을 재시작해 실서버에서 txt·xlsx
  응답(파일명·본문)을 확인했다. 복사 버튼의 클립보드 쓰기는 앱 내 브라우저가 권한 자체를 막아
  (`NotAllowedError: Write permission denied`) 거기선 확인하지 못했다 — 두 보관함의 복사와 같은 코드다.

## 사진 추정 모아 보기 — 「전체 선택」 → 한곳에 모아 보기 (2026-09-15)

- **문제**: 사진 추정 기사는 2026-08-21(B안)부터 제자리(소제목 안)에서 흐리게만 보였다.
  골라 숨기려면 「📷 사진 추정 전체 선택」으로 전부 체크한 뒤 목록 전체를 훑으며 오탐의
  체크를 풀어야 했는데, 사진이 여러 소제목에 흩어져 있어 훑는 비용이 컸다. 실측: 정기
  9/1 11:00 회차는 57건 중 12건이 소제목 3곳에, 수시 9/15 「인사청문회」 원본은 117건 중
  62건이 사진 추정이었다.
- **두 안을 실물로 비교했다**(PHOTO_GROUP_MOCKUP.html):
  - **A 맨 아래 한 칸(늘 따로)** — 기각. 숨기기 전까진 보고서엔 원래 소제목으로 나가는데
    화면엔 거기 없어 **화면과 보고서가 어긋난다**. 9/1 11:00의 「인사 인사」는 2건이 전부
    사진이라 화면엔 빈 소제목, 보고서엔 2건이 나간다. 소제목 📋 복사도 화면에 없는 기사를
    복사한다. 오탐을 칸에서 꺼내려면 「제자리로」 버튼과 그걸 기억할 저장소가 따로 필요했다.
  - **B 모아 보기(필요할 때만)** — 채택(사용자 결정). 「보기 순서」(시간순·언론사순)와
    같은 **화면 전용 보기**라 저장 데이터·보고서 텍스트는 그대로다. 오탐은 체크를 풀어 두면
    「원래 화면으로」 누를 때 제자리에 그대로 있다. 서버 변경 없이 버튼 하나 + 화면 전환.
- **정해 둔 것**: 이름은 정기·수시 모두 「📷 사진 추정 모아 보기 (N)」(옛 「전체 선택」
  자리를 대신하고, 전체 선택은 모아 보는 중 띠의 체크박스가 맡는다). 모아 보는 동안엔
  흐림·📷 칩을 뗀다(전부 사진 추정이라 행마다 같은 값 = 장식 — `/keywords`에서 「실시간」
  배지를 뺀 기준). 대신 행마다 원래 소제목 이름표를 단다(보고서에 나갈 자리). 띠는 sticky —
  60건을 훑으며 체크를 풀어도 버튼이 안 사라진다. 숨기기가 화면을 새로고침해도 모아 보기가
  풀리지 않게 회차(수시는 카드)별로 탭 안에만 기억한다.
- **수시엔 선택 바 「🗑 숨기기」가 없었다** — 옮기기·보내기뿐이라 골라서 뺄 길이 없어,
  이 기능이 설 자리가 없었다. `POST /adhoc/card/bulk-hide`(`card.hide_articles`)를 소제목
  통째 숨기기와 같은 규칙(보이는 것만, 한 번의 락, 되돌리기 한 걸음, 숨길 게 없으면 되돌리기
  안 쌓음)으로 새로 만들었다. ADHOC_SELECT_PHOTO_MOCKUP.html ③이 이것이다(그 목업의 ①
  소제목 체크박스·A/B는 아직 결정 전이다).
- **구현에서 걸린 것**: 「보기 순서」가 시간순이면 두 보기가 같은 행을 서로 다른 자리로
  옮겨 끌 때 제자리를 못 찾는다 — 켤 때 소제목별로 돌려놓고 모아 보는 동안 잠근다. 정기의
  🗑 한 건 숨기기는 새로고침 없이 행만 지우므로 띠의 건수를 MutationObserver로 따라 고친다.
  수시는 폼 제출 → 303이라 페이지가 새로 뜨는데, 스크롤 복원 값이 모아 보기 화면 기준이라
  모아 보기를 **스크롤 복원보다 먼저** 되살린다. 초안의 기억 키는 `ROUND_DEADLINE_MS`인데
  그 상수가 이 스크립트보다 뒤에 선언돼 있어 DOMContentLoaded에 판단한다.
- **검증**: 통합 테스트 24(정기 버튼 유무·수시 버튼·bulk-hide가 보이는 것만 숨기고 되돌리기를
  한 번만 쌓음·빈 숨기기는 안 쌓음) 통과. 앱을 재시작해 실서버 화면에서 확인했다 — 수시
  「인사청문회」 63건 모으기·전체 선택·새로고침 뒤 유지·끄면 117건 제자리. 정기는 오늘
  확정본의 사진이 이미 전부 숨겨져 있어, 화면 DOM에서 행 몇 개를 사진으로 표시해 모으기·
  이름표·건수 갱신·끄면 순서 그대로(시간순에서 켠 경우 포함)·0건 「다 치웠어요」를 확인했다.
  실제 숨기기 버튼은 실데이터라 누르지 않았다(수시 bulk-hide는 테스트에서 격리 폴더로 확인).

## AI 기사 나누기 — 확정본·초안 하단바 (2026-09-15)

- **요청**: AI 버튼이 「AI 모든 기사 재분류」(전부 새로)와 「AI 기사 배정」(미분류 → 기존)
  둘뿐이라, 한 소제목이 너무 커졌을 때 그 칸만 쪼갤 길이 없었다. 실측(2026-09-15 14시 초안):
  75건 중 62건이 `<이형일 경제부총리 인사청문회>` 한 칸이었고, 그 안에 비거주 1주택 논란·
  경제정책 구상·**다른 청문회(김승원 후보자)**·대미투자가 섞여 있었다.
- **처음 제안(헤더 버튼) → 사용자 안(체크 → 하단바)으로**: 처음엔 소제목 헤더의 ✂️ 버튼을
  제안했는데, 사용자가 "소제목 전체를 체크하면 연보라 버튼이 생기는" 쪽을 골랐다. 이 안이
  더 나은 점이 하나 있었다 — **일부만 골라 나누기가 저절로 된다.** 규칙이 「고른 것만 나눈다,
  안 고른 건 원래 칸에 남는다」 하나로 정리되고, 전체 선택은 그중 남는 게 0건인 경우일 뿐이다.
  시안: SUBHEADING_SPLIT_MOCKUP.html. 추천대로 확정한 것 넷 — 일부 선택 허용 / 옮긴 행 전체
  연보라 / 확정본에도(초안부터 먼저) / 「기타」도 막지 않음.
- **이름에 「재분류」를 안 쓴 이유**: 버튼 이름은 "무엇을 안 건드리는가"로 갈려야 한다
  (2026-08-26 세 버튼 정리와 같은 교훈). 배정 = 기존 소제목 안 건드림 / 나누기 = 나머지
  소제목 안 건드림 / 재분류 = 전부 새로.
- **저장 경로를 「AI 기사 배정」과 같게 한 이유**: LLM 캐시의 그 소제목 항목을 고쳐 쓰는
  방법도 있었지만, 초안은 새 기사가 들어올 때마다 어느 캐시 항목이 재사용될지(_find_partial)
  달라져 결과가 흔들린다. 배정 기록(group_overrides) + AI가 지은 이름 목록(assigned_groups)은
  마감 때 classify_for_finalize가 이미 읽는 저장소라 확정본까지 그대로 따라간다 — 새 경로를
  만들 필요가 없었다.
- **plan_split에 모은 규칙들**(`app/group_split.py`):
  - 다른 소제목과 이름이 겹친 묶음을 그 칸에 섞으면 "다른 소제목은 그대로"가 거짓말이 된다 —
    섞지 않고 제자리에 남긴다. 「기타」만 예외(받이라 합쳐도 뜻이 같다).
  - AI가 원래 이름을 그대로 쓴 묶음은 제자리 — 여러 쟁점에 두루 걸치는 기사가 원래 칸에
    남는 자연스러운 모양이다(실측에서 2건이 이렇게 남았다).
  - 통째로 골랐는데 새 이름 하나로 돌려준 건 **이름만 바꾼 것**이라 적용하지 않는다.
  - 칸 수 상한(15)을 넘는 묶음은 작은 것부터 제자리로.
- **새 소제목의 자리**: apply_group_order는 저장된 순서에 없는 이름을 맨 뒤에 붙인다 —
  그대로 두면 나뉜 칸들이 목록 끝으로 가서 "그 칸이 나뉘었다"로 안 읽힌다. 원래 이름 바로
  뒤에 끼운 순서를 저장한다.
- **덤으로 고친 것**:
  - 되돌리기 스냅샷에 assigned_groups가 없었다 — 「AI 기사 배정」을 되돌려도 지은 이름이
    목적지로 계속 남아 있었다. 넣으면서, 스냅샷에 키가 **아예 없는** 옛 기록을 되돌릴 때
    `get(key)`의 None이 "그땐 파일이 없었다"로 읽혀 **새 파일을 지우는** 문제도 같이 막았다
    (키가 있을 때만 복원).
  - 배정 기록으로 생긴 소제목(「AI 기사 배정」이 지은 이름)에 🤖 배지가 안 붙었다 — 배지가
    캐시에 있는 이름만 봤기 때문. 초안은 assigned_groups도 더한다.
  - 하단바가 800px에 딱 맞아(실측 800px) 「62개 선택됨」처럼 두 자리가 되면 두 줄로 넘어갔다 —
    「이동할 소제목」 드롭다운이 가장 긴 소제목 이름만큼(199px) 넓어지는 게 원인이라 150px 상한.
  - 확인창·안내 문구에서 이름 바로 뒤 조사(「…청문회」는)를 뺐다 — 받침 유무를 모르면 은/는이 틀린다.
- **검증**: 통합 테스트 25(계산 규칙 8가지, 서버 핸들러의 stale·mixed·too_few·no_split 거부와
  성공 시 저장 3종, 다음 렌더링에서 원래 자리에 나오는지, 되돌리기 옛 기록) 통과. 초안이 막
  17시 회차로 넘어가 기사가 0건이라, 14시 회차 기사로 그린 페이지에서 버튼 켜짐·잠김 두 가지·
  확인창·한 줄 배치를 확인했고, 실서버에 보낸 요청은 stale로 아무것도 안 저장하고 돌아왔다.
  실제 AI 호출(14시 「인사청문회」 70건)은 31초 걸려 8개 새 소제목 + 원래 이름 2건 + 기타 1건 —
  김승원 후보자 청문회 7건이 따로 떨어져 나왔다. 1건짜리 칸(퇴직연금)도 하나 생겼다.
- **확정본에도 붙였다(같은 날)**: 사용자 요청. 버튼 CSS·마크업·JS를 `app/renderer.py`의
  `split_button_style/html/script`로 옮겨 두 화면이 한 곳에서 받는다(쓰레기통과 같은 방식 —
  복제해 두면 한쪽만 고쳐져 어긋난다). 서버는 `_split_group(final=)` 하나로 합치고 "지금 화면의
  소제목"을 구하는 방법만 갈랐다(확정본은 회차 파일의 저장된 배정). **확정본은 회차 파일에서
  옮긴 기사의 group만 직접 고쳐 쓴다** — 다른 큐레이션 핸들러처럼 snapshot_group_names로
  통째로 다시 계산하면 캐시 상태에 따라 다른 소제목이 흔들릴 여지가 생기는데, 이 기능의 약속이
  바로 "다른 소제목은 안 건드린다"라서다. 배정 기록도 같이 남기므로 나중에 다른 동작이
  스냅샷을 다시 계산해도 같은 결과가 나온다. 「AI 기사 배정」 결과 표시(연보라 행)는 원래
  초안 전용이라 확정본에 CSS·표시 코드를 새로 넣었다. 검증: 통합 테스트 25에 확정본 경우
  (일부만 골라 새 이름 하나로 떼기 → 회차 파일에서 옮긴 4건만 바뀌고 나머지·다른 소제목 그대로,
  순서 저장) 추가, 전부 통과. 실서버 확정본(14시, 인사청문회 70건)에서 켜짐·잠김·확인창과
  요청이 `/split-group-final`로 70건을 싣고 나가는 것까지 확인했다(실제 나누기는 보고서
  데이터라 누르지 않고 요청을 가로챘다). 데스크탑 폭에서 「70개 선택됨」이어도 한 줄이다.

## 수시 4단 흐름 — 원본은 로데이터, 확정본은 저절로 (2026-09-15)

- **문제의식(사용자)**: 수시는 「새 수집 → 수집 원본 → 수집 확정본 → 수시 보관함」이어야
  사고방식이 맞는데, 지금은 원본과 확정본이 **둘 다 소제목 배정·숨기기를 들고 있어 겹쳐
  보였다**. 원본은 「조건에 걸린 걸 다 끌어오는」 곳이라 실시간현황처럼 로데이터여야 하고,
  판단(고르기·소제목·숨기기)은 전부 확정본에서 해야 한다. 새 수집은 조건을 정하는 창인데
  지난 1년간 무엇을 모았는지 단어로 보여주면 좋겠다.
- **먼저 따져본 대안 — 새 수집에 로데이터를 띄우고 원본을 없애기**: 기각. 새 수집은 카드가
  생기기 전의 화면(빈 출발점 + 지난 조건 목록)이고 원본은 카드 하나의 결과라 한 화면이 두
  역할을 하게 된다. 정기에서도 검색어 설정과 실시간은 따로다. 겹쳐 보인 원인은 화면이 둘이라서가
  아니라 원본이 정리 도구를 들고 있어서였다 — 그걸 빼면 원본/확정본 경계는 정기의
  실시간 ↔ 초안·확정본 경계와 같은 선이 된다.
- **실측(카드 26장)**: 원본 25장 중 소제목을 쓴 건 1장, 숨김은 3장 — 원본에서 판단 도구를 빼도
  잃는 게 거의 없다. 원본 3장은 150건을 넘어(178·183·517) 원래 AI 소제목 분류가 안 됐다 —
  골라낸 일부만 다루는 확정본으로 옮기면 오히려 잘 돈다. 확정본(모음)은 1장뿐이고 비어 있었다
  — 「빈 확정본부터 만들어야 하는」 문턱이 안 쓰인 원인으로 보고, 처음 보낼 때 저절로 생기게 했다.
- **정한 것(사용자 결정)**:
  - 원본에서 봤는데 안 보낼 기사는 **그냥 목록에 남는다**(로데이터 — 실시간처럼). 「봤음/안 봄」은
    노랑(마지막 불러오기의 새 기사)과 「아직 안 보낸 기사만」이 가른다. 한계: 확인하기 전에 두 번
    연속 불러오면 앞 것의 노랑이 사라진다 — 감수.
  - **확정본은 자동으로**: 같은 날·같은 사안의 확정본으로 가고, 없으면 원본 이름·원본 사안으로 그
    순간 만든다. 사안으로 묶는 이유는 같은 날 같은 사안의 원본이 여럿이어도(9/2 「이형일」 3장)
    보고서는 하나여야 자연스러워서다. 여러 원본을 한 확정본에 모으는 길(▾ 메뉴)은 남겼다.
  - **원본에도 xlsx는 남긴다** — 로데이터를 통째로 뽑는 건 원본에 어울린다. 복사·txt는 소제목이
    붙는 보고서 형식이라 확정본에만.
  - **확정본 머리줄에 원본 시각(B안)**: 받는 사람이 언제 기준인지 알아야 해서. 시안
    ADHOC_BUNDLE_HEADER_MOCKUP.html에서 A(지금대로)·B(가장 늦은 원본 시각)·C(원본 시각 전부)·
    D(화면 칩에만)를 원본 하나/둘/더 불러왔지만 안 보냄 세 상황으로 비교했다. 시각은 **보낸 순간의
    원본 기준 시각**을 기사마다 적어 두고 그중 가장 늦은 것 — 원본의 지금 시각을 따라가면, 원본을
    더 불러오기만 해도 확정본 머리줄이 바뀌어 거짓말이 된다. B의 흠(12:30까지만 모은 원본이 섞이면
    그쪽을 살짝 부풀림)은 알고 감수했다.
  - **보관함엔 확정본만, 옛 원본 25장은 그대로**: 새 규칙대로면 보관함이 텅 빈다(확정본이 사실상
    0장). 옛 원본은 소제목·숨김까지 하던 「원본 겸 확정본」이라 그대로 두고, 새로 만드는 원본에만
    `raw` 표식을 달아 새 규칙을 적용한다(옛 카드를 옮겨 적지 않는다 — 담당자가 정리한 흔적이
    그대로 남아야 한다).
  - **아이콘**: 새 수집 🔎(원본이 쓰던 돋보기를 넘겨받음), 원본 📃(단일 문서). 📃는 설정의 「기사
    제목 형식」「소제목 형식」에도 쓰이지만 서로 다른 화면이라 둔다.
- **홈 흐름도(D안)**: A(번호순 4칸 — 원본이 초안 밑이라 「원본 = 초안」으로 읽힘) · B(새 수집만
  앞으로 튀어나온 입구, 확정본 두 칸 폭) · C(정기 입구에 검색어, 5칸 대칭) · D(C + 실시간을 초안
  위 부록으로) · E(실시간 한 줄 전체 — 정기·수시 둘 다에 걸친 것으로 읽혀 기각) · F(실시간과
  원본을 같은 열에, 점선/실선으로 필요 정도만 다르게, 초안+확정본을 한 덩어리로)를 차례로 봤다.
  사용자가 짚은 핵심: **초안은 실시간을 거쳐 모이는 게 아니다**(정기 검색이 직접 채운다) — 그래서
  「실시간 → 초안」 화살표는 사실이 아니었다. D로 결정: 두 줄이 4:4로 맞아 C의 넓은 칸 문제도
  사라진다. F는 원본↔실시간의 대칭을 살렸지만 사용자가 D를 골랐다.
- **덤으로 고친 것**:
  - 카드 화면을 열 때마다 `cards_for_date`가 저장된 카드 **전부**를 파싱했다(1년치면 초 단위).
    요약 캐시(`list_card_summaries`, 파일 서명이 같으면 재사용)로 그 날짜 카드만 연다. 새 수집의
    지난 목록도 이 캐시를 쓴다.
  - 지난 원본을 다시 열면 무엇을 보냈는지 안 보였다(보낸 표시를 오늘 카드에서만 셌다) — 그 카드
    날짜의 확정본으로 센다.
  - 원본에서 처음 보낼 때 같은 이름의 확정본이 저절로 생기면서 탭 줄에 「인사청문회 183건」·
    「인사청문회 12건」이 나란히 서게 됐다 — 탭은 같은 종류끼리만.
  - 지난 원본의 「오늘 날짜가 아니에요」 경고에 「이 조건으로 오늘 새로 모으기 →」를 달았다
    (`/adhoc/new?from=` — 지난 수집 줄을 누른 채로 연다).
  - 홈의 입구 칸 점선이 안 보였다 — 뒤에 오는 `.tile`이 테두리를 transparent로 다시 정해 한
    클래스짜리 규칙이 졌다. `.tile.t-regkw`처럼 올렸다.
- **검증**: 통합 테스트 26(원본 화면에서 빠진 것들·최신순, __auto__로 확정본 생성·같은 사안 재사용,
  머리줄 시각·숨김 반영·시각 없는 옛 기사, 보관함엔 확정본·옛 원본만, 지난 수집 ×N·?from=) 추가,
  18·24는 옛 원본/확정본으로 옮겨 전부 통과. 실데이터로 새 수집의 지난 목록(16줄)·줄 클릭 채우기를,
  오늘 「인사청문회」 원본(183건)을 임시 폴더에 복사해 원본(한 목록 117건·「확정본 7건 →」·▾ 메뉴·
  선택 바)과 자동 생성된 확정본(「13시 05분 기준」)을 확인했다. 앱은 새 코드로 다시 띄웠다.

## 검색어 — 그룹 스위치 + 정기 체크 → 목적지 스위치 두 개 (2026-09-15)

- **요청**: `/keywords` 카드의 머리줄 on/off 스위치와 아랫줄 「☑ 정기 스크랩」이 갈라져 보인다 —
  「실시간 on/off」·「초안·확정본 on/off」를 나란히 두자. 홈 흐름도를 실시간 → 초안 → 확정본으로
  정리하고 있어 검색어 화면도 같은 말을 쓰는 게 맞다는 판단(사용자).
- **진단**: 두 컨트롤은 사실 한 질문(이 그룹이 어디에 쓰이나)의 답을 두 자리에 나눠 들고 있었다.
  계층(`enabled` ⊇ `include_in_scrap`)이라 네 조합 중 「꺼짐 + ☑정기」는 **정기에 체크돼 보이는데
  어디서도 검색되지 않는** 뜻 없는 상태였다. 맨 위 안내 첫 줄(「켜 둔 그룹은 모두 실시간…그중
  ☑정기 스크랩을 체크한 그룹만…」)은 이 계층을 풀어 쓰려고 있던 문장이다.
- **결정**: 목적지 스위치 두 개를 서로 독립으로(`include_in_live` 새 필드 + 기존 `include_in_scrap`).
  네 조합이 전부 뜻이 있고, 「초안·확정본만」이 새로 생긴다 — 실시간을 뒤덮는 넓은 검색어를
  실시간에선 빼고 보고서에만 남기는 쓰임. 연동(초안·확정본을 켜면 실시간도 켜짐)은 안 했다 —
  스위치 하나가 다른 스위치를 움직이면 "갈라져 보인다"가 모양만 바꿔 돌아온다.
- **시안** [KEYWORDS_TWO_SWITCH_MOCKUP.html](mockups/KEYWORDS_TWO_SWITCH_MOCKUP.html): A(머리줄 이름표 스위치 둘) ·
  B(머리줄 ✓ 알약 둘 — 9/11 「머리줄 정기 알약」을 기각한 이유(그룹 스위치와 헷갈림)는 사라졌지만
  바로 아래 키워드 칩과 모양 계열이 비슷했다) · C(아랫줄 체크 둘 — 가장 적게 바뀌지만 훑을 때 덜 걸림).
  이름은 `초안·확정본` / `정기 스크랩` / `보고서`를 비교. 사용자가 **A · 초안·확정본**을 골랐다.
- **옛 저장분 옮기기가 이 변경의 함정이었다**: `enabled`를 그대로 "실시간"으로 다시 읽으면 뜻이
  바뀐다 — 옛 `enabled=False`는 "둘 다 끔"이라, 정기 체크가 남은 꺼진 그룹이 옮기는 순간 정기에
  들어간다. 그래서 `초안·확정본 = 켜짐 AND 정기 체크`로 옮기고, 읽는 곳은 전부 `group_in_live`·
  `group_in_scrap`·`group_in_use` 헬퍼를 거치게 했다(옛 형식 dict도 옛 규칙으로 읽는다). `enabled`는
  `live or scrap`을 저장만 해 두는 파생값으로 남겼다 — 옛 코드로 되돌려도 초안·확정본 그룹이 그대로
  수집되고, 「어딘가에 쓰이는 그룹」이 필요한 두 곳([단독]·[속보] 감시 그룹, 추이의 등록 검색어
  칩)이 그 뜻 그대로 읽는다. 실데이터(5개 그룹) 옮긴 결과: 전부 실시간 켬, 초안·확정본은 예전 정기
  체크 그대로(재정경제부·재경위) — 달라진 수집은 없다.
- **덤으로 바로잡은 것**: 저장 바의 「정기 스크랩엔 다음 회차부터」는 초안까지 뭉뚱그렸다. 초안은
  요청마다 다시 그려져(`/preview.html` → `generate_preview_page`) 새 검색어로 곧바로 다시 모은다 —
  「실시간·초안엔 바로, 확정본엔 이번 회차 마감부터」. 저장 차단도 두 규칙(사용 중 ≥1, 정기 ≥1)이
  「초안·확정본 ≥1」 하나로 줄었다(그게 있으면 사용 중 ≥1은 저절로 참).
- **검증에서 걸린 것**: 648px 창에서 카드 두 장씩 놓으니 이름표 스위치 둘(약 180px)에 밀려 그룹명이
  「재정경」 「재경!」처럼 두세 글자로 잘렸다 → 두 장씩은 780px 초과에서만, 그룹명 칸 최소 5em.
  폰 폭(375px)에선 🗑만 혼자 아랫줄로 떨어져 → 스위치 둘 + 🗑를 한 묶음(`.head-ctrl`)으로.
  통합 테스트 27(옛 형식 옮기기 — 특히 「꺼짐 + 정기 체크」가 새지 않는지, 초안·확정본만 그룹이
  확정본엔 들어가고 실시간엔 안 들어가는지, 둘 다 끈 그룹, 저장 차단 문구) 추가, 전부 통과.
  앱은 새 코드로 다시 띄웠다.

## 수집 원본 🗑 · 전부 보내기 · 불러올 때마다 새 확정본 (2026-09-15)

- **제보(사용자, 직접 써 보고)**: "나도 모르게 수집 원본에서 삭제하려고 한다" — 정기와 수시가 내용은
  달라도 비슷하게 가야 할 것 같다. 처음 제안은 「원본 = 실시간 역할(안 봐도 됨) + 수집 초안 +
  수집 확정본」이었다.
- **진단**: 손이 간 이유는 초안이 없어서가 아니라 **원본에 실시간의 🗑가 빠져서**였다. 정기에서 기사가
  들어오는 두 화면(실시간·초안)은 둘 다 행마다 🗑가 있다(실시간은 목록에서 안 빼고 「🗑 숨김」 표시만).
  같은 날 원본을 로데이터로 바꾸며 숨기기를 뺄 때 실시간에 있던 🗑까지 같이 빠졌다.
- **「수집 초안」을 기각한 이유**: (1) 정기의 실시간과 초안은 내용이 다르다(검색어 그룹 스위치·선택
  언론사·같은 제목 접기·사진 제외가 초안에만). 수시 카드는 조건이 하나라 원본과 초안이 거의 같은
  목록이 돼, 오전에 없앤 "두 화면이 겹쳐 보인다"가 돌아온다. (2) 정기의 초안 → 확정본은 **회차 마감이
  저절로** 넘기는 시간 경계다. 수시엔 마감이 없어 「확정」 버튼을 새로 만들어야 하고, 같은 이름에 다른
  동작이 붙는다 — 📌 담아두기를 없앤 것과 같은 함정이고, 사용자가 걱정한 "정기 쓰는 사람이 헷갈린다"의
  실체가 이것이다.
- **시안** [ADHOC_RAW_TRASH_ROUND_MOCKUP.html](mockups/ADHOC_RAW_TRASH_ROUND_MOCKUP.html) — 원본·확정본·보관함을
  한 화면에서 실제로 눌러 보는 시안. 비교한 두 축:
  - **확정본 나누기**: 하나로 계속(그때까지의 동작) / **보낼 때마다**(사용자의 두 번째 제안 — 15시에
    보낸 것과 16시에 보낸 것을 따로) / **불러올 때마다**. 「보낼 때마다」는 보내기가 기사 한 건씩 누르는
    동작이라 12건 보내면 확정본 12개가 생기고, 탭 이름(보낸 시각)과 머리줄(원본 시각)이 서로 달라져
    기각. 경계를 「지금까지 불러오기」에 두면 정기의 회차 마감과 같은 자리가 된다 — 시계 대신 담당자가
    정할 뿐. 사용자가 **불러올 때마다**를 골랐다.
  - **확정본에서 뺀 기사가 원본에서**: 「🗑 숨김」(정기에서 초안·확정본에서 숨긴 기사가 실시간에
    「🗑 숨김」으로 보이는 것과 같은 뜻) / 「처리 안 함」으로 복귀(그때까지의 동작). 복귀로 두면 다음
    「전부 보내기」에 방금 뺀 기사가 다음 확정본으로 도로 끌려간다. 사용자가 **「🗑 숨김」**을 골랐고,
    "원본에서는 기사가 사라지지 않고 실시간처럼 숨겨졌다는 표시만"을 다시 확인했다.
- **사안명에 번호·보낸 시각을 붙이자는 안**도 기각 — 확정본 머리줄의 「16시 10분 기준」이 이미 그
  확정본의 이름이고, 사안명은 보고서 첫 줄로 나가는 글자라 기계가 붙인 「인사청문회 2」가 섞이면
  안 된다(「기타2」를 안 만든 것과 같은 이유). 목록(탭·▾ 메뉴·보관함 줄·원본 툴팁)에서만
  「인사청문회 15:05」로 곁들인다. 덤으로 머리줄 시각(B안)에서 알고 감수했던 흠(12:30 원본 기사가
  「13시 05분 기준」에 섞임)이 기본 흐름에선 사라진다.
- **구현에서 정한 것**:
  - 원본 표시는 기사의 `hidden`이 아니라 카드의 `raw_marks`에 적는다 — `hidden`은 조건 필터·「목록 밖
    기사」가 보는 값이라 쓰면 기사가 목록에서 사라진다.
  - 확정본에서 뺀 기사를 원본에서 되돌리면 `shown`을 **시각과 함께** 적고, 확정본 사본엔 숨긴 시각
    (`hidden_at`)을 남긴다. 되돌린 뒤 확정본에서 **다시** 빼면 원본에서 다시 「숨김」이어야 해서다(시각
    없이 표시만 적으면 한 번 되돌린 기사는 영영 「처리 안 함」으로 남는다).
  - 원본에서 되돌릴 때 확정본 카드는 안 건드린다 — 원본 화면의 ↩ 되돌리기는 그 카드 하나만
    스냅샷하므로, 다른 카드까지 고치면 되돌리기가 반쪽이 된다.
  - 확정본에서 뺀 기사를 다시 보내면 **되살린다**. 그 전엔 「같은 URL은 1건」 규칙에 걸려 조용히
    건너뛰어, 원본에선 보냈는데 확정본엔 빠진 채였다(이번 흐름에서 처음 밟히는 길이라 같이 고쳤다).
  - 옛 확정본(시각 필드 없음)은 담긴 기사의 원본 시각으로 대신 맞춰, 같은 날 먼저 쓰던 확정본을
    그대로 이어 쓴다.
- **검증에서 걸린 것**: 「전부 보내기」 확인창 문구에 `\n`을 한 겹만 적어 **카드 화면 스크립트 전체가
  죽었다**(f-string 안의 JS라 `\\n`이어야 한다 — 같은 파일에 이미 경고 주석이 있던 함정). 통합 테스트는
  JS를 안 돌려 못 잡았고, 실제 화면 코드로 그린 페이지를 브라우저에 열어 콘솔에서 찾았다. 렌더링한
  다섯 화면의 스크립트를 `node --check`로 전부 확인했고, 테스트에 그 문구의 이스케이프를 막는 줄을 넣었다.
  통합 테스트 28(원본 🗑 표시·되돌리기, 전부 보내기가 표시 없는 기사만, 확정본에서 뺀 기사의 원본
  「숨김」·되돌리기·되살리기·다시 빼기, 불러온 뒤 새 확정본·같은 시각은 그 확정본, 탭·보관함 시각) 추가,
  26은 「두 번째 원본도 같은 확정본」 대신 ▾로 고른 확정본으로 보내게 고쳐 전부 통과. 앱은 새 코드로
  다시 띄웠다.

## 수시 칸 이름 — 「수집」 떼기: 원본 · 확정본 (2026-09-15)

- **제보(사용자)**: 홈 흐름도의 수시 줄 이름이 안 와닿는다. 처음 제안은
  `검색어 > 초안 > 확정본 > 정기함` / `새 수집 > 결과 > 보고용 > 수시함`.
- **진단**: 안 와닿는 진짜 이유는 수시 줄이 `새 수집 · 수집 원본 · 수집 확정본`으로 「수집」을
  세 번 말해 칸마다 무게가 같아 보인 것. 3차(2026-09-02)에 「수집」을 붙인 이유(맨몸 `확정본`이
  정기와 헷갈린다)는 지금 두 자리에서 이미 풀려 있다 — 수시 화면엔 정기를 가리키는 말이
  하나도 없고(2026-09-02), 홈 흐름도에선 줄 이름표 `정기`/`수시`가 가른다(같은 날 4차).
- **기각한 것**:
  - `결과` — 오늘 원본을 「거르지 않은 로데이터」로 바꾼 게 핵심인데 "여기가 끝"으로 읽혀,
    `결과 > 보고용`이면 결과물이 둘이 된다.
  - `보고용` — 수시엔 마감이 없어 「확정」이 어색하다는 지적은 맞지만, 같은 열의 정기 확정본도
    보고용이라 짝으로 안 읽히고(한쪽은 문서 상태, 한쪽은 용도), 꾸미는 말이라 탭·칩에 혼자
    서면 어색하다(`오늘의 보고용`). 「확정」은 마감이 아니라 담당자가 골라 확정한 것이라
    수시에도 성립한다고 봤다.
  - `정기함`/`수시함` — 줄 이름표와 같은 말을 두 번 하고(4차에서 바로 그 이유로 둘 다
    `보관함`), `수시함`은 "수시로 함"으로도 읽힌다.
- **결정(사용자)**: `정기 │ 검색어 · 초안 · 확정본 · 보관함` / `수시 │ 새 수집 · 원본 · 확정본 · 보관함`.
  두 줄에서 다른 이름은 `초안 ↔ 원본` 하나뿐이고, 그게 실제로 두 흐름이 다른 지점(마감)이다.
- **바꾼 자리**: 홈 흐름도 칸 이름·말풍선, 수시 상단바 링크(`원본`/`확정본`), 탭 머리(`오늘의 원본`/
  `오늘의 확정본 · …`), 버튼 툴팁·확인창·오류 문구, 수시 보관함 부제·빈 화면, 「새 확정본 만들기」 폼.
  **두 흐름이 한 화면에 나란히 서는 라벨 보관함 빈 화면 안내만** `정기 초안·확정본, 수시 확정본`으로
  흐름 이름을 붙였다(맨몸이면 `확정본·초안·확정본`이 된다). 코드 주석·이 문서의 옛 기록은 안 고쳤다.
  테스트 문구 3곳을 새 이름으로 맞춰 28개 전부 통과, 앱을 다시 띄워 홈·수시 카드 화면을 확인했다.

## 숨긴 기사 — 자정 해제를 없애고 7일 유지로, 행은 `[원문][복구]` 두 칸으로 (2026-09-16)

**시작은 화면 지적이었다.** `/hidden`을 보던 담당자가 "지금 「복구」와 「원문」이 혼용돼.
이러면 안 돼, 나란히 있어야 돼"라고 짚었다 — 오늘 구획 행 끝엔 「복구」, 지난 날짜 구획
행 끝엔 「원문」이 **같은 자리에 번갈아** 놓여 한 목록의 오른쪽 끝이 두 가지 말을 하고
있었다. 시안 셋을 만들어 비교했다([HIDDEN_ROW_ACTIONS_MOCKUP.html](HIDDEN_ROW_ACTIONS_MOCKUP.html)):
A안 지난 행에 복구 폭만큼 빈 칸을 남겨 원문 세로줄 맞추기 / B안 빈 칸 없이 오른쪽 끝에
붙이기(구획이 바뀌면 원문이 복구 자리로 옮겨 앉아 문제가 절반 남는다) / C안 지난 행에도
복구를 흐리게 그려 모양을 완전히 맞추기. **담당자는 C안 + 호버 사유 설명을 골랐다.**

**그 다음 질문이 진짜였다** — "근데 복구는 자정 넘으면 못 하게 막혀 있는 거야? 안 막을 순
없어?" 막아둔 게 아니었다. 숨김은 2026-07-26부터 "오늘 화면만 정리하는" 하루짜리 필터라
(`_load_records(days=1)`) 자정이 지나면 **저절로 풀렸고**, 그래서 지난 날짜 줄은 되살릴
것이 없는 기록이었다(2026-09-04에 열람만 7일로 늘리며 그 경계를 화면에 그렸다). 담당자의
머릿속 모형은 "쓰레기통에 있으면 언제든 되살린다"였고, 실제 화면이 그 말을 배신하고 있었다.

**재보고 판단할 거리 셋을 정리해 물었다.**
- **덤으로 맞아떨어지는 것**: 정기 보관함이 `filter_hidden`을 거치므로, 자정에 숨김이
  풀리면 *어제 실제로 발송한 보고서에는 없던 기사가 오늘 보관함에는 다시 보였다* —
  2026-09-10에 소제목 순서로 고쳤던 "보관함 텍스트가 발송본과 다르다"와 같은 종류의 어긋남.
- **뒤집는 것**: 2026-09-04에 "필터까지 늘리면 어제의 숨김 결정이 오늘 화면으로 샌다"고
  적어둔 그 결정을 되돌리는 셈이다. 다만 회차 수집이 당일(KST) 기사만 담으므로 같은 URL이
  다음 날 회차에 다시 잡히는 경로는 사실상 없다.
- **못 하는 것**: 영구 보관. `app/undo.py`가 `hidden_articles.json`을 통째로 20단계
  복사한다 — 실측으로 지금 7일치가 531건·172KB인데 `undo_stack.json`이 이미 9.5MB다.
  1년치(환산 약 3.6만 건·11MB)면 되돌리기 스택이 200MB대가 되어, 2026-08-25에
  `group_overrides`를 1년 → 30일로 줄인 그 사고가 그대로 재발한다.

**담당자 결정: 7일 유지.** 유지 기간과 열람 기간을 **같은 상수**(`HIDDEN_VIEW_DAYS`)로
묶어, "쓰레기통에 보이는데 못 되살리는 줄"이 원리적으로 안 생기게 했다. 8일째엔 기록이
만료돼 그 기사가 다시 보인다 — 경계가 없어진 게 아니라 자정에서 7일로 옮겨진 것이고,
그 사실은 화면 안내에 그대로 적었다.

**같이 뒤집힌 규칙 둘.** 2026-09-04의 "중복 검사와 복구는 오늘 기록만 본다"는 자정 해제를
전제로 한 장치였다 — 3일 전 기록 때문에 오늘의 🗑️가 먹통이 되는 것을 막으려던 것인데,
이제 그 3일 전 기록은 여전히 살아 있어 그 기사가 애초에 화면에 없다. 그대로 뒀으면
**같은 기사가 쓰레기통에 두 줄로 쌓이고 한 줄만 복구해도 안 돌아왔을 것**이다. 그래서
중복 검사는 읽어온 기록 전체를 보고, 복구는 날짜를 안 가리고 그 URL의 기록을 지운다.

**화면에서 조용히 깨질 뻔한 것**: `restorable`(되살릴 수 있나)과 `is_today`(「오늘」 칩·미리
펼치기)가 예전엔 같은 값이라 한 변수로 쓰이고 있었다 — 유지 기간만 늘리고 그대로 뒀으면
7일치 **전부에 「오늘」 칩**이 붙는다. 두 축으로 갈랐고, 되살릴 수 있는 날짜의 기준은 화면이
따로 계산하지 않고 판정과 같은 헬퍼(`app.curation.active_hidden_dates`)에서 받는다.

**행 버튼**(C안): 모든 줄이 `[원문 ↗][복구]`다. 원문은 아이콘만 — 2026-08-14에 확정본에서
↗를 뺐던 이유(↗와 복사 아이콘이 17px에선 같은 "네모 두 겹" 실루엣)가 이 행엔 다른 아이콘이
없어 걸리지 않고, 앱 관례(글자 = 주 동작, 아이콘 = 가끔 한 번)에도 맞는다. 잠긴 복구는
`disabled` 속성이 아니라 **`aria-disabled` + 흐림**이다 — `disabled` 버튼은 브라우저가
마우스 이벤트를 주지 않아 정작 "왜 못 누르는지"를 말할 `title` 툴팁이 안 뜬다(확정본
「AI 기사 나누기」 잠김이 쓰던 그 방식). 지금 설정에선 잠기는 줄이 아예 안 나오지만,
두 상수가 갈리는 날 화면이 거짓말하지 않도록 경로는 남겼다.

**↩ 아이콘을 되돌리기 전용으로 비웠다.** 복구 버튼 네 자리(`/hidden`의 행·묶음 헤더·일괄
복구 바, 확정본·초안 좌하단 팝오버)에서 ↩를 뗐다 — 특히 팝오버는 좌하단 ↩ 되돌리기 FAB
**바로 위**에 떠서, 같은 그림이 40px 거리에서 "직전 동작 취소"와 "이 기사 되살리기" 두
뜻으로 쓰이고 있었다. 팝오버의 묶음 버튼 라벨도 `모두` → `모두 복구`로 고쳤다(아이콘이
빠지자 「모두」 혼자로는 무엇을 하는지 말하지 못했다).

**검증**: 통합 테스트 30개 통과(`test_hidden_articles_newest_first`를 새 규칙으로 다시
씀 — 자정 이후에도 숨김·기록 유지, 8일째 판정·열람 동시 만료, 다음 날 또 숨겨도 기록이 두
줄로 안 늘어남, 복구는 날짜를 안 가림). 앱을 다시 띄워 실제 `/hidden`(오늘 28건 + 9/10~15
503건)에서 오늘·지난 날짜 구획 모두 `[↗][복구]`가 같은 자리에 서고 지난 구획 복구가 실제로
눌리는 것을 확인했다.

### 같은 날 2차 — 복구를 글자에서 ↩ 아이콘으로 되돌림 (2026-09-16)

위 변경을 사파리에서 실제로 띄워 본 담당자가 **"복구 글자도 되돌리기 svg로 바꿔줘"**라고
했다. 그래서 한나절 전에 세운 "↩ = 되돌리기 전용, 복구는 글자만" 규칙은 폐기됐다 — 화면에서
보니 `[↗][복구]`가 아이콘 하나 + 글자 하나라 두 칸의 무게가 안 맞았고, 오른쪽 묶음이 한
덩어리로 안 읽혔다. 지금은 `[↗][↩]`로 같은 크기·같은 상자다.

**대신 규칙을 하나 더 좁혔다 — 글자는 「한 번에 여러 건」일 때만 붙인다.** 기사 한 건을
되살리는 자리(행, 팝오버 낱개 줄)는 아이콘만, 여러 건을 한꺼번에 되살리는 자리(묶음 헤더
`↩ 모두 복구`, 선택 바 `↩ 선택한 기사 복구`, 팝오버 묶음 줄)는 아이콘 + 글자다. 거기선
"모두"·"선택한"이 장식이 아니라 실제로 무엇이 되살아나는지를 가르는 정보라서다.

**포기한 근거**: 좌하단 ↩ 되돌리기 FAB과 복구 버튼이 같은 그림을 쓴다는 것(팝오버에선
40px 거리다). 다만 그 화면 안에서 ↩는 언제나 "되살린다"이고, 되돌리기 FAB은 방금 한
동작을·복구 버튼은 그 줄의 기사를 되살린다 — 대상이 다를 뿐 동작의 뜻은 같은 방향이라
담당자 판단을 따랐다. 아이콘 전용 버튼에는 전부 `aria-label`을 달았다(글자가 없으므로).

작업 중 실수 하나: `.undo-btn`을 글자 전용으로 바꾸며 `gap: 4px`를 빼놨던 탓에 아이콘을
되살리자 「↩모두 복구」로 붙어 나왔다 — 화면을 눈으로 보고 잡았다.

## 화면 여백 정리 — 실시간 상단·기사 행·소제목 간격·보관함 기간 바 (2026-09-16)

사용자가 "전체적으로 여백이 많고 낭비되는 부분이 있다"고 해서 주요 화면 7개를 1440×900에서
실측해 다섯 군데를 제안했고, 목업(A/B/C)을 만들어 비교한 뒤 **네 곳을 B안으로** 골랐다
(시안 파일: `LIVE_URL_LINE_MOCKUP.html`, `LIVE_TOP_COMPACT_MOCKUP.html`,
`ARTICLE_ROW_DENSITY_MOCKUP.html`, `SUBHEAD_SPACING_MOCKUP.html`,
`HISTORY_PERIOD_BAR_MOCKUP.html`).

**먼저 기각한 두 가지**
- **화면 폭 넓히기(800px → 더 넓게)**: 좌우 여백이 가장 크게 느껴지는 자리지만, 실측상
  확정본 제목 줄은 751px에서 끝나(자리가 남는데 안 쓴다) 넓혀도 가운데 공백만 커진다.
  낭비는 전부 세로에 있었다.
- **실시간현황 URL 줄 → 「원문보기 ↗」(1번)**: 한 건 83 → 65px로 가장 크게 줄지만
  **현행 유지**(사용자 결정). 2026-08-13에 "원문 보기는 접근이 빨라야 한다"로 남긴 줄이다.
  확정본·초안(08-14)·수시(08-19)는 이미 같은 변경을 했으므로, 실시간만 URL 줄이 남는 것은
  알고 둔 상태다.

**채택한 네 가지 (전부 B안)**
1. **실시간현황 상단** — 건수 카드(`.stat-card`, 59px + 여백 20px)를 없애고 그 문장을
   「● 실시간」 옆으로(`784건 · 0시~16:49 누적 · 전체 언론사`), 체크박스 상자와 검색·그룹 칩
   상자(41 + 93px + 사이 35px)를 **한 상자 두 줄**로. 기사 앞 영역 433 → 288px(4번 포함).
   C안(경고 배너를 한 줄로 접기)은 경고가 뜬 날에만 22px을 더 버는데 경고 본문을 한 번 더
   눌러야 봐서 뺐다. `stat-count`·`stat-scope` id는 그대로라 필터가 건수를 예전처럼 고쳐 쓴다.
   제목 검색칸은 체크박스 줄 앞으로 옮겼지만 **"등록된 검색어 그룹이 있을 때만"이라는 조건은
   그대로**다(`_title_search_html`) — 그룹이 0개인 화면은 예전처럼 검색칸이 없다.
2. **기사 행** — 한 건 74 → 64px. **처음 진단이 틀렸다**: 메타 줄이 24px 줄높이를 물려받아
   비는 줄 알았는데 메타 글자는 이미 `line-height:19.2px`였고, 액션 줄 23.5px은 오른쪽
   **버튼 묶음**이 만드는 높이였다. 목업을 실서비스 값에 맞추다가 A안이 4px 낮게 나와서
   알아냈다(summary의 `flex gap:4px`를 빠뜨렸었다). 그래서 버튼·글자는 안 건드리고 여백 세
   개만 줄였다 — `padding 6→4px`, summary `gap 4→2px`, `margin 10→6px`. C안(2px/0/4px, 56px)은
   제목과 메타가 붙어 뺐다. **CSS가 네 파일에 복제돼 있었다**(확정본·초안·정기 보관함·수시) —
   `render_article`을 공유하니 한 곳만 고치면 된다고 처음엔 말했는데, 공유하는 건 마크업뿐이고
   스타일은 각자 들고 있었다. 수시는 2026-09-02 "정기와 같은 값" 결정에 따라 같이 바꿨다
   (수시 한 건 76 → 66px, 구조 차이로 정기보다 2px 큰 건 원래부터다).
3. **소제목 간격·카드 위 여백** — 소제목 사이 `28→20px`, 카드 위 `padding-top 60→44px`.
   **넷 중 효과가 가장 작다**(소제목 4개 회차 −48px)는 걸 목업에서 재고 밝혀둔 채 골랐다 — 2번과
   겹쳐야 체감된다. 60px은 상단 고정바(54px)를 피하려는 값이라 글자 위로 30px이 비고 카드의
   둥근 모서리는 상단바 뒤에 숨어 있었다. **일곱 화면**(확정본·초안·실시간·정기 보관함·설정·
   수시·정책 단어 추이)이 같은 값을 써서 전부 같이 바꿨다 — 한 곳만 바꾸면 화면을 오갈 때 제목이
   16px씩 들썩인다. 수시·추이는 `84px` 한 값으로 적혀 있어 `68px`이 됐다.
4. **보관함 기간 조회 바** — 정기 101 → 52px. 두 줄을 만들던 건 안내 문구 둘
   (`날짜 기준으로 조회합니다` / `시작일 ~ 종료일을 직접 넣고 조회`)과 두 줄 사이 구분선이었다.
   앞 문구는 「기간」 라벨이, 뒤 문구는 날짜 칸 두 개와 `~`가 이미 한 말이라 뺐다(본업 화면은
   자기를 해명하지 않는다). C안(직접 지정 접기)은 **만들어 재보니 B안과 높이가 같아서**(어차피 한
   줄) 클릭만 늘어 기각했다. **수시 보관함은 날짜 입력이 별도 `<form>`이라** 빠른 설정 줄에 그냥
   넣을 수 없었다 — 날짜 칸·조회 버튼을 기간 줄로 옮기고 `form="adhoc-range-form"` 속성으로
   숨긴 폼에 묶었다(빠른 설정은 숨은 start/end, 직접 입력은 입력한 start/end를 보내야 해서 폼을
   나눠 둔 예전 이유는 그대로). 수시의 `비워두면 이 기간의 모든 사안`(사안명 줄)은 이번 범위에
   없어 남겼다.

**검증**: 통합 테스트 30개 통과. 앱을 다시 띄워(`launchctl kickstart`) 실제 화면에서 — 실시간
상단 288px·제목 검색과 선택 언론사 체크가 건수·범위 문구를 고쳐 씀, 확정본 14쌍·초안 7쌍 모두
한 건 64px, 정기 보관함 기간 바 52px·직접 입력 조회(37일 → 9/14·9/15 → 복원), 수시 보관함
직접 입력 제출 URL(`?start=…&end=…&q=`)과 프리셋 제출에 `start`가 한 번만 실림, 수시 카드 한 건
66px, 일곱 화면 모두 가로 넘침 없음을 확인했다.

**곁가지로 발견**: `.claude/launch.json`의 `static-preview`(`python3 -m http.server 8899`)가
`--bind` 없이 모든 인터페이스(`*:8899`)로 열려 프로젝트 루트(`.env` 포함)를 같은 네트워크에
노출했다. 목업 측정에 한 번 띄웠다가 확인 즉시 껐고, 설정 자체는 고치지 않았다(사용자에게 알림).

## 문서 정리 — 커밋 스냅샷 · 시안 폴더 · CLAUDE.md 다이어트 (2026-09-16)

**왜**: 마지막 커밋이 2026-08-07이라 6주치 작업(새 모듈 30여 개 포함)이 git 밖에 있었고,
CLAUDE.md는 712줄·319KB로 부풀어 있었다. 맨 위에 "배경은 HISTORY.md로"라고 적어놓고도
`[수정]`/`[추가]` 태그가 125개, 이름 변경 1~5차 경위·실측 수치·기각안이 본문 대부분을 차지했다.
CLAUDE.md는 새 대화마다 통째로 읽히므로 크기가 곧 비용·속도였다.

**한 일**:
1. 정리 전 상태를 한 번 커밋(`bc3a2b4`) — 되돌릴 지점.
2. 루트의 시안 HTML 중 문서가 가리키는 34개는 `mockups/`로 옮기고 링크를 고쳤다. 어느 문서도
   안 가리키던 30개는 `trash-can/2026-09-16_옛시안정리/`로. 작업 중인 오늘자 시안 9개는 루트에 뒀다.
3. CLAUDE.md를 **규칙만** 남기고 다시 썼다(약 94KB, 548줄). 날짜 태그와 경위는 뺐다 — 경위는
   이미 이 파일의 같은 항목에 있다. 정리 전 전체본은 `archive/CLAUDE_2026-09-16_정리전_전체본.md`에
   그대로 두었다. 앞으로 CLAUDE.md에는 날짜 태그를 쓰지 않는다.
4. 다시 쓰며 코드와 대조해 **어긋나 있던 서술을 바로잡았다**:
   - 자동 실행 항목의 "놓친 회차 보충은 가장 최근 1개뿐" — 2026-09-04부터 전부 보충이었다.
   - 라벨 절의 "숨김이 자정에 풀려" — 2026-09-16부터 7일 유지.
   - 수시 소제목의 "정기는 8개" — `MAX_SUBHEADINGS`는 15.
   - "하단 🤖 키워드 블록" 언급 — 2026-09-10에 없어진 블록.
   - 의존성 3개 → `openpyxl` 포함 4개.
   - 루트 폴더 `my_app` → `~/press-monitor`(`my_app`은 심볼릭 링크).
   - 대상 OS "Windows" → 운영은 macOS, 파일명은 Windows 호환 유지.
   - 수시 보관함 엑셀이 `/adhoc/download-excel-archive`로 서버 재계산한다는 서술 — 실제로는 구운
     rows를 공용 `/download-excel`로 보낸다(그 엔드포인트는 없다). 수시 카드 xlsx는
     `GET /adhoc/card/download-excel`.
   - 수시 상단바 함수 이름 `_nav_html` → `nav_html`.
   - 수시 원본·확정본의 옛 이름(「수집 원본」·「모음」)을 지금 이름으로 통일.

5. README.md를 지금 기능 기준으로 다시 썼다(536줄 → 약 140줄). 수시 모니터링·라벨·엑셀·알림·홈·
   자동 실행이 통째로 빠져 있었고, "최근 7일"·"Windows"·"소제목 최대 8개"·`PLAN.md` 같은 옛
   서술이 남아 있었다. 세부 동작 설명은 CLAUDE.md에 맡기고 README는 사용자용 소개·설치·한계만 둔다.
   `.env.example`도 실제로 읽는 키(Anthropic·이메일)를 채우고, 설정 화면으로 옮겨 간
   `TELEGRAM_CHAT_ID`(최초 1회 이관용으로만 남은 값)는 뺐다.
6. 끝난 설계 문서 `MORNING_ISSUES_DESIGN.md`와 요약 기획서 `prd_lite.md`는 `archive/`로 옮기고
   링크를 고쳤다. `DESIGN.md`는 코드 주석이 `DESIGN.md §N`으로 가리켜 루트에 두되, 맨 위에
   "보관용 — 지금 동작과 다른 곳이 많다"는 안내를 붙였다. `ADHOC_DESIGN.md`는 계속 갱신되고
   § 번호로 인용돼 그대로 둔다.
