# HISTORY.md

CLAUDE.md 규칙 끝의 「→ H: 항목명」이 가리키는 **경위만** 담는다. 그 규칙이 왜 생겼는지, 무엇을 기각했는지, 어떤 사고가 있었는지 — 곧 **다시 하면 안 되는 것**의 기록이다.

- **여기에 적는 것**: 기각한 대안과 그 이유, 사고 경위, 규칙을 정한 실측 수치. CLAUDE.md 규칙에서 「→ H: 항목명」으로 이 파일의 같은 제목을 가리킨다.
- **적지 않는 것**: UI 미세 조정 과정, 시안 비교, 나중에 다시 바뀐 중간 단계. 그런 경위는 커밋 메시지에 한두 줄로 남긴다(커밋은 기능 하나씩 작게).
- **2026-09-22 이전의 전체 기록**(항목 290여 개, 없앤 기능의 경위 포함)은 [archive/HISTORY_2026-09-22_전체본.md](archive/HISTORY_2026-09-22_전체본.md)에 그대로 있다. 코드 주석의 「HISTORY.md "…" 참고」가 여기서 안 보이면 거기서 찾는다.

## 되돌리기 스택 크기 (2026-09-23)

- 사고 경위랄 것은 없고 누적이다. 스냅샷은 "큐레이션 상태를 통째로 뜬다"는 정의 하나로 동작마다 역연산을 짜는 걸 피한 선택이고 그 판단은 그대로 옳다. 다만 스냅샷 대상이 9개 파일 + LLM 캐시 + 회차 파일로 늘고 그 파일들 자체가 커지면서, **큐레이션 버튼 한 번이 수 MB를 읽고 다시 쓰는** 비용이 됐다(스택은 push마다 통째로 읽고 쓴다).
- 실측(2026-09-23, 실제 스택 항목 11개 6.0MB) — 조각별 「항목당 크기 / 서로 다른 값」: `llm_cache` 215K/2개, `hidden` 198K/4개, `overrides` 184K/6개, `run_file` 26K/1개. **전체의 66%가 같은 값의 반복 저장**이었다. 큐레이션 한 번은 파일 하나만 바꾸는데 스냅샷은 매번 전부를 다시 담았기 때문이다.
- 고친 뒤 실측(기사 20건을 차례로 숨겨 20단계를 채운 실제 데이터): 파일 17.2MB → 5.1MB, `push` 한 번 140ms → 29ms, 스택 읽기 47ms → 12ms. 되돌리기 27ms.
- **기각**: LLM 캐시를 스냅샷에서 빼기(항목당 215K로 가장 크다). 확정본 화면은 매 렌더링마다 분류를 캐시에서 가져오므로 캐시를 안 되돌리면 "되돌렸는데 화면은 그대로"가 된다 — 이 모듈 맨 위가 처음부터 경고하던 자리다.
- **기각**: 단계 수를 20에서 줄이기. 크기는 줄지만 담당자가 되돌릴 수 있는 범위가 좁아진다 — 원인(중복 저장)이 아니라 증상을 건드리는 쪽이다.
- **기각**: 항목마다 파일을 따로 두기(`data/undo/<n>.json`). push 쓰기는 한 항목만큼으로 줄지만 상한 정리·자정 초기화·원자적 쓰기가 전부 디렉터리 조작이 되고, 중복 저장 자체는 그대로 남는다.
- **기각**: 차이(diff)만 저장. 복원이 "직전 상태로 통째로 되돌린다"는 정의에서 "여러 단계를 거꾸로 되감는다"로 바뀌어, 스냅샷 방식을 고른 이유(역연산을 안 짜도 된다)가 무너진다.
- 해시는 sha1이고 값을 JSON 문자열로 굳혀 계산한다 — 조각이 파일 원문(str)·「파일 없음」(None)·dict로 제각각이라 한 표현으로 맞춰야 비교가 된다. `None`도 `"null"`이라는 정상적인 blob이다: 키가 **아예 없는 것**(그 파일이 목록에 들어오기 전 기록)과 구별돼야 하고, 그 구별에 `undo()`의 `if key in files`가 기대고 있다.

## 자동발송 무한 재발송 (2026-09-23)

- 사고 경위: `send_confirmed_run`이 "채널 하나라도 **전원 성공**하면 발송 완료"(`SendResult.ok`)로 판단했다. `ok`는 대상 전원 성공일 때만 참이라, 받는 사람 셋 중 하나가 봇을 차단하거나 chat id에 오타가 있으면 `ok`가 영영 거짓 → `record_send` 미호출 → `send_count`가 0에 머물고 → `check_pending_confirm_and_send`의 `if send_count == 0`이 **매 tick(10초) 참**이 됐다. 멀쩡한 두 사람은 같은 회차를 다음 회차가 올 때까지 10초마다 다시 받는다. `record_send_failure`는 원인이 같으면 다시 안 쓰므로 회차 파일도 화면도 안 바뀌어 **담당자가 눈치챌 단서가 없었다.**
- 실측(목으로 영구 부분 실패를 만들어 tick 반복): 고치기 전 5 tick 연속 `sent=False`·`send_count=0`, 40분이면 240회 발송. 고친 뒤 1회.
- **기각**: 전원 성공 기준은 두고 재시도 횟수만 제한. 그래도 멀쩡한 사람이 같은 보고서를 5번 받는다 — 중복 발송이 이 사고의 핵심 피해라 기준 자체를 고쳐야 했다.
- **기각**: 이미 받은 사람을 빼고 못 받은 사람에게만 다시 보내기. 받는 사람별 발송 상태를 회차 파일에도 남겨야 해 발송 기록(`send_log`)과 진실이 둘이 되고, 재시도 사이에 담당자가 확정본을 고치면 사람마다 다른 판본을 받는다.
- **기각**: 재시도 횟수를 회차 파일에 저장. tick마다 파일 쓰기가 생긴다. 메모리(`_auto_send_attempts`)에 두고 앱을 재시작하면 다시 몇 번 시도하는 쪽을 골랐다 — 이 경로는 **아무에게도 안 간 회차만** 타므로 중복 발송 위험이 없다(같은 이유로 `app.scheduler._slot_failures`의 메모리 보관과 방향이 반대여도 안전하다).
- 상한을 1분 간격 5번(약 4분)으로 둔 건 네트워크 순단은 넘기고 봇 토큰 만료·SMTP 차단처럼 사람이 고쳐야 풀리는 실패는 포기하는 선이라서다. tick이 10초라 "3번"으로 하면 30초 만에 포기한다.

## 발송 기록 — 읽음 확인 (2026-09-23)

- **기각**: 받는 사람이 **읽었는지** 적기. 텔레그램 봇 API는 봇에게 읽음 표시를 주지 않는다. 메일 읽음 확인은 추적 이미지뿐인데 앱이 `localhost`라 받아 줄 외부 서버가 없고, 있어도 Gmail은 이미지를 미리 받아 안 읽어도 「읽음」, 기관 메일은 이미지를 막아 읽어도 「안 읽음」이 된다. 틀린 값을 적느니 안 적는다.
- **지금**: 도착까지만 적는다 — 텔레그램 「도착」(텔레그램이 대화방에 넣음), 이메일 「메일 서버가 받음」(나중에 반송될 수 있음). 화면 이름도 「수신확인」이 아니라 「발송 기록」.

## 수시 보관함 — 사안별 묶기 (2026-09-22)

- **기각**: 「사안 → 날짜 → 회차」. 실측(보관함 사안 16개): 여러 날에 걸친 사안은 「재경위」 하나뿐이고 나머지는 사안 하나 = 날짜 하나 = 회차 1~3개라 사안 층은 한 겹 더 펼치게만 했다. 담당자는 예전 이름에 맞추려 사안명 정하기를 부담스러워했고, 이름에 날짜를 적어 넣는 흔적(「9/1 민생대책」·「9/1 국무회의」)까지 생겼다.
- **지금**: 날짜 → 회차, 회차 줄에 `HH:MM 기준 · 사안명`. 이어지는 사안은 사안명 검색으로 모은다. **기각한 건 「층을 한 겹 더 두는 것」이지 「사안으로 보는 것」이 아니다** — 2026-09-23에 「보기」 스위치(날짜별/사안별)를 더했다. 축만 바꿔 여전히 2단이고, 기본은 날짜별이며 고른 보기를 기억하지 않는다(늘 날짜별로 시작). 사안 줄 정렬 칩 `가나다순/최근순`도 같이 왔다. 줄 모양 `사안명 HH:MM`(확정본 탭 이름과 같은 모양)은 시각이 이름 길이 따라 들쭉날쭉해 세로로 훑기 어려워 기각.

## 수시 원본 검색어 편집 없앰 (2026-09-22)

- **왜**: 원본 편집 창에서 검색어·검색 방식을 고치면 **같은 카드**의 조건만 바뀌고 목록이 새 조건으로 다시 걸러졌다(기사는 카드에 남고 화면에서만 빠짐). 그런데 확정본은 원본의 기준 시각(끝 시각)마다 생기고 검색어 편집은 끝 시각을 안 바꾸므로, 고치기 전에 보낸 기사와 뒤에 보낸 기사가 **한 확정본에 섞였다** — 확정본 머리줄은 `N시 기준(사안명)`뿐이라 흔적도 없었다. 원본에는 마지막 조건만 남아 「지난 수집」도 처음 모은 조건을 잃었고, 뺀 검색어로 보낸 기사는 원본 목록에서 사라진 채 확정본에만 남았다.
- **기각**: 원본 안에서 검색어 고치기(위 이유). 시간은 남겼다 — 같은 조건을 넓게 보는 것이고, 끝 시각이 바뀌면 확정본이 새로 생겨 섞이지 않는다.
- **대가**: 잡음 검색어를 ×로 빼 보는 즉석 거르기(네이버 호출 0회·되돌리기 가능)가 없어졌다. 대신 `?from=`으로 조건이 채워진 새 수집을 연다.
- **같이 필요했던 것**: 보냄 표시가 원본 카드 id 하나로만 세어져, 새 원본(#2)에선 #1에서 보낸 기사가 처리 안 한 기사로 보여 두 번 보낼 수 있었다 → 같은 날·같은 사안의 판은 보냄을 함께 본다. 기준 시각이 같으면 #2가 #1의 확정본을 이어 쓰던 것도 → 확정본에 만든 원본(`source_card_id`)을 적어 막았다.
- **이름**: 같은 사안 원본을 건수(`인사청문회 183건`)로 가르던 탭은 숨기거나 다시 수집할 때마다 이름이 바뀌고 무엇이 다른지 말해 주지 않았다 → `#2`. `(2)`는 앱의 다른 곳에서 괄호 숫자가 건수라 기각, `②`는 목록 번호처럼 보여 기각. `#1`을 첫 원본에 붙이면 둘째가 생기는 순간 보던 탭 이름이 바뀌어 첫 원본은 번호 없음.

## 텔레그램 받는 것 칩 — 켜진 「요약」이 꺼진 것처럼 보이던 문제 (2026-09-22)

- **왜**: 사용자 지적(스크린샷) — 켜진 「요약」이 잘 안 보인다. 켜진 「기사」는 채움, 켜진 「요약」은 옅은 바탕이었고, 두 칸이 테두리 하나를 나눠 써서 「둘 중 하나를 고르는」 버튼처럼 읽혔다(채운 쪽 = 고른 쪽). 처음 만들 때 「기사가 주, 요약은 덧붙임」을 옅은 색으로 나타낸 것이 켜짐/꺼짐 차이와 섞였다.
- **시안** `mockups/TELEGRAM_PAIR_CHIP_MOCKUP.html`: A안(켜지면 모두 흐름 색 채움 + ✓), B안(모두 옅은 바탕 + 실선 + ✓ — [단독]·[속보]와 같은 문법). A안 모양에 요약 하늘색만 진하게 한 C·D안도 만들었으나 사용자가 **B안**으로 결정.
- **결과**: 두 칸을 4px 띄우고 칸마다 제 테두리(꺼짐 점선), 켜지면 기사·요약 같은 모양 + ✓. [단독]·[속보] 칩도 켜지면 ✓.

## 글자 크기 단계 (2026-09-22)

- **왜**: 모양 규칙 다음 순서(사용자 요청). 아이콘 크기를 빼고도 글자 크기가 38가지(0.61~1.4rem)였다. 같은 자리인 화면 제목이 확정본 1.4 · 추이 1.35 · 초안·설정·보관함 1.3 · 수시 보관함 1.2 · 수시 머리줄 1.1 · 라벨 관리·홈 1.05로 8가지, 확정본과 초안이 서로 달랐다. 정기 툴바 0.9 · 수시 툴바 0.8, 홈 그룹 칩은 0.61rem(10px 미만).
- **고른 안**: 시안 `mockups/TYPE_SCALE_MOCKUP.html` — 여섯 단계(0.72 · 0.8 · 0.88 · 1 · 1.1 · 1.3). 지금 값을 가장 가까운 단계로 모았다(0.745 미만 → xs, ~0.835 → sm, ~0.945 → md, ~1.075 → base, ~1.225 → lg, 그 위 → xl). 기사 행은 이미 단계 값에 가까워 거의 안 바뀐다. 사용자가 세 질문 모두 추천대로 결정:
  1. 홈 머리 제목(로고 옆 1.05)은 예외로 그대로 — 머리를 한 줄로 줄인 결정(`HOME_HEADER_COMPACT_MOCKUP` B안)을 지킨다.
  2. 수시 원본·확정본 머리줄 1.1 → 1.3 — 정기 확정본 제목과 같은 역할의 줄.
  3. 펼친 기사 요약 0.95 → 0.88 — 제목(1rem)과 거의 같아 구별이 안 됐다.
- **값만으로 안 갈린 곳**: 날짜 줄 1.05 → lg(1.1, 소제목과 같은 층), 빈 화면 안내 1.3 → lg, 전체 기사 「● 실시간」 1.25 → xl(그 화면의 제목), 수시 툴바 `.btn.sm` 0.8 → md(정기 툴바와 같게).
- **아이콘 크기는 뺐다**: ↩·목차·⋯·✏️·▲▼·×·🗑·▸처럼 `font-size`가 아이콘 크기인 곳은 숫자 그대로(58곳). 단계에 넣으면 아이콘이 작아지거나 커진다.
- **옮기다 생긴 사고**: 검증 중 통합 테스트를 돌렸더니 새로 들어온 `test_breaking_burst_alert`(다른 세션 작업)가 `TELEGRAM_RECIPIENTS_FILE`을 임시 폴더로 돌리지 않은 채 `save_telegram_recipients`를 불러 **실제 `data/telegram_recipients.json`을 테스트 값(나 / 111)으로 덮어썼다**. 켜진 앱이 곧바로 111로 [단독] 알림을 보내다 실패해 드러났다. 받는 사람은 그날 오전 화면에서 본 값(담당자 · 정기 기사 · 하루 종일)으로 되살렸다. 테스트는 받는 사람 파일을 임시 폴더로 돌리게 고쳤고, 전체 테스트 뒤 `data/` 파일이 하나도 안 바뀌는 것을 확인했다.

## 모양 규칙 (2026-09-22)

- **왜**: 디자인 점검 요청(「네모칸, 동그란칸 등 규칙도 필요해 보여」). 실측해 보니 모서리 둥글기가 18가지(2~25px·999px·50%), 버튼 높이 11가지(22~34px), 그림자 25가지였다. 같은 칩이 정기 999px·수시 14px·홈 20px·보관함 10px, 떠 있는 버튼이 46·50·56px. 색은 뜻별로 한 곳에 모여 있는데 모양만 화면마다 따로 정해져 있었다.
- **고른 안**: 시안 `mockups/SHAPE_RULES_MOCKUP.html`(떠 있는 앱의 실제 CSS를 Shadow DOM에 넣어 「지금 / 제안」을 나란히). 모양 넷(네모·큰 네모·알약·동그라미)에 뜻을 하나씩, 높이 세 단계, 그림자 셋. 사용자가 세 질문 모두 제안대로 결정:
  1. 「지금까지 다시 수집」 알약 → 네모(예전 `ADHOC_RELOAD_NOW_MOCKUP`에서 알약으로 정했던 버튼 — 동작 버튼이라서).
  2. 상단바의 지금 화면 칩 14px → 알약(「지금 여기」는 상태 표시).
  3. 떠 있는 버튼 두 크기 56 / 48(↩ 50 → 48, 목차 46 → 48). 「N건 숨김」 안내는 ↩ 옆 자리라 `left`·높이를 `--fab-sm`에서 계산한다.
- **같이 고친 어긋남**: 수시 확정본 「소제목 배정」이 테두리만 연보라였다 → 채운 연보라. 수시 확정본 내보내기 테두리 박스 `복사 · txt로 저장 · ⬇ xlsx` → 원본·정기와 같은 글자 버튼 `copy · txt · xlsx`(마크업을 원본과 한 곳에서 만든다). 키워드 메모 「삭제」가 「수정」과 같은 채운 파랑 → 글자 버튼.
- **CSS 변수로 한 이유**: 렌더러마다 CSS가 `.format` 템플릿·f-string·그냥 문자열로 섞여 있어 `{radius_md}` 같은 자리표시자를 쓰면 문맥마다 중괄호 처리가 달라 깨진다. `var(--r-md)`는 중괄호가 없어 어느 문맥에서도 그대로 쓸 수 있다. 정의는 모든 화면이 한 번씩 넣는 `topnav_style()`과 홈에서 낸다(29개 화면 모두 정의가 들어가는지 확인).
- **옮긴 방법**: 388곳을 값으로 기본 분류(2~5px 작은 네모, 6~7px 네모, 8~12px 큰 네모, 14px 이상 알약)한 뒤, 값만으로 갈리지 않는 곳(8px 버튼·입력칸·기사 행 → 네모, 9~12px 칩·배지 → 알약, 14~20px 동작 버튼 → 네모)은 줄마다 골랐다. 기본 `button`·입력칸의 4px은 6px로(행 안 작은 컨트롤만 4px).
- **범위 밖**: 글자 크기(40가지 넘음)는 다음 시안으로 미뤘다. 설정 화면 버튼은 이미 36px 안팎이라 높이를 강제하지 않았다(작은 버튼까지 늘어날 위험).

## 여러 기사 한 번에 숨기기 — 소제목 🗑가 느리던 문제 (2026-09-22)

- 사용자 질문: 소제목 🗑가 기사 🗑보다 왜 오래 걸리나, 이용자가 알아야 하지 않나.
- 원인: 소제목 통째 숨기기·선택 바 일괄 숨기기가 전용 요청 없이 `/hide-article`을 **기사 수만큼** 동시에 보냈다. 서버는 요청마다 (1) 되돌리기 스택 `data/undo_stack.json`(당시 17MB, 읽기 0.05초·쓰기 비슷) 읽고 다시 쓰기 — 같은 batch면 URL을 덧붙여 매번 저장, `_lock`으로 한 줄씩 (2) 화면 세 개 재생성(확정본 0.09·보관함 0.02·홈 0.03초 실측)을 했다. 10건이면 수 초. 기사 🗑는 요청 한 번에 새로고침도 없어 빨랐다.
- 알리는 것만 하는 안보다 원인을 없애는 쪽을 먼저(사용자 결정: 둘 다). 한 요청에 기사마다 `url·outlet·title·pubDate·group`을 같은 순서로 되풀이해 싣고(폼 파싱이 `keep_blank_values`라 빈 값도 자리를 지킨다), 서버는 되돌리기 기록·`hide_articles`(파일 쓰기 한 번)·화면 재생성을 한 번씩만 한다. 빠진 값 폴백도 `_hide_meta_fallbacks`로 초안 캐시·회차 역조회를 한 번에. 옛 탭의 한 건씩 요청도 그대로 받는다.
- 진행 표시: 누르는 순간 그 기사 줄을 흐리게(`.is-hiding`) 하고 🗑 앞에 파랑 `N건 숨기는 중…`, 🗑는 잠근다(두 번 누르기 방지). 처음엔 소제목 칸 전체를 흐렸는데 안내 글자까지 흐려져 안 읽혀 머리줄은 흐리지 않게 바꿨다. 실패하면 원래대로 돌리고 알림.
- `keepalive`는 뺐다 — 본문 상한 64KB를 기사 100건 안팎이면 넘는다(한글 제목이 퍼센트 인코딩으로 9배).

## Shift+클릭 범위 선택 (2026-09-22)

- 사용자 요청: 체크박스를 하나 누르고 아래 것을 누르면 사이가 한꺼번에 선택되게. 수정키 없이 두 번 누른 것을 범위로 보는 안은 기각 — 떨어진 두 건만 고를 때 사이가 딸려 오고, 선택 바 🗑 일괄 숨김은 확인창이 없다. Gmail·Finder와 같은 Shift+클릭으로.
- 안내 방식 세 안(A: 선택 바에 늘 흐린 안내 / C: 한 번 쓰면 사라짐 / B: 툴팁만) 중 **A안** 채택(사용자 결정) — 선택 바는 체크했을 때만 올라오므로 평소 화면을 방해하지 않는다.
- 부작용 점검: 「보기 순서」는 행을 DOM에서 실제로 옮기므로 범위는 보이는 순서와 같다. 소제목을 넘는 범위는 막지 않는다(선택 바가 이미 ↑↓·나누기를 잠근다). 수시 원본은 체크박스로 숨김 행을 보내면 `clear_raw_marks`가 표시를 지우므로, 범위로 쓸려 들어가지 않게 `.is-sent`·`.is-raw-hid` 행을 건너뛰기로 했다(사용자 결정). Shift+클릭이 글자를 파랗게 선택하는 것은 `getSelection().removeAllRanges()`로 지운다.
- 시안 `mockups/SHIFT_RANGE_SELECT_MOCKUP.html`(지금 초안 화면을 떠서 만든 것).
- **선택 바 안내 글자를 뺐다**(같은 날, 사용자 결정): A안의 흐린 `[Shift]+클릭으로 범위 선택`은 처음 알아채게 하려는 것인데, 쓰는 사람이 한 명이고 이미 기능을 안다 — 선택 바를 열 때마다 같은 말을 보게 되고, 동작 버튼만 모인 줄에 누를 수 없는 글자가 섞인다. 체크박스 툴팁(`선택 · Shift+클릭하면 사이 기사까지 한꺼번에`)은 남겨 B안이 됐다. 다섯 화면이 `RANGE_HINT_HTML` 하나를 썼으므로 그 상수·CSS(`range_select_style`)를 통째로 없앴다.

## 초안 「+ 수기로 기사 추가」 — 네이버에서 직접 찾은 기사 끌어오기 (2026-09-21)

- 담당자가 네이버 홈에서 직접 검색하다 앱에 안 들어온 기사를 발견한 데서 시작했다. "네이버 검색을 대체할 수는 없고, 때로는 직접 검색할 필요도 있다"는 게 전제다 — 누락을 없애는 기능이 아니라 **손으로 찾아온 것을 넣는 입구**다.
- **왜 검색어 화면을 거치면 안 되나**: 끌어오는 유일한 길이 「검색어에 임시 키워드 등록 → 전체 기사 → 담아두기 → 지우기」였다. 검색어는 *상시 조건*의 집이라, 일회용 키워드 하나가 그 회차 확정본의 수집 조건까지 바꾼다(화면 캡션 「저장하면 전체 기사·초안엔 바로, 확정본엔 이번 회차 마감부터 반영됩니다」가 그대로 부작용이다). 지우는 걸 잊으면 다음 회차에 엉뚱한 기사가 딸려 온다. 그래서 고칠 것은 검색어 화면이 아니라 "일회성이 거길 안 거치게 하는 것"이었다.
- **착지점을 📌 담아둔 기사로 한 이유**: 그 칸은 이미 「보고서 밖 임시보드」라 승격 전까지 복사·txt·발송 어디에도 안 나간다. 밖에서 들어온 기사가 담당자 확인을 한 번 거치게 되고, 승격은 기존 소제목 드롭다운을 그대로 쓴다 — 새 경로를 만들 필요가 없었다.
- **기각한 대안 둘**:
  - *전체 기사에 「지금 한 번만 검색」 칸* — 수시 새 수집(사안·검색어·시간창 직접 지정)과 기능이 그대로 겹친다. 검색이 필요한 경우는 수시로 모은 뒤 행의 ⋯ `복사하기`로 URL을 옮겨 붙이면 되고, 그러면 통로를 안 뚫고도 이어진다.
  - *수시 → 정기 통로 뚫기* — 「정기로 가는 통로는 0개」 경계를 깨면 수시 화면에 정기 용어가 새어든다. 사람이 클립보드로 건너는 걸 코드로 뚫을 이유가 없다.
- **발행시각 실측(2026-09-21)**: 네이버 API를 안 거친 기사라 `pubDate`가 없다. 저장된 회차의 `pub_date`를 정답으로 놓고 원문 페이지를 재봤다.
  - `n.news.naver.com` 30건 — `data-date-time` **30/30 정확**, 페이지당 정확히 1개(수정시각이 함께 찍히는 경우 없음)라 첫 매치가 곧 게시시각이다. 같은 표본에서 `article:published_time`·`datePublished`·`meta[name=date]`는 **0/30**, 즉 부재.
  - 언론사 자체 도메인 14건 — `data-date-time` 0건, `article:published_time` 13건 존재. 그중 7건 정확, 6건은 1~14분 차이(최초게시 vs 최종수정으로 보인다), 1건(동아일보 자체 도메인)은 후보가 아예 없었다.
  - 저장된 기사 7,071건 중 **93.5%가 네이버 미러**라 주 경로는 오차가 없다. 자체 도메인의 ±15분은 그대로 뒀다 — 같은 언론사 안에서 정렬 자리가 조금 달라지는 정도다. 「담당자가 시각을 직접 입력」·「비워둔다」는 이 수치를 보고 기각했다.
- **oid가 표에 없는 매체**: `resolve_outlet`의 도메인 폴백이 네이버 링크에는 `n.news.naver.com`이라는 쓸모없는 값을 내놓는다. 실측에서 `og:article:author`가 서로 다른 22개 매체 **22/22** 존재했고 `| 네이버` 접미사만 떼면 22/22가 `NAVER_OID_OUTLETS`의 이름과 글자까지 같았다 — 그 경우에만 폴백으로 쓴다.
- **자동 새로고침을 뺀 경위**: 처음엔 성공 시 1.4초 뒤 `location.reload()`였다. 실제로 눌러보니 결과 줄(특히 `✗ URL이 아닙니다`)이 읽기 전에 지워졌다 — CODING_CONVENTIONS §3 위반이다. 반영을 「닫기」에 옮기고 버튼 글자를 `닫고 반영하기`로 바꿨다.
- **되돌리기 대상이 아니다**: 📌 담아두기 자체가 원래 되돌리기 대상이 아니고(`_handle_unpin_manual_article` — "숨기기와 달리 전역 영향이 없다"), 담아둔 기사는 보고서 밖이라 📌 담아둠 해제로 바로 뺀다. 승격부터가 되돌리기 대상이다.
- 상한 20건은 담당자가 실수로 긴 텍스트를 통째로 붙여넣는 것을 막는 용도다(수집이 아니라 손으로 고르는 동작). 원문 동시 요청 5는 네이버 API가 아니라 언론사 페이지라 전역 세마포어와 무관하다.
- **담당자 검토 후 고친 것(같은 날)**: 이름 `+ URL로 추가` → **`+ 수기로 기사 추가`**(담당자 선택 — 「수기」가 "앱이 아니라 사람이 넣었다"를 가장 분명히 말한다). 여러 줄 입력 → **한 건씩**(여러 건이면 결과가 목록이 되고 어느 줄이 왜 실패했는지 대조해야 한다 — 이걸로 상한 20건·동시 요청 5도 같이 없앴다). 테두리 회색 점선 → **파랑 실선**(점선 회색은 바로 위 키워드 메모 칸과 같아 구분이 안 됐고, 파랑 **점선**으로 바꿨더니 이번엔 직접 만든 소제목의 하늘색 굵은 점선과 헷갈려 실선으로 갈랐다). 결과 줄 언론사를 제목과 **같은 색·크기로 붙여 놔** 제목의 일부처럼 읽히던 것을 `.title-outlet`과 같은 회색 규칙으로 분리.
- **`og:site_name`을 언론사 폴백으로 쓰지 않는 이유(실측 2026-09-21, 자체 도메인 10곳)**: 담당자가 직접 넣어본 `asiatime.co.kr` 기사가 언론사명 자리에 도메인 문자열로 찍힌 데서 시작했다. 후보를 재보니 `og:site_name` 6/9, `meta[name=author]` 4/9, `meta[name=publisher]` 0/9 존재(셋 중 하나라도 7/9). 그런데 값이 **계열 매체의 하위 브랜드**였다 — `monthly.chosun.com`→`월간조선`(저장값 조선일보), `woman.donga.com`→`여성동아`(동아일보), `magazine.hankyung.com`→`한경매거진&북`(한경비즈니스). 그대로 쓰면 화이트리스트 문자열 비교에서 튕긴다. 그래서 **도메인 표가 실패해 도메인 문자열이 나올 때만** 폴백한다(`finomy.com`→`현대경제신문`, `asiatime.co.kr`→`아시아타임즈`) — 아무것도 없던 자리를 채우는 것뿐이라 기존 이름은 안 건드린다.
- **자체 도메인 발행시각의 1~2분 차이는 오차가 아니다**: 같은 실측에서 시각 후보가 7/9 존재했고 안 맞는 5건은 전부 1~2분 차이였다(한경 1분·아시아투데이 1분·월간조선 1분·finomy 1분·여성동아 2분). 대조한 "정답"이 네이버의 **색인** 시각이고 페이지 값은 언론사 **자체 표기**라서다 — 수기로 넣는 기사엔 네이버 색인 시각이 아예 없으므로 언론사 표기가 오히려 맞는 값이다. 본문에서 「입력 2026.09.21 10:00」을 긁는 안은 3/9밖에 안 잡히고 인용문 속 날짜를 물 위험이 있어 기각(「새 신호는 전수 실측으로 오탐 0을 확인한 뒤에만」).

- **자리 옮김·바로 담기·기사 아닌 주소 거르기(같은 날, 담당자 검토)**: 툴바에 두니 입력칸은 화면 맨 위에 열리는데 기사는 약 1,700px 아래 📌 칸에 떨어졌고, 담아둔 기사가 0건이면 그 칸이 아예 없어 "어디로 갔지?"가 됐다. 툴바 왼쪽 박스 버튼은 「초안을 바꾸는 버튼」 자리인데 이 동작만 보고서 밖이라 규칙에서도 벗어나 있었다. 담당자가 「가외라 담아둔 기사와 묶는 게 맞다」고 골라 입구를 📌 칸 머리로 옮겼다(시안 B안 「툴바 유지 + 결과에서 칸으로 데려가기」는 넣을 때마다 화면이 크게 뛰어 기각). 이어서 「주소 입력 → 추가 → 제목 확인 → 닫고 반영하기」가 번거롭다는 지적으로 확인 단계를 없앴다 — 앞서 자동 새로고침을 뺀 이유(결과 줄이 지워진다)는 결과가 칸 안의 기사 행 자체가 되면서 사라졌다. `닫기` 글자는 ×로, `추가`는 주소칸 바로 옆으로.
  - **입력칸 색**: 📌 칸이 파랑(띠 + #EFF6FF 바탕)이라 연파랑 테두리 입력칸이 한 덩어리로 묻혔다. 가(흰 바탕 + 회색 테두리 + 그림자)·나(진한 파랑 테두리 — 띠·추가 버튼과 파랑이 셋 겹침)·다(테두리 없음 — hover한 기사 행과 비슷) 중 가. 다른 계열은 뜻이 이미 있다(앰버 = 주의라 경고 상자와 섞인다). 예전 회색이 키워드 메모 칸(회색 점선)과 헷갈렸던 문제는 입력칸이 맨 아래로 가며 두 칸이 한 화면에 안 서게 돼 없어졌다.
  - **블로그·커뮤니티 실측(2026-09-21)**: 담당자 걱정("다른 사람이 블로그나 커뮤니티 글을 복사할 수도")을 재 보니 **그때까진 그대로 담겼다** — 네이버 검색 API로 뽑은 블로그 40건(네이버 블로그 39·티스토리 1)·카페 15건이 전부 성공, 언론사 칸엔 `blog.naver.com` 같은 도메인. 첫 후보 「언론사 이름을 끝내 못 찾아 도메인 문자열이 남으면 기사가 아니다」는 블로그·카페 55/55를 걸렀지만, 같은 날 재정경제부 뉴스의 언론사 자체 도메인 74곳 중 **22곳(30%)**도 막았다(이투데이·아주경제·부산일보·연합뉴스TV·에너지경제 등. 5곳은 페이지 자체를 못 받음). og:type=article도 기사 65/69 vs 블로그 1/40(티스토리는 article)이라 기준이 못 된다. 그래서 **주소 호스트 목록**으로 판정한다 — 55/55 걸림, 74/74 통과. 목록 밖 블로그 플랫폼은 새지만 담아둔 기사는 보고서 밖이라 담당자 눈을 한 번 거친다.
  - 성공한 기사는 처음엔 칸 **맨 아래**에 붙였다(저장 순서 = 실시간 📌와 같은 규칙). 담당자 요청으로 **맨 위**로 바꿨다 — 입력칸이 칸 맨 위라 결과가 바로 밑에 떠야 찾으러 내려가지 않는다. 실시간 📌 담아두기는 맨 뒤 그대로(`at_top`은 이 입구만 켠다).
  - 담당자 요청으로 **확정본 📌 칸에도 같은 입구**를 붙였다. 초안에만 있던 CSS·JS·마크업을 `app/renderer.py`의 공용 함수로 옮겨 두 화면이 한 코드를 쓴다(한쪽만 고쳐져 어긋나는 일을 막으려고 — 휴지통·사진 모아 보기와 같은 방식). 확정본에서 넣어도 착지점은 같은 📌 칸이고, 승격 드롭다운이 「확정본에 넣기…」라는 것만 다르다.

### 겹치는 기사 판정 두 구멍 (2026-09-21)

- **초안이 앞 회차와 대조하던 문제**: 「이미 이 회차에 있다」를 `load_latest_run()`으로 봤는데, 초안의 지금 회차는 아직 저장 전이라 실제로는 앞 회차 확정본과 비교했다. 지금 초안에 보이는 기사를 넣으면 통과해 📌 칸에 한 번 더 보였고, 앞 회차 기사는 「이 회차」라는 틀린 문구로 막혔다. 초안은 다시 검색하지 않고 마지막으로 그린 초안 목록(`draft_seen`의 `last_visible`) + 예약 기사로 대조하게 바꿨다. 앞 회차 기사는 `이미 앞 회차에 실린 기사입니다.`로 따로 말한다(확정본에서도 오늘 앞 회차 기사를 이제 막는다 — 전엔 통과했다).
- **주소 모양이 다르면 통과하던 문제**: 수집은 `?sid=` 꼬리를 떼고 저장하는데 수기 입력은 그대로 대조해, 네이버에서 복사한 주소(`?sid=101`, `/article/…`, `m.news`)가 늘 통과했다. 확정본 승격엔 중복 검사가 없어 같은 기사가 보고서에 두 번 나갈 수 있었다. 네이버 뉴스 주소만 저장 모양으로 맞춘다 — 언론사 자체 도메인은 쿼리가 곧 기사 번호일 수 있어 안 건드린다.
- 같이 막은 것: 숨긴 기사를 수기로 넣으면 📌 칸이 숨김을 걸러 「성공」 뒤 아무것도 안 보였다(조용히 사라짐, CODING_CONVENTIONS §3). `숨긴 기사입니다. / 휴지통에서 되살려 주세요.`
- 남은 구멍(보류): 앱엔 네이버 주소로, 담당자는 언론사 원문 주소로(또는 반대) 넣으면 URL이 달라 못 잡는다. 제목 대조로 막는 안은 "중복·숨김은 URL로만" 원칙과 부딪혀 보류. → 2026-09-22에 원문 주소로 막았다(아래 「수기 추가 — 원문 주소로 같은 기사 알아보기」).

## 설정 메뉴 개편 — 2026-09-18

- 메뉴를 정기 / 수시 / 공통 · 수집 / 받는 사람 / 홈 / 연동으로 다시 묶었다. 이전엔 맨 위 묶음 없는 두 칸(검색어·형광펜) + 「뉴스 스크랩」·「메일머지」·「발송 · 알림」·「기타」·「연동」이었는데, 정기 전용과 공용이 섞여 있어 수시에도 적용되는지 알 수 없었다.
- **수집 범위(`/scrap-page`, 사진·인사 기사 제외) 삭제**: 둘 다 꺼진 채로 쓰는 게 이미 원칙이었고, 켜면 판정이 틀린 기사가 화면에 안 보이고 빠진다(CODING_CONVENTIONS §3). 사진 판정 기준을 자꾸 다시 고치던 것도 이 설정 때문에 판정 실수의 비용이 컸던 탓 — 설정이 없으면 판정이 틀려도 칩 하나 잘못 붙는 것으로 끝난다. 수시의 카드별 「사진·인사 제외」 옵션은 그대로 둔다(수시는 이 설정을 읽은 적이 없다). 정기 수집·초안·실시간 `_auto_drafted_urls`에서 읽던 곳을 모두 지웠다.
- **수시 보고서 형식 분리**: 수시가 정기 기사 제목·소제목 형식을 물려받던 것을 따로 둔다(수시 화면엔 정기를 가리키는 말이 없어야 한다는 원칙). 처음 값은 그때의 정기 값을 복사. 두 흐름이 섞인 화면(라벨 보관함·휴지통·홈 전광판 복사)은 정기 형식을 그대로 쓴다 — 라벨 보관함 텍스트는 머리줄이 하나인 평평한 목록이라 줄마다 형식이 달라지면 보고서 모양이 깨진다.
- **형광펜은 정기만**: 수시는 사안 검색어로 모은 기사라 칠할 단어가 뻔하다(사용자 결정). 수시 원본·확정본의 제목·요약은 이스케이프만 한다.
- 이메일은 남기고 메뉴에 「정기 확정본만」 한정어(수시는 텔레그램만 보낸다). 쓰는지 애매했지만 업무 메일(korea.kr) 받는 사람이 켜져 있어 두기로 했다.
- **이메일 보내는 계정을 설정 화면으로**(연동 › 이메일 보내는 계정, `/email-sender`): 네이버·AI 키와 같은 방식(화면 값 > `.env`). 메일 서비스는 네이버·Gmail 칩 둘만 — 「직접 입력」은 기관 메일이 외부 앱 발송을 막는 경우가 많아 뺐다(사용자 결정). 보내는 주소로 korea.kr을 넣으면 네이버 서버가 거절하므로 네이버는 @naver.com만 저장한다. 보내는 사람 이름(선택, 예 「재경부 디소팀」)을 새로 넣었다. 시험 메일은 받는 사람 명단이 아니라 보내는 주소 자신에게만.
- 기각: 「메일머지」 이름 유지(메일만이 아니다), 수집 범위를 「공통」에 두기(수시는 안 읽는다), 두 흐름이 섞인 화면에서 기사 출처별로 형식 적용.

## [단독]·[속보] 같은 기사 중복 — 2026-09-18

**증상**: 홈 [속보] 목록에 같은 기사가 2~3번 뜨고 칩 건수(20)가 부풀었다(부산일보·매일경제·경향신문 「유류세 인하 두 달 연장」 각 3번, 강원일보 2번, [단독] 일요신문 2번). 발견 경위: 홈 목록에 「복사」를 넣다가 눈에 띔.

**원인**: 폴링·몰아보내기는 키워드별 검색 결과를 평평하게 합쳐 `detect_and_alert`에 넘기는데, 같은 기사가 여러 키워드(예: 재정경제부·유류세·기름값)에 동시에 걸리면 목록에 여러 번 들어온다. `detect_and_alert`는 "이미 보낸 URL"만 걸렀지 **이번 묶음 안의 중복**은 안 걸렀고, `mark_alerted`도 기존 기록과만 대조했다. 중복 항목의 기록 시각(`at`)이 모두 같았던 게 단서. 그래서 **텔레그램 메시지에도 같은 기사가 여러 번 실렸다**(8:10 속보 묶음 등).

**고침**: 후보를 모을 때 URL을 `seen`에 바로 넣어 한 묶음 안 중복을 거름 · `mark_alerted`가 넘어온 items 안의 중복도 거름 · 홈이 읽는 `alerted_items`도 URL로 한 번 더 거름(고치기 전에 쌓인 오늘 기록이 자정까지 남아 있어서). 테스트 `test_breaking_alert_same_article_once`.

## 홈 부정 추정 기사 — 전광판을 대체 — 2026-09-18

**무엇**: 같은 날 붙인 「재경부 논조 전광판」(아래 항목)을 떼고, 남색 카드 한 줄 「부정 추정 기사 N건 ▾ · HH:MM 회차까지」로 바꿨다. 시안 `mockups/HOME_NEGATIVE_GUESS_MOCKUP.html`. 옛 코드·데이터·스위치 시안은 `trash-can/2026-09-18_논조전광판/`.

**경위**
- 전광판 첫 판 실측이 하루 약 230~260원(근거 줄인 뒤)으로 나오자 사용자가 "논조 한 줄에 하루 200원은 애매하다". 스위치 시안(A~D)까지 만들었으나 채택 전 방향이 바뀜.
- "오늘의 쟁점과 겹치나?" → 한 줄 요약은 쟁점 TOP 5를 문장으로 쓴 것과 사실상 같았다. 전광판에만 있던 건 비율과 부정 목록. (정정: 주유소업계 반발을 "정기에선 거의 안 잡혔다"고 설명했으나 쟁점 3위였다.)
- 비용은 대부분 **AI가 기사를 읽는 값**이라 "부정만 뽑기"로 바꿔도 같은 범위면 거의 안 준다(약 180원) → 범위를 좁혀야 의미가 있다.
- 사용자 결정: 논조는 빼고 부정 추정 기사만, **정기 기사만**, 그중 **「재경부」·「재정경제부」가 나온 것만**("기관 전용이니까"). 회차 간격으로 뜨니 「HH:MM 회차까지」 표기, 새로 더해진 표시는 「new」.

**실측(9/18 정기 5회차, 숨긴 기사 제외 82건, 회차마다 1번 = 5번 호출)**: 입력 18,512 / 출력 699 토큰 ≈ $0.022(약 31원). 부정 추정 15건 — 주유소업계 반발 8, 전기차 세액공제 요구 2, 후보자 부적격·부동산 2, 국고 부족 보좌진 월급 1, 세액공제 「그림의 떡」 1, 한방병원 가업상속공제 1(의견서 제출 사실만 전한 기사라 오판 → 프롬프트에 "의견서를 냈다는 사실만 전한 기사는 고르지 않는다"를 더함). 실제 앱 첫 가동(같은 5회차, 숨기기 전 목록 181건)은 입력 35,709 / 출력 1,232 토큰 ≈ 약 59원 — 시험이 숨긴 기사를 빼고 재서 절반으로 나왔다(이날 재경부 기사의 절반 이상을 담당자가 숨김). 하루 약 50~60원. 부정 26건 중 숨긴 것을 뺀 17건이 홈에 뜬다. 프롬프트에 의견서 예외를 넣었는데도 한방병원 기사는 여전히 부정으로 나왔다.

**기각**: 소제목 분류 호출에 얹기(하루 20~30원 추정이나 핵심 분류 품질을 건드린다 — 실측 없이 보류), 켜고 끄는 스위치(하루 30~40원이면 필요 없다고 봄).

**소제목 칩 → `<소제목>` 글자 (2026-09-22)**: 목록의 소제목 칩(회색 테두리 알약)이 바로 옆 「오늘의 쟁점」의 검색어 그룹 칩(`.gchip`)과 모양이 같아 뜻이 갈리지 않았다. 칩 내용을 검색어로 바꾸는 안을 먼저 따졌으나 기각 — 실측(9/22, 부정 추정 11건): 11건 중 10건이 `재정경제부`/`재경부`를 달고 있었다(이 칸이 그 단어가 나온 기사만 모으니 당연하다). 나머지는 `이형일`·`재경위`·`재정경제기획위원회`가 한 기사에 2~4개씩. 반면 소제목은 `대미투자 국회 보고` 4 · 기타 4 · 나머지 1건씩이라 기사를 실제로 가른다. 그래서 내용은 소제목으로 두고 모양만 바꿨다: 시안 세 안(A 보고서 소제목 표기 `<…>` 회색 글자 / B 왼쪽 남색 띠의 각진 태그 / C 쟁점 순위 번호를 앞에 붙이기) 중 A안. 새 칩 모양을 만들지 않고 이미 「소제목」을 뜻하는 표기를 빌렸다. C안은 쟁점 이름이 회차 병합 이름이라 소제목 글자와 달라(「대미투자 1호 국회 보고」 vs 「대미투자 국회 보고」) 기사 소속으로 이어야 하는 부담이 있었다. 덧붙여 발견: 두 그룹에 같은 키워드가 있으면 `matched_keywords`에 같은 단어가 두 번 들어간다(`('재정경제부', '재정경제부')`) — 이번 변경과 무관, 미처리.

**소제목 표시 없앰 (2026-09-22)**: 같은 날 `<소제목>` 글자로 바꾼 뒤 담당자가 "굳이 안 보여줘도 된다"고 판단해 목록에서 소제목을 통째로 뺐다. 제목과 근거 한 줄로 충분하다. 시안 `mockups/NG_SUBHEADING_CHIP_MOCKUP.html`은 결정 기록으로만 남긴다.

## 화면 용어 — 제목 앞 화면 이름 칩, 탭 제목에 화면 이름, 「쓰레기통」 — 2026-09-18

- 화면 이름 전수 조사에서 드러난 것: 정기 확정본 화면엔 「확정본」이라는 글자가 없었다(탭·제목이 보고서 머리줄 「언론 모니터링 N시 기준」). 수시 원본·확정본은 탭 제목이 둘 다 「수시 모니터링 — 사안명」이라 같은 사안이면 탭으로 못 가렸다. 숨긴 기사 화면은 「숨긴 기사 관리」(제목)·「쓰레기통」(링크) 두 이름이었다.
- 사용자 제안: 이름을 제목 앞 **칩**으로 — 복사되지 않게. 초안도 헷갈리지 않게 확정본과 같은 문장으로 맞춤: `(초안) 언론 모니터링 14시 기준(예정, 지금 …)` / `(확정본) 언론 모니터링 11시 기준`. 수시도 같은 규칙으로 `(원본)` 회색·`(확정본)` 청록을 앞에(수시 확정본 칩은 원래 머리줄 뒤에 있었다). 초안 제목의 문서 아이콘은 칩과 겹쳐 뺐다. 초안 복사 첫 줄 `[초안] 언론 모니터링 …`은 원래 이 모양이라 그대로.
- 탭 제목: 화면 이름을 맨 앞에(좁아지면 뒤가 잘린다). 수시 확정본은 목록 이름(bundle_label)을 써서 같은 사안 확정본끼리도 갈린다.
- `/hidden`은 「쓰레기통」으로 — 팝오버 링크·🗑 아이콘과 맞춘다. 수시 확정본 좌하단 「숨긴 기사」는 카드 안 목록이라 이름을 안 바꿨다.
- 시안 `mockups/NAME_CHIP_MOCKUP.html`(제안안), `mockups/TAB_TITLE_MOCKUP.html`.
- 같은 날 이어서: 「쓰레기통」 → 「휴지통」. 하는 일은 「숨기기」이고 7일 안에 되살릴 수 있는데 「쓰레기통」은 버린다는 느낌이 강하다. 「휴지통」은 맥·윈도우 한국어 이름이라 「지웠지만 되살릴 수 있고, 시간이 지나면 비워진다」로 바로 읽힌다(🗑 아이콘도 그대로). 「숨긴 기사」는 수시 카드 안 목록과 겹쳐 기각. 받침이 같아(ㅇ) 조사는 그대로. 화면·주석·테스트·현행 문서 전부, 이 파일의 과거 항목은 그대로.

## 화면 용어 — 「실시간 현황」·「자동발송」·「텔레그램 받는 사람」으로 표기 통일 — 2026-09-18

- 화면 이름 전수 조사에서 같은 것을 여러 이름으로 부르던 곳을 사용자 결정으로 정리.
- 실시간: 화면은 「실시간」, 규칙서·주석은 「실시간현황」 66곳·「실시간 현황」 34곳이 섞여 있었다 → 전부 「실시간 현황」. 화면에선 흐름도 칸·탭 제목·상단바 링크(확정본·초안·정기 보관함)·홈 말풍선·API 호출량 안내·네이버 키 안내. 행 머리 「● 실시간」 배지(상태 표시)와 검색어 목적지 스위치 「실시간」은 화면 이름이 아니라 그대로.
- 자동 발송: 「자동발송 설정」(붙임)과 본문 「자동 발송」(띄움)이 섞여 있었다 → 「자동발송」으로 붙여 쓴다(화면 `🤖 N시 M분에 자동발송 완료` 포함).
- 「텔레그램 수신 대상자 지정」 → 「텔레그램 받는 사람」(설정 메뉴·탭·제목·알림 설정의 안내 링크). 규칙서가 이미 이 이름을 쓰고 있었다.
- 같은 날 이어서: 검색어 화면 목적지 스위치 「실시간」과 저장 바 캡션도 「실시간 현황」으로(화면 이름과 스위치 이름이 같아야 어디로 가는지 읽힌다). 실시간 현황 화면 안의 「● 실시간」 배지는 상태 표시라 그대로. 「이메일 수신 대상자 지정」 → 「이메일 받는 사람」(텔레그램과 설정 메뉴에 나란히 선다).

## 화면 용어 — 정기 「확정안」 → 「확정본」 되돌림, 수시 흐름도 이모지 🍅 → 🥗 — 2026-09-17

- 사용자 결정: 두 흐름의 ③ 보고서 칸은 기능이 같은데 이름이 달라(확정안/확정본) 오히려 헷갈린다 → 둘 다 「확정본」. 같은 날 아침의 「확정본 → 확정안」을 그대로 되돌렸다(그 변경 전 커밋 HEAD엔 「확정안」이 0회라, 현행 코드·테스트·문서의 「확정안」 438곳을 기계적으로 「확정본」으로 바꿔도 다른 뜻이 섞이지 않는다). 받침이 같아(ㄴ) 조사는 그대로 맞는다.
- ② 칸은 이름을 다르게 둔다. 정기 초안은 **덜어내서** 그대로 확정본이 되는 곳, 수시 원본은 **많은 기사 중 골라 옮기는** 곳이라 뜻이 다르다. 그 차이를 흐름도 이모지로 말한다: 정기 ⛏️ → 💎(캐서 깎는다), 수시 📃 → 🗂️ 였던 것을 🍅 → 🥗(골라서 한 접시에 모은다)로.
- 검토한 수시 ② 이름: 결과(이미 기각 — 일반 표현과 겹침), 후보(인사청문회 「후보자」와 겹침), 재료·원석. 사용자가 「원본」 유지로 결정.
- 검토한 이모지: 과일 따기 🌳 → 🧺, 낚시 🌊 → 🎣, 집게 🧩 → 🥢, 홀케이크 🎂 → 🍰. 케이크는 「잘라서 덜어낸다」라 정기의 비유와 겹치고, 칸 크기에서 둘 다 크림·분홍이라 구분이 약해 기각. 시안 `mockups/FLOW_EMOJI_MOCKUP.html` A안.
- 같은 날 검토한 수시 보관함 「정리함」·🗃️(`trash-can/ARCHIVE_NAME_MOCKUP.html`)은 보류 — 이번엔 이름을 바꾸지 않았다.
- 규칙서에선 두 확정본을 「정기 확정본」/「수시 확정본」으로 가른다(CLAUDE.md 화면 용어).
- 범위: 화면 글자와 코드 주석·테스트 문구·현행 문서. 내부 용어·저장 데이터·URL은 안 바뀐다. 보고서 텍스트 `[초안] 언론 모니터링 …`·`스크랩초안_….txt`는 원래 「확정안」이 없어 영향 없음. 이 파일의 과거 항목·`archive/`·`mockups/` 옛 시안은 당시 이름 그대로 둔다.
- 남은 일(사용자와 합의, 미착수): 화면 이름을 `app/config.py` 한 곳으로 모으기(상단바 6곳 복제 통합 → 이름 상수 + 조사 함수).

## 화면 용어 — 정기 「확정본」 → 「확정안」 — 2026-09-17

- 사용자 요청: 정기 흐름의 「확정본」을 「확정안」으로. 이유는 「초안 → 확정안」으로 짝이 맞게.
- 범위: 정기 쪽 화면 글자(홈 흐름도 칸·상단바 링크·초안 마감 배너·검색어 목적지 스위치 「초안·확정안」과 저장 차단 문구·설정 안내·툴팁·확인창)와 코드 주석, 테스트 문구, 현행 문서(CLAUDE.md·PRD.md·README.md·LLM_COST_USAGE.md·CODING_CONVENTIONS.md·ADHOC_DESIGN.md의 정기 언급).
- **수시의 「확정본」은 그대로 둔다** — 수시엔 초안이 없어 짝 맞출 이유가 없고, 사용자가 「정기의 확정본」으로 범위를 정했다.
- 내부 용어(`index.html`, `confirmed`, `/regenerate-subheadings-final` 등)는 안 바뀐다.
- 이 파일(HISTORY.md)의 과거 항목, `archive/`, `mockups/`·`figma/` 시안은 당시 기록이라 옛 이름 「확정본」을 그대로 둔다.

## 회차 파일 날짜 불일치 — 2026-08-26

**증상**. 홈 시안을 만들다 `data/articles/2026-08-26_19-30.json`의 `run_at`이 `2026-08-25T19:33:47`이고 기사 70건이 전부 08-25 17:02~19:28 발행인 걸 발견했다. `2026-08-25_19-30.json`은 아예 없었다 — 어제 저녁 회차가 오늘 이름표를 달고 있었다. 저장된 76개 회차 파일을 전수조사하니 어긋난 건 이 한 건뿐이었다.

**처음엔 피해를 과대평가했다.** "홈 건수·정기 보관함·워드클라우드가 다 오염된다"고 봤는데, 코드를 확인하니 **앱은 원래부터 날짜 판단을 전부 `run_at`으로 하고 있었다** — `list_run_dates`, `list_runs_for_date`, `load_today_runs`, `delete_expired_runs`, `app.run_index.find_path` 전부. `run_index.py`에는 "파일명으로는 못 짚는다(파일명 날짜는 저장 시각, run_at은 실행 시각이라 자정을 넘겨…)"는 주석까지 이미 있었다. 오염된 건 앱이 아니라 파일명으로 glob하던 **시안 생성 스크립트**였다.

**진짜 피해는 다른 데 있었다.** 파일명 자체가 키라서 두 가지가 깨진다.

1. **회차 통째 누락**: `run_exists(run_slot, date_str)`만은 파일명으로 판단한다(스케줄러가 "이 회차 이미 돌았나"를 보는 자리). 어제 회차가 오늘 이름으로 있으면 오늘 그 회차를 "이미 실행됨"으로 오판해 **건너뛴다**. 실제로 그날 저녁 19:30 회차가 그렇게 사라질 상황이었다.
2. **앞 회차 덮어쓰기**: 같은 이름이 다시 필요해지면 `write_text`가 그대로 덮는다. 임시 폴더에서 재현해보니 어제 70건이 오늘 5건에 덮여 사라졌다.

**고침**. `save_run`이 파일명 날짜를 `datetime.now()`가 아니라 **`run_at`에서** 뽑게 했다(`_date_from_run_at`). 파일명이 `{run_at의 날짜}_{run_slot}.json`으로 항상 성립하므로 두 결함이 구조적으로 사라진다. `run_at` 형식이 깨졌을 때만 현재 시각으로 물러서고 경고를 남긴다 — 회차를 저장하지 못하는 것보다 날짜가 하루 어긋난 채로라도 기사를 지키는 게 낫다.

**파일명을 만드는 곳이 `save_run` 하나뿐인지 확인하고 고쳤다.** `update_run_articles`/`confirm_run`/`record_send`/`app.undo`는 전부 `_find_run_file(run_at, run_slot)`으로 **이미 있는 경로**를 찾아 쓰므로 새 이름을 짓지 않는다. 그래서 이 한 곳을 고치면 전부 덮인다.

**원인 경로는 확정하지 못했다.** `run_at`이 어제 19:33이면 그때 실행된 것인데, 파일이 오늘 09:10에 쓰였다. `save_run`의 호출부가 `collect_run` 하나뿐이라 큐레이션 재저장은 아니다. 여기서 더 좁히려면 실행 기록이 필요했는데 — 바로 그날 오전에 로그 파일을 넣은 이유가 이것이었다(위 "로그 파일 도입"). 다만 원인이 무엇이든 **어긋남 자체가 구조적으로 불가능해졌으므로** 고침은 원인 규명과 독립적으로 유효하다.

**데이터 복구**: 잘못된 이름의 파일을 올바른 이름(`2026-08-25_19-30.json`)으로 복사하고, 원본은 `trash-can/`으로 옮겼다(작업 규칙 — 바로 삭제하지 않음). 복구 후 `run_exists('19:30', '2026-08-26')`가 False로 돌아와 그날 저녁 회차가 정상 실행될 수 있게 됐고, 08-25는 6회차 전부가 제자리를 찾았다.

**회귀 테스트**: `tests/test_integration.py`의 `test_run_filename_follows_run_at_not_save_time` — 자정을 넘긴 회차의 파일명, 같은 슬롯 다른 날짜의 비덮어쓰기, `run_exists`의 날짜별 구분, 깨진 `run_at`에서도 저장이 실패하지 않는 것 네 가지를 함께 본다.

## 담당자 > AI: 정렬·분류 우선순위 3종 확정 — 초안→확정본 소제목 붕괴 재발 (2026-08-14)

2026-08-14 09:30 회차에서 초안이 확정본으로 넘어가며 소제목이 또 뭉개졌다는 제보. 2026-08-13에 한 번 고쳤던(`classify_for_finalize` 도입) 같은 증상이라 저장 데이터를 전수로 다시 뜯어 **원인이 두 개**임을 확인했다.

- **원인 1 — 큐레이션 핸들러가 숨긴 기사까지 분류에 넣었다.** `app/scraper.py` `collect_run`은 `filter_hidden`을 거친 **보이는 기사 9건**으로 분류하고 그 구성으로 캐시를 심는데(`seed_cache`), `app/settings_server.py`의 큐레이션 핸들러들(`_handle_move_article`·`_handle_bulk_move_article`·`_handle_undo` 등)은 `run["articles"]` = **숨긴 기사 포함 14건**을 그대로 `snapshot_group_names`에 넘겼다. 기사 집합이 다르니 캐시 정확 적중이 실패하고, `_find_reusable`은 "기사가 줄어들기만 한" 경우만 재사용하는데 여기선 오히려 늘어난 쪽이라 그것도 실패 → **진짜 API 재호출**이 나가 전체가 새로 분류됐다. 실측 재현: 09:30 회차 캐시를 사고 직전 상태(보이는 9건짜리 항목만)로 되돌린 뒤 14건을 넘기면 API 호출 시도 2회(1회+재시도), 9건을 넘기면 0회. 결과가 회차 파일에 그대로 남아 있었다 — 담당자가 쓰던 `세제 개편 발표`가 `세제 개편 관련`으로 바뀌고, 손수 만든 `<우리부 관련>` 옆에 AI가 지은 `우리부 관련`이 따로 생겨 화면엔 같은 이름 소제목이 두 개, 없던 `비거주 1주택 세제`까지 새로 생겼다. 소제목 이름을 키로 저장되는 `group_order.json`/`group_labels.json`/`group_overrides.json`이 한꺼번에 고아가 된 것이 "작업한 게 AI로 복귀"의 실체다. → `snapshot_group_names`가 **자기 안에서** `filter_hidden`을 거쳐 분류하고(= `collect_run`과 같은 입력), 숨긴 기사는 원래 `group` 값을 그대로 유지한 채 반환 목록에 남기도록 고쳤다. 호출부를 하나씩 고치지 않고 함수 안에서 막은 이유: 같은 실수를 할 수 있는 호출부가 6곳이고 앞으로 더 늘어난다.
- **원인 2 — 캐시가 담당자의 ↑/↓ 순서를 매번 되돌렸다.** `app/llm_classifier.py` `_rebuild`가 소제목 안 기사를 **캐시에 적힌 URL 순서**대로 늘어놨다. 그 순서는 곧 "캐시를 만든 시점의 언론사 우선순위 정렬"이라, 담당자가 ↑/↓로 올려둔 기사가 렌더링 한 번에 원래 자리로 되돌아갔다(확정본·초안 양쪽). 실측: 같은 소제목 안 두 기사를 맞바꾼 뒤 `classify_articles`를 다시 부르면 순서가 그대로 원상복구됨(수정 후 유지됨 확인). 다른 분류 경로(`_parse_groups`·`_apply_forced_groups`)는 이미 "입력 순서 유지" 원칙을 지키고 있었고 여기만 예외였다. → 캐시는 **소속만** 정하고 **순서는 지금 넘어온 리스트**를 따르도록 고쳤다. 같은 이유로 `snapshot_group_names`의 반환 순서도 분류 결과 순(소제목별로 뭉치는 순서)이 아니라 입력 순서 그대로로 바꿨다 — 큐레이션 조작 한 번마다 저장 목록이 재배열되면 잡아둔 순서가 계속 흔들린다.
- **규칙 변경 — ↑/↓는 소제목 경계를 넘지 않는다.** 원래는 소제목 맨 위/아래에서 한 번 더 누르면 옆 소제목으로 기사가 통째로 넘어갔다(규칙21). 순서를 다듬다 한 번 더 눌렀을 뿐인데 분류가 바뀌어 기사를 놓치는 사고가 반복돼, ↑/↓를 순서 전용으로 좁히고 소제목 간 이동은 "다른 소제목" 드롭다운 전담으로 확정했다(사용자 결정). 버튼도 그 소제목의 첫/마지막 기사에서 바로 비활성화한다 — 예전엔 화면 전체의 첫/마지막에서만 꺼져서 "눌리는데 아무 일도 안 일어나는" 구간이 있었다. 수시 모니터링(`app/adhoc/card.py` `move_article_order`)이 처음부터 이 규칙이었으므로 이제 정기·수시가 같아졌다.
- **안 고친 것.** `app/curation.py` `move_article`의 반환값 `(new_articles, new_override)`에서 `new_override`는 이제 항상 `None`이지만 튜플 형태는 남겼다 — 호출부 두 곳(`_handle_move_article`, `preview_move_article`)이 이 형태를 기대하고, 두 곳 다 `None`일 때의 경로가 이미 정상 동작이라 지금 바꿔봐야 얻는 게 없다.

## 숨김 제목 대조 되돌림 — 제목 대조로 인한 조용한 실종, 자동 숨김을 없앴다 (2026-08-27)

### 제보

"이 기사는 '스크랩' 됐고 [초안]에서도 확인했는데 [확정본]에서는 안보여. 왜그래?" —
조선일보 `한은, 올해 성장률 전망 2.6%→3.3% 상향`(09:53). 실시간현황 화면에서는
`✔️ 스크랩됨` 배지가 붙어 있었다.

### 원인 — 두 겹이었다

**1. 제목 대조가 다른 기사를 잡아먹었다.** 10:32에 담당자가 숨긴 건 **세계일보**의
`한은, 올해 성장률 전망 2.6%→3.3% 상향`(10:12)이었는데, 조선일보 기사의 제목이
한 글자까지 같았다. `filter_hidden`이 URL **또는 제목**으로 대조했으므로 누른 적
없는 조선일보 기사가 함께 걸러졌다. 회차 파일(`2026-08-27_11-00.json`)에는
`group: 기타`로 멀쩡히 저장돼 있었고, 사라진 건 화면에 그릴 때뿐이었다.

**2. 실시간현황만 판정이 달랐다.** `app/live_renderer.py`가
`article["url"] in hidden_urls`로 URL만 봤다 — 확정본은 제목까지 보는데. 그래서
"확정본에서 걸러질 운명인 기사"에 `✔️ 스크랩됨`을 붙여 보여줬다. `curation.py`가
2026-08-25에 `hidden_match_keys()`/`is_hidden()`으로 판정을 한 곳에 모아두고
"두 벌로 갈라지면 어긋남이 재발한다"고 주석까지 달아뒀는데, 실시간현황만 그
규칙을 안 따르고 있었다(수시 모니터링은 따르고 있었다).

담당자가 원인을 추적할 방법이 **하나도 없었다** — 실시간현황은 거짓말했고,
확정본엔 그냥 없었고, "숨긴 기사 관리"에도 조선일보 기사는 안 뜬다(숨김 기록엔
세계일보만 있으니). 오늘만 이렇게 조용히 사라진 게 5건이었다.

### 같은 메커니즘이 정확히 일주일 전엔 반대 방향으로 터졌다

제목 대조를 넣은 게 바로 이 상황 때문이다(2026-08-20, 아래 항목):

> 숨긴 기사가 그 제목의 대표였다가 나중에 우선순위가 더 높은 언론사가 같은 제목으로
> 기사를 내면 대표가 교체되면서 부활한다

8/20의 담당자는 "숨겼는데 왜 **되살아나**?"였고, 8/27은 "숨기지도 않았는데 왜
**사라져**?"다. 코드가 보는 신호는 양쪽 다 "다른 URL, 같은 제목"으로 똑같아서
구별할 방법이 없다. 당시 근거였던 "제목만으로 판단해도 화면에서 사라지는 기사가
늘지 않는다(dedup이 어차피 1건만 남기므로)"는 전제가 깨지는 지점이 여기다 —
**dedup 대표는 회차마다 바뀔 수 있다**. 색인이 늦은 상위 언론사 기사가 나중에
들어오면 대표가 교체되고, 그때 옛 숨김 기록이 새 대표를 잡는다.

### 처음엔 배너로 보정하려 했다 (기각)

1차 대응으로 확정본·초안 상단에 "제목이 같은 다른 기사를 숨겨서 함께 안 보이는
기사 N건" 배너 + 되돌리기 버튼을 만들었다(`title_collision_dropped`,
`unhide_by_title`, `POST /unhide-by-title`). 실제로 동작했지만 사용자가 방향을
뒤집었다 — **"ai가 임의로 숨기는게 없어야 하네"**, **"기사 1개 1개가 소중해서
아예 '자동으로 숨기는' 것을 없애는게 낫지 않아? 제목이 똑같아도 이용자가 다 볼 수
있게 하는거지"**. 자동 숨김을 없애면 배너가 알릴 대상 자체가 사라지므로
("그렇게 되면 이런 불필요한 배너도 필요 없을거 아냐") 배너 코드는 통째로 걷어냈다.

### 세 방식을 실측으로 비교했다

오늘 회차 데이터 기준:

| 방식 | 되살아나는 기사 | 잘못 숨기는 기사 |
|---|---|---|
| 제목만 (사고 당시) | 0건 | **3건** |
| 언론사+제목 | 0건 | 0건 |
| URL만 (채택) | 2건 | **0건** |

`언론사+제목`이 오늘 데이터에선 만점이었는데도 `URL만`을 골랐다. 근거는 **두 실패의
무게가 대칭이 아니라는 것**: 부활은 중복이 화면에 *보이고* 🗑️ 한 번 더 누르면
끝나지만, 과잉 숨김은 *안 보이고* 회차 파일을 직접 파봐야 원인을 안다. 그리고
`언론사+제목`에도 "담당자가 클릭 안 한 기사가 자동으로 숨는" 성질은 그대로 남는데,
없애려던 게 정확히 그 성질이었다. 되살아난 2건도 실제로는 뉴시스 사진 캡션이었다.

저장된 회차 전체(고유 제목 2,515건)로도 재봤다 — 제목 중복은 38건뿐이고 그중
**20건이 제목만 우연히 겹친 별개 기사**였다. 제목 대조가 구해내는 재게재(18건)보다
잘못 치우는 쪽이 더 많았다. 부활 위험은 숨김 기록이 자정에 초기화되므로 하루 안으로
갇힌다는 점도 판단에 들어갔다.

### 최종

- `filter_hidden`/`is_hidden` — URL만 대조. `hidden_match_keys()`는 이미 있던
  `load_hidden_urls()`로 대체해 없앴고, `is_hidden(article, hidden_urls)` 2인자로
  줄였다(호출부 7개 파일: live/landing/trend_renderer/trend_data/adhoc 2곳/curation).
- 실시간현황이 `is_hidden()`을 거치도록 고쳤다 — **이 수정만은 남긴다**. 자동 숨김이
  없어져도 "URL로 직접 숨긴 기사"의 표시는 두 화면이 같은 기준을 봐야 한다.
- `title_collision_dropped`/`unhide_by_title`/배너 HTML·JS/`POST /unhide-by-title`/
  `raw_articles`·`raw_out` 배선 전부 제거.
- `hide_article`의 outlet/title은 이제 순수 표시용("숨긴 기사 관리" 화면이 URL만
  보여주지 않으려는 용도)이고 대조에 관여하지 않는다. 세 경로가 `hideArticleBody`를
  공유하고 서버가 `_hide_meta_fallback`으로 채우는 구조는 그대로 뒀다.

검증: 11:00 회차 45건 → 화면 12건, 빠진 33건이 **전부** URL로 직접 숨긴 것임을
확인(제목으로만 걸린 기사 0건). 통합 테스트 11개 통과.

## AI 분류 실패가 조용히 묻히던 3중 결함 (2026-08-27)

### 제보

홈 화면 "오늘의 쟁점"에 `국회`(2위) · `1주택자`(3위) · `이를`(5위)가 떴다. 사용자: "왜 또 이렇게 뜨는거?"

### 추적

그 이름들은 06:00 회차에 저장된 소제목이었고, `이를`·`한다`·`했다`·`강조`는 규칙 기반(단어 빈도) 폴백의 산물이다.

```
2026-08-27_06-00.json → 국회 4 · 1주택자 3 · 기타 3 · 이를 2 · 한다 1 · 했다 1 · 부총리 1 · 강조 1
```

로그에는 06:04:04에 Anthropic POST 200이 딱 한 번 찍혀 있고 경고가 한 줄도 없었다. LLM 캐시 파일에도 `06:00` round_id 항목이 아예 없어(09:00·09:30·11:00·14:00은 있음), **호출은 성공했는데 쓸 만한 분류를 못 받았다**는 게 확인됐다. 캐시 상한(40개) 때문에 밀려난 것도 아니다 — 그 사이 호출이 2번뿐이라 40에 닿을 수가 없다.

### 결함 셋이 겹쳐 있었다

**(1) 빈 응답이 재시도를 안 썼다.** `classify_with_llm`의 재시도 루프는 **예외일 때만** 재시도했다. HTTP 200인데 응답에 text 블록이 없거나 `_parse_groups`가 빈 결과를 주면 그냥 `break`로 빠져나가 남은 재시도 1회를 버렸다. 게다가 로그를 한 줄도 안 남겨, **결국 둘 중 어느 쪽이었는지 끝내 판별하지 못했다** — 코드가 증거를 버린 것이다.

**(2) `classification_degraded`가 저장된 적이 한 번도 없었다.**

```bash
python3 -c "import json,glob;print({k for f in glob.glob('data/articles/*.json') for k in json.load(open(f)) if k!='articles'})"
```
→ 81개 파일 **전부** 이 필드 없음. `finalize_new_subheadings`도 마찬가지.

원인은 `collect_run`이 `{"run_slot", "run_at", "articles"}` 세 개만 손으로 지어 돌려주고, 그 값을 받은 `confirm_and_promote`가 곧바로 `confirm_run`을 부르는데 그 함수가 `{**run, "confirmed": True, ...}`로 **파일을 통째로 덮어쓰는** 구조였다. `save_run`이 기껏 적어둔 필드가 저장 몇 밀리초 뒤에 사라진 것이다. 그 탓에 도입 이후 한 번도 동작한 적 없는 기능이 셋이었다 — 확정본 "⚠️ AI 분류 실패" 배너, 분류 실패 시 자동발송 차단, "마감 후 자동 배정" 배지.

**(3) 최신이 아닌 회차는 손댈 방법이 없다.** 재분류를 포함한 큐레이션 핸들러 8곳이 전부 `load_latest_run()` 하드코딩이고, 정기 보관함은 설계상 읽기 전용이다. 06:00 회차가 09:30 회차에 밀려난 뒤로는 담당자가 복구할 자리가 아예 없었다.

### 폴백 회차를 사후에 알아보는 법 — 실측으로 문턱을 찾았다

(2) 때문에 옛 회차에는 판정 근거가 저장돼 있지 않다. 저장된 데이터만으로 알아볼 방법을 찾으려고 80개 회차 전수를 재봤다 — "소제목 이름 중 한 어절(공백 없음)짜리 비율":

| 비율 | 회차 수 | 내용 |
|---|---|---|
| 0% | 68 | 정상 |
| 10~30% | 8 | `반부패정책협의회` `중앙지방협력회의` `금리정책` `예결위` — **정상 AI 이름** |
| **100%** | **4** | `2026-08-27_06-00` `2026-08-11_17-00` `2026-08-25_09-30` `2026-08-22_10-00` |

**0.3과 1.0 사이가 완전히 비어 있었다.** 100%인 4개는 전부 규칙 기반 폴백 회차였고 오탐이 0건이었다. 반면 처음 떠올린 "이름 하나하나를 보고 한 어절이면 버린다"로 갔다면 위 10~30% 구간의 멀쩡한 이름 10개를 같이 죽였을 것이다 — **이름 단위가 아니라 회차 단위로 봐야 갈린다**는 것이 실측의 결론이다(CODING_CONVENTIONS §1).

소제목이 3개 미만이면 판정하지 않는다. 정상 AI 이름이 우연히 전부 한 어절일 수 있어 표본이 너무 적고, 틀리는 방향이 "폴백을 못 알아본다"여야지 "멀쩡한 회차를 폴백으로 몰아붙인다"이면 안 되기 때문이다.

### 고친 것

1. `app/scraper.py` `collect_run`이 **방금 저장한 파일을 다시 읽어 돌려준다.** 필드를 손으로 나열해 채우는 대신 되읽는 이유는, 나열하면 `save_run`에 필드가 늘 때마다 여기도 같이 고쳐야 하고 안 고치면 같은 사고가 조용히 재발하기 때문이다. 되읽으면 "collect_run이 돌려주는 값 = 디스크에 저장된 값"이 구조적으로 보장된다. 회차당 파일 1개(하루 6번)라 비용은 무시할 수준.
2. `app/llm_classifier.py` — 빈 응답도 실패로 취급해 `continue`하고, 그 지점에 `stop_reason`·content 블록 타입·응답 앞부분을 `logger.warning`으로 남긴다. 다음에 같은 일이 나면 5초 만에 판별된다.
3. `app/classifier.py` `looks_rule_based`/`run_looks_rule_based` — 저장된 `classification_degraded`가 있으면 그 값을 우선하고(수집 시점에 실제로 관측한 사실이 사후 추정보다 정확하다) 없을 때만 이름 모양으로 추정한다. **소급 적용**되므로 오늘 06:00 회차도 즉시 걸린다. 확정본 배너·자동발송 차단(`app/confirm_send.py`)·정기 보관함 표식(`.degraded-mark`, 앰버) 셋이 이걸 쓴다.

### 남긴 것

(3)의 "최신이 아닌 회차 재분류"는 **일부러 안 만들었다.** "정기 보관함은 읽기 전용"이라는 기존 원칙을 깨는 일이라, 판정만 먼저 붙여 "이 회차는 AI가 못 묶은 회차"라는 사실을 정직하게 알리는 데서 멈췄다. 복구 통로를 열더라도 폴백으로 판정된 회차에만 열어 원칙 훼손을 최소화하는 쪽으로 간다 — [MORNING_ISSUES_DESIGN.md](archive/MORNING_ISSUES_DESIGN.md) §4.3.

### 곁가지로 드러난 더 큰 문제

이걸 파는 과정에서 홈 "오늘의 쟁점"이 정기 회차에 100% 종속이라는 점, 그래서 **담당자가 등록한 검색어 5개 그룹 중 2개(세제개편·추가 검색어, 오늘 972건)가 쟁점 카드에 원리적으로 못 올라온다**는 사실이 드러났다. 하필 담당자가 정책 단어 추이에서 지켜보겠다고 고른 단어(`종부세`·`비거주`)가 그 빠지는 그룹에 있었다. 그 대응은 별도 설계로 뺐다 — [MORNING_ISSUES_DESIGN.md](archive/MORNING_ISSUES_DESIGN.md).

## 앱 중복 실행 — 두 프로세스가 같은 회차를 각자 확정하던 사고 (2026-09-02)

담당자 제보: "또 초안에서 확정본 넘어가면서 소제목이 통째로 바뀌고, 초안에서 이미 본
기사들이 「마감 후 자동 배정」으로 떴다."

### 관측

- `ps`에 `main.py`가 **두 개** 떠 있었다(16:19 시작·16:47 시작).
- 로그에 같은 회차가 두 번 저장돼 있었다 — `17:00:11 (run_at=17:00:01)`,
  `17:00:16 (run_at=17:00:05)`. 저장된 파일의 `run_at`이 `17:00:05`이므로 담당자가 본
  확정본은 **나중에 켠 프로세스가 만든 것**이었다.
- 화면 배너: "기사 26건이 자동으로 배정됐어요 · 소제목 3개는 이때 새로 만들어졌어요."
- 확정본 소제목은 `국제경제 외교`·`이형일 부총리 후보 인사청문회`처럼 **멀쩡한 LLM
  이름**이었고 `classification_degraded`도 `False`였다 — **규칙 기반 폴백이 아니었다.**
  문제는 폴백이 아니라 초안에서 다듬어둔 이름(`G20 재무장관·중앙은행 총재 회의`,
  `부총리 후보자 언론간담회 등`, `중앙행정기관·공공기관 이전 계획`)이 통째로 다른
  이름으로 갈아엎힌 것이었다. 체감은 폴백과 같아 제보 문구도 그렇게 나왔다.

### 원인

LLM 분류 캐시는 **프로세스 시작 때 한 번만** 파일을 읽고(`_ensure_cache_hydrated`),
저장할 땐 **메모리 전체를 파일에 통째로 덮어썼다**(`_persist_cache_locked`). 프로세스
간 공유 장치가 하나도 없었다.

1. 16:47:20 — P2 시작, 그 시점의 캐시 파일을 읽음
2. 16:47:43 / 16:48:29 — **P1**(설정 서버를 쥔 쪽)이 초안 22건을 분류해 파일에 저장.
   P2는 이미 하이드레이트를 끝내 이 결과를 영영 모른다
3. 17:00 — P2가 확정 처리. 자기 메모리에서 찾을 수 있는 최선이 **14:39경에 만들어진
   11건짜리 옛 초안 스냅샷**뿐이라 `_find_partial`이 그걸 재사용 → 나머지 26건이
   미분류로 떨어지고 `assign_to_existing`이 소제목 3개를 새로 지어냄
4. P2가 캐시 파일을 자기 메모리로 덮어써, P1이 저장해둔 22건짜리 분류가 **디스크에서도
   사라졌다**(사후 확인: 캐시 파일에 그 항목이 아예 없음)

두 번째 앱이 살아남은 이유는 bind가 데몬 스레드 안에서 일어났기 때문이다 —
`Address already in use`로 그 스레드만 조용히 죽고 **스케줄러 스레드는 계속 돌았다.**

덤으로, 두 프로세스가 각각 `collect_run`을 도는 바람에 `clear_preview_order()`·
`clear_draft_pending_articles()`도 두 번 불려, 먼저 돈 쪽이 비운 뒤 나중 쪽이 계산해
**초안에서 ↑/↓로 정리한 순서가 사라졌다.** `group_overrides`·`hidden_articles`의
읽기-수정-쓰기 잠금도 프로세스 안에서만 걸려 있어 두 앱 사이엔 무방비였다.

### 고친 방법 — 포트를 단일 실행 자물쇠로

`main.py`가 설정 서버 포트를 **데이터를 건드리는 어떤 단계보다 먼저, main 스레드에서
동기로** bind한다(`app.settings_server.create_settings_server`). 실패하면 "이미 앱이
켜져 있다"는 뜻으로 읽고 브라우저만 그 앱으로 열어준 뒤 물러난다.

- **PID 파일을 안 쓴 이유**: 앱이 강제 종료되면 "죽은 자물쇠"가 남는다. 포트는 OS가
  프로세스 종료 시 알아서 풀어준다.
- **실측(2026-09-02)**: `HTTPServer`는 `allow_reuse_address = 1`(SO_REUSEADDR)이지만,
  다른 프로세스가 **LISTEN 중인** 같은 주소·포트에 거는 bind는 그래도 `EADDRINUSE`로
  실패한다(errno 48). 자물쇠로 쓰기에 안전하다는 걸 실제로 확인하고 넣었다.
- **부수효과가 오히려 이득이다**: 앱이 멈춘 것 같아 다시 실행했을 때(이 날도 10:49~10:57
  `settings.json` PermissionError 폭주 뒤에 두 번 다시 켰다) 조용히 두 개가 도는 대신
  **원래 창이 열린다** — 담당자가 실제로 하는 행동을 벌주지 않고 받아준다.

**2차 방어선**으로 `_persist_cache_locked`를 "덮어쓰기"에서 "파일을 읽어 병합한 뒤
쓰기"로 바꿨다. 같은 키는 메모리 쪽이 이기고, 파일에서 온 항목은 최근 것부터 상한의
2배까지만 남긴다. 중복 실행 자체는 포트 자물쇠가 막지만, 그 방어가 뚫려도 **남의 분류를
지우지는 않게** 하려는 것이다.

## 초안에서 숨긴 기사가 확정본에서 되살아나던 문제 — 원인은 정렬이었다 (2026-09-02)

같은 날 함께 제보된 두 번째 증상: "삭제한 게 다 튀어나왔어."

### 관측

`data/hidden_articles.json`의 16:52~16:56 기록 33건은 `group` 값이
`구윤철 부총리 G20 외빈 면담`(32건)·`G20 재무장관·중앙은행 총재 회의`(1건)이었다 —
**초안에서 소제목 통째로 숨긴 것**이지 실시간현황에서 낱개로 숨긴 게 아니었다.
그런데 확정본에는 제목·발행시각이 한 글자도 다르지 않은 같은 기사가 그대로 실려 있었다.

| 담당자가 숨긴 URL | 확정본에 뜬 URL | 제목 |
|---|---|---|
| 연합 001/00162859**32** | 001/00162859**31** | 구윤철 부총리, 스콧 베선트 미국 재무장관 면담 |
| 연합 001/00162859**37** | 001/00162859**39** | 구윤철 부총리, 니르말라 시타라만 인도 재무장관 면담 |
| 뉴시스 003/00141648**35** | 003/00141648**37** | 이탈리아 재무장관 만난 구윤철 부총리 |

…9쌍 전부 같은 패턴. **숨긴 ID는 확정본 회차 파일에 아예 없었고, 쌍둥이 ID만 있었다** —
즉 "숨김이 풀린" 게 아니라 **대표 기사가 뒤바뀐** 것이었다.

### 원인 — 부분 정렬이라 대표가 재현되지 않았다

통신사는 같은 기사를 네이버 기사 ID만 다르게 여러 건 올린다.
`app.filters.deduplicate_by_title`은 제목이 같으면 **"먼저 나온 1건"**만 남기는데,
그 "먼저"의 기준인 도착 순서가 두 화면에서 다르다:

- **초안** = 증분 캐시(`data/preview_cache.json`)에 여러 번에 걸쳐 쌓인 순서
- **확정본** = 마감 시점에 회차 시간창을 한 번에 재검색한 순서

`app.sorter.sort_by_outlet_priority`가 **언론사 순위 하나만 보는 안정 정렬**이라, 같은
언론사 안에서는 순서가 정의돼 있지 않고 입력 순서가 그대로 대표를 결정했다. 초안 캐시
위치를 실측하니 4쌍 모두 **담당자가 숨긴 쪽이 앞**(106↔110, 100↔107, 102↔108,
130↔145)이었다 — 초안에서는 그게 대표라 그걸 숨겼고, 확정본에서는 순서가 달라 쌍둥이가
대표가 됐다. 숨김 대조는 URL 하나뿐이라(2026-08-27 결정, "제목 대조로 인한 조용한 실종"
참고) 숨긴 적 없는 URL이 그대로 보고서에 실렸다.

### 측정 (오늘 원시 기사 1,702건)

| | 묶음 수 |
|---|---|
| 제목이 완전히 같은 묶음 | 34 |
| ↳ **같은 언론사끼리**(= 쌍둥이 케이스) | **11** |
| ↳ 다른 언론사 섞임 | 23 |

같은 언론사 11묶음은 전부 같은 기사 재게재이거나 서로 바꿔도 무방한 연속 사진이었고,
**우연히 제목만 겹친 별개 기사는 0건**이었다.

### 고친 방법 — 정렬을 완전 정렬로

`sort_by_outlet_priority`에 2차 키를 넣어 `(언론사 순위, 발행시각 최신순, URL)`로
정렬한다. 정렬 키가 결정론적이면 초안과 확정본이 **항상 같은 대표를 고르므로**, 담당자가
숨긴 URL이 곧 확정본에 나오는 URL이 된다. 검증: 오늘 원시 218건의 입력 순서를 30번
무작위로 섞어도 대표 기사 집합이 항상 동일했다(고치기 전에는 두 화면에서 갈렸다).

2차 키를 발행시각 최신순으로 잡은 건 사용자 결정이다 — URL 순도 재현되기는 하지만
순서에 뜻이 없고, 같은 언론사 안에서 최신 기사가 위로 오는 편이 읽기에도 낫다.
`pub_date`가 없거나 형식이 깨진 기사는 없는 시각을 지어내지 않고 맨 뒤로 보낸다.

### 기각한 대안

- **숨김 대조를 (언론사+제목)으로 되살리기** — 이걸로도 막히긴 한다. 하지만 2026-08-27에
  내린 "자동 판단이 담당자 대신 기사를 치우지 않는다"를 되돌리는 것이고, **정렬 수정이면
  그 원칙을 안 건드리고도 막힌다.** 원칙을 되돌릴 이유가 없었다.
- **확정본이 재검색 대신 초안 목록을 그대로 이어받기** — 문제 계열 전체(순서·분류·대표)를
  한 번에 없애는 가장 근본적인 길이지만, 마감 직전 기사를 놓칠 위험이 있고 수정 범위가
  너무 크다. 정렬 한 줄로 되는 일에 쓸 방법이 아니다.

### 남는 구멍

**실시간현황**은 "필터 없이 날 것" 원칙이라 중복 제거를 안 해 쌍둥이가 다 보인다. 거기서
대표가 아닌 쪽을 숨기면 확정본엔 여전히 대표 쪽이 뜰 수 있다. 이번 사고는 그 경로가
아니었고(초안 일괄 숨김), 그 화면에서 사진기사를 낱개로 숨기는 일이 실제로 잦아진 뒤에
다루기로 했다 — 지금 같이 고치면 그 화면의 원칙과 부딪힌다.

## 초안이 확정본보다 좁은 창을 보던 문제 (2026-09-04)

바로 아래 2026-09-03 항목의 **덜 끝난 절반**이다. 그때 `collect_run`의 하한만
`COLLECT_LOOKBACK_MIN`(60분)만큼 앞으로 물리고 초안(`_load_preview_search_base`)은
회차 시작을 바닥으로 그대로 뒀다 — 그래서 확정본은 앞 회차 기사를 주워 담는데
**초안은 그 기사를 끝까지 못 본다**.

담당자 제보(09:53): 실시간현황에는 09:27 뉴시스 「발전5사 합치고, LH 쪼개고…」가
버젓이 있는데 초안에도 확정본에도 없다. "이러면 앱의 존재 의미가 없다"까지 나왔고,
"차라리 설계를 바꿔 수동으로 넘기게 하자"는 말도 함께 나왔다.

**추적한 사실.** 그 기사는 09:27 발행이라 09:30 회차 창(06:00~09:30] 안인데,
09:30:07 수집 시점에 네이버가 아직 색인하지 않아 09:30 확정본에 안 들어갔다(같은 회차
로그에 `앞 회차 기사 회수 — 2건`이 남아 있다 — 이 방어 자체는 돌고 있었다). 그다음
11:00 회차는 하한이 08:30이라 **회수 대상이 맞다**. 즉 기사가 사라진 건 아니었다.
문제는 그 사이 담당자가 보는 화면인 초안이 09:30부터만 검색해서, 회수될 기사 7건이
통째로 안 보였다는 것.

측정(2026-09-04 09:54, 정기 스크랩 그룹 기준, 오늘 저장된 URL 제외 후 같은 필터를 태움):

| 창 | 건수 |
|---|---|
| 초안이 보던 창 (09:30~) | 11건 |
| 11:00 확정본이 볼 창 (08:30~) | 17건 |
| **초안엔 없고 확정본엔 들어올 기사** | **7건** (09:27 뉴시스 · 09:27/09:23 연합뉴스 · 09:11 뉴스1 · 09:09 뉴시스 2건 · 08:42 뉴시스) |

**고친 것.** 초안 바닥을 `slot_start − COLLECT_LOOKBACK_MIN`(당일 0시 하한)으로 맞춰
확정본과 같은 창을 보게 했다. 딸려 붙은 두 가지:

- **이미 나간 기사 제외를 초안에도 넣었다** — `collect_run`이 쓰던
  `_already_published_urls`를 **그대로 import**해서 쓴다(복제하지 않는다. 이 사고
  자체가 "같은 규칙이 두 곳에 갈려 있어서" 난 것이다). 자리도 collect_run과 같은
  자리 — 화이트리스트·정렬·중복제거보다 **먼저**. 나중에 빼면 이미 보고서에 나간
  기사가 `deduplicate_by_title`의 대표 자리를 차지해 새 기사를 밀어낸다.
- **`⏱ 앞 회차 기사` 칩이 초안에도 붙는다** — 안 붙이면 "왜 09시 기사가 11시 초안에
  있지?"라는 새 의문이 생긴다. `late_pickup`/`late_pickup_slot`은 확정본과 같은
  필드라 `render_article`이 알아서 그린다. 칩 CSS만 확정본 템플릿 안에 박혀 있어서
  `late_badge_style()`로 빼내 두 화면이 같은 값을 받게 했다(`.photo-badge`처럼 양쪽에
  복붙해 두면 한쪽만 고쳤을 때 조용히 어긋난다 — 이 사고의 축소판이다).

**캐시 서명에 `lookback_min`을 넣었다.** 서명이 안 바뀌면 더 좁은 창에서 모은 옛
캐시를 그대로 이어 써서, 고친 그 회차만 여전히 기사가 빈다. 값을 서명에 담아두면
규칙을 바꾸는 순간 캐시가 저절로 무효화돼 회차 시작−60분부터 한 번 다시 훑는다.

**남는 한계(감수).** 초안은 캐시 기반이라 두 번째 계산부터는 하한이 "지금−60분"으로
따라 올라간다(`incremental_search_after`). 그래서 발행 후 60분이 넘어서야 색인되는
기사는 초안이 못 보고 확정본만 회수한다 — 실측 색인 지연이 3~18분이라 실제로는 거의
안 걸리고, 회차 첫 계산은 항상 회차 시작−60분부터 전부 훑는다.

**설계는 안 바꿨다.** "수동으로 넘기는 식"은 필요 없었다 — 두 화면이 같은 창을 보게
하면 끝나는, 규칙이 한쪽에만 적용돼 있던 문제였다.

## 검색어 — 그룹 스위치 + 정기 체크 → 목적지 스위치 두 개 (2026-09-15)

- **요청**: `/keywords` 카드의 머리줄 on/off 스위치와 아랫줄 「☑ 정기 스크랩」이 갈라져 보인다 —
  「실시간 on/off」·「초안·확정본 on/off」를 나란히 두자. 홈 흐름도를 실시간 → 초안 → 확정본으로
  정리하고 있어 검색어 화면도 같은 말을 쓰는 게 맞다는 판단(사용자).
- **진단**: 두 컨트롤은 사실 한 질문(이 그룹이 어디에 쓰이나)의 답을 두 자리에 나눠 들고 있었다.
  계층(`enabled` ⊇ `include_in_scrap`)이라 네 조합 중 「꺼짐 + ☑정기」는 **정기에 체크돼 보이는데
  어디서도 검색되지 않는** 뜻 없는 상태였다. 맨 위 안내 첫 줄(「켜 둔 그룹은 모두 실시간…그중
  ☑정기 스크랩을 체크한 그룹만…」)은 이 계층을 풀어 쓰려고 있던 문장이다.
- **결정**: 목적지 스위치 두 개를 서로 독립으로(`include_in_live` 새 필드 + 기존 `include_in_scrap`).
  네 조합이 전부 뜻이 있고, 「초안·확정본만」이 새로 생긴다 — 실시간을 뒤덮는 넓은 검색어를
  실시간에선 빼고 보고서에만 남기는 쓰임. 연동(초안·확정본을 켜면 실시간도 켜짐)은 안 했다 —
  스위치 하나가 다른 스위치를 움직이면 "갈라져 보인다"가 모양만 바꿔 돌아온다.
- **시안** [KEYWORDS_TWO_SWITCH_MOCKUP.html](mockups/KEYWORDS_TWO_SWITCH_MOCKUP.html): A(머리줄 이름표 스위치 둘) ·
  B(머리줄 ✓ 알약 둘 — 9/11 「머리줄 정기 알약」을 기각한 이유(그룹 스위치와 헷갈림)는 사라졌지만
  바로 아래 키워드 칩과 모양 계열이 비슷했다) · C(아랫줄 체크 둘 — 가장 적게 바뀌지만 훑을 때 덜 걸림).
  이름은 `초안·확정본` / `정기 스크랩` / `보고서`를 비교. 사용자가 **A · 초안·확정본**을 골랐다.
- **옛 저장분 옮기기가 이 변경의 함정이었다**: `enabled`를 그대로 "실시간"으로 다시 읽으면 뜻이
  바뀐다 — 옛 `enabled=False`는 "둘 다 끔"이라, 정기 체크가 남은 꺼진 그룹이 옮기는 순간 정기에
  들어간다. 그래서 `초안·확정본 = 켜짐 AND 정기 체크`로 옮기고, 읽는 곳은 전부 `group_in_live`·
  `group_in_scrap`·`group_in_use` 헬퍼를 거치게 했다(옛 형식 dict도 옛 규칙으로 읽는다). `enabled`는
  `live or scrap`을 저장만 해 두는 파생값으로 남겼다 — 옛 코드로 되돌려도 초안·확정본 그룹이 그대로
  수집되고, 「어딘가에 쓰이는 그룹」이 필요한 두 곳([단독]·[속보] 감시 그룹, 추이의 등록 검색어
  칩)이 그 뜻 그대로 읽는다. 실데이터(5개 그룹) 옮긴 결과: 전부 실시간 켬, 초안·확정본은 예전 정기
  체크 그대로(재정경제부·재경위) — 달라진 수집은 없다.
- **덤으로 바로잡은 것**: 저장 바의 「정기 스크랩엔 다음 회차부터」는 초안까지 뭉뚱그렸다. 초안은
  요청마다 다시 그려져(`/preview.html` → `generate_preview_page`) 새 검색어로 곧바로 다시 모은다 —
  「실시간·초안엔 바로, 확정본엔 이번 회차 마감부터」. 저장 차단도 두 규칙(사용 중 ≥1, 정기 ≥1)이
  「초안·확정본 ≥1」 하나로 줄었다(그게 있으면 사용 중 ≥1은 저절로 참).
- **검증에서 걸린 것**: 648px 창에서 카드 두 장씩 놓으니 이름표 스위치 둘(약 180px)에 밀려 그룹명이
  「재정경」 「재경!」처럼 두세 글자로 잘렸다 → 두 장씩은 780px 초과에서만, 그룹명 칸 최소 5em.
  폰 폭(375px)에선 🗑만 혼자 아랫줄로 떨어져 → 스위치 둘 + 🗑를 한 묶음(`.head-ctrl`)으로.
  통합 테스트 27(옛 형식 옮기기 — 특히 「꺼짐 + 정기 체크」가 새지 않는지, 초안·확정본만 그룹이
  확정본엔 들어가고 실시간엔 안 들어가는지, 둘 다 끈 그룹, 저장 차단 문구) 추가, 전부 통과.
  앱은 새 코드로 다시 띄웠다.

## 첫 분류 기사를 다 숨기면 소제목이 사라지던 문제 (2026-09-17)

- **증상**: 14:00 회차 초안에서 소제목 `금융시장 점검·국채시장 관리`(이름표 「확대거시경제금융회의」)가 통째로 사라지고, 남은 KBS·MBC·문화일보 2건이 📂 소제목 미분류로 떨어졌다. 담당자는 기사를 숨기기만 했고 옮기지 않았다.
- **경위(로그·숨김 기록·배정 기록으로 확인)**: AI 첫 분류는 이 소제목에 3건(SBS·파이낸셜뉴스·뉴스핌)을 넣었다. 13:35:30 「AI 기사 배정」이 7건을 더 이 이름으로 `group_overrides`에 넣었다. 13:39:09~37에 담당자가 첫 분류 3건을 모두 숨기자 소제목이 사라졌다. 실제 캐시와 배정 기록으로 다시 돌려 같은 결과(미분류 4건)를 재현했다.
- **원인**: `_rebuild`는 보이는 캐시 기사가 하나도 없는 소제목을 뺀다. 그다음 `_apply_forced_groups`는 결과에 없는 이름으로의 배정을 조용히 무시한다(「사라진 소제목을 억지로 되살리지 않는다」). 그래서 배정으로 들어온 기사가 아직 보이는데도 소제목이 없어졌고, 이름표·순서도 이름에 묶여 있어 함께 사라졌다.
- **고침**: `llm_classifier.cached_group_layout`이 classify_with_llm과 같은 조회로 이번 회차 캐시의 `[(이름, 요약)]`을 돌려주고, `_apply_forced_groups(revivable_groups=)`가 배정 대상인 이름을 캐시 순서상 원래 자리에 빈 칸으로 끼운다. 끝까지 비면 마지막 필터가 버리므로, 가리키는 기사까지 다 숨기면 예전처럼 사라진다. `classify_articles`의 LLM 경로와 `classify_for_finalize` 두 곳에 넣었고, 확정안 큐레이션(`snapshot_group_names`)도 `classify_articles`를 거치므로 같이 적용된다.
- **기각**: `_rebuild`가 빈 소제목도 돌려주게 하는 안 — 수시 분류·배정 후보 등 `classify_with_llm`을 직접 쓰는 곳에 빈 소제목이 새어 나간다. `forced_group_target_names`에 넣는 안 — 캐시 순서 자리·요약을 잃고, 다른 회차의 옛 배정이 이름을 되살릴 수 있다(늦게 들어온 기사).
- 테스트 `test_assigned_articles_keep_subheading_after_ai_members_hidden`.

## 캐시 통째 비우기로 확정본 소제목이 규칙 기반으로 덮이던 문제 (2026-09-17)

- **증상**: 11:00 확정안 소제목이 `후보자` 11 · `부총리` 6 · `국민` 2 · `기타` 3처럼 한 어절 이름으로 바뀌었다. 홈 「오늘의 쟁점」에도 그대로 떴다. `AI 분류 실패` 표시·경고 로그는 없었다.
- **경위(로그·캐시 파일·되돌리기 스냅샷으로 확인)**: 11:00:05 마감 때는 초안 분류(10:29 호출)를 재사용해 AI 이름으로 저장됐다. 그때 `seed_cache`가 남긴 22건 항목(`이형일 경제부총리 인사청문보고서 채택` 6 · `美 금리인상 시장영향 점검 및 대응` 6 · `개각 관련 야당 비판·의문제기` 6 · `기타` 4)이 캐시 파일에 그대로 있었고, 11시 당시 숨김·캐시로 마감 분류를 다시 돌리면 같은 결과가 나왔다. 11:14 앱 재시작 → 11:14:53 14:00 초안 자동 분류 → 그 뒤 초안에서 숨기기·이동을 할 때마다 핸들러가 최신 확정안(11:00)을 `snapshot_group_names`로 다시 저장했다. 13:38 되돌리기 스냅샷에는 이미 규칙 기반 이름이 들어 있었다. 11:00 캐시 항목을 뺀 채로 스냅샷을 돌리면 위 숫자가 정확히 재현된다.
- **원인**: 메모리 캐시에 넣을 때 `len(_cache) >= _CACHE_MAX(40)`이면 `_cache.clear()`였다. `_ensure_cache_hydrated`는 파일 항목(최대 80+)을 상한 없이 올리므로, 재시작한 프로세스는 **새 분류 하나를 넣는 순간 다른 회차 분류를 전부 잃었다**. 파일에는 남아 있어도 하이드레이트는 프로세스당 한 번뿐이다. 스냅샷 저장은 API를 부르지 않으므로(`allow_llm_call=False`) 캐시를 못 찾으면 조용히 규칙 기반으로 떨어지고, 이건 "실패"가 아니라 `classification_degraded`도 안 켜진다.
- **고침**: `llm_classifier._store_locked` — 상한에 닿으면 오래된 항목부터 하나씩 빼고, 넣는 항목과 같은 회차는 가장 나중까지 남긴다. `classify_with_llm`·`seed_cache` 두 곳이 이걸 쓴다. 11:00 회차 파일은 남은 캐시로 `snapshot_group_names`를 다시 돌려 복구했다(덮인 판은 `trash-can/`). 이름표·순서는 AI 이름에 묶여 있어 함께 돌아왔다.
- **같이 고침 — 테스트가 실제 캐시 파일에 쓰던 문제**: 통합 테스트 러너가 `LLM_CLASSIFICATION_CACHE_FILE`을 임시 폴더로 옮기지 않아, 회차를 저장하는 테스트마다 실제 `data/llm_classification_cache.json`에 `example.com` 등 가짜 항목이 그날 날짜로 5개씩 쌓였다. 러너에서 파일 경로와 메모리 캐시(`_cache`/`_cache_hydrated`/`_last_names`/`_last_round_id`)를 테스트마다 격리하고, 이미 쌓인 25개는 지웠다(정리 전 파일은 `trash-can/`).
- 테스트 `test_cache_store_keeps_other_rounds`(옛 코드에선 실패함을 확인).

## 초안 첫 분류 기사를 다 숨기면 규칙 기반으로 떨어지던 문제 (2026-09-21)

- 17:00 회차 초안: 자동 분류(회차당 1번)가 기사 6건일 때 쓰였다(`메가특구 사회적 대화`·`수협은행장 임원인사`). 담당자가 15:47에 그 6건을 전부 숨기자 `_find_partial`이 겹침 0으로 탈락했고, `allow_call=False`라 부를 수도 없어 단어 빈도 소제목 + 「AI가 소제목을 묶지 못했어요」 배너가 떴다. API 실패는 없었다.
- 고친 것: 이 회차 캐시가 있는데 담긴 기사가 전부 숨김이면 남은 기사를 전부 📂 미분류로 돌려준다(가짜 한 단어 소제목보다 정직하다). 이때 「AI 기사 배정」은 후보가 0개라 예전엔 「배정할 소제목이 아직 없어요」로 막혔으므로, 흐트러질 기존 소제목이 없는 이 경우엔 처음부터 분류한다. 마감은 전부 미분류를 받으면 초안 분류가 없던 회차처럼 처음부터 분류한다(끼워 넣을 곳이 없다).
- 기각: 이 경우 자동 분류 기회를 한 번 더 주는 안 — 비용이 늘고, 담당자가 버튼을 누르는 쪽이 원칙(자동 호출은 회차당 1번)과 맞다.

## [속보] 몰림 알림 (2026-09-22)

- 담당자 질문: "[단독]이든 [속보]든 갑자기 늘어나는 걸 알리는 기능도 있나." 없었다 — 알림은 한 건씩이고 홈 건수는 보여 주기만 한다. 8/26~27에 기각한 「급등 단어 제안」(앱이 단어를 골라 추이 그래프에 권함)과는 다른 기능이라 따로 판단했다 — 이번엔 담당자가 이미 지켜보는 [속보]에 대한 알림이다.
- 실측(8/11~9/22 저장 회차의 고유 [속보]·[단독] 201건): [속보]가 30분 안에 5개 언론사 이상 몰린 날은 16일 중 4일(9/1 김용범 사퇴, 9/17 청문보고서 채택, 9/18 유류세 연장, 9/22 장관 임명 재가) — **모두 한 사건을 여러 언론사가 한꺼번에 보도한 경우**였다. 4곳으로 내리면 9/16(윤석열 2심 무죄)이 더해진다. [단독]은 33일 동안 30분 안 최대 3건이고 서로 다른 기사라 대상에서 뺐다.
- 기사 수가 아니라 언론사 수로 센다 — 9/1 파이낸셜뉴스는 종부세 [속보]를 6분 사이 세 건 냈는데, "여러 곳이 달려든다"는 신호로는 한 곳이다.
- A안(개별 알림은 두고 한 통 더) / B안(그 순간 메시지의 머리줄만 🔥로 바꿔 메시지 수 유지) 중 A안으로 결정(담당자). 알림 시간 밖에도 몰림만은 소리를 낼지 물었더니 **그대로 조용히**.
- 판정 창은 "지금 − N분 ~ 지금"이다. 실측은 하루 중 가장 많이 몰린 구간(미끄러지는 창)으로 셌는데, 실제 판정은 새 [속보]를 보낼 때마다 지금 기준으로 세므로 발견이 늦은 만큼 조금 덜 셀 수 있다. 대신 앱 시작 몰아보내기(9/22 09:13 한 통에 [속보] 39건)는 지난 몰림(08:28~08:56)이 창 밖이라 저절로 안 울린다.
- 설정 화면의 "지난 기록에 대 보면"은 폴링 기록이 아니라 정기 회차 파일로 센다 — 알림 기록(`alerted_urls`)은 하루치만 남기 때문이다. 감시 그룹과 정기 수집 그룹이 달라 실제 알림과 조금 다를 수 있어 문구에 「정기 회차에 저장된」을 밝혔다.

## [속보] 같은 사건 묶기 (2026-09-23)

- 시작은 다른 질문이었다 — 담당자: "[단독]·[속보]가 모든 검색어에서 나오면 시도때도 없이 울리니, 검색어 설정 화면에 알림 on/off를 만들어야 할까?" **재보고 기각했다.** 실측(8/11~9/23 저장 회차 200개, 말머리 기사 203건 / 34일): 말머리 기사는 전부 `재정경제부`(115) · `재정경제기획위원회`(59) · `재경부`(49) · `이형일`(39) · `기획재정부`(25) · `구윤철`(20) · `기재부`(8)에서 나왔고, 넓어서 의심했던 그룹(세제개편의 양도세·종부세·ISA·보유세·장특공제·비거주, 예결위, 이슈어의 담배·생맥주·주세)은 **0건**이었다. 즉 검색어마다 스위치를 달아도 끌 수 있는 건 이미 안 울리는 것들뿐이고, `/breaking-alert`의 감시 그룹 선택과 「같은 걸 정하는 자리」가 두 곳이 된다. (세제개편·예결위는 정기 회차 파일에 안 남는 그룹이라 전체 기사 캐시 한나절로만 확인했다 — 그 줄만 근거가 약하다.)
- 통수가 튀는 원인은 검색어가 아니라 **한 사건을 여러 언론사가 동시에 내는 것**이었다: 9/22는 [속보] 28건 중 15건이 「이형일·강신철·홍지선 장관 임명안 재가」 한 사건(08:28~08:43), 9/17은 19건 중 8건이 「재경위 청문보고서 채택」.
- **함정 — 한 통 안에서 묶는 것만으로는 통수가 안 줄어든다.** 폴링이 3분마다 돌아 그 8분이 이미 여러 통으로 갈라져 있기 때문이다(9/22의 그 사건은 3분 버킷 6개에 흩어졌다). 설계 도중 이걸 먼저 재서 알았고, 그래서 **통과 통 사이의 후속 억제**가 본체이고 한 통 안 묶기는 길이만 줄이는 보조다. 묶기만 만들고 끝내면 안 된다.
- 효과 실측(3분 폴링 가정, 새 코드로 재확인): 34일 **174통 → 157통**(10%). 평균은 작지만 터지는 날만 깎는다 — 9/22 19→12, 9/17 15→10, 9/18 11→8이고 조용한 날(8/25 10통, 9/21 8통)은 **한 건도 안 접혔다**. 9/22 그 통의 실제 메시지는 블록 14개 → 2개(299자)가 된다.
- 임계 0.4: 0.3/0.4/0.5로 접히는 [속보]가 32/25/22건이다. 0.3은 접히는 수만 늘고 다른 사건을 묶을 위험이 커져 가운데 값을 썼다. 창은 30분(23건)보다 60분(25건)이 조금 더 접어 60분.
- **[단독]은 대상에서 뺐다** — 34일 전수에서 [단독]끼리 같은 사건으로 걸리는 쌍이 임계 0.3에서도 0건이다(한 언론사가 단독으로 내는 기사라 제목이 안 겹친다). 효과가 0이면 오탐 위험만 남는다. `[단독]` 하루 3.5건은 서로 다른 기사라 묶어서 줄일 방법이 없다.
- 접은 기사를 알림 기록에서 빼지 않았다 — 홈 [속보] 목록이 그 기록만 보고(`app/alerted_urls.py`), 몰림 알림도 그 기록으로 센다. 접기만 한 통에서도 몰림 판정을 돌려, 쏟아진 사실은 몰림 한 통으로 드러나게 했다. 이 둘을 같이 껐으면 첫 통 뒤의 보도가 어디에도 안 남는다.
- 기존 몰림 테스트(`test_breaking_burst_alert`)가 같은 제목으로 5개 언론사를 만들어 개별 알림이 접히면서 깨졌다 — 몰림은 사건을 가리지 않고 언론사 수로 세므로, 개별→몰림 순서는 **사건이 다를 때** 확인하도록 그 테스트를 고쳤다.
- 같이 나왔다가 접은 안: 알림 메시지에 `🔍 재정경제부, 재경부`처럼 **검색어 줄을 넣는 것**(담당자 요청 → 담당자 취소). 경로마다 그 줄의 뜻이 달라진다 — 폴링은 감시 그룹의 검색어, 정기 회차 「바닥」 경로는 설정과 무관하게 초안·확정본을 켠 그룹 전부라서, 같은 `🔍` 줄이 통마다 다른 걸 가리킨다.

## 가져가기 버튼 이름 — 복사 · 텍스트 · 엑셀 (2026-09-23)

- 담당자: "copy txt xlsx 글자가 너무 안 예뻐. 대문자로 할까, 한글로 할까." 안 예뻐 보인 진짜 원인은 소문자가 아니라 **셋이 서로 다른 종류의 말**이었다 — `copy`는 영어 동사, `txt`·`xlsx`는 파일 확장자. 한글 화면에 영문 소문자 셋이 몰린 데다 품사까지 안 맞아 한 묶음으로 안 읽혔다.
- **대문자(`COPY · TXT · XLSX`) 기각.** 또렷해지긴 하는데 한글 옆에서 영문 대문자는 무게가 확 올라가, **가장 약해야 할 글자 버튼이 툴바에서 제일 세게 읽힌다**(왼쪽 동작 버튼보다 먼저 눈에 들어옴). 「채움 > 테두리 > 글자」 위계가 뒤집힌다. 품사가 섞인 문제도 그대로 남는다.
- **「메모장」 기각.** 받는 건 메모장 파일이 아니라 txt 파일이고, macOS엔 메모장이 없다(텍스트편집기). 화면 이름 자리에 **프로그램 이름**을 넣으면 "그 프로그램으로 열어야 하나"로 읽힌다. → `텍스트`.
- **섞는 안(`복사 · txt · 엑셀`) 기각** — 한 묶음 안에서 글자 종류가 두 번 바뀌어 지금보다 더 어수선하다.
- 확장자를 라벨에서 뺀 근거: 받은 **파일 이름**(`2026-09-23_언론모니터링_14-00기준.xlsx`)이 이미 말해 주고, 툴팁도 「엑셀 파일로 받기」 그대로다.
- 정기 보관함·수시 보관함·라벨 보관함은 이미 「복사」만 한글이고 `txt`·`xlsx`가 영문이라 **한 화면 안에서도 종류가 섞여 있었다** — 이번에 일곱 화면을 같은 말로 맞췄다.
