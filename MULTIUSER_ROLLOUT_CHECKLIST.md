# 사내 인트라넷 전사 배포 준비 체크리스트

> 이 문서는 현재 단일 사용자·로컬 전용으로 동작하는 언론 모니터링 앱을
> "사내 인트라넷에서 여러 명(잠재적으로 전 직원)이 각자 로그인해서 큐레이션까지 하는"
> 형태로 확장하기 위한 준비 체크리스트다. 착수 전 대화에서 정리된 내용을 기반으로 한다.
>
> 항목 옆 「지금은 …」은 **이 저장소의 현재 상태**다 — 앱이 자라면서 이미 해결된 것도
> 있으므로, 착수 전에 이 줄부터 다시 확인한다.

## 0. 착수 전 반드시 숫자로 확정해야 할 것

이 세 가지가 확정되지 않으면 아래 모든 설계가 가정 위에 서 있는 것이다. 제일 먼저 확인한다.

- [ ] **실사용자 규모** — "전사"가 정확히 몇 명인지 (30명대인지 수백 명대인지에 따라 DB·서버 사양·쿼터 전략이 전부 달라짐)
- [ ] **네이버 API 하루 쿼터 및 현재 사용량** — 네이버 개발자센터 대시보드에서 직접 확인
- [ ] **서버를 놓을 네트워크 세그먼트에 전 직원이 실제로 접근 가능한지** — 망분리(업무망/인터넷망) 구조 확인. 이 앱은 네이버 API 호출을 위해 인터넷이 되는 세그먼트에 있어야 하므로, 그 세그먼트에 전 직원이 접속 가능한지가 관건

---

## 1. 필수 — 없으면 프로젝트 자체가 성립하지 않음

### 인증/계정
- [ ] 자체 회원가입·로그인·비밀번호 재설정 구현 (사내 SSO는 폐쇄망에 있어 연동 불가 확정)
- [ ] 비밀번호 해싱(bcrypt/argon2), 세션 관리, 로그아웃, 세션 타임아웃
- [ ] 계정 발급/회수 프로세스 — 특히 인사이동·퇴사 시 계정 정지 절차
- [ ] 권한 구분 — 일반 큐레이터 계정 vs 시스템 설정(키워드/스케줄 등)을 만지는 관리자 계정 분리

### 데이터 분리
- [ ] 사용자별 데이터 네임스페이스 설계 (사용자별 폴더 분리 JSON, 또는 DB의 user_id 컬럼)
- [ ] 규모가 수백 명대면 SQLite보다 PostgreSQL 검토 (30명 안팎이면 JSON 폴더 분리로도 가능)

### 서버 아키텍처
- [ ] `http.server` → Flask/FastAPI + gunicorn/uvicorn 계열로 교체 — **지금은** 표준 라이브러리 `ThreadingHTTPServer`다(싱글스레드가 아니다). 바꿀 이유는 동시성이 아니라 로그인 세션·동적 렌더링·권한 검증이다
- [ ] 정적 HTML 전체 재생성 방식 → 로그인 세션 기반 동적 렌더링으로 전환
- [x] 스케줄러와 웹서버를 분리된 **스레드**로 구성 — `main.py`가 둘 다 데몬 스레드로 띄운다. 남은 것은 **프로세스 분리**뿐이다(지금은 한 프로세스라 앱이 죽으면 둘 다 죽는다)

### 네이버 API 쿼터 대응
- [ ] 키워드 단위 **공유** 캐시 계층 — **지금도** 키워드별 증분 캐시가 있다(`app/live_cache.py`, 마지막으로 본 시각 이후만 재검색). 없는 것은 「사용자 간 공유」뿐이라, 새로 만들 게 아니라 이 캐시를 공용 계층으로 올리는 일이다
- [ ] 전체 기사 화면(`/live.html`)에 짧은 TTL 캐시 적용 — 「매 요청마다 라이브 검색」은 옛 서술이다(위 증분 캐시가 있다). 그래도 사용자가 늘면 **각자의 새로고침이 각자 네이버를 부르는** 구조라 가장 먼저 한도에 닿는 지점인 건 그대로다

### 서버 인프라
- [ ] 상시 구동 서버(관리되는 PC 또는 VM) 확보 — 개인 PC/노트북 사용 금지
- [ ] 고정 IP 할당, 방화벽 포트 개방 (IT부서 협조 필수)
- [ ] 재부팅·장애 시 자동 재기동 등록 — **지금은** macOS LaunchAgent(`autostart/install.sh`)다. 서버 OS를 먼저 정하고 그에 맞는 방식(Windows 서비스 / systemd 등)을 고른다

---

## 2. 중요 — 없으면 초반엔 굴러가도 곧 사고로 이어짐

### 보안
- [ ] 사내망 내부 HTTPS 적용 검토 (사설 CA 인증서) — 사내망도 스니핑 위험 존재, 로그인 자격증명 보호 필요
- [ ] 서버 사이드 권한 검증 — 사용자가 URL 조작 등으로 타인의 데이터에 접근하지 못하도록 (IDOR 방지)
- [ ] 로그인 시도 제한 (무차별 대입 공격 방어)
- [ ] 시크릿 관리 — **지금은** `.env`와 `data/credentials.json`(설정 화면 저장값이 우선) 두 갈래다. 이 둘을 서버 환경변수/시크릿 저장소 하나로 합친다

### 감사/추적
- [ ] 큐레이션 행위 로그 — 누가 언제 어떤 기사를 숨기고 순서를 바꿨는지 (정부기관 특성상 사후 추적 필요성 높음). **발송은 이미 남는다**(`app/send_log.py`, 90일, 화면 `/send-log`) — 남은 것은 큐레이션 쪽이다

### 텔레그램 다중 사용자 대응
- [ ] 봇 토큰은 공유, 사용자별 chat_id는 개별 등록하는 온보딩 절차 설계
- [ ] 봇 이름(설정 「텔레그램 발송 계정」 `/telegram-sender`)은 봇에 붙은 값이라 바꾸면 모두에게 바뀐다 — 사용자별 설정이 아니라 관리자만 고치는 공용 설정으로 둔다
- [ ] 외부(고위공무원 등) 보고용 별도 브로드캐스트 그룹/채널 구성 검토

### 운영 담당자
- [ ] 시스템 관리자 역할 지정 (서버 재기동, 계정 발급, 1차 장애 대응) — 최소 1~2명

---

## 3. 운영 단계에서 필요 (초기 구축 이후 곧 필요해짐)

- [ ] 백업 정책 수립 및 정기 복구 테스트
- [ ] 서버 다운 등 장애 발생 시 자동 알림 (예: 관리자 텔레그램으로 알림)
- [ ] 사용자 매뉴얼/온보딩 가이드 작성
- [ ] 업데이트/배포 절차 및 롤백 계획 수립
- [ ] 부하 테스트 (예상 동시접속 시나리오, 특히 출근 직후 몰림 대비)

---

## 4. 조직적으로 미리 해둘 것 (기술 외 준비)

- [ ] IT/보안팀과 서버 설치 위치·방화벽 규칙·계정 정책 사전 협의
- [ ] 개인정보 검토 — 전 직원 로그인 정보 + 텔레그램 chat_id 저장에 대한 내부 개인정보 담당자 검토
- [ ] **단계적 배포 계획 수립** — 전사 일괄 배포 대신 홍보실(약 30명) 규모로 먼저 파일럿 운영 후 문제를 해결하고 전사로 확대하는 순서 권장

---

## 권장 착수 순서

1. 인증 + 사용자별 데이터 분리
2. 서버 아키텍처 교체 (동시접속 대응)
3. 네이버 API 캐싱 계층
4. 홍보실 규모로 파일럿 운영
5. 문제 해결 후 전사 확대

한 번에 모두 구현하려 하지 말고 이 순서로 단계를 나눠 진행한다.
