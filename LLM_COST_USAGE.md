# LLM_COST_USAGE.md

이 앱에서 **실제로 Claude API를 호출하는 지점**과, 각 지점이 하루에 얼마나 불리는지·한 번에 얼마나 드는지 정리한 문서다. 유지보수·비용 점검용 — 코드가 실제로 어떻게 동작하는지는 [CLAUDE.md](CLAUDE.md)와 [HISTORY.md](HISTORY.md)에 있고, 여기는 그중 "돈이 드는 부분"만 모아 별도로 본다.

**API를 부르는 함수는 여섯이다.** 다섯은 `app/llm_classifier.py`에 있고(소제목 분류+요약 · 「기타」 재정리 · 미분류 배정 · AI 기사 나누기 · 설정 「연결 확인」), 하나는 `app/negative_guess.py`(홈 부정 추정 기사)다. 수시 모니터링은 전용 함수를 만들지 않고 앞의 둘(분류·배정)을 그대로 부른다(7절). 이 앱의 다른 모든 기능 — 기사 요약(네이버 description 그대로), 워드클라우드, 형광펜, 언론사 정렬 등 — 은 전부 규칙 기반이라 비용이 0원이다.

## 1. 소제목 분류 + 요약 (`classify_with_llm`)

**언제 불리나**: `app/classifier.py`의 `classify_articles()`가 기본적으로 먼저 시도하는 경로. 아래 세 곳에서 도달한다.

| 트리거 | 호출 빈도 |
|---|---|
| 초안 첫 자동 분류 (`app/preview_renderer.py`) | 회차당 기사 5건 이상 되는 시점에 딱 1회 — 그 뒤로는 담당자가 버튼을 눌러야만 다시 호출됨 |
| 정기 회차 마감 (`app/scraper.py` `collect_run` → `app/classifier.py` `classify_for_finalize`) | **보통 0회** — `allow_call=False`로 초안 분류를 재사용한다. 초안에서 한 번도 분류되지 않은 회차(아무도 열어보지 않은 심야 회차 등)에만 1회 |
| 「AI 모든 기사 재분류」 버튼(확정본·초안) | 담당자가 직접 누를 때만 — 상한 없음 |

**캐시로 실제 호출이 줄어든다는 점이 중요하다**: 기사 URL 집합이 이전과 같으면(예: 화면만 다시 열기, 순서만 바꾸기) API를 다시 안 부르고 캐시를 그대로 쓴다. 위 표의 "1회"는 캐시가 없어 실제로 API까지 도달하는 경우만 센 것이다.

**1회당 토큰량과 비용**:
- 실측(2026-08-11, 요약 필드 추가 전 — 분류만 하던 시점): 기사 배치당 입력 ~3,184 토큰 / 출력 ~141 토큰.
- Haiku 가격 기준($1/MTok 입력, $5/MTok 출력) ≈ $0.0039/회 ≈ **약 5.4원/회**.
- **[실측 갱신 — 2026-08-20, 소제목 상한 8→15 검토 중 실측]** 소제목별 요약이 추가된 뒤 실제 `usage` 값을 여러 회차로 찍어봤다(모델 Haiku, 상한 15, 밀도 규칙 프롬프트 적용 후 기준):

  | 회차 규모 | 입력 토큰 | 출력 토큰 | 비용 |
  |---|---|---|---|
  | 14건 | 3,747~4,057 | 948~1,092 | 약 12~14원 |
  | 76건 | 13,050~13,396 | 1,284~2,003 | 약 27~33원 |

  즉 예전 추정(7~9원)은 기사 적은 회차 기준으로도 낮게 잡은 값이었다 — 입력 토큰이 기사 수에 거의 정비례해서 늘기 때문에, 기사 많은 회차는 추정치의 3~4배까지 간다. **상한(8/15)을 바꿔도 입력 토큰은 거의 그대로다** — 같은 76건 회차에서 상한 8은 입력 13,050, 상한 15는 입력 13,050으로 동일, 출력만 1,284→1,533로 늘어 회당 +2원 안팎 차이. 소제목 개수를 늘리는 비용 영향은 입력(기사 배치, 상한과 무관)이 아니라 출력(소제목명+요약, 상한에 비례)에서만 온다.
  - 참고: 회차마다 실제 필요한 만큼만 쓰고 멈추므로(=`max_tokens`은 상한일 뿐 청구액이 아님), 76건 회차가 상한 15를 받아도 실제로는 11~15개만 만들었다(꽉 채우려 억지로 안 쪼갬).

## 2. "기타" 재정리 (`classify_with_llm` 내부 `_refine_etc_bucket`)

**언제 불리나**: 1번 호출(1차 분류) 직후, "기타"가 `_ETC_REFINE_MIN_COUNT`(6건) 이상이면서 전체 기사의 `_ETC_REFINE_MIN_RATIO`(25%) 이상일 때만 — 같은 `classify_with_llm` 호출 안에서 자동으로 이어진다(별도 버튼 없음). [추가: 2026-08-24] CLAUDE.md "'기타' 재정리" 참고 — 1차 호출이 "기타"에 절반 가까이 몰아넣는 회차가 실제로 흔해서(89건 회차 실측 44~51%) 만든 2차 시도다.

**1회당 토큰량(실측, 2026-08-24, 89건 회차 중 "기타" 39건이 남았던 경우)**: 입력 7,101 / 출력 347 토큰 — 1차 호출(입력 14,794 / 출력 2,233)의 절반 이하다("기타" 기사만 다시 보내므로 입력이 훨씬 작다). 비용은 1차 호출과 합쳐 회당 약 40~55원 수준(1차 단독 대비 +10~15원 안팎) — Haiku 가격 기준.

**효과(같은 회차로 재현, 여러 트라이얼)**: "기타" 44~51% → 0~19%(평균 약 11%)로 줄었다. 이 재정리가 "기타"가 이미 작은 회차(문턱 미만)에서는 아예 안 걸려 추가 비용이 0원이다 — 실측: 7건 회차는 API 호출이 정확히 1회만 발생했다.

## 3. 미분류 기사 배정 (`assign_to_existing`)

**언제 불리나**: 입구가 넷이고, 그중 하나는 **자동**이다.

| 입구 | 호출 빈도 |
|---|---|
| 초안 📂 미분류 칸의 「AI 기사 배정」 버튼 | 담당자가 누를 때만 |
| 초안 📌 담아둔 기사 칸의 「AI 기사 배정」(승격 후 이어서 배정) | 담당자가 누를 때만 |
| **회차 마감 시 자동 배정**(`app/classifier.py` `classify_for_finalize`) | 초안에 미분류 기사가 남아 있고 끼워 넣을 소제목이 있으면 **회차마다 1회** |
| 수시 카드의 소제목 배정 | 7절 참고 |

즉 "자동으로는 절대 안 불린다"가 아니다 — 담당자가 미분류를 그대로 두고 회차가 마감되면 앱이 대신 한 번 부른다. 1절 표에서 마감 분류가 0회가 된 만큼, 회차마다 나가는 호출은 이쪽으로 옮겨 왔다고 보면 된다.

**1회당 토큰량**: 위 분류 호출보다 보통 작다 — 이미 분류된 소제목 목록 + 미분류 기사 몇 건만 프롬프트에 넣기 때문(전체 회차 기사를 다시 보내지 않음). 배정할 기사가 2~3건이면 입력 500~1,000토큰 수준으로 추정 — 미실측.

**하루 예상 횟수**: 담당자 재량 — 초안에 미분류 기사가 쌓였을 때만 누르므로 0~여러 번, 예측 불가.

## 4. 전체 재분류 (같은 `classify_with_llm`을 `force=True`로)

기능적으로는 1번과 같은 함수·같은 토큰량이지만, **캐시를 무시하고 강제로 새 API 호출을 낸다**는 점이 다르다 — 그래서 비용이 튈 수 있는 유일한 지점이다. 확정본·초안 양쪽에 있고, 누를 때마다 항상 1회씩 새로 나간다(직전과 기사 구성이 완전히 같아도 캐시를 안 쓴다). 2번(기타 재정리)도 같은 호출 안에서 조건이 맞으면 그대로 이어 붙는다 — 재분류를 눌렀을 때 최대 2회까지 나갈 수 있다는 뜻이다.

## 5. AI 기사 나누기 (`split_group_articles`) [추가: 2026-09-15]

**언제 불리나**: 확정본·초안 선택 바(체크박스 일괄이동 바)의 **「AI 기사 나누기」** — 담당자가 기사를 골라 직접 누를 때만. 자동으로는 절대 안 불린다. 잠긴 상태(4건 미만·여러 소제목·15개 꽉 참)나 서버가 막는 경우(화면이 바뀜 등)는 호출 전에 끝나 비용이 0이다.

**1회당 토큰량**: 1번과 같은 프롬프트에 **고른 기사만** 넣으므로 그 기사 수만큼 — 회차 전체를 다시 보내는 4번(전체 재분류)보다 작다. 응답이 비면 1번과 같이 한 번 더 시도한다(최대 2회).

**하루 예상 횟수**: 담당자 재량 — 한 소제목이 너무 커졌을 때만 누르므로 0~몇 번.

## 6. 홈 부정 추정 기사 (`app/negative_guess.py`) [추가: 2026-09-18]

**언제 불리나**: 정기 회차가 저장된 뒤 스케줄러 tick이 회차마다 한 번. 그 회차 기사 중 제목·요약에 「재경부」·「재정경제부」가 나온 것만(사진 제외) 보낸다. 대상이 0건이면 호출 0. AI 키가 없으면 안 부른다. 실패한 회차는 5분 뒤(최대 3번).

**실측(2026-09-18, 정기 5회차 · 82건 · 5번 호출)**: 입력 18,512 / 출력 699 토큰 ≈ **$0.022(약 31원)**. 부정 기사 번호와 근거만 받아서 출력이 거의 없고, 비용의 대부분은 입력(기사를 읽는 값)이다.

**앱 실제 첫 가동(2026-09-18 17:46, 같은 5회차를 숨기기 전 목록으로)**: 181건, 입력 35,709 / 출력 1,232 토큰 ≈ **$0.042(약 59원)**. 위 시험(82건)이 담당자가 확정본에서 숨긴 기사를 빼고 쟀던 탓에 절반으로 나왔다 — 이날 재경부 기사의 절반 이상을 담당자가 숨겼다.

**하루 예상**: 약 50~60원. 회차 수보다 재경부 기사 수에 비례한다.

**이전 설계(같은 날 떼어 냄)**: 9~18시 매시 네이버를 직접 검색해 논조 비율·한 줄 요약까지 쓰던 전광판 — 네이버 직접 검색이 정기 저장분의 약 3.6배라 첫 판 459건에 입력 97,314 / 출력 19,850 토큰(약 275원), 근거를 부정·긍정만 받게 줄여도 하루 약 230~260원(60건 A/B: 출력 2,836 → 1,470, 비용 −26%). 경위는 HISTORY.md.

## 7. 수시 소제목 배정 (`app/adhoc/classifier.py` `classify_card`)

**언제 불리나**: 수시 확정본(과 옛 원본) 화면의 소제목 배정 버튼 — 담당자가 누를 때만. **자동 호출 0회**가 수시의 설계 원칙이다(ADHOC_DESIGN.md §6.5).

**어떤 호출인가**: 전용 함수가 없다. 그 카드에 소제목이 하나도 없으면 1절과 같은 `classify_with_llm`(다만 소제목 상한이 5), 이미 있으면 3절과 같은 `assign_to_existing`을 부른다. 정기의 「직전 이름」 힌트(`_last_names`)를 호출 전후로 저장·복원할 뿐 API 호출 자체는 정기와 같다.

**1회당 토큰량**: 카드에 보이는 기사 수에 비례 — 정기 회차보다 대체로 작다(한 사안만 모으므로). 미실측.

**하루 예상 횟수**: 담당자 재량 — 수시 수집을 한 날만, 0~몇 번.

## 8. 설정 「연결 확인」 (`test_llm_credentials`)

`/llm` 화면의 [연결 확인] 버튼이 `max_tokens=1`짜리 메시지 하나를 보낸다. 키가 살아 있는지만 보는 것이라 비용은 사실상 0원이다.

## 모델을 Haiku에서 바꾸려 할 때 주의할 점 (2026-08-20 실측)

`app/llm_classifier.py`가 `messages.create`에 `thinking` 파라미터를 아예 안 넘긴다. Haiku 4.5는 그 기능 자체가 없어 상관없지만, **Sonnet 5(또는 그 윗급)로 바꾸면 파라미터를 안 줘도 기본으로 thinking이 켜진다** — 그 추론 토큰도 `output_tokens`에 포함되어 그대로 청구된다.

실측(14건짜리 회차, 제일 가벼운 케이스): Sonnet 5로 돌렸더니 `output_tokens=4,783` 중 `thinking_tokens=4,233`(88%)이 안 보이는 추론이었고, 실제 소제목 JSON은 12%뿐이었다. 그 결과:
- `max_tokens=4096`(위에서 올린 값)으로도 부족해서 잘렸다 — `max_tokens=8000`을 줘야 겨우 끝났다.
- 비용은 **회당 78원**(같은 회차 Haiku는 12.6원) — 겉보기 단가차(2배)보다 훨씬 크게 벌어진다.

또 하나 — **홈 부정 추정 기사(6절)는 구조화 출력(`output_config`의 `json_schema`)으로 응답 형식을 강제한다.** 모델을 바꾸려면 그 모델이 구조화 출력을 지원하는지도 함께 확인해야 한다(지원하지 않으면 판정이 통째로 건너뛰어진다).

이 작업(기사를 쟁점별로 묶고 짧은 요약 쓰기)은 깊은 추론이 필요한 게 아니라 패턴 매칭에 가까워 Haiku로 충분하다고 판단해 **모델은 그대로 뒀다**. 나중에 다시 모델 교체를 검토한다면 `thinking: {"type": "disabled"}`를 명시하고 다시 실측할 것 — 지금처럼 파라미터를 안 주고 바꾸면 조용히 비용이 몇 배로 뛴다.

## 오늘 설정 기준 하루 예상 비용

`data/settings.json`에 저장된 실제 스케줄(2026-08-13 기준). 단가는 위 실측(2026-08-20, 상한 15 기준) 범위인 **회당 ~12~33원**으로 갱신 — 기사 적은 새벽 회차는 아래쪽, 재경위 있는 날처럼 기사 많은 회차는 위쪽에 가깝다:

| 그룹 | 시간대 수 | 초안 자동분류(회당 ~12~33원) | 회차 마감(분류 재사용) |
|---|---|---|---|
| 주중 | 6개 | 최대 6회 (~72~198원, 회차마다 기사 5건 이상 될 때만) | 분류는 0회 + 미분류가 남은 회차마다 배정 1회(작은 호출) |
| 주말 | 3개 | 최대 3회 (~36~99원) | 위와 같음 |

**주중 하루 상한선(담당자가 재분류 버튼을 한 번도 안 눌렀을 때)**: 초안 자동분류 최대 6회 + 마감 배정 최대 6회(작은 호출) ≈ **약 80~220원**. 아무도 초안을 열지 않은 회차는 반대로 마감에서 분류 1회가 통째로 나간다 — 어느 쪽이든 회차당 큰 호출은 하나를 넘지 않는다. 담당자가 재분류·미분류 배정 버튼을 누른 횟수만큼 여기에 더해진다 — 이 앱의 비용 구조는 "가만히 두면 하루 12회 안쪽"이고, 그 이상은 전부 담당자가 실제로 누른 결과다(사용자가 페이스를 조절하므로 조용히 쌓이지 않는다). [수정: 2026-08-24] 이 12회 중 "기타"가 많이 남는 회차는 2번(재정리)이 자동으로 붙어 회당 +10~15원 더 든다 — 위 상한선엔 아직 안 넣었다(트리거 조건이 회차 내용에 달려 있어 "몇 회"로 고정할 수 없다), 실제로는 이보다 조금 더 나갈 수 있다는 뜻.

**여기에 홈 부정 추정 기사(6절)가 더해진다** — 회차마다 1번, 하루 약 50~60원(2026-09-18 앱 실측 59원).

## 비용이 안 드는 것 (참고)

- 확정본 화면(index.html)을 열거나 새로고침 — 저장된 분류를 그대로 읽을 뿐, API를 다시 안 부른다(2026-08-12부터).
- 기사 숨기기·순서 바꾸기·소제목 이름 바꾸기 — 캐시가 있으면 재사용, 없어도 "줄어들기만 한 경우"는 재호출 안 함(`_find_reusable`).
- 정기 보관함(history.html) 조회 — 저장된 스냅샷만 읽는다.
- 규칙 기반 폴백 경로(소제목 묶기·소제목 요약) — API 미설정이거나 실패했을 때도 전부 무료로 계속 동작한다.

## 실측 갱신 방법 (나중에 정확한 값 넣을 때)

`app/llm_classifier.py`의 `classify_with_llm()`이 `response = _get_client().messages.create(...)`를 부르는 지점에서, `response.usage.input_tokens`/`response.usage.output_tokens`를 한 번 로그로 찍어보면 정확한 값을 알 수 있다(Anthropic SDK가 모든 응답에 `usage` 필드를 함께 돌려준다). 지금 이 문서의 "미실측" 표시는 전부 이 방법으로 확인 가능하다 — 실제로 로그를 찍어보고 싶으면 별도로 요청. 홈 부정 추정 기사(6절)는 이미 `usage`를 INFO 로그로 남기므로 `data/logs/app.log`만 보면 된다.
