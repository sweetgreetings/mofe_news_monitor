# 코딩 규약 (Coding Conventions)

이 문서는 **"왜 그렇게 했는가"의 원칙만** 모아둔 곳이다.
개별 기능이 무엇을 하는지, 언제 어떻게 바뀌었는지는 [CLAUDE.md](CLAUDE.md)에 있다.
새 기능을 만들거나 기존 동작을 바꿀 때 **먼저 이 문서의 원칙과 어긋나지 않는지** 확인한다.

---

## 0. 작업 규칙 (기본)

| 규칙 | 비고 |
|---|---|
| 모든 설명·주석은 한국어로 쓴다 | |
| 새 파일은 프로젝트 루트(`~/press-monitor`) 안에만 만든다 | |
| 코드를 바꾸면 무엇을 왜 바꿨는지 한 줄로 알려준다 | |
| `.env` 등 비밀 정보는 `.gitignore`에 두고 절대 커밋하지 않는다 | |
| 파일을 지울 땐 삭제하지 말고 `trash-can` 폴더로 옮겨만 둔다 | 사용자가 직접 확인 후 삭제 |
| 바꾼 뒤엔 **직접 실행해 눈으로 확인**하고, 스스로 리뷰하고, 문제가 있으면 고쳐서 다시 확인한다 | |

---

## 1. 검증 — 추측하지 말고 잰다

이 프로젝트에서 가장 많이 틀렸던 지점이다. 아래는 전부 **실제로 겪은 실패**에서 나온 규칙이다.

### 1-1. 결론을 내기 전에 실제 데이터로 잰다
저장된 회차 파일(`data/articles/*.json`)이 42개나 있다. 감으로 판단할 이유가 없다.

### 1-2. 측정에는 **가장 극단적인 케이스**를 반드시 넣는다
> 소제목 상한을 5→8로 올릴지 판단할 때 7·23·36건 회차만 재고 "상한이 병목이 아니다"라고 기각했다.
> **정작 가장 큰 61건 회차를 빼먹었다.** 나중에 넣어보니 상한 5에서 44건이 한 덩어리로 뭉쳐 있었고, 결론이 통째로 뒤집혔다.

상한·임계값을 다룰 땐 **그 값에 실제로 닿는 케이스**가 표본에 있어야 한다.

### 1-3. A/B 비교 전에 전역 상태를 초기화한다
> 프롬프트를 A/B로 비교하면서 `_cache`만 지우고 `_last_names`(직전 소제목 힌트)를 안 지웠다.
> 앞 조건의 소제목 이름이 뒤 조건으로 새어 들어가 결과가 오염됐다.

### 1-4. LLM 출력은 편차가 크다 — 1회 측정으로 결론 내지 않는다
> 같은 입력·같은 설정인데 `19,1,1,1,1`과 `9,8,3,2,1`이 번갈아 나왔다.

최소 2회 이상 돌려보고, **편차 자체도 결과의 일부**로 본다(편차가 줄어드는 것도 개선이다).

### 1-5. 상호작용하는 두 값을 따로 재면 틀린다
> 프롬프트와 소제목 상한은 독립이 아니었다. **프롬프트가 하한(과분할)을 지켜주면 상한은 넉넉히 둬도 된다.**
> 옛 프롬프트로 상한만 재서 "올리면 나빠진다"는 잘못된 결론을 냈다.

### 1-6. 임계값에는 근거를 남긴다
`_MAX_ARTICLES = 60`처럼 근거 없이 보수적으로 잡은 값이 **61건 회차에서 소제목을 통째로 망가뜨렸다.**
상수를 정할 땐 주석에 **실측 수치와 계산**을 남긴다(토큰 수, 비용, 관측된 최대값 대비 여유).

### 1-7. 폼 검증을 curl로 재현할 때는 실제 데이터가 아니라 백업본에 대고 한다
> 시간대 30분 단위 검증을 테스트하다가 실제 운영 중인 `data/settings.json`의 수집 시간
> 설정(6개 시간대)을 curl로 직접 덮어써 1개로 줄여버렸다. 즉시 알아채고 이전에 확인해둔
> 값으로 복구했지만, 애초에 `load_settings()`/`save_*` 함수를 파이썬에서 직접 호출해
> 격리된 상태로 검증했어야 했다.

폼 핸들러(`/save-*`, `/schedule/add-slot` 등)를 curl로 때리는 검증은 **그 요청이 실제
`data/*.json`을 그대로 덮어쓴다.** 저장 로직 자체를 검증할 땐 앱을 거치지 말고
`app.settings`의 함수를 파이썬에서 직접 호출하거나(그 자체로 저장 로직을 검증하는 것),
반드시 먼저 `cat data/settings.json`으로 현재 값을 백업해둔다.

---

## 2. 자동화와 사람 손

### 2-1. 두 가지 기본 규칙
1. **사람 손을 안 타면 기본적으로 모두 자동으로 나간다.**
2. **사람 손을 안 탈 수는 없으니, 타되 최소화한다.**

### 2-2. 자동화가 실패한 순간에만 사람을 부른다
정상일 땐 손대지 않고 나가고(규칙1), **자동화가 스스로 실패를 감지했을 때만** 멈추고 알린다(규칙2).
> 규칙기반으로 폴백한 회차가 아무도 모르는 채 자동발송된 사고가 있었다.

### 2-3. 되돌릴 수 없는 지점에만 유예를 둔다
"확정" 단계는 눌러도 계속 수정이 가능해서 **지킬 게 없는 걸 지키는 단계**였다 — 통째로 없앴다.
지금 유예가 걸린 건 진짜 되돌릴 수 없는 **발송** 하나뿐이다.

### 2-4. 빈 설정으로도 잘 돌아가고, 채울수록 내 것이 된다
설정을 하나도 안 한 상태가 **곧 기본 동작**이어야 한다.
설정은 자동을 대체하는 게 아니라 **자동과 수동 사이의 다이얼**이다.
> 소제목 규칙 0개 = 완전 AI 자동 / 규칙 12개 = 거의 수동 제어. 같은 코드, 모드 전환 스위치 없음.

### 2-5. 아무 일도 안 일어나는데 카운트다운을 보여주지 않는다
자동발송을 끄면 카운트다운도 뜨지 않는다. 화면이 거짓말을 하면 안 된다.

---

## 3. 담당자의 작업은 절대 조용히 사라지지 않는다

이 앱의 신뢰는 **"내가 다듬어둔 게 안 날아간다"**에 달려 있다.

### 3-1. 소제목의 정체성은 "이름 문자열"이다 — 이름이 바뀌면 다 끊긴다
수동 이동(`group_overrides`)·이름표(`group_labels`)·순서(`group_order`)가 **전부 소제목 이름을 키로** 저장된다.
AI가 재분류하며 이름을 한 글자만 바꿔도 그 기록이 통째로 고아가 된다.

**여기서 파생된 복잡함이 8개다.** 새 기능이 이 사슬을 더 늘리지 않는지 확인한다:
```
이름이 바뀜 → 재분류를 막음(allow_call=False) → 새 기사가 갈 데가 없음
→ 📂 소제목 미분류 칸 → _find_partial → 🤖 버튼 경고창 → 되돌리기가 LLM 캐시까지 스냅샷
```

### 3-2. 이름이 저장 키다 — 바꾸기 전에 반드시 검색한다
사용자에게 보이는 이름을 바꿀 땐 `group_labels` / `group_order` / `group_overrides` / `custom_groups` / 저장된 회차 파일을 **옛 이름으로 전부 검색**한다. 남아 있으면 마이그레이션, 없으면 그냥 바꾼다.

### 3-3. 되돌리기는 역연산이 아니라 스냅샷으로 한다
동작마다 역연산을 짜면 하나라도 빠뜨렸을 때 조용히 어긋난다.
이 앱의 큐레이션 상태는 작은 JSON 몇 개뿐이라 통째로 뜨는 비용이 무의미하게 작다.
**단, 메모리 캐시(LLM 분류 결과)도 함께 떠야 한다** — 파일만 되돌리면 화면은 캐시의 옛 답을 계속 보여준다.

### 3-4. 파괴적 동작 앞에는 무엇이 위험한지 숫자로 알린다
"소제목을 새로 만들면 직접 옮겨둔 기사 **N건**의 위치가 초기화될 수 있습니다."

### 3-5. 같은 기사 목록을 두 경로가 서로 다른 필터로 넘기면 캐시가 깨진다
LLM 분류 캐시의 키는 **기사 URL 집합**이다. 저장 경로(`collect_run`)는 숨긴 기사를 빼고 분류했는데
큐레이션 경로(`snapshot_group_names`)는 넣어서 넘기면, 집합이 달라 캐시가 안 맞고 → 진짜 API 재호출 →
소제목 이름이 새로 지어지고 → 3-1의 사슬이 통째로 끊긴다(2026-08-14 실사고).
**필터는 호출부마다 챙기지 말고 함수 안에서 강제한다** — 같은 실수를 할 수 있는 호출부가 여섯 곳이었다.

### 3-6. 정렬은 담당자가 마지막에 만진 순서가 이긴다
언론사 우선순위는 **출발 순서일 뿐**이고, 그 뒤 모든 단계는 넘겨받은 리스트 순서를 보존해야 한다.
캐시·분류 결과는 "어느 소제목인가"(소속)만 정하고 "몇 번째인가"(순서)는 정하지 않는다 —
캐시에 적힌 순서를 그대로 쓰면 그건 곧 *분류 시점의 언론사 순서*라, 담당자의 ↑/↓가 렌더링 한 번에
되돌아간다(2026-08-14 실사고, `_rebuild`). 큐레이션 조작 결과를 저장할 때 목록을 재배열하지도 않는다.

---

## 4. 화면에만 있는 것이 보고서로 새면 안 된다

`📂 소제목 미분류`, `🤖 AI 배지` 같은 **화면 전용 표시**가 복사·다운로드·텔레그램·이메일 텍스트에 섞여 나간 적이 실제로 있다.

### 4-1. 배지는 이름 문자열 **바깥**의 별도 `<span>`으로 만든다
이름에 섞으면 내보내기 텍스트까지 따라간다.

### 4-2. 임시 칸은 내보낼 때 실제 소제목으로 접어서 내보낸다
기사는 절대 빠뜨리지 않되(=`기타`로 합침), 임시 칸 **이름**은 나가지 않는다.

### 4-3. 새 표시를 만들면 유출 지점을 전부 확인한다
화면 4개 · `plain_text` · `.txt` 다운로드 · `build_latest_plain_text()` — 매번 다 검사한다.

---

## 5. UI / UX

### 5-1. 호버로만 보이는 기능은 모바일에서 존재하지 않는 것과 같다
이 앱은 폰에서도 본다. `opacity: 0` → `:hover` 방식은 **모바일에서 영영 누를 수 없다.**
같은 이유로 **설명을 `title` 툴팁에만 두지 않는다** — 툴팁도 마우스가 있어야 보인다.

### 5-2. 메뉴에는 "한 번 누르면 끝나는" 동작만 넣는다
`↑`/`↓`처럼 원하는 자리까지 여러 번 눌러야 하는 동작을 메뉴에 넣으면
`열기 → 누르기 → 닫힘 → 다시 열기`를 반복하게 된다.

### 5-3. 가장 자주 쓰는 동작은 한 번에 닿아야 한다
🗑️는 한 번이면 끝나지만 큐레이션에서 가장 빈번해서 메뉴에 넣지 않는다.

### 5-4. 단서조항이 많은 앱은 친절한 앱이 아니다
구조가 이미 말하고 있으면 글로 또 쓰지 않는다.
> `이 아래는 복사·내보내기에 포함되지 않음` 구분선이 이미 "보고서 밖"을 말하므로, 그 아래 칸에 같은 말을 또 적지 않는다.

**힌트는 "화면 어디에도 안 적혀 있고, 모르면 사고 나는 정보"에만 붙인다.**

### 5-5. 안내 문구는 안심시킬지 경고할지를 먼저 정한다
저절로 해결되는 상태에 경고 문구를 붙이면, 담당자가 안 눌러도 될 버튼(=돈이 나가는 버튼)을 누르게 된다.

### 5-6. 보이는 실수가 안 보이는 실수보다 낫다
과잉 매칭(엉뚱한 게 들어옴)은 화면에 보이니 한 번 클릭으로 고친다.
누락(들어와야 할 게 안 옴)은 **보이지도 않는다.** 판단이 갈리면 재현율 쪽을 택한다.

### 5-7. 색 하나에 의미 하나
| 색 | 의미 |
|---|---|
| 노랑 `#FFF9C4` | 방금 승격됨 / 새로 도착함 |
| 연두 `#E7EFE8` | 위치가 방금 바뀜 |
| 파랑 | 선택됨 |
| 회색 `#F3F4F6` | 마우스가 올라간 행 |

새 상태를 추가할 땐 **이미 쓰는 색을 재사용하지 않는다.** 세 가지 의미가 한 색이면 아무 의미도 없다.

### 5-8. 표식은 "예외"에만 붙인다
출처가 셋이라고 표식을 셋 만들면 모든 줄에 표식이 붙는다.
**기본은 무표식**, 예외만 표시한다(AI가 만든 것 🤖, 내가 만든 것 하늘색 점선).

### 5-9. 배지가 약속한 일을 버튼이 실제로 해야 한다
> 🤖 버튼에 미분류 건수 배지가 붙어 있는데 누르면 전체를 갈아엎고 "초기화될 수 있습니다" 경고가 떴다.
> 배지는 "6건만 처리해줄게"라고 유혹하는데 동작은 정반대였다.

### 5-10. 담당자 본인의 표현이 곧 멘탈모델이다
대화에서 계속 "담아둠"이라 부르면 화면 라벨도 `담아둔 기사`로 맞춘다.
(`직접 추가한 기사` → "어디에 추가됐다"처럼 읽혀 오히려 오해를 키웠다)

### 5-11. 확인창·삭제창은 담당자가 화면에서 본 이름을 보여준다
저장 키는 원래 이름이더라도, **사람에게 보여주는 문구는 표시 이름**으로 분리한다.

---

## 6. LLM 사용 (`app/llm_classifier.py`)

### 6-1. LLM은 있으면 좋은 것이지 의존 대상이 아니다
API 키 없음 / 패키지 미설치 / 호출 실패 / 기사 수 범위 밖 — **모든 실패는 조용히 삼키고 규칙 기반으로 폴백**한다.
소제목 품질 개선 기능 하나 때문에 스크랩 전체가 멈추면 안 된다.

### 6-2. 조용히 삼키되 **로그는 반드시 남긴다**
> 기사 수 초과 분기만 로그 없이 `return None` 했다. 그 결과 3개 회차가 폴백했는데도
> "소제목이 갑자기 이상해졌다"는 증상만 보이고 원인을 찾을 수 없었다.

삼키는 것과 침묵하는 것은 다르다.

### 6-3. 재분류는 비싸서가 아니라 **파괴적이라서** 아낀다
비용은 회차당 12원 수준이라 무의미하다. 진짜 비용은 **담당자 큐레이션의 소실**이다(§3-1).

### 6-4. 호출 시점을 설계에 명시한다
"새 기사가 오면 즉시" 같은 순진한 설계는 새로고침마다 호출돼 **비용이 4배**가 된다.
호출은 **회차당 정해진 횟수**로 묶고, 그 외에는 담당자가 버튼을 누를 때만 부른다.

### 6-5. 캐시는 정확 일치뿐 아니라 부분집합·상위집합도 재사용한다
기사가 줄기만 했으면(숨기기) 기존 분류를 그대로 쓰고,
늘기만 했으면 기존 묶음은 두고 새 기사만 따로 모은다.

### 6-6. 프롬프트로 하한을 지키면 상한은 넉넉히 둬도 된다
`기사가 10건 미만이면 억지로 쪼개지 마라`가 과분할을 막아주므로, 소제목 상한을 올려도 적은 회차가 망가지지 않는다.

### 6-7. 실패하면 "새로 짓지" 말고 "이미 있는 걸 우선"한다
> 기사가 하나 늘고 하나 빠지기만 해도 캐시 재사용 조건이 깨져 규칙 기반(단어 빈도)으로 떨어졌다.
> 앱을 재시작해도 메모리 캐시가 비어 같은 일이 벌어졌다. API 호출이 실패해도 곧장 규칙 기반으로 떨어졌다.

세 가지 다른 원인이 같은 증상(멀쩡하던 이름이 한 단어로 뭉개짐)을 냈다. 공통 처방: **AI 실패 시 기본 동작을 "규칙 기반으로 새로 짓기"가 아니라 "직전 결과 재사용"으로 바꾼다.** 완전히 새로 지어야 하는 경우(첫 분류)에만 규칙 기반이 안전망으로 나선다. 대체할 이전 결과가 있는데도 실패했다고 새로 지으면, 낡았지만 멀쩡한 이름보다 훨씬 나쁜 결과(한 단어 이름)를 만든다.

이 원칙을 지키려면 캐시가 **디스크에도** 있어야 한다 — 메모리에만 있으면 "이전 결과"가 재시작 한 번에 사라져 원칙 자체가 무력화된다.

---

## 7. 저장소 패턴

### 7-1. 하루짜리 상태는 **lazy reset**으로 만든다
파일에 `{"date": "YYYY-MM-DD", ...}`를 같이 저장하고, 읽을 때 날짜가 오늘이 아니면 빈 값으로 취급한다.
파일을 그 자리에서 지우지 않는다(다음 쓰기에서 자연히 덮인다).
> `manual_articles` · `custom_groups` · `draft_articles` · `undo` · `manual_keyword_note` 전부 같은 패턴.

### 7-2. 형식을 바꿀 땐 옛 데이터를 조용히 버리지 않는다
`load_*()`에서 옛 형식을 새 형식으로 옮긴다. 마이그레이션 없이 바꾸면 **사용자에겐 버그로 보인다.**

### 7-3. 다만 **효력 없던 값은 이어받지 않는다**
> `telegram_auto_send: False`는 "끄고 싶다"는 의사가 아니라, 아무도 안 읽던 고아 설정의 잔재였다.
> 그대로 옮겼다면 실제로 나가던 자동발송이 갑자기 멈췄을 것이다.

마이그레이션 전에 **그 값이 실제로 동작에 영향을 줬는지** 확인한다.

### 7-4. 읽기-수정-쓰기는 락으로 감싼다
`ThreadingHTTPServer`라 요청이 동시에 들어온다. 일괄 숨기기가 `/hide-article`을 기사 수만큼 **동시에** 부르므로, 락이 없으면 마지막 쓰기가 앞의 것을 덮어 조용히 유실된다.

### 7-5. 파일 쓰기는 항상 `atomic_write_text`
쓰는 도중 앱이 죽어도 파일이 깨지지 않는다.

---

## 8. 화면 렌더링

### 8-1. HTML은 정적 파일로 다시 만든다 — 웹 프레임워크는 없다
`app/settings_server.py`(`http.server`만 씀)가 유일한 예외이며, 설정 저장·큐레이션·정적 파일 서빙만 한다.

### 8-2. `render_article`은 3화면(확정본·초안·정기 보관함) 공용이다
**HTML은 한 곳만 고치면 되지만 CSS와 JS는 공유가 안 된다** — `renderer.py` / `preview_renderer.py` / `history_renderer.py` 세 곳에 각각 넣어야 한다.
전체 기사(`live_renderer.py`)는 행 마크업 자체가 달라 항상 별도다.

### 8-3. 파이썬 문자열 안의 JS를 조심한다
JS 코드가 파이썬 삼중따옴표 안에 들어 있어서, `\'` 같은 이스케이프가 **파이썬 파서에 먼저 먹혀** 브라우저에 깨진 JS가 전달된다.
따옴표가 낀 인라인 `onclick` 대신 `document.createElement`로 만든다.

### 8-4. CSS는 `{{ }}`로 이스케이프한다
템플릿이 `.format()`을 쓰므로 CSS 중괄호는 두 번 쓴다.

### 8-5. 팝오버는 같은 패턴을 재사용한다
재클릭으로 닫힘 + 바깥 클릭으로 닫힘 + (여러 개면) 한 번에 하나만 열림.

---

## 9. 외부 API

### 9-1. 자격 증명은 `.env`에서만 읽는다 — 하드코딩·커밋 금지
### 9-2. 네이버 외의 연동(텔레그램·이메일·Claude)은 **없어도 앱이 돌아가야** 한다
설정이 없거나 호출이 실패하면 그 기능만 건너뛰고 로그를 남긴다.
### 9-3. 동시 호출은 상한을 둔다
키워드 검색은 최대 8개 병렬(`_MAX_CONCURRENT_KEYWORD_SEARCHES`, 실측 상한)이고, 정기·수시·알림이 이 전역 세마포어 하나를 함께 쓴다. 더 늘려도 빨라지지 않고(가장 느린 하나가 전체 시간을 정한다) rate limit에 더 잘 걸린다.
### 9-4. 하나가 실패해도 나머지는 살린다 — 다만 **빈 결과로 바꿔치지 않는다**
키워드 하나가 429를 맞아도 검색 전체를 죽이지 않는다. 그렇다고 그 키워드를 빈 결과로 대체하면 「오늘 0건」과 구별할 수 없으므로, 3번 재시도 후에도 실패하면 `failed_keywords`로 올려 보낸다 — 정기 회차는 회차 전체를 재시도하고, 전체 기사는 그 키워드 캐시를 건드리지 않고 경고 배너를 띄운다.

---

## 10. 지켜야 할 업무 규칙 (제품 정의)

코드가 돌아가도 이걸 어기면 기능이 틀린 것이다. 자세한 건 [PRD.md](PRD.md).

- 같은 제목 기사는 하나로 합치되, **다른 언론사·판본은 남긴다**
- 방송사(KBS·MBC) → 주요 일간지 → 나머지 순서. **나머지도 절대 버리지 않는다**
- 회차는 각자의 시간창(`start` 제외 ~ `end` 포함) 안 기사만 모은다
- 출력 줄에 **게시 날짜를 넣지 않는다** (화면 표시는 무관)
- 원문 크롤링 금지 — 네이버 API의 `description`만 저장한다
- 빈 회차는 빈 목록이 아니라 `💤`
- 실패하면 5분 간격으로 3번까지 재시도
- 회차 파일·수시 카드는 1년 뒤 삭제 (숨긴 기사 기록만 7일)
