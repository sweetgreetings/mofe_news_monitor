# ADHOC_DESIGN.md — 수시 모니터링(비정기 스크랩) 설계 명세

> **상태: 구현 완료** (2026-08-13 설계 확정·구현, 이후 4단 흐름으로 개편)
>
> **이 문서는 「왜 그렇게 정했나」만 담는다.** 지금 유효한 규칙은 [CLAUDE.md](CLAUDE.md)
> "수시 모니터링"이 진실이고, 이 문서와 어긋나면 CLAUDE.md가 맞다. 구현 중 바뀐 세부
> 결정·잡은 버그·기각한 대안은 [HISTORY.md](HISTORY.md) "수시 모니터링" 항목에 있다.
>
> **절 번호(§)는 주소다** — 코드 주석과 CLAUDE.md가 `ADHOC_DESIGN.md §6.5`처럼 인용하므로
> 번호를 다시 매기지 않는다. 설계가 뒤집힌 절도 번호 자리를 남기고 「바뀜」으로 적는다.

---

## 1. 무엇을 푸는 기능인가

정기 모니터링은 **시간대**가 축이다 — 09:00 / 10:30 / 13:30 / 16:30에 자동으로 돈다.
그런데 정책 담당자의 실제 업무에는 다른 축이 있다:

> "재경위가 3시부터 열려서 4시쯤 끝난다. 후속 기사가 그 사이에 나올 테니
> **2시 30분~4시 구간만** 따로 모아서 보고해야겠다."

상위자가 "이거이거 검색해와"라고 지시하는 상황이다. 매일일 수도, 하루 두 번일 수도 있고,
정기 모니터링과는 **완전히 별개로** 돌아간다.

그리고 이렇게 모은 결과가 쌓이면 **기관의 주요 이슈 아카이브**가 된다 —
사안별로 큐레이션된 데이터로 남는다.

### 정기와의 차이 요약

| | 정기 모니터링 | 수시 모니터링 |
|---|---|---|
| 축 | 시간대 (스케줄) | 사안 |
| 트리거 | 스케줄러 자동 | 사람이 [수집] 클릭 |
| 시간창 | 설정된 슬롯 | 담당자가 매번 지정 |
| 검색어 | 등록된 키워드 그룹 | 그 회차 전용 임시 검색어 |
| 단계 | 4단 (전체 기사·초안·확정본·보관함) | 4단 (새 수집→원본→확정본→보관함, §6.14) |
| 발송 | 텔레그램·이메일, 자동발송 있음 | **텔레그램만, 담당자가 누를 때만** (회차 마감이 없어 자동발송이 없다) |
| 보관 | 1년 후 삭제 | 1년 후 삭제 (같다) |
| 상태 저장 | 종류별 전역 파일 12개 | **카드 하나 = 파일 하나** |

---

## 2. 왜 초안이 없는가 (2026-09 「2단계」에서 4단 흐름으로 바뀜)

정기에 **초안**이 있는 이유는 "수집이 아직 안 끝났기 때문"이다.
13:30~16:30 회차는 16:30이 돼야 닫히고, 그때까지 기사가 계속 들어온다.
**시계가 끝을 정한다.**

수시 모니터링은 담당자가 [수집]을 누르는 순간 **그 시간창이 이미 닫힌 과거**다.
더 들어올 기사가 없다. "수집 중"이라는 상태가 물리적으로 존재하지 않으므로,
초안 단계를 만들면 빈 껍데기가 된다. **이 판단은 지금도 유효하다** — 수시엔 초안이 없다.

다만 처음 적었던 「카드 하나 = 수집·정리 둘 다」(2단계)는 2026-09에 **원본과 확정본으로
갈라졌다.** 시계가 아니라 **담당자의 선택**이 두 화면을 가르기 때문이다 — 조건에 걸린
기사 전부(원본, 로데이터)와 그중 골라 보고서로 낼 것(확정본)은 지우는 기준이 다르다.
원본에서 지우면 "이 기사는 안 봐도 된다"이고, 확정본에서 지우면 "보고서에서 뺀다"이다.
한 화면에서 둘을 다 하면 그 둘이 같은 🗑에 묶인다. 지금 흐름은 §6.14.

```
[새 수집] ─(수집)─> [원본: 로데이터] ─(골라 보냄)─> [확정본: 보고서] ─> [수시 보관함]
                         ↑                                                (확정본만 쌓임)
                         └─ 조건(수집 시간) 바꿔 몇 번이든 재수집
```

---

## 3. 저장 구조 — 카드 하나 = 파일 하나

### 3.1 왜 정기와 다른가

정기는 상태가 `data/` 아래 **평평한 전역 파일 12개**에 흩어져 있다
(`hidden_articles.json`, `group_overrides.json`, `group_order.json`, `custom_groups.json`,
`preview_order.json`, `manual_keyword_note.json` 등).

이게 잘못된 설계는 아니다 — 정기는 "오늘 살아있는 회차가 하나"고, 그 하나의 상태를
**네 화면(실시간·초안·확정본·history)이 공유**해야 하므로 종류별로 쪼개두는 게 편하다.

수시 모니터링은 정반대다. **카드가 동시에 여러 개 살아있고 각각 독립**이다.
같은 구조를 쓰면 12개 파일 전부에 카드 id 네임스페이싱을 넣어야 한다.

### 3.2 디렉터리

```
data/adhoc/
├── issues.json              사안 목록 (메타만)
├── cards/
│   ├── 20260813-143012-a7f3.json
│   └── 20260806-100455-b2c1.json
└── trash/                   삭제한 카드 (CLAUDE.md 규칙 — 바로 지우지 않음)
```

### 3.3 카드 파일 스키마

```json
{
  "id": "20260813-143012-a7f3",
  "issue_id": "issue-a7f3",
  "report_title": "재경위",
  "collect_date": "2026-08-13",
  "window": { "start": "14:30", "end": "17:00" },
  "keywords": ["재정경제위원회", "국정감사", "세제개편"],
  "options": {
    "use_outlet_whitelist": false,
    "exclude_photo": false,
    "exclude_personnel": false
  },
  "must_keywords": [],
  "raw": true,
  "version": 1,
  "status": "editing",
  "created_at": "2026-08-13T14:30:12",
  "archived_at": null,
  "collect_log": [
    { "at": "2026-08-13T16:05:12", "window_end": "16:00", "found": 20, "added": 20 },
    { "at": "2026-08-13T17:10:44", "window_end": "17:00", "found": 25, "added": 5 }
  ],
  "group_order": ["세제개편 질의", "현장 반응"],
  "custom_groups": ["현장 반응"],
  "group_summaries": { "세제개편 질의": "…" },
  "articles": [
    {
      "outlet": "KBS",
      "title": "재경위, 세제개편안 두고 여야 공방",
      "url": "https://n.news.naver.com/…",
      "summary": "…",
      "pub_date": "2026-08-13T15:12:00+09:00",
      "group": "세제개편 질의",
      "order": 0,
      "hidden": false,
      "added_at": "2026-08-13T16:05:12",
      "added_by": "collect"
    }
  ]
}
```

확정본 카드(§6.13)는 이 스키마에 `kind` 한 필드만 더한다 —
`"collect"`(기본, 지금까지의 모든 카드) / `"bundle"`(검색을 안 하는 카드). 필드가
없는 옛 카드는 `"collect"`로 읽는다.

**설계 뒤에 늘어난 필드**(자세한 뜻은 CLAUDE.md "수시 모니터링"):

| 필드 | 어디에 | 무엇 |
|---|---|---|
| `must_keywords` | 원본 | 「모두 포함」이면 `keywords`와 같은 목록, 「하나라도」면 빈 목록 — 새 필드를 만들지 않고 두 목록의 관계로 검색 방식을 적는다(§6.4b) |
| `raw` / `version` | 원본 | 로데이터 화면인가 · 같은 날·같은 사안의 몇 번째 판인가(`#2`) |
| `raw_marks` | 원본 | `{url: {state: hidden\|shown, at}}` — 원본의 🗑는 기사를 빼지 않고 「숨김」 표시만 하므로 기사의 `hidden`과 따로 둔다 |
| `basis_time` · `source_card_id` · `source_version` | 확정본 | 머리줄 기준 시각 · 어느 원본의 어느 판에서 왔는가 |
| `send_count` · `sent_at` · `send_log` | 확정본 | 텔레그램 발송 기록(§6.8) |
| 기사의 `sent_from` · `hidden_at` | 확정본 | 어느 원본·어느 기준 시각에서 보냈는가 · 언제 숨겼는가(원본의 「보냄 취소」가 이 시각을 견준다) |

**`status`·`archived_at`은 쓰기만 하고 읽는 코드가 없다** — 보관은 별도 동작이 아니라
저절로 되는 일이라 상태값이 필요 없어졌다(§5.3). `memo`는 아예 없앴다(카드 메모 칸 없음).

### 3.4 이 구조가 자동으로 해결하는 것들

| 정기 방식 | 수시 | 효과 |
|---|---|---|
| `hidden_articles.json` (URL 목록) | 기사의 `hidden` 불린 | **숨긴 기사 복구 UI가 저절로 생김** — 카드를 읽으면 숨긴 것도 같이 읽힌다 |
| `group_overrides.json` (전역 URL 맵) | 기사의 `group` 필드 | `snapshot_group_names`가 하던 일을 저장 구조가 대신함. 정기 화면 오염 불가 |
| `preview_order.json` | 기사의 `order` | 순서가 기사에 붙어 있어 재수집 병합 시 안 흐트러짐 |
| `custom_groups.json` (전역·자정 리셋) | 카드의 `custom_groups` | **자정 지나도 아카이브 소제목 이름이 안 사라짐** |
| ~~`manual_keyword_note.json` → 카드의 `memo`~~ | — | **없앴다** — 수시 카드에는 메모 칸을 두지 않는다 |
| — | `collect_date` | 자정 롤오버 방어 (§6.3) |
| — | `added_by: "manual"` | 담아둠에서 넣은 기사는 "구간 밖" 검사 면제 |
| `run_slot`이 정체성 (파일명) | `id`가 정체성, 이름은 필드 | **사안명을 바꿔도 파일을 안 건드림** |

### 3.5 issues.json

```json
{ "issues": [
  { "id": "issue-a7f3", "name": "재경위",
    "last_keywords": ["재정경제위원회", "국정감사", "세제개편"],
    "created_at": "2026-07-29T15:02:33" }
] }
```

**`card_ids` 목록은 두지 않는다.** 카드 파일을 glob으로 훑어 `issue_id`로 묶는다
(`app/storage.py` `list_all_runs()`와 같은 방식) — 같은 정보를 두 곳에 두면 어긋난다.

`last_keywords`는 사안을 고르면 지난번 검색어를 자동으로 채우는 용도다.

---

## 4. 코드 배치 — 같은 폴더, 하위 패키지

```
app/
├── (기존 모듈 — 공유 계층, 손대지 않음)
└── adhoc/
    ├── card.py               카드 CRUD
    ├── collector.py          수집·병합
    ├── renderer.py           카드 화면
    ├── archive_renderer.py   사안 목록 + 지난 사안
    └── routes.py             /adhoc/* 핸들러
```

### 의존 방향은 단방향

```
app/adhoc/*  ──import──>  app/*        ✅
app/*        ──import──>  app/adhoc/*  ❌ 금지
```

정기 코드가 수시를 import하는 순간 격리가 깨지고, "수시를 건드렸는데 정기가 망가지는"
상황이 시작된다.

**예외는 하나** — `app/settings_server.py`의 라우팅. 진입점이라 어쩔 수 없다. 대신 한 줄만:

```python
elif self.path.startswith("/adhoc/"):
    adhoc_routes.handle(self, self.path, form)
```

if/elif 체인에 수시 라우트를 늘어놓지 않는다 (`settings_server.py`는 이미 3,358줄).

### 재사용하는 것

`naver_api.py` · `filters.py` · `sorter.py` · `llm_classifier.py` · `summarizer.py` ·
`settings.py` · `config.py` · `icons.py` · `atomic_write.py` · `excel_export.py` ·
`topnav.py` · `report_message.py` · `send_log.py` · `telegram_bot.py`, 그리고 두 흐름이
함께 쓰는 저장소 `labels.py` · `summary_overrides.py` · `telegram_recipients.py`.
`app/renderer.py`에서는 화면 조각 몇 개만 가져다 쓴다(`AI_SUMMARY_HEADING` ·
`apply_line_template` · `apply_subheading_format` · `format_slot_time_kr` ·
`range_select_script` · `known_label_chips_html`).

수시 폴더 안도 갈려 있다 — `card.py`(저장·규칙) · `collector.py`(수집) ·
`classifier.py`(소제목) · `renderer.py`(화면) · `routes.py`(라우팅) ·
`archive_renderer.py`(보관함) · `send.py`(발송) · `undo.py`(되돌리기).

---

## 5. 화면

### 5.1 진입 (home.html)

> **바뀜**: 아래 「수시 모니터링 카드」는 만들지 않았다. 홈은 정기·수시 **두 줄짜리 흐름도**
> 하나로 가고, 수시 줄이 `새 수집 → 원본 → 확정본 → 보관함`이다(규칙은 CLAUDE.md "홈").
> 「정리 중 N건」처럼 **세어서 보여 주는 표시는 두지 않는다** — 수시는 담당자가 방금 만든
> 것을 바로 이어 보는 흐름이라, 홈에서 개수를 세어 줄 필요가 없었다.

(옛 설계) 기존 스크랩 흐름 아래에 별도 카드로:

```
수시 모니터링
  ➕ 새 수집        🗂️ 정리 중 1건        📚 지난 사안 더 보기
```

### 5.2 새 수집 — 입력은 3줄

```
사안    [재경위 ▾ / + 새 사안]        ← 고르면 지난 검색어 자동 입력
검색어  [재정경제위원회] [국정감사] [+]   3 / 5   [하나라도 | 모두]
수집시간 [14 ▾:30 ▾] ~ [16 ▾:00 ▾]  [지금] [0시부터][최근 1시간][최근 3시간]   [수집]
거르기  ☐ 등록 언론사만   ☐ 사진기사 제외   ☐ 인사 발령 기사 제외
────────────────────────────────────────────
지난 수집  (1년치 · 날짜 · 사안명 · 검색어 — 누르면 폼이 채워진다)
```

> **바뀐 것**: 시각은 자유 입력이 아니라 **시 ▾ : 분 ▾ 드롭다운**이다(분은 10분 단위이되,
> 「지금」이 집어넣는 56분처럼 목록에 없는 값은 그 자리에 끼워 넣는다 — 끝 시각이 보고서
> 머리줄에 찍히므로 반올림하면 안 된다). 체크 칸 이름은 「이 회차에만 적용」이 아니라
> **「거르기」**다(수시엔 회차가 없다). 검색 방식 `하나라도 | 모두`가 붙었고(§6.4b),
> 형광펜은 수시에 칠하지 않으므로 ⓘ 안내에서 뺐다. 폼 아래 「지난 수집」 목록이 는다.

**상위자 지시에 대응하는 기능이라 카드 생성이 30초 안에 끝나야 한다.** 설정 항목이 많으면
담당자는 그냥 네이버에서 직접 검색하고 만다.

### 5.3 카드 정리

> **바뀜**: 아래 스케치에서 넷이 없어졌다 — **✍️ 키워드 작성 메모**(카드에 메모 칸을 두지
> 않는다, §3.3), **📥 수집 이력 칩**(`10:10 6건 · 10:16 +2건` — 기준 시각은 머리줄이 이미
> 말한다. `collect_log`는 「새로 들어온 기사」 노랑 판정용으로 계속 쌓인다), **본문에 펼쳐진
> 재수집 줄**(지금은 접힌 편집 창 안이고, 헤더엔 「지금까지 다시 수집」 버튼 하나), **[보관]
> 버튼**(보관은 누르는 동작이 아니라 저절로 된다 — 그래서 `status`도 안 읽는다).
> 남는 근거는 아래 「요약은 반드시 맨 아래」와 좌하단 🗑 / 우하단 ☰뿐이다.
> 지금 화면은 CLAUDE.md "수시 모니터링".

(옛 설계)

```
수시 모니터링 17시 00분 기준([재경위])      ← 사안명은 직접 입력 가능
🔍 재정경제위원회 · 국정감사 · 세제개편   🕒 14:30~17:00
📥 16:05 (~16:00) 20건 · 17:10 (~17:00) +5건

재수집  [14:30] ~ [17:00]  [다시 수집]   기존 기사는 그대로 두고 새 기사만 추가돼요

✍️ 키워드 작성 메모 [                                    ]

[🤖 소제목 배정] [+ 소제목 추가] [소제목별 ▾]        [📋 복사] [⬇️ txt로 저장]

<세제개편 질의> 🤖  7건  ▲▼
  ☐ ㅇ (KBS) 재경위, 세제개편안 두고 여야 공방
     https://…
     15:12 · 2시간 전       [다른 소제목▾] [↗] [⧉] [⋯] [🗑] [↑] [↓]

💬 AI가 읽은 소제목별 주요 요약     ← 맨 아래
                                        [사안 목록에서 삭제] [보관 (지난 사안으로)]

좌하단 플로팅: 🗑 2 (숨긴 기사)      우하단 플로팅: ☰ (소제목 목차)
```

**요약은 반드시 맨 아래.** 담당자는 제목만 훑어 흐름을 봐야 하는데, 소제목마다 요약이 붙으면
정보 유입이 스크리닝을 방해한다. 이 앱은 요약 도구가 아니라 모니터링 도구다.

### 5.4 지난 사안 더 보기

> **바뀜(2026-09-22)**: 수시 보관함도 정기처럼 「날짜 → 회차」 2단이 됐다(회차 줄
> `HH:MM 기준 · 사안명`). 아래 사안 축은 **층으로는 기각**됐지만 — 3단으로 쌓으면 이름이
> 같은 사안을 회차마다 맞춰야 한다 — 「보기」 스위치로 **축을 바꾸는 길**로 남았다:
> `[날짜별 | 사안별]`, 사안별일 때만 정렬 칩 `[가나다순 | 최근순]`이 붙고 두 보기가 같은
> 마크업을 쓴다. 고른 보기는 기억하지 않는다(주소로만 오간다). 보관함엔 **확정본만** 쌓인다.
> 이유는 HISTORY.md 「수시 보관함 — 사안별 묶기」.

(옛 설계) 정기의 "지난 기사 더 보기"가 「날짜 → 시간대」인 것과 **축이 다르다**.

```
▾ 재경위                    재정경제위원회 · 국정감사 · 세제개편      회차 3
    8/13   14:30~17:00   보관됨   25건
    8/06   10:00~11:30   보관됨    8건
▸ 부동산 대책                                                    회차 4
▸ 국감 답변 자료            [정리 중]                             회차 1
```

---

## 6. 동작 규칙

### 6.1 검색

- `search_articles_by_groups(groups, after=시작, before=끝)` 호출 — **`naver_api.py` 수정 불필요**
- 당일 구간 수집은 이미 지원된다 (놓친 회차 보충 실행과 구조가 동일 —
  [naver_api.py:409](app/naver_api.py:409)의 `before` 초과 기사는 `continue`로 건너뛰고 페이지네이션 계속)
- 검색어 최대 **5개**. **[수정: 2026-09-01] OR 고정이 아니라 라디오 2지선다다** —
  `하나라도 있으면`(기본, OR) / `모두 있는 기사만`(AND). "모두"를 고르면 그 목록이
  `must_keywords`에도 **그대로 복사돼** 저장된다(§6.4a의 두 줄을 손으로 채운 것과 같은
  상태). 일부만 넣으면 1,000건 상한 검사(§6.4b, `must_keywords`만 본다)가 나머지 단어를
  안 덮어 교집합에서 기사가 조용히 빠진다.
  - 그래서 "모두" 모드의 상한은 5가 아니라 **3**(`MAX_ADHOC_MUST_KEYWORDS`)이다. 화면이
    카운터를 `0/3`으로 바꾸고 넘으면 [수집]을 잠그며, `new_card`도 서버측에서 거부한다.
  - 검색어가 1개 이하면 두 선택지가 같은 뜻이라 라디오를 흐리게 잠근다.
  - **"급하게 걸었다가 0건이 최악"이라는 원래 근거는 폐기했다** — `_handle_collect`이
    카드를 먼저 만들고 그 다음에 수집하므로, 0건이 나와도 카드는 남고 편집 패널에서
    조건을 지우면 즉시 돌아온다. 1,000건 상한 거부도 같은 이유로 여기서 이미 작동한다
    (`AdhocKeywordCapError`는 `AdhocCollectError`의 하위 클래스라 카드 화면으로 전달된다).
- 정기 키워드 함께 검색 옵션 **없음** (헷갈림)

### 6.2 시간창 검증

| 상황 | 처리 |
|---|---|
| 끝 시각이 현재보다 미래 | **차단.** 자동 보정 안 함 (받지도 않은 데이터를 받았다고 착각하게 됨) |
| 시작 > 끝 | **차단** |

경고 문구 (확정):
```
아직 17시 00분이 안 됐어요 (지금 16시 40분).
16시 40분까지만 수집할 수 있어요.
```

> **바뀜**: [앞뒤 바꾸기]·[16:40으로 맞추기] 버튼은 만들지 않았다 — 문구만 띄우고 담당자가
> 고친다. 시각이 드롭다운이 되면서(§5.2) 잘못 고르기 자체가 줄었고, 「수집 시간을 앱이
> 대신 정해 준다」는 인상을 주지 않는 편이 낫다(끝 시각은 보고서 머리줄에 그대로 찍힌다).

### 6.3 자정 롤오버

`collect_date`가 오늘이 아니면 **시간 입력·재수집을 비활성화**한다.
그대로 재수집하면 시간창이 오늘 날짜로 해석돼 엉뚱한 기사를 가져오기 때문.

```
⚠️ 8월 13일 기준으로 수집한 카드입니다 (오늘은 8월 14일).
   시간대를 그대로 다시 수집하면 8월 14일 기사를 가져오게 돼요.
                                          [오늘 회차 새로 만들기]
```

**기준일 변경 = 새 카드 만들기.** 같은 사안 아래 8/14 회차를 새로 만들고,
8/13 카드는 그대로 보관한다. 기존 기사를 전부 "구간 밖"으로 만드는 대신
실제로 일어나는 일(새 회차)을 그대로 표현한다.

### 6.4 재수집은 교체 — 단, 빠지는 기사를 지우는 것은 금지

> **[뒤집힘: 2026-08-20]** 원래 이 절은 "재수집은 병합 — 교체 절대 금지"였다. 방향이
> 반대로 바뀌었지만 **지키려던 목적은 그대로다.** 아래 "왜 뒤집혔나" 참고.
> (스펙 확정 → **구현 완료: 2026-08-20**. `app/adhoc/collector.py` `run_collect`가 교체로 동작하고 CLAUDE.md도 갱신됐다.)

카드는 **"조건의 스냅샷"이 아니라 "조건 그 자체"다.** 화면에 있는 목록은 언제나
*지금 이 카드의 조건으로 불러올 수 있는 기사 전부*여야 한다 — 조건을 고쳤는데 옛 조건의
기사가 남아 있으면, 담당자는 **자기 화면이 무슨 조건의 결과인지 말할 수 없게 된다.**

| 상황 | 처리 |
|---|---|
| 조건에 맞는 기존 URL | **그대로 유지** — 숨김·순서·소제목 배정 전부 보존 |
| 조건에 맞는 새 URL | 추가 (소제목이 이미 있으면 `📂 소제목 미분류`로 → 🤖 증분 배정) |
| **조건에서 벗어난 기사** | **삭제 금지.** 목록에서만 빼고 숨김함으로 — 조건을 되돌리면 저절로 돌아온다 |
| 담당자가 직접 숨긴 기사 | 조건과 무관하게 계속 숨김 (조건이 되살리지 않는다) |
| 소제목 배정·순서 | **기사 URL에 붙어 카드 JSON에 남는다** — 조건을 왕복해도 그대로 |

정기의 `_find_partial` + `UNCLASSIFIED_GROUP_NAME` + `/assign-unclassified`를 그대로 재사용한다.

**왜 뒤집혔나.** 원래 규칙의 근거는 이 시나리오였다:

```
16:05  수집 → 20건
16:20  5건 숨김, 순서 정리, 🤖 소제목 3개 배정      ← 담당자 노동 15분
17:10  시간창 17:00으로 수정 → 재수집 → 15분이 사라진다
```

이건 **교체가 곧 삭제일 때만** 참이다. 위 표처럼 "빠지는 기사는 숨김함으로"면 노동은
하나도 안 사라지고 ([CODING_CONVENTIONS.md](CODING_CONVENTIONS.md) §3 충족), 조건을 되돌리면
소제목 배정까지 그대로 돌아온다. 즉 **원래 목적(담당자 노동 보존)은 유지하면서 방식만 바꾼 것**이다.

반대로 병합을 고집하면 새 문제가 생긴다 — 검색어 편집(§6.4a)이 들어오면서
"검색어를 뺐는데 그 검색어로 들어온 기사가 화면에 남아 있는" 상태가 만들어지는데,
이건 담당자 눈에 **고장으로 보인다**. 사용자 표현 그대로:

> "그냥 아예 이용자가 '지금 조건에서 불러올 수 있는 기사만 나열'됐으면 좋겠어."

**실측이 병합의 전제를 무너뜨렸다.** 저장된 카드 18개를 전수 조사했을 때 (2026-08-20):

| 기사 | 검색어 | 숨김 | 사안 |
|---:|---:|---:|---|
| 1,100 | 2 | **0** | 비상대책 |
| 732 | 1 | **0** | 재정경제부 |
| 187 | 5 | **0** | 테스트 8/19 |

**카드 18개 전체에서 담당자가 숨긴 기사는 총 1건.** 위 시나리오가 전제한 "담당자 노동
15분"은 실제로는 일어나지 않았다 — 1,100건을 하나씩 정리하는 일은 아무도 시작하지
않는다. 보호할 노동이 없는 규칙이 정작 필요한 기능(검색어로 한 번에 줄이기)을 막고
있었던 셈이다.

**기각한 대안 — 작업대(하단 보관 구역).** 조건이 바뀌어도 살아남는 별도 구역을 카드
하단에 두는 안을 검토했다(정기의 `📌 담아둔 기사`와 같은 모양). 세 가지 이유로 기각:

1. **내보내기 기준이 모호해진다.** 지금은 "카드 = 보고서"라 복사/txt/엑셀이 뭘 내보내는지
   질문 자체가 없다. 작업대가 생기면 매번 "둘 중 뭐가 보고서냐"가 생기고, "작업대가
   비었으면 전체"는 같은 버튼이 상황에 따라 다르게 동작하는 것이라 더 나쁘다.
2. **위 실측이 그대로 적용된다.** 작업대는 "일일이 담는" 행동을 요구하는데, 담당자는
   일일이 고르는 행동을 하지 않는다(숨김 1건). 작업대가 빈 채로 남으면 보고서가 빈다.
3. **정기에 담아두기가 있는 이유가 수시엔 없다.** 정기의 `📌`는 실시간 현황(다른 화면)에서
   확정본으로 **건너가는 다리**다. 수시는 한 화면 안이라 건널 강이 없다.
   (`added_by: "manual"` 스키마가 읽는 코드만 있고 쓰는 코드가 없는 채로 남아 있는 것도
   같은 이유로 보인다 — 정기에서 개념만 옮겨왔다가 안 만든 자리.)

조건 두 벌이 필요하면 **카드를 두 개 만든다.** 그게 이미 이 설계의 분류 단위(사안 › 일자 ›
회차)와 맞고, 사용자 판단도 같았다 — "차라리 이용자가 사안명을 2개 짜겠지."

### 6.4a 카드 안에서 검색어 편집 — **없앴다 (원본 하나 = 검색 조건 하나)**

> **뒤집힌 절이다.** 원본 안에서 고칠 수 있는 것은 **수집 시간뿐**이고, 검색어·검색 방식은
> 읽기 전용 한 줄 + 「검색어를 바꿔 새로 수집 →」이다(새 원본이 `#2`로 선다).
>
> **왜 뒤집혔나**: 한 원본 안에서 검색어를 고치면 (1) 고치기 전·후에 보낸 기사가 한
> 확정본에 섞이고 (2) 그 원본이 무엇을 모았는지 기록이 사라진다. 원본은 로데이터이고
> 로데이터의 값은 「이 조건으로 이때 모은 전부」라는 기록에 있다. 기각 경위는 HISTORY.md
> 「수시 원본 검색어 편집 없앰」.
>
> 남은 것: **두 칸(검색어 / 꼭 포함할 검색어)이 아니라 한 칸 + 방식**이라는 저장 모양(§6.4b),
> 접어 두는 편집 창과 [편집] 버튼(지금은 수집 시간만), 「결과 배너를 띄우지 않는다」는
> 원칙(건수와 좌하단 배지가 이미 말한다), 그리고 그 유일한 예외인 1,000건 상한 경고.
> **옛 두 칸 카드**는 읽을 때 `normalize_condition`이 지금 모양으로 맞춘다.

### 6.4b 꼭 포함할 검색어와 1,000건 상한

AND는 URL 교집합이다(`app/naver_api.py` `_match_group`) — 텍스트 매칭이 아니다.
그래서 어느 한 키워드가 `_MAX_START`(1,000건)에 걸려 잘리면 **남아야 할 기사가 교집합에서
잘못 빠진다.** OR일 때의 잘림("덜 가져옴")과 손해 방향이 정반대다.

하필 좁히는 데 쓸 만한 말일수록 넓어서 상한에 닿기 쉽다(실측: `정부`는 하루 약 6,800건).
**상한에 닿으면 적용 자체를 거부한다** — 조용히 잘못 지우느니 아예 안 하는 쪽이 안전하다.

```
⚠️ '정부'는 오늘 기사가 1,000건을 넘어 정확히 거를 수 없어요.
   수집 시간대를 좁히거나 더 구체적인 말을 써주세요.
```

### 6.4c 키워드별 캐시

교체 모델이라 조건을 바꿀 때마다 다시 검색해야 하는데, 매번 전체를 돌리면 조건 하나
고칠 때마다 몇 초씩 기다리게 된다. **키워드별로 검색 결과를 캐시**해 같은 조건을
다시 돌릴 때 네이버를 다시 부르지 않는다 — 전체 기사(`app/live_cache.py`)와 같은 패턴이다.

> **바뀜**: 위 표(새 키워드 추가 / 다시 추가 / 제거 / 시간창 확대)는 **카드 안에서 검색어를
> 고치던 시절**의 호출량이라 §6.4a와 함께 무효다. 지금 캐시는 키가
> `(collect_date, keyword, window.start, window.end)`이고 **메모리 전용·40개**이며, 실패한
> 키워드는 캐시하지 않는다. 시간창이 바뀌면 키가 통째로 달라지므로 「늘어난 구간만」
> 받는 동작은 없다(그 최적화는 만들지 않았다 — 수시는 담당자가 화면 앞에서 한두 번 누르는
> 흐름이라 값이 없다).

호출량은 문제가 아니다 — 사안 2개를 하루 종일 만져도 약 500요청으로,
`NAVER_DAILY_CALL_LIMIT`(25,000)의 2% 수준이다. 실제 소진량은 추정이 아니라
`app/api_usage.py`가 실측으로 세고 있다.

### 6.5 소제목

- **소제목이 있는 화면은 확정본(과 옛 원본)뿐이다.** 새 원본(로데이터)은 「소제목 없이 한
  목록, 최신순」이 그 화면의 규칙이라 배정 버튼 자체가 없다(§6.14).
- **자동 분류 0회.** 버튼을 눌러야만 API 호출 (정기 초안의 `MIN_ARTICLES_FOR_AUTO` 같은
  자동 트리거를 넣지 않는다 — 카드는 계속 살아 있어서 성격이 다르다)
- 최대 **5개** (정기는 8개). 사안이 한정적이라 늘릴 필요가 없다
- 애매한 기사는 "기타"로 — 정기와 동일
- 분류 프롬프트에 별도 분기 필요: 정기 프롬프트의 "10건 미만이면 굳이 나누지 말라"는
  20~30건에서 5개로 제한하는 수시와 맞지 않음
- **`app/llm_classifier.py`의 전역 `_last_names`를 저장/복원해야 한다** —
  수시 분류가 정기 회차의 "직전 소제목 이름 이어쓰기"를 오염시킨다.
  안 하면 정기 소제목이 이유 없이 바뀌는, 원인 찾기 어려운 버그로 나타남
- LLM 캐시(`llm_classification_cache.json`)는 `frozenset(article_urls)` 기준이라 공유해도 안전
- **소제목 헤더의 🗑은 상태에 따라 뜻이 갈린다** — 기사가 있으면
  「이 소제목 통째로 숨기기」(`/adhoc/card/hide-group`), 화면에 보이는 기사가 없는
  커스텀 소제목이면 「빈 소제목 삭제」. 정기 확정본·초안의 같은 자리와 같은 규칙이다.
  **삭제가 아니라 숨기기**이고(기사는 카드에 남고 `hidden` 플래그만 붙는다),
  **화면에 보이는 기사만** 대상이다 — 조건 밖 기사까지 숨기면 조건을 되돌렸을 때
  §6.4("교체이지 삭제가 아니다")가 깨진다. 배경은 HISTORY.md
  "수시 모니터링 — 소제목 통째로 숨기기" 참고

### 6.6 요약

소제목 분류 호출이 **소제목 이름과 요약을 한 번에** 만들므로 추가 API 호출은 없다.
(정기의 `summarize_group()`을 부르지는 않는다 — 분류 응답에 실려 온 요약을 그대로
`group_summaries`에 넣는다. "추가 호출 없음"이라는 결론은 같다.)

- 위치는 **페이지 맨 아래** (§5.3 참조)
- `AI가 추출한 주요 키워드` 블록은 **넣지 않는다** — 검색어를 담당자가 직접 정했으므로
  AI가 키워드를 다시 뽑아줄 이유가 없다
- `UNCLASSIFIED_GROUP_NAME`이 복사/다운로드 텍스트로 새지 않도록 `_merge_unclassified()`
  동일 처리 필수 ([CODING_CONVENTIONS.md](CODING_CONVENTIONS.md) §4)

### 6.7 담아둔 기사 — 세 번째 출구 ~~(폐기: 2026-09-02)~~

> **[폐기: 2026-09-02] 만들지 않았고, 앞으로도 만들지 않는다.** 아래 설계는 정기의
> `📌 담아둔 기사`를 수시 카드에도 띄우는 안(정기 → 수시 방향)인데, 끝내 구현되지 않았다.
> 반대 방향(수시 → 정기)은 2026-08-25에 `📌` 버튼으로 잠깐 만들어졌다가 2026-09-02에
> 기능째 제거됐다 — **근거는 바로 위 §6.4가 「작업대」를 기각하며 쓴 그 문장이다**:
> "정기에 담아두기가 있는 이유가 수시엔 없다. 정기의 `📌`는 실시간 현황에서 확정본으로
> 건너가는 다리다. 수시는 한 화면 안이라 건널 강이 없다." 지금 정기-수시 사이에 기사가
> 오가는 통로는 **하나도 없다**(라벨만 두 흐름을 가로지르는데, 그건 기사를 옮기는 게
> 아니라 양쪽에 같은 태그를 다는 전역 저장소다). 배경은 HISTORY.md
> "수시 → 정기 담아두기 › 기능째 제거" 참고. 아래 내용은 기록으로만 남긴다.
>
> **다만 수시 *안에서* 기사를 옮기는 길은 따로 있다 — §6.13 모음 카드.** 그건 흐름
> 경계(수시→정기)가 아니라 검색 조건 경계(원본 A·B → 모음)를 넘는 것이라 위 기각
> 근거에 걸리지 않는다.

`📌 담아둔 기사`(`data/manual_articles.json`)는 이미 **초안·확정본 두 화면이 공유**하는
전역 임시함이다. 수시 카드가 세 번째 소비자가 된다.

```
       [전체 기사] ── 📌 (변화 없음)
                        │
                  ┌─────▼─────┐
                  │  담아둠    │  전역 임시함, 자정 소멸
                  └─────┬─────┘
            ┌───────────┼───────────┐
       초안(pop)   확정본(pop)   수시 카드(copy)
```

| 승격 대상 | 시맨틱 | 이유 |
|---|---|---|
| 정기 초안 / 확정본 | **pop** (현행 유지) | 둘은 같은 회차의 두 얼굴 — 한 번 가면 끝 |
| 수시 카드 | **copy** | 독립 아카이브 — 같은 기사가 정기에도 남아야 한다 |

**주의:**
- 카드 승격 핸들러는 `set_group_override()`를 부르면 **안 된다** (전역 파일 오염).
  `_render_manual_section` 렌더 함수만 재사용하고 저장은 카드 JSON에만.
- `→ 스크랩` 버튼의 "오늘 정기 회차 ≥1" 활성화 조건은 카드행에는 적용하지 않는다
  (카드는 정기 회차와 무관 — 아침 첫 회차 전에도 담을 수 있어야 한다).
- 카드 화면의 담아둠 섹션은 **기본 접힘** + 건수 배지 (전역 함이라 다른 사안 기사도 보인다).

### 6.8 출력

- 헤더: `수시 모니터링 17시 00분 기준(재경위)`
  - 시각은 **반올림 없이 입력값 그대로**. 담당자가 텍스트 상태에서 직접 고친다
  - 사안명은 화면에서 직접 입력 (기본값 = 사안 이름)
- 복사 / txt / 엑셀. **발송은 텔레그램만, 확정본에서 담당자가 누를 때만** 한다 —
  수시엔 회차 마감이 없어 자동발송이 없고, 이메일로도 보내지 않는다(받는 사람 화면의
  수시 칸은 텔레그램 쪽에만 있다). 확인창에서 이번 발송만 뺄 사람을 고를 수 있고,
  두 번째 발송부터 제목에 「(수정)」. 기록은 카드의 `send_count`·`sent_at`·`send_log`와
  발송 기록 화면(`/send-log`)에 남는다.
  ("발송 없음"은 설계 당시의 판단이었다 — 담당자가 텍스트를 복사해 붙여 넣는 것으로
  충분하다고 봤으나, 정기와 같은 명단에 같은 방식으로 보내는 편이 실제로 더 짧았다.)
- 메모 줄은 없다(§3.3 — 카드에 메모 칸을 두지 않는다).
- 다운로드 파일명: **`{날짜}_수시모니터링_{사안명}.txt`**(원본은 끝에 `_원본`) —
  정기와 같은 `날짜_종류_구분` 규칙으로 통일했다(PRD 기능11 규칙7).
  - 사안명의 `/ : * ? " < > |` 치환 필요 ([storage.py:13](app/storage.py:13)과 같은 처리)

### 6.9 수집 실패

정기(`MAX_RETRIES` 3회 × 5분)와 **다르게** 처리한다. 담당자가 화면 앞에 앉아 있으므로
15분을 기다리게 하면 안 된다.

- **자동 재시도 0회**
- 즉시 에러 표시 — 다시 해 보는 길은 편집 창의 「다시 수집」·헤더의 「지금까지 다시 수집」이라
  배너에 [다시 시도] 버튼을 따로 만들지 않았다
- 이미 모아둔 기사는 그대로 둔다(수집이 실패하면 카드를 아예 안 건드린다). 다만 그 사실을
  **배너에 적지는 않는다** — 목록이 그대로 보이는 화면에서 "그대로 있습니다"는 없는 걱정을
  만든다(같은 사실을 두 번 말하지 않는다, §6.4a)

```
⚠️ 수집에 실패했어요 — '국정감사' 검색 중 요청 한도 초과(429).
```

### 6.10 API 동시 호출

`ThreadPoolExecutor(max_workers=8)`가 [호출할 때마다 새로 생성](app/naver_api.py:513)되므로,
정기와 수시가 겹치면 8+8=16 동시가 된다. [naver_api.py:25](app/naver_api.py:25)의 실측:

| 동시 호출 | 429 발생률 |
|---|---|
| 5 | 0% |
| 10 | 5% |
| 15 | **20%** |
| 20 | 86% |

**대응: 모듈 레벨 전역 세마포어 하나** (`threading.Semaphore(8)`), **HTTP 요청 단위**로 획득.
키워드 단위로 걸면 페이지네이션(최대 10회) 동안 슬롯을 붙들어 다른 검색을 막는다.

단, **겹칠 확률 자체는 낮다.** 자동으로 도는 네이버 호출은 스케줄러의 회차 수집 하나뿐이고
(초안·실시간은 전부 화면 요청 시점), 하루 4회 × 7초 = 28초에 불과하다.
따라서 이건 성능 최적화가 아니라 **만에 하나의 429 폭주 방지**이며,
이것 때문에 설계를 복잡하게 만들지 않는다. 대기 안내 UI도 만들지 않는다.

### 6.11 보관·삭제

- **[수정: 2026-08-18] 1년 보관 — 원래는 기간 제한 없음이었다.** 정기와 정책이 다르면
  담당자가 "어느 쪽이 언제까지 남는지"를 따로 기억해야 한다는 이유로 맞췄다
  (`ADHOC_RETENTION_DAYS`, `app/adhoc/card.py` `delete_expired_cards()` — 배경은
  HISTORY.md "수시(카드) 보관도 무기한 → 1년" 참고). 판단 기준은 카드의 `created_at`.
  `app/scheduler.py`(정기 코드)는 여전히 `app.adhoc`를 import하지 않는다 — `main.py`가
  `cleanup_adhoc_cards` 콜백으로 주입한다(단방향 의존 규칙, §4 유지)
- 삭제는 `data/adhoc/trash/`로 이동만. 실제 삭제는 사람이
- 라벨 붙은 기사는 걱정 없다 — `app/labels.py`가 라벨을 붙이는 순간 기사 정보를 이미
  별도 저장소로 복사해 카드 생애주기와 독립시켜 뒀다(카드가 trash로 가도 라벨은 남는다)
- **줄 끝 🗑에는 확인창이 없다** — 지운 자리에 곧바로 「삭제함 · 되살리기」 줄이 남기
  때문이다(둘은 세트다). 확인창은 여러 개를 한 번에 지우는 곳(보관함의 [선택 삭제],
  날짜·사안 줄의 🗑)에만 둔다. 카드 화면의 삭제(탭 ×·「삭제」)도 같은 trash로 가고,
  지운 직후 도착하는 보관함 그 자리에 되살리기 줄이 펼쳐진 채 뜬다
- 동시 카드 개수 상한 없음(1년 보관 기준 실측: 정기 1,460파일 22.8MB 상당 규모에서도
  카드 목록 렌더는 문제없음 — HISTORY.md "회차 메타 인덱스 도입" 참고)

### 6.12 쓰기 동시성

`atomic_write_text` + **카드 id별 잠금**. 카드 파일을 읽어 고쳐 다시 쓰는 방식이라
여러 기사를 한꺼번에 숨기는 조작에서 [정기가 겪었던 경합](app/curation.py:13)이 재현될 수 있다.
카드별 잠금이면 서로 다른 카드끼리는 막히지 않는다.

---

### 6.13 모음 카드 — 여러 원본에서 골라 한 곳으로

> **이 절의 「모음 카드」가 지금 화면의 「확정본」이다**(내부 이름 `kind: "bundle"`은 그대로).
> 아래 설계는 뼈대가 그대로 살아 있고, 화면 규칙만 여러 줄 바뀌었다 — 바뀐 자리마다
> 「바뀜」으로 적어 뒀다.

> **상태: 구현됨(2026-09-02).** 아래는 확정 스펙이다.

> **[수정: 2026-09-03] 화면 이름이 제자리를 찾았다.** `kind:"collect"` 카드가 화면의
> **수집 원본**, `kind:"bundle"` 카드가 화면의 **수집 확정본**이고, `/adhoc/new`는
> 화면이 아니라 **새 수집** 폼이다(3단에 자리가 없다). 이 문서와 코드의 내부 용어
> (`bundle`·`send-to-bundle`·`모음`)는 한 글자도 안 바꿨다 — CLAUDE.md "화면 용어 —
> 내부 용어와 분리한다"의 그 경계다. 함께 정한 것 둘: (1) **원본은 소제목 안에서
> 시간순, 확정본은 언론사 우선순위순** — 원본은 표시할 때(`_article_groups`) 다시
> 세우고, 확정본은 **들어올 때**(`send_to_bundle`) 같은 소제목 안 자기 자리에 끼워
> 넣는다(매 렌더링 재정렬은 담당자의 ↑↓를 지우므로 금지). (2) 그래서 **원본에는 기사
> ↑↓가 없다**(소제목 ▲▼는 그대로). 배경은 HISTORY.md "수시 3단 이름이 한 칸씩 밀려
> 있던 문제" 참고.

**푸는 문제.** 카드 하나는 검색 조건 하나다. 그래서 `종부세` 카드와 `재정경제부`
카드에서 **각각 필요한 기사만 골라 보고서 하나로 묶는 길이 없다.** 담당자가 실제로
부딪힌 상황이 그것이다 — 두 카드 다 건수는 너무 많은데, 정작 필요한 기사는 양쪽에
흩어져 있다.

근본 원인은 **수집 단위와 산출물 단위를 1:1로 묶어둔 것**이다. 검색 조건은 넓을수록
좋고(놓치면 안 되니까) 보고서는 좁을수록 좋은데(읽는 사람이 있으니까), 그 둘이 한
몸이라 서로를 벌준다. "종부세가 너무 많이 걸린다"가 **문제로 느껴지는 것 자체가
증상**이다 — 넓게 걸리는 건 원본의 미덕인데, 원본이 곧 보고서라 벌을 받는다.

#### 해법 — 모음 카드는 "검색을 안 하는 카드"다

새 저장 단위를 세우지 않는다. **카드의 두 번째 종류**로 만든다.

```
 [종부세 원본]  ──┐
 (검색 조건 A)    │  담당자가 고른 기사만 복사
                  ├──▶  [모음 카드 "9/2 예결위 보고"]
 [재경부 원본]  ──┘      검색 조건 없음 · 사람이 이름 붙임
 (검색 조건 B)            소제목·순서·복사·엑셀은 기존 카드와 100% 동일
```

이렇게 두면 §3.3 카드 스키마에 이미 있는 것들(`articles` / `group_order` /
`custom_groups` / `group_summaries`)이 그대로 일한다. **공짜로 딸려오는 것**: 소제목
배정(`classify_card`), 소제목 ▲▼, 기사 ↑↓, 일괄이동, 숨기기, 소제목 통째 숨기기,
좌하단 되돌리기, 복사·txt·엑셀(`build_adhoc_plain_text` / `build_adhoc_excel_rows`),
수시 보관함 노출, 라벨. **§3의 "카드 하나 = 파일 하나 자기완결" 규칙도 안 깨진다.**

받는 쪽 배선도 절반은 이미 깔려 있다 — 스키마의 `added_by: "manual"`과, 그 값을 시간창
검사에서 면제하는 `article_out_of_window`(§6.4)다. 지금 이 값을 **만드는 코드가 하나도
없어서 놀고 있는 자리**다(폐기된 §6.7의 흔적).

#### 기각한 대안 셋

| 대안 | 기각 이유 |
|---|---|
| **라벨 보관함을 키운다** (소제목·순서·복사를 붙여 라벨명 = 보고서명) | 담당자 판단 — 라벨은 정기·수시를 가로지르는 **별도 제3의 기능**이라 여기 엮지 않는다. 설계상으로도 라벨은 한 기사에 여러 축이 겹쳐 붙는 다축 태그(AND 검색)이고 모음은 순서가 본질인 문서라, 한 화면에 얹으면 라벨 쪽 뜻이 흐려진다 |
| **확정본을 카드 밖의 새 저장 단위로 세운다** | §3 자기완결 규칙이 깨지고 저장·되돌리기·보관함·엑셀·소제목이 **두 벌**이 된다. 그러고 얻는 게 없다 — 모음이 필요로 하는 것이 카드 스키마에 이미 전부 있다 |
| **검색어를 한 카드에 합친다** (`종부세` OR `재정경제부`) | 지금도 되지만, 두 조건을 항상 같이 봐야 하고 한쪽만 좁힐 수 없다. 건수 문제는 오히려 심해진다 |

#### 스키마 — `kind` 한 필드만 는다

```json
{
  "kind": "bundle",          // "collect"(기본, 기존 카드 전부) | "bundle"
  "keywords": [],            // bundle은 항상 빈 배열
  "must_keywords": [],
  "window": null,            // 시간창 없음 — 검색을 안 하므로
  "collect_log": []          // 항상 빈 배열 — 수집한 적이 없다
}
```

`kind`가 없는 옛 카드는 `"collect"`로 읽는다(마이그레이션 없음). `bundle`은
`_validate_keywords` / `_validate_window`를 타지 않는 별도 생성자
(`new_bundle_card`)로만 만들어진다.

기사 쪽은 기존 필드 그대로, `added_by: "manual"` + `sent_from: {card_id, report_title}`
한 줄만 더 붙인다(어느 원본에서 왔는지 화면 표시용 — 원본 카드가 1년 뒤 사라져도 이
값은 모음 카드 파일 안에 그대로 남는다).

#### 규칙

1. **복사다, 이동이 아니다** (담당자 결정). 보낸 뒤에도 원본 카드에 그대로 남는다 —
   원본은 "검색 조건의 결과"라 손대면 조건과 어긋나고, 어차피 재수집하면 다시
   딸려온다(§6.4 교체 규칙). **소제목의 건수도 안 변하고 구멍도 안 난다**: 12건짜리
   소제목에서 3건을 보내면 그 소제목은 여전히 12건이고, 3건에 표시만 붙는다(아래
   "보낸 기사 표시"). 소제목을 통째로 보내도 원본에선 12건이 그대로 남는다.
   그 대가로 **원본이 줄어들지 않으므로 필터가 반드시 함께 필요하다**(아래 참고).
2. **자정 잠금은 원본과 똑같이 건다** (담당자 결정). 모음 카드의 `collect_date`가
   오늘이 아니면 받기·편집이 잠긴다(§6.3과 같은 배너). 검색을 안 하니 §6.3의 원래
   근거("시간창이 오늘로 해석된다")는 안 걸리지만, **수시 전체를 "하루 단위"로 일관되게
   유지하는 쪽**을 골랐다. 결과적으로 어제 원본에서 오늘 모음으로는 못 보낸다.
3. **조건 필터 면제.** `article_in_condition`이 `added_by == "manual"`이면 무조건 통과
   시킨다. 지금은 `matched_keywords`를 `card["keywords"]`와 대조하는데, 모음 카드는
   `keywords`가 비어 있어 **보내는 즉시 전부 조건 밖으로 떨어진다** — 이 한 줄이 없으면
   기능이 통째로 안 돈다. `article_out_of_window`는 이미 면제하고 있다.
4. **같은 URL은 1건.** 이미 있는 URL을 또 보내면 조용히 건너뛴다(덮어쓰지 않는다 —
   먼저 들어온 것의 소제목 배정·순서를 지킨다).
5. **되돌리기는 받는 카드 스택에만.** 복사라 보내는 쪽 카드는 안 변한다(원본 행의
   `→ 보냄` 표시는 대상 카드를 읽어 매번 다시 계산 — 저장하지 않는다. §6.4의
   `matched_keywords`·`article_in_condition`과 같은 원칙).
6. **정기와의 통로는 여전히 0개.** 이 기능은 수시 안에서만 돈다(수시 원본 → 수시 모음).
   §4 단방향 의존 규칙(`app/adhoc/* → app/*`)을 한 줄도 건드리지 않는다.
7. **소제목 배정은 기존 그대로.** 낱개로 보낸 기사는 `group: null`(미분류)로 들어오고,
   모음 카드에서 「소제목 배정」 버튼을 눌러야 묶인다(§6.5 그대로 — 자동 호출 0회).
   **예외는 소제목 통째 보내기 하나** — 그때는 이름을 들고 간다(아래 참고).
8. **원본이 변해도 모음은 안 흔들린다.** 원본에서 그 기사를 숨기든, 검색어를 좁혀
   조건 밖으로 밀어내든, 카드가 1년 뒤 삭제되든 모음은 그대로다. **이것이 이 기능의
   가장 큰 실질 이득이다** — 지금은 검색어를 좁히는 순간 그 기사의 소제목 배정까지
   화면에서 통째로 사라지는데(§6.4), 모음에 보낸 것은 그 위험 밖에 있다.

#### 보낸 기사 표시 — 「띠 + 흐리기 + 칩」 세 겹 [담당자 결정: 2026-09-02]

> **바뀜**: 아래 세 겹은 **옛 원본(로데이터가 아닌 카드)에만** 남았다. 지금 원본에서는
> 행의 동작 버튼 자리가 그대로 **`✓ 보냄`**(툴팁에 어느 확정본인지)으로 바뀌고 **내용만
> 흐려진다** — 왼쪽 띠도, 별도 칩도 없다. 버튼 자리가 이미 그 행의 상태를 말하므로 신호를
> 셋이나 겹칠 필요가 없었고, 같은 자리에서 **다시 누르면 보냄 취소**가 되어야 해서
> (아래 규칙 9) 표시가 곧 버튼이어야 했다. 아래 「흐리기는 내용에만」·「배경색은 안 건드린다」·
> 「새 색을 안 만든다」와 두 기각안(아래로 내리기·음영만)은 지금도 그대로 유효하다.

| 겹 | 언제 일하나 | 값 |
|---|---|---|
| **왼쪽 3px 청록 띠** | 페이지를 훑을 때 — 왼쪽 가장자리만 따라가도 처리한 곳이 잡힌다 | `COLOR_ADHOC_TEXT`, 정기 `.ai-mark`와 같은 패턴 |
| **내용 흐리기** | 행에 눈이 닿을 때. `:hover`면 다시 또렷 | `opacity: .55` — 📷 사진 추정 기사가 쓰는 바로 그 값·같은 뜻 |
| **`✓ …로 보냄` 칩** | 읽을 때 — **어느 모음**으로 갔는지까지 | 청록 칩 |

- **흐리기는 행이 아니라 「내용」(`.a-title` / `.a-bot`)에만 건다.** 행 전체에 걸면 띠와
  칩까지 같이 흐려져, 정작 눈에 걸려야 할 두 신호가 약해진다. **신호는 100%, 내용만 55%.**
- **배경색은 절대 안 건드린다.** 이 행에는 이미 hover 회색 · 체크 파랑 · `.just-moved`
  세이지가 붙는다. 네 번째 배경색을 넣으면 겹칠 때 무엇이 이기는지 알 수 없게 된다.
- **새 색을 하나도 안 만든다** — 셋 다 이미 있는 값이다.
- 그 행의 `확정본으로 ▾`는 `✓ 보냄`으로 바뀐다(잠기는 게 아니라 취소 버튼이 된다).

**기각한 「아래로 내리기」(C안).** 보낸 기사를 목록 끝으로 모으는 안. 깔끔해 보이지만
대가가 화면 밖에 있다: (1) **담당자의 ↑↓ 순서가 깨진다** — CLAUDE.md가 명시적으로
금지하고 실제로 사고가 났던 지점이다(HISTORY.md "담당자 > AI: 정렬·분류 우선순위 3종"),
(2) 규칙 9의 되돌림이 생기면 **원래 자리를 기억해야 해** 저장할 것이 는다,
(3) **아래 필터와 하는 일이 겹친다** — 필터를 켜면 A·B·C가 완전히 같아진다는 것이
C가 불필요하다는 증거다. 「음영만」(A안)도 기각: 43건짜리 실제 원본에서는 훑을 때 안
잡히고, 📷 사진 추정 기사와 같은 `opacity`라 **왜 흐린지 구분되지 않는다.**

#### ☑ 아직 안 보낸 기사만 — 필터가 반드시 함께 간다

규칙 1(복사)의 직접적 대가다. 43건에서 12건을 보내도 화면은 여전히 43건이라,
**두 번째 원본부터 골라내는 작업이 급격히 힘들어진다.** 원본 카드 툴바에 체크박스 하나:

> ☐ **남은 기사만 보기 (보냄 N · 숨김 M)** — 보냄·숨김 표시가 붙은 행을 감춘다
>
> (문구는 「아직 안 보낸 기사만」에서 바뀌었다 — 숨김까지 함께 빠지는데 이름이 보냄만
> 말하면 건수가 안 맞아 보인다. 괄호 안에 무엇이 빠졌는지 숫자로 적는다.)

정기 전체 기사 화면의 「아직 처리 안 한 기사만」과 **같은 문제·같은 해법**이고, 그 화면의
규칙(넷 다 「~만 보기」 방향, 툴팁에 대상을 빠짐없이 나열)을 그대로 따른다.
클라이언트 필터라 서버 왕복이 없다.

#### 되돌리는 길 — 「모음에서 숨기기」가 곧 「보내기 취소」다

9. **모음에서 🗑 숨기면 원본의 `보냄`이 풀린다.** `보냄` 판정에서 **숨긴 기사를 뺀다**
   (판정은 저장하지 않고 대상 카드를 읽어 매번 다시 계산하므로 — 규칙 5 — 자연히 따라온다).
   확정본에는 완전 삭제가 없고 숨기기뿐이라, 이 규칙이 없으면 실수로 보낸 기사를 되돌릴
   길이 막다른 골목이 된다.
   > **바뀜**: 「별도의 「보내기 취소」 동작을 만들지 않는다」는 뒤집혔다. 확정본 쪽에서
   > 숨기는 길만 두면 **원본에서 잘못 보낸 것을 원본에서 되돌릴 수 없다** — 담당자는 방금
   > 누른 그 자리에서 무르려 한다. 그래서 원본의 **`✓ 보냄`을 다시 누르면 보냄 취소**다
   > (확인창 없음, 「🗑 숨김 → ↩ 되살리기」와 같은 문법). 확정본의 사본을 숨기고 원본
   > 표시를 되돌리며, 되돌리기는 **두 카드 스택 모두**에 쌓인다.
10. **소제목 헤더의 📥를 다시 누르면 남은 것만 간다.** 다 보낸 뒤에는 대상이 0건이라
   **버튼을 아예 안 그린다**(잠긴 채 `전체 보냄`으로 두지 않는다 — 누를 수 없는 버튼을
   남기느니 자리를 비우는 쪽이 조용하다). 규칙 4 "같은 URL은 1건"의 귀결이다.

#### 소제목 단위로 통째 보내기 [담당자 결정: 2026-09-02]

수시 카드에는 「소제목 배정」 버튼이 **이미 있다**(§6.5). 그러니 새로 만들 것은 분류가
아니라 **소제목 단위 보내기**다 — 43건에서 체크박스를 하나씩 찍는 대신
`<세율 인하 논의> 12건`을 통째로 던지는 것. 소제목 헤더 아이콘 묶음(`📋 🗑 ▲▼`)에
📥 하나를 더하고, 2026-09-02의 「소제목 통째 숨기기」와 **완전히 같은 패턴**으로 구현한다
(`card_lock` 한 번에 처리, 되돌리기 자연히 1스텝).

**소제목 이름이 모음으로 따라가는가 — 보내는 방식에 따라 갈린다** (담당자 결정):

| 보내는 방식 | 모음에 도착했을 때 |
|---|---|
| 낱개로 보냄 | `group: null`(미분류) — 규칙 7 그대로 |
| **소제목 통째로 보냄** | **그 이름을 들고 간다.** 모음에 같은 이름이 없으면 커스텀 소제목으로 만들어 담는다 |

근거는 **담당자의 의도가 이미 그 선택 안에 들어 있다**는 것이다. 묶음을 통째로 집었다는
건 "이 묶음이 하나의 덩어리로 쓸 만하다"고 판단한 것이므로 이름을 버릴 이유가 없고,
낱개로 집은 건 "이것 하나만 필요하다"라 원래 소속이 의미가 없다. 부수 효과로 모음에서
「소제목 배정」을 아예 안 눌러도 되는 경우가 생겨 **LLM 호출이 한 번 절약된다.**

**자동 분류는 하지 않는다.** 수시는 자동 호출 0회가 규칙이고, 원본 카드는 재수집·검색어
편집으로 목록이 계속 바뀌어 **검색어 한 글자 고칠 때마다 비용이 나간다.** 버튼 유지.

#### 화면

- **만들기**: ~~`/adhoc/new`에 `빈 모음 만들기` 경로 하나~~ → **확정본은 처음 보낼 때
  저절로 생긴다.** 경로(`?kind=bundle`)는 남아 있지만 그리로 가는 링크가 화면에 없다.
  「먼저 빈 확정본을 만들고 그다음 보낸다」는 두 걸음이었고, 담당자가 하려는 일은
  언제나 「이 기사를 보고서로 보낸다」 한 걸음이었다. 지금은 `[확정본으로]`가 같은 날·
  같은 사안·같은 기준 시각·같은 원본 판의 확정본을 **서버가 찾아 주거나 새로 만든다**.
- **보내기**: **원본**(`/adhoc/card`)의 기사 행과 선택 바에 `확정본으로 ▾` — 메뉴는 칸
  머리로 「이 원본의 확정본」과 「오늘 다른 확정본」을 가르고(목록에 오늘 만든 확정본이
  전부 들어와 이름만으로는 구분이 안 된다), 맨 아래가 `+ 새 확정본 만들어 보내기`다.
  **확정본 → 확정본으로 보내는 길은 없다**(서버도 거부) — 확정본끼리는 「옮기기」다.
  보낸 행은 위 「보낸 기사 표시」대로 `✓ 보냄`이 되고, 툴바에 ☐ `남은 기사만 보기`가 선다.
- **확정본 화면**: `render_card_page`를 그대로 쓰되 **검색어 편집 패널 · 다시
  불러오기 · 수집 이력 칩을 안 그린다**(전부 검색 조건이 있어야 뜻이 서는 것들).
  대신 상단 메타 칩이 `←] 종부세 3건`처럼 **어느 원본에서 몇 건 왔는지**를 보여주고,
  **그 칩 하나하나가 해당 원본 카드로 가는 링크**다(아이콘은 원본의 보내기 `[→`와 짝이
  되는 `←]`이고, 원본이 하나뿐이면 같은 숫자를 되풀이하는 `전체 N건` 칩은 안 그린다) — 모음에서
  "이거 어디서 왔더라, 더 없나"가 생기는 유일한 자리이고, 칩에 이미 원본 이름이
  적혀 있으므로 새 UI를 만들 게 아니라 링크만 걸면 된다.
  - **그 자리에 「모음 관리」 같은 대체 패널을 넣지 않는다.** 검색어 편집 패널이
    존재하는 이유는 "조건을 고치면 목록이 바뀐다" 하나뿐인데 모음엔 조건이 없다.
    넣을 후보를 전부 세어보면 이름 바꾸기(헤더 인라인 입력칸) · 출처별 건수(📥 칩) ·
    기사 빼기(🗑 숨기기) · 삭제(`delete_card`, 원본 카드와 같은 자리)가 **이미 다른
    자리에서 되고**, 남는 건 위의 원본 링크 하나뿐이다. 빈 칸을 못 견뎌 만든 패널은
    담당자를 열게 만든 다음 실망시킨다. 출처가 대여섯 개로 늘어 칩 줄이 넘치면 그때
    다시 판단한다.
- **좌하단 「목록 밖 기사」 팝오버는 그린다 — 이름만 「숨긴 기사」로 바뀐다.**
  `hidden_articles`는 `숨김 or 조건 밖`으로 모이는데(§6.4), 모음은 조건 밖이 구조적으로
  0이라 **자연히 숨긴 기사만 남는다** — 목록을 만드는 코드는 손댈 게 없다. 바꿀 것은
  둘뿐: (1) 제목 `목록 밖 기사` → `숨긴 기사`, (2) 그 아래 안내 문구
  `"조건 때문에 빠진 기사는 검색어를 되돌리면 저절로 돌아와요"` 줄을 **통째로 뺀다** —
  모음엔 되돌릴 조건이 없어 그대로 두면 거짓말이 된다. 이 팝오버를 안 그리면 🗑 누른
  기사를 복구할 길이 사라진다(초안 설계에 그렇게 적혀 있던 것을 고쳤다).
- **수시 보관함**: 날짜 → 회차 2단(§5.4)이고 **확정본만 쌓인다**(원본은 새 수집의
  「지난 수집」에서 찾는다). 회차 줄은 `HH:MM 기준 · 사안명`이라 따로 칩이 필요 없고,
  청록 `확정본` 칩은 **시각이 없는 옛 확정본에만** 붙는다.
- **출력 헤더**: ~~시각을 안 붙인다~~ → **붙인다.** `수시 모니터링 13시 05분 기준(사안명)`.
  확정본에 수집 시간창은 없지만, 담긴 기사마다 「어느 기준 시각에 보냈는가」가 적혀 있어
  그중 가장 늦은 것이 곧 이 보고서가 말하는 시점이다(숨긴 기사는 빼고 센다). 보고서를
  받는 사람에게 시점 없는 머리줄은 쓸 수 없다.

#### 안 만드는 것

- **확정본 → 확정본 보내기**(복사해서 합치기). 대신 **「옮기기」는 만들었다** — 오늘 만든
  **다른 사안의 확정본으로 기사를 옮긴다**(보낸 쪽에선 숨김, 받는 쪽엔 미분류·언론사 순위
  자리로). 보내기(복사)와 달리 옮기기는 사본이 늘지 않아 「이 기사가 지금 어느 보고서에
  있나」가 하나로 유지된다. 규칙은 CLAUDE.md "수시 모니터링".
- **모음 카드의 재수집·검색어 편집.** 검색을 안 하는 게 정의라 이 둘은 존재할 수 없다.
- **원본에서 보낸 기사를 원본 쪽에서 지우기.** 위 규칙 1(복사)의 당연한 귀결.
- **자동 반영.** 원본에서 기사가 바뀌거나 사라져도 모음은 안 따라간다 — 담당자가 손으로
  보낸 순간 고정된다. 그게 이 기능의 존재 이유다.

### 6.14 수시 4단 흐름 — 원본은 로데이터, 확정본은 저절로

§6.13을 한 걸음 더 민다. **원본(`raw: true`)은 보고서를 다듬는 도구를 안 든다** — 소제목
배정·기사 ↑↓·라벨이 없고, 조건에 걸린 기사를 **한 목록 최신순**으로 보여준다.
[확정본으로 ▾]와 ⋯(복사 포함)·🗑만 행에 붙는다.

> **다만 아래 셋은 설계 직후에 되돌렸다** — 원본에도 **복사·텍스트·엑셀**이 있고(파일명 끝에
> `_원본`, 「🗑 숨김」 표시 기사는 텍스트에서 뺀다), **사진 추정 모아 보기**도 있다(로데이터를
> 훑을 때야말로 사진기사를 한 번에 걷어내고 싶다). **숨기기**는 「진짜 숨기기」 대신
> **「🗑 숨김」 표시**다 — 원본에서 기사를 빼면 「이 조건으로 모은 전부」라는 기록이 깨지므로,
> 전체 기사 화면처럼 흐리게 + 취소선만 두고 다시 누르면 되돌린다(`raw_marks`, §3.3). **확정본은 따로 만들지 않는다** — 원본의 [확정본으로]가 같은 날·
같은 사안의 확정본으로 보내고, 없으면 그 순간 만든다(다른 확정본은 ▾). 확정본 머리줄은 보낸
순간의 원본 시각 중 가장 늦은 것을 붙인다. 수시 보관함엔 확정본만 쌓이고(옛 원본은 그대로),
원본은 새 수집의 「지난 수집」(1년치, 날짜·사안명·검색어, 누르면 폼 채움)에서 찾는다.
위 §6.13의 "입구는 원본 부제 링크", "헤더에 시각 없음", "원본은 소제목 안에서 시간순", "보냄 칩"은
옛 원본에만 해당한다. **원본 하나 = 검색 조건 하나**이고, 같은 날·같은 사안에 원본이 여럿이면
`#2`·`#3`으로 가른다(PRD 기능9 규칙13). 규칙 전문은 CLAUDE.md "수시 모니터링" 절 첫 항목, 배경은 HISTORY.md
"수시 4단 흐름".

## 7. 1차 범위에서 제외

| 항목 | 이유 |
|---|---|
| ~~**↩ 되돌리기**~~ | ~~`app/undo.py`가 전역 스냅샷 구조라 카드에 맞추려면 통째로 다시 짜야 함~~ → **구현됨.** 통째로 다시 짤 필요가 없었다 — 카드 하나 = 파일 하나라 **카드 JSON 전체를 스냅샷으로 뜨면** 그게 곧 되돌리기다(`app/adhoc/undo.py`, 카드별 10단계). 두 카드가 함께 바뀌는 동작(다른 사안으로 옮기기·보냄 취소)은 `attach_link`로 잇는다 |
| ~~**⋯ 더보기의 원문 재수집 / 제목 수정**~~ | ~~`summary_overrides.json`이 전역이라 정기 화면까지 영향~~ → **[2026-09-02] 구현됨.** 전역인 게 문제가 아니라 의도였다 — 담기는 값이 "이 URL의 진짜 제목"이라는 사실 하나라 두 흐름이 공유하는 게 맞고, `summary_overrides.json`은 `labels.json`과 함께 원래 그러라고 `app/` 최상위에 둔 저장소다. 화면에 입히는 지점은 `app/adhoc/renderer.py` `_article_groups` 한 곳(HISTORY.md "수시 기사 행을 정기와 같은 규격으로") |
| **워드클라우드 반영** | 임시 검색어가 당일 워드클라우드를 오염시킴 (`home.html` 미포함) |
| **history.html 편입** | 자기 아카이브 화면이 따로 있음 |
| **카드 → 정기로 기사 보내기** | ~~담아둠 경유로 충분~~ → 그 담아둠 경유(📌)도 2026-09-02에 없앴다(§6.7). **지금 정기로 가는 통로는 0개**이고, 수시에서 본 기사를 정기에 넣고 싶으면 검색어를 고친다 |
| **사안 병합** | "재경위"와 "기재위"를 따로 만든 경우 — 나중에 |
| **1000건 한도 경고** | 당일 좁은 검색어라 거의 안 걸림. 우선순위 낮게 |
| ~~**전용 발송 수신자**~~ | ~~발송 기능 자체가 없음~~ → **발송이 생겼고(§6.8), 수신자 칸도 생겼다.** 텔레그램 받는 사람마다 `수시 [기사\|요약]` 칸이 정기 칸과 따로 있다. 다만 명단 자체는 정기와 같은 한 곳(`/telegram`)이다 — 사람은 같고 받을 것만 다르다 |

---

## 8. 구현 전 준비물 — 넷 다 끝났다

- [x] `app/icons.py`에 **원문 열기 아이콘**(`external`) 추가
- [x] `naver_api.py`에 전역 세마포어 (§6.10)
- [x] LLM 분류 프롬프트의 수시용 분기 (§6.5)
- [x] `_last_names` 저장/복원 헬퍼 (§6.5 — 메모리뿐 아니라 캐시 파일까지 되돌린다)

## 9. 화면 시안

처음 그린 `ADHOC_MOCKUP.html`(홈 진입점 / 새 수집 / 입력 차단·실패 / 카드 정리 / 지난 사안)은
화면이 4단 흐름으로 바뀌면서 버렸다. 지금 화면의 시안은 규칙 옆에 하나씩 붙어 있다 —
[흐름 나누기](mockups/ADHOC_FLOW_SPLIT_MOCKUP.html) ·
[새 수집](mockups/ADHOC_NEW_COMPACT_MOCKUP.html) ·
[수집 시간 드롭다운](mockups/ADHOC_TIME_DROPDOWN_MOCKUP.html) ·
[지금까지 다시 수집](mockups/ADHOC_RELOAD_NOW_MOCKUP.html) ·
[원본 정리](mockups/ADHOC_RAW_CLEANUP_MOCKUP.html) ·
[보낸 기사 표시](mockups/ADHOC_SENT_MARK_MOCKUP.html) ·
[확정본으로 메뉴](mockups/ADHOC_SEND_MENU_MOCKUP.html) ·
[옮기기](mockups/ADHOC_MOVE_DROPDOWN_MOCKUP.html) ·
[보기 순서](mockups/ADHOC_BUNDLE_VIEW_ORDER_MOCKUP.html) ·
[선택 바](mockups/ADHOC_BULK_BAR_MOCKUP.html) ·
[보관함](mockups/ADHOC_ARCHIVE_BY_DATE_MOCKUP.html).
