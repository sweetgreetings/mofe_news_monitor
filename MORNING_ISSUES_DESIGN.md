# MORNING_ISSUES_DESIGN.md — 실시간 우선 구조 · 🌅 모닝 쟁점

**한 줄**: 이 앱에서 검색어의 상위집합은 **실시간현황**인데, 화면과 홈 쟁점 카드가 둘 다 그걸 거꾸로 다루고 있다. 화면 문구(A)와 쟁점 카드 원천(B)을 순서대로 바로잡는다.

- 확정 후 유효 규칙은 [CLAUDE.md](CLAUDE.md)로, 배경·시행착오는 [HISTORY.md](HISTORY.md)로 옮긴다.
- [ADHOC_DESIGN.md](ADHOC_DESIGN.md)와 같은 성격의 **확정 스펙 원본**이다.
- 작업 순서: **A → B**, 그리고 A·B와 별개로 **C(결함 수정)**.

---

## 1. 배경

### 1.1 이 앱의 검색어 계층 (코드 확인)

```
실시간현황   enabled=True 인 모든 그룹                      ← 상위집합
   ⊇
정기 스크랩  enabled=True AND include_in_scrap=True 인 그룹   ← 부분집합
```

근거: `app/settings.py` `active_search_groups` — *"groups를 생략하면 settings["keyword_groups"] 전체를 쓴다(실시간 기사 현황). app.scraper.collect_run은 먼저 include_in_scrap이 켜진 그룹만 추려 groups로 넘긴다."*

수시 모니터링은 카드마다 자기 검색어를 따로 갖는 완전 별개 축이라 이 계층 밖이다.

### 1.2 직접 계기 (2026-08-27)

홈의 "오늘의 쟁점"에 `국회`(2위) · `1주택자`(3위) · `이를`(5위)가 떴다. 06:00 회차 소제목이 규칙빈도 폴백으로 저장돼 있었고, 카드가 그 이름을 그대로 순위에 올렸다.

```
2026-08-27_06-00.json → 국회 4 · 1주택자 3 · 기타 3 · 이를 2 · 한다 1 · 했다 1 · 부총리 1 · 강조 1
```

이건 §4(C)로 고친다. 그런데 이걸 파다 더 큰 게 나왔다.

### 1.3 진짜 문제 — 등록 검색어의 절반이 쟁점 카드에 안 보인다

홈 쟁점 카드는 정기 회차 파일만 읽는다. 정기는 `include_in_scrap` 그룹만 담는다. 그래서 **부분집합에서 뽑은 요약을 전체인 것처럼 보여주고 있었다.**

실측 (2026-08-27):

```
오늘 정기 회차가 걸린 검색어 → 전부 scrap=True 그룹뿐
  재정경제부 115 · 구윤철 65 · 재경부 33 · 재정경제기획위원회 28 · 예결위 24 · 기획재정부 11 …

실시간에만 있는 그룹(scrap=False) → 오늘 972건
  인플레이션 376 · 물가안정 251 · 세제 개편 72 · ISA 58 · 보유세 55
  IMF 55 · 초과세수 40 · 종부세 38 · 양도세 26 · 장특공제 1
```

등록된 5개 그룹 중 **2개(세제개편 · 추가 검색어)가 통째로 쟁점 카드에 못 올라온다.**

결정적으로 — **담당자가 정책 단어 추이(`/trend`)에서 지켜보겠다고 고른 단어가 `종부세`·`비거주`다.** 그게 정확히 빠지는 그룹에 있다. 지켜보겠다고 고른 단어가 쟁점 카드에선 구조적으로 안 보인다.

### 1.4 폐기한 전제 (기록)

설계 초기에 "정기 스크랩을 안 쓰는 사용자도 쟁점을 봐야 한다"를 1번 근거로 삼았으나 **틀렸다.**

`app/settings.py` `validate_schedule_groups`: *"그룹은 최소 1개, 그 중 최소 1개는 enabled여야 한다(전부 꺼두면 아무 회차도 실행되지 않으므로)"* — **이 앱에서 정기를 안 쓰는 사용자는 만들어질 수 없다.** 이 앱은 재정경제부 단일 사용자 로컬 전용이고([CLAUDE.md](CLAUDE.md) "What this app is"), 다중 사용자는 [MULTIUSER_ROLLOUT_CHECKLIST.md](MULTIUSER_ROLLOUT_CHECKLIST.md)가 스스로 "현재 구조에는 적용되지 않는 미래 문서"라고 못박고 있다.

없는 사용자를 위해 지금 설계를 늘리지 않는다. 이 문서의 근거는 §1.3(오늘 972건짜리 실측) 하나다.

---

## 2. Phase A — 검색어 화면이 계층을 바로 말하게 (0원) ✅ **완료 2026-08-27**

로직은 이미 맞다. 화면도 **틀린 말은 안 한다.** 문제는 읽는 순서와 단어가 인상을 뒤집는 것이다.

### 2.1 고칠 문구 4곳 (`app/settings_server.py`)

| 위치 | 지금 | 바꿀 방향 |
|---|---|---|
| 캡션 `:926` | `* 저장 후 다음 스크랩 회차부터 적용됩니다. (실시간 현황은 즉시 반영)` | 어순을 뒤집는다 — `* 실시간 현황에 즉시 반영됩니다. (정기 회차는 다음 회차부터)` |
| 체크박스 `:601` | `정기 스크랩(예정된 회차)에도 포함 — 꺼두면 실시간 기사 현황에만 나오고, "→ 스크랩"으로 직접 옮겨야 정식 반영됩니다.` | ① **"정식" 삭제** ② 없어진 버튼 이름 교정 |
| 안내문 `:929` | 실시간 → 정기 순으로 서술되나 "실시간엔 기본으로 들어간다"가 없음 | 기본값을 명시 |
| 에러 `:430` | `"정기 스크랩에도 포함"으로 켠 그룹이 최소 1개는…` | 라벨을 바꾸면 같이 맞춘다 |

**"정식"을 지우는 이유**: 실시간을 비공식·미완으로 규정하는 말이라, 실제 계층(실시간 ⊇ 정기)과 정반대 인상을 준다. 실시간현황은 임시 화면이 아니라 상위집합이다.

**버튼 이름 교정**: `:602`가 `"→ 스크랩"`을 안내하는데 그 버튼은 2026-08-11에 **`📌 담아두기`**로 바뀌었다([HISTORY.md](HISTORY.md) 참고). 화면에 없는 컨트롤을 안내하는 죽은 문구다. (`live_renderer.py:930,966`과 `settings_server.py:4651`의 같은 문자열은 주석이라 화면과 무관 — 같이 손볼지는 선택.)

### 2.2 그룹 카드 배지 (`_KEYWORD_GROUPS_SCRIPT`)

문구만으로는 계층이 한눈에 안 읽힌다. 각 그룹 카드에 배지를 단다:

```
[ 재정경제부 ]        🔴 실시간   💎 정기
[ 세제개편 ]          🔴 실시간
[ 추가 검색어 ]        🔴 실시간
```

- `🔴 실시간` — **항상 표시**(그룹이 `enabled`인 한). "기본은 실시간"을 글이 아니라 구조로 말한다.
- `💎 정기` — `include_in_scrap`이 켜졌을 때만.
- 색은 홈 흐름도가 이미 쓰는 실시간/확정본 계열을 그대로 재사용한다(새 색 만들지 않는다 — [CLAUDE.md](CLAUDE.md) "Design direction" 규칙 1).
- 그룹이 `enabled=False`면 배지 둘 다 흐리게 — 지금 칩이 흐려지는 것과 같은 처리.

이러면 화면을 훑는 것만으로 "실시간이 전부, 정기는 그중 일부"가 읽힌다.

### 2.3 Phase A 범위 밖

- 실시간현황 화면에 검색어 추가 진입점을 두는 것 — 동선까지 바꾸는 별건이다. A의 효과를 보고 판단한다.
- `include_in_scrap` 플래그 이름·저장 형식 변경 — 저장 키라 건드리면 마이그레이션이 따라온다. 지금 문제는 표현이지 구조가 아니다.

---

## 3. Phase B — 홈 쟁점 카드를 🌅 모닝 쟁점으로 교체

### 3.1 창 (window)

**당일 00:00:00 ~ 09:00:00 (KST), 고정.**

고정 창의 핵심 성질 — **창이 닫히면 기사 집합이 더 늘지 않는다.** 세 가지가 따라온다.

1. **하루 한 번만 계산하면 된다** — throttle·재계산·캐시 무효화가 통째로 불필요.
2. **언제 계산해도 결과가 같다** — 앱이 09시에 꺼져 있어 11시에 켜져도 동일.
3. **날마다 같은 잣대다** — 롤링 창(최근 N시간)은 경계가 매번 달라 비교가 원리적으로 불가능.

09:00을 고른 근거는 §5.1.

### 3.2 원천

**등록 검색어 그룹의 활성 키워드 전체**로 네이버 검색. 이게 이 설계의 핵심이다 — §1.3의 972건이 여기서 살아난다.

- `enabled=False` 그룹은 제외 (실시간현황과 같은 규칙).
- **`include_in_scrap`은 보지 않는다** — 상위집합에서 뽑는 것이 목적이다.
- **`live_cache`에 그 구간이 있으면 재사용**한다(실시간현황은 당일 0시부터 누적하므로, 담당자가 그 화면을 한 번이라도 열었으면 00~09가 이미 캐시에 있다 → 네이버 호출 0회). 없으면 그 구간만 직접 검색한다.
- 정기 회차 파일은 **한 개도 읽지 않는다.**

### 3.3 좁히기 (분류 입력)

정기 수집과 같은 순서를 그대로 쓴다:

```
sort_by_outlet_priority
  → deduplicate_by_title
  → exclude_photo_articles
  → exclude_personnel_articles
  → filter_hidden
  → PRIORITY_OUTLETS 13개 매체만
```

**`filter_hidden`을 적용하는 이유**: 숨김은 "보고서에서 뺀다"는 담당자의 명시적 판단이다(PRD 기능4 규칙5). 아침 요약은 보고서 성격이므로 담당자가 뺀 기사가 1위로 올라오면 안 된다. 라벨 보관함이 숨긴 기사를 남기는 것(= "나중에 본다")과는 목적이 다르다.

**`PRIORITY_OUTLETS` 13개로 자르는 이유**: 분류 상한이 150건인데 창 전체는 307건이라 그대로 못 넣는다(§5.2). "상위 150건 컷"은 경계가 임의적이라 날마다 다른 매체가 들쭉날쭉 들어간다. 매체 목록으로 자르면 정의가 또렷하고("주요 13개 매체가 다룬 것 = 쟁점") 재현 가능하다.

```
KBS · MBC · 조선일보 · 중앙일보 · 동아일보 · 한국경제 · 매일경제
서울경제 · 한국일보 · 머니투데이 · 이데일리 · 연합뉴스 · 뉴시스
```

### 3.4 분류

- **요약 없는 경량 경로**를 새로 만든다. 홈 카드는 쟁점 이름과 순위만 쓰고 요약을 안 쓴다 — 기존 분류는 소제목마다 3문장 요약을 받아오는데 그게 출력 토큰의 대부분이다.
  - `AI_RULES.md`에 전용 섹션 추가 + 스키마에서 `summary` 제거.
  - **기존 정기 분류 경로(`_SYSTEM_PROMPT`)는 손대지 않는다.**
- 소제목 상한 5개(`MAX_MORNING_ISSUES`). TOP5만 쓰므로 더 만들 이유가 없다.
- `"기타"`와 통신사 정형 일정 나열 기사(`app.filters.is_schedule_listing`)는 순위에서 제외 — 지금 `_issue_streams`가 하는 그대로.
- **`_refine_etc_bucket`(기타 재정리)은 부르지 않는다** — 어차피 기타를 순위에서 빼므로 재정리할 이유가 없고 회당 10~15원이 절약된다.
- **LLM 캐시 격리**: 호출 전후로 `llm_classifier._last_names`/`_last_round_id`를 저장·복원하고 `_persist_cache_locked()`까지 맞춘다. 수시 모니터링(`app/adhoc/classifier.py` `_classify_isolated`)이 이미 하는 그대로 — 안 하면 이 카드의 쟁점 이름이 정기 회차 분류에 "직전에 쓰던 소제목" 힌트로 새어든다(수시에서 실제로 났던 버그).
- **API 키가 없으면 카드를 안 그린다.** 규칙빈도 폴백을 쓰지 않는다 — 그게 정확히 `이를`·`한다`다.

### 3.5 저장

`data/morning_issues.json` — 하루 한 개.

```json
{
  "date": "2026-08-27",
  "window": {"start": "00:00", "end": "09:00"},
  "generated_at": "2026-08-27T09:00:12",
  "source": "live_cache",
  "input_count": 50,
  "issues": [
    {"name": "부동산 세제개편안 논란", "count": 6, "groups": ["세제개편"]}
  ]
}
```

- `date`가 오늘이 아니면 없는 것으로 친다(`live_cache.json`과 같은 자정 리셋 패턴).
- `count`는 저장하되 **화면에는 안 쓴다**(§3.6). 판단이 바뀌거나 디버깅할 때 필요하다.
- 실패하면 파일을 쓰지 않는다 — 다음 홈 방문 때 다시 시도한다.

### 3.6 표시

```
🌅 모닝 쟁점    00:00~09:00 · 8월 27일

1  부동산 세제개편안 논란       [세제개편]
2  CPTPP 가입 검토            [재정경제부]
3  대법관 인선 갈등
4  배우자 상속공제 확대         [세제개편]
5  전월세 안심신탁 사업
```

- **순위만, 건수 없음.** 창이 하나뿐이라 건수가 비교 대상을 못 갖는다. 있으면 시선만 뺏는다.
- 쟁점 이름은 **원본 그대로** — "📃 소제목 형식" 템플릿(`<{section}>`)을 적용하지 않는다(지금 오늘의 쟁점 카드와 동일).
- 검색어 그룹 칩은 `_dominant_tags`를 그대로 재사용(그 쟁점 기사의 **과반**이 걸린 그룹만). Phase A의 배지와 같은 어휘라 "이 쟁점이 어느 그룹에서 왔나"가 바로 읽힌다.
- **09시 전에는** `오늘 모닝 쟁점은 오전 9시에 나옵니다`. 빈 카드가 아니라 **약속**이라 "왜 비었지?"가 생기지 않는다.
- 쟁점이 0개면 `💤 오늘 아침 쟁점이 아직 없습니다`.

### 3.7 산출 시점

**스케줄러가 09:00 이후 첫 tick에서 백그라운드 스레드로 1회.** 담당자가 몇 시에 홈을 열든 이미 완성돼 있어 **로딩 0초**다.

- 스케줄러 루프는 절대 블로킹하지 않는다 — 별도 스레드에서 돌린다(정시성 원칙).
- 앱이 09시에 꺼져 있었으면 **켤 때 catch-up**한다. `app.breaking_alert_sender.catch_up_on_wake`와 같은 패턴. 창이 이미 닫혀 있으므로 늦게 만들어도 결과가 같다.
- 아직 없는 상태에서 홈을 열면 그 자리에서 백그라운드로 시작하고 카드는 "만드는 중"을 보여준다. 중복 시작은 모듈 전역 락으로 막는다 — `app.live_renderer._ensure_live_generation_running`과 같은 방식.
- **`generate_landing_page()` 안에서 직접 분류하지 않는다.** 이 함수는 홈 방문뿐 아니라 큐레이션 핸들러 36곳(`_regenerate_screens()`)에서도 불린다 — 여기에 분류를 넣으면 비용이 클릭 수에 비례한다. 홈은 **저장된 파일만 읽는다.**

### 3.8 기존 "오늘의 쟁점" 교체

**교체한다(병존 아님).**

| | 오늘의 쟁점 (제거) | 모닝 쟁점 (신규) |
|---|---|---|
| 원천 | 정기 회차 파일 (부분집합) | 실시간 검색 (상위집합) |
| 갱신 | 회차마다 | 하루 1회 고정 |
| 폴백 회차 영향 | 같이 망가짐 | 무관 |
| 비용 | 0원 | 하루 약 13원 |

`_issue_streams` / `_dominant_tags` / `_ISSUE_TOP_N`은 **재사용**한다(창 안에서도 이름 병합은 그대로 필요하다). 홈 카드의 렌더링부만 교체한다. 나머지 세 카드(오늘 건수 · 정책 단어 추이 · 흐름도)는 건드리지 않는다.

---

## 4. Phase C — 함께 고칠 결함 셋 (A·B와 독립) ✅ **완료 2026-08-27**

오늘 사고에서 드러난 실제 결함. **모두 정기 스크랩 쪽이고, A·B를 하든 안 하든 고쳐야 한다.**

### 4.1 `classification_degraded` 배선 복구 ✅

`collect_run`의 반환값이 이 필드를 빠뜨리고(`app/scraper.py:236`), 직후 `confirm_and_promote` → `confirm_run`의 `{**run, ...}`이 그 축소된 dict로 파일을 덮어써 필드가 통째로 날아간다.

```bash
python3 -c "import json,glob;print({k for f in glob.glob('data/articles/*.json') for k in json.load(open(f)) if k!='articles'})"
```
→ 81개 파일 **전부** `classification_degraded` 없음. `finalize_new_subheadings`도 마찬가지.

그 결과 지금까지 한 번도 동작하지 않은 것:
- 확정본의 `⚠️ AI 분류 실패 — 확인 후 직접 발송해주세요` 배너
- 분류 실패 시 **자동발송 차단**(`app/confirm_send.py:215`)
- "마감 후 자동 배정" 배지

`collect_run` 반환값에 두 필드를 포함시키면 끝난다. **우선순위 최상** — 오늘 06시 회차도 이것만 살아있었으면 발송 전에 담당자가 알았다.

### 4.2 빈 응답도 재시도 + 로깅 ✅

`classify_with_llm`의 재시도 루프(`app/llm_classifier.py:776`)는 **예외일 때만** 재시도한다. 오늘 06:00처럼 HTTP 200인데 응답에 text 블록이 없거나 `_parse_groups`가 빈 결과를 주면 그냥 `break`로 빠져나가 **남은 재시도 1회를 안 쓴다.** 게다가 로그를 한 줄도 안 남겨 사후 판별이 불가능하다(그래서 오늘 원인을 둘 중 어느 쪽인지 끝내 못 갈랐다).

- 빈 응답도 실패로 취급해 `continue`
- 그 지점에 `logger.warning`으로 `stop_reason`·content 블록 타입·응답 앞부분을 남긴다

### 4.3 폴백 회차 표시 ✅

§5.4의 판정(소제목 3개 이상 & 전부 한 어절 → 폴백)을 유틸로 만들어, 확정본·정기 보관함이 그 회차에 표시할 수 있게 한다. 저장된 데이터만 보므로 **소급 적용된다** — 오늘 06:00 회차도 즉시 걸린다.

**최신이 아닌 회차는 지금 재분류할 방법이 없다**(큐레이션 핸들러 8곳이 전부 `load_latest_run()` 하드코딩). 이 판정이 있으면 "폴백한 회차에만 복구 버튼을 연다"는 길이 열리지만, 그건 "정기 보관함은 읽기 전용"이라는 기존 원칙을 깨는 일이라 **이번 범위에는 넣지 않는다.**

---

## 5. 실측 근거

별도 표기가 없으면 2026-08-27 `data/live_cache.json`(당일 누적 1,156건, 등록 키워드 20개) 기준.

### 5.1 창 경계를 09시로 잡은 근거

시간대별 원본 건수:

```
0시 43 · 1시 5 · 2시 5 · 3시 6 · 4시 13 · 5시 34 · 6시 62 · 7시 70 · 8시 87
```

조간은 05~09시에 몰려 올라온다. 09시로 끊으면 그 물결을 온전히 담고 담당자 업무 시작과도 맞는다. 07시로 끊으면 조간의 절반(7·8시 157건)을 놓친다.

### 5.2 창 크기와 좁히기

| 단계 | 건수 |
|---|---|
| 00~09 원본 | 325 |
| → 제목 중복 제거 | 317 |
| → 사진기사 제외 | 308 |
| → 인사 제외 | 307 |
| → **PRIORITY_OUTLETS 13개 매체만** | **50** |

`MAX_ARTICLES_FOR_CLASSIFY`는 150이다. 307건은 못 넣고, 50건은 넉넉히 들어가면서 TOP5를 뽑기에 충분하다.

### 5.3 비용

`LLM_COST_USAGE.md` 실측(14건 입력 ~3,900토큰 / 76건 입력 ~13,200토큰)에서 **기사 1건당 약 150토큰**, 고정 오버헤드 ~1,800토큰.

| | 입력 | 출력 | 회당 | 하루 |
|---|---|---|---|---|
| 50건 · 요약 없음 · 재정리 없음 | ~9,300 | ~300 | **약 13원** | **약 13원**(1회) |

현재 하루 총액(144~396원)의 **3~9%**. 회당 단가는 실측이 아니라 위 실측의 외삽이다 — 구현 후 `response.usage`를 찍어 `LLM_COST_USAGE.md`를 갱신한다.

### 5.4 폴백 회차 판정 (§4.3의 근거)

저장된 80개 회차에서 "소제목 이름 중 한 어절(공백 없음)짜리 비율":

| 비율 | 회차 수 | 내용 |
|---|---|---|
| 0% | 68 | 정상 |
| 10~30% | 8 | `반부패정책협의회` `중앙지방협력회의` `금리정책` `예결위` — **정상 AI 이름** |
| **100%** | **4** | `2026-08-27_06-00` `2026-08-11_17-00` `2026-08-25_09-30` `2026-08-22_10-00` |

**0.3과 1.0 사이가 완전히 비어 있고**, 100%인 4개는 전부 규칙빈도 폴백 회차다(오탐 0건). 이름 단위로 거르면 정상 이름 10개를 같이 죽이므로 **회차 단위**로 판정한다.

### 5.5 어제 대비 비교가 성립하지 않는 근거

"어제 모닝 vs 오늘 모닝"을 검토했으나 기각. 저장된 모닝 회차(06:00+09:30) 12일치로 재보니 오늘 TOP5 중 어제와 이어진 쟁점은 **55개 중 2개(4%)**뿐이었다. 원인은 이름 드리프트다:

```
08-25  2위  부동산 세제개편 재검토      {부동산, 세제개편, 재검토}
08-26  2위  부동산 세제개편안 논란      {부동산, 세제개편안, 논란}   → Jaccard 0.2 < 0.4, 남남 처리
```

이름 이어쓰기 힌트(`carryover_names`)를 어제 창까지 확장하면 확률은 오르지만 **보장이 아니다**([CLAUDE.md](CLAUDE.md): best-effort). 화면이 "이어짐"을 전제하면 안 되므로 비교는 넣지 않는다.

---

## 6. 의존성 명세

### 6.1 종속 (읽는다)

| 대상 | 어떻게 |
|---|---|
| **등록 검색어 그룹** (`settings.json`) | 검색할 말. `enabled=False` 제외, `include_in_scrap`은 안 봄. **유일한 실질 종속** |
| `PRIORITY_OUTLETS` (`app/naver_api.py`) | 분류 입력 좁히기 |
| `hidden_articles.json` | `filter_hidden` |
| `live_cache.json` | 있으면 재사용(없어도 동작) |
| `ANTHROPIC_API_KEY` | 없으면 카드를 안 그림 |

### 6.2 독립 (안 읽고 안 쓴다)

- 정기 회차 파일(`data/articles/`) · 확정본 · 정기 보관함 · 초안
- 소제목 큐레이션 전역 파일 — `group_overrides` / `group_labels` / `group_order` / `custom_groups` / `preview_order`
- 수시 모니터링 카드(`data/adhoc/`) · 라벨(`labels.json`)
- 되돌리기 스택 3종 — 큐레이션 대상이 아니다
- **정책 단어 추이의 단어**(`home_trend_words.json`) — 성격이 완전히 다르다. 그건 저장된 기사 본문에 그 단어가 있나 **세는 문자열**이고(현재 `종부세`·`비거주` 2개), 검색어가 아니다. 모닝 쟁점은 등록 검색어 그룹 20개로 검색한다. (§1.3 — 다만 그 두 단어가 정기에서 빠지는 그룹에 속한다는 사실이 이 설계의 출발점이다.)
- "📃 소제목 형식" 템플릿

### 6.3 쓴다

- `data/morning_issues.json` (신규, 이 기능 전용)
- `data/api_usage.json` — 네이버 호출은 기존 `record_api_call` 경로를 그대로 탄다

---

## 7. 기각한 대안

| 안 | 기각 사유 |
|---|---|
| **롤링 창**(최근 1시간) | 기사가 계속 들어와 캐시 키가 매번 깨짐 → throttle 필요, 하루 130~275원. 날짜 간 비교 불가 |
| **하이브리드**(정기 있으면 정기, 없으면 실시간) | 같은 카드가 날마다 다른 모집단을 보여줘 담당자가 숫자를 해석할 수 없다 (사용자 판단) |
| **어제 vs 오늘 병렬 비교** | 이어진 쟁점 4% (§5.5) |
| **이름 단위 필터**(한 어절 이름 제거) | 정상 AI 이름 10개를 같이 죽인다 (§5.4) |
| **실시간 캐시를 원천으로 강제** | 캐시는 담당자가 `/live.html`을 열어야만 채워진다(`save_live_cache` 호출부 1곳) — 정기 종속을 실시간현황 방문 종속으로 옮기는 것뿐. 그래서 §3.2는 "있으면 재사용, 없으면 직접 검색" |
| **홈 렌더링 중 동기 분류** | `generate_landing_page()`는 큐레이션 핸들러 36곳에서도 불린다 → 비용이 클릭 수에 비례 |
| **규칙빈도 폴백 허용** | 그게 정확히 `이를`·`한다`다. API가 없으면 카드를 안 그리는 편이 낫다 |
| **"정기를 안 쓰는 사용자" 근거** | 그런 사용자가 구조적으로 존재 불가 (§1.4) |
| **`include_in_scrap` 구조 변경** | 저장 키라 마이그레이션이 따라온다. 지금 문제는 표현이지 구조가 아니다 (§2.3) |

---

## 8. 작업 순서

```
C  결함 셋 (4.1 → 4.2 → 4.3)   0원 — ✅ 완료 (2026-08-27)
   ↓
A  검색어 화면 문구·배지         0원 — ✅ 완료 (2026-08-27)
   ↓
B  모닝 쟁점                    하루 13원
```

**C를 먼저** 두는 이유: 4.1은 지금도 자동발송이 분류 실패를 못 걸러내는 상태라 매일 재발할 수 있다. A·B와 무관하게 가장 급하다.

**A를 B보다 먼저** 두는 이유: B는 "실시간이 상위집합"이라는 전제 위에 서는데, 지금 화면은 그 반대를 말하고 있다. 화면이 먼저 계층을 바로 말해야 B의 카드가 왜 그 원천을 쓰는지 담당자에게 설명된다. A는 0원이고 코드도 작아 B의 리스크를 안 늘린다.

---

## 9. 미결정

1. **회당 단가 13원은 외삽** — 구현 후 `response.usage`로 갱신.
2. **13개 매체가 좁은지**는 며칠 써보고 판단. 좁으면 `PRIORITY_OUTLETS`를 넓히거나 "우선순위 매체 + 부족분 채우기(최대 150)"로 바꾼다.
3. **창 경계(09:00)는 정기 회차 시간대와 무관하게 정한다.** 모닝 쟁점은 정기 스크랩의 보조가 아니라 **앱을 쓰는 사람을 위한 독립 기능**이다(사용자 확정, 2026-08-27). 특정 담당자의 회차 설정과 겹치는지는 판단 기준이 아니다 — 회차 시간대는 사람마다 다르고, 조간이 05~09시에 몰린다는 §5.1의 실측이 창을 정하는 유일한 근거다.
4. **여러 창(모닝/오후/석간)** 확장은 하지 않는다. 모닝 하나로 쓸모를 확인한 뒤 판단한다.
5. **실시간현황에 검색어 추가 진입점**(§2.3) — A의 효과를 보고 판단.

---

## 10. 열어둔 논의 (2026-08-27, 결론 안 남)

Phase A를 마친 뒤 대화에서 나온 것들. **아직 아무것도 정하지 않았다** — 다음에 이어서 볼 때 여기서부터 시작한다.

### 10.1 정책 단어 추이(`/trend`)도 같은 병을 앓고 있다

`app/trend_data.py:11`이 `list_run_dates`/`list_runs_for_date`를 읽는다 — **저장된 정기 회차 파일만** 본다. 즉 이 화면도 §1.3의 부분집합 문제를 그대로 갖는다. 실측(2026-08-27):

| 단어 | 그래프(정기 기준) | 실시간 같은 잣대 | 검색어로 직접 |
|---|---|---|---|
| 종부세 | **7건** | 25건 | 38건 |
| 비거주 | **4건** | 24건 | (검색어 아님) |

3.5~6배 과소집계다. 원인은 `세제개편` 그룹이 `include_in_scrap` 꺼져 있는 것 — 담당자가 지켜보겠다고 고른 단어가 하필 정기 관문 밖 그룹에 있었다.

**주목할 점**: `app/trend_renderer.py:281`이 이미 `정기 수집 기사의 제목·요약에…`라고 화면에 쓰고 있는데도 사용자가 "그래프는 실시간을 끌어온다"로 인식했다. **문구를 더 쓰는 것으로는 안 풀린다**는 증거다.

### 10.2 그래서 나온 방향 — "모닝 브리핑"으로 묶기

쟁점만 모닝으로 옮기는 게 아니라 단어 추이도 같은 창을 쓰게 하는 안:

```
🌅 모닝 브리핑   00:00~09:00 · 실시간 원천 · 하루 1회 확정
   ├─ 쟁점 TOP5
   └─ 지켜보는 단어 추이
```

- 정기 체크박스의 영향에서 **벗어난다** — 홈 카드와 검색어 설정 사이의 보이지 않는 연결이 끊긴다. 개념을 설명하는 것보다 연결을 없애는 쪽이 근본적이다.
- 추가 비용 0원 (모닝 산출에 이미 기사를 손에 들고 있다).
- **대가 (1) 과거가 오늘부터.** 지금 그래프도 실제 데이터는 17일치뿐이라 잃는 건 크지 않다. 다만 **모닝 창 기사(제목+요약)를 같이 저장해야** 나중에 새 단어를 골라도 과거가 소급 계산된다 — 집계값만 저장하면 그게 안 된다. 실측: **148 KB/일 → 52.8 MB/년**(참고: 지금 정기 회차가 약 129 KB/일).
- **대가 (2) 아침만 본다.** 오후에 터진 이슈를 못 잡는다. 하루 전체를 보려면 자정에 실시간 캐시를 통째로 스냅샷해야 한다(1,364건/일 → 약 124 MB/년) — 모닝과 별개의 새 작업.

### 10.3 창을 오전/오후로 쪼개는 안 (실측만 해둠)

(2)의 대가를 줄이는 방법. 분류 상한 150건 기준으로 둘 다 들어간다:

| 창 | 원본 | 필터 후 | 우선순위 13개 매체 |
|---|---|---|---|
| 모닝 00~09 | 325 | 307 | **50** |
| 오전 00~12 | 936 | 843 | **122** |
| 오후 12~24 | 428 | 409 | **50** |

※ 측정 시각이 13:5x라 오후 창은 미완성이다 — 하루가 끝나야 실제 크기를 안다. **오후 창을 확정하기 전에 하루치를 다시 재야 한다.**

창이 둘이면 비용도 2배(하루 약 26원)이고, 홈 카드가 길어진다. 정할 것: 창 1개(모닝)인가 2개(오전/오후)인가.

### 10.4 [정기]와 [수시]를 별개로 보이게 하는 문제

사용자가 제기. **수시 모니터링(`app/adhoc/*`)은 이미 완전히 별개다** — 카드마다 자기 검색어(`keywords`/`must_keywords`)를 갖고, 정기의 검색어 그룹을 안 쓰며, 정기 큐레이션 파일을 한 개도 안 건드린다(단방향 의존 규칙). 즉 데이터 구조상 이미 분리돼 있다.

**갈라져 있지 않은 것은 실시간현황 ↔ 정기 스크랩 하나뿐이고**(같은 검색어 목록 + `include_in_scrap` 플래그), 그건 의도된 설계다(1차 실시간 → 2차 선별). 문제는 구조가 아니라 그 뜻이 화면에서 안 읽히는 것 — Phase A가 1차 대응이고, 10.2가 근본 대응이다.

남은 질문: 화면상 [정기]/[수시]/[실시간] 세 갈래가 지금 홈 흐름도로 충분히 갈라져 보이는가? (판단 안 됨)

